# LOG — one line per loop run. The improve loop reads this; the human skims spend weekly.
# date | loop | item | attempts | outcome | risk | est. cost
2026-07-09 | install | — | — | kit installed via product-loop-setup; baseline pulled (north star = 0); backlog seeded 001-003 | — | —
2026-07-10 | heartbeat | dry-run | — | signals pulled live; 002 rescored 8.7; 2 questions for the human | — | ~$0 (ran inside install session)
2026-07-10 | build (in-session with human) | 002 | 1 | DONE — instrumentation sound; plan_saved double-fire root-caused & fixed (planSavedSignature dedupe, TDD, 190 assertions green); Q-a/Q-b answered → standing decisions | LOW | ~$0 (ran inside session)
2026-07-10 | heartbeat-update | 004 | — | added: surface share CTA at bloom (0 shares / 5 plans / 167 sessions); new Q-c (plan_created revisit semantics) | HIGH | —
2026-07-10 | build (sonnet-5 agent a927f15) | 004 | 1 | IN_REVIEW — verifier (fable) PASS: ~40-line diff, share prompt in sticky checkout col, handlers reused, EN+KO, tokens only, reduced-motion gated (css:1654); residual = human visual QA | HIGH | ~163k subagent tokens
2026-07-10 | build (sonnet-5 agent aa5e43e) | 001 | 1 | IN_REVIEW — verifier (fable) PASS: .mcp.json matches official docs (endpoint re-fetched independently), zero secrets, deferred criteria honestly documented; residual = org MCP enable + /mcp OAuth | HIGH | ~150k subagent tokens
2026-07-10 | operator note | — | — | .claude/ is gitignored → .claude/agents/verifier.md is local-only (works here; re-copy from product-loop-kit/agents/ on fresh clones per README step 2) | — | —
2026-07-10 | policy (human directive) | — | — | autonomy → (c) verifier PASS = auto-merge any tier; NEVER list stays human-gated; build.md step 5 updated + dirty-tree abort guard added to step 1; operator flagged visual-QA gap until 003 | — | —
2026-07-10 | heartbeat-update (human review as signal) | 005,007 | — | prod review: 004 redundant w/ footer → 005 (one image-first hub, link always travels); 007 arrival moment for recipients; image = hero artifact (standing decision) | — | —
2026-07-10 | build (sonnet-5 agent aad71cd) | 005 | 1 | IN_REVIEW — verifier (fable) PASS: single share surface, image path carries URL (canShare files / clipboard fallback), method:'image' gated on linkAttached, footer row removed, EN+KO | HIGH | ~236k subagent tokens
2026-07-10 | build (sonnet-5 agent a2710c2) | 007 | 1 | IN_REVIEW — verifier (fable) PASS: arrival banner w/ correct suppression (presets/fresh/saved-plan), plan_created chain traced, EN+KO anchored per spec, +90 lines | HIGH | ~260k subagent tokens
2026-07-10 | ship (human paste) | 001,002,004,005,007 | — | commit bce752d pushed → Vercel prod; ship-tick cron (*/15) + ship-queue mechanism installed — shipping now hands-free | — | —
2026-07-10 | heartbeat-update (human review) | 008 | — | prod review: 3 competing CTAs in checkout → hierarchy item; also 009 added (008 agent found waitlist has ZERO instrumentation) | — | —
2026-07-10 | build (sonnet-5 agent a58d483) | 008 | 1 | verifier (fable) PASS — one button-styled share CTA, link demoted to focus-ring'd text link, spacing tightened, EN+KO; QUEUED via .ship-queue (ships ≤15 min) · needs human visual spot-check | HIGH | ~170k subagent tokens
2026-07-10 | build (sonnet-5 agent a440440) | 009 | 1 | verifier (fable) PASS — waitlist_opened/waitlist_submitted wired (3 exclusive submit branches), +32 lines, found dead ctaElement code (flagged, not fixed) | LOW | ~137k subagent tokens
2026-07-10 | ship-tick failure | — | — | cron got EPERM reading ship-tick.sh — macOS quarantine on Cowork-written files; fix = xattr clear + $HOME copy of ship-tick in installer; REQUIRES one repair paste, Monday 8:00/9:00 crons also at risk until then | — | —
2026-07-10 | diagnosis (operator, after agent a8eb1bb died to API error ~207k tokens no output) | 010 | 2 | DONE — canonical lie (static canonical→root on 20,831 param URLs) + thin identical shells + dead tokens; specs/010-diagnosis.md; fixes ranked 011-015 | LOW | 3 probes
2026-07-10 | build (sonnet-5 agent a017b58) | 011 | 1 | verifier (fable) PASS — static canonical removed, canonical.js pure fn (24 assertions, wired into npm test), router sets canonical/og:url both modes, ANALYTICS_PARAMS byte-identical; QUEUED | HIGH | ~222k subagent tokens
2026-07-10 | build (sonnet-5 agent a0c6595) | 012 | 1 | verifier (fable) PASS — noindex toggle idempotent, alternatives gated by DEFAULT_MIN_TVL+isAnomalousApy, zero new fetches, EN+KO, disclosed style.css hook; QUEUED | HIGH | ~333k subagent tokens
2026-07-10 | build (sonnet-5, worktree loop-003) | 003 | 1 | SHIPPED — verifier PASS: 185→190 unit assertions + 6 new smoke assertions (360/768/1280px), sabotage check reproduced (router break → npm test red; revert → green, home.html diff empty), diff scoped to package.json/package-lock.json/test_smoke.js only; runtime ~7.7s; auto-merged per policy (c) as merge commit 3f58e8a onto 460b99f (main had advanced mid-build — ff-only not possible, ran regular no-conflict merge); explainer+quiz at specs/003-pr.md; out-of-scope finding logged (fast-xml-parser critical CVE, pre-existing, needs its own HIGH item) | HIGH | ~ (single session, no subagent build cost beyond verifier)
2026-07-10 | migration (cloud bootstrap) | — | — | loop moved to claude.ai/code routines; gitignore fixed so verifier ships to clones; specs 016/017 landed; BACKLOG synced; any Mac working-tree drafts are superseded — cloud rebuilds from specs | — | —
2026-07-11 | post-mortem (operator) | 017 | 1 | fixtures passed, product failed — acceptance measured parsing, not rendered behavior (14/14 unit fixtures green, search still bad in preview); branch push rejected on non-claude prefix; run waited unbounded on a background test job; superseded by 018 | HIGH | —
2026-07-11 | directives (human) | 016,018,019 | — | 016 shipped clean via PR #97 (#96 closed as superseded); specs 018/019 added; ship path = claude/loop-<id> branch + PR, merged by the run; UX acceptance = Playwright on real UI; 5-min foreground timebox on test runs; theme → SEO-landers to north star | — | —
2026-07-11 | build (sonnet-5) | 018 | 3 | SHIPPED — verifier PASS on 3rd round (2 findings fixed in between): round 1 caught a real regression (word-boundary check dropped entirely in protocol matching, false-matched "compare rates"->Compound/"global yields"->Balancer/etc.), round 2 caught spec's own named success metric (search_success/search_abandonment) never firing on the code path this item modifies (handleKeyDown Enter-branch) — both fixed and re-verified clean; root cause was availableProtocols only computing live protocol data once a chain/token filter was already selected, so the parser's protocol context was always empty on a user's first search (silently fell back to a static list missing e.g. Kamino) — not the parser itself, which is why 017's fixture-only tests could never have caught it; also fixed: chain matching falls back to the live allChains list (was hardcoded ~25-alias dict, missed e.g. Plasma), Lending pool-type stem-matched (catches "lenders"), protocol-only/no-chain queries default into existing "All chains" render mode. test_search.js (new): Playwright drives the real mounted app, 19/19 assertions (14 canonical + 5 negative-regression queries), each also asserting the correct analytics event fired. Sandbox network policy blocks unpkg.com+yields.llama.fi in this session — test_search.js probes first and falls back to local-vendored React/Babel + a DefiLlama-shaped fixture only when blocked, using live requests wherever reachable (disclosed, not hidden); test_smoke.js correctly left unrun (pre-existing, unrelated network dependency). A prior separate session had already attempted this item on branch claude/loop-018 and parked it BLOCKED on the identical network wall — never reached main's BACKLOG.md, so this was picked up as a fresh READY item per build.md, not a continuation; that branch's heavier full-parser-extraction approach (touching home.html) was not carried forward — this diff is app.js-only, home.html untouched. Merged as PR #99 (merge commit 15b3491); prior branch left unmerged, flagged for human cleanup decision. Measuring search_success/search_abandonment through 2026-07-25. | HIGH | ~ (single session, verifier x3 ~242k subagent tokens)
2026-07-11 | heartbeat | 020 | — | live Mixpanel pull (connector wired); 14d: 247 sessions, 50 search_success (ALL results_count=0), 4 abandon, 6 plan_created, 0 pool_view/pool_click/share/waitlist, 0 errors; keystone = 3 code-verified measurement gaps → new item 020 (LOW, 8.5, spec written); 013/014 gated behind 020; 018/019 reads flagged blind; 1 question (Q-d: internal-search vs SEO conversion path) | LOW | ~0 (triage run)
2026-07-11 | build (sonnet-5) | 019 | 3 | SHIPPED — verifier PASS on 3rd round (2 HIGH-tier explainer findings fixed in between): round 1 confirmed 6/6 acceptance criteria met on the first construction (futureValue formula verified byte-identical to planner.js, persona mapping traced to getRiskAssessment()'s existing 25/50 risk-score bands, all 9 new translation keys verified present+correct in both en/ko by executing createTranslationFunction directly, zero gradients, planner.js untouched) but FAILed on a missing required artifact — HIGH-tier items need a full explainer + 5-question quiz at specs/<id>-pr.md before merge, per NORTH_STAR.md risk policy, and it didn't exist yet; round 2 caught a real error inside that explainer once written — the base64-encoded quiz answer key marked the wrong option correct for "where does the persona come from" (encoded A, "a new planner.js classifier," when the document's own text and the code prove the answer is C, the existing risk-score bands); both fixed and re-verified clean on round 3. Also found+fixed during manual pre-verification: a reused planner.js translation key (degenHaircutNote) rendered as a literal string because it lives nested under translations.en.planner.* while PoolDetail.js's t() does a flat lookup — fixed by adding a new flat key (poolDegenHaircutNote). PoolDetail.js: honest $200/mo-over-5y mini-projection (always visible, not collapsed) with the same ⅓ degen haircut disclosure the planner uses when the pool's risk tier maps to the degen persona band, plus 30d-mean-APY/exposure/IL-risk cells added to the existing pool-info grid — no new fetches, same pool object already in hand. "Garden this pool" CTA deep-links into the planner via its existing shareable-plan URL contract (goal=retirement/pace=<persona>/monthly=200) — zero planner.js changes, since the planner is designed to never quote a single named pool's rate. CTA instrumented via the existing Analytics.trackPoolClick pattern (click_type: garden_cta). npm test's Playwright suite (test_smoke/test_canonical/test_search) hit a real-network hang against yields.llama.fi past the 5-min foreground timebox in this sandbox — killed per standing decision; substituted a mocked-network Playwright run (ad hoc, deleted after use) driving the real /?pool= route across stable/rwa/anomalous fixture pools, all passing (exactly one CTA, correct prefill params, anomaly warning visible, zero page errors). Also found a pre-existing headless-rendering anomaly (hero action-card region renders visually blank in this sandbox's screenshot tool) and ruled it out as unrelated by reproducing it identically on unmodified origin/main before reapplying this diff — flagged for a real-device check, not fixed here (out of scope). Merged as PR #101. Measuring garden_cta click-through → plan_created through 2026-07-25. | HIGH | ~ (single session, verifier x3 ~133k subagent tokens)
2026-07-11 | build (sonnet-5) | 013 | 1 | SHIPPED — verifier PASS: wrote specs/013.md from specs/010-diagnosis.md evidence (no spec existed for the top READY item). generate-sitemap.js now gates sitemap-tokens-all.xml / sitemap-chain-*.xml / sitemap-category-*.xml URLs behind the same default-rendering threshold app.js already applies (>=2 qualifying pools at DEFAULT_MIN_TVL=$10M, non-anomalous per APY_SANITY_LIMIT=1000%, both values byte-matched against app.js:729-730/2016) — closes the gap where the sitemap advertised URLs the live page renders empty by default. Chain landing pages and minTvl=/minApy= threshold pages left untouched (out of scope per spec). Verifier independently re-derived the per-combo gating logic in its own synthetic scenario (proved a token qualifying globally can still correctly fail a specific chain's combo gate) rather than trusting builder's claims, confirmed byte-match against app.js constants, ran node --check + full 190-assertion test suite itself, and confirmed the live-regen gap was pre-authorized by the spec's own Territory notes. Diff scoped to generate-sitemap.js only — no sitemap-*.xml hand-edits; real before/after URL counts land on the next scheduled sitemap-update.yml CI run (this sandbox's egress policy blocks yields.llama.fi, same wall specs/010-diagnosis.md hit, confirmed independently by both builder and verifier via 403). specs/013-pr.md HIGH-tier explainer + quiz written before merge. Concurrent heartbeat run gated 013/014 behind new item 020 (funnel-instrumentation gaps) after this build was already spec'd/built/verified — resolved on merge: 013's measurement is GSC-indexing-class based (external signal), not the Mixpanel funnel 020 fixes, so the gate doesn't retroactively apply to already-completed work; 014 (new landing pages, unbuilt) correctly stays gated. Merged as PR #103 (includes a reconciliation merge of origin/main for this bookkeeping conflict — no code conflict, generate-sitemap.js untouched by concurrent work). Measuring GSC Discovered/Crawled-not-indexed trend through 2026-07-25 (2-6wk validation cycle); proxy this week = sitemap URL count delta on next CI regen. | HIGH | ~50k subagent tokens (verifier only)
2026-07-11 | build (sonnet-5) | 020 | 1 | SHIPPED — verifier PASS on 2nd pass (1 finding fixed in between): round 1 confirmed all instrumentation correct and live-verified via Playwright but FAILed on diff size (151 lines via git diff --numstat, 1 over the LOW-tier 150-line cap; specs/020.md and 020-notes.md had understated it via a hand/grep count that missed blank lines) — trimmed comments (no logic change) to 133 lines and corrected the line-count claims, re-verified clean. search_success now carries filteredPools.length (debounced follow-up effect keyed on [filteredPools], since that state is derived via useEffect and can pass through an intermediate value before settling — a plain single-fire-on-first-change was observed to occasionally catch results_count=0 for a genuinely non-empty search, "CRV LP on Curve" flake). pool_view fires source:'url_direct' on a ?pool= deep link landing (ref-guarded against double-fire vs card click, which now sends source:'card_click'). Also found+fixed while writing the required Playwright coverage: the pool_view Playwright assertion for the url_direct path was itself racing the event (trackPoolView fires automatically as soon as the mocked pools fetch resolves, beating a post-goto page.evaluate spy install) — fixed via page.addInitScript instead. test_search.js 20/20 (2 new assertions); test_planner/test_protocol_parsing/test_qualifier_fix/test_canonical all pass; test_smoke.js hit a pre-existing sandbox chromium/playwright version mismatch unrelated to this diff (file untouched), documented per the 5-min timebox standing decision. Merged as PR #104 (merge commit e5001f9). Measuring results_count>0 share and pool_view(source=url_direct) count through 2026-07-25 — decision rule is enabling instrumentation only; the product call comes from what the now-visible funnel shows next heartbeat. | LOW | ~100k subagent tokens (verifier x2)
2026-07-11 | build (operator, in-session with human) | 015 | 1 | SHIPPED — verifier PASS (LOW): one-line planner.js fix, waitlist tweet URL apex junk-path `defi.garden/referral=<h>` → canonical `https://www.defi.garden/?ref=<encodeURIComponent(h)>` (matches canonical.js SITE_ROOT + app.js ref convention); no decode-side coupling (referralHandle is state, never read from URL); 4-file offline test chain green. Merged PR #106. GSC redirect-class trend, 2-6wk. | LOW | ~30k subagent tokens (verifier)
2026-07-11 | build (operator, in-session with human) | 014 | 1 | SHIPPED — verifier PASS (HIGH, 7/7, adversarial): new generate-token-pages.js emits self-canonical static top-100-by-TVL token pages (server-delivered title/desc/content + ?token= deep link), mirrors generate-stories.js + 013 gate; anomalous pools return before aggregation so they never inflate rank/count/content (verifier proved with a $5B-anomaly fixture + $10M/1000% boundary checks); offline --fixture path makes it verifiable where yields.llama.fi is 403-blocked. test_token_pages.js 17/17 added to chain. Phase 1 wires NOTHING (no sitemap/vercel/home.html/canonical, no tokens/ prod output committed) — phase 2 = networked real pages + sitemap wiring + canonical consolidation (/tokens/<slug> vs ?token= self-canonical) needs a human eyeball. specs/014-pr.md explainer+quiz. Merged PR #107. | HIGH | ~45k subagent tokens (verifier)
2026-07-11 | build (operator, in-session with human) | 014-threshold | 1 | SHIPPED — verifier PASS (HIGH, 7/7 adversarial): relaxed token-page eligibility per human directive — $10M→$100K floor (MIN_POOL_TVL), 2→1 min pools, 100→0 cap (no cap; --limit still caps top-N). Anomaly rail (>1000% APY) held (verifier proved a $900M @2100% pool leaves zero trace, ANOM ranks by its $2M real pool); app/planner DEFAULT_MIN_TVL + 013 sitemap gate untouched. 20 assertions. Merged PR #109. | HIGH | ~37k subagent tokens (verifier)
2026-07-11 | heartbeat-update (operator, in-session with human) | 021-026 | — | generated 6 next-improve tickets grounded in a 2026 SEO/GSC scan (reports/2026-07-11-seo-2026.md): north-star (024 share-image plan-URL truth-check, 025 garden_cta CTR review) + SEO 2026 (021 014-phase-2 sitemap+internal-links+canonical consolidation, 023 token-page content depth/internal linking, 022 IndexNow, 026 GSC Index Coverage into heartbeat). Strategy note resolves the 013-vs-newer-tokens tension: capture newer tokens via 014's rich static pages, keep the ?token= shell gate strict. | — | —
2026-07-11 | build (operator, in-session with human) | 025 | 1 | SHIPPED — verifier PASS (HIGH, 6/6): concrete prefilled projection on the garden_cta — non-anomalous pools show "Garden this pool → ~$X in 5y" (reuses 019's projectionAmount + degen haircut), deep link prefills years=5, anomalous pools keep generic CTA (trust rail: flagged rate never hyped on a button), garden_cta now sends ctaVariant/projectionYears. EN+KO. Merged PR #111. Measuring ctaVariant=concrete → plan_created through 2026-07-25. | HIGH | ~26k subagent tokens (verifier)
2026-07-11 | direction (human) | 021,024 | — | 021: KEEP BOTH /tokens/<slug> (static SEO content) and ?token= (interactive app) as distinct surfaces, each self-canonical, different content — NO canonical consolidation. 024: reframed — plan share is a unique working LINK that reproduces the sender's exact garden one-tap on open, not an image download. Both recorded as standing decisions. | — | —
2026-07-11 | diagnosis (operator, human screenshot) | 027 | 0 | Share-card generator bugs root-caused: (1) first-row icon hardcoded '🌱 ' at planner.js:2858 — per-service `icon` domain never passed to canvas; (2) label/headline mismatch — doWaitlistDownload (~1611) builds headline from the selectedSubs bundle but goalLabel from top-level `goal`, so first row said "Spotify" under a "ChatGPT Plus" headline. Human chose bundled-local-logos. Spec 027 written; NOT built here — needs browser+network (asset fetch + canvas drawImage→toBlob visual verify; a taint bug breaks ALL image shares). Queued HIGH 8.6. | HIGH | 3 probes
2026-07-11 | build (operator, in-session with human) | 023 | 1 | SHIPPED — verifier PASS (HIGH, 3/3): token pages gained a unique per-token intro (from own top pool + totals) + a Related-tokens nav of internal /tokens/<slug> links (co-chain first, self excluded) — 2026-SEO thin/orphan mitigation. Trust rails/ranking/floor/anomaly gate untouched (verifier re-proved the $900M@2100% fixture leaves no trace). 26 assertions. Merged PR #114. | HIGH | ~37k subagent tokens (verifier)
2026-07-11 | status (operator) | 021,022,024,026,027 | — | "do all pending" — 023 shipped; the rest are ENV-BLOCKED in a no-network/no-browser session, annotated in BACKLOG: 021 needs live yields.llama.fi (route via sitemap-update.yml CI); 022 needs deployed key + CI submit; 024 core round-trip already works, remainder is browser-verified UX promotion; 026 is heartbeat/GSC-connector config not repo code; 027 needs browser+network (canvas taint risk). Not blind-building unverifiable changes to SEO/share surfaces. | — | —
2026-07-11 | build (sonnet-5) | 024 | 1 | SHIPPED — verifier PASS (HIGH, 8/8): promoted the working share link (encodePlanToUrl/decodePlanFromUrl, already round-tripping full plan state per 007) to the primary .gp-share-btn CTA in the bloom share hub, demoting the image (doShare) to a text link — reverses spec 008's image-primary call per the 2026-07-11 standing decision, since only a link can close share_link_opened→plan_created. Handlers byte-for-byte unchanged, only which JSX element wears which class/label; 3 new EN+KO copy keys. Wrote specs/024.md from the backlog row + NORTH_STAR standing decision (no spec existed for the top READY item). This session's sandbox network policy turned out stricter than 021/027 assumed — unpkg.com (React's own CDN host) is blocked, not just yields.llama.fi, so a plain Playwright run can't even mount the app here. Worked around it for verification only (no shipped file touched): routed those two hosts inside the BrowserContext to local stand-ins — npm-installed react@18.3.1 UMD bundles (byte-identical to unpkg's react@18) and a small realistic pools fixture (test_planner.js's fixture shape) — so the real planner.js ran against a real DOM. 15/15 checks across both bloom archetypes, 1280/360px, light/dark, copy+native-share branches; verifier independently reproduced the same routing approach from scratch (didn't trust builder's count) and got 8/8 criteria met, including a focus-ring control check against unmodified origin/main to rule out a pre-existing Chromium-quirk false positive. Merged as PR #116. Measuring share_link_created method split (native/copy vs image) + share_link_opened→plan_created through 2026-07-25. | HIGH | ~132k subagent tokens (verifier)
2026-07-11 | build (sonnet-5) | 028 | 1 | SHIPPED — verifier PASS (LOW): token-page generator (generate-token-pages.js) now links /style.css and styles its content with the app's neumorphic --color-*/--neuro-* tokens instead of a bare inline stylesheet — neuro surface card for the pool table, press physics + focus ring on the CTA (.cta→.tp-cta) and related chips, reduced-motion honored, light/dark for free via prefers-color-scheme. RESTYLE ONLY per human choice: content (title/meta/canonical/intro/table/related nav) unchanged, no planner CTAs, no content differentiation. Zero hardcoded hex in the scoped block (new grep-style assertion added), trust-rail/ranking gate byte-identical, 27 assertions + offline chain green. Merged as PR #117 (merge commit 2c7cab0). Residual: light/dark look needs a human browser eyeball on the pulled page. NOTE: this build shipped its spec/notes/pr files but the merge omitted the BACKLOG.md/LOG.md updates — reconciled after the fact (this entry + the 028 BACKLOG row added on 2026-07-11 once origin/main was reviewed). | LOW | (external loop run; verifier cost not recorded in this session)
2026-07-11 | build (sonnet-5) | 027 | 1 | SHIPPED — verifier PASS (HIGH, 6/6): share-card canvas (renderShareImage) drew a hardcoded 🌱 sprout instead of the featured service's real logo, and doWaitlistDownload (plus the same bug shape in doShare's capital-path branch, not called out in the spec's evidence) could show a row-1 label built from a different source than the headline (reported: "Spotify" label under "ChatGPT Plus" headline). Bundled 11 official brand marks as local Path2D vector data (brand-icons.js, sourced from simple-icons + gilbarbara/logos, both MIT) — no network fetch at share time, so toBlob/canvas can never be tainted; 6 services absent from both source sets (Amazon, Hulu, Disney+, Xbox, Peacock, Walmart) fall back to their existing emoji. Headline/row-1-label/row-1-icon now derive from one "featured service" value per branch. This session's sandbox turned out to have network access 021/027 assumed it lacked (raw.githubusercontent.com + registry.npmjs.org reachable, though yields.llama.fi + unpkg.com are not) — fetched real brand SVGs+hex from simple-icons' npm tarball and gilbarbara/logos, then verified with a real Playwright/Chromium render (pools API + unpkg.com routed to local fixtures/node_modules, same workaround pattern as 024) reproducing the exact reported waitlist-download scenario plus 4 other archetype/service combos, zero draw errors. Verifier independently re-ran the same browser verification from scratch (didn't trust builder's screenshots) and got 6/6 criteria met, risk tier HIGH confirmed. Merged as PR #122. | HIGH | ~134k subagent tokens (verifier)
2026-07-11 | reconcile (build-loop pickup) | 021,029,030,031,032,033,026 | — | BACKLOG.md was stale at pickup: 021 (real /tokens/ pages via CI) merged as PR #119 but still showed READY/spec"—"; items 029-033 (pool-row links, yield quality gates x3, stale-page cleanup) shipped as PRs #120/#121/#123/#125/#126 with specs+notes+PRs on disk but never got BACKLOG/LOG rows at all — confirmed via `git log --oneline` against each spec file, no code changes. Reconciled all 6 rows to SHIPPED with PR numbers. Also corrected 026 from READY to BLOCKED: its own row text says it needs a GSC connector wired into the heartbeat routine (human/operator config, not repo code) — no build loop can close it, so leaving it READY would cause every future pickup to stall on it. Next real pickup after this: 022 (IndexNow ping, 6.5, only remaining buildable READY item). | — | — |
2026-07-11 | build (sonnet-5) | 022 | 1 | SHIPPED — verifier PASS (HIGH, 7/7, independently re-derived): new indexnow-ping.js submits sitemap-token-pages.xml's URLs to IndexNow (Bing+Yandex crawl-notification API) after CI sitemap regen, authenticated by a committed <hex>.txt key file read at runtime (never hardcoded, so key/filename can't drift). Payload construction, sitemap-URL extraction/dedup/cap (10,000 IndexNow limit), and the --dry-run/zero-URL no-network-call guarantee are all pure/unit-tested (test_indexnow.js, 10 assertions, added to npm test chain) and independently exercised by the verifier (monkey-patched https.request to prove it's never called on those paths). Real submission needs the networked CI (api.indexnow.org is 403-blocked here, same wall as yields.llama.fi) — same shape as 021's residual. Verifier overrode the builder's LOW risk guess to HIGH, correctly citing NORTH_STAR's risk policy naming .github/workflows/ changes HIGH by rule; specs/022-pr.md explainer+quiz written before merge per that tier. Full npm test was killed by the 5-min timebox on test_smoke.js — independently reproduced by the verifier as a pre-existing Playwright/Chromium build-version mismatch on a clean, unmodified origin/main (unrelated to this diff); every test in this diff's blast radius (test_planner/protocol_parsing/qualifier_fix/token_pages/indexnow) run individually and green. Also reconciled stale BACKLOG.md bookkeeping before this build started (021 already shipped as PR #119 but still showed READY; 029-033 shipped with no rows at all) via a separate small PR (#127) — not counted as this session's build item. | HIGH | ~42k subagent tokens (verifier)
2026-07-11 | build (pickup, no-op) | — | 0 | no READY items, no qualified promotions — every BACKLOG.md row is DONE/SHIPPED/superseded except 026 (correctly BLOCKED, non-repo heartbeat/cron config). Checked both promotion sources per build.md step "no READY item": specs/010-diagnosis.md's ranked fixes (011-015) are all SHIPPED; the newest report (reports/2026-07-11-seo-2026.md, tickets 021-026) is fully SHIPPED except 026. The one open thread with unbuilt product surface — signals/2026-07-11.md's keystone gap #3, "50 search_success events → 0 pool_view → 0 pool_click" (handlePoolClick confirmed wired at app.js:2343, so this is a real behavioral drop-off, not an instrumentation bug) — is already on record as Q-d, an explicit open question for the human (does internal-search→pool-card CTR need a UX fix, or is SEO-landing→pool-detail the intended conversion path instead?). Its answer changes what gets built, so per build.md step 1 ("open question whose answer changes the architecture: don't guess") it cannot be silently promoted into a spec. No other unbuilt, evidence-backed opportunity exists in specs/ or reports/. Recorded per instruction rather than inventing a low-value item to fill the slot. | — | ~0 (triage only, no code changes)
2026-07-11 | research (operator, human directive: "improve sitemaps and northstar, develop new initiatives... SOTA grade") | 039-043 | — | Broader research pass than a normal heartbeat: WebSearch across GEO/AEO, JSON-LD/rich-results, programmatic-SEO internal linking, CWV/crawl-budget, DeFi-specific SEO, and fintech CRO/referral-loop practices for 2026 + a full Explore-agent code audit of the SEO/funnel machinery, cross-checked with direct greps. Keystone finding: zero of DeFi Garden's 2,048 static SEO pages (2,045 `/tokens/<slug>` + 3 `stories/*.html`) loaded `analytics.js` — the entire recent SEO investment (014/021/023/028-033) shipped completely unmeasured, the same shape of gap 020 closed for the in-app funnel, one layer upstream. Also confirmed: zero JSON-LD/structured-data anywhere in the repo; several `app.js` analytics calls are disabled at the call site (`trackFilterChange` x4, `trackFiltersReset`, `trackFilterCombination`, `trackNavigation`, plus a live TODO on search-input analytics) with the disable reason undiagnosed; ~100 `sitemap-chain-*.xml` entries are still thin `?chain=` shells never given the 014/021 static-page treatment. Wrote product-loop-kit/reports/2026-07-11-sota-growth.md, scored and ranked 5 new items. ID COLLISION caught mid-session: drafted as 034-038, but `git fetch origin main` revealed a concurrent build-loop session had just merged PR #130 under ID 034 for an unrelated fix — renumbered the whole batch to 039-043 (confirmed free on origin/main) before branching, avoiding a spec/BACKLOG collision. | — | ~175k subagent tokens (Explore audit) + WebSearch
2026-07-11 | build (sonnet-5) | 039 | 1 | SHIPPED — verifier PASS (HIGH, 6/6, independently re-derived): wired the exact home.html/plan.html Mixpanel + analytics.js pipeline into generate-token-pages.js and generate-stories.js via a shared `renderAnalyticsBootstrap()` helper, firing `page_view` with `page_type`/`token`(or `persona`) on `window.load` for every generated static page — previously these 2,048 pages had zero `<script>` tags of any kind. The Mixpanel stub is reproduced via `String.raw` specifically to avoid a subtle bug: a normal template literal would silently corrupt the snippet's own `/^\/\//` regex literal via backslash-escape processing; both builder and verifier independently confirmed byte-identity against home.html programmatically (not by eyeballing). Deliberately left outbound click-tracking out of scope — `analytics.js`'s existing `getBaseContext()` already stamps `referrer` on every event and this site's `Referrer-Policy: strict-origin-when-cross-origin` sends the full same-origin path, so token-page → app click-through is already measurable via `referrer` filtering with zero new code, verified rather than assumed. test_token_pages.js +5 assertions (41 total), new test_stories.js (12 assertions, added to the npm test chain) — both exercise real generated-string content, not tautological. Verifier independently regenerated fixture pages from both pre- and post-diff generator code and diffed them, confirming the change is purely additive (only the analytics script block inserted; title/canonical/meta/pool-table/related-nav byte-identical otherwise); confirmed no touch to trust rails, canonical/ranking/sitemap logic, or hand-edited generated output. test_smoke.js/test_canonical.js/test_search.js cannot run in this sandbox (`playwright` never installed — `node_modules/` absent entirely, confirmed pre-existing on origin/main before this diff, unrelated to this diff's blast radius). specs/039-pr.md HIGH-tier explainer + quiz written before merge. Merged as PR #131. 040 (JSON-LD structured data) and 041-043 (chain landing pages, disabled-analytics audit, semantic clustering) left spec'd/logged for future pickup — not overloading one session. | HIGH | ~80k subagent tokens (verifier)
2026-07-12 | build (sonnet-5) | 040 | 1 | SHIPPED — verifier PASS (HIGH, 7/7, independently re-derived): added JSON-LD structured data where the repo had zero — sitewide Organization+WebSite (home.html, static, both modes), BreadcrumbList (every generated /tokens/<slug> page + PoolDetail.js's pool-detail view, additive-only alongside the untouched visual breadcrumb), FAQPage on stories/kevin.html (mainEntity built directly from the same p.faq array the visible Q&A block renders, so schema can never diverge from displayed content — tomoko/lucia correctly emit none, no faq array). Judgment call: the "Tokens" breadcrumb crumb omits its `item` URL since no /tokens hub page exists anywhere in the repo (confirmed via vercel.json + grep) — an unlinked schema.org ListItem is valid and honest; linking a 404 would be worse. All dynamic JSON-LD is JSON.stringify'd then `<`→`<` escaped to block a `</script>` breakout from a malicious token symbol — verified with a live XSS-shaped fixture in test_token_pages.js. This sandbox's network policy blocks both unpkg.com (React's CDN) and yields.llama.fi (pools API) outright (confirmed via curl + the proxy status endpoint's recentRelayFailures) — the pre-existing bare-/ and ?token=USDC Playwright checks (from spec 003, untouched by this diff) fail identically for the same reason, so PoolDetail.js's live-DOM render could not be independently confirmed in-session; every other surface (home.html static check, token-page/story-page generator fixtures) verified clean via node unit tests (44 + 15 new assertions). test_smoke.js also got a pre-existing-pattern fix (pin chromium.launch() to the sandboxed executable, mirroring test_search.js) just to get the smoke gate running at all after a fresh npm install. specs/040-pr.md HIGH-tier explainer + quiz written before merge. Merged as PR #133 (squash). Flagged "needs human visual/render spot-check" per NORTH_STAR's render-path-merge advisory. | HIGH | ~51k subagent tokens (verifier)
2026-07-12 | build (sonnet-5) | 042 | 1 | SHIPPED — verifier PASS (LOW, 5/5, independently re-derived): re-enabled 7 Analytics.* calls in app.js disabled since a 2025-08-21 render-crash hotfix (trackFilterChange x4, trackFiltersReset, trackFilterCombination, trackNavigation). Started as a no-READY-item pickup — every BACKLOG row was DONE/SHIPPED/BLOCKED except 041-043 (unbuilt, needing promotion per build.md's fallback path); unshallowed this session's shallow clone (`git fetch --unshallow`) to root-cause 042's disable history (the report's own precondition for promoting it), tracing it to three same-day commits (a9b8550 disabled all analytics after a real 'Cannot read properties of undefined' crash; adc8fd73 re-enabled the safe ones same day, explicitly naming "complex filter combination tracking that caused crashes" and "problematic real-time search input tracking" as why those stayed off; eff82dda fixed a syntax slip). Confirmed every current call site now passes a well-formed object/string literal (never the possibly-undefined value that caused the 2025 crash) — corroborated by an already-live canary (Analytics.getFiltersActiveCount runs on the identical object one line above the disabled trackFilterCombination call, proving it never throws). Live Playwright verification (local-vendored-React + fixture-pools routing, same pattern as test_search.js, since unpkg.com/yields.llama.fi are sandbox-blocked) drove the real UI through TVL/protocol filter changes, filter combination, filters reset, and pool-detail back navigation — 6 of 7 fired the expected Mixpanel event, zero page/console errors. The 7th (trackFilterChange('pool_type',...) inside handlePoolTypeToggle) turned out to be pre-existing unreachable dead code — the live pool-type UI (google-nav-tab buttons) calls setSelectedPoolTypes directly, bypassing that handler entirely — documented in specs/042-notes.md rather than silently fixed (out of scope) or left unexplained. The two stale "TODO: Re-enable" search-input comments were confirmed superseded by already-shipped completion-based trackSearchSuccess/trackSearchAbandonment (live per signals/2026-07-11.md) — left untouched, not a real gap. Diff: app.js only, 7 one-line uncomments, well under the LOW-tier 150-line cap. test_smoke.js fails identically on this branch and on unmodified origin/main (pre-existing sandbox network block, not a regression); test_search.js 20/20, test_canonical.js 24/24, test_planner.js 190/190, test_token_pages.js 44/44, test_indexnow.js 10/10, test_stories.js 15/15 all green — verifier independently re-ran all of these plus its own from-scratch Playwright script. Merged as PR #135. | LOW | ~97k subagent tokens (verifier)
2026-07-12 | heartbeat | 044 | — | live Mixpanel pull; 14d: 326 sessions, 100 search_success, 6 plan_created, 3 pool_view (all card_click), 0 errors; north star still structurally 0 (share events absent from lexicon). 020 CONFIRMED LIVE — results_count real (22/100 non-zero, 1-34 pools), keystone gap #1 closed, 018 partial honest read (searches find pools). New keystone: `/tokens/*` page_view=0 + `?pool=` url_direct pool_view=0 despite 039/020 shipped → can't tell untrafficked from unwired (fixture-asserted only) → new item 044 (LOW, 7.0, spec written). 018/019/020/039 evidence refreshed. Q-d retired (answered by 07-11 standing decision); new Q-e (wire GSC connector vs keep building SEO surface). No experiment window ended; nothing closed to LEARNINGS. | LOW | ~0 (triage run)
2026-07-12 | build (sonnet-5) | 041 | 1 | SHIPPED — verifier PASS (HIGH, 16/16, independently re-derived): new generate-chain-pages.js extends the proven 014/021 static-landing-page pattern from tokens to chains — real, server-delivered /chains/<slug>.html pages (title/description/self-canonical/content) replacing the thin ?chain= JS shells the 111 pre-existing sitemap-chain-*.xml files pointed at (largest remaining unaddressed category from specs/010-diagnosis.md). Reuses generate-token-pages.js's exported trust-rail gate (isQualifyingPool — same $100K floor + APY_SANITY_LIMIT anomaly exclusion, one source of truth) and formatting/analytics helpers instead of duplicating them (one escapeHtml export added, additive-only, test_token_pages.js unaffected at 44/44). Shipped generator + CI/sitemap-index wiring in ONE PR — unlike 014/021's original two-PR phase split — since 021 already proved the full pattern end-to-end for tokens, leaving no exploratory risk to de-risk by splitting. New test_chain_pages.js (39 assertions, new fixture) exercises every gate branch independently: sub-floor exclusion, anomaly exclusion (content/count/TVL), 0%-rounding, top-8 truncation, slug safety (space→dash on "Multi Chain"), BreadcrumbList shape + XSS-safety, related-chains co-token ordering, analytics bootstrap. Verifier independently re-ran all tests, confirmed the sitemap-index guard is a byte-structural mirror of the already-proven token block, and validated the CI workflow YAML. specs/041-pr.md HIGH-tier explainer + quiz written before merge. Merged as PR #136 (squash). LIVE-VERIFIED same session: the first sitemap-update.yml CI run after merge (triggered automatically via the push.paths match) generated 86 real /chains/<slug> pages from live DefiLlama data and wired sitemap-chain-pages.xml into the sitemap index — confirmed by inspecting the resulting commit on main, not just a fixture run. This sandbox itself cannot reach yields.llama.fi (same wall every prior SEO-generator item hit); test_smoke.js/test_search.js fail with pre-existing MODULE_NOT_FOUND (playwright/node_modules absent), unrelated to this diff (neither file touched). Picked up via the no-READY-item promotion path (build.md fallback): 041 was the highest-scored (7.5 vs 043's 6.0) unbuilt, evidence-backed item already logged with rationale in reports/2026-07-11-sota-growth.md, explicitly flagged there as "promote to spec'd/READY on a future pass" — this was that pass. | HIGH | ~76k subagent tokens (verifier)
2026-07-12 | heartbeat-refill (interactive, human directive: "refill the backlog with fresh ideas... ONLY SOTA grade SEO for 2026") | 045-051 | — | SEO-only backlog refill grounded in a grep audit of the CURRENT shipped state (post-040/041/042) + 2026 SOTA sources (WebSearch + prior 07-11 research). Confirmed-present gaps (not guessed): (A) the entire SEO surface is ORPHANED — home.html links to /tokens//chains/ 0×, no hub/index pages exist (2,108 spokes, 2,022 tokens + 86 chains, no crawl path from /); (C) the pool tables carry zero structured data (only BreadcrumbList/ListItem/WebSite/Organization present — no ItemList, no Dataset; 040 deferred it); (D) no GEO/AEO answer content or FAQPage on token/chain pages (only kevin story has one); (E) no freshness signal despite daily CI regen; (F) token pages link to chain pages 0×; (G) 0 hreflang on 2,108 pages though ko is a first-class app language; (H) one shared og-image.png. Ranked 7 SEO-only items 045-051, spec'd the top 3 (045 de-orphan/hubs 8.6, 046 ItemList/Dataset 7.8, 047 GEO answer+FAQ 7.5). 045 is the keystone — orphaning is the structural root of the original 010 GSC "not indexed" crisis, one level up; every already-shipped SEO page is optimizing content Google may never reach until there's a crawl path from home. Report: reports/2026-07-12-sota-seo.md. Noted 049 folds in/supersedes the pre-existing 043 — reconcile next heartbeat. No build this run (refill only, per the ask). | — | ~0 (research/triage) + 1 WebSearch
2026-07-12 | spec-out (interactive, human: "make the spec for logged ones as well") | 048-051 | — | Wrote full verifier-checkable specs for the four previously logged-only SEO items: 048 (freshness signals — visible "Last updated" + dateModified schema; generation date already computed for sitemap lastmod, just not surfaced), 049 (cross-surface internal linking token↔chain↔category — SUPERSEDES 043, which is its category leg; grep-confirmed token→chain links = 0; hard invariant = no links to un-generated slugs → no soft-404s), 050 (hreflang + Korean static pages — ko is a first-class app lang via translations.js; largest item, ~2× page count; one open architecture Q = /ko/ subdirectory vs ?lang=ko, don't guess if subdirectory needs vercel.json routing), 051 (per-page dynamic OG images — reuse 027's canvas/brand-logo work; trust rail = card APY must be the gated best APY, never anomalous). All four flipped READY in BACKLOG; 043 marked SUPERSEDED by 049. Backlog now has 8 READY SEO items (044 + 045-051). No build this run (spec-out only). | — | ~0 (spec authoring)
2026-07-12 | build (sonnet-5) | 045 | 1 | SHIPPED — verifier PASS (HIGH, 6/6, independently re-derived): built the missing crawl path from `/` into the 2,108-page static SEO surface (2,022 `/tokens/<slug>` + 86 `/chains/<slug>`), which had zero internal links from home.html — the structural root of the 010 GSC "not indexed" crisis, one level up. New `tokens/index.html` (top-60-by-TVL direct + 26 A–Z sub-hub pages at `/tokens/az/<letter>` so every token stays ≤3 clicks from `/`) and `chains/index.html` (all ~86 chains linked flat — small enough for one page). `home.html` gets one additive `<footer class="seo-hub-links">` with static (pre-JS) anchors to `/tokens`/`/chains`, placed after both `#root` and `#planner-root`, present in both router modes, router/canonical.js logic byte-identical otherwise. `renderTokenSitemap`/`renderChainSitemap` gained an optional `extraLocs` param (backward compatible) so hub + A–Z URLs ride along in the existing `sitemap-token-pages.xml`/`sitemap-chain-pages.xml`, discoverable via sitemap.xml's existing index chain. Deviated from the spec's literal "/tokens/" wording to canonicalize without a trailing slash, matching vercel.json's `trailingSlash:false` and every other URL on the site (documented in specs/045-notes.md). Real `tokens/`/`chains/` directories not regenerated in-session (yields.llama.fi network-blocked, confirmed via curl 403) — land on the next sitemap-update.yml CI run after merge, same mechanism 013/021/041 used. New test_hub_pages.js (22 assertions, added to npm test chain) plus the existing test_token_pages.js/test_chain_pages.js/test_canonical.js all green; test_smoke.js reproduced failing identically pre- and post-diff via git stash (pre-existing sandbox network block, not a regression). specs/045-pr.md HIGH-tier explainer + quiz written before merge. Measurement: GSC indexing-class drain is the real signal (2-6wk cycle, needs 026); proxy = hub/A–Z `page_view` counts once organic traffic arrives. | HIGH | ~58k subagent tokens (verifier)
2026-07-12 | build (sonnet-5) | 046 | 1 | SHIPPED — verifier PASS (HIGH, 4/4, independently re-derived): added content-matched `ItemList` + `Dataset` JSON-LD to every generated `/tokens/<slug>` and `/chains/<slug>` page — the pages' core ranked-pool table carried zero structured data before this (040 shipped Organization/WebSite/BreadcrumbList/FAQPage, deliberately deferred Product/FinancialProduct for pools and left ItemList/Dataset unaddressed). New shared `poolHrefFor` helper is used by BOTH the visible `<tr>` row rendering and the new `renderItemListJsonLd`, so the schema cannot drift from the visible table by construction, not just by test coverage — verifier independently regenerated pre/post fixture HTML and confirmed the only diff is the two new `<script type="application/ld+json">` blocks. `Dataset.dateModified` takes a new optional `generatedDate` param on `renderTokenPage`/`renderChainPage` (defaults to CI generation date; 048's freshness work can wire its own timestamp through the same param later). New ItemList/Dataset test sections in test_token_pages.js (51/51) and test_chain_pages.js (45/45): item-count/order/url match against rendered rows, XSS script-breakout guard, record-specific content, additive-only diff check. NORTH_STAR canonical test command (190 assertions) + test_canonical.js/test_hub_pages.js/test_indexnow.js/test_stories.js all green. test_smoke.js/test_search.js pre-existing, diff-unrelated sandbox failures (yields.llama.fi network-blocked; one Playwright browser-lifecycle flake) — same documented precedent as 040/045. specs/046-pr.md HIGH-tier explainer + quiz written before merge. Merged as PR #141 (squash). No isolable Mixpanel funnel metric (structured data affects search/AI-citation behavior, not a trackable event) — same disclosure pattern as 040; proxy is GSC Enhancements once 026 unblocks. | HIGH | ~66k subagent tokens (verifier)
2026-07-12 | build (sonnet-5) | 047 | 1 | SHIPPED — verifier PASS (HIGH, 5/5, independently re-derived): added a data-derived direct-answer opener (2 sentences, right after `<h1>`, before the pool table — answers "what's the highest honest `<TOKEN>`/`<CHAIN>` yield right now") and a 3-item `FAQPage` (visible Q&A + matching JSON-LD, `mainEntity` byte-for-byte the visible text — the 040 kevin invariant) to every generated `/tokens/<slug>` and `/chains/<slug>` page; only the kevin story page had FAQPage before this. New shared `buildAnswerAndFaq` (exported from generate-token-pages.js, reused by generate-chain-pages.js per the reuse-before-inventing directive) reads only the SAME already-gated `rec.pools[0]`/`rec.qualifyingCount`/`rec.totalTvl` the visible table uses — never raw pool data — so an anomalous (>1000% APY) or sub-$100K-TVL pool structurally cannot reach the answer or FAQ; verified against the ANOM/Anom fixtures (real $2M pool + excluded $900M@2100% pool leaves zero trace in either block). Verifier's first pass FAILed on a missing artifact only (specs/047-pr.md, the HIGH-tier explainer+quiz NORTH_STAR requires before merge, precedented by 046-pr.md) — no code defect found; added the file and the verifier PASSed on re-review, confirming no product code changed between the two passes. New "047" test sections in test_token_pages.js (59/59) and test_chain_pages.js (53/53): placement, content correctness, FAQ↔JSON-LD byte-match, trust-rail exclusion via the anomaly fixtures, per-record content variance, XSS-safety, additive-only diff. NORTH_STAR canonical test command (190 assertions) also green, no regression. test_smoke.js/test_search.js pre-existing, diff-unrelated sandbox failures (no node_modules/playwright installed, yields.llama.fi network-blocked) — same documented precedent as 040/045/046. specs/047-pr.md HIGH-tier explainer + quiz written before merge. Merged as PR #143 (squash). No isolable Mixpanel funnel metric (structured data + content affects search/AI-citation behavior, not a trackable event) — same disclosure pattern as 040/046; proxy is GSC impressions/clicks + AI-citation appearances once 026 unblocks, 2-6wk GSC cycle. | HIGH | ~161k subagent tokens (verifier, 2 passes)
2026-07-12 | build (sonnet-5) | 044 | 1 | SHIPPED — verifier PASS (HIGH, 5/5, independently re-derived, risk re-tiered from builder's LOW guess): spec asked whether 039/020's SEO-surface analytics instrumentation actually fires on a live render, since both shipped verifier-PASS on fixture-string assertions only and 14d Mixpanel showed `/tokens/*` page_view=0 + `pool_view source=url_direct`=0. Built new test_analytics_fires.js (real Chromium load of a generated `/tokens/<slug>` page, intercepts `Analytics.track`) — it failed against the unmodified generator: zero events, a genuine wiring bug, not a traffic gap. Root cause: `renderAnalyticsBootstrap()`'s guard `if (window.Analytics && ...)` was always false because `analytics.js` declares `const Analytics = {...}` at the top of a classic (non-module) script — top-level `const` never becomes a `window` property, only a same-scope bare identifier visible to later classic `<script>` tags (confirmed empirically by both builder and verifier with a minimal 2-script-tag fixture in the repo's own Chromium). `app.js` calls `Analytics.trackPageView` unguarded (bare identifier), which is why the app's own page views tracked fine — the bug only lived in the generator-injected bootstrap's defensive check, silently zeroing page_view on all ~2,133 generated `/tokens/*`+`/chains/*` pages regardless of traffic. Fixed the one-line guard in generate-token-pages.js and generate-stories.js (`typeof Analytics !== 'undefined'`); generate-chain-pages.js imports the same function so chain pages fixed transitively. `pool_view(source=url_direct)` was already live-render-verified sound by an existing test_search.js check from 020 — no fix needed there, reran standalone and still green (20/20). Verifier independently reproduced the fail→pass transition via git show against origin/main's pre-fix generator, reran all touched/adjacent test suites (test_token_pages 59/59, test_chain_pages 53/53, test_stories 15/15, test_search 20/20), and confirmed test_smoke.js's failure is pre-existing (unpkg.com egress blocked in-sandbox, identical on unmodified main via git worktree). Diff confined to 2 generator one-liners + new test file + package.json test-chain wiring — no trust rails, no `?pool=`/router changes. specs/044-pr.md HIGH-tier explainer + quiz written before merge. Merged as PR #145 (squash). Residual (documented, out of scope): real `tokens/*.html`/`chains/*.html` (~2,133 files) still carry the pre-fix guard until `.github/workflows/sitemap-update.yml`'s next run (triggers on push-to-main touching these generator files, network-blocked in-sandbox); `stories/*.html` (3 files) predates 039 entirely and has no CI regen path — needs a manual `node generate-stories.js` run with network access. | HIGH | ~55k subagent tokens (verifier)
2026-07-12 | build (sonnet-5) | 048 | 1 | SHIPPED — verifier PASS (HIGH, 5/5, independently re-derived; risk re-tiered from builder's LOW→MED guess): grep-confirmed 0 freshness signals across `tokens/*.html`/`chains/*.html` despite daily `sitemap-update.yml` CI regen. Added `todayGeneratedDate()` + `renderLastUpdatedHtml()` to generate-token-pages.js (reused by generate-chain-pages.js), rendering a visible "Last updated <date>" `.note` line on every token/chain page, fed from the SAME `genDate` variable already wired into 046's `Dataset.dateModified` — the two are structurally incapable of drifting apart (single variable, not two independent computations), satisfying the spec's byte-for-byte match criterion. `main()` in both generators now computes one date per run so a full daily regen can't disagree with itself across a midnight rollover. generate-stories.js's 3 persona pages already computed a human-readable `generatedDate` once per run (used in narrative sentences) but exposed no freshness signal or schema; added an explicit "Last updated" footer line + a `WebPage` JSON-LD node (`dateModified`) emitted for every persona (not just kevin, the only one with a pre-existing FAQPage). Deliberately scoped OUT the `/tokens`, `/tokens/az/<letter>`, `/chains` hub/index pages — navigational indexes, not their own dataset, mirroring 046's precedent of never touching hub pages with Dataset/ItemList JSON-LD either. New "048" test sections in test_token_pages.js (66/66), test_chain_pages.js (60/60), test_stories.js (21/21): visible-line presence + byte-for-byte match to the schema field, both explicit-date and production-default cases. Live-verified (not fixture-only): ran both generators against the repo's real fixture into a scratch dir, confirmed actual emitted HTML shows `Last updated July 12, 2026` and `"dateModified":"July 12, 2026"` identically, then discarded the scratch output. Verifier independently ran all three test files (not trusted from notes), confirmed the diff was confined to exactly the 3 generators + 3 test files + notes + a BACKLOG.md line, and re-tiered risk to HIGH per NORTH_STAR's "generated SEO surface leans HIGH" default (same territory as 045/046/047, all HIGH). test_smoke.js fails on its two live-`yields.llama.fi`-dependent cases only — pre-existing sandbox network block, confirmed unrelated (diff touches none of home.html/planner.js/app.js/PoolDetail.js), same documented precedent as every prior SEO-generator item this session. specs/048-pr.md HIGH-tier explainer + quiz written before merge. Flagged "needs human visual spot-check" (render-path merge, backlog 003's smoke gate doesn't cover static-page generators). Not Mixpanel-isolable (proxy = GSC freshness/re-crawl cadence + AI-citation recency once 026 unblocks). | HIGH | ~56k subagent tokens (verifier)
2026-07-12 | build (sonnet-5) | 049 | 1 | SHIPPED — verifier PASS (HIGH, 5/5, independently re-derived): token pages (`/tokens/<slug>`) now link to the chains their pools trade on (`chainLinksFor`), chain pages (`/chains/<slug>`) link back to their top on-chain tokens (`topTokensOnChain`) — both filtered against the REAL generated-slug set computed from the same run's live pool data, so no cross-link can point at an ungenerated page (soft-404 risk, the exact GSC class 011-013 fought). A third leg (`categoryLinksFor`, reusing generate-sitemap.js's existing `getPoolType` classifier) folds in the separately-proposed 043 by linking each page's distinct pool-type categories to the live `?token=/chain=&poolTypes=<cat>` app view — the spec's own builder's-call interim target, since no category hub page exists yet (045 only shipped token/chain hubs). Uncovered and fixed a latent prerequisite bug along the way: generate-token-pages.js had `module.exports` assigned AFTER its `require.main()` invocation — harmless before, but 049's token->chain direction needs a lazy `require('./generate-chain-pages.js')` inside `main()`, which requires this module right back; with the old ordering that circular require observed `module.exports` still at its default `{}` (`isQualifyingPool is not a function`). Fixed via a 4-line reorder, verified with a from-scratch repro before touching the real files. New cross-link nav markup reuses `.related`/`.related-links` CSS via an added class token (`class="related xlink-chains"` etc.) so the pre-existing exact-`class="related"` tests keep passing untouched. New "049" test sections in test_token_pages.js (71/71) and test_chain_pages.js (63/63): dedupe/cap, exclusion-outside-generated-set, and a no-dead-link invariant checked against the REAL cross-fixture ranking from the sibling module (not hand-picked fixtures) — the verifier ran these itself plus test_canonical/test_hub_pages/test_search/test_indexnow/test_stories/test_analytics_fires, all green. test_smoke.js skipped (documented pre-existing sandbox network block on unpkg.com/yields.llama.fi, unrelated — diff never touches home.html/app.js/planner.js). 043 reconciled (already marked superseded-by-049 in BACKLOG.md). specs/049-pr.md HIGH-tier explainer + quiz written before merge. Merged as PR #148 (squash). Not Mixpanel-isolable (structural crawl-graph change, same disclosure pattern as 040/045-048); proxy is GSC indexation/authority flow once 026 unblocks, real `/tokens/`+`/chains/` regen with the new cross-links lands on the next sitemap-update.yml CI run. | HIGH | ~57k subagent tokens (verifier)

2026-07-12 | build (sonnet-5) | 050 | 1 | SHIPPED — verifier PASS (HIGH, 5/5, independently re-derived; risk re-tiered from the builder's "possible vercel.json touch" reasoning, which didn't materialize, to the actual trigger: this diff touches .github/workflows/sitemap-update.yml, NORTH_STAR's unconditional config/infra HIGH rule): generates a Korean counterpart of every static SEO page (/tokens/<slug>, /chains/<slug>, hubs, A–Z sub-hubs) at /ko/tokens/<slug> and /ko/chains/<slug>, with reciprocal hreflang (en/ko/x-default) and self-canonical URLs. Resolved the spec's one open question (URL scheme) without asking: the spec's own "?lang=ko, no routing change" fallback turns out to be a non-option for these specific pages — they're pre-generated static files, and a query string can't select different static content on Vercel — so went with the subdirectory scheme, then confirmed it needs zero vercel.json change anyway (cleanUrls:true is unscoped and already resolves nested paths like /tokens/az/<letter> for free). Copy sourced from a new ~60-key tcp* section added to translations.js's existing en/ko catalog (Node-requireable via the pre-existing createTranslationFunction), keyed off raw counts rather than pre-pluralized English word fragments so Korean's no-plural-inflection grammar doesn't leak an English "pool"/"pools" into a Korean sentence. Pool data/rankings/trust-rail gating are structurally identical between language variants — both renderers take the SAME `rec` object, only the `lang` argument differs. Self-review caught and fixed three real issues before verification: (1) $100K/1000% sanity-rail figures were first localized into Korean numeral notation ($10만), reverted to literal en-US text per CLAUDE.md; (2) a koOutDir path bug where path.resolve('ko', args.out) silently discards 'ko' whenever --out is absolute, fixed to always resolve as a sibling of outDir; (3) two shared helpers (renderFaqBlockHtml's FAQ heading, renderItemListJsonLd's "X on Y" JSON-LD name) hardcoded English that the per-page translation pass missed, both now lang-aware. New test_i18n_pages.js (19 assertions: hreflang reciprocity, ko-copy-from-catalog with a 21-phrase English-leak denylist, en/ko pool-parity, sitemap ko coverage), wired into npm test. Verifier independently ran the full suite (test_i18n_pages 19/19, test_token_pages 71/71, test_chain_pages 63/63, test_hub_pages 22/22, test_planner 190/190, test_canonical 24/24, test_stories 21/21, test_analytics_fires 1/1 — all byte-identical to origin/main for the untouched test files, confirming zero regression) plus a manual fixture-generation eyeball pass on the actual ko HTML output (not just test assertions), and confirmed vercel.json is genuinely untouched (git diff empty). test_smoke.js fails identically pre/post-diff (yields.llama.fi network-blocked, documented precedent since 040/044/045). specs/050-pr.md HIGH-tier explainer + quiz written before merge. Merged as PR #150 (squash). Real ~2,100 ko/tokens + ko/chains pages + sitemap-token-pages-ko.xml/sitemap-chain-pages-ko.xml land on the next sitemap-update.yml CI run (network-blocked in-sandbox, same mechanism 021/041/045 used); measurement is GSC Korean-query impressions/clicks + hreflang validity once 026 unblocks, 2-6wk+ window. | HIGH | ~123k subagent tokens (verifier)
2026-07-12 | build (sonnet-5) | 051 | 1 | SHIPPED — verifier PASS (HIGH, 4/4, independently re-derived; risk re-tiered from the builder's MED guess to HIGH per NORTH_STAR's explicit "new dependencies" trigger): every /tokens/<slug> and /chains/<slug> page shipped the one shared og-image.png; added generate-og-images.js (new, @napi-rs/canvas — headless native Canvas-2D, no browser, prebuilt linux-x64-gnu binary matches ubuntu-latest CI) rendering a unique 1200x630 card per page (name, best gated APY, pool count), wired into generate-token-pages.js/generate-chain-pages.js's main() before their HTML write so a per-record render/write failure can fall back to the shared image in that same page's own meta tags — never a two-pass reconciliation. Card APY is Math.max(...rec.pools.map(poolTotalApy)), the identical expression the page's own headline APY already uses on the already-gated (non-anomalous, >=$100K TVL) rec.pools slice, so an anomalous/sub-floor pool structurally cannot reach a card — verified against the ANOM fixture ($900M/2100% + $2M/6%) both programmatically and by visually inspecting the rendered PNG (shows 6.00%, no trace of 2100%). One image per slug, shared across en/ko (card data doesn't vary by language). First render surfaced a real bug: the 🌱 emoji rendered as a tofu box (headless canvas has no bundled color-emoji font) — replaced with a plain vector dot, re-rendered and visually confirmed the fix before shipping. Also set twitter:title/twitter:description (previously omitted on token/chain pages) and added stale-PNG cleanup mirroring 031's .html cleanup. New test_og_images.js (18 assertions: real PNG bytes/dimensions, path convention, anomaly exclusion, generation+cleanup+fallback, a <5s CI-timebox sanity check, and per-page meta-tag wiring for both token and chain templates including the hub-page/no-map fallback cases), wired into npm test. Verifier independently ran test_og_images.js (18/18), test_token_pages.js (71/71), test_chain_pages.js (63/63), confirmed test_smoke.js's failure is pre-existing/unrelated (diff never touches app.js/home.html/PoolDetail.js), confirmed @napi-rs/canvas installed with the CI-matching prebuilt binary, and confirmed no PNGs were actually committed in this diff (only generator code — images land on the next CI run, consistent with every prior SEO-generator item). specs/051-pr.md HIGH-tier explainer + quiz written before merge. Real og/tokens/*.png + og/chains/*.png land on the next sitemap-update.yml CI run (yields.llama.fi network-blocked in-sandbox, same mechanism as every prior SEO-surface item). Measurement: social/SERP CTR proxy (referral + GSC image-search once 026 unblocks), 2-6wk+ window; not Mixpanel-isolable. | HIGH | ~78k subagent tokens (verifier)
2026-07-12 | heartbeat-update (operator, human PageSpeed request) | 052-057 | — | PageSpeed/CWV audit (PSI API 429-rate-limited → derived from repo assets): dominant issue = in-browser Babel transpile (~2.9MB @babel/standalone + main-thread transpile of app.js 119K/PoolDetail 52K on every analytics/SEO page); + zero minification (~557K raw), no preconnect (0 tags), no static-asset cache headers, og-image 280K. Refilled backlog 052-057 (perf wave), specs for 052/053; report reports/2026-07-12-pagespeed.md. Already-good noted (font-display swap, non-blocking CSS, planner path avoids babel). Framing: CI-compiled/minified committed artifacts fit the existing generator pattern, not a local build step. | — | —
2026-07-12 | build (sonnet-5) | 052 | 1 | SHIPPED — verifier PASS (HIGH, 5/5, independently re-derived; risk confirmed HIGH per NORTH_STAR's home.html/new-dependencies/config-infra triggers, all three present at once): dropped the in-browser text/babel + @babel/standalone (~2.9MB unpkg) transpile pipeline that ran on every analytics/SEO page load (?token=/?chain=/?pool=/every /tokens/->app link) — the dominant mobile PageSpeed hit per reports/2026-07-12-pagespeed.md. First attempt tried the spec's suggested reduced path (app.js/PoolDetail.js use React.createElement exclusively, no JSX — grep-confirmed zero matches — so just drop Babel, load as plain <script> tags, no compile step). test_search.js (offline, vendors React locally via page.route, exercises the real mounted analytics app) caught this was wrong: two sibling <script> tags sharing one global lexical scope both declare top-level `const { useState, ... } = React`, a SyntaxError when loaded raw; separately app.js's autocompleteTokens useMemo references allAvailableChains (both in its body and its own dependency array) before that const's own declaration later in the same function — a TDZ ReferenceError with real let/const semantics. Babel's default transform was silently downleveling every let/const to var this whole time (no TDZ, redeclaration is a no-op), masking both. Confirmed transform-dependent (not pre-existing) by reverting to the Babel-active baseline in the same sandbox and re-running test_search.js clean (20/20) before making any further change. Given a 3,140-line file could hide more of the same bug class, reverted the hand-var-patch attempt and shipped the spec's originally-proposed CI-compile path instead: new compile-app.js runs @babel/plugin-transform-block-scoping (the one specific transform Babel was load-bearing for here) via @babel/core, writes committed app.compiled.js/PoolDetail.compiled.js; home.html loads the compiled output as plain scripts, PoolDetail.compiled.js chained via onload before app.compiled.js (dynamically-inserted <script src> tags don't guarantee execution order, and app.js's App component renders the global PoolDetail on the very first paint of ?pool= deep links). @babel/core + @babel/plugin-transform-block-scoping added to package.json "dependencies" (not devDependencies) since sitemap-update.yml's CI runs `npm install --only=production`; wired a compile step + the two compiled files' path triggers + git-add into that workflow, same pattern as the sitemap/token/chain generators. New test_compiled_assets.js (4 assertions: compiled output byte-identical to a fresh in-memory transform of current source — catches drift/hand-edits without mutating the tree; home.html has no @babel/standalone fetch or text/babel type; correct load order) added to the front of npm test. Verifier independently re-ran test_compiled_assets.js (4/4) and test_search.js (20/20, including the ?pool= deep-link case) itself rather than trusting the builder's numbers, ran the full remaining suite (12/12 files green, zero regressions), diffed home.html's __APP_MODE/ANALYTICS_PARAMS block directly against origin/main (byte-identical), ran `npm run compile` itself and confirmed zero drift, grepped compiled output for any remaining let/const (zero, any scope), and confirmed trust-rail constants (APY_SANITY_LIMIT, DEFAULT_MIN_TVL) survive the transform unchanged. test_smoke.js unrunnable in-sandbox (unpkg.com/yields.llama.fi network-blocked, curl-confirmed 403) — verifier independently reproduced the identical hang against origin/main's untouched files before accepting it as pre-existing, same documented precedent as 040/044/045/051; flagged needs human visual spot-check. Verifier flagged one non-blocking issue (compile-app.js's header comment described an already-abandoned hand-patch state) — fixed in a same-day follow-up commit before merge, along with the required specs/052-pr.md HIGH-tier explainer + 5-question quiz. Merged as PR #154 (squash). Not independently Mixpanel-isolable (perf/PageSpeed change, no funnel step); proxy is Core Web Vitals / PSI score once re-measured live, and the real compiled-output regen for any future app.js/PoolDetail.js edit lands on the next sitemap-update.yml CI run. | HIGH | ~64k subagent tokens (verifier)
2026-07-12 | build (sonnet-5) | 053 | 1 | SHIPPED — verifier PASS (HIGH, 4/4, independently re-derived; matches builder's own tier claim — home.html/plan.html render-path asset references + new dependencies + CI workflow, all NORTH_STAR HIGH triggers): sequenced after 052, adds minify-assets.js (terser for JS, clean-css for CSS) minifying app.compiled.js/PoolDetail.compiled.js (052's compile output, so the two perf passes stack instead of double-transforming)/planner.js/translations.js/style.css, same generate-in-CI/commit-output pattern as compile-app.js and the sitemap generators. home.html/plan.html swapped from ~557K raw JS/CSS to the committed *.min. files (~2.9x smaller on JS via terser mangle+compress, ~1.3x on CSS via clean-css); PoolDetail-before-app onload chain order preserved, just pointed at the new filenames. Wired into sitemap-update.yml right after the 052 compile step, with the new source files added to the push-trigger paths and the five .min. outputs added to the commit step. New test_minified_assets.js (7 assertions: committed .min. output byte-identical to a fresh re-minify of current source — catches drift/hand-edits without mutating the tree; home.html/plan.html reference the minified filenames, not raw ones) added to npm test; test_compiled_assets.js's two home.html assertions (which literally string-matched the old unminified addScript('...compiled.js') calls) updated to a prefix match so they still catch the real regression (load order) without hardcoding a filename this PR intentionally changed — not a weakening, the assertion's purpose is unchanged. terser/clean-css added to package.json "dependencies" (not devDependencies) since sitemap-update.yml's CI runs `npm install --only=production`, same tier @babel/core was added at for 052. Scoped to exactly the 5 files named in spec 053's Evidence section; deliberately left planner-styles.css (83K) and pool-detail-styles.css (23K) unminified as a documented follow-up rather than silent scope creep (specs/053-notes.md). Verifier independently re-ran test_compiled_assets.js (4/4) and test_minified_assets.js (7/7), ran node --check on all 4 minified JS outputs, diffed CSS custom-property counts (333 --neuro- occurrences, source vs minified, exact match — no selector corruption), confirmed top-level function names survive terser's mangle (no toplevel:true set), confirmed the package-lock.json transitive tree is exactly terser/clean-css's expected dependencies (acorn, commander, source-map, etc — nothing unexpected swept in), and confirmed no NEVER-list touches (trust rails, credentials, SEO deletion untouched). test_smoke.js unrunnable in-sandbox (unpkg.com/yields.llama.fi network-blocked, curl-confirmed) — verifier independently reproduced the identical timeout against unmodified main before accepting it as pre-existing, same documented precedent as 040/044/045/051/052; flagged needs human visual spot-check. specs/053-pr.md HIGH-tier explainer + 5-question quiz written before merge. Merged as PR #156 (squash). Not independently Mixpanel-isolable (perf/PageSpeed change, no funnel step); proxy is Core Web Vitals/PSI once re-measured live. | HIGH | ~44k subagent tokens (verifier)
2026-07-12 | build (sonnet-5) | 054 | 1 | SHIPPED — verifier PASS (LOW, 6/6, independently re-derived, agrees with builder's tier claim): no spec existed yet (054 was READY with no spec column entry), so wrote specs/054.md from reports/2026-07-12-pagespeed.md finding 3 evidence before building. Added `<link rel="preconnect">` for `https://unpkg.com` (with `crossorigin`, matching the existing `crossorigin` React script tags) and `https://yields.llama.fi` (no `crossorigin`, uncredentialed fetch) to home.html and plan.html, placed directly before the existing `dns-prefetch` fallback pair in each file (kept, not replaced) — 4 lines added total, 0 deletions, no other head content touched. All 190 node-test assertions pass. test_smoke.js render assertions time out in-sandbox because the network proxy blocks both unpkg.com and yields.llama.fi outright (curl → "CONNECT tunnel failed, response 403"); verifier independently confirmed this is pre-existing (not a regression) by re-running the identical test against pre-diff home.html/plan.html checked out from origin/main and getting a byte-identical failure signature — same documented precedent as 040/044/045/051/052/053. specs/054-pr.md LOW-tier explainer written before merge. Merged as PR #158 (squash). Not independently Mixpanel-isolable (perf/resource-hint change, no funnel step); needs human visual spot-check on next live run (render-path touch, per the render-path advisory until 003's smoke gate can run against a live network). | LOW | ~33k subagent tokens (verifier)
2026-07-12 | build (sonnet-5) | 055 | 1 | SHIPPED — verifier PASS (HIGH, 7/7, independently re-derived, agrees with builder's tier claim — vercel.json is explicit config/infra per NORTH_STAR): no spec existed yet (055 was READY with no spec column entry), so wrote specs/055.md from reports/2026-07-12-pagespeed.md finding 4 + a direct read of vercel.json before building. Added two new Cache-Control header rules to vercel.json: /fonts/(.*) gets `public, max-age=31536000, immutable` (one font file, never content-modified in repo history — safe); /(.*)\.(js|css) gets `public, max-age=300, stale-while-revalidate=86400` instead of the naive immutable recipe — filenames (app.compiled.min.js, style.min.css, etc.) are unhashed and this loop merges to main (auto-deploying via Vercel) multiple times a day, so a 1-year immutable cache would risk serving stale/broken JS to repeat visitors for up to a year after any deploy; 5min-fresh + 1day-SWR gives most of the perf win (kills same-session re-fetch) while a deploy still reaches a returning visitor on their next request after one background refetch. `/` and `/home` (the sacred __APP_MODE router entry points) left untouched at max-age=0, must-revalidate. New test_cache_headers.js (7 assertions, offline, parses vercel.json directly) wired into npm test; package.json's test-script chain is the only other file touched (no new dependencies). Verifier independently ran test_cache_headers.js (7/7), diffed vercel.json/home.html/plan.html/rewrites against origin/main to confirm zero drift outside the two new header rules, and ran the fast node-test suite itself rather than trusting reported output; test_smoke.js's render-path Playwright assertions unrunnable in-sandbox (yields.llama.fi/unpkg.com network-blocked), same documented precedent as 040/044/045/051-054 — this diff touches no render path (headers only), flagged needs human/network spot-check on next live deploy anyway per the render-path-merge convention. specs/055-pr.md HIGH-tier explainer + 5-question quiz written before merge. Merged as PR #160 (squash). Not independently Mixpanel-isolable (perf/resource-hint change, no funnel step); proxy is PageSpeed Insights' "efficient cache lifetimes" audit once re-measured live (PSI was rate-limited during the 2026-07-12 audit that raised this item, no before/after number available in-session). | HIGH | ~55k subagent tokens (verifier)
2026-07-12 | heartbeat-update (operator, human idea) | 058 | — | filed a FEASIBILITY STUDY item (not a build): DefiLlama data wrapper/cache layer — poll ~5min, store (Turso/libSQL or Cloudflare D1 = real sqlite; or static snapshot via cron), serve pre-filtered payload from our domain for speed + independence + custom KPIs on history. Explicitly flagged: reverses the no-backend tenet → BUILD is human-owned; a build loop must NOT stand up a backend/DB off this — output is docs/feasibility-data-layer.md + a recommendation only. Gating first-question = DefiLlama ToS/attribution. Score 7.5. | — | —
2026-07-12 | build (sonnet-5) | 058 | 1 | SHIPPED — verifier PASS (LOW, independently re-derived — spec's own HIGH/human-decision tag applies to the eventual recommended build, not this diff, which is docs-only): wrote docs/feasibility-data-layer.md answering all 8 questions in specs/058.md — options matrix (static-snapshot CI/cron vs serverless+edge-DB vs hybrid), sqlite reality check (Turso/libSQL named as the concrete choice over Cloudflare D1, smaller blast radius on a Vercel-hosted stack), KPI list (rate momentum, N-day mean APY cross-check, new-pool detection, TVL trend, honest trust score — ties to the hook-model/degen-honesty language), staleness/fallback design (generatedAt stamp, 15-min threshold, live-DefiLlama fallback, visible freshness stamp), and trust-rail treatment (layer enforces APY_SANITY_LIMIT/DEFAULT_MIN_TVL upstream, never relaxes them). Recommendation: ship the static-snapshot option first (same shape as existing 014/021/041 generator pattern, doesn't reverse the no-backend tenet); defer the KPI/history feature's real backend/DB to a separate, later, human-approved decision. Two answers are estimated/ASSUMED rather than measured — payload-size comparison and the DefiLlama ToS/attribution question — because yields.llama.fi is policy-blocked from this sandbox (confirmed via a failed curl + a live proxy-status connect_rejected log entry), the same wall hit by 010/013/018/040/044/045/052-055; both are disclosed prominently in the doc itself (not just the notes file) and made the first two steps of the phased plan rather than guessed. Follow-up build item 059 (static-snapshot option only) drafted inside the doc, explicitly marked BLOCKED pending human ToS confirmation + sign-off — deliberately not promoted to a live BACKLOG.md row (no independently-scoped spec exists yet). specs/058-pr.md LOW-tier explainer written before merge. Merged as PR #163 (squash). No isolable Mixpanel funnel metric — this is a decision doc, not a product change; no measurement window. | LOW | ~45k subagent tokens (verifier)
2026-07-12 | build (sonnet-5) | 056 | 1 | SHIPPED — verifier PASS (HIGH, 7/7, independently re-derived, agrees with builder's tier claim — home.html/plan.html render-path script-source change): no spec existed yet (056 was READY with no spec column entry), so wrote specs/056.md from reports/2026-07-12-pagespeed.md finding 5 evidence before building. Vendored react.production.min.js/react-dom.production.min.js at the repo root (byte-identical to the pinned react/react-dom 18.3.1 devDependency's node_modules UMD builds — the exact same files test_search.js was already using to intercept unpkg requests during offline test runs) and pointed home.html + plan.html's React `<script>` tags at the local copies instead of unpkg.com, dropping the now-dead unpkg preconnect/dns-prefetch (both files) and repointing (not deleting) plan.html's unpkg preload pair to the local files. __APP_MODE/ANALYTICS_PARAMS diffed byte-identical; no vercel.json/CI/package.json change needed (055's existing generic /(.*)\.(js|css) cache rule already covers the new files by extension). Verifier independently ran every check itself rather than trusting the notes file, including building a throwaway worktree at the parent commit (pre-change, unpkg-based) to establish a real baseline: on that baseline, React fails to load at all in this sandbox (unpkg network-blocked) and the planner selector never appears — in some runs the browser process crashed outright from repeated SSL handshake failures — versus this diff, where a custom Playwright probe confirmed React+ReactDOM load and #planner-root mounts real app markup. That baseline comparison is the core evidence this is a genuine reliability fix, not just "equally broken in-sandbox either way." Verifier also independently reproduced test_analytics_fires.js's pre-existing unrelated failure (static /tokens/big page timeout, zero React/unpkg involvement) on the same baseline worktree before accepting the notes' claim. One minor notes inaccuracy the verifier caught and this session corrected before shipping: "Deviations: None" understated that plan.html's preload hints were repointed rather than literally deleted — doesn't change any acceptance criterion, now logged accurately. specs/056-pr.md HIGH-tier explainer + 5-question quiz written before merge. Merged as PR (see GitHub — created + merged this session). Not independently Mixpanel-isolable (perf/dependency change, no funnel step); proxy is PageSpeed/Core Web Vitals + reduced unpkg-outage blast radius once re-measured live; needs human visual spot-check on next deploy (render-path touch, same precedent as 040/044/045/051-055). | HIGH | ~78k subagent tokens (verifier)
2026-07-12 | ops (fable operator, human live) | net-unblock + 044-closeout + PSI baseline | — | Human directives recorded as standing decisions: (1) operating model — Fable always operates (plan/redteam/backlog/interview/judge), Opus/Sonnet coding agents write all product code; (2) network partially unblocked and verified live: curl/node/generators reach yields.llama.fi (live data confirmed), but browser-originated HTTPS is still connection-level blocked at the proxy (diagnosed: empty NSS store fixed + CA installed + cert-checks-off diagnostic STILL reset → policy, not trust — Playwright keeps fixture-routing; real browser measurement = PSI on prod). Ran the 044-flagged stories/*.html regen with live data (picked up the fixed Analytics guard, test_stories.js 21/21) — 044 fully closed. First real PSI measurement of prod (human's API key, in-session only, never committed): MOBILE PERF 88/100, TBT 0ms, CLS 0, FCP 1.9s, LCP 3.5s, zero opportunities >100ms — the 052-056 perf wave validated on Google infra; LCP is the only residual lever; no CrUX field data (traffic below threshold — traffic, not perf, is now the binding constraint). 056 confirmed live on prod (self-hosted React serving). PSI API works keyed; GSC still needs 026 connector; Mixpanel MCP still needs interactive OAuth. | — | ~0 (ops session)
2026-07-12 | ops (fable operator, human interview answers) | interview-closeout | — | Five decisions recorded as standing decisions: (1) distribution = X protocol spotlights (small keen protocols, Curve upper bound; human's Canva video template per pool, never slop; loops build the data/copy/asset pack) → new item 060 (9.0, READY); (2) business = yield-funded subscription CARD (deposit → position → disposable card pays the sub from yield, we take card fees; waitlist = card funnel) → north-star metric switched to weekly waitlist_submitted (viral closure demoted to input metric; FLAGGED operator-inference, human may veto) + new item 061 (8.0, READY, honest early-access copy EN+KO); (3) GSC = human checks manually ~6wks, 026 PARKED, no nagging; (4) Mixpanel MCP: human says on, cloud session sees unauthenticated — human to authorize the connector in claude.ai settings + enable on routines; (5) PSI key wrong account — discarded never committed, no CI rail, 057 PARKED. Weekly theme set: Distribution + card funnel (week of 2026-07-12). | — | ~0 (ops session)
2026-07-12 | build (sonnet-5) | 057 | 1 | HALTED PRE-MERGE, not shipped — picked up 057 as the only READY item at pickup (BACKLOG.md read before the same-day human interview closeout landed on origin/main); wrote specs/057.md from evidence (no spec existed), built the PNG→JPEG switch for og-image.png + 051's per-page cards, and ran it through 2 verifier passes: pass 1 caught a real bug (og-image.jpg shipped at 2400x1260 instead of the declared 1200x630 — deviceScaleFactor:2 + clip without scale:'css' emits device pixels not CSS pixels), pass 2 (after adding scale:'css' and regenerating — file also shrank further, 49,724B vs the buggy 119,871B, ~83% smaller than the original 286,318B PNG) PASSED 6/6, independently re-assigning HIGH tier (diff >150 lines, render/asset-pipeline change touching og:image sitewide). On final pre-merge sync with origin/main, found 057 had already been marked PARKED by the human same day via live interview (ops entries above: "PSI key wrong account... no CI rail, 057 PARKED" + "compression later; PSI 88 mobile shows zero image-related opportunity — no urgency") — this landed on main after my pickup but before my ship step. Did not merge, respecting the human's explicit call; the verified fix (including the real dimension-bug catch) is preserved on pushed branch `claude/loop-057` (2 commits: 5e650e4f, 20898479) for whenever compression is revisited. No PR opened for it — nothing to merge. Backlog/north-star reconciliation for this session: 060 (X-spotlight pack generator, 9.0) and 061 (waitlist reframe, 8.0) are the current top READY items per the 2026-07-12 interview closeout; next pickup should start there, not 057. | HIGH | ~110k subagent tokens (2 verifier passes)
2026-07-12 | build (sonnet-5) | 061 | 2 | SHIPPED (PR #171, merged 0e28b650) — no spec existed at pickup; wrote specs/061.md from NORTH_STAR.md's 2026-07-12 Q2 interview decision (yield-funded subscription card, waitlist = its funnel) plus a code audit of the live waitlist flow (planner.js:2588-2599 CTA, 2430-2507 modal). Reframed 4 translations.js keys (ctaWaitlistMicro/waitlistTitle/waitlistBenefits/waitlistGarden, EN+KO) from generic "early access list" language to naming the actual card+yield-pays-subscription mechanic. First-draft copy used "disposable card" and failed a pre-existing test_planner.js "ICP alignment" guardrail suite (bans crypto jargon in these exact keys) — revised to plain language, 190/190 assertions passed. Verifier attempt 1 FAILED on a real bug the builder missed: translations.min.js (what plan.html actually loads, per backlog 053) was never regenerated, so the new copy shipped nowhere; fixed via npm run minify + node test_minified_assets.js (7/7). Verifier attempt 2 independently reproduced the rendered UI via its own fixture-routed Playwright script (test_search.js's pattern, no live-network dependency) and confirmed the new EN/KO copy genuinely renders in the live waitlist modal — PASS, LOW tier (9/9 criteria, diff well under 150 lines, no trust-rail/schema/dependency touch). Also surfaced, documented, and deliberately left unfixed (scope creep guard): ctaWaitlistMicro's only render site is pre-existing dead code (ctaElement, never mounted — same bug class as 009's finding), flagging it as a candidate future item. Measuring waitlist_opened → waitlist_submitted (009 events) through 2026-07-26. | LOW | ~170k subagent tokens (2 verifier passes)
2026-07-12/13 | build (sonnet-5) | 060 | 1 | SHIPPED — no spec existed at pickup (BACKLOG row had no spec column entry); wrote specs/060.md from the 2026-07-12 distribution-decision standing decisions plus a full code-archaeology pass on the share-URL/trust-rail/share-card machinery 060 depends on (024/027). That pass found a real, code-confirmed gap in 024's already-shipped-and-measuring share URL: `encodePlanToUrl`/`decodePlanFromUrl` never carried `chain`/`token`, so no share link could actually pin a specific pool — closing this additively (backward-compatible, old links unaffected) was a spec-scoped prerequisite, not scope creep, since a spotlight pack's core promise ("a working example-garden link") was otherwise unachievable. Dispatched implementation to a sonnet coding agent per the 2026-07-12 operating-model standing decision (Fable/orchestrator plans+verifies, dispatched agents write product code); orchestrating session wrote the spec, reviewed the diff, ran tests directly, dispatched the verifier, and handled shipping itself. Build: `generate-spotlight.js` (new pack generator — Curve-`curve-dex`-aggregate-TVL ceiling for "small protocol" classification, its own `DEFAULT_MIN_TVL=$10M`/`APY_SANITY_LIMIT=1000` literals matching app.js/planner.js exactly, not generate-token-pages.js's relaxed SEO floor), planner.js share-URL fix + planner.min.js regen, test_spotlight.js (25/25) + test_spotlight_url.js (3/3, real-Chromium Playwright following test_search.js's fixture-routing pattern — caught two real bugs during the build: a persona-classifier gap where the spec's literal APY-only description would have tagged a pool `stable` that the live TVL-gated `curatePools` would then reject into an empty grid, and the stale-`planner.min.js` issue). Verifier PASS, HIGH tier (independently agreed, not just re-stated), 8/8 acceptance criteria independently re-derived — ran every test itself, independently confirmed trust-rail parity via grep + a hand-built anomalous-pool fixture CLI run, confirmed zero diff to home.html/app.js/telegram-bot/whatsapp-bot/workers, confirmed planner.min.js is genuinely current via test_minified_assets.js. Pre-existing sandbox-network-flaky tests (test_smoke.js/test_search.js/test_analytics_fires.js) fail identically on the pre-060 baseline worktree — not a regression. `npm test`'s host chain halts at the first of these; every other member (including both new spotlight tests) verified individually. specs/060-pr.md HIGH-tier explainer + 5-question quiz written before merge. `spotlights/` generated output intentionally not committed this pass (no live-network production run made in-sandbox against real data — same standing convention as tokens/chains/og: a future `npm run spotlight` run should commit real output). No isolable Mixpanel funnel metric (infrastructure for a not-yet-posted distribution channel); proxy is whether the human ships a real spotlight post using the pack, and once posted, share_link_opened/plan_created traceable via that link's chain/token/goal combination. | HIGH | ~450k subagent tokens (1 build dispatch + 1 verifier pass)
2026-07-13 | build (pickup, no-op) | — | 0 | no READY items, no qualified promotions — every BACKLOG.md row is DONE/SHIPPED/PARKED/SUPERSEDED/BLOCKED (grep-confirmed zero `READY` status rows on a fresh `origin/main` checkout). Checked both promotion sources per build.md's fallback path: specs/010-diagnosis.md's ranked fixes (011-015) are all SHIPPED; the newest reports/ file by commit timestamp (reports/2026-07-12-pagespeed.md, 17:58 — items 052-057) is fully SHIPPED except 057 (correctly PARKED by human directive, "compression later"); reports/2026-07-12-sota-seo.md (items 045-051) is likewise fully SHIPPED. 026 (GSC heartbeat connector) correctly stays PARKED — human-owned config, not repo code, not re-flagged. 059 (static-snapshot data-layer build) stays correctly un-promoted: docs/feasibility-data-layer.md explicitly marks it BLOCKED pending human ToS confirmation + sign-off, an open architectural question (build.md step 1's guard against guessing). One loose thread noted but deliberately NOT promoted, since it isn't sourced from either sanctioned promotion path (010-diagnosis.md or the newest report): 061's build note flagged `ctaWaitlistMicro`'s only render site as pre-existing dead code (`ctaElement`, never mounted) — a candidate for a future heartbeat to evidence/score/spec properly, not guessed into an item here. Q-e (GSC connector vs more SEO surface) remains an open question for the human, unchanged. Verified test_planner.js (190/190) + test_protocol_parsing.js + test_qualifier_fix.js green on this checkout before recording this, confirming the tree itself is healthy. Recorded per instruction rather than inventing a low-value item to fill the slot — same call as the 2026-07-11 no-op precedent (this file, entry for 2026-07-11 "build (pickup, no-op)"). | — | ~0 (triage only, no code changes)
2026-07-13 | build (pickup, no-op, 2nd this date) | — | 0 | fresh-context pickup on a new claude/loop-* branch cut from `origin/main` (a24d1a56, itself the tip commit of the prior no-op entry above — PR #174 had already merged before this session started, so build.md's merged-branch restart rule applied: branch reset to `origin/main`, no prior unmerged work existed to carry forward). Independently re-ran both fallback-path checks rather than trusting the prior entry: `grep -n "READY" BACKLOG.md` still matches only the header legend (zero READY rows); `specs/010-diagnosis.md`'s ranked fixes 011-015 remain SHIPPED per BACKLOG; `reports/` directory unchanged since the prior check (same 7 files, same mtimes, no new report landed); `docs/feasibility-data-layer.md`'s 059 follow-up item is still explicitly "BLOCKED — do not build until both are confirmed" (ToS answer + human sign-off), unchanged. No new evidence exists to promote against since the prior pickup — state is identical. Ran `node test_planner.js && node test_protocol_parsing.js && node test_qualifier_fix.js` clean (exit 0) to confirm the tree itself is healthy before recording this. Recording per build.md's fallback instruction rather than inventing a low-value item; next pickup should re-check after a heartbeat run or human interview adds new READY rows or a new report. | — | ~0 (triage only, no code changes)
2026-07-13 | build (pickup, no-op, 3rd this date) | — | 0 | third consecutive no-op pickup today, on the designated branch `claude/jolly-turing-8pwmui` (already at parity with `origin/main` tip 63f0a56f — PR #175 from the prior pickup had already merged before this session started, so no restart was needed). Independently re-derived the same fallback checks rather than trusting either prior entry: `grep -n READY BACKLOG.md` still matches only the header legend (zero READY rows across all 61 numbered items — everything is DONE/SHIPPED/PARKED/SUPERSEDED/BLOCKED); `specs/010-diagnosis.md`'s ranked fixes 011-015 remain SHIPPED; `reports/` directory unchanged byte-for-byte since the prior two checks (same 7 dated files, same 2026-07-12 20:22 mtimes, no new heartbeat report has landed); `docs/feasibility-data-layer.md`'s 059 follow-up item is still explicitly "BLOCKED — do not build until both are confirmed" (DefiLlama ToS answer + human sign-off), unchanged. `git fetch origin main` confirmed no new commits landed beyond the 2nd no-op's own merge. Ran `node test_planner.js && node test_protocol_parsing.js && node test_qualifier_fix.js` clean (exit 0) before recording this. No new evidence exists to promote against — state is identical to the prior two pickups. Flagging for the human: three build-loop pickups have now found nothing to build on the same date: the bottleneck is upstream (no heartbeat has run today to produce a new report or READY row, and Q-e from the 2026-07-12 report — GSC connector vs. more SEO surface — is still open). Running the build loop again before a heartbeat cycle or human answer will keep producing this same no-op; recommend a heartbeat run or human interview before the next build pickup. | — | ~0 (triage only, no code changes)
2026-07-13 | build (pickup, no-op, 4th this date) | — | 0 | fourth consecutive no-op pickup today, on a freshly-restarted branch (`claude/jolly-turing-dfzrv9` reset to `origin/main` tip 05f2737f — the 3rd no-op's PR #176 had already merged before this session started, so no unmerged work to carry forward). Independently re-derived all fallback checks rather than trusting the prior three entries: `grep -n READY BACKLOG.md` still matches only the header legend (zero READY rows across all 61 numbered items); `specs/010-diagnosis.md`'s ranked fixes 011-015 remain SHIPPED; `product-loop-kit/reports/` directory byte-identical to the prior three checks (same 7 dated files, same 2026-07-12 20:22 mtimes — no heartbeat has run since); `docs/feasibility-data-layer.md`'s 059 follow-up item is still explicitly "BLOCKED — do not build until both are confirmed" (DefiLlama ToS answer + human sign-off), unchanged. Ran `node test_planner.js && node test_protocol_parsing.js && node test_qualifier_fix.js` clean (190/190 assertions + parsing suites, exit 0) confirming the tree is healthy. No new evidence exists to promote against — state is identical to the prior three pickups today. Reiterating the 3rd pickup's flag for the human: four build-loop pickups have now found nothing to build on the same date; the bottleneck is upstream (no heartbeat cycle has produced a new report or READY row today, and Q-e — GSC connector vs. more SEO surface — is still open). Recommend a heartbeat run or human interview before the next build pickup runs into the same wall. | — | ~0 (triage only, no code changes)
2026-07-13 | build (pickup, no-op, 5th this date) | — | 0 | fifth consecutive no-op pickup today, branch `claude/jolly-turing-fkrtqb` already at parity with `origin/main` tip c24ddc5c (the 4th no-op's PR #177 had already merged before this session started, so no restart was needed). Independently re-derived all fallback checks rather than trusting the prior four entries: `git fetch origin main` showed only the 4th no-op's own merge commit since the last-known tip — no heartbeat or human interview landed; `grep -n READY BACKLOG.md` still matches only the header legend (zero READY rows across all 62 numbered items); `specs/010-diagnosis.md`'s ranked fixes 011-015 remain SHIPPED; `product-loop-kit/reports/` directory byte-identical to all four prior checks (same 7 dated files, same 2026-07-12 20:22 mtimes); `docs/feasibility-data-layer.md`'s 059 follow-up item is still explicitly "BLOCKED — do not build until both are confirmed" (DefiLlama ToS answer + human sign-off), unchanged. Ran `node test_planner.js && node test_protocol_parsing.js && node test_qualifier_fix.js` clean (190/190 assertions, exit 0). While here, fixed a real bookkeeping gap unrelated to today's no-op wall: BACKLOG.md was missing a row for item 034 (pool-detail projection/CTA tied to the calculator's investmentAmount) — its PR #130 was merged 2026-07-11 (confirmed via `pull_request_read`: merged=true) but the row never landed, per the 2026-07-11 heartbeat's own note about an ID collision (034-038 drafted, then renamed to 039-043 after discovering a concurrent session had just taken 034 for this unrelated fix). Added the row (SHIPPED, HIGH, specs/034.md) so BACKLOG.md's ID space is now contiguous and accurate — same reconciliation pattern as 029-033's rows. This is bookkeeping, not new product code; no verifier dispatch, no user-facing change. Escalating to the human this time (not just logging): five consecutive no-op pickups on the same date means the build loop has been idling since the last real work landed (060/061, 2026-07-12/13) — the bottleneck is entirely upstream (no heartbeat cycle producing a new READY row or report, Q-e from 2026-07-12's report — GSC connector vs. more SEO surface — still unanswered). Recommend running a heartbeat cycle or answering Q-e before scheduling further build pickups; repeating the build loop alone will keep producing the same no-op. | — | ~0 (triage only, no code changes)
2026-07-12 | heartbeat-update (operator, human "refill toward north star") | 062-066 | — | routine ran dry (051-061 shipped: perf wave + spotlight engine + waitlist reframe). Re-oriented to the CURRENT north star (waitlist_submitted / card funnel, changed 2026-07-12) + weekly theme (distribution + card funnel). Biggest gap = the 2,131 SEO pages have ZERO path to the waitlist. Filed: 062 (waitlist CTA on SEO pages = the SEO→north-star bridge, 8.5, spec), 063 (waitlist funnel drop-off instrumentation, spec), 064 (spotlight→waitlist attribution+cadence), 065 (value-prop framings pre/post), 066 (honest per-token earning KPI, no-backend, ties the 058 data direction to conversion). | — | —
2026-07-13 | build (sonnet-5) | 062 | 1 | SHIPPED (PR #180, merged) — spec already existed (specs/062.md, filed by the 2026-07-12 heartbeat as the top-scored READY item, 8.5, directly closing the SEO-surface's zero-path-to-waitlist gap). Built the CTA generator wiring (generate-token-pages.js's new renderWaitlistCtaHtml/renderWaitlistCtaStyle, reused by generate-chain-pages.js — same "chain generator reuses token generator's exports" pattern as 049/050) + planner.js's `?waitlist=1&src=` auto-open. Deliberately did NOT reuse Bloom's existing waitlist modal for the auto-open path: it depends on a completed plan (goal/persona/pools) that doesn't exist on a fresh SEO landing, and synthesizing a fake plan just to reuse it would have shown a dishonest personalized-looking plan — shipped a standalone, plan-independent modal instead, reusing the same gp-waitlist-* CSS classes/copy. Mid-build, the new "062" test assertions caught a translation-key-scoping bug before it ever reached the verifier: generate-token-pages.js's createTranslationFunction only does a flat top-level lookup and can't reach the nested `planner.*` keys the client app uses, so a first pass silently rendered literal key names ("waitlistTitle") as visible page text — fixed by adding 3 new flat tcpWaitlist* keys (EN+KO) matching the existing tcp*-prefix convention. Verifier pass 1: FAIL — caught a real, reproducible defect (renderWaitlistCtaHtml interpolated the token/chain-specific pitch line without escapeHtml(), unlike every other interpolation in these files; chain names have no character-safety regex and come from the live DefiLlama API, so an adversarial chain name could inject markup into a public SEO page). Fixed in a follow-up commit + added a targeted regression test to both test_token_pages.js and test_chain_pages.js. Verifier pass 2: PASS, HIGH tier (independently re-derived — SEO-surface generators + planner.js waitlist-open path), 5/5 criteria. New test_waitlist_seo_entry.js drives the real rendered plan.html UI via Playwright (2026-07-11 standing decision), using waitUntil:'domcontentloaded' instead of this repo's usual 'load' — root-caused during the build that plan.html's async Mixpanel-lib request (mp.defi.garden) hangs rather than failing fast in this sandbox, the same class of pre-existing limitation NORTH_STAR.md's 2026-07-12 standing decision already documents for unpkg.com/yields.llama.fi, confirmed via git-stash comparison against the unmodified tree (test_smoke.js/test_search.js/test_analytics_fires.js hang identically on main, not a regression). specs/062-pr.md HIGH-tier explainer + 5-question quiz written before merge. Real `/tokens/`/`/chains/` pages carrying the CTA land on the next sitemap-update.yml CI run (no live-network production run made in-sandbox, same precedent as every prior token/chain-page item). Measuring waitlist_opened(source=seo_token|seo_chain) → waitlist_submitted through 2026-07-27. | HIGH | ~125k subagent tokens (2 verifier passes)
2026-07-13 | heartbeat | 064/066 specs written, 063/065 traffic-gated (scores cut to 6.5), 064 bumped 8.0 | north-star=0 (TRAFFIC not wiring — waitlist funnel audited SOUND end-to-end in prod bundle, NOT the 044 bug); traffic DECAYING 167→80→80→21/day; distribution is the binding constraint; 060 spotlight engine shipped but unposted (human-gated). Q-f: pre-build ready-to-post packs vs build 064 attribution? Mixpanel MCP live this run.
2026-07-13 | build (sonnet-5) | 063 | 1 | SHIPPED (PR #181, verifier PASS LOW 3/3) — spec pre-existed (specs/063.md), picked up as the top-scored READY item (7.5) before this same-day heartbeat's traffic-gated re-score (6.5, sequence-after-064) landed concurrently on main; since the build was already complete/tested/zero-risk by the time the conflict surfaced, shipping stands rather than discarding finished, verifier-passed work (see BACKLOG.md's 063 row for the reconciliation note). Instrumented waitlist_email_entered/waitlist_submit_attempt/waitlist_error(reason) across both waitlist entry points (bloom modal + 062's SEO quick-entry modal); 009's waitlist_opened/waitlist_submitted unchanged. New test_waitlist_funnel.js (Playwright, real rendered plan.html, mocked Formspree) caught a real pre-merge bug: onFocus fired waitlist_email_entered immediately via autoFocus, landing before waitlist_opened in the tracked-event queue (impossible funnel order, zero-signal event) — fixed by switching to first-onChange (first keystroke), ref-guarded to fire once per open; verified via an explicit event-order assertion. Regenerated planner.min.js via npm run minify (plan.html loads the minified file — the exact gap that shipped stale in 061) and verified byte-identical via test_minified_assets.js. Full npm test chain still halts at test_smoke.js on the pre-existing, extensively-documented sandbox network limitation (Chromium can't reach unpkg.com/yields.llama.fi); confirmed unrelated — diff touches only analytics.js/planner.js/package.json's test-script wiring, no home.html/router/app.js. specs/063-pr.md LOW-tier explainer written before merge. Measuring the opened→email_entered→submit_attempt→submitted funnel + error-reason breakdown; decision rule = the next waitlist item (065/066) targets the biggest observed drop. | LOW | ~55k subagent tokens (1 verifier pass)
2026-07-13 | ship-path change (human directive, mid-session) | — | — | human hit Vercel's free-tier deployment/API quota (100/mo) — traced to the branch-push-then-merge-to-main pattern paying for 2 deployments/item, tripled when bookkeeping shipped as a separate follow-up commit/PR (058's 3-PR precedent: #162 file, #163 study, #164 bookkeeping, confirmed by content-diffing the stale claude/loop-058 branch against main). NORTH_STAR.md updated (2026-07-13 standing decisions): build loops now commit directly to main, ONE commit per item (code + BACKLOG/LOG/notes/pr-explainer together), no feature branch, no PR — verifier step unchanged. Also recorded a revised execution-model split (thinking=Fable 5, build execution=Opus 4.7, lightweight subagent work=Sonnet 5) per the same human message; this session (Sonnet 5) could not self-reconfigure mid-run (2026-07-10 precedent — routine config is human-owned), flagged rather than silently claimed. Attempted `git push --delete` on the two confirmed-merged stale branches (claude/loop-058, claude/loop-063) — 403, this session's git-remote credentials lack branch-delete rights; left for the human or a GitHub-MCP-authorized path. | — | —
2026-07-13 | build (sonnet-5) | 064 | 1 | SHIPPED (direct-to-main single commit, per the ship-path change above — no branch/PR this time) — spec pre-existed (specs/064.md, filed by the 2026-07-12 heartbeat, 8.0). generate-spotlight.js's buildShareUrl now appends src=x_spotlight + a stable per-pool ref (reuses the same slug already computed for the pack's output dir); planner.js threads Planner's existing `?src=` read (waitlistSrc, already used by 062) into Bloom as a new `source` prop, applied to trackPlanCreated and both trackWaitlistOpened call sites; analytics.js's trackPlanCreated gained a `source` field (didn't have one before — needed since the spec's AC covers plan_created too, not just waitlist_opened). Added a cadence/coverage doc: rankCandidates (extracted from pickPool's auto-pick logic) + buildCadence/renderCadenceMarkdown (pure) + loadCoveredPacks (the one IO wrapper) write spotlights/CADENCE.md on every run — covered pools + next N qualifying, uncovered candidates, so the human's weekly X posting stays systematic. New test_spotlight_attribution.js (Playwright, real rendered plan.html, same harness as test_spotlight_url.js/test_waitlist_seo_entry.js) drives a spotlight-shaped URL end to end and asserts plan_created/waitlist_opened both carry source=x_spotlight, plus a no-`?src=` control case (source=null, no regression). Extended test_spotlight.js with cadence pure-function tests + a two-run CLI test proving real coverage state persists. Regenerated planner.min.js via npm run minify, verified byte-identical via test_minified_assets.js (the exact staleness gap 061 hit). Verifier: PASS, LOW tier (independently re-derived — builder guessed MED, verifier corrected to LOW: product-code diff is 138 lines under the 150-line cap, no new deps, no trust-rail/router/config touch), 6/6 criteria, independently re-ran every test cited above plus a from-scratch minify diff. specs/064-pr.md LOW-tier explainer written before landing. Measuring waitlist_opened/plan_created(source=x_spotlight) through 2026-07-27 — attribution existence, not yet a conversion claim (real read comes once the human posts). | LOW | ~67k subagent tokens (1 verifier pass)
2026-07-13 | bugfix (human-reported, mid-session) | 067 | 1 | SHIPPED (direct-to-main single commit, verifier PASS HIGH 6/6) — human reported (with screenshots) that `/plan.html?goal=retirement&pace=rwa&capital=1000&fm=capital&years=5` rendered "≈ $0 in 5 years" / "vs $0 savings" / "deposited $0" despite the sidebar correctly showing $1,000 capital + 10.58% blended APY. Root cause: every GROWTH-archetype projection call site used only `futureValue(monthly, ...)` — a pure monthly-annuity formula — and `monthly` is null on a capital-funded plan; `capital` was never read at all in GROWTH's paths (TARGET/SUBSCRIPTION already had their own capital-aware hero shapes; a literal `// GROWTH: unchanged` comment sat at the exact gap). Fix: new `capitalGrowth(capital, rate, years)` lump-sum helper wired into buildPlanHero's growth branch, Bloom's persisted projection/bankProjection/deposited, Bloom's live-slider headline (gated on isCapitalPath — slideCapital's useState defaults to 5000 even on monthly plans, so an ungated add would have leaked phantom capital into monthly-only projections; trap caught pre-commit, locked in with a regression test), and GardenReport's returning-visitor recompute. New test_growth_capital_projection.js (Playwright, real Chromium, drives the EXACT reported URL) 2/2 + 6 new test_planner.js unit tests (196/196). planner.min.js regenerated + byte-identical-verified. Also investigated the report's "amount in pool view" leg: NOT a bug — the earnings calculator verifies exactly against the pool's 142.84% total APY; the 405.32% figure on the same page is the 30D MEAN APY, a different legitimate metric (reward-emission volatility on a farming pool), flagged to the human as a possible future clarity/UX question rather than "fixed". Verifier independently re-ran every test (incl. real-Chromium Playwright), re-derived the isCapitalPath asymmetry as correct, confirmed HIGH tier. specs/067{.md,-notes.md,-pr.md} written before landing. No measurement window — correctness fix ("a bug at a funnel step outranks any experiment at that step"). | HIGH | ~52k subagent tokens (1 verifier pass)
2026-07-13 | build (sonnet-5) | 066 | 2 | SHIPPED (PR #182, merged) — spec pre-existed (specs/066.md, filed by the 2026-07-12 heartbeat, 7.0). Ship-path deviation flagged: NORTH_STAR.md's 2026-07-13 standing decision says build loops commit directly to main (no branch/PR) to conserve Vercel's deployment quota; this session's outer harness (a Claude Code cloud routine invocation, not a Cowork/CLI session) instead pinned a specific designated branch (`claude/jolly-turing-ydkpjw`) with an explicit "never push to a different branch without permission" constraint and its own branch+PR+merge ship-path instructions — followed the harness's branch assignment (a session-level operational constraint, not repo content) over NORTH_STAR's direct-to-main revision, since the two conflicted and the harness constraint is the actual git-push permission boundary for this session; used the assigned branch name rather than inventing `claude/loop-066`. Flagging for the human: if future harness-dispatched sessions keep pinning a branch, either NORTH_STAR's direct-to-main decision needs revisiting for this dispatch mode, or the routine config needs to stop assigning a fixed branch. Built `yieldHeadlineFor`/`renderYieldHeadlineHtml` in generate-token-pages.js: honest "Your idle X could earn ~Y% — park $Z and it could run a $20/mo Claude Pro subscription, forever" headline above the pool table on every /tokens/<slug> page, reusing planner.js's blendedApy (median APY, confirmed live that planner.js IS Node-requireable despite generate-spotlight.js's header comment claiming otherwise) + foreverNumber — no parallel calc path. Returns no headline (never a fabricated number) when the blended MEDIAN rounds to 0.00%, even if a non-zero pool exists on the page. Same field exposed in generate-spotlight.js's Canva pack (pack.foreverAmt/foreverAmtStr). EN+KO translations.js strings added together; translations.min.js regenerated. Verifier pass 1: FAIL — a real, consequential gap: the generator code shipped but none of the 2,131 committed static token pages actually carried the new headline, and the notes' given reason ("yields.llama.fi network-blocked in-sandbox") was factually wrong — the verifier disproved it live (200 response, full generator run in ~1m40s), contradicting NORTH_STAR.md's own 2026-07-12 standing decision that generators can reach the live API in-session. Fixed by actually running `node generate-token-pages.js` live and committing the real output (2011 tokens/*.html + 2011 ko/tokens/*.html + hub/A-Z pages + 2011 og/tokens/*.png + both sitemaps), correcting the false claim in the notes. Verifier pass 2: PASS, HIGH tier, 6/6 criteria — independently re-ran the live generator into a scratch dir and diffed it byte-for-byte against the committed surface (0 differences), confirming the fix was real, not re-faked; confirmed chain-page surface untouched (no scope leakage). specs/066-pr.md HIGH-tier explainer + 5-question quiz written before merge. Real regenerated surface picked up 8 tokens dropping off / 8 newly qualifying since the last regen (2026-07-12) — pre-existing 030 quality-bar behavior, not caused by this item's code. Measuring page_view -> waitlist_opened(source=seo_token) through 2026-07-27 once GSC-driven organic traffic exists (insufficient data until then, per NORTH_STAR's <30-event rule). | HIGH | ~152k subagent tokens (2 verifier passes, incl. two live full-surface generator runs)
2026-07-13 | build (fable-5 operator, opus builder — per the 2026-07-13 execution split) | 065 | 1 | SHIPPED — verifier PASS (LOW, 9/9, independently re-derived: re-ran every test, byte-checked translations.js additions-only, confirmed default render identical to 061's copy). Waitlist pitch-variant system: variant A = 061's copy stays the default (061's measurement window through 2026-07-26 protected by design), B ("Your subscriptions, paid by your yield" — the keep-the-money flip) and C ("Yield pays the bill — the money stays yours" — ownership/withdraw-anytime) live behind `?pitch=b|c` on both waitlist modals, EN+KO, ICP ban-list-clean, every variant states the card doesn't exist yet. All five waitlist events now carry `pitch_variant` (analytics.js trackers + 21 planner call sites), so submit-rate is segmentable per NORTH_STAR's ≥30-events pre/post rule; spotlight links can carry `&pitch=` from day one, making the framing test runnable through the 064 distribution channel instead of waiting on organic traffic. New test_waitlist_pitch.js (7/7, rendered Playwright on plan.html incl. KO + event assertions through a real mocked submit) wired into the npm test chain; test_planner.js ICP suite extended +12 assertions over the new copy. Spec deviation (documented, verifier-confirmed correct): no analytics.min.js exists — analytics.js is loaded raw by both HTML shells, so spec §4's mention of it was wrong; planner.min.js/translations.min.js regenerated and committed. Shipped as ONE commit (code + spec + notes + pr explainer + this bookkeeping) on the harness-pinned branch claude/dazzling-ride-190iql via PR, self-merged after PASS + green tests (2026-07-13 docs-in-first-commit rule, 066 precedent for the pinned-branch mode). | LOW | ~135k subagent tokens (1 build + 1 verifier pass)
2026-07-13 | heartbeat (backlog-add, human-supplied) | 068 | — | added: planner hero-copy CRO test from external SPLITSTREAM audit (title→outcome / tagline→mechanism / hook→live proof anchor); baked trust-rail guardrail into the spec (V03 rate must be live blended APY via formatApy, not the audit's hardcoded "8-15% within months") + the 061 translations.min.js regen gotcha; READY, score 8.0, north-star (waitlist funnel top) | HIGH | ~0 (backlog-only)
2026-07-13 | build (fable-5 operator, opus builder — per the 2026-07-13 execution split) | 068 | 1 | SHIPPED — verifier PASS (HIGH, 6/6, independently re-derived: re-ran every test incl. its own test_search.js 20/20 analytics-path check, grepped both regenerated .min files for the new strings, traced V03's number to the trust rails). Planner hero-copy CRO (SPLITSTREAM audit): title→"Pay your bills with yield, forever", tagline→"Tell us what you need. Live DeFi yield covers it automatically.", splash hook→live-derived proof anchor rendering formatApy(stableGuidanceApy) — the median APY of trust-railed stable curation ($50M band floor, APY_SANITY_LIMIT, anomaly exclusion), null-safe with the untouched number-free splashHook as the honest degrade path; guidanceApy's hardcoded-5.5 fallback deliberately NOT used; the audit's "8-15% within months" rejected as fabricated (timeframe omitted — uncomputable at splash, spec allows). EN+KO together; planner.min.js + translations.min.js regenerated (061's gotcha) and byte-verified. New test_hero_copy.js (rendered Playwright plan.html, fixture-routed pools with per-rail decoys, expected rate derived through the real exported curatePools+blendedApy — not hardcoded) added to the npm chain. Shipped as ONE commit (code + spec notes + pr explainer + this bookkeeping) on the harness-pinned branch claude/dazzling-ride-q8wk3x via PR, self-merged after PASS + green tests (2026-07-13 docs-in-first-commit rule, 065/066 precedent for the pinned-branch mode). needs human visual spot-check (hero render path — highest-visibility surface; test_smoke.js remains environmentally red in-sandbox, pre-existing). Measuring the 009 waitlist_opened→waitlist_submitted funnel pre/post through 2026-07-27. | HIGH | ~122k subagent tokens (1 opus build + 1 verifier pass)
2026-07-13 | build | 069 | 1 | SHIPPED — no READY items existed, promoted 069 from documented evidence (reports/2026-07-13.md Q-f pack option + standing decision 2026-07-12 assigning pack production to loops); blindspot pass found a real honesty gap (pack foreverAmt used HEADLINE APY while the planner projects degen at ⅓ — planner.js:1354-1357 vs generate-spotlight.js:375 — and auto-pick selects degen pools per 060-notes' own smoke test); fixed generator-side (effectiveApy mirror of planner.js:657, additive fields, degen tweet/card carry the haircut statement) and committed the first 3 live packs (3 distinct small protocols, one per persona) + CADENCE.md + test_spotlight_packs.js in the npm chain. Verifier PASS (HIGH, 8/8, independently re-ran all tests, decoded PNGs 1200x630, re-ran generator live vs yields.llama.fi, confirmed test_smoke.js failure pre-existing browser-network-block). auto-merged (HIGH tier, autonomy (c)). Ship mechanics: ONE commit incl. all bookkeeping on the harness-pinned branch claude/dazzling-ride-ms64lp + one PR, per standing decisions 101/104 (deviation from loop/<id> naming: harness pinned the branch, 068 precedent). Human follow-ups: eyeball the 3 card.png before posting; confirm @handles in tweet drafts (placeholders by design); Q-f from the 2026-07-13 report remains open but both branches benefited. | HIGH | ~200k subagent tokens (build 115k Opus + verify 79k Sonnet) + orchestration
2026-07-13 | build | 070 | 1 | SHIPPED — no READY items existed, promoted 070 from documented evidence (specs/009-notes.md + specs/061-notes.md dead-code flags; NORTH_STAR 2026-07-12 honest-card-copy directive): the honest "Card doesn't exist yet" disclaimer (ctaWaitlistMicro, EN+KO, shipped in 061) rendered nowhere — its only call site was the dead ctaElement in planner.js Bloom(). Deleted the dead element, rendered the disclaimer under the live checkout waitlist CTA (both bloom layouts), regenerated planner.min.js, new rendered Playwright gate test_waitlist_microcopy.js (6/6) appended to npm test. Verifier PASS HIGH 6/6, independently re-run incl. clean-HEAD baseline worktree confirming test_smoke/test_search/test_analytics_fires failures are pre-existing sandbox browser-network blocks, not this diff. auto-merged (HIGH tier, autonomy c). needs human visual spot-check (bloom checkout panel 360/768/1280 + dark). Ship mechanics: harness-pinned branch claude/dazzling-ride-jk671k, ONE commit carrying code + all bookkeeping (070.md/-notes/-pr, BACKLOG flip, this entry) per 2026-07-13 docs-in-first-commit rule; one PR, self-merged after PASS. Model note (per 2026-07-13 precedent): operator session = Fable 5 as directed; builder dispatched via Agent-tool 'opus' alias which resolved to Opus 4.8, not the directed Opus 4.7 — no 4.7-pinned override exists in this harness; flagging, not silently claiming compliance. | HIGH | ~1 build agent (Opus) + 1 verifier run
2026-07-13 | build (fable-5 operator, opus-4.7 builder per 2026-07-13 execution split) | 071 | 1 | SHIPPED — promoted by the loop (no READY items; all 010-diagnosis ranked fixes + every reports/2026-07-13.md opportunity already shipped) from documented evidence in specs/067-notes.md: human production screenshots showed one pool page with hero "TOTAL APY 142.84%" and "30D MEAN APY 405.32%" side by side, zero explanation — both numbers correct (067 verified reward-emission volatility, calculator correctly on current rate), page silently trust-killing for the cautious-saver ICP, and Pool Information is expanded by default. Fix: calm neuro-token `.rate-volatility-note` after the pool-info stats grid when current total APY vs apyMean30d diverge ≥1.5× (guarded: both > 0, typeof number — never fires on missing data), copy says rates move and projections use the current rate; new `rateVolatilityNote` key EN+KO; PoolDetail.compiled(.min).js + translations.min.js regenerated in the same change (061 lesson). New test_rate_volatility.js (rendered Playwright, fixture-routed /pools per test_search.js pattern, 5/5) appended to the npm chain. Verifier PASS (HIGH, 7/7, independently re-derived: re-ran the rendered test live, grepped all regenerated artifacts, diffed app.js/planner.js/style.css at 0 lines confirming trust rails/router/calculator untouched, and reproduced the test_smoke.js failure signature on its OWN git-stash baseline — pre-existing sandbox proxy block, not a regression). Shipped as ONE commit (code+tests+bookkeeping) on the harness-pinned branch claude/dazzling-ride-rkywxh + self-merged PR per standing decisions 101/104. needs human visual spot-check (pool-detail render path; smoke gate network-blocked in-sandbox). | HIGH | ~140k subagent tokens (builder 63k + verifier 74k)
2026-07-13 | build (fable-5 operator; builder via Agent-tool 'opus' alias → Opus 4.8, no 4.7 pin exists in this harness — flagged per 070/071 precedent; verifier subagent on sonnet-5 per 2026-07-13 split) | 072 | 1 build / 2 verify | SHIPPED — promoted by the loop (no READY items; every 010-diagnosis ranked fix and every reports/2026-07-13.md opportunity already shipped) from documented evidence in specs/012-pr.md ("?pool=<dead-id> deep links … candidate for a future item"): a dead pool id silently rendered the FULL homepage grid under the self-canonical ?pool= URL — soft-404/duplicate mechanism from the 010 GSC diagnosis (3,568-URL class) AND a trust-killing arrival for shared-garden/token-page/spotlight links whose pool ids rot as pools delist. Fix reuses 012's machinery end-to-end: new deadPoolParam/deadPoolResolved state in app.js routes the miss through the existing empty-state block (new poolNotFoundTitle/poolNotFoundExplanation EN+KO) + getEmptyStateAlternatives(null) stable alternatives + the 012 robots effect widened to noindex the dead render (restores index,follow on nav into a live pool); pool_view stays gated on foundPool so dead hits never pollute the funnel. app.compiled(.min).js + translations.min.js regenerated same commit (061 lesson). New test_dead_pool.js (rendered Playwright, fixture-routed, 5 blocks / all 6 spec criteria) appended to the npm chain. Verifier attempt 1 FAIL — code judged 6/6 with tier HIGH independently confirmed, but the two pre-merge bookkeeping artifacts (specs/072-pr.md explainer+quiz, this LOG entry) were not yet written; fixed, attempt 2 PASS (HIGH). Shipped as ONE commit (code+tests+bookkeeping) on the harness-pinned branch claude/dazzling-ride-c1eb3v + self-merged PR per standing decisions 101/104. needs human visual spot-check (analytics-app empty-state render path; smoke gate network-blocked in-sandbox). | HIGH | ~189k subagent tokens (builder 82k + verify-1 58k + verify-2 49k)
2026-07-13 | build (fable-5 operator, opus builder via Agent model override — standing decision names Opus 4.7; the harness override exposes only the 'opus' tier, flagged not silently claimed) | 073 | — | promoted: no READY items existed (backlog fully SHIPPED/DONE/PARKED/BLOCKED; 010-diagnosis ranked fixes 011-015 all built; newest report 2026-07-13's items 063/064/065/066 all shipped; SOTA-SEO 045-051 + pagespeed 052-056 all shipped). Promoted 053's explicitly documented follow-up (specs/053-notes.md line 12): planner-styles.css (83K) + pool-detail-styles.css (24K) still ship RAW on the funnel-top `/`+plan.html and analytics/pool-detail routes while style.css ships minified. Score 6.0 (evidence: proven −22.5% pipeline exists, funnel-critical routes, S effort, zero new deps). Spec specs/073.md written before build. | HIGH | ~0 (backlog+spec only)
2026-07-13 | build (fable-5 operator, opus builder) | 073 | 1 | SHIPPED — verifier PASS (HIGH, 7/7, independently re-derived: re-ran every test itself, re-derived minify freshness via its own minify-assets.js run + git-status diff, repo-wide raw-reference grep, and a mutation check proving the rendered gate fails on a deleted min file). CSS_FILES += planner-styles.css/pool-detail-styles.css; plan.html link+noscript and home.html both addCSS router sites now load the .min.css variants; sitemap-update.yml paths trigger watches both sources + git add carries both min outputs; test_minified_assets.js 9/9 (freshness auto-covers via CSS_FILES + new reference assertions); new rendered gate test_css_minified_render.js 2/2 (real Chromium: 200 on the min URL, zero raw-sheet requests, computed .gp-tagline text-align:center that only the planner sheet provides) added to the npm chain. Savings: 83,011→67,196 B + 23,812→19,244 B = 20,383 B (−19.08%) off the waitlist-funnel-top and SEO→pool-detail routes. Sources byte-untouched; no new deps; trust rails no contact. Canonical chain green (test_planner 208 / protocol_parsing / qualifier_fix); test_smoke.js environmentally red in-sandbox (pre-existing, NORTH_STAR 2026-07-12). Shipped as ONE commit (code + min outputs + spec + notes + pr explainer + this bookkeeping) on the harness-pinned branch claude/dazzling-ride-58oxq4 via PR, self-merged after PASS (docs-in-first-commit rule, 068/071/072 precedent for the pinned-branch mode). needs human visual spot-check (render-path merge — both router modes' stylesheet URLs changed). No isolable Mixpanel funnel metric (perf, 053-056 disclosure precedent); proxy = PSI transferred-bytes/Minify-CSS audit on next live measurement. | HIGH | ~121k subagent tokens (1 opus build + 1 verifier)
2026-07-13 | build (fable-5 operator, opus builder per 2026-07-13 execution split) | 074 | 1 | SHIPPED — promoted by the loop itself (no READY items; evidence = specs/003-notes.md 2026-07-10 'recommend a new backlog item' + live npm-audit re-confirmation): fast-xml-parser ^4.4.1 → ^5.10.0 clears the repo's only CRITICAL audit finding (6 advisories) on the daily SEO-pipeline CI dep. Verifier PASS (HIGH, 5/5, independently re-derived: audit 0 vulns, parse-stage 23,599 URLs byte-identical to pre-change baseline, all 123 sitemaps valid, unit chain green); verifier also PROVED the test_smoke.js npm-test failure pre-exists the diff (git stash + npm ci v4 baseline → identical ERR_CONNECTION_RESET signature — builder had only argued it by grep) and corrected the notes' understated footprint (6 new transitive packages + strnum 1.1.2→2.4.1, not '1 lockfile entry'). Zero call-site changes; diff = package.json + package-lock.json + bookkeeping, single commit per 2026-07-13/104 standing decisions. auto-merged | HIGH | ~$4 est.
2026-07-13 | build (fable-5 operator, opus builder per 2026-07-13 execution split) | 075 | 1 | SHIPPED — no READY items existed; promoted 066's explicitly documented deferred leg (chain pages were scoped out "token-first this pass" in specs/066.md; 062's waitlist CTA sat on 86×2 /chains/ pages with no concrete pitch number). Reused yieldHeadlineFor unchanged (median blend over the displayed trust-railed rec.pools, Claude-Pro anchor, null gate), generalized renderYieldHeadlineHtml with two defaulted trailing params (token output verifier-confirmed byte-identical, 10/10 fixture pages vs origin/main), new tcpYieldHeadlineChain EN+KO + translations.min.js regen, .cp-yield-headline mirrors .tp-yield-headline token-for-token. Live regen committed: 86 EN + 86 KO pages, 77 each carrying the headline, 9 honestly null-gated (verifier numerically confirmed each 0.00% median, e.g. berachain). Verifier PASS (HIGH, 8/8, independently re-derived: re-ran all 6 suites, redid byte-identity, re-ran the live generator itself, spot-checked ethereum median 2.28% vs max 4.88%). test_chain_pages 67→79 assertions. One-commit PR on the harness-pinned branch (101/104 ship path; auto-merged). Verifier-found latent footgun logged for a future item: generate-chain-pages.js og-image path is cwd-coupled and ignores --out (its scratch regen rewrote repo og/chains PNGs + briefly deleted metis.png, restored). needs human visual spot-check (static-generator render surface, 003 gate doesn't cover it) | HIGH | ~$5
2026-07-13 | build (fable-5 operator, opus builder via Agent model override — standing decision names Opus 4.7, harness exposes only the 'opus' tier, flagged per 070-073 precedent; verifier on sonnet-5 per the 2026-07-13 split) | 076 | 1 | SHIPPED — promoted by the loop (no READY items; 010-diagnosis ranked fixes and every reports/2026-07-13.md opportunity already shipped) from documented evidence in specs/075-notes.md's verifier disclosure: generateOgImages(..., process.cwd()) at generate-chain-pages.js:448 AND generate-token-pages.js:926 ignored --out, so ANY scratch/verification regen rewrote + stale-deleted the repo's committed og/tokens|chains PNGs (demonstrated harm: 075's verification briefly deleted og/chains/metis.png). Fix: resolve outDir first, pass path.dirname(outDir) — the generators' existing koOutDir sibling convention — CI path (--out tokens|chains from repo root) provably byte-identical. generate-og-images.js untouched (its outRoot param already worked; only the callers were wrong). New test_og_outroot.js (child-process, fixture-driven, decoy-og assertions + CI-shape invariance + self-guard refusing cwd===repo-root) in the npm chain. Verifier PASS (HIGH, 6/6, independently re-ran all suites: 208+18+88+79+4 assertions, confirmed clean git status under og//tokens//chains//ko/, package.json diff test-script-only). Not a render-path merge (no committed page/asset bytes change) — no visual spot-check needed. Shipped as ONE commit (code+tests+all bookkeeping) on the harness-pinned branch claude/dazzling-ride-198cif + self-merged PR per standing decisions 101/104. | HIGH | ~104k subagent tokens (builder 53k opus + verifier 51k sonnet)
2026-07-13 | build (fable-5 operator, opus builder) | 056-dup | 1 | ABORTED-DUPLICATE — this run's container cloned a STALE main (44de4329, item-058 era, ~30 PRs behind); its BACKLOG showed 056 READY, so the loop spec'd + built + verified self-hosted React from scratch (opus builder; verifier PASS HIGH 6/6, independently re-derived incl. fresh npm-tarball sha256s and a both-paths Playwright render proof with React un-routed) — the push then failed: remote claude/loop-056 already existed and 056 SHIPPED as PR #167 on 2026-07-12. Nothing was pushed or merged; the local duplicate branch was discarded; the remote branch/audit trail untouched. Operational fix for every future run: `git fetch origin main && git reset --hard origin/main` (or verify HEAD == origin/main) BEFORE backlog pickup — a stale clone silently voids an entire iteration and, with a careless force-push, could have clobbered the shipped item's audit branch. | n/a (nothing shipped) | ~115k subagent tokens
2026-07-13 | build (fable-5 operator, opus builder via Agent model override — standing decision names Opus 4.7, harness exposes only the 'opus' tier, flagged per 070-076 precedent; verifier on sonnet-5 per the 2026-07-13 split) | 077 | 1 | SHIPPED — promoted by the loop (no READY items; 010-diagnosis fixes 011-015 and every reports/2026-07-13.md opportunity already shipped) from repeatedly documented evidence: test_smoke.js was the ONLY Playwright test without the fixture-routing fallback NORTH_STAR 2026-07-12 prescribes, failing 7/8 in-sandbox on the browser-only yields.llama.fi block and halting the whole `npm test` &&-chain every run (069/071/073/074 notes each had to stash-baseline-prove it). Fix (test_smoke.js only, +85/−5): curl-probe once at startup → serve the LIVE /pools payload via page.route (live-snapshot mode ran here — curl reaches llama, Chromium can't), inline DefiLlama-shaped fixture as fallback; ignorable-error matching switched from console text to msg.location().url (text never carries the URL — the old pattern silently never matched resource failures), ignore-set limited to repo-referenced degrade-gracefully hosts (fontshare, mixpanel, api.llama.fi/protocols, Google favicon service). Result: 1/8 → 8/8 in-sandbox, chain proceeds past test_smoke into test_canonical (24/24) + test_search before the timebox. Verifier PASS (LOW, 5/5, independently re-derived: own run, own stash-baseline 1/8, own mutation check with byte-identical revert, scope+dependency audit). ONE commit (code + spec + notes + explainer + this bookkeeping) on the harness-pinned branch claude/dazzling-ride-g8okry + self-merged PR per 101/104. Not a render-path merge (test-only) — no visual spot-check needed. Ops note for future runs: this container's git-remote fetch served a STALE DIVERGED origin/main (44de4329, 53 divergent commits); GitHub API confirmed the true head d195661bc which the local clone already had — verified via mcp list_commits before basing the branch (056-dup precedent, one level deeper: fetch itself can lie here, cross-check the API). | LOW | ~158k subagent tokens (builder 98k opus + verifier 60k sonnet)
2026-07-13 | build (fable-5 operator) | — | 0 | NO-OP — no READY items, no qualified promotions. BACKLOG is fully SHIPPED/DONE/PARKED/BLOCKED; every documented evidence source is exhausted: 010-diagnosis ranked fixes 011-015 all shipped; reports/2026-07-13.md opportunities (064/066/063/065 + Q-f→069) all shipped; both SOTA reports' ranked items (039-043, 045-051) all shipped; notes-flagged candidates either fixed (061-notes→070, 075-notes→076, 003-notes→074, 012-notes ?pool= leg→072) or non-qualifying (042-notes pool_type dead code = unreachable cleanup, no funnel step; 012-notes poolTypes/protocols-only empty-state gap = no sitemap URL carries poolTypes without token=, hand-typed URLs only). Per signals/2026-07-13.md the binding constraint is DISTRIBUTION (traffic 167→80→80→21/day) and the funnel plumbing is verified sound — the next lever is human-owned (post the 3 ready 069 spotlight packs) or heartbeat-owned (refill the backlog). Ops notes for future runs: (1) origin/main freshness verified via GitHub API pre-pickup this run (dee3fe098 = true head; 056-dup rule followed, clone clean); (2) investigated the scary "Regenerated sitemap.xml with 0 URLs" line in the daily CI commit b2debb8a2 — cosmetic message-only miscount (sitemap-update.yml:128 greps '<url>' in sitemap.xml, which has been a <sitemap>-index since the split; children healthy: category-Lending 451, token-pages 2071, main 12 URLs) — one-line message fix = heartbeat candidate, not a qualified build item. | n/a (nothing shipped) | ~$1 est.
2026-07-13 | build (fable-5 operator, opus builder via Agent model override, sonnet-5 verifier — per the 2026-07-13 execution split) | 078 | 1 | SHIPPED — promoted by the loop (no READY items) from documented in-repo evidence the previous run's NO-OP entry itself surfaced: sitemap-update.yml's daily commit message counts `grep -c '<url>' sitemap.xml`, but sitemap.xml has been a <sitemapindex> since the category split (verified: 0 <url> / 110 <sitemap>), so the message reads "0 URLs" on every run. Promotion overrides the prior run's "heartbeat candidate, not a qualified build item" judgment, with reasoning recorded in specs/078.md: this is a DEAD HEALTH SIGNAL on the SEO guardrail surface (a real zero-URL wipe is indistinguishable from a healthy run) and it already cost a loop run a false-alarm investigation — not merely cosmetic; fix cost ~1 line vs. the same PR/deploy overhead a second consecutive NO-OP log commit would have burned. Fix: message now reports index-child count + total <url> across root sitemap*.xml via `{ grep -o … || true; } | wc -l` (per-occurrence, glob-safe; hardened against Actions' bash -eo pipefail assignment-context abort — builder empirically proved the plain form dies in `MSG=$(…)` though not inside `git commit -m` args; || true swallows exit code only, a genuine 0 still renders honestly). Rendered today: "110 child sitemaps and 25039 total URLs". Verifier PASS (HIGH, 6/6, independently re-derived: own 4-way count cross-check, own zero-match set -eo pipefail scratch runs in both contexts, full-file byte-identity read of every other workflow step, own green test-chain run 208+suite+suite). Verifier's role-level "HIGH blocks auto-merge" note overridden by NORTH_STAR autonomy level (c) (human directive 2026-07-10: PASS + tests green → merge ANY tier, explainer-before-merge) — the precedent every HIGH item since 040 followed; full explainer + quiz in specs/078-pr.md written pre-merge. Not a render-path merge (workflow commit-message string only) — no visual spot-check needed. ONE commit (code + spec + notes + explainer + this bookkeeping) on the harness-pinned branch claude/dazzling-ride-s0neow + self-merged PR per standing decisions 101/104. Live proof lands on the next daily sitemap-update.yml run. | HIGH | ~106k subagent tokens (builder 50k opus + verifier 56k sonnet)
2026-07-13 | build (fable-5 operator per the 2026-07-13 execution split; no builder agent dispatched — nothing qualified to build) | — | 0 | NO-OP — no READY items, no qualified promotions (second evidence-exhausted tick; the 078 override consumed the last qualifying candidate). Independently re-verified every documented evidence source rather than trusting the prior NO-OP entry (#198): 010-diagnosis ranked fixes 011–015 all SHIPPED; reports/2026-07-13.md opportunities (064/066/063/065 + Q-f→069) all SHIPPED; SEO-2026 tickets 021–026 SHIPPED/PARKED; SOTA-growth ranked 039–043 SHIPPED/SUPERSEDED; notes-flagged candidates either fixed by later items (061-notes→070, 075-notes→076, 003-notes→074, 053-notes→073, 012 ?pool= leg→072, smoke-gate→077, sitemap-signal→078) or disqualified with reasons that still hold — 042-notes pool_type wiring = unreachable dead code with no funnel step (prior operator judgment, no new facts to override); 019-notes blank hero-paint = harness-only pixel anomaly (DOM assertions pass on unmodified main; human prod-reviewed 2026-07-10 without reporting it), unverifiable in-sandbox; additional 069 spotlight packs = inventory ahead of demand (the 3 committed packs remain unposted); 059 data layer = BLOCKED on human ToS sign-off, build human-owned per 058's study. NEW question surfaced for the heartbeat/human (not buildable without guessing product intent — build.md §1 forbids): specs/066-notes.md documents SUBSCRIPTION_LADDER drift — planner.js ships spotify/netflix/claude/chatgpt/amazonprime while CLAUDE.md + docs/garden-planner-v3-yield-funded.md mandate the everyday-bills set (Spotify → Netflix → Claude Pro → Gym → Phone bill); which set is current product direction? Binding constraint unchanged: DISTRIBUTION (session_start 167→80→80→21/day; waitlist funnel audited sound end-to-end in the production bundle but 0 entrants) — unblocking moves are human-owned (post the 3 ready spotlights/ packs from 069; answer the ladder question) or heartbeat-owned (refill the backlog from the next signals read). Ops: origin/main freshness cross-checked via GitHub API pre-pickup (cef8417ab = true head; local fetch force-updated from the stale 44de43296 divergence the 077 run documented — the stale-remote condition persists in these containers, keep cross-checking). | n/a (nothing shipped) | ~$1 est.
2026-07-14 | build (fable-5 operator per the 2026-07-13 execution split; no builder agent dispatched — nothing qualified to build) | — | 0 | NO-OP — no READY items, no qualified promotions (third consecutive evidence-exhausted tick). Re-verified both sanctioned promotion sources independently: specs/010-diagnosis.md ranked fixes 011–015 all SHIPPED; newest report (reports/2026-07-13.md) opportunities 064/066/063/065 + Q-f→069 all SHIPPED. Prior disqualifications re-checked, no new facts to override any (042-notes pool_type dead code; 019-notes harness-only hero-paint; extra spotlight packs = inventory ahead of demand, the 3 committed packs remain unposted; 059 BLOCKED on human ToS sign-off; SUBSCRIPTION_LADDER drift stays a question for the human — CLAUDE.md/v3 mandate gym+phone-bill, planner.js ships chatgpt+amazonprime, intent unknowable in-loop). NEW verification value this run (not just re-reading prior entries): (1) 062's waitlist CTA confirmed LANDED on the committed SEO surface via the 07-13 18:22 CI regen (b2debb8a2) — 2043/2044 tokens/, 85/86 chains/, KO mirrors identical; the only misses are the two 045 hub index pages (tokens/index.html, chains/index.html — navigational hubs, 2-page gap noted as a heartbeat candidate, NOT promoted: outside sanctioned sources and marginal vs. a deploy); (2) no sitemap-update.yml run since 18:22 07-13 → 078's live commit-message proof still pending, nothing to check yet; (3) origin/main freshness cross-checked via GitHub API pre-pickup (72a098609 = true head; local fetch force-updated from a stale ref — the documented stale-remote condition persists, keep cross-checking); (4) full NORTH_STAR test chain green on this checkout (test_planner.js 208 asserts exit 0, test_protocol_parsing.js exit 0, test_qualifier_fix.js exit 0). Mixpanel MCP visible but UNAUTHENTICATED in this cloud session (per interview Q4 the human must authorize the connector in claude.ai settings — still pending). Binding constraint unchanged: DISTRIBUTION; unblocking moves are human-owned (post the 3 ready spotlights/ packs; authorize Mixpanel; answer the ladder question) or heartbeat-owned (refill the backlog from the next live signals read). | n/a (nothing shipped) | ~$1 est. (triage only)
2026-07-14 | build (fable-5 operator per the 2026-07-13 execution split; no builder agent dispatched — nothing qualified to build) | — | 0 | NO-OP — no READY items, no qualified promotions (fourth consecutive evidence-exhausted tick, ~3h after #201 on the same head). Re-verified rather than trusted: origin/main true head via GitHub API = b52610342 (= local after a forced fetch update — the documented stale-remote condition persists, keep cross-checking); ZERO commits, open PRs, or issues landed since #201; sanctioned promotion sources unchanged (010-diagnosis ranked fixes 011–015 all SHIPPED; reports/2026-07-13.md opportunities 064/066/063/065 + Q-f→069 all SHIPPED); no new heartbeat report or signals snapshot; Mixpanel MCP still visible-but-unauthenticated in this cloud session. Prior disqualifications stand and were NOT re-litigated absent new facts — notably the 2-page hub-CTA gap (tokens/index.html, chains/index.html) that #201 assigned to the heartbeat: same operator calculus, 3 hours old, nothing changed. New verification value this run: (1) the post-18:22-07-13 Update-Sitemap silence is NOT an anomaly — the workflow's push trigger is path-filtered (generators/app assets only) and pushes #196–#201 touched only tests/workflows/product-loop-kit, so no run was due; (2) today's 02:00 UTC cron had not fired by 03:11 UTC (yesterday's fired 05:35 — GitHub cron delay is normal), so 078's live commit-message proof remains pending — check the first scheduled run after ~06:00 UTC; (3) test chain deliberately not re-run: working tree byte-identical to the head #201 ran it green on. Loop state: STARVED — every unblock is human-owned (post the 3 committed spotlights/ packs; authorize the Mixpanel connector in claude.ai settings; answer the SUBSCRIPTION_LADDER drift question from #200) or heartbeat-owned (backlog refill from the next live signals read); each starved tick still spends a PR + Vercel deploys, so pausing the build routine's schedule until an unblock lands is worth the human's consideration. Human notified via push notification this run (first starved-state alert). | n/a (nothing shipped) | ~$0.5 est. (triage only)
2026-07-14 | build (fable-5 operator; opus builder via Agent model override — standing decision names Opus 4.7, harness exposes only the 'opus' tier, flagged per 070-078 precedent; verifier subagent judged independently) | 079 | 1 | SHIPPED — promoted by the loop (no READY items) by OVERRIDING the #201 tick's "marginal, 2 pages" disqualification with recorded reasoning (078 precedent): recount showed the gap is 58 pages, not 2 (tokens/az/*.html ×27 and all KO mirrors were never checked), they are the 045 internal-linking SPINE (home.html links only to the hubs; every non-top-N token is reachable exclusively via an az page), and they were the ONLY pages on the ~2,190-page static surface with zero path to the waitlist (north-star funnel) — while the alternative, a fourth consecutive NO-OP LOG commit, would have burned the same PR + Vercel deploy shipping nothing. Change: one new EN+KO key pair tcpWaitlistPitchHub (subject-free honest card pitch), 3 generator insertion points reusing 062's exported renderWaitlistCtaHtml/renderWaitlistCtaStyle with cssPrefix 'hub' (the emitted anchor inherits the hub pages' EXISTING .hub-cta button chrome — zero new styles beyond the block wrapper), renderHubStyleBlock appends the wrapper style, planner.js untouched (src is free-form so seo_tokens_hub/seo_tokens_az/seo_chains_hub segment in Mixpanel from day one), 58 spine files regenerated LIVE and committed (leaf pages deliberately left on their snapshot — daily CI reconciles), test_hub_pages 32→42 asserts + test_waitlist_seo_entry 3→5 asserts incl. a real-Chromium hub click-through to the plan.html waitlist modal. Verifier PASS (HIGH, 7/7, independently re-derived: own 58-file count + per-surface src distribution, own rendered-Playwright runs, own live regen proving generator-produced output, fixture-pinned old-vs-new-generator FULL-output diff proving leaf/og byte-identity, added-lines-only trust-rail + color grep, minify idempotence ×2, two mutation checks with byte-identical restores; test_search accepted on stash-proof + failure-class reproduction, truncation disclosed). Ops incidents this run, both recovered: (1) verifier agent froze 25+ min on a swallowed Bash tool result — TaskStop + SendMessage resume preserved its 60k-token context; (2) verifier's own mutation-restore momentarily clobbered uncommitted tokens/index.html, self-recovered byte-identical, operator re-spot-checked. ONE commit (code + 58 pages + spec + notes + explainer + this bookkeeping) on the harness-pinned branch claude/dazzling-ride-fgzzc5 + self-merged PR per 101/104. Needs human visual spot-check (rendered SEO-surface merge, 003's gate isn't pixel-level). Measurement: inside 062's waitlist_opened(source=seo_*) stream to 2026-07-27. | HIGH | ~174k subagent tokens (builder 107k opus + verifier 67k)
2026-07-14 | build (fable-5 operator per the 2026-07-13 execution split; opus-tier builder via Agent model override — standing decision names Opus 4.7, harness exposes the 'opus' tier, flagged per 070-079 precedent; verifier subagent judged independently) | 080 | 1 | SHIPPED — promoted by the loop (no READY items) from NEW in-repo evidence that arrived since the starved #200-#202 ticks: 078's first live CI count (commit 6e2853340, 03:27 UTC) reported 6,202 total sitemap URLs while the committed tree held 25,051 — root-caused to the daily workflow regenerating ALL ~106 sitemap children via generate-sitemap.js but `git add`-ing only 5 of them, so 013's GSC crawl-budget gate (shipped 2026-07-11, expected by specs/013-notes.md:21 to "appear naturally on the next CI run") NEVER deployed: fresh index committed over stale ~20.7k-URL children (lastmod lying), 12 orphan child sitemaps (combos/chains/10 chain files) deployed unreferenced, and the human's manual GSC validation window (through ~2026-08-23) measuring a NO-OP. Verified live before speccing: full 15,408-pool fetch, generator-owned output = 448 URLs across 106 children, drop-logs firing per 013's spec. Change: cleanupStaleSitemaps() in generate-sitemap.js (031 pattern; FOREIGN_PAGE_SITEMAPS keep-set for the 4 page-sitemaps, per-file deletion logging, exported + unit-tested in new test_sitemap_cleanup.js wired into npm test) + one workflow line `git add -A -- 'sitemap*.xml'` (quoted pathspec so git stages deletions a shell glob can't see) + real live regen committed (066 precedent): 25,051→4,774 deployed URLs (448 gated + 4,326 page-sitemaps), 12 orphans deleted, all 110 index refs resolve, zero unreferenced children, robots.txt refreshed, tokens//chains//ko//og//stories untouched, trust rails byte-identical. Verifier PASS (HIGH, 7/7, independently re-derived incl. own scratch-repo demo that the quoted pathspec stages D/M/A, two mutation checks on the new test, own count re-derivation via git-show sums) and independently judged the NEVER list NOT triggered (no content page deleted/noindexed; sitemap membership only = 013's already-sanctioned change; 12 deleted files are index-unreferenced stale lists). Verifier's non-blocking notes recorded: 080-notes sentence fixed pre-commit; theoretical EISDIR on a sitemap-named directory (fails loudly in CI, none exists); pathspec crosses directory boundaries (inert — no such paths). ONE commit (code + test + 118 regenerated/deleted sitemap files + spec + notes + explainer + this bookkeeping) on the harness-pinned branch + self-merged PR per 101/104. FLAG FOR TOP BILLING in the next report: deployed sitemap membership shrank 81% by design — the human should expect this in GSC. | HIGH | ~121k subagent tokens (builder 70k opus + verifier 51k)
2026-07-14 | build (fable-5 operator per the 2026-07-13 execution split; opus-tier builder via Agent model override — standing decision names Opus 4.7, harness exposes the 'opus' tier, flagged per 070-080 precedent; verifier subagent judged independently) | 081 | 1 | SHIPPED — promoted by the loop (no READY items, both sanctioned sources exhausted by #200-#202) from NEW same-day in-repo evidence: commit 8fee9e9ba (04:50 UTC) landed 18 min after 117d14a2d with a diff of 560/560 lines across 109 sitemap files — verifier-confirmed 1114 of 1120 changed lines are <lastmod> — proving every CI run always commits (generate-sitemap.js stamped `now` on all ~4.8k URLs + 110 index entries unconditionally, so the porcelain commit gate always fired), each commit costing a Vercel deploy (the documented 2026-07-13 quota crisis) and making lastmod lie daily (the exact "lastmod lies" class 080 called out — Google ignores routinely-inaccurate lastmod, forfeiting 013/080's crawl-budget benefit during the human's manual GSC window to ~08-23). Change: placeholder-based per-entry lastmod preservation in generate-sitemap.js (entry byte-identical modulo timestamp → keeps committed lastmod; changed/new → now; unchanged children come out byte-identical), index per-child lastmod = max URL lastmod (foreign page-sitemaps derived from their on-disk content instead of fabricated), 3 helpers exported + new test_lastmod_honesty.js (8 cases, fixture-only) wired into npm test, workflow concurrency group (sitemap-update, cancel-in-progress false — today's 04:32/04:50 pair ran in that collision window). URL membership/priority/changefreq/hreflang provably unchanged. Verifier PASS (HIGH, 8/8, independently re-derived: own scratch-copy live double-run — run-1 diff = ONLY the one-time 4-entry foreign-index honesty correction, run-3 porcelain EMPTY; own index-honesty script 110/110; own 15,407-pool fixture old-vs-new diff 0 files differ modulo lastmod; mutation check failing 3 cases with sha256-proven byte-identical restore; NEVER list independently judged NOT triggered). VERIFIER RESIDUAL, recorded as the next promotion candidate (NOT built — one-item rule): generate-llms.js stamps unconditional new Date().toISOString() at 3 sites (llms.txt/llms-full.txt — the 6 non-lastmod lines in 8fee9e9ba), and the workflow commit gate is whole-tree porcelain, so daily CI STILL commits every run until llms timestamps get the same content-derived treatment; 081's deploy saving is real for the ~110-file sitemap churn + serialized runs, but the "no-change run skips its commit" proxy will not fire yet — 081-pr.md corrected pre-merge to state this honestly. ONE commit (code + test + spec + notes + explainer + this bookkeeping) on the harness-pinned branch claude/dazzling-ride-b9ftr1 + self-merged PR per 101/104. No isolable funnel metric (loop-infrastructure/SEO-integrity fix, 074/076/077/078/080 disclosure pattern); live proof = next daily CI diff shrinks to really-changed children. | HIGH | ~171k subagent tokens (builder 104k opus + verifier 66k)
