# Spec 002: Share-loop instrumentation truth check — share_link_* fired 0× in 30 days

## Evidence
Mixpanel (4042048), 30d to 2026-07-09: `share_link_created` 0, `share_link_opened` 0, while `plan_created` 5 and `session_start` 132. Both events are defined in the codebase (analytics.js / planner.js). The north-star funnel is 0 → 0. Either the share feature is unused, unreachable, or its tracking never executes — and the north star cannot move until this is answered.

## Hypothesis
We believe the share path is broken or never surfaced (CTA not shown after plan creation, event call not executing, or tracking call racing/failing) because 5 plans were created yet zero share events fired. Confidence: guess until reproduced.

## Change
Reproduce the full share flow locally: serve with `npm run dev`, drive with Playwright — create a plan → trigger share → capture the outgoing analytics request; then open the produced share URL in a fresh browser context → confirm `share_link_opened` fires. Fix the smallest thing that makes both events actually fire, or (if the CTA is genuinely unreachable) document exactly where the flow dead-ends. OUT of scope: share-card PNGs, new share UX, redesigns, any modification to `workers/mixpanel-proxy` (out-of-scope area — read-only for verification), router/URL semantics.

## Acceptance criteria
- [ ] `specs/002-notes.md` documents the observed root cause with file:line references
- [ ] A reproducible Playwright run (script committed or commands documented) shows `share_link_created` firing on the share action and `share_link_opened` firing when the share URL is opened in a fresh context
- [ ] If a fix was needed: the diff touches only instrumentation/UI-copy paths; any user-facing string updated in BOTH EN and KO via translations.js
- [ ] Events carry properties sufficient to attribute a later `plan_created` to a prior `share_link_opened` (session or distinct_id join possible in Mixpanel)
- [ ] Tests: `node test_planner.js && node test_protocol_parsing.js && node test_qualifier_fix.js` exit 0

## Measurement
Metric: weekly `share_link_created` + `share_link_opened` counts · Window: 14 days · Decision rule: events > 0 from organic usage → instrumentation confirmed, next item targets CTA visibility; still 0 → the CTA itself is the next backlog item.

## Risk tier (builder's guess — verifier assigns independently)
LOW if instrumentation/copy only. Anything touching `home.html` router or URL semantics → HIGH.

## Open questions
None blocking.

## Territory notes
