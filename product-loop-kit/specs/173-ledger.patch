diff --git a/product-loop-kit/BACKLOG.md b/product-loop-kit/BACKLOG.md
index cd433db56..1eebfdb17 100644
--- a/product-loop-kit/BACKLOG.md
+++ b/product-loop-kit/BACKLOG.md
@@ -170,3 +170,4 @@
 | 170 | **The browser lane — the rendered half of the merge gate — is structurally unrunnable, so it has not run on any shipped item for a week.** `run-tests.js:200` forced `jobs=1` on the lane (`// forced — fixed-port real-Chromium files must serialize.`) and silently discarded `--jobs`; the lane holds **64 of 100** files at ~30s each (`test_default_sort.js` measured 28.3s this run), so it is ~30min serial — worse in practice (`specs/163-notes.md:180`: `test_smoke.js` blows a 120s cap, `test_search.js` needs ~550s). Five consecutive runs (162/163/165/166/169) recorded *"Browser lane NOT run — 5-minute timebox"* in LOG.md and shipped on a hand-picked `--only` subset: each item's own surface checked, nothing checking that item N didn't break item N-30's. The stated blocker is real — port literals collide across files (7 files on 8796, 5 on 8799, 5 on 8795, 4 on 8819…), and two on one port means EADDRINUSE or a green run against the *other* file's server. Fix = don't rewrite 59 test files; make the runner conflict-aware (static port extraction → per-file conflict keys + a shared-resource key for the two files writing the committed `audit-findings.json`; unknown-port files exclusive; scheduler starts only key-disjoint files). Promoted by the build loop 2026-07-28 (no takeable READY item: 148 reads READY on `main` but has been human-gated PR #306 since 07-26, and the 07-28 report's build order 166→168→169 all shipped) | 7.0 | SHIPPED (2026-07-28, ONE commit on `claude/loop-170` + PR, merged by this run per the 2026-07-11 ship path after verifier **PASS 7/7, tier HIGH independently assigned** — the verifier proved non-vacuity itself with THREE mutants in a scratch mirror: killing `keysConflict` → RED, killing the exclusive branch → RED, and collapsing `while (activeCount < workerCount)` → `< 1` (silently serialise everything) → RED on exactly the load-bearing line *"a non-conflicting pair must overlap when jobs > 1"*; mirror restored byte-exact, repo untouched. It also independently reproduced the concurrent run at `1m26.898s` 4/4 vs a 144.75s per-file sum, walked every browser file's transitive require graph to confirm the port-scan has **no** false-negative surface today (exactly 8 files reach a listening helper — `audit-app.js`'s lowercase `port` at :1499 — and those 8 are precisely the already-exclusive set), and confirmed the `--json` shape and the 16 pre-170 assertions are unchanged). Measured: 4 browser files spanning 3 conflict groups incl. the real shared-port 8799 pair — `--jobs=1` **142.3s** → default `--jobs=3` **87.6s**, 4/4 pass both. `--lane=plain` 36/36; `test_run_tests.js` 26/26 (was 16), 5 consecutive runs, no flakiness. Scope = `run-tests.js` + `test_run_tests.js` ONLY; trust rails / `home.html` router / `?token=/?chain=/?pool=` / generated SEO surface / `package.json` deps byte-untouched (verified by `--name-only` + `-S` searches on the rail constants, not by assertion); not on the NEVER list. THREE VERIFIER-FOUND DEFECTS, all in the notes, all corrected before the commit rather than after: baseline assertion count was 17, is 16; the plain-lane path is *behaviourally*, not byte-for-byte, identical (`workerCount` hoisted, `runFile` aliased); and exclusive files are **deferred, not drained-to** — an exclusive entry at list position 2 of 8 starts last, which keeps the safety invariant but makes the inline comment `// must drain first.` inaccurate. That third one is a real semantic gap and was DISCLOSED, not patched post-verdict (editing code after PASS would invalidate the verified diff) — ticketed in `170-notes.md`. HONEST LIMIT: the full 64-file lane was **NOT run** (it exceeds the timebox — the very problem this shrinks rather than closes; Amdahl floor is `test_search.js`'s ~550s), and the first 8-file measurement set was killed at the 290s cap and is recorded as truncated with 3 files' status unknown. No isolable Mixpanel funnel metric (loop tooling under the 2026-07-23 pre-traffic quality mandate, 154/163/166/169 disclosure pattern); the honest proxy is whether the NEXT run can state a browser-lane result instead of "NOT run". needs human visual spot-check: NO (no render path touched) | HIGH | specs/170.md | 1 | — |
| 171 | **The product audit reports a blocking P0 for a defect its own rendered check disproved — and always will.** `node audit-app.js` on `main` (2026-07-29, clean `npm ci`) → `6 total, 6 blocking, exit 1`. Five are item 148's junk slugs (real, human-gated PR #306). The sixth, `pool-prescan:mean30d-rail-breach` **P0** on `201e5f6e (balancer-v2/WSTETH-AAVE)` `apyMean30d = 27421.09636`, is a false alarm: the same run promoted that pool, rendered it as `pool-detail:201e5f6e`, ran the full pool battery, and returned **zero findings** — because item 144's `mean30dSane` (`PoolDetail.js:161-164`) already gates all five `apyMean30d` render sites (`:1298/:1384/:1412/:1460/:1506`, the only ones in the product per `grep -n apyMean30d`). The aggregate finding is emitted pre-render (`audit-app.js:690`/`:926`) and never revisited, so promotion's answer is computed and discarded. Structurally unclearable: upstream data regenerates daily, 144's gate keeps correctly suppressing it — so `blocking > 0` becomes a constant and a genuinely new P0 lands invisibly. `ci-signal-honesty` class, inverted (misleading RED). Fix = reconcile aggregate prescan findings against their own promoted surfaces; downgrade to non-blocking `P2` ONLY when every suspect was promoted, covered, and clean — unverified is not clean, which is what keeps 148 at P1 | 6.2 | SHIPPED (2026-07-29, verifier PASS 10/10 tier HIGH, one commit + PR merged by the run) — promoted by the build loop: NO takeable READY item existed (148 reads READY on `main` but is in flight as human-gated PR #306, ninth consecutive run). Measured: blocking 6 → 5, exit stays 1 (148 correctly still red). No isolable funnel metric (loop quality instrumentation, pre-traffic mandate); honest proxy = the next tick's blocking count reflects only real defects | HIGH | specs/171.md | 1 | — |
| 172 | **The static-page prescan — the checker over all 2,183 generated HTML SEO pages — has never looked at a link.** `PRESCAN_SIGNALS` (`audit-app.js:175`) is `junk-slug`/`zero-yield-claim`/`broken-number-literal`/`absurd-magnitude`: every signal a number-or-slug check. Measured 2026-07-29 on this checkout: those pages emit ~41,300 same-origin links, **4,989 of them `/?pool=<id>`** — the hop SEO traffic must make to reach the north-star CTAs — and no gate has ever asserted any of them addresses a pool. Item 169 shipped `link-target-integrity` for the two generated TEXT surfaces and explicitly deferred HTML (`specs/169.md:41`, "one surface family per item"); this is that follow-on, on a surface 1,000× larger by file count. Fourth instance of the documented pattern 148 → 159 → 166 (`playbooks/product-audit.md:219-224`): a checker's signals are drawn from the last bug, so the next bug lands in the class nobody has been bitten by yet | 5.6 | SHIPPED on merge (2026-07-29, ONE commit on `claude/loop-172` + PR, merged by the run after verifier PASS **15/15 tier HIGH** — independently re-derived, incl. non-vacuity proven on REAL `tokens/usdc.html` bytes broken three ways rather than on fixtures, four verifier mutations different in kind from the builder's, and byte-identical md5 restores). PROMOTED by the build loop: no takeable READY item existed (148 reads READY on `main` but is in flight as human-gated PR #306, tenth consecutive run). Ships a GATE, not a repair — all three rules green on today's surface (0 suspects), so the true negative was proven non-vacuous rather than reported as health. Out of scope by design: `?pool=` liveness (needs network; measured 15/4,989 dead today, and the `data/pools-snapshot.json` shortcut false-positives 4,233/4,989 on the $10M-vs-$100K floor mismatch). No isolable funnel metric (loop tooling, pre-traffic mandate); observable at the next heartbeat as `prescan.bySignal['link-target-integrity']` | HIGH | specs/172.md | 1 | — |
+| 173 | **The rendered leg of `audit-app.js` has never known the trust rail.** `scanNumbers()` (`audit-app.js:1334`), the ONLY number check on every rendered surface (called once from `auditText()` `:1438`), is magnitude-only — `|value| >= ABSURD_MAGNITUDE (1e11)`, tuned to item 122's `−900,719,925,474,097.9`. It never references `APY_SANITY_LIMIT`, while the constant IS enforced on both NON-rendered legs (`:323` text-surface prescan, `:1010`/`:1017` pool-snapshot prescan). So item 144's real P0 — `apyMean30d = 36452.38798`, 36× the rail, rendered as a trusted "30d Mean APY" card on a pool whose `totalApy = 0.24` kept it unflagged — is only 3.6e4 and sails straight through: it was found BY HAND and would still be invisible to the scanner today. `playbooks/product-audit.md` §Automatability names this exact gap in writing ("Known blind spot (144)… Candidate extension: a rail-relative percent check per the decision rule in class 1"). Fourth instance of 148 → 159 → 166 → 172 (a checker's signals come from the last bug), on a new axis: not a missing surface family, a missing LEG of a tool that already covers the surface. FIX: rail-relative `apy-rail-breach` (P0) on the rendered leg, structural not numeric (the `.tvl-trend-note` deposit-base percent legitimately exceeds 1000%), anomaly-aware and DOM-scoped per `.pool-card`. Promoted by the build loop 2026-07-29 (no takeable READY item — 148 in flight as human-gated PR #306, eleventh consecutive run) | 6.8 | SHIPPED (2026-07-29, ONE commit on `claude/loop-173` + PR, merged by the run after verifier PASS **9/9 tier HIGH** independently re-derived — its own `origin/main` worktree baseline over a `diff`-confirmed byte-identical snapshot, its own execution of all 10 test files, and THREE of its own mutation cycles deliberately different in kind from the builder's seven: whole-block deletion (module-load crash, every consumer red), inverted exclusion set (`EXCLUDED = '*'` → exactly 2 red), and a naive whole-body `innerText.match()` reimplementation (exactly 8 red) — each restored to `md5 77b87ebdfdde32a346b72d3a23cc70f6`). **Ships a GATE, not a repair — stated as such**: zero findings on today's data because every rate render site reachable now is already railed (144's `mean30dSane`, 145's KPI bound, 165's projection gates, `apyBase`/`apyReward` transitively via the anomaly flag), so the true negative was PROVEN non-vacuous rather than reported as health — a real Chromium render with `kpis.apyMomentum = 5000` on an anomaly-unflagged pool puts "5,000%" on screen through `PoolDetail.js`'s rate-momentum note and fires a genuine P0 quoting it verbatim (`apyMomentum` is the one field bounded only transitively, via 145's series endpoints — which is exactly why the positive control exists). Measured both sides: working tree 6 findings/5 blocking with `apy-rail-breach` 0, isolated `origin/main` worktree 6/5. Predicate is an EXCLUDE-list (every rendered `%` is a rate figure unless its container is `.tvl-trend-note`/`.gp-item-fill`) — rejected the allow-list because a checker that only sees registered rate sites is the 148→172 pattern applied to itself; accepted cost is that a future NON-rate percent false-positives, the loud direction. Verifier independently grepped every `%` render site in app.js/PoolDetail.js/planner.js/translations.js/generated pages and confirmed every dynamic non-rate percent is excluded by class or `Math.min(100, …)`-clamped. No product file, generator, generated page, CSS or dependency touched; `package.json` diff is the one `test:serial` line; NEVER list independently re-derived clean (this only ADDS a bound to a read-only detector). New `test_audit_apy_percent_sanity.js` (22 assertions: 8 pure-helper, 6 real-Chromium DOM fixtures, 8 rendered `runAudit()` acceptance) wired into `test:serial`, auto-classified browser lane. DISCLOSED, not fixed (candidate ticket the verifier found): `app.js:2963` renders `${minApy}%+` from the unbounded user-controlled `?minApy=` param, so `?minApy=50000` would render "50000%+" unflagged — unreachable today (no audit rotation URL sets `minApy`) and a URL-echoed filter label rather than pool data. HONEST LIMITATION: per-`.pool-card` suppression scoping is validated on real-Chromium DOM fixtures, not a real grid render — today's grid shows one percent per card and it is the same value that derives the anomaly flag, so a breached-but-unflagged card beside a flagged one is unconstructible from real markup; the verifier judged this correctly disclosed and its V2/V3 mutations confirmed the branch is load-bearing. NOT RUN: the full 60+-file browser lane (5-minute foreground timebox; spec-exempt) — stated unrun, never implied green. No isolable Mixpanel funnel metric (loop quality instrumentation under the 2026-07-23 pre-traffic mandate); observable at the next heartbeat as a rendered `apy-rail-breach` count. needs human visual spot-check: NO (no render path touched) | HIGH | specs/173.md | 1 | — |
diff --git a/product-loop-kit/LOG.md b/product-loop-kit/LOG.md
index b4126cd66..4f8186823 100644
--- a/product-loop-kit/LOG.md
+++ b/product-loop-kit/LOG.md
@@ -285,3 +285,4 @@
 
 2026-07-29 | build | 171 | 1 | SHIPPED on merge — ONE commit on `claude/loop-171` + PR, merged by this run after verifier **PASS 10/10, tier HIGH** | HIGH (verifier-assigned independently, agreeing with the spec's author-guess: 269-line code diff vs LOW's 150-line cap, and the change alters the blocking semantics of `audit-app.js`'s own exit contract — the repo's red gate) | ~ (in-session: 1 build agent (Sonnet 5, 195k subagent tokens) + 1 verifier (90k)) | PICKUP: **no takeable READY item existed** — 148 reads READY on `main` but has been in flight as human-gated PR #306 since 07-26 (NEVER-list question, unanswered for the ninth consecutive run, build.md §1 says skip), and the 07-28 report's whole build order (166/168/169) plus the promoted 170 all shipped as #324-#327. Promoted 171 from evidence **measured this run, not read from a report**: `node audit-app.js` on a clean `npm ci` checkout of `main` → `6 total, 6 blocking, exit 1`. **The item had no spec** — `specs/171.md` written this run and committed in the same PR. WHAT WAS WRONG: five of those six findings are item 148's junk slugs (real, correctly red, human-gated). The sixth — `pool-prescan:mean30d-rail-breach` **P0** on `201e5f6e (balancer-v2/WSTETH-AAVE)`, `apyMean30d = 27421.09636`, 27× the rail — was a **false alarm the audit had already disproved itself**: the same run promoted that pool, rendered it as `pool-detail:201e5f6e` through the full pool check battery, and returned zero findings. Root cause traced before the spec was written: item 144's `mean30dSane` (`PoolDetail.js:161-164`) gates all five `apyMean30d` render sites (`:1298` card + `:1384`/`:1412`/`:1460`/`:1506` notes), and `grep -n apyMean30d` across all product files proves `PoolDetail.js` is the only render site — so the product handles the upstream garbage correctly. The aggregate prescan finding is built pre-render (`:690` static / `:926` pool) and never revisited, so promotion's whole purpose — "does this data suspicion show up on screen?" — was computed and thrown away. STRUCTURALLY UNCLEARABLE, which is why it rated a build: the snapshot regenerates daily from the same feed and 144's gate keeps correctly suppressing it, so `blocking > 0` becomes a constant and a genuinely new P0 arrives inside an already-non-zero count (`audit-app.js:1702` is `process.exit(blocking.length > 0 ? 1 : 0)`). WHAT SHIPPED: one shared `reconcilePrescanFindings()` (exported) called twice from `runAudit()` **after** the render loop and **after** `opts.only` filtering; an aggregate `<prefix>:<signal>` finding downgrades to non-blocking `P2` — never deleted, and its detail gains the names of the surfaces that cleared it — iff EVERY suspect for that signal was promoted, covered in this run, and rendered zero findings. The load-bearing rule is the negative one: **unverified is not clean**. That is what keeps item 148 at P1 (7 junk-slug suspects, promotion cap 4, so 3 never rendered → `allPromoted` false → untouched). MEASURED, both sides live by the verifier in an isolated `origin/main` worktree, not read from the builder: blocking **6 → 5**, findings total unchanged at 6, exit **still 1** (148 correctly still red). The verifier proved non-vacuity with its OWN mutation, deliberately different from the builder's (no-op'd `f.severity = 'P2'` rather than early-returning), and reported the sharp detail that the finding then emitted as P0 *while still carrying the "reconciled" suffix* — isolating the severity assignment as the sole cause of the count change; md5 identical either side of the mutation. Its four-mutation battery each turned exactly one new test red (`!allPromoted` → A4; `!wasRendered` → A4b + the pre-existing end-to-end A3b; `findingCount > 0` → A5; the severity no-op → A3), so no new test passes by construction. It also independently confirmed the surface-name safety by construction: `keyToSurface()` only emits colon-bearing `pool-detail:<prefix>`/`static-page:<slug>`, which can never equal the anchor's `pool-detail-360`/`-dark`/`-ko` siblings — the false-clear that would have been the dangerous bug here. `--lane=plain` 36/36; all seven `test_audit_*` files 87/87; plus `test_seo_surface_audit.js` 5/5, the eighth `require('./audit-app.js')` consumer, run by the verifier because the builder had not. **NOT RUN: the remaining 56 of the 64 browser-lane files** — exceeds the 5-minute foreground timebox; unmeasured, stated rather than implied green (170 shrank this gap, it did not close it). Scope: `audit-app.js` + two test files + the `signals/audit-findings.json` artifact every CLI run rewrites; no product file, `git diff -S APY_SANITY_LIMIT` empty, `package.json` byte-identical, no new dependency, NEVER list independently checked clean. needs human visual spot-check: NO (no render path touched). +playbook: `ci-signal-honesty` (UPDATED — new **inverted variant**: a misleading RED, where a two-stage detector discards what stage 2 concluded; adds the promoted-suspect reconciliation decision rule, the "a permanent red is a dead gate — but separate product-broken from detector-wrong" trap, and the sibling-surface false-clear trap).
2026-07-29 | build | 172 | 1 | SHIPPED on merge — ONE commit on `claude/loop-172` + PR, merged by this run after verifier PASS **15/15 tier HIGH** | HIGH (verifier-assigned independently from the measured diff: 521 added lines across `audit-app.js` (+242/-5) and `test_audit_prescan.js` (+279/-1), far over the 150-line LOW cap — the 154/157/163/166/169 precedent; NEVER list independently re-derived clean) | ~ (in-session: 1 Sonnet 5 build agent ~385k subagent tokens + 1 verifier ~127k) | **Gave `prescanStaticPages()` a link signal.** `link-target-integrity` (P1) joins the four slug-or-number signals in `PRESCAN_SIGNALS`, over the 2,181 generated HTML SEO pages the prescan already reads: (a) every query key on every owned link must be routed — for `/` links, membership in ANALYTICS_PARAMS ∪ PLANNER_PARAMS ∪ {lang} **parsed live out of `home.html:77-78`** via 169's own `loadRouterAllowedParams()` (reused verbatim, no second copy); for `/plan.html` links, where the IA router never arbitrates, membership is single-sourced from `planner.js`'s OWN `urlParams.get('<key>')` call sites by a new `loadPlannerAllowedParams()` rather than allow-listing `waitlist`/`src` by hand; (b) every pool-row anchor (`tp-pool-link`/`cp-pool-link`, item 029's classes) must target a `?pool=<id>` URL, never the bare origin or a grid URL; (c) every owned path link must resolve on disk (`<p>` / `<p>.html` / `<p>/index.html`) — the orphan-link class items 030/031/032/033 make live by dropping pages that 023's cross-links still point at. Detector only — no product file, generator, page, sitemap, OG asset or dependency; `package.json` byte-identical by structured check. **PROMOTED, not taken: NO takeable READY item existed** — 148 reads READY on `main` but is in flight as human-gated PR #306 for the **TENTH consecutive run** (build.md step 1's in-flight check caught it before any work started). Alternatives ruled out on evidence, not assertion: a full `node audit-app.js` run (29 surfaces, 6 findings / 5 blocking, **all** the known 148 junk-slug class — nothing new); `specs/010-diagnosis.md`'s ranked fixes (011-015 all shipped); the newest report's opportunities (`reports/2026-07-28.md` → 166/168/169, all three shipped 07-28); and 170/171's own candidate-ticket sections (test-harness polish, lower value). **EVIDENCE, measured before the spec was written** (plain fs+regex over the committed pages, no network): the generated HTML surface emits **~41,300** same-origin links, **4,989** of them `/?pool=<id>` — the exact hop an SEO lander must make to reach the north-star CTAs — and not one signal had ever looked at a link. Item 169 shipped this same signal for the two generated TEXT surfaces a day earlier and explicitly deferred HTML (`specs/169.md:41`, "one surface family per item"); this is that follow-on, on a surface 1,000× larger by file count. **Fourth instance** of the documented pattern 148 → 159 → 166 (a checker's signals come from the last bug, so the next bug lands in the class nobody has been bitten by yet). **Ships a gate, not a repair — stated as such**: all three rules are GREEN on today's committed surface (0 suspects), so the value is regression coverage, and the true negative was therefore proven non-vacuous rather than reported as health. **Three traps found and encoded** (each cost real measurement): (1) `href="…?a=1&amp;b=2"` is CORRECT HTML — splitting the raw attribute on `&` invents a phantom `amp;src` key on 2,183 pages; pinned by test and proved load-bearing when the verifier deleted the decode branch and watched 2,182 real pages fire. (2) `?pool=` LIVENESS must never be approximated with `data/pools-snapshot.json`: the snapshot is the ≥$10M-floored 745-pool set while token pages use a $100K floor over the full ~16,000-pool feed, so snapshot-validation reports **4,233 of 4,989** links dead, all false; measured against a live DefiLlama fetch the same day the true number is **15** (0.3%, 14 pages), self-healing on the next daily CI regen — left explicitly out of scope as an online check. (3) porting a signal across surface families means re-deriving the RULE from the new surface's semantics, not copying the predicate: rule (a) splits by link path on HTML because the router only arbitrates `/`. **VERIFIER'S OWN WORK** (re-derived by execution, not read from the notes): its own true-negative run (2181 scanned, 0 link-target-integrity suspects); non-vacuity proven on **REAL bytes rather than fixtures** — it copied the real `tokens/usdc.html` and broke it three ways (a `?pool=` → `?search=lido`, a real `tp-pool-link` retargeted to `?token=USDC`, a real cross-link broken), each firing exactly its own rule; four of its own mutations, deliberately DIFFERENT in kind from the builder's (block deletions rather than short-circuited conditions), each turning exactly the expected tests red (7/6/3 failures) with `md5sum audit-app.js = c4e93b8ffccf2f8b53d4a13b7a243431` byte-identical restores after every cycle; its own runtime measurement (285ms → 479ms best-of-3, 1.68×, inside the ≤2× budget); its own full `node audit-app.js` with `AUDIT_OUT` redirected to scratch (29 surfaces identical to HEAD's set both directions, findings array byte-equal, `bySignal` gains the key at 0). **TWO ACCURACY DEFECTS THE VERIFIER CAUGHT IN THE RECORD, corrected before the commit rather than shipped**: (1) the committed `signals/audit-findings.json` recorded `playwright.version 1.56.1` and the notes explained it as `npm ci` resolving a cached version — WRONG: `npm ci` installed 1.61.1 (matching `package.json`'s `^1.61.1`); the 1.56.1 came from an **operator** audit run made earlier in the session, before `node_modules` existed, resolving playwright from the `/opt/node22` global fallback. A committed snapshot claiming a version the repo neither pins nor installs is exactly what `ci-signal-honesty` is about, so the artifact was REGENERATED with local playwright present (1.61.1, findings 6 unchanged, `link-target-integrity` 0) and the notes corrected in place. (2) the spec's "tests run in the plain lane" clause is false about a PRE-EXISTING fact — `test_audit_prescan.js` is browser-lane because of its own pre-existing Chromium criteria 3/4/6/7; the new cases are genuinely browser-free. Both recorded as operator addenda in `172-notes.md` rather than silently patched. The verifier also disclosed one caveat it judged non-blocking: `test_audit_prescan.js:448` holds a verbatim copy of `home.html`'s `ANALYTICS_PARAMS` declaration as a `String.replace` anchor guarded by an assert that fails loudly — a test-only anchor, not a second allow-list that can drift; a minimal anchor would remove the ambiguity if that test is touched again. Tests: `node test_audit_prescan.js` 29/29 · `run-tests.js --lane=plain` 36/36 · all 8 `require('./audit-app.js')` consumers individually green (`test_audit_text_surfaces` 32/32, `test_audit_app` 3/3, `test_seo_surface_audit` 5/5, `test_audit_pool_prescan` 14/14, `test_audit_runner`, `test_audit_planner_surface` 9/9, `test_audit_planner_flow` 11/11). **The remaining ~56 non-audit browser-lane files were NOT run — 5-minute timebox; stated unrun, never implied green.** Measurement: none — loop tooling under the 2026-07-23 pre-traffic mandate; observable at the next heartbeat as `prescan.bySignal['link-target-integrity']`. needs human visual spot-check: NO (no render path touched). Model note per the 2026-07-13 precedent: this session ran as Opus 5 (operator/judgment only); product code was written by a dispatched Sonnet 5 build agent and judged by a separate verifier subagent — flagging the tiers actually used rather than claiming compliance with a routine config a session cannot change itself. +playbook: product-audit (UPDATED — check 10 gains its HTML half: the path-dependent allowed-param rule, the `&amp;` phantom-key trap, the snapshot-population trap with both measured numbers, and the on-disk-existence sub-rule) · detector-signal-coverage (UPDATED — the 169 → 172 row, plus two rules: write down the family you deferred WITH its size, and re-derive a ported rule from the new surface's own semantics)
+2026-07-29 | build | 173 | 1 | SHIPPED on merge — ONE commit on `claude/loop-173` + PR, merged by this run after verifier PASS **9/9 tier HIGH** | HIGH (verifier-assigned independently from the measured diff: `audit-app.js` +111, `test_audit_apy_percent_sanity.js` +358, `package.json` +1 = 471 lines, ~3× over the 150-line LOW cap — the 154/157/163/166/169/172 precedent; no single enumerated HIGH trigger applies, so the tier rests on the size cap plus NORTH_STAR's "when in doubt → HIGH"; NEVER list independently re-derived clean) | ~ (in-session: 1 Sonnet 5 build agent ~242k subagent tokens + 1 verifier ~132k) | **Taught `audit-app.js`'s RENDERED leg the trust rail.** `scanNumbers()` — the only number check on every rendered surface — is magnitude-only (`|value| >= 1e11`, tuned to item 122) and never referenced `APY_SANITY_LIMIT`, while the constant IS enforced on both non-rendered legs (`:323` text prescan, `:1010`/`:1017` pool prescan). Consequence, measured not asserted: item 144's real P0 (`apyMean30d = 36452.38798`, 36× the 1000% rail, on a pool whose `totalApy = 0.24` kept it unflagged) is only 3.6e4 and passed clean — it was found BY HAND and was still invisible to the scanner this morning. New `apy-rail-breach` (P0) implements `playbooks/product-audit.md` class 1's decision rule verbatim: a browser-side `collectApyPercentCandidates()` (TreeWalker over rendered text nodes, script/style excluded) records every `"<number>%"` with two DOM facts — is it in a non-rate container, and does its SCOPE carry the product's own anomaly marker — and a pure exported `filterApyRailBreaches(candidates, limit)` makes the single rail decision in Node. **PROMOTED, not taken: NO takeable READY item existed** (148 reads READY on `main` but is in flight as human-gated PR #306, **eleventh consecutive run**; build.md §1's in-flight check caught it before any work started). Rivals ruled out on evidence, each checked rather than assumed: `specs/154-notes.md:250`'s "`test_audit_app.js` case 2 is red on origin/main — a positive control that cannot fail is worse than no test" — **STALE, verified false this session, 3 passed 0 failed**, item 155 rewrote case 2 after 154 observed the red, so no ticket; 154's sampling-rotation follow-up (largely superseded by 157+171+172's prescan, which now scans all 2,181 pages and promotes suspects); `specs/172-notes.md:528`'s `?pool=` liveness (needs network, 15/4,989 dead, self-healing in 24h); `specs/170-notes.md:163`'s harness polish; `specs/010-diagnosis.md` (011-015 all shipped); `reports/2026-07-28.md`'s opportunities (166/168/169, all shipped 07-28); and a fresh full `node audit-app.js` (29 surfaces, 6 findings/5 blocking, every one the known 148 class). **Ships a GATE, not a repair, and says so**: every rate render site reachable today is already railed (144's `mean30dSane`, 145's KPI bound, 165's projection gates, `apyBase`/`apyReward` transitively via the anomaly flag), so the expected and observed result is a true negative — **proven non-vacuous rather than reported as health**: a REAL Chromium render with `kpis.apyMomentum = 5000` on an anomaly-unflagged pool puts "5,000%" on screen via `PoolDetail.js`'s rate-momentum note and fires a genuine P0 quoting it verbatim. `apyMomentum` is the ONLY field bounded merely transitively (145 nulls it when a series ENDPOINT breaches), which is precisely why a positive control exists at all — the generalisable move now in the playbook. **The design decision worth arguing with**: the predicate is an EXCLUDE-list (every rendered `%` is a rate figure unless its container is `.tvl-trend-note`/`.gp-item-fill`), not an allow-list of rate containers — an allow-list only ever sees rate sites someone remembered to register, which is 148 → 159 → 166 → 172 applied to the checker itself; accepted cost is that a future NON-rate percent false-positives, the loud direction to fail in. **The trap that makes the rule structural rather than numeric**: `.tvl-trend-note` (`PoolDetail.js:1516`) renders a deposit-base CHANGE — a 20× pool shows a legitimate `"1,900%"`, gated only on `|tvlTrend| >= 0.25` with no upper bound — so there is no threshold above which a TVL change becomes wrong and a blanket "any % > 1000" check would be wrong, not merely noisy. Suppression is DOM-scoped (nearest `.pool-card`, else page) so a legitimately flagged anomalous card cannot mask an unflagged breach in another card. **VERIFIER'S OWN WORK** (re-derived by execution, not read from the notes): its own isolated `origin/main` worktree with `node_modules` symlinked and the snapshot `diff`-confirmed byte-identical before comparing — working tree **6 findings / 5 blocking, `apy-rail-breach` 0** vs `origin/main` **6 / 5** (and `grep -c collectApyPercentCandidates` = 0 there, confirming the check genuinely does not exist on the base); its own execution of all ten test files; and THREE of its own mutation cycles deliberately DIFFERENT IN KIND from the builder's seven targeted condition-defeats — whole-block deletion of both helpers (module-load `ReferenceError`, every `audit-app.js` consumer red), inverted exclusion set (`EXCLUDED = '*'` → exactly 2 red: the rendered positive control and the not-excluded fixture), and a naive whole-body `innerText.match()` reimplementation of the collector (exactly 8 red: the 4 scoping/exclusion fixtures, criterion 3, both criterion-4 sub-cases, criterion 5's KO suppression) — each restored and `md5sum`-verified at `77b87ebdfdde32a346b72d3a23cc70f6`. It also independently grepped every `%` render site across `app.js`/`PoolDetail.js`/`planner.js`/`translations.js`/generated pages and confirmed each dynamic non-rate percent is either excluded by class or `Math.min(100, …)`-clamped — and in doing so found **ONE CANDIDATE TICKET, disclosed not fixed**: `app.js:2963` renders `${minApy}%+` from the unbounded user-controlled `?minApy=` query param, so `?minApy=50000` would render "50000%+" neither excluded nor anomaly-flagged — unreachable today (no audit rotation URL sets `minApy`) and a URL-echoed filter label rather than pool data, so fixing it inside this item would be scope creep. Tests (verifier's own runs): `test_audit_apy_percent_sanity.js` 22/22 · `test_audit_runner` 9/9 · `test_audit_app` 3/3 · `test_seo_surface_audit` 5/5 · `test_audit_prescan` 29/29 · `test_audit_pool_prescan` 14/14 · `test_audit_text_surfaces` 32/32 · `test_audit_planner_surface` 9/9 · `test_audit_planner_flow` 11/11 · `run-tests.js --lane=plain` 36/36. **The full 60+-file browser lane was NOT run — 5-minute foreground timebox, spec-exempt; stated unrun, never implied green.** HONEST LIMITATION, judged by the verifier as correctly disclosed rather than an unmet criterion: per-`.pool-card` suppression scoping is validated on real-Chromium DOM fixtures (`page.setContent`, not jsdom) rather than a real grid render — today's grid renders one percent per card and it is the same value that derives the anomaly flag, making a breached-but-unflagged card beside a flagged one unconstructible from real markup. Artifact honesty (the 172 precedent): `signals/audit-findings.json` regenerated once with the locally installed playwright, verifier-confirmed `1.61.1` matching what `package.json` resolves. Scope: `audit-app.js` + one new test + one `package.json` test-list line + the signals artifact + docs; no product file, generator, generated page or CSS; no new dependency; `package-lock.json` untouched. Measurement: none — loop quality instrumentation under the 2026-07-23 pre-traffic mandate; observable at the next heartbeat as a rendered `apy-rail-breach` count. needs human visual spot-check: NO (no render path touched). Model note per the 2026-07-13 precedent: this session ran as Opus 5 (operator/judgment/spec only); product code was written by a dispatched Sonnet 5 build agent and judged by a separate Sonnet 5 verifier subagent — flagging the tiers actually used rather than claiming compliance with a routine config a session cannot change itself. +playbook: `product-audit` (UPDATED — §Automatability's "Known blind spot (144)" struck through and CLOSED, with the three reusable rules: structural-not-numeric, exclude-list-not-allow-list, DOM-scoped-not-page-global, plus the anomaly-marker selectors for any future check) · `detector-signal-coverage` (UPDATED — the new axis: the gap can be a missing LEG of a surface you already cover, with the one-line `grep -n <RAIL_CONSTANT> <detector>` probe; that two checks over one field are not redundant when they answer different questions; and that the field to hunt for a positive control is the one bounded only TRANSITIVELY)
