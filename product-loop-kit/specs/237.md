# Spec 237: exactly one "Garden this pool" primary per pool-detail page

## ADDENDUM 2026-08-06 — contract unmet ON THE RENDER (audit C3)
The human's preview screenshot (dark, WEETH) shows the calculator block closing with a FULL blue
"Garden this pool → ~$1,125 in 5y" + bordered protocol button — visually identical weight to the hero
pair, despite round 3c's notes claiming a "slim echo". Acceptance upgraded: the echo must be measurably
SECONDARY — computed background ≠ primary fill, height ≤ the hero primary's −25%, or text-level link;
a class-count assertion alone can pass while the page renders two primary pairs, so the test asserts
computed visual weight, not class names.


## Evidence
UX audit F2: the hero CTA pair ("Garden this pool →" + "Start Earning on <protocol> ↗") renders twice —
hero card AND the mid-page `repeatCtaHeading` block (`PoolDetail.js:1287`, "Ready to start this
garden?"), visible in the human's pool-detail screenshot. Two full-strength primary blocks = item 008's
anti-pattern (one action per intent level) on the north-star surface; `pool_click{garden_cta}` fires
from duplicate render sites, muddying per-site attribution.

## Hypothesis
One primary CTA location (hero) + one slim contextual echo at the post-calculation moment (carrying the
computed projection — the 025 value, e.g. "→ ~$1,274 in 5y") will clarify the page's action hierarchy
without losing the calculator's conversion moment. Confidence: supported (008 precedent + human's
"all over the place" directive). 
- Instance of: duplicated primary-action blocks (008's class).
- Class closed: pool-detail yes; audit other surfaces for the same pattern and state the count in notes.

## Change
`PoolDetail.js`: the page renders exactly ONE primary-styled "Garden this pool →". The calculator block's
moment keeps its projection as a SECONDARY-styled contextual link (builder decides: scroll-to-hero or
direct navigate — same destination as hero, same `pool_click{source=garden_cta}` event with an added
`cta_position` property distinguishing hero vs calculator). "Start Earning on <protocol> ↗" appears once
(hero). EN+KO strings via translations.js. OUT of scope: CTA copy redesign, hero layout, any style-token
change (Quiet classes only, after #393).

## Acceptance criteria
- [ ] Rendered page (`?pool=<live id>`, both entry paths url_direct + card_click) contains exactly ONE
      element with the primary CTA class for garden_cta and ONE protocol link — counted from the DOM,
      360/768/1280, EN+KO, light+dark.
- [ ] The calculator echo navigates to the same planner deep-link as the hero CTA (assert the URL params
      match, incl. capital/fm — the 143 honesty contract unchanged).
- [ ] `pool_click` fires with `source=garden_cta` from BOTH positions, now carrying `cta_position`
      (`hero` | `calculator`) — north-star query (`source ∈ {garden_cta, protocol_link}`) unaffected;
      test asserts event payloads on both clicks.
- [ ] Non-vacuity: re-add a second primary-classed garden CTA → the count assertion goes red; restore.
- [ ] `test_northstar_cta_fires.js` stays green.

## Measurement
Metric: `pool_click` by `cta_position` (new property) — gate: ≥30 real exercised pool-detail events
(unchanged 123 gate) · Backstop 60d from ship → UNEXERCISED. Decision: attribution clarity is the
deliverable; no rate claim.

## Risk tier (builder's guess)
HIGH — north-star surface markup + analytics payload addition.

## Open questions
None.
