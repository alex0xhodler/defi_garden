# Spec 272: Loop efficiency reset — verdict semantics, ceremony caps, pickup arithmetic, clock, Impact+vitals

Impact: guardrail — loop efficiency: tokens/ship and attempts/ship stop climbing; targets falsifiable via weekly vitals

## Evidence
Human directive, 2026-08-11 live session, after reviewing the post-v2 numbers:
- avg attempts per item **0.97 → 1.72** pre/post loop-v2
- 3-attempt runs **1.8% → 28%** of runs
- one day of **100% process/parked spend** (zero product impact shipped)
- **6 open PRs**, of which **4** were duplicates or parks
Human's words: *"make this loop ship, ship well, continuously looking at impact, sustainable and efficient sota grade"* / *"precisely directed at little details ... than prose and fluff overengineered"*.

## Hypothesis
We believe the attempt/token climb is caused by three unbounded quantities — verifier rounds spent on prose findings, artifact length, and process-item share of the queue — and that naming a number for each stops the climb without weakening any check. Confidence: supported (each number is derived from a measured run above, and each has a named counterexample it must not break: 228 must ship round 1, 263 must still park).
- Instance of: loop-process invariants enforced by prose a run is trusted to remember (population = every imperative directive in build.md + heartbeat.md + verifier.md; 53 remaining MEMORY-only per row 271).
- Class closed by this item: **no** — 272 adds 5 more prose rules to that population, none script-enforced. Residue tracked by the already-open row **271**; no new ticket filed (filing one would itself breach the process-item cap this spec writes).

## Change
Prose-rule class. Text-only edits to five kit files, no scripts, no product code:
1. `agents/verifier.md` — BLOCKING vs AMEND resolution-type semantics + oscillation rule; ceremony caps as a new numbered check (FAIL category `ceremony`, fixed by DELETING text); output shape `PASS | PASS-WITH-AMENDS | FAIL` with AMENDs carrying exact replacement text.
2. `prompts/build.md` — §0 caps table + wall clock + Impact field; §1 pickup arithmetic; §4/§5 aligned to the new verdict types and caps.
3. `prompts/heartbeat.md` — §2d vitals line + breach thresholds; §4 process-item filing cap.
4. `specs/_template.md` — `Impact:` field with allowed forms + pointer to the caps table.
5. `NORTH_STAR.md` — the standing-decision entry recording the human's five rules.
OUT of scope: any executable check for these rules (that is 271's territory), any change to the extension-attack / criteria / new-check / instrumentation / scope / deviation checks' standards — the reset re-scopes their CONSEQUENCES, not their teeth. They found 4 real bugs this week.

## Acceptance criteria
- [ ] NORTH_STAR.md "Standing decisions" ends with the 2026-08-11 LOOP EFFICIENCY RESET entry, all five numbered rules present, numbers matching the Evidence above.
- [ ] `agents/verifier.md` defines BLOCKING and AMEND by RESOLUTION TYPE (code behavior vs claim/residue text), states AMEND consumes no attempt and ships in-round, and states the oscillation rule (BLOCKING introduced by the previous round's fix → park immediately).
- [ ] `agents/verifier.md` output block reads `VERDICT: PASS | PASS-WITH-AMENDS | FAIL` and separates BLOCKING from AMENDS, AMENDs requiring exact text.
- [ ] The ceremony-caps table appears in `agents/verifier.md` and `prompts/build.md` with IDENTICAL numbers (spec 80/150 · notes 60/200 · PR 40/100+quiz · LOG 300 chars · non-vacuity 1/gate-file LOW, 1/rule HIGH · rounds 1/2 · test:code 2:1/3:1), and over-cap is stated as FAIL category `ceremony` resolved by deleting text in both.
- [ ] `prompts/build.md` §1 states the >1.0 process-vs-product pickup margin, the ONE-process-item-in-flight cap, and cites the 263-vs-265 backtest; `prompts/heartbeat.md` §4 mirrors it as a filing cap.
- [ ] `prompts/build.md` §0 states the wall clock (T+10 / T+75 / T+95 / T+120) and the three terminal states MERGED / PARKED-with-recipe / SKIPPED (<20k tokens).
- [ ] `prompts/heartbeat.md` carries the vitals line in the exact fixed format, says it prints in EVERY heartbeat report, names its inputs (LOG.md + open PRs), and lists the three breach thresholds with the at-most-ONE triage item rule.
- [ ] `specs/_template.md` carries the one-line `Impact:` field with its allowed forms and a pointer to the caps table; both build.md and verifier.md require the PR body's first line to repeat it verbatim.
- [ ] This spec ≤80 lines, `272-notes.md` ≤60, `272-pr.md` ≤40, the LOG line ≤300 chars — the patch obeys the caps it writes.
- [ ] Tests: `node product-loop-kit/test_pr_orphan_detector.js` and `node product-loop-kit/test_item_inflight_check.js` still exit 0 (neither file touched — prove via `git diff --stat`).
- [ ] Non-vacuity: n/a — no new gate file and no new executable rule; every rule here is prose read by an agent, which is precisely the residue row 271 tracks.
- [ ] Population: the caps' consistency is checked across all three files that state them (verifier.md, build.md, NORTH_STAR.md), not only the one edited last.

## Measurement
Metric: median tokens/ship, median attempts/ship, first-round ship rate, product:process ship ratio — all read off the §2d vitals line. Gate: build-leg calendar-verifiable now (the rules are in the files); the VALUE leg opens after ≥1 full week of vitals lines exist to compare against the 0.97→1.72 / 1.8%→28% baseline. Decision rule: keep if attempts/ship trends back toward ~1.0 and process share stays ≤25% over two weeks; if attempts/ship keeps climbing, the caps are the wrong lever and this reverts to a human review of the verifier's finding mix.

## Risk tier (builder's guess — verifier assigns independently)
LOW — loop-tooling prose only. Zero product files, zero deps, zero scripts, no trust rail touched.

## Open questions
None. The human directed all five rules and the numbers; the operator transcribed them, and the human edits or vetoes the NORTH_STAR entry directly.
