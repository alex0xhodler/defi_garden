# Spec 274 (renumbered from 266 — same collision): P0 — grid-view APY numerals (⚠ anomalous and large) clip at the card edge; daily-USD line truncated

Impact: guardrail — pool-card numeral legibility on the sacred ?token=/?chain= grid-view surface

## Evidence
Human live report + screenshot, 2026-08-12 session: grid view, dark, desktop (~1280+), "I'm feeling
degen" / high-APY result set. Visible on ONE screenshot: `WETH-CBBTC 705.51` clipped at the card's
right edge (missing "%"), `IDAI-IUSDC-IUSDT ⚠ 3,385.12%` with its `$92.74/da` line cut,
`USDC-VELVET ⚠ 128,…` cut mid-number, `USDC-CBBTC 215.84` cut, `$19.33/c` and `$0.10/d` truncated.
Human verdict verbatim: "anomalous apy in grid view is badly positioned in the card, and in general
terrible experience in grid view."

## Hypothesis
The grid card's APY block inherits the 246 `nowrap` discipline without a containment rule, so a wide
numeral overflows the card's padding box and gets clipped by the card boundary — the SAME class as
246 → 260 (a `nowrap` numeral cell with no width discipline relative to its container), now the
grid-card instance, and exactly what item 264 (containment predicate, filed 2026-08-11) predicted the
guard could not see: neighbour-intersection has no neighbour here, the numeral escapes into the card
edge. Confidence: supported (screenshot; measure the real boxes before coding).
- Instance of: numeral-track-escape class (`playbooks/sibling-grid-column-alignment.md` +
  `numeral-cell-track-escape.md` on branch #429 — read both).
- Class closed by this item: grid-card containment only. Item 264 (test-side containment predicate)
  is the guard for the whole class — build 264's check INTO this item if ≤ ~60 lines, else leave 264
  READY.

## Change
Smallest layout change that CONTAINS every numeral inside its card: let the APY figure wrap to its
own line below the symbol row (or scale down within a bounded range) when it exceeds the available
track — numerals are never ellipsized and never clipped (check H, #428). ⚠ stays glued to its number
(246's fix must not regress). Keep the anomaly ⚠ + demotion rails untouched. NO redesign of the card
— the human's broader "terrible experience in grid view" verdict is 267-adjacent design work and
human-gated; this item only makes every number fully visible.

## Acceptance criteria
- [ ] Rendered Playwright, grid view, fixture pools including: a 6-digit anomalous APY (128,000%+),
  a 4-digit APY (3,385.12%), a 3-digit APY, and a normal 2-digit APY — at 360/768/1280/1540 ×
  light/dark: every `.pool-apy-*` numeral box ⊆ its card's content box (containment, both edges), no
  clipping (`scrollWidth <= clientWidth` on the numeral's clipping ancestor), and the daily-USD line
  fully visible.
- [ ] ⚠ and its number share a line (246 regression guard stays green: `test_card_numeral_wrap.js`
  18/18).
- [ ] Normal cards (2-digit APY): zero pixel drift vs baseline worktree across the 16 view×theme×
  viewport combos (the 260 technique) — the fix may only change cards whose numeral would overflow.
- [ ] Non-vacuity: revert fix → containment assertion RED on the anomalous fixtures; restore → green.
- [ ] Population: numeral classes scanned derived from the DOM (coverage assertion from #428 — a
  class scanned zero times fails the run).

## Measurement
No funnel claim. Build-leg only: keep if containment stays green on the next 2 audit ticks.

## Risk tier (builder's guess — verifier assigns independently)
HIGH — edits the grid card template every `?token=`/`?chain=` URL renders (sacred surface). Zero-drift
on normal cards is the safety proof.

## Open questions
None for the containment fix. The grid-view EXPERIENCE redesign ("terrible experience") is a separate
design increment: screenshot-first, human-approved (2026-08-05 bar) — do not attempt it here.
