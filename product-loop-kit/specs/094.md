# 094 — TICKET: checkout CTA set (rescoped per redteam O1/O2/O7)

Status: BLOCKED on human answers Q3/Q4/Q5 (label set, "free", CTA count) + sequenced with 093b (shared
surface, one window reset — redteam O5) · Risk: HIGH (north-star funnel step; NEVER-tier honesty rails)
· Score: 7.8

## Report (human)

"…make the onboarding change accordingly as well as the checkout options, make join the waitlist, start
saving or get a free savings card."

## Redteam-corrected interpretation (operator inference — human confirms via Q3-Q5)

The "or" list most plausibly enumerates the user-facing CHOICE SET, where "join the waitlist" and "get
the savings card" are the SAME choice (the card funnel). Shape: exactly TWO controls, one action each
(008's one-action-per-intent-level, whose window runs to 07-24):

1. **Primary — card CTA → waitlist modal** (the north-star funnel). HONESTY RULES (NEVER-tier,
   2026-07-12 decision): label may NOT promise receipt of a nonexistent product — no bare "Get a free
   savings card". Candidates for the human: "Join the card waitlist" · "Get early access to the savings
   card" (access exists; the card doesn't yet). The word **"free" ships only if the human confirms the
   user-side fee structure** (business model: "we pocket card fees"). 070's disclaimer stays visible.
2. **Secondary — "Start saving"-intent control → the plan's IN-APP pool-detail page** (education
   surface, existing garden_cta instrumentation) — NEVER a direct protocol deposit link (redteam O7:
   education-not-advice). Label must describe what it does; candidates: "See this plan's pools" ·
   "Start saving" only if the human owns that positioning.

## Human questions (batch report; block the build)

- **Q3**: Two controls as above, or did you want three? (Three = two controls opening the same waitlist
  modal, which 008 exists to prevent — recommend two.)
- **Q4**: May the card CTA say "free"? What does the user actually pay?
- **Q5**: Is "Start saving" as a label acceptable given it navigates to the plan's pool page (not an
  actual deposit "with us"), or prefer a descriptive label?

## Instrumentation

Existing waitlist events segment the new framing (pitch_variant pattern); the secondary control reuses
garden_cta conventions with a checkout source. Declared window: shipping voids 061/065 (→07-26) and
restarts a 14d window at merge — or hold to 07-26 (human's call, same as 093b).

## Archetype-awareness

Per 093a's table (which must exist first — redteam O5). Build adjacent to 093b, one shared window reset.
