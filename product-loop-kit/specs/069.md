# Spec 069: First ready-to-post spotlight packs (live, committed) — with degen-haircut-honest numbers

## Evidence
- `reports/2026-07-13.md`: traffic is DECAYING (session_start 07-09 **167** → 07-12 **21**); "distribution — the weekly theme — is the binding constraint, and it's the ONLY lever that fills the (verified-sound) card funnel"; bullet 5: "060 spotlight engine SHIPPED but output uncommitted + unposted"; Q-f explicitly poses "pre-build the first N complete spotlight packs (live numbers + working garden URL + tweet draft + Canva field values) so your posting is one-tap" as one of the two next-build options — the other option (064, attribution plumbing) has since SHIPPED, leaving this as the remaining fork. Producing packs is safe under either possible human answer to Q-f: if the human is only Canva-blocked, ready packs still make posting one-tap; nothing here forecloses their choice.
- NORTH_STAR.md standing decision 2026-07-12 (distribution channel, human interview Q1): "Loops build the per-spotlight data/copy/asset pack (live numbers through trust rails, a working example-garden share URL, tweet draft, template field values, share-card PNG); the Canva template itself + posting = human-owned." Pack production is already loop-assigned by the human — this item does not need a new mandate.
- `spotlights/` does not exist in the repo (verified this session). 060 shipped the generator; 064 added `src=x_spotlight`/`ref` attribution + the cadence doc; 066 added `foreverAmt`. Zero packs have ever been produced — the human has nothing to post.
- Sandbox network is OPEN for Node/curl (standing decision 2026-07-12; re-verified this session: `curl https://yields.llama.fi/pools` → 200, 10.5MB). Committing real generator output is the established convention (tokens/chains/og), and 066's verifier precedent FAILs false "network-blocked" claims.
- **Code-confirmed honesty gap (found this session, blindspot pass):** the pack's `foreverAmt` is computed from **headline** APY (`generate-spotlight.js:375`, `foreverNumber(monthly, apy)`), but the live planner computes every Bloom forever-number from the **haircut-applied** APY — `planner.js:1354-1357` (`apy = degenHaircut ? rawApy / 3 : rawApy`), fed to `foreverNumber` at :1470/:4058 etc.; `PERSONAS.degen.degenHaircut = true` (:544). 060-notes.md's own live smoke test auto-picked a **degen**-persona pool (orca-dex SOL-USDC, 31.24% APY) — so the first real pack would tweet a "runs your sub forever" claim whose capital figure is ~3× smaller than what its own linked garden renders on open. CLAUDE.md degen-honesty rail: "the Degen LPs persona projects at a ⅓ haircut of headline APY **and says so**." Broken beats persuasion: this must be fixed before any pack is posted.

## Hypothesis
We believe committing the first 3 ready-to-post, protocol-diverse spotlight packs — with degen-persona numbers projected at the same ⅓ haircut the live planner applies — will let the human start the weekly X-spotlight cadence immediately (one-tap: pick a pack, fill the Canva template, post), because everything else in the chain (generator, attribution, waitlist funnel) is verified sound and the only missing artifact is the packs themselves. Confidence: supported (report 2026-07-13 + standing decision 2026-07-12; the haircut gap is a code-confirmed fact, not a guess).

## Change
**In scope:**
1. `generate-spotlight.js` — degen-haircut honesty (mirrors `effectiveApy`, `planner.js:654-658`; STRENGTHENS a trust rail, never weakens):
   - Compute `effectiveApy = persona === 'degen' ? apy / 3 : apy` in `buildPack`. `foreverAmt`/`foreverAmtStr` now derive from `effectiveApy` (identical result for stable/rwa — haircut only bites degen).
   - `pack.json` + `canvaFields` carry BOTH numbers, clearly named: headline `apy`/`apyStr` (the live fact, unchanged) and new `effectiveApy`/`effectiveApyStr` (the projection basis). Never delete/rename existing fields — additive only.
   - Tweet draft (degen persona only): the "runs your sub" claim states the haircut plainly, e.g. "projected at ⅓ of today's rate — farm rates decay" (same honesty stance as the planner's `degenHaircutNote`). Stable/rwa tweet text byte-identical to today's.
   - Share-card PNG (degen persona only): "Live APY <headline>" stays (it IS the live rate); add one small caveat line with the projected (⅓) rate so the card never implies the headline rate is the projection basis. Stable/rwa cards byte-identical logic (no new line).
2. Produce + commit the first **3 packs** via live `npm run spotlight` runs (`--pool <id>` for explicit picks), chosen from `rankCandidates` on live data:
   - 3 **distinct protocols** (each post tags a protocol — collaboration outreach needs variety), all small-enough per the existing Curve-ceiling rail.
   - Aim for one per persona class (stable / rwa / degen) so the human can choose risk-framing and the degen haircut honesty is demonstrated in a real artifact. If a class has no qualifying candidate on live data, take the next-best distinct-protocol candidate and document it in 069-notes.md.
   - Committed output: `spotlights/<slug>/pack.json` + `card.png` ×3, plus regenerated `spotlights/CADENCE.md` (covered=3, next candidates listed).
3. Tests — extend `test_spotlight.js` (or a sibling `test_spotlight_packs.js` added to the npm test chain):
   - Fixture-driven: a degen-persona pool's pack has `foreverAmt == monthly*12/(apy/3/100)` (tolerance), tweet contains the haircut wording; a stable-persona pool's pack is unchanged vs today's semantics (foreverAmt from headline == effective).
   - Committed-output validation (structural, not live-rate-dependent): every `spotlights/*/pack.json` parses; recorded `apy ≤ APY_SANITY_LIMIT`; recorded `tvl ≥ DEFAULT_MIN_TVL`; `shareUrl` parses with `goal/monthly/pace/src=x_spotlight/ref` present and `foreverAmt` self-consistent with the pack's own recorded rate+persona; sibling `card.png` exists, non-empty, 1200×630.

**Out of scope:**
- Posting, Canva video production (human-owned per standing decision 2026-07-12).
- Any planner.js / app.js / UI change (the haircut fix is generator-side only — the planner already does this correctly).
- Changing pool-selection ranking, trust-rail thresholds, or the small-protocol ceiling.
- EN/KO translations — generator/CLI output only, no user-facing app strings (060 precedent).

## Acceptance criteria
- [ ] Degen-persona pack: `foreverAmt` derives from headline/3; tweet + card carry an explicit haircut statement; `pack.json`/`canvaFields` expose both `apyStr` (headline) and `effectiveApyStr` (projection basis).
- [ ] Stable/rwa-persona pack: `foreverAmt`, tweet text, and card rendering semantically unchanged from pre-069 output (haircut is a no-op there).
- [ ] The number a degen pack claims matches what its own `shareUrl` garden renders: verified by computing `foreverNumber(monthly, blended/3)` against the pack's `foreverAmt` for the same pool (fixture-driven; the rendered-UI round trip for chain/token share URLs is already gated by `test_spotlight_url.js` — keep it green).
- [ ] `spotlights/` committed with 3 packs from 3 distinct small-enough protocols (each `pack.json` + valid non-empty 1200×630 `card.png`) + regenerated `CADENCE.md` showing covered=3.
- [ ] All 3 packs' recorded numbers clear trust rails (`apy ≤ 1000`, `tvl ≥ $10M`); `shareUrl` carries `goal/monthly/pace/chain/token/src=x_spotlight/ref`.
- [ ] `npm test` chain includes the new/extended assertions and is green (each test file timeboxed 5 min; pre-existing sandbox-blocked failures documented, not caused).
- [ ] No trust rail weakened: `APY_SANITY_LIMIT`/`DEFAULT_MIN_TVL`/Curve ceiling values untouched; the haircut is applied on top of, never instead of, existing rails.
- [ ] No new dependencies.

## Measurement
Once the human posts a pack, spotlight-origin funnel activity is attributable end-to-end via 064's plumbing: `waitlist_opened`/`plan_created` with `source=x_spotlight` + per-pool `ref`. Window: 14 days from first post. This item's own success gate is binary and immediate: 3 honest, verifier-passed packs exist in the repo for one-tap posting. Decision rule: if the human posts and any pack's `ref` shows `share_link_opened`/`plan_created`/`waitlist_opened` activity, the cadence continues with the next candidates from CADENCE.md; no conversion claim below 30 events (NORTH_STAR minimum sample).

## Risk tier (builder's guess — verifier assigns independently)
HIGH — touches degen-haircut semantics (a named trust-rail surface; the change strengthens it, but "when in doubt → HIGH" governs) and produces the outward-facing hero distribution artifact the human will post publicly. No router/sitemap/config surface touched.

## Open questions
None that change the architecture. Q-f (reports/2026-07-13.md) remains open for the human, but both of its branches benefit from ready packs, and the standing decision 2026-07-12 already assigns pack production to loops — this item pre-empts nothing. The haircut-wording choice mirrors the planner's existing `degenHaircutNote` stance (builder picks exact phrasing; keep it factual, no urgency language).

## Territory notes
- `effectiveApy` (`planner.js:654-658`): `PERSONAS[pk].degenHaircut ? raw / 3 : raw`; Bloom's `apy` memo (`planner.js:1354-1357`) applies the same rule, and every Bloom `foreverNumber` call uses that value. The generator's mirror must use the identical ÷3 rule keyed on its own `classifyPersona` result (its `PERSONA_BANDS` don't carry a `degenHaircut` flag — key on `persona === 'degen'`, same effect).
- `generate-spotlight.js` already imports `foreverNumber` from `planner.js` (Node-safe export added by 066) — reuse it with the effective rate; do NOT reimplement the math.
- `pack.generatedAt` uses `new Date()` in a plain Node script — fine (no workflow-runtime restriction applies to repo scripts).
- Committed-output validation must assert self-consistency against the pack's own recorded numbers, never against live rates (live rates drift; the test must stay green until the packs are regenerated).
