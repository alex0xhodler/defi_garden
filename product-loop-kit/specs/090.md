# 090 — TICKET: standardized footer content across all app screens (strictly additive)

Status: READY — sequenced AFTER 091 (redteam O10: the planner footer must be part of 091's entrance
treatment, and 091's before/after traces must not be contaminated)
Risk: HIGH (render paths, both modes) · Score: 7.4

## Report (human)

"Attribution for defillama api and As featured on Leviathan News should be also added in the latest
footer, now only the browse tokens and chains is showing in the footer, standardized footer across the
app screens."

## Triage

- Analytics `.app-footer`: Powered-by + (086) Browse links. No press mention.
- Planner: NO app footer — only static `.seo-hub-links` (Browse links alone; the reported screen).
- Press mention renders only inside the bloom foot (planner.js:2477-2485).

## Change (rescoped per redteam round 1 — strictly additive content parity)

1. One standardized footer CONTENT set on every screen: Powered by Defillama API (attribution link) ·
   As featured on Leviathan News (press link) · Browse tokens · Browse chains. EN+KO via existing keys
   (poweredBy, pressFeatureLabel/pressFeatureName, browseTokens/browseChains) + a new footer-attribution
   key only if the exact sentence needs one.
2. Analytics `.app-footer`: ADD the press mention line. Its POSITION/fixed behavior is untouched
   (086 shipped same-day, still awaiting human spot-check — redteam O10).
3. Planner: gets a rendered in-flow footer (all steps) with the standardized content — NOT
   position-fixed (no imported occlusion class). Coordinated with 091's staged entrance so it never
   becomes the next "labels all around the screen" (091 lands first; 090's footer joins its reveal
   conventions).
4. Bloom's in-content press mention: UNTOUCHED (credibility signal at checkout, inside live windows).
   The static `.seo-hub-links` crawler anchors: UNTOUCHED (086 decision). Dedup is explicitly out of
   scope this pass.

## Measurement-window declaration (redteam O3)

Touches the bloom-adjacent viewport during 005/007/008 (→07-24) and 061/065 (→07-26) windows only as
below-the-fold additive content; declared LOW contamination — if the human disagrees, hold to 07-26.

## Acceptance sketch

Rendered Playwright: standardized footer content present on bare `/` (goal step AND bloom), `/?token=`,
`/?pool=`; EN+KO strings; analytics footer position byte-identical CSS; 086's test_footer_hub_links.js
still 4/4; planner footer in normal flow, links clickable, reduced-motion respected.
