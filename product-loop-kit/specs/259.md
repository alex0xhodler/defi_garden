# SPEC 259 — Perp Exchange Referral & Affiliate Revenue Mapping
**Parent**: Item 259 in `product-loop-kit/BACKLOG.md` | **Risk Tier**: HIGH | **Status**: READY

## 1. Goal
Monetize outbound perp trading clicks by mapping exchange-specific referral parameters (`code=DEFIGARDEN` / `ref=defi.garden`) for Hyperliquid, Lighter, Drift, GMX, Synthetix, and Jupiter Perps.

## 2. Evidence
- Perp trading generates high trading volume ($10K–$1M per active trader).
- On-chain perp exchanges share 10%–30% of trading fee revenues on referred trading volume forever.

## 3. Implementation Plan
1. **Perp Referral Map (`app.js` / `getProtocolUrlWithRef`)**:
   - Hyperliquid: `https://app.hyperliquid.xyz/join/DEFIGARDEN` (or `?code=DEFIGARDEN`)
   - Lighter: `https://app.lighter.xyz/?ref=defi.garden`
   - Drift: `https://app.drift.trade/?ref=defi.garden`
   - GMX: `https://app.gmx.io/#/?ref=defi.garden`
   - Jupiter Perps: `https://jup.ag/perps?ref=defi.garden`
2. **Integration**:
   - Update `getProtocolUrlWithRef(pool)` to check if `pool.project` is a perp exchange and attach the exact referral code.
3. **Acceptance Tests**:
   - `test_perp_referral_mapping.js`: Assert outbound links to Hyperliquid, Lighter, Drift, GMX, and Jupiter carry valid referral parameters.

## 4. Verification
- `node test_perp_referral_mapping.js` passes.
- Inspect rendered outbound CTA links for Hyperliquid OIL and Lighter SPCX to verify referral codes.
