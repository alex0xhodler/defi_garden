# 054 — PR explainer (LOW tier)

**What changed:** Added `<link rel="preconnect">` hints for `https://unpkg.com` (with `crossorigin`, matching the existing `crossorigin` React script tags) and `https://yields.llama.fi` (no `crossorigin` — an uncredentialed `fetch()`) to both `home.html` and `plan.html`, placed directly before the existing `dns-prefetch` tags for the same two hosts. `dns-prefetch` is kept as a fallback, unchanged.

**Why:** `reports/2026-07-12-pagespeed.md` flagged these as the #3 PageSpeed finding — both pages fetch React UMD bundles from unpkg and pool data from yields.llama.fi on every load, but only warmed DNS, not the TCP+TLS handshake. `preconnect` completes the full connection setup ahead of time, shaving a round-trip off the critical path on both router paths (planner `/` via `plan.html`, analytics via `home.html`).

**Diff:** 4 lines added, 0 removed, across `home.html` and `plan.html` only. No router logic, canonical logic, trust rails, or generated SEO surface touched.

**Verification:** All 190 node-test assertions pass. `test_smoke.js` (Playwright) times out on render assertions in this sandbox because the network proxy blocks both `unpkg.com` and `yields.llama.fi` outright (`curl` → `CONNECT tunnel failed, response 403`) — confirmed pre-existing by re-running the same test against pre-diff `home.html`/`plan.html` and getting an identical failure signature. Needs a human visual spot-check on the next live run to confirm both router paths still render and no browser preconnect warnings appear (advisory, not a merge gate, per NORTH_STAR.md).
