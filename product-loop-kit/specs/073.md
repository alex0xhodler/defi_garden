# Spec 073: Perf — minify planner-styles.css + pool-detail-styles.css (053's documented follow-up)

## Evidence
- `specs/053-notes.md` (deviations, line 12): 053 minified exactly the 5 files its evidence named and explicitly left `planner-styles.css` (83K) and `pool-detail-styles.css` (23K) out — "sizable but outside what 053's evidence named; a natural follow-up, not silently rolled into this diff." This item is that follow-up.
- `planner-styles.css` (83,011 B) ships RAW on `plan.html` (link line 56 + noscript line 58) AND on `home.html` planner mode (`addCSS('planner-styles.css')`, line 132) — i.e. on the default `/` route, the top of the `waitlist_submitted` funnel.
- `pool-detail-styles.css` (23,812 B) ships RAW in analytics mode (`home.html:131`) — the SEO-lander → pool-detail conversion path (019).
- The identical pipeline already exists and is proven: `minify-assets.js` (clean-css) took `style.css` 125,333 → 97,152 B (−22.5%). Expected combined saving here: roughly 20–25 KB across the two funnel-critical routes.
- Perf-wave precedent: 052–056 all shipped from the 2026-07-12 pagespeed report; PSI mobile baseline 88. Unminified CSS is a standing PSI "Minify CSS" audit line.
- Grep-confirmed: only `home.html` and `plan.html` reference these two files (no generated token/chain/story page does). `vercel.json`'s `/(.*)\.(js|css)` cache header already covers any new `.min.css` — no config change needed there.

## Hypothesis
Shipping the two remaining raw stylesheets through the existing minify pipeline cuts ~20–25 KB from the planner (funnel top) and pool-detail (SEO-conversion) routes at zero behavioral risk, because the transform is the same byte-reduction already applied to `style.css`. No isolable Mixpanel funnel metric (perf change — same disclosure pattern as 053/054/055/056); proxy = PSI transferred-bytes / "Minify CSS" audit on next live measurement.

## Change
1. `minify-assets.js`: `CSS_FILES = ['style.css']` → `['style.css', 'planner-styles.css', 'pool-detail-styles.css']`. No other pipeline change (`minPath` already derives `*.min.css`).
2. `plan.html`: both the `<link>` (line 56) and `<noscript>` (line 58) references → `planner-styles.min.css`. Keep the existing "edit the source, never the .min" comment convention.
3. `home.html`: `addCSS('pool-detail-styles.css')` → `addCSS('pool-detail-styles.min.css')` (line 131); `addCSS('planner-styles.css')` → `addCSS('planner-styles.min.css')` (line 132).
4. Run `npm run minify`; commit `planner-styles.min.css` + `pool-detail-styles.min.css` (and refresh any existing `.min.*` only if the freshness test says stale — sources are NOT edited by this item).
5. `.github/workflows/sitemap-update.yml`: add `planner-styles.css` + `pool-detail-styles.css` to the `paths` trigger list (alongside `style.css`), and add `planner-styles.min.css pool-detail-styles.min.css` to the regen job's `git add` list (~line 123) so CI keeps them fresh.
6. `test_minified_assets.js`: the CSS freshness loop iterates `CSS_FILES` from the module, so the two new files are covered automatically. ADD reference assertions: `plan.html` contains `planner-styles.min.css` and no `href="planner-styles.css"`; `home.html` contains `addCSS('planner-styles.min.css')` + `addCSS('pool-detail-styles.min.css')` and no raw `addCSS('planner-styles.css')` / `addCSS('pool-detail-styles.css')`.
7. Rendered acceptance (2026-07-11 standing decision — rendered behavior, not fixtures alone): a Playwright check that real `plan.html` (served locally, fixture-routed for external hosts per the test_hero_copy.js / test_search.js pattern) requests `planner-styles.min.css` with a 200 response AND at least one planner element carries a computed style that only `planner-styles(.min).css` provides (proving the sheet applied, not just downloaded); plus an analytics-mode (`?token=` fixture-routed) check that `pool-detail-styles.min.css` is the URL requested when pool-detail CSS loads. May live in a small new `test_css_minified_render.js` added to the npm test chain, or extend an existing rendered test — builder's choice, smallest honest diff.

OUT of scope: ANY change to either `.css` source file (both must be byte-identical after this item); critical-CSS inlining; pruning/splitting planner-styles; touching `style.css`/`style.min.css`.

## Trust-rail guardrail
No user-facing numbers, copy, or translations touched. Both CSS sources byte-unchanged (acceptance-checked). Only file references + pipeline/CI/test wiring change. Trust rails not in the blast radius.

## Acceptance criteria
- [ ] `node minify-assets.js` emits `planner-styles.min.css` + `pool-detail-styles.min.css`; both committed and byte-identical to a fresh run (`test_minified_assets.js` freshness loop green, incl. the new files)
- [ ] `plan.html` + `home.html` reference ONLY the `.min.css` variants of the two files (raw references gone from link, noscript, and both `addCSS` sites); no other file in the repo references the raw names
- [ ] Rendered Playwright: `plan.html` loads `planner-styles.min.css` (200) and a planner-styles-derived computed style is applied; analytics mode requests `pool-detail-styles.min.css`
- [ ] `.github/workflows/sitemap-update.yml` paths trigger + `git add` list updated for the two new files
- [ ] `git diff` shows zero changes to `planner-styles.css`, `pool-detail-styles.css`, `style.css`
- [ ] NORTH_STAR canonical tests green (`node test_planner.js && node test_protocol_parsing.js && node test_qualifier_fix.js`) + `test_minified_assets.js` green + the new/extended rendered test green; every test invocation timeboxed 5 min
- [ ] No new dependencies (terser + clean-css already in the lockfile from 053)

## Measurement
No isolable Mixpanel funnel metric (perf/asset change — 053/054/055/056 disclosure precedent). Proxy: PSI "Minify CSS" audit + transferred bytes on next live measurement; expected ~20–25 KB reduction on the two routes. File-size delta recorded in the notes file at build time is the immediate, honest measurement.

## Risk tier (builder's guess — verifier assigns independently)
HIGH — render-path change (alters which stylesheet URLs BOTH router modes load; a wrong reference = unstyled planner on the default route). Small diff, zero new deps, sources untouched — but 052–056 precedent and "when in doubt → HIGH" both point HIGH.
