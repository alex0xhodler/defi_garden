# 092 — TICKET: planner → analytics transition paint continuity (rescoped)

Status: READY, GATED on post-091 re-triage (redteam O9: if no symptom survives 091, close as
superseded) · Risk: HIGH (IA router adjacent — decision logic untouchable) · Score: 6.6

## Report (human)

"Make the app=1? link more smoother."

## Triage

planner.js:3467 header icon → plain anchor `/?app=1` → full navigation; the jar is the destination's
first paint (white/background flash before CSS + React mount), not the departure.

## Change (rescoped per redteam O9 — destination-side only)

1. Paint continuity at the destination: correct page background + theme present PRE-CSS (inline
   background-color derived from the theme attr the router already stamps pre-paint; both light and
   dark), and a calm analytics boot skeleton on the neumorphic background instead of a blank page.
2. NO blanket prefetch of analytics bundles from the planner (bandwidth cost paid by ~100% of visitors
   for a ~0%-taken path — contradicts 091's perf goal). At most: prefetch on pointerdown/hover of the
   📊 icon. Builder measures before adding even that.
3. NO exit fade (fake latency), NO anchor hijacking — cmd/ctrl/middle-click/new-tab semantics must
   remain native.
4. Router `__APP_MODE` DECISION logic byte-identical (sacred).

## Measurement-window declaration (redteam O3)

No copy/funnel-step change; presentation-only on a cross-mode path. No window declared void.

## Acceptance sketch

Rendered Playwright: navigate `/` → `/?app=1`; screenshot at navigation boundary shows background
continuity (both themes); analytics renders pool cards; modifier-click opens a new tab (semantics
preserved); test_smoke.js 8/8.
