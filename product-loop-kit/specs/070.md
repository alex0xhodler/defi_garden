# 070 — Surface the honest "card doesn't exist yet" disclaimer at the live waitlist CTA (wire `ctaWaitlistMicro`, delete dead `ctaElement`)

**Status at promotion**: READY → IN_PROGRESS 2026-07-13 (promoted by the build loop — no READY items existed; evidence below is all pre-documented in the repo).
**Score**: 7.2 · **Risk (proposed)**: LOW (existing translated copy + existing CSS class surfaced on the bloom checkout panel; dead-code removal; no new deps; diff well under 150 lines. Verifier tiers independently — the bloom render path is touched, so HIGH is acceptable if the verifier judges it so.)

## Evidence (all pre-existing in-repo documentation)

1. `specs/009-notes.md` ("Important finding — one of the two open sites is dead code"): `ctaElement` (planner.js) is built but never inserted into either JSX tree `Bloom()` returns; only `checkoutPanelElement`'s button reaches the DOM. Explicitly "flagged for a future backlog item to either wire it up or delete it."
2. `specs/061-notes.md` ("Dead-code finding"): after 061 reframed the copy, `ctaWaitlistMicro`'s only call site is that dead `ctaElement` — "the honest 'card doesn't exist yet' micro-disclaimer directly under the primary CTA button is currently unreachable by any user. … Flagging for the next heartbeat as a candidate backlog item."
3. NORTH_STAR standing decision 2026-07-12 (business model, Q2): "Card copy stays honest: it's a waitlist for early access, the card does not exist yet — no fake availability." Today the live CTA ("Join the waitlist →", planner.js `checkoutPanelElement`) carries NO pre-click disclosure that the card doesn't exist; the honest framing only appears after the modal opens. The disclaimer string already exists, translated, styled — just unrendered.
4. North-star relevance: `waitlist_submitted` is the north-star metric; this is the exact CTA that opens that funnel. Trust is the conversion currency (CLAUDE.md ICP). Honest pre-click framing also protects submit-quality (emails from people who understood the pitch), which the 061/065 pre/post windows measure.

## Change (smallest that satisfies)

In `planner.js`, inside `Bloom()`:

1. **Delete the dead `ctaElement` declaration** (the `// CTA element (shared) — opens waitlist modal` block, ~planner.js:2425-2443). Grep `\bctaElement\b` must return 0 hits afterwards. Do NOT delete the `ctaWaitlistMicro` translation keys — they get a live render site in step 2.
2. **Render the micro-disclaimer under the live CTA**: in `checkoutPanelElement`, directly after the `gp-primary-cta gp-checkout-cta` button (before the `gp-checkout-trust` pills), add `e('p', { className: 'gp-cta-microcopy' }, t('ctaWaitlistMicro'))`. One insertion point covers both bloom layouts (subscription and target/growth both mount `checkoutPanelElement`).
3. **Regenerate `planner.min.js`** via `npm run minify` — plan.html loads the min bundle; 061's attempt-1 FAIL is the precedent for forgetting this.
4. **CSS**: reuse `.gp-cta-microcopy` (planner-styles.css:1411) as-is. Only touch CSS if the existing rule visibly misrenders inside `.gp-checkout-panel` (e.g. spacing collision with the trust pills) — and then only via existing neumorphic tokens, no one-offs. If touched, regenerate the min CSS too.
5. **No translation changes.** EN "Free to join • Card doesn't exist yet • We'll email you when it does" and KO "무료 가입 • 카드는 아직 없어요 • 준비되면 이메일로 알려드려요" ship as-is (061 already wrote and guard-tested them).

Out of scope: any modal/copy changes, analytics changes (waitlist events untouched), pitch variants (065), share surfaces.

## Acceptance criteria (rendered product behavior — standing decision 2026-07-11)

1. **Rendered Playwright check, real UI**: fixture-routed pools API (`page.route('https://yields.llama.fi/pools', …)` per `test_search.js`/`test_waitlist_pitch.js` pattern, no live network), drive a bloom URL (e.g. `/plan.html?goal=claude&pace=stable&fm=monthly` or the retirement share URL 061 used), assert:
   - `.gp-checkout-panel .gp-cta-microcopy` is visible and its text equals the exact `ctaWaitlistMicro` EN string;
   - with `&lang=ko`, equals the exact KO string;
   - it renders on BOTH archetype layouts (one subscription goal + one growth/target goal);
   - clicking the CTA still opens the waitlist modal (no handler regression);
   - zero page errors (external-host fetch failures per CLAUDE.md exempt).
2. `grep -c '\bctaElement\b' planner.js` → 0.
3. `node test_minified_assets.js` passes (min bundles regenerated, not stale).
4. Full `npm test` passes, each test timeboxed ≤5 foreground minutes.
5. No trust-rail, translation-key, or analytics diffs; no new CSS one-offs (neumorphic token check).
6. New rendered check committed as `test_waitlist_microcopy.js` and appended to the npm `test` chain (pattern: test_hero_copy.js precedent).

## Measurement

No isolated funnel metric of its own (honesty/trust fix on an existing step, same disclosure pattern as 027). It lands inside 061/065's `waitlist_opened → waitlist_submitted` measurement windows (to 2026-07-26 / 2026-08-09); any effect shows up there. Instrumentation unchanged.

## Territory notes

- Live CTA: planner.js `checkoutPanelElement`, button at ~2676-2688, trust pills at ~2689, `checkoutNote` at ~2694.
- Dead element: planner.js ~2425-2443 (`gp-cta-row` wrapper + button + microcopy p).
- `.gp-cta-row` CSS (planner-styles.css:1384) may become unreferenced after the deletion — leave the CSS alone (other usages unverified; CSS pruning is not this item).
- `test_planner.js` contains a "waitlist copy — ICP alignment" guardrail suite over these keys — strings must not be edited.
