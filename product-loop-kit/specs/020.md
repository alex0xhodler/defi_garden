# Spec 020: Make the SEO-lander → north-star funnel measurable (outcome, not intent)

## Evidence
Live Mixpanel pull, 14d (signals/2026-07-11.md), project defigarden (4042048):
- `search_success` 50, broken down by `results_count`: **50/50 events have results_count=0**.
- `pool_view` 0, `pool_click` (incl. `click_type=garden_cta`) 0, over the same 14d.
- `page_view` by path: `/` 114, `/search/*`+`/chain/*` ≈48, **`/pool/*` = 0**.
- Code: `app.js:1933` (NL-Enter path) → `analytics.js:125` calls `trackSearchSuccess(query, sel, 0, …)` — results_count hardcoded 0. `app.js:1006-1024` sets pool-detail from `?pool=` URL without any `trackPoolView`; `trackPoolView` fires only in `handlePoolClick` (`app.js:2139`), which is wired to card `onClick` (`app.js:2343`).

## Hypothesis
We believe wiring outcome instrumentation (real `results_count` on search_success + `trackPoolView` on direct `/?pool=` landings) will make the search→pool→garden_cta→plan funnel computable, because today every step of the weekly theme's funnel reports 0 or a parse-only proxy and we cannot tell "no traffic" from "traffic we don't measure" from "traffic that drops off." Confidence: **supported** (three code-verified gaps + matching zero/constant data).

## Change
Smallest version that makes the funnel honest — instrumentation only, no UI/logic/trust-rail change:
1. Populate a real result count on `search_success` for the NL-Enter path: compute the post-filter result count (the same `filteredPools.length` the grid renders) and pass it instead of the hardcoded `0`. If the count is not yet available synchronously at parse time, fire/patch the event once results settle (e.g. a follow-up effect keyed on the search), or add a distinct `results_count` property sourced from the rendered grid. Also set/keep `has_results` truthfully.
2. Fire `Analytics.trackPoolView(pool, { source: 'url_direct' })` inside the URL-driven pool-detail effect (`app.js:1006-1024`) when a `?pool=` deep link resolves to a pool, so SEO/direct landings are counted. Dedupe so an internal card click that also updates the URL does not double-fire (guard on a ref/prev-pool id).

OUT of scope: changing search parse logic, pool-card UX, the garden_cta, any copy, any trust rail, retention/cohort work. No new events beyond reusing `pool_view`/`search_success` with corrected/added properties (a new `source` value on pool_view is fine).

## Acceptance criteria
- [ ] Emitting a successful NL search (e.g. type "USDC" + Enter) fires `search_success` with `results_count` equal to the number of pool cards actually rendered (NOT 0 when results exist). Verified by driving the real UI (Playwright) and asserting the event property, per the 2026-07-11 standing decision (UX/behavior acceptance = Playwright on the real UI, never fixtures alone). A search that yields zero pools fires `results_count: 0` (and `search_abandonment` per existing behavior).
- [ ] Loading a `/?pool=<id>` deep link that resolves to a real pool fires exactly one `pool_view` with `source: 'url_direct'`. Loading via internal card click still fires exactly one `pool_view` (no double-fire). Verified by Playwright driving both entry paths.
- [ ] `test_smoke.js`, `test_canonical.js`, `test_search.js` still pass (or documented network-blocked with the standing 5-min timebox). New behavior covered by an assertion in the existing Playwright suite.
- [ ] Instrumentation: `search_success` carries a non-constant `results_count`; `pool_view` fires on the `?pool=` URL path with `source: 'url_direct'`.
- [ ] Diff ≤ 150 lines, no new dependencies, `analytics.js` + `app.js` only (planner untouched).

## Measurement
Metric: (a) share of `search_success` with `results_count>0`; (b) `pool_view` count with `source=url_direct`; (c) once populated, the funnel `search_success(results>0) → pool_view → pool_click[garden_cta] → plan_created`.
Window: 14 days from ship. Decision rule: this is enabling instrumentation — keep if the events populate as specified; the *product* decision (fix search results / boost card click-through / boost SEO-lander conversion) comes from what the now-visible funnel shows in the next heartbeat.
Minimum sample per NORTH_STAR.md (≥30 events per claim; below → "insufficient data").

## Risk tier (builder's guess — verifier assigns independently)
LOW — analytics instrumentation only (explicitly LOW per NORTH_STAR risk policy), no trust-rail / IA-router / parameterized-URL behavior change, ≤150 lines, no deps. Verifier: confirm no `?pool=` render/canonical behavior changed (that path is sacred SEO surface) — instrumentation is additive; a behavior change would bump this HIGH.

## Open questions
- None that block build. One product question is pending with the human (report 2026-07-11): whether internal-search → pool-card click-through is a goal or the pool-detail garden_cta is meant to be reached primarily via SEO landings. Either answer, this instrumentation is the prerequisite to measure it — so build is not blocked.

## Territory notes
<!-- Build loop appends findings from the blindspot pass here. -->
