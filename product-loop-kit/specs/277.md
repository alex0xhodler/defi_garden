# Spec 277: advertise the live agent surface (API + MCP) on llms.txt — the deferred 228 leg, unblocked by the deploy

Impact: north-star leg A — llms.txt is the first file an agent reads and it currently mentions the live API and MCP server zero times (live grep 2026-08-12: 0 hits for `/api` or `MCP` in served llms.txt)

## Evidence
- 2026-08-12 heartbeat: the Worker is DEPLOYED and live-verified — `/api/health` 200 (7,325 railed
  pools), `/api/pools` railed, `/api/forever-number?monthly=20` returns a blended-rate forever
  number, `/mcp` answers `initialize`/`tools/list`/`tools/call` (signals/2026-08-12.md).
- `curl https://www.defi.garden/llms.txt | grep -iE "/api|mcp"` → **0 matches**. The primary
  agent-discovery file does not know the agent endpoints exist.
- Item 228 shipped "NOT ADVERTISED" deliberately: "publishing a URL that 404s would be a false
  claim on the surface whose pitch is honesty" (LOG 2026-08-11). The URLs no longer 404 — the
  deferral condition has expired.

## Hypothesis
We believe adding an "API & MCP" section to llms.txt/llms-full.txt (endpoint URLs, one-line rail
summary, pointer to `GET /api` for the machine contract) will let agents that read llms.txt find
the structured endpoints, because llms.txt is the established agent entry point and currently
routes 100% of that attention to HTML/markdown only. Confidence: supported for discoverability
(the file is demonstrably fetched — it is the surface 254/261 fought to keep truthful); guess for
consumption volume (leg A instrument = D1, first read pending).
- Instance of: agent-pillar leg 4 (static citation layer) referencing legs 2-3 (API/MCP).
- Class closed by this item: no — registry listings remain human-owned (228's residue), and
  the md twins/README mention is included only if the generator already owns those sections.

## Change
Smallest version: extend `generate-llms.js` (NEVER hand-edit llms.txt — the 0 2 * * * cron regen
overwrites it) to emit an agent-endpoints section: `https://www.defi.garden/api` (contract),
`/api/pools`, `/api/forever-number`, `https://www.defi.garden/mcp` (MCP, Streamable HTTP, 4 tools),
with the rail figures derived from `trust-rails.js` exactly like the existing sections (254's
import precedent) and endpoint paths derived from `edge/api-core.js`'s ENDPOINTS +
`edge/mcp-core.js`'s TOOLS — never hand-typed (the 212 mirror rule; 262 is the cautionary
instance). Regenerate and commit outputs in the same commit. OUT of scope: fixing the
`.well-known` cards (265), openapi.json (262), registry listings (human).

## Acceptance criteria
- [ ] Served-from-repo llms.txt and llms-full.txt each carry the section with `/mcp` and `/api`
      URLs and rail figures equal to `trust-rails.js` values.
- [ ] Endpoint list in the section set-equals the routes `edge/api-core.js` actually dispatches
      (both directions) and the tool names `edge/mcp-core.js` exports.
- [ ] Tests: extend `test_llms_rails.js`/`test_agent_surface_rail_claims.js` population to the new
      section; a drift test goes red if a route is added to api-core without llms regen.
- [ ] Non-vacuity: mutate a route id in the generator's derived list → red; restore → green.
- [ ] Survives the cron: the section is emitted by `generate-llms.js`, verified by running the
      generator twice and diffing (seo-surface-regen-delta playbook).

## Measurement
Metric: leg A — agent reads of /api + /mcp path_class in D1 `agent_reads` · Gate: ≥30 real
agent-classified reads · Backstop: 60d from ship → UNEXERCISED · Decision rule: build-leg keep on
green tests; traffic claim belongs to the pillar.

## Risk tier (builder's guess — verifier assigns independently)
HIGH — generated SEO/agent surface (NORTH_STAR risk policy: changes go through generate-*.js only).

## Open questions
None — sequencing note: land 265 first or in the same wave, so llms.txt never points at a surface
whose discovery cards still contradict it.

## Territory notes
