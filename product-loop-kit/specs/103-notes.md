# 103 — build notes (rate-momentum note on pool detail)

Built 2026-07-15 by an Opus coding agent dispatched by the build loop (operator = Fable-role session).
Purely additive, single surface (pool detail), mirrors the 088.1 rate-track-record note exactly.

## What shipped
- `PoolDetail.js`: new `.rate-momentum-note` element inserted immediately after the 088.1
  `.rate-track-record-note`, before the Tokens Section. Same inline neuro styling, same IIFE body
  shape. Guard: NOT 071-volatile AND `pool.kpis` object AND `typeof kpis.apyMomentum === 'number'` AND
  `historyPoints >= 7` AND `|apyMomentum| >= 0.5`. Two tiers on the sign of momentum
  (RISING `>= 0.5`, FALLING `<= -0.5`). Reads only `pool.kpis.*` + apyBase/apyReward/apyMean30d; touches
  no rail constant, mutates no pool list.
- `translations.js`: `rateMomentumRising(delta, hp)` + `rateMomentumFalling(delta, hp)` added to EN
  (~L49) and KO (~L673), both together.
- Regenerated `PoolDetail.compiled.js`, `PoolDetail.compiled.min.js`, `translations.min.js` via
  `npm run compile && npm run minify`. `app.compiled.*` regenerated byte-identical (no diff).
- New `test_kpi_momentum.js` (copied from `test_kpi_track_record.js`, PORT 8796, selector
  `.rate-momentum-note`), wired into `package.json`'s test chain right after `test_kpi_track_record.js`.

## Deviation (ONE, test-only — render logic is spec-exact)
- Spec §D1/D2 asked the test to assert the rendered delta substring `"2.30%"`. But the render uses
  `_formatApy` (= app.js `formatApy`: `toLocaleString('en-US', {maximumFractionDigits:2})`, NO
  `minimumFractionDigits`), so a momentum of `2.3` renders `"2.3%"`, not `"2.30%"`. Verified live:
  `formatApy(2.3) === "2.3%"`. The agent kept the render logic exactly as the spec mandates (`_formatApy`
  for consistency with the 071/088.1 notes) and corrected the TEST assertion to `"2.3%"` (the value
  actually rendered), with an inline comment. No render-logic deviation. The spec's "2.30%" was simply a
  wrong assumption about the shared formatter; correcting the test to match real product behavior is the
  right call (test measures rendered product, per the 2026-07-11 standing decision).

## Conservative scope decision (documented, not a deviation)
- BACKLOG 103 says "pool detail/**cards**". This leg does **pool detail only**, mirroring how 088.1 did
  detail and left the `?pool=` SEO deep-link enrichment as a separate follow-up (item 105). One
  surface, smallest-change, one-item rule. **Card surfacing remains a documented follow-up under the
  still-open 088 parent** (alongside 104 TVL-trend and 105 SEO deep-link kpis). If the operator wants
  cards, promote a new sub-item — do not retrofit this one.

## Data reality
- With one retained history point today, `kpis.apyMomentum` is `null` for every pool (needs ≥2, and this
  note needs ≥7), so the momentum note renders for NO pool in production yet. It activates automatically
  as 087's daily CI accrues ≥7 history points and a pool's rate actually moves ≥0.5pp. Code handles every
  case; the data catches up — identical situation to 088.1 at ship time.

## Verification
- `node test_kpi_momentum.js` → 8/8 rendered-DOM assertions (exit 0).
- `node test_compiled_assets.js` → 4/4; `node test_minified_assets.js` → 9/9. Regenerated artifacts
  consistent.
- Full `npm test` chain attempted end-to-end (timeboxed): stages 1–8 (`test_planner`,
  `test_protocol_parsing`, `test_qualifier_fix`, `test_compiled_assets`, `test_minified_assets`,
  `test_css_minified_render`, `test_smoke`, `test_canonical`) all passed, then the chain halted at
  stage 9 `test_search.js` with `page.goto: Target page, context or browser has been closed` — a browser
  crash from sandbox resource exhaustion under sustained Playwright load (test_search alone also times
  out in this sandbox). test_search exercises analytics-search parsing in app.js, which this diff does
  NOT touch — it is an environmental/ordering flake, not a regression from 103. The `&&` chain stopped
  there before reaching `test_kpi_momentum` (stage ~47), so the diff's own gate was run in isolation
  instead (8/8, verifier-confirmed) along with `test_compiled_assets` (4/4) and `test_minified_assets`
  (9/9). Per standing decision 2026-07-11 (timebox test runs, document, proceed): documented and
  proceeding — the three gates covering this diff are green.
