# 134 — PR explainer (LOW tier)

**Item:** 134 — STUDY: serve obscure-token discovery data fast from our own layer.
**Risk:** LOW — docs-only. No product code, no trust-rail touch, no SEO-surface edit, no
out-of-scope dir. Diff is one new doc + kit bookkeeping.

## Goal
Decide, from the real code, whether to (a) serve a lower-floored "discovery" dataset for obscure
tokens, or (b) keep the live DefiLlama fallback. Produce a recommendation the human can approve.

## What changed
- **New:** `docs/discovery-data-layer-134.md` — the recommendation. Maps every TVL floor
  ($10M snapshot, $10M poller, $1000 CI transient) with file:line; quantifies the (a)/(b) tradeoff
  on payload (a served $1000-floored dataset ≈ 1.29 MB gzip vs the current ~65 KB — ~20× regression),
  trust-framing, and 108's no-backend-read-path; pinpoints the 133 dead-end
  (`app.js:2343-2344` resolves empty instantly; the escape hatch fires only on a minTvl change,
  `app.js:1159`); recommends **(b)** and drafts the follow-up build for item 133.
- **Bookkeeping (same commit):** `specs/134.md` (spec, written this loop — item had none),
  `specs/134-notes.md`, this file, BACKLOG.md status → SHIPPED, LOG.md entry.

## Recommendation in one line
Keep live DefiLlama for below-floor discovery (b); the real fix is to trigger the *existing* live
refetch when a token search comes back empty from the $10M snapshot, instead of only when the user
lowers the TVL slider — no new served dataset, no lowered floor, hot-path payload win preserved.

## How to review
Read `docs/discovery-data-layer-134.md`. Nothing runs; nothing in the app changed. If you approve,
promote the drafted "133 build" item.
