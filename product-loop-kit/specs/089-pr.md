# 089 — Default browse sort → TVL (LOW-tier explainer)

**Goal.** The analytics browse grid defaulted to APY sort, which tops the list with
anomalous / reward-decayed pools — the opposite of what the cautious retail ICP scans for
(NORTH_STAR: "size = safety signal the cautious saver reads first"). Human analytics-UX
audit (2026-07-14) asked for TVL-first by default, matching the DeFi mental model and SOTA
yield tables.

**What changed.** One product-code line — `app.js:765`,
`const [sortBy, setSortBy] = useState('apy')` → `useState('tvl')`. That's the whole
behavior change. The three filter/sort `useEffect` branches (token-first, chain-first,
token-mode) already handle `sortBy === 'tvl'` with spec 092's demote (yielding pools before
no-supply-yield, then TVL desc), so the top rows won't read a stark "0%". The APY/TVL toggle
and the `userSortedApy` anomaly-demote semantics are untouched:
- default now `sortBy='tvl', userSortedApy=false` → TVL sort with 092 demote active;
- clicking **APY** → raw APY sort (anomalies shown but ⚠-flagged/High-risk, existing);
- clicking **TVL** → back to TVL.

Compiled + minified assets regenerated (home.html loads `app.compiled.min.js`); only
app.compiled.js / app.compiled.min.js changed (a single `"apy"`→`"tvl"` token in the min).

**Verification.** New rendered-Playwright gate `test_default_sort.js` (fixture-routed,
sandbox-safe): fresh `/?token=` and `/?chain=` loads render TVL-descending with no
interaction; the TVL toggle is `active` by default; the toggle round-trips to APY order
and back. 4/4 pass. Verifier independently re-ran all three sort tests (4/4, 4/4, 3/3) and
mutation-checked the min asset (revert to `"apy"` → 3/4 fail) — the test genuinely bites.
Sibling tests test_zero_yield_demote.js (092) and test_list_default.js (090) still green.

**Risk: LOW.** Component-state default-value flip, 5 lines across 5 files, no new
dependencies, no trust-rail touch, no new user-facing strings (so no translations work).
Directly analogous to the 090 `viewMode` default flip. SEO lifeline intact
(`?token=`/`?chain=` still render pool cards). Verifier PASS + tests green → auto-merged
per NORTH_STAR risk policy (c). Needs no human visual spot-check beyond the standing
advisory on render-path merges (003 smoke gate covers both router paths render).
