# PR explainer: 061 — Waitlist reframe to the card value prop

**Tier: LOW** (pure copy change, EN+KO together, 12 lines, no new deps, no layout/logic touched).

**What changed**: the waitlist CTA microcopy and modal (step 1) in `translations.js` now name the actual mechanic — your position's yield pays your subscription automatically through a card — instead of generic "join the early access list" language. This follows the 2026-07-12 human interview decision (NORTH_STAR.md Q2): the waitlist is the funnel for the yield-funded subscription card, and the north-star metric is now `waitlist_submitted`.

**Why this shape**: a pre-existing test guardrail (`test_planner.js`, "waitlist copy — ICP alignment") bans crypto jargon like "disposable card"/"self-custody" in this exact copy — the ICP is a cautious retail saver, not a crypto-fluent analyst. The first draft used "disposable card" and failed that test; revised to "a simple card" that "pays your subscription automatically," which keeps the value prop legible without the jargon.

**Verification**: `node test_planner.js && node test_protocol_parsing.js && node test_qualifier_fix.js` — 190/190 assertions pass. Rendered-UI check via Playwright was attempted but blocked by the sandbox's documented browser-network restriction (same as items 040/044/045/051-056); falls back to the code-level `makeT()`-equivalent string assertions already in `test_planner.js`. Needs a human visual spot-check on next deploy.

**Measurement**: `waitlist_opened → waitlist_submitted` (009 events), 14-day pre/post window, decision rule in `specs/061.md`.
