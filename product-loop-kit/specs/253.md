# Spec 253: restore `poolNotFoundTitle` — the dead-pool heading renders its own key name on the `?pool=` surface

## Evidence

Commit `6fceca79bb` ("feat: full 00K TVL floor alignment across app, poller, snapshots, and sitemaps",
author `alex@0xhodler.nl`, 2026-08-08 00:36 +0200, direct to `main`) deleted `poolNotFoundTitle` from both
translation namespaces. Its diff's `-` block removed the key from EN and KO; its `+` block replaced it with
a `tvlTrendShrinking` entry that already existed ~50 lines above (that duplicate is item **255**).

Occurrence count of `poolNotFoundTitle:` in `translations.js`, traced commit by commit:

| commit | count |
|---|---|
| `9ab58d990d` (2026-08-07 12:24) | 2 |
| `e8aac65f03` | 2 |
| `6f1d624451` (the commit immediately before) | 2 |
| **`6fceca79bb`** | **0** |
| `7edc0f418d`, `433e9aa001`, `a9c0e931fe`, `748582f2d4` (`HEAD`) | **0** |

Loaded at `HEAD`: `translations.en.poolNotFoundTitle === undefined` and
`translations.ko.poolNotFoundTitle === undefined`.

`translations.js:1546` is the fallback line:

```js
return fallback ? (typeof fallback === 'function' ? fallback(...mappedParams) : fallback) : key;
```

When a key is missing in the active language it falls back to EN; when it is missing in EN too, **`t()`
returns the key string itself**. The call site is `app.js:3558` (compiled: `app.compiled.js:3492`):

```js
}, deadPoolResolved ? t('poolNotFoundTitle') : chainMode && …
```

rendered as the `.empty-state .empty-message` heading. So a visitor arriving at
`/?pool=<id-not-in-live-data>` — the `url_direct` leg of the north-star surface, the leg the 3,664-strong
deep-link estate points at — sees a heading reading **`poolNotFoundTitle`**.

**Three independent instruments agree** (this is the strongest confirmation this loop has produced for one
defect):

1. `node test_dead_pool.js` → **exit 1**, 2 of 12 assertions fail:
   - `✗ dead ?pool= renders honest empty state (EN title)… — page.waitForFunction: Timeout 8000ms exceeded`
   - `✗ dead ?pool=&lang=ko renders the KO poolNotFoundTitle string — page.waitForFunction: Timeout 8000ms exceeded`
   - the file's own summary prints `10 dead-pool assertions passed (EN: "undefined", KO: "undefined")`
   (The tests read the expected strings from `translations.js` rather than hardcoding them —
   `test_dead_pool.js:32-33` — so they compare rendered text against `undefined` and time out.)
2. `audit-app.js` run 2026-08-08T07:22:44Z, 83 surfaces: **NEW** finding
   `{"surface":"dead-pool","viewport":"1280px","check":"dead-pool-empty-state","severity":"P1",`
   `"detail":"dead ?pool= id did not resolve to the honest empty state within 10s"}`. The same surface
   produced no finding on 08-07.
3. Direct dictionary load, above.

**`npm test` is RED on `main`.**

*Instrument note (RAZOR §2a, recorded because it nearly produced a phantom finding):* the first reading of
this test was `node test_dead_pool.js | tail -25`, whose exit status is `tail`'s — it reported **0**, and a
"the gate prints ✗ and exits 0" vacuous-green P0 was one step from being filed against a gate that behaves
correctly. Re-run without the pipe: exit **1**. Read exit codes without a pipe.

## Hypothesis

We believe restoring the two deleted strings will make the dead-pool empty state render its honest heading
again and turn `test_dead_pool.js` and the audit's `dead-pool` surface green, because the empty state's
markup, routing, `robots` handling and alternatives list are all untouched by `6fceca79bb` — the *only*
thing that changed is the dictionary lookup. Confidence: **validated-pattern** (the mechanism is read
directly from the diff and confirmed by three instruments; nothing here is inferred).

- **Instance of:** a rendered `t()` key with no entry in either namespace, so the fallback chain terminates
  at `return … : key` and a developer identifier reaches a user. The population is **every `t('…')` call
  site in the repo** — not this one key.
- **Class closed by this item: NO.** This item restores one key. Nothing prevents the next deletion:
  every gate in the repo walks the *parsed* dictionary, where a deleted key is indistinguishable from a key
  that never existed. The class is ticketed as item **255** (source-level dictionary integrity: no
  duplicate declarations; every `t()` call site resolves in EN and KO). Number left open: the repo's full
  `t('…')` call-site count, which 255 must enumerate at test time rather than hand-list.

## Change

Restore, in `translations.js`, in both namespaces, at the position the diff removed them from:

- `en.poolNotFoundTitle: "This pool is no longer tracked"`
- `ko.poolNotFoundTitle: "더 이상 추적되지 않는 풀입니다"`

Both values are recoverable verbatim from `git show 6f1d624451:translations.js` — **take them from there,
do not re-translate**. Regenerate `translations.min.js` through the repo's existing minification path (the
same path `6fceca79bb` used); never hand-edit the minified file.

**Explicitly OUT of scope:** the duplicate `tvlTrendShrinking` (item 255), the nine stale `$10M` claims
(item 254), the source-level dictionary gate (item 255), and any change to the dead-pool empty state's
markup, routing or `robots` behaviour.

## Acceptance criteria

- [ ] `require('./translations.js').translations.en.poolNotFoundTitle` and `.ko.poolNotFoundTitle` are both
      non-empty strings, and each is byte-identical to the value at `6f1d624451`.
- [ ] The KO value contains Hangul and is not byte-identical to the EN value (EN + KO updated together is a
      hard rule; one language alone is a verifier FAIL).
- [ ] `translations.min.js` is regenerated, not hand-edited, and its parsed `poolNotFoundTitle` values match
      `translations.js` in both namespaces.
- [ ] Tests: `node test_dead_pool.js` exits **0** with **12/12** assertions passing — **run without a pipe**,
      and record the raw `echo $?`. Also green: `node test_snapshot_first.js`, `node test_pool_artifact_paint.js`
      (both read `tr.en.poolNotFoundTitle` at module load), plus `node test_translations_fallback.js` and the
      NORTH_STAR-mandated `node test_planner.js && node test_protocol_parsing.js && node test_qualifier_fix.js`.
- [ ] `node audit-app.js --only=dead-pool` produces **no** `dead-pool-empty-state` finding. Report the exit
      code read without a pipe.
- [ ] Instrumentation: none added — this restores existing rendered copy. The surface is already covered by
      `pool_view{source=url_direct}`.
- [ ] Non-vacuity: after the fix, re-delete `poolNotFoundTitle` from the EN namespace only → `test_dead_pool.js`
      must go RED on the EN assertion; restore and confirm `md5sum translations.js` is byte-identical to the
      pre-mutation value; re-run green. Repeat for KO alone (this second cycle proves the KO assertion is not
      passing via the EN fallback — without it, deleting only KO would still look green).
- [ ] Population: the tests must draw the expected strings from `translations.js` at test time (as
      `test_dead_pool.js:32-33` already does), never from a hardcoded literal — a test that hardcodes the
      string cannot fail when the dictionary is emptied, which is precisely how this defect reached `main`.

## Measurement

Metric: **north-star leg (B)**, `pool_click{source ∈ garden_cta, protocol_link}` on pool-detail views —
the dead-pool state is the failure branch of the `url_direct` arrival path this metric depends on.
Gate: window opens at **≥30 real exercised** `pool_view{source=url_direct}` events (crawler-classified never
count; **operator-classified never count either** — see item 252). Real 14d = **0** today, so the gate is
shut and the traffic leg cannot be measured.
Backstop: gate unopened 60 days after ship → close **UNEXERCISED** in LEARNINGS.
**Build-leg verification is calendar-based and is what actually gates this merge:** `test_dead_pool.js`
12/12 and the audit's `dead-pool` surface finding-free, both verified in-session.
Decision rule: keep unconditionally — this is a defect repair, not an experiment. Revert only if the
restored strings regress another test.

## Risk tier (builder's guess — verifier assigns independently)

**LOW.** Copy-only change to `translations.js`, EN + KO together (NORTH_STAR's LOW fast lane names
copy/text changes explicitly); expected diff well under 150 lines; no new dependency; no trust rail, no
`__APP_MODE` router change, no parameterized-URL behaviour, no generated SEO surface hand-edit.
Note for the verifier: `translations.min.js` is a build artifact of this change, not a hand edit — confirm
it was regenerated through the repo's existing path. If the builder touches anything beyond `translations.js`
and its minified twin, the tier is wrong and the item is out of its own scope.

## Open questions

None. The EN and KO strings are recoverable verbatim from git history; no product decision is required.

## Territory notes
<!-- Build loop appends findings from the blindspot pass here. -->
