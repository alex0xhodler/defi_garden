# 090 — Analytics default view → list/rows — PR explainer (LOW tier)

## Goal
Make the analytics browse default to a row/list layout instead of a grid of boxes — SOTA
yield tables are row-first (denser scan, aligned numbers, less scroll), the read the cautious
ICP does first. Human's 2026-07-14 Analytics-UX audit.

## Change (surgical)
- `app.js:758`: `useState('grid')` → `useState('list')`. That one line is the whole product
  change — `viewMode` is unpersisted component state, both views use the same `renderPoolCard`,
  and only the container class differs (`app.js:2904`; `.pools-list` CSS already exists). The
  grid/list toggle keeps working both ways.
- Regenerated the loaded artifacts: `app.compiled.js`, `app.compiled.min.js` (home.html loads
  the min).
- New rendered Playwright gate `test_list_default.js` (3/3, real chromium, fixture-routed):
  `/?token=USDC` + `/?chain=Ethereum` default to `.pools-list` with cards; toggle round-trips.

## Why it's safe
Same data, same cards, same trust rails — purely a CSS-layout default. SEO lifeline intact
(`?token=`/`?chain=`/`?pool=` still render pool cards). Bare `/` planner untouched. Verifier
PASS LOW 7/7 incl. its own mutation check (mutated the loaded `app.compiled.min.js`, test
failed as expected, restored) + a 360/768/1280 + dark spot-check.

## Not in scope
Row-level polish (column alignment, tabular-nums, calmer 0% styling) = item 095, the soft-pair
follow-up. Advisory: human visual spot-check on the live analytics browse (render-path default
change).
