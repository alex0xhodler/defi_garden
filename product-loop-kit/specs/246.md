# Spec 246: 3-screen micro-consistency batch (low-hanging, audit C4)

## Evidence
`reports/2026-08-06-ux-audit-3screens.md` C4, from the human's own preview screenshots (~`88ecb7bfaa`):
- Card view: "$0.06/day" wraps to two lines on WBETH and USDY cards while identical cells render one
  line elsewhere — both themes. The per-day cell has no nowrap/min-width, so wrap depends on numeral
  width.
- Pool detail: "Underlying Assets" chips render mono-caps ("WEETH ↗") on the otherwise-composed page —
  the 238 terminal-skin class's visible on-page remnant.
- Card grid orphans a single card on the final row at wide viewports.

## Hypothesis
Three CSS-level fixes close the visible inconsistencies without behavior change. Confidence:
validated-pattern (each is a one-rule fix of a class already documented — cell-wrap = the 136 clip
family's soft variant; mono chip = 238's class).
- Instance of: per-cell wrap/width discipline in data grids; 238's mono remnants.
- Class closed: these instances only — 238 keeps the full sweep; state any other wrap-prone cell found.

## Change
`style.css` / `pool-detail-styles.css` (+ regen minified): (a) per-day (and any sibling numeral) cell
gets `white-space: nowrap` + a min-width sized to the widest realistic value ("$0.00/day"); (b)
underlying-asset chips drop the mono stack/uppercase for the body stack (ticker text itself stays
uppercase — it is data); (c) card grid last-row orphan handled (auto-fit/minmax or content-sized last
row — builder's judgment, no new layout system). OUT of scope: any layout recomposition, card-view
existence question, 238's full sweep.

## Acceptance criteria
- [ ] Rendered card view (fixture with WBETH/USDY-length values): every per-day cell single-line at
      360/768/1280, both themes — asserted via computed line count/height, population from the fixture.
- [ ] Underlying-asset chip computed font-family = body stack; no `text-transform: uppercase` on it.
- [ ] No single-card orphan row at 1280/1540 with the fixture population.
- [ ] Approved list view byte-unchanged (its tests stay green); non-vacuity: revert the nowrap → cell
      test red; restore → green.

## Measurement
Build-leg only.

## Risk tier (builder's guess)
LOW — CSS polish on grid + detail.

## Open questions
None.
