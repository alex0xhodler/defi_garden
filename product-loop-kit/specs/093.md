# 093 — TICKET (SPLIT per redteam O4/O6): category-true flows

Status: 093a READY (doc only) · 093b/093c BLOCKED until 085+089 merged AND the human answers Q1/Q2
Risk: HIGH (onboarding funnel top) · Score: 8.2

## Report (human)

"Keep the default categories, but make them smarter, some have different paths, subscriptions for
example doesn't have to be monthly — it's not ideal to save for a subscription for a few years if you
put 1k per month. Think about the actual flows usefulness before the 'categories' predefined, make the
onboarding change accordingly as well as the checkout options."

## Redteam findings folded in

- Subscriptions ALREADY skip the monthly step and seed capital-first (pickGoal subscription branch);
  089 (in flight) makes the seed rate-honest. The human's complaint targets RESIDUAL vestiges: monthly
  handling for subscription in pickMonthly (planner.js:3859), subscription in monthlyChips (~3961), and
  copy/CTA framing that still speaks "save monthly".
- TARGET capital-first is already MANDATED by docs/garden-planner-v3-yield-funded.md — the work is a
  gap audit, not new design.
- Any flow table must be written against the POST-089 tree (089 moves capital seeding to persona-pick).

## Split

- **093a (READY now, doc only, no product code):** write docs/category-flows.md — the per-archetype
  flow table (steps, funding default, checkout options per 094) derived from the post-089 code +
  v3 spec, PLUS the two human questions below with a recommendation each. No builder guessing.
- **093b (BLOCKED on 089 merge + Q1):** subscription vestige cleanup — remove/reframe the monthly-path
  remnants per Q1's answer, copy ban-list respected ("save up"/"afford"/"budget"), EN+KO, rendered
  Playwright, 089's capital-invariant test stays green.
- **093c (BLOCKED on 093a + Q2):** target-archetype capital-first gap-close per the v3 spec.

## Human questions (asked in the batch report — answers unblock 093b/093c/094)

- **Q1**: For subscription goals, does the monthly option die entirely, or become "top up capital
  monthly until the forever number" (a different framing than "save for it")? Recommendation: top-up
  framing — it keeps low-capital users in the funnel without the nonsensical "save for years" path.
- **Q2**: Confirm target goals default to the capital-first/yield-funded path (v3 flip) with
  monthly-to-deadline as the secondary option. Recommendation: yes, per the v3 spec's own mandate.

## Measurement-window declaration (redteam O3)

093b/c change onboarding+checkout inside 061/065 (→07-26) / 008 (→07-24) windows if built before those
close: shipping DECLARES the touched windows void with a restart at merge; human may veto by holding
implementation to 07-26. (093a is a doc — no window impact.)

## Constraint

SUBSCRIPTION_LADDER membership unchanged (separate open human question). Trust rails untouched.
