# Spec 080: Deploy 013's sitemap quality gate for real — CI must commit the regenerated sitemap children

## Evidence (all in-repo, gathered live 2026-07-14)

013 (SHIPPED 2026-07-11, HIGH, verifier PASS) added the ≥2-qualifying-pools + $10M-TVL quality gate to `generate-sitemap.js`, expecting deployment via the daily CI: specs/013-notes.md line 21 says counts "will appear naturally on the next run of `.github/workflows/sitemap-update.yml` (the existing daily CI job that already has live network access **and commits regenerated sitemap files**)". That belief is false for the files 013 gates:

- `.github/workflows/sitemap-update.yml` line 75 runs `node generate-sitemap.js` (regenerates ALL ~106 child sitemaps + index + robots.txt), but the `git add` at line 125 stages only `sitemap.xml sitemap-token-pages.xml sitemap-chain-pages.xml sitemap-token-pages-ko.xml sitemap-chain-pages-ko.xml` (+ non-sitemap assets). The ~106 generator-owned children (`sitemap-chain-*.xml`, `sitemap-category-*.xml`, `sitemap-tokens-all.xml`, `sitemap-main.xml`, …) are regenerated in the CI workspace and thrown away, every run.
- Proof from 078's live count (its first live proof, commit `6e2853340`, 2026-07-14 03:27 UTC): commit message says "6202 total URLs" while the committed tree contains **25,051** — the 6,202 was counted over the fresh workspace (fresh children + stale orphans), the 25,051 is what actually deploys.
- Live regen this session (network open per standing decision 2026-07-12; full fetch confirmed, 15,408 pools): generator-owned children = **448 URLs across 106 files** vs ~20,700 URLs in their committed versions. `sitemap-tokens-all.xml`: fresh 107 vs committed 4,147. `sitemap-chain-Ethereum.xml`: fresh 67 vs committed 2,301. Per-family drop logs fired per 013's spec ("expect a material drop" — 013.md line 30/34).
- Index/children desync: the CI **does** commit the fresh index (`sitemap.xml`, 110 refs, fresh `<lastmod>`) over stale children — the index claims freshness for content that never updates.
- 12 orphan child sitemaps are committed+deployed but unreferenced by the index and no longer produced by the generator: `sitemap-combos.xml` (1,300 URLs), `sitemap-chains.xml` (96), and 10 `sitemap-chain-*.xml` (Arbitrum-Nova, Bitcoincash, Blast, Chiliz, Cosmos, Doge, Litecoin, Mode, Soneium, Wc). Stale-artifact cleanup precedent: 031.
- Consequence for the north star: the 010 GSC crisis diagnosis attributes the Discovered/Crawled-not-indexed classes to exactly this sitemap bloat; the human is manually checking GSC through ~2026-08-23 (interview Q3) — the validation window is currently measuring a NO-OP for 013's sitemap leg. Organic traffic is the top of the waitlist funnel (062/079 CTAs) and distribution is the binding constraint (signals/2026-07-13.md).

## Hypothesis

Committing the generator's real output (and removing its stale orphans) finally deploys 013's crawl-budget fix; GSC not-indexed classes should start draining over the human's ongoing manual validation window. This is the missing last mile of an already-sanctioned change, not a new de-indexing decision.

## NEVER-list assessment (verifier: judge this independently)

"Deleting or de-indexing SEO surface" is NOT triggered: no content page is deleted or noindexed; `?token=`/`?chain=`/`?pool=` URLs keep rendering unchanged; only sitemap *membership* changes, applying the gate 013 shipped through the sanctioned path (verifier PASS, auto-merged per the human's risk policy, visible in BACKLOG/reports since 07-11 with the shrink expectation documented). The 12 deleted files are index-unreferenced stale lists (031 cleanup precedent), not content. If the verifier disagrees → BLOCKED, PR left open, per build.md step 5.

## Plan

1. `generate-sitemap.js`: after writing children + index, delete root `sitemap-*.xml` files it did not write this run, EXCLUDING the four page-sitemaps it does not own (`sitemap-token-pages.xml`, `sitemap-token-pages-ko.xml`, `sitemap-chain-pages.xml`, `sitemap-chain-pages-ko.xml` — generated by generate-token-pages.js/generate-chain-pages.js). Log every deletion (auditable, never silent). Use the existing `generatedFilenames` list as the source of truth.
2. `.github/workflows/sitemap-update.yml`: stage ALL sitemap children including deletions — add `git add -A -- 'sitemap*.xml'` (quoted pathspec so git matches deleted tracked files; shell glob would miss them). Keep the rest of the existing add-list unchanged.
3. Run `node generate-sitemap.js` at repo root LIVE this session and commit the real output (066 precedent: network is open, "network-blocked" claims are disproven — commit real regenerated files, never fixtures): updated index + children + robots.txt + the 12 orphan deletions. Do NOT run generate-token-pages.js / generate-chain-pages.js (leaf pages out of scope, daily CI owns them).
4. New `test_sitemap_cleanup.js` added to the npm test chain: exercises the cleanup logic in a temp dir (stale generator-owned file → deleted; page-sitemap → never deleted; fresh-written file → kept). No network.

## Acceptance criteria

- [ ] Workflow stages every regenerated/deleted root `sitemap*.xml` (quoted git pathspec, `-A` so deletions land) — the exact files `generate-sitemap.js` + the page generators write, nothing outside them added to the list
- [ ] `generate-sitemap.js` deletes stale generator-owned children it didn't write, logs each deletion, and NEVER deletes the four page-sitemaps (tested)
- [ ] Committed tree after the item: every `sitemap.xml` index ref resolves to an existing file; zero root `sitemap-*.xml` unreferenced by the index; children content = real live generator output from this session (post-013 gate), orphans gone
- [ ] Trust rails byte-untouched: `SITEMAP_MIN_TVL`/`APY_SANITY_LIMIT` values and gate logic unchanged; no change to app.js/planner.js
- [ ] `node validate-sitemaps.js` passes on the committed set; `test_sitemap_xml.js` passes; new `test_sitemap_cleanup.js` passes and is wired into `npm test`
- [ ] NORTH_STAR test chain green (`node test_planner.js && node test_protocol_parsing.js && node test_qualifier_fix.js`), all runs timeboxed ≤5 min foreground
- [ ] Before/after URL counts recorded in specs/080-notes.md (committed 25,051 → expected ~4.9k total incl. page sitemaps; generator-owned ~20.7k → ~448)

## Measurement

GSC Discovered/Crawled-not-indexed classes over the human's manual checks (through ~2026-08-23, interview Q3 — no connector, no nagging). Proxy visible immediately: the next daily CI commit message (078's count) should report a total matching the committed tree — the count and the truth converge.

## Territory notes

- `generatedFilenames` (generate-sitemap.js:391) tracks exactly what the generator wrote; index skips empty families (`sitemaps[filename].length > 0` guard) — cleanup must key off files actually written, and note an empty-family file that was written but excluded from the index should still be kept on disk only if written (edge: current code writes only non-empty families at line 383? verify — if a family is written but empty it's a generator invariant to preserve, not change).
- The four page-sitemaps join the index via `fs.existsSync` guards (generate-sitemap.js:399-430) — running at repo root (where they exist) keeps them referenced; the scratch run (106 refs) vs repo run (110 refs) difference is exactly these guards.
- The workflow's commit races with other pushes and rebases with `-X theirs` (line ~151) — regenerated output rebases safely; nothing in this change alters that.
