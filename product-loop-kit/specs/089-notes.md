# 089 — build notes (deviations, conservative choices)

## What was built
- `app.js` sortBy default `'apy'` → `'tvl'` (single product-code line).
- Regenerated `app.compiled.js` + `app.compiled.min.js` (home.html loads the min).
- New rendered-Playwright gate `test_default_sort.js` + appended to the npm test chain.

## Conservative choices
- Reused 092's existing `sortBy === 'tvl'` branch verbatim — the demote (yielding
  pools before no-supply-yield, then TVL desc) already exists in all three sort blocks;
  089 only flips which branch runs by default. No new sort code.
- Did NOT touch `userSortedApy`: its semantics (APY click disables the anomaly-demote;
  TVL click re-enables it) are unchanged. The initial state is now
  `sortBy='tvl', userSortedApy=false`, which is a valid, already-exercised combination.
- No translations, no analytics-event, no persistence changes — out of scope per spec.

## Deviations
- None material. Token-mode fixture uses distinct symbols `USDC-AAA/BBB/CCC` (the `-`
  splits so the `USDC` segment matches `?token=USDC`, while the rendered `.pool-symbol`
  text stays fully distinct), which let the test assert the FULL rendered TVL-desc
  ordering in token mode too (not just a first-card check). Compile/minify required a
  local `npm install` (node_modules was absent); only app.compiled.js + app.compiled.min.js
  changed — PoolDetail + other min assets regenerated byte-identical.

## Test result (pre-verifier)
- test_default_sort.js: 4/4 passed. test_zero_yield_demote.js: 4/4 (no regression).
  test_list_default.js: 3/3 (no regression).

## Test / verify
- Acceptance = rendered DOM order on the real UI (Playwright, fixture-routed), per the
  2026-07-11 standing decision — never source strings.
