# 090 notes — Analytics default view → list/rows (build loop 2026-07-14)

## Scope
Smallest change per spec: flip the analytics browse default from grid to list.

## What shipped
- `app.js:758` `useState('grid')` → `useState('list')` — the entire product change.
  `viewMode` is component state only (grep-confirmed: 4 sites — the useState + the two
  toggle buttons + the container-class selector; no localStorage, no URL param). Both views
  render the SAME `renderPoolCard`; only the container class differs (`app.js:2904`), and
  `.pools-list` CSS already exists (`style.css:1957+`). No data/render-logic change.
- Regenerated `app.compiled.js` + `app.compiled.min.js` (via `node compile-app.js` +
  `node minify-assets.js`) — home.html loads the MIN file, so regen is what actually takes
  effect (the verifier's mutation check confirmed this: reverting app.js alone doesn't bite;
  mutating app.compiled.min.js does). Other compiled/min files regenerated byte-identical
  (no git change).
- `test_list_default.js` (new, rendered Playwright, fixture-routed + snapshot-stale-stubbed
  per test_search.js / spec-059 pattern): asserts `/?token=USDC` and `/?chain=Ethereum`
  default to `.pools-list` with cards, and the ▦/☰ toggle switches both ways — on the real
  rendered DOM, not source strings (2026-07-11 UX-acceptance standing decision). Appended to
  the `npm test` chain. 3/3, zero page errors.

## Conservative choices / deviations
- None of substance. Did NOT ship 095's row polish (column alignment, tabular-nums, calmer 0%
  styling) — that's the soft-paired follow-up item; 090 is the default flip only, exactly as
  the human's audit split them (090 READY independently, higher-scored than 095).
- No translations touched (toggle titles pre-existed; no new user-facing strings).

## Verification
- `node test_list_default.js` → ✓ 3/3 (real chromium).
- Verifier PASS LOW 7/7 independently re-derived, incl. its own mutation check on the loaded
  artifact (app.compiled.min.js) and a 360/768/1280 + dark render spot-check (all `.pools-list`
  with cards). SEO lifeline preserved (`?token=`/`?chain=`/`?pool=` still render pool cards —
  same renderPoolCard). Bare `/` planner unaffected (no viewMode in planner.js/plan.html).

## Follow-up (not this loop — one-item rule)
- 095 (list-view SOTA table polish) now has more reason to ship: list is the default the ICP
  sees first. Human visual spot-check advisable on the live analytics browse (render-path
  default change; the 003 smoke gate doesn't cover pixel-level list layout).
