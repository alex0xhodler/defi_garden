# 128 — PR explainer (LOW tier)

**What:** Removed the redundant third copy of the daily-earnings number on the pool-detail page. The figure was showing in the top "Daily/Monthly earnings" stat cards, in the yield calculator's result, AND a third time as a `"Quick estimate for $1,000: $0.1/day"` subhead on the calculator header. That subhead is now a non-numeric invite line ("See your daily, weekly & monthly returns" / KO equivalent).

**Why:** Pool-detail is the north-star conversion surface (NORTH_STAR.md 2026-07-23). The same number three times lengthens the page and dilutes the "Garden this pool" / "Start Earning" CTAs. One at-a-glance earnings surface (the stat cards) + the interactive calculator is enough; the collapsed-header preview added nothing.

**Diff (reading order):**
- `translations.js` — `quickEstimate` key (EN + KO) → `calcSubPrompt` (non-numeric). Only call site was PoolDetail.js:750, so the old key is gone, not orphaned.
- `PoolDetail.js:750` — subhead now renders `t('calcSubPrompt')` instead of the `$X/day` `quickEstimate` string.
- `PoolDetail.compiled.js`, `PoolDetail.compiled.min.js`, `translations.min.js` — regenerated committed outputs (`npm run compile && npm run minify`); the runtime loads the `.min.js`, so these carry the change to prod.
- `test_earnings_dedup.js` — new rendered-Playwright acceptance test (4/4).

**Risk:** LOW — copy/markup dedup on one surface, EN + KO updated together, no dependency changes, no number math changed (trust rails untouched), diff well under 150 lines.

**Test:** `node test_earnings_dedup.js` (4/4). Manually: `npm run dev`, open `/?pool=747c1d2a-c668-4682-b9f9-296708a3dd90` → the calculator header reads "See your daily, weekly & monthly returns" with no "$…/day", and the daily/monthly figures appear only in the stat cards (+ the calculator when expanded).

**Needs human:** render-path merge — flag `needs human visual spot-check` per the pre-003 policy (no automated pixel QA). Nothing blocking.
