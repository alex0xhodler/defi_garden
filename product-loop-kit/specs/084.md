# 084 — Real assertions for the NORTH_STAR test gate: test_protocol_parsing.js + test_qualifier_fix.js

Status: IN_PROGRESS (2026-07-14)
Risk: HIGH (tests-only by category, but the rewrite exceeds the LOW 150-line diff cap; when in doubt → HIGH)
Score: 6.7 — guardrail-integrity band (077 = 6.8, 076 = 6.6, 078 = 5.8); this one repairs the HUMAN-OWNED
test gate itself, which every loop and the verifier run verbatim.

## Promotion provenance (no READY items existed 2026-07-14)

Promoted by the build loop per the "no READY items" rule. Sources checked this run, in order:

- BACKLOG.md: zero READY rows (082/083 shipped earlier today; everything else SHIPPED/DONE/PARKED/BLOCKED).
- reports/2026-07-14.md: its one filed item (082) is shipped; its two open threads (SUBSCRIPTION_LADDER
  drift, spotlight posting) are explicitly human-gated questions — not loop-buildable.
- specs/010-diagnosis.md ranked fixes: all built (011–015).
- Documented unbuilt follow-ups grep across specs/*-notes.md: 012's dead-`?pool=` candidate → built (072);
  061's dead ctaWaitlistMicro → built (070); 075's og outRoot → built (076); 081's llms churn → built (083);
  082's deploy-integrity follow-up is conditional on a recurrence that hasn't happened. The oldest
  still-unbuilt, explicitly recorded follow-up is 018-notes.md:152-156 — THIS item.
- North-star preference note: all north-star-direct work is shipped, traffic-gated (063/065 precedent),
  or human-gated (ladder question blocks subscription-copy work; spotlight posting is human-owned).
  Guardrail integrity is the strongest qualified candidate; NORTH_STAR: broken beats persuasion.

## Evidence

1. `test_protocol_parsing.js` (113 lines) and `test_qualifier_fix.js` (124 lines) contain ZERO assertions
   and ZERO non-zero exit paths — grep for `assert|process.exit` matches nothing in either file
   (verified live this run). They print `console.log` output and always exit 0. They CANNOT fail.
2. Both test a STALE INLINE COPY of a simplified parser, not the real one. The real
   `parseNaturalLanguageQuery` (app.js:129, top-level const, self-contained) has since gained behavior
   the copies know nothing about: lend-stem regex (`\blend(ing|ers?)?\b`, the 018 "kamino lenders" fix),
   word-boundary chain aliases with live-chain fallback (the "Plasma" fix), dynamic protocol aliases from
   `allProtocols` with chain-name conflict skips, protocol→chain auto-set. This is the exact 017 failure
   class on record: "fixtures passed, product failed" (BACKLOG 017, specs/018-notes.md).
3. These two files are 2 of the 3 commands in NORTH_STAR.md's verbatim Test line
   (`node test_planner.js && node test_protocol_parsing.js && node test_qualifier_fix.js`), which
   "Build loops and the verifier run these verbatim." Two-thirds of the human's official test gate is
   theater: a real parser regression passes it silently.
4. Left in writing as a known follow-up: specs/018-notes.md:152-156 ("the stale inline-copy tests 017's
   evidence section flagged — they only console.log, never assert... Left as a known follow-up").
5. Funnel relevance: NL search is the SEO-lander funnel entry (search_success 101/14d;
   22/100 searches carry real results_count per BACKLOG 018/020 rows).

## Change

Rewrite BOTH files (keeping their exact filenames — the NORTH_STAR Test command is human-owned and must
keep passing verbatim; NORTH_STAR.md itself is NOT touched) so each:

- Loads the REAL parser from `app.js` source: `fs.readFileSync` (repo-relative via `__dirname` — the 083
  ENOENT class is banned), slice the top-level `const parseNaturalLanguageQuery = (query, ...` declaration
  by brace/paren matching to its closing `};`, and evaluate it in a `node:vm` context (the
  test_translations_fallback.js pattern). Zero inline copies of parser logic may remain in either file.
  The extraction must FAIL LOUDLY (non-zero exit, clear message) if the declaration can't be found —
  a renamed/moved parser must break the gate, not silently skip it.
- Asserts real expected parse results and exits non-zero on any failure, with a per-case pass/fail line
  and a final summary (the house test style, e.g. test_translations_fallback.js / test_lastmod_honesty.js).

Case split (assert on the CURRENT, verified-real behavior of the extracted parser — derive expectations
by running it, then lock in only cases whose behavior is clearly intended per code comments/017/018
history; if a probe case surfaces what looks like a genuine parser bug, DOCUMENT it in 084-notes.md and
drop the case — fixing the parser is out of scope):

- `test_protocol_parsing.js` — protocol parsing on the real function: context-keyword method
  ("usdc on aave" → Aave), chain-name conflict skip ("best yields on base" → NO protocol, chain Base),
  direct detection ("compound yields" → Compound), protocol-first ("aave on arbitrum" → Aave + Arbitrum),
  dynamic aliases via `allProtocols` (friendlyName/originalNames objects, e.g. kamino-lend → "kamino
  lenders" matches — the 018 fix), static fallback when `allProtocols` is empty, dedupe.
- `test_qualifier_fix.js` — qualifier-word handling on the real function: qualifier words never parse as
  tokens/protocols ("best yields on base" → token '', "top usdc yields" → USDC, "highest apy on ethereum"
  → chain Ethereum + token ''), lend-stem poolTypes ("usdt lending on venus" → Lending), generic "yields"
  does NOT add Yield Farming, explicit "farming" does.

## Acceptance criteria

1. Zero assertion-free paths: both files fail (exit ≠ 0) when any expectation is violated; neither
   contains a copy of parser logic (grep: no `protocolAliases`/parsing-logic literals defined in-file
   beyond expected-value fixtures).
2. Both evaluate the parser EXTRACTED from `app.js` at test time; deleting or renaming
   `parseNaturalLanguageQuery` in app.js makes both fail loudly.
3. NORTH_STAR verbatim gate passes at HEAD: `node test_planner.js && node test_protocol_parsing.js &&
   node test_qualifier_fix.js`.
4. Mutation check (verifier-reproducible): a representative regression in the real parser — e.g. removing
   the qualifier-word filter, or the chain-name skip in protocol Method 1, or the lend-stem regex — makes
   the corresponding test FAIL while at HEAD it passes.
5. `app.js` and all product/render code byte-identical; npm test chain membership/order unchanged.
6. Both tests are pure Node (no Playwright, no network), run from any cwd, each completes in seconds
   (5-min timebox trivially met).

## Out of scope

- Fixing any parser bug the new assertions surface (document in 084-notes.md as a follow-up candidate).
- The npm-test-chain 5-min-timebox wall (documented separate candidate — LOG 082/083 entries).
- test_search.js (018's rendered E2E gate) — untouched; this item complements it at the unit layer.
- NORTH_STAR.md — human-owned, not edited.

## Measurement

No isolable Mixpanel funnel metric (loop-infrastructure/guardrail-integrity fix, 074/076/077/078
disclosure pattern). Live proof = the NORTH_STAR test gate can now actually fail: mutation check in the
verifier run + the gate catching real parser regressions in future loops.
