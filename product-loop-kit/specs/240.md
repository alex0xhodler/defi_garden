# Spec 240: one footer, one voice — retire "Degen Love" from the money surfaces

## Evidence
UX audit F5: grid + pool-detail footers render "Powered by Defillama API. Made with AI & Degen Love."
(`app.js:2989`, `app.js:3541`; `translations.js:140` `madeWith`, `:193` `footerMadeWith` — all four
survive the 225 branch) while the landing's closing section reads "A calmer way to explore yield."
The ICP (NORTH_STAR) is the cautious retail saver; "Degen" names the persona the product explicitly
does not serve, placed in the footer trust position of the two surfaces where money decisions happen.
The three surfaces also render structurally different footers.

## Hypothesis
One footer component/copy set with ICP-consistent voice closes the brand split. DefiLlama attribution
STAYS — honest sourcing is a trust rail of the copy, not decoration. Confidence: supported (human's
"off putting" directive + ICP decision 2026-06).
- Instance of: brand-voice divergence across surfaces (the content half of the two-systems root cause).
- Class closed: app surfaces yes; generated static pages' footer (landing.js template) included —
  regen required; stories/ excluded (persona pages have deliberate voice), noted.

## Change
One footer contract (Quiet classes, after #393): DefiLlama attribution + Browse tokens / Browse chains
+ an ICP-consistent sign-off (builder drafts, e.g. "Education, not advice." — reuse the landing's
existing trust vocabulary, no new claims). Replace `madeWith`/`footerMadeWith` values in EN + natural
KO together; delete the "Degen Love" string everywhere (grep-zero). Regen the static estate if
`landing.js`/generators embed it. OUT of scope: footer link additions, legal pages.

## Acceptance criteria
- [ ] `grep -ri "degen love" app.js translations.js landing.js tokens/ chains/ stories/` → stories-only
      or zero; app surfaces zero.
- [ ] Rendered footer on `/`, `/?token=USDC`, `/?pool=<id>`: identical structure + copy (EN and KO both
      natural, updated together — one language alone is a verifier FAIL).
- [ ] DefiLlama attribution present on all three (the honesty leg — its absence is a FAIL).
- [ ] If generators embed footer copy: regen delta controlled per `seo-surface-regen-delta.md` (copy-only
      delta, zero `<loc>` changes).
- [ ] Non-vacuity: reintroduce the old string in one surface → grep/render test red; restore.

## Measurement
Build-leg only.

## Risk tier (builder's guess)
LOW — copy + footer markup (EN+KO). Escalates to HIGH only if the generated-estate regen is needed.

## Open questions
None — sign-off wording is builder's draft from existing vocabulary; the human can veto post-merge
(policy c).
