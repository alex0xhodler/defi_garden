# 042 — PR explainer (LOW tier)

Re-enables 7 `Analytics.*` calls in `app.js` that were disabled at the call site since a 2025-08-21 render-crash hotfix (`trackFilterChange` x4, `trackFiltersReset`, `trackFilterCombination`, `trackNavigation`). Root-caused the disable (git history, unshallowed for this) to a real crash from that era; confirmed every current call site now passes a well-formed object/string literal, never the possibly-undefined value that caused the original crash. Live-verified 6 of 7 firing correctly via Playwright against the real rendered UI with zero page errors; the 7th (`pool_type` variant) turned out to be pre-existing unreachable dead code (its handler is never called by the current UI), documented in specs/042-notes.md rather than silently left unexplained.

Diff: `app.js` only, 7 one-line uncomments, no logic changes, no new dependencies. Restores `filter_change`, `filters_reset`, `filter_combination`, and back-navigation instrumentation to the analytics app — closes part of the same "unmeasured product surface" gap 020/039 addressed elsewhere.
