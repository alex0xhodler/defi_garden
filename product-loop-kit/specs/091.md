# 091 — TICKET: smooth cold-load planner intro (kill the unstyled-flash "web 1.0" sequence)

Status: READY (first in the batch build order) · Risk: HIGH (funnel-top first paint, CSS loading
strategy) · Score: 8.0

## Report (human)

"Make the intro into plan smoother when no cache, now it's very ugly the 1s loading sequence with labels
all around the screen, make it smooth animation like modern chat interfaces instead of the old web 1.0
search mechanics."

## Triage (operator hypothesis — verification is BLOCKING, redteam O8)

`style.min.css` + `planner-styles.min.css` load via the async `media="print" onload=media='all'`
pattern (perf era 052-054; home.html head + plan.html:55-58). Cold cache ⇒ markup renders before the
swap ⇒ a window of unstyled/partially-styled scattered labels ⇒ snap. HYPOTHESIS — the builder must
reproduce with a throttled, cache-disabled Playwright trace (screenshots at first paint) BEFORE writing
any fix. If the flash has a different cause, STOP and respec (don't build the wrong fix).

## Change (hardened per redteam O8)

1. BLOCKING: cold-cache reproduction trace, before/after screenshots committed to the notes.
2. Fix mechanism, in preference order:
   a. Render-blocking load for the small planner-critical CSS in PLANNER mode only (the router already
      knows the mode pre-paint), keeping analytics-mode loading untouched; measure the cost.
   b. Gate the planner mount/reveal on the stylesheet's load event (content hidden-then-staged, never
      unstyled).
   c. Hand-inlined critical CSS is LAST RESORT (no-build repo ⇒ permanent drift risk across TWO HTML
      heads) and, if chosen, REQUIRES a sync-check test that fails when the inline block drifts from
      planner-styles.css.
3. Entrance: chat-interface staged reveal (calm fade/translate, ~100-150ms stagger, existing
   gp-animate-in conventions), `prefers-reduced-motion` ⇒ instant. Neumorphic tokens only; no bounce.

## Hard perf floor (redteam O8)

PSI mobile ≥ 85 (baseline 88, 2026-07-12) — or local Lighthouse equivalent when PSI unavailable
in-sandbox, with the numbers recorded in notes. "No material regression" is defined as this floor.

## Measurement-window declaration (redteam O3)

Redesigns the splash presentation while 068 (hero copy, →07-27) measures that surface. Shipping DECLARES
068's window void and restarts a 14d presentation-inclusive window at merge (copy itself byte-identical
— 068's strings untouched). Human may veto by holding 091 to 07-27.

## Acceptance sketch

Cold-cache trace shows zero unstyled-flash frames (computed-style asserts at first paint); staged
entrance rendered; reduced-motion instant; PSI/Lighthouse floor met with numbers in notes;
test_hero_copy.js still green (copy untouched); test_smoke.js 8/8.
