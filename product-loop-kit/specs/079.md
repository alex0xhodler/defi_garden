# 079 — Waitlist CTA on the hub/az spine pages (closes 062's last gap)

**Status:** IN_PROGRESS 2026-07-14 · **Score:** 6.9 · **Risk:** HIGH (user-facing block on generated SEO surface; 062 precedent)

## Evidence (documented, in-repo)

- LOG.md entry #201 (2026-07-14 NO-OP tick) verified 062's waitlist CTA landed on
  2043/2044 `tokens/` and 85/86 `chains/` pages — the misses being `tokens/index.html`
  and `chains/index.html` — and recorded the gap as a candidate.
- This run's recount shows the gap is **58 pages, not 2**: the prior tick never checked
  `tokens/az/*.html` (27 files, `grep -c waitlist` = 0) or the KO mirrors
  (`ko/tokens/index.html`, `ko/tokens/az/*` ×27, `ko/chains/index.html` — all 0).
- These are the internal-linking **spine** shipped by 045: home.html links only to the
  hubs, and every non-top-N token is reachable exclusively via home → hub → az → leaf.
  They are now the ONLY pages on the 2,131-page SEO surface with zero path to the
  waitlist (the north-star funnel, `waitlist_submitted`).
- Promotion overrides the #201 tick's "marginal vs. a deploy" disqualification with
  recorded reasoning (078 precedent): the alternative this run is a fourth NO-OP LOG
  commit costing the SAME PR + Vercel deploy while shipping nothing; and the "2 pages"
  premise undercounted by 29×.

## Change

All in the generators — never hand-edit generated output (NORTH_STAR risk policy).

1. **`translations.js`**: one new key pair, `tcpWaitlistPitchHub` (no args — generic,
   not per-subject), EN + natural KO together. Honest card framing consistent with
   `tcpWaitlistPitchToken`/`tcpWaitlistPitchChain`: card spends yield, principal stays,
   waitlist = early access, card doesn't exist yet (micro line `tcpWaitlistMicro` is
   reused verbatim by the shared renderer — do not re-word). Regenerate
   `translations.min.js` via `npm run minify` (061 lesson: min artifacts must never
   drift, even though this key is only read at generation time).
2. **`generate-token-pages.js`**:
   - `renderTokenHubPage`: insert `renderWaitlistCtaHtml(t('tcpWaitlistPitchHub'), 'hub', 'seo_tokens_hub', t)`
     after the hub cards, before the trust note `<p class="note">` (same relative
     placement as leaf pages).
   - `renderTokenAzPage`: same block with source `seo_tokens_az`.
   - `renderHubStyleBlock()`: append `renderWaitlistCtaStyle('hub')`. The renderer's
     `<a class="hub-cta">` then reuses the hub pages' EXISTING `.hub-cta` button style —
     zero new button chrome (2026-07-10 reuse rule). Do not duplicate any style rule
     that `.hub-cta` already provides.
3. **`generate-chain-pages.js`**: `renderChainHubPage` gets the same block with source
   `seo_chains_hub`. (It already imports `renderWaitlistCtaHtml`/`renderWaitlistCtaStyle`
   from the token module; the hub style block comes via `renderHubStyleBlock` —
   confirm import list, extend if needed.)
4. **planner.js: NO changes.** `src` is free-form (`urlParams.get('src')` → `source`
   property on all five waitlist events) — new sources segment in Mixpanel automatically.
5. **Committed output** (066 attempt-1 lesson — never claim "lands on next CI run" when
   the sandbox can fetch): run both generators LIVE (`node`/`curl` reach
   yields.llama.fi; browser doesn't — 2026-07-12 standing decision) with `--out` to a
   scratch dir (076 made scratch runs og/-safe), then copy ONLY the 58 spine files into
   the repo: `tokens/index.html`, `tokens/az/*.html`, `chains/index.html` + the three KO
   mirrors sets. Full-surface regen is NOT committed (leaf pages would churn thousands of
   data-only diffs; daily sitemap-update.yml reconciles the snapshot drift, as it already
   does between CI runs).

## Acceptance criteria

1. All 58 spine files in the repo contain exactly one waitlist CTA block: heading
   `tcpWaitlistHeading` copy, pitch, `href="/plan.html?waitlist=1&src=<source>"` with
   the correct per-surface source (`seo_tokens_hub` / `seo_tokens_az` /
   `seo_chains_hub`), and the verbatim `tcpWaitlistMicro` honesty line ("Card doesn't
   exist yet"). KO files carry the KO strings.
2. Rendered product behavior (Playwright, real Chromium, fixture/local-server pattern —
   NOT string fixtures alone, per NORTH_STAR 2026-07-11): serve the repo statically,
   load `tokens/index.html`, assert the CTA link is visible, click it, assert plan.html
   opens the waitlist modal and `waitlist_opened` fires with `source=seo_tokens_hub`
   (test_waitlist_seo_entry.js already establishes this exact pattern for leaf pages —
   extend it, don't fork it).
3. `test_hub_pages.js` extended: CTA-presence assertions for hub, az, chain-hub, EN+KO,
   plus a no-double-render guard (exactly one `-waitlist` block per page).
4. Full NORTH_STAR test chain green (5-min foreground timebox per test per standing
   decision 2026-07-11; test_smoke.js now passes in-sandbox since 077).
5. Trust rails untouched (no APY/TVL logic in the diff). No new colors/gradients — the
   block reuses `renderWaitlistCtaStyle` tokens + existing `.hub-cta` chrome.
6. `translations.min.js` regenerated and byte-consistent with translations.js
   (test_minified_assets.js guards this).
7. Sitemaps/canonicals/og images untouched (hub URLs already in the index; no new URLs).

## Measurement

No isolated window: hub sources land inside 062's `waitlist_opened(source=seo_*)`
stream (window to 2026-07-27). New sources `seo_tokens_hub`/`seo_tokens_az`/
`seo_chains_hub` are distinguishable from day one — zero extra instrumentation needed.

## Out of scope

- The yield headline (066/075) on hub pages — hubs have no single honest blended rate
  (mixed-token/mixed-chain aggregate would be a fabricated number; 032's display-honesty
  rule).
- Any leaf-page or planner change; any sitemap change.
