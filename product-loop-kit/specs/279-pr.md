Impact: north-star leg A — every eligible token page answers the canonical token-yield question with a trust-railed, citation-ready comparison.

# 279 — PR explainer (HIGH)

**What changed**: every eligible EN/KO token HTML page and Markdown twin now receives a `Rate stability` answer over pools already admitted by that page's rails, or an honest insufficient-history answer when fewer than two pools qualify.

**The comparison**: DefiLlama's top-level `count` and `sigma` drive a deterministic public ordinal after a 30-observation threshold. Ranked candidates keep TVL, current APY, protocol, and chain visible. Public output contains no numeric Sharpe, sigma/count, or history series.

**Trust boundary**: visible copy says stability measures APY history only and excludes protocol, exploit, depeg, liquidity, governance, and principal-loss risk. x402 remains dark. Generated source links carry `src=seo_token`.

**Consistency**: one shared data object supplies HTML, the Markdown twin, and FAQ JSON-LD facts in EN/KO, avoiding a second factual contract.

<<<<<<< HEAD
**Evidence**: item test 8/8; contract populations 109 token pages, depth 27, 16 Markdown twins, 6 attribution checks, and 5 registry checks. Browser checks covered real generated AAVEUSDC EN desktop/mobile plus KO ranked output and 0X0 insufficient history. Rebased live generation processed 15,598 pools into 2,052 token records and 795 sitemap heads; a second run retained the population.

**Review state**: IN_REVIEW, attempt 1. Source attribution was repaired after the first AMEND; focused re-review and the final rebased review both passed.
=======
**Evidence**: item test 8/8; contract populations 109 token pages, depth 27, 16 Markdown twins, 6 attribution checks, and 5 registry checks. Browser checks covered real generated AAVEUSDC EN desktop/mobile plus KO ranked output and 0X0 insufficient history. Live generation processed 15,607 pools into 2,069 token records and 795 sitemap heads; a second run retained the population.

**Review state**: IN_REVIEW, attempt 1. The reviewer requested source attribution; `src=seo_token` is fixed but has not been re-reviewed.
>>>>>>> 78a1faa4a6 (feat(discovery): add citation-ready rate stability)

**Outcome boundary**: no traffic or citation outcome is claimed. Retrieval correctness, qualified reads, and observed citations/referrals remain separate gated layers.

**Risk**: HIGH. The large generated delta is the expected estate-wide clean cutover, not unrelated hand edits.
