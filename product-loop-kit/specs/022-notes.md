# 022 build notes

- Picked up after reconciling stale BACKLOG.md (021 already shipped as PR #119 but still showed READY; 029-033 shipped with no rows at all — fixed in a separate PR #127, merged before this build started, so this pickup wasn't a duplicate of already-done work).
- Real 021/029-033 output already committed to main: 2,028 real `/tokens/<slug>.html` pages + `sitemap-token-pages.xml`, generated daily by `sitemap-update.yml` CI. 022 rides the same CI job (same reason 021 had to: `api.indexnow.org` is unreachable from this sandbox, confirmed via `curl` → 403, same wall as `yields.llama.fi`).
- Key file: generated a fresh 32-hex-char key via `node -e "crypto.randomBytes(16).toString('hex')"` once, committed as `<key>.txt` at repo root (IndexNow convention: filename = key + `.txt`). `indexnow-ping.js` reads the key from that file at runtime (`findKeyFile`/`loadKey`) rather than hardcoding a duplicate — a mismatch between the two would silently break every future submission, so there's exactly one source of truth.
- `collectUrls`/`extractLocs` reuse the same lightweight `<loc>` regex approach already used elsewhere in this codebase for sitemap XML (no new XML-parsing dependency, no `fast-xml-parser` involvement).
- Capped at IndexNow's documented 10,000-URL limit per submission (current sitemap-token-pages.xml has 2,028 URLs — comfortably under it, but the cap exists as a safety rail for whenever that count grows).
- `--dry-run` and the zero-URLs path both short-circuit before `submitToIndexNow` is ever called — verified in `test_indexnow.js` by monkey-patching `https.request` to throw if invoked, so the "no network I/O without real URLs / outside CI" claim is enforced, not just asserted in prose.
- Workflow: new "Ping IndexNow" step placed immediately after the existing "Ping Google about sitemap update" step, same `if: steps.verify-changed-files.outputs.changed == 'true'` guard — only fires when the regen actually produced a diff, matching how the Google/Bing ping already behaves.
- Full `npm test` run hit the 5-minute foreground timebox and was killed (exit 143) — investigated instead of blindly retrying: `test_smoke.js` alone reproduces a pre-existing sandbox issue, unrelated to this diff — `browserType.launch` fails because the installed `playwright` npm package (freshly `npm install`'d in this session, since `node_modules/` didn't exist yet) expects Chromium build 1228 but `/opt/pw-browsers` only has build 1194 pinned. `test_smoke.js` itself is untouched by this change. Per the standing decision (never wait unbounded; document and proceed), ran the non-browser tests individually instead: `test_planner.js` (190 assertions), `test_protocol_parsing.js`, `test_qualifier_fix.js`, `test_token_pages.js` (36 assertions), and the new `test_indexnow.js` (10 assertions) — all green. `test_smoke.js`/`test_canonical.js`/`test_search.js` (the three Playwright-driven suites) could not be exercised in this sandbox; this mirrors the same class of pre-existing environment gap prior loops (018, 019, 020) hit and documented rather than working around by weakening the tests.
- Residual (same shape as 021): real IndexNow submission can only be verified after a human triggers the "Update Sitemap" Action and checks the job log for the IndexNow response status code.
