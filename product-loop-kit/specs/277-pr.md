Impact: north-star leg A — llms.txt is the first file an agent reads and it currently mentions the live API and MCP server zero times (live grep 2026-08-12: 0 hits for `/api` or `MCP` in served llms.txt)

# 277 — advertise the live API and MCP surface (HIGH)

## Goal and root cause
The deployed agent endpoints were absent from both generated llms artifacts. Because daily regeneration overwrites hand edits, ownership belongs only in `generate-llms.js`.

## What changed, in reading order
1. Import runtime `ENDPOINTS` and `TOOLS`; no endpoint or tool list is hand-typed.
2. Export one shared API & MCP section helper and call it from both llms builders; rail text comes from `trust-rails.js`.
3. Register `test_llms_agent_endpoints.js`, covering builder output and committed artifacts.
4. Regenerate `llms.txt` and `llms-full.txt`.

## Evidence
- After shortening public inventory copy, two runs used the same live 15,607-pool population; run two was unchanged. SHA-256: `llms.txt` `4ecba9b6dbb5a9d105dbf8c782232e5077819237944a35f0d059ba39d315f389`; full `de7a36d6141044b2ce144a88a271dd4a7bbea0d377b54b1296131216e7b89ddf`.
- Green counts: endpoints 2/2; rails 14/14; freshness 8/8; shared source 12/12; link integrity 57/57; rail claims 11/11; registry 5/5.
- Mutation: runtime `/api/regen-mutation` made the test exit 1 and name the stale-output omission; byte-identical restore (`53b421802a9eec7eb04966ce6ee62969a8cba6f81d53f354e6d470e2742d2952`) returned 2/2 green.
- Item 265 already shipped as PR #441, satisfying sequencing. No traffic outcome is claimed; the ≥30-read gate remains open.

## Quiz
1. Why is a direct llms output edit wrong? A permanent B generator overwrites it C MCP rejects it D rails forbid it
2. What owns endpoint/tool names? A fixtures B runtime `ENDPOINTS`/`TOOLS` C markdown D registry
3. Why one helper? A fewer URLs B both builders share one derived section C faster fetch D lower TVL
4. What proved drift detection? A snapshot deletion B runtime fake route failed against stale output C lint D live traffic
5. What remains unclaimed? A deterministic output B traffic outcome C test counts D item 265 sequencing

Answers (base64, `1:B` through `5:B`): `MTpCCjI6QgozOkIKNDpCCjU6Qg==`
