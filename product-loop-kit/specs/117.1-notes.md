# 117.1 — build notes (pool-detail Sharpe annotation, surface leg 1 of 117)

Spec: `specs/117.1.md` (+ `specs/117.md` for the KPI meaning/caveat). Built 2026-07-18,
one build-loop iteration. Coding agent = Opus; operator/verify = Fable.

## Selection note (honest record)
This iteration first mis-selected item **110** (score 5.5) — the READY grep was run against the
pre-reset tree, and after resetting the harness-pinned branch to the latest `origin/main` (the
07-18 heartbeat commit `ee00e8fdd`, which added 117.1) a misread count kept 110 as the pick. 110
was built + verifier-PASSED, then reverted cleanly (tree returned to origin/main state, 110 back
to READY) once the error was caught: **117.1 (6.8) is the highest-scored READY item AND more
north-star-aligned** (a live user-facing trust annotation on the pool-detail SEO-conversion
surface, vs 110's deploy-inert CI plumbing blocked behind human-provisioned 108). Sunk cost on
110 did not justify shipping the lower-scored item. 110 remains READY for a future iteration.

## What shipped
Surfaces the already-computed `pool.kpis.apySharpe` (rate-stability Sharpe, from item 117 /
compute-kpis.js: `(mean_apy − 4.0 risk-free)/stdev`, null for <8 history points or sd=0) as ONE
calm extra line inside the existing 088.1 `rate-track-record-note` block on pool detail.

- `PoolDetail.js` (~L1357): a second child of the `rate-track-record-note` div, after the stdev
  IIFE, guarded by `typeof pool.kpis.apySharpe === 'number'` (absent when null → graceful hide,
  same discipline as the block's `historyPoints >= 1` guard). Inline `marginTop:'6px'` only —
  ZERO new CSS, reuses the parent note's neuro surface/color/font. Value via `_formatNum`
  (dimensionless ratio, NOT `_formatApy`).
- `translations.js`: new `rateSharpeNote(s)` key, EN + natural KO. Both name the ~4% risk-free
  benchmark AND carry the mandated principal-risk caveat ("not a measure of principal safety" /
  "원금 안전성을 나타내는 지표가 아닙니다") — never framed as a safety score.
- Regenerated `PoolDetail.compiled.js`, `PoolDetail.compiled.min.js`, `translations.min.js`
  (prod loads compiled+min). `app.compiled.*` correctly UNCHANGED (app.js not touched).
- `package.json`: `node test_kpi_sharpe_annotation.js` added after `test_kpi_tvl_trend.js`.

## Scope
Pool-detail annotation ONLY. 117.2 (analytics "risk-adjusted" sort) and 117.3 (planner blend
preference) are separate later sub-items per 117's "one at a time" mandate — untouched here.

## Verification
- `node test_kpi_sharpe_annotation.js` → 4/4, REAL rendered Playwright (house harness, vendored
  React/Babel + DefiLlama-shaped fixture, external hosts routed/blocked per the 2026-07-12
  sandbox pattern): S1 finite apySharpe renders the annotation with the "4%" benchmark + the
  principal-safety caveat, no raw key; S2 apySharpe:null → parent note renders but annotation
  ABSENT; S3 `?lang=ko` → Korean copy present; S4 zero page errors.
- `node test_kpi_track_record.js` → 7/7, `node test_kpi_tvl_trend.js` → 8/8 (no regression on the
  sibling notes in the same block).
- Regen idempotency: a second `compile-app.js` + `minify-assets.js` run reproduced the identical
  3-file diff (compiled/min byte-consistent with source) and left `app.compiled.*` untouched.

## Trust rails / NEVER-list
Byte-untouched. The annotation reads a derived `kpis.apySharpe` and renders it with a caveat;
`APY_SANITY_LIMIT`/`DEFAULT_MIN_TVL`/anomaly flags/degen haircut, the `__APP_MODE` router, and
`?token=`/`?chain=`/`?pool=` behavior are all unchanged (grep-confirmed). It renders only inside
the existing block (which runs only on a rendered, rail-passing detail pool). Not on the NEVER
list. Renders dormant until a pool accrues ≥8 daily history points (~2026-07-22) — the "land now,
lights up as data accrues" pattern the compute leg used.

## Risk tier: HIGH
User-facing render-path change presenting a NEW number derived from trust-rail data + EN+KO on the
pool-detail SEO arrival surface ("anything user-facing beyond copy/styling" + "when in doubt →
HIGH"). Full explainer + 5-question quiz in `specs/117.1-pr.md`. Auto-merges after verifier PASS +
tests green (autonomy (c)); not on the NEVER list. Needs human visual spot-check (pool-detail
render path; 003 pixel gate unshipped).
</content>
