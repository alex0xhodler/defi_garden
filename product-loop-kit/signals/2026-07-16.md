# Signals snapshot — 2026-07-16 (heartbeat, live `Mixpanel` connector)

Project: defigarden (4042048) · org Equitee (2885044) · via `Mixpanel` connector (uppercase — live again this run; lowercase `mixpanel` still needs OAuth). Prod filter = `$current_url contains www.defi.garden`. `Get-Business-Context` still errors (org id not configured) — read-only queries used directly.

## HEADLINE: genuine no-op — every number re-confirms the 2026-07-15 close
North star `waitlist_submitted` = **0** (structurally absent). Prod traffic flat. Guardrails clean. Backlog drained of tractable autonomous work; distribution stays the sole binding lever and it is human-owned. Nothing to build, nothing to close, no scores to change.

## Lexicon — 19 events (unchanged)
`Get-Events` = **19** = 17 product events + 2 Mixpanel auto-events (`$session_start`, `$session_end`). Still structurally absent: `waitlist_submitted`, `waitlist_opened`, `share_link_created`, `pool_click`, `garden_cta`.
→ **North star `waitlist_submitted` = 0 from any host, ever.** No card-funnel event has ever fired (distribution-gated; plumbing verified sound 07-13).

## Real prod trend (`$current_url contains www.defi.garden`, daily total, `query_id eb800dd5`)
| date | session_start | plan_created | search_success | error_occurred | share_link_opened |
|------|---------------|--------------|----------------|----------------|-------------------|
| 07-09 | 7 | 1 | 0 | 0 | 0 |
| 07-10 | 6 | 1 | 0 | 0 | 0 |
| 07-11 | 10 | 0 | 0 | 0 | 0 |
| 07-12 | 16 | 0 | 1 | 0 | 0 |
| 07-13 | 11 | 0 | 0 | 1 | 0 |
| 07-14 | 5 | 0 | 0 | 0 | 0 |
| 07-15 | 5 | 0 | 0 | 0 | 0 |
| 07-16 | **0 so far** | 0 | 0 | 0 | 0 |

→ Flat ~5–16/day trickle, unchanged. `plan_created` prod = **0 since 07-10**. `search_success` prod = 1 total (07-12). **No prod `share_link_opened` ever** (share row absent entirely from the prod-filtered result → 0). 07-15 finalized at 5 sessions (yesterday's eighth pull read 4 mid-day). 07-16 has no events yet — acting on an empty partial day would break the minimum-sample rule.

## Guardrails — clean
- `error_occurred` (prod): **0 on 07-14 and 07-15, 0 so far 07-16.** The 07-13 `ReferenceError: translations is not defined` (082, shipped 07-14) has **not recurred** — window to 2026-07-28 clean so far.
- Analytics-URL SEO lifeline: `?token=`/`?chain=`/`?pool=` render path intact (unchanged; no render-path merges since the 07-15 close).
- Planner conversion: uncomputable honestly at this sample (0 prod `plan_created` since 07-10) → insufficient data, qualitative only.

## Sample discipline
Real-prod n: ~59/wk sessions (07-09→15 = 7+6+10+16+11+5+5 = 60 total, one active week), `plan_created` 0/wk since 07-10, everything downstream 0. **Insufficient data** for any funnel-step rate or retention/cohort claim. Qualitative only. No ≥30-event claim available.

## Experiments — nothing to close
Earliest open measurement window = 005/007/008 → **2026-07-24** (8 days out). 075→07-27; 061/065/068→07-26/27; 082→07-28; 101→14d from first real share (no shares yet). No window ends on 07-16. LEARNINGS.md unchanged.

## Backlog state — tractable autonomous work drained (unchanged since 07-15)
- Only **110** carries a READY marker, and it is "READY (after 109)"; 109 is BLOCKED behind **108** (human keystone: Cloudflare Worker Cron + D1 provisioning + secrets, NEVER-list credentials) → **110 is not actually buildable**.
- Everything else on the board is SHIPPED / DONE / BLOCKED (human) / CULLED. Since the 07-15 close, 112/113/114/115 all SHIPPED (#253/#254 + groom #252); `origin/main == d08dc9d35`, local in sync.
- Distribution (069 spotlight packs ×3, ready + one-tap) remains **unposted** — human-owned. This is the sole north-star lever and it has not moved.
- No new specs manufactured (near-zero-data day + no ungated tractable work; the standing anti-marginal-polish discipline holds). No BACKLOG scores changed (no new evidence; a sub-threshold/empty day is noise, not signal).

## Diff vs 2026-07-15 (eight pulls)
1. 07-15 finalized 4→5 sessions; 07-16 empty so far. Otherwise every prod number identical.
2. Lexicon 19, north star 0 — unchanged.
3. Guardrail error 0, 082 holding (window to 07-28) — unchanged.
4. Backlog: 112–115 now SHIPPED and merged to main; tractable autonomous queue still empty behind the human-gated data-layer DB leg (108/109/110). No score changes.
5. Three human questions from 07-14 still pending (SUBSCRIPTION_LADDER canonical set · 069 posting go/no-go · yield.garden mirror decision) — logged, not re-asked.
