# Signals snapshot — 2026-08-09 (heartbeat, live `Mixpanel` connector)

Project: defigarden (4042048) · org Equitee (2885044) · prod filter `$current_url contains www.defi.garden`.
Queries this run: prod daily 14d `553da6e2`; unfiltered control 14d `02ea4256` / `c35d2597`; `pool_click` /
`pool_view` 30d by `source` `1b1dcaa3`; 08-08 sessions by region×OS×browser `4cb2c37a`; 08-08 sessions by
`src`×`$referrer` `afa1970d`; 08-08 uniques `3c97ae97`; all URLs 08-07..08-09 (control) `a44748a3`;
referrers 14d `e75b847f`; waitlist 7d by `src` `3c272c4c`.

## HEADLINE: both P0s filed yesterday are still live on `main` — and CI re-published one of them again this morning.

Nothing was built between yesterday's tick and this one (the only loop merge was item **237**, a pre-existing
build). `poolNotFoundTitle` is still absent from both namespaces and `node test_dead_pool.js` still **exits 1**
(re-run this tick, unpiped). The `$10M` rail claims are still live — and the daily `sitemap-update.yml` cron
(`0 2 * * *`) regenerated `llms.txt` / `llms-full.txt` at **03:41 UTC today** (`7ab2994aff`) and re-emitted
`TVL ≥ $10M` verbatim. That is the **third** automated republication of the false rail since `6fceca79bb`.
Item 254 is therefore not nine stale strings: it is a job that re-mints a false trust-rail claim onto the
agent citation layer once every 24 hours.

Second finding, and it is a **correction to yesterday's own report**: the audit's `dead-pool-empty-state`
finding is **gone today while the defect is confirmed present**, because that check asserts an element
*exists* and never reads what it says. Yesterday's "three independent instruments, one defect — the strongest
confirmation this loop has produced" was **two instruments**; the third cannot see this defect at all.

## Real prod trend (daily totals, `553da6e2`)
| date | session_start | error_occurred | pool_view | pool_click | page_view | plan_created |
|------|----|----|----|----|----|----|
| 07-26 | 6 | 0 | 2 | 0 | 4 | 0 |
| 07-27 | 3 | 0 | 0 | 0 | 1 | 0 |
| 07-28 | 8 | 0 | 0 | 0 | 1 | 0 |
| 07-29 | 5 | 0 | 0 | 0 | 2 | 0 |
| 07-30 | 6 | 0 | 0 | 0 | 2 | 0 |
| 07-31 | 4 | 0 | 0 | 0 | 0 | 0 |
| 08-01 | 9 | 0 | 1 | 0 | 2 | 0 |
| 08-02 | 3 | 0 | 0 | 0 | 0 | 0 |
| 08-03 | 92 (crawler burst) | 0 | 0 | 0 | 23 | 0 |
| 08-04 | 8 (6 = fuzz replays) | 0 | 0 | 0 | 6 | 0 |
| 08-05 | 4 | 0 | 0 | 0 | 1 | 0 |
| 08-06 | 76 (crawler burst #2) | 0 | 0 | 0 | 23 | 0 |
| 08-07 | 18 (operator sweep) | 0 | 4 | 1 | 11 | 1 |
| **08-08** | **11** | **0** | **0** | **0** | **7** | **0** |
| 08-09 | 0 so far | 0 | 0 | 0 | 0 | 0 |

Prod 14d sessions: **253** (control `c35d2597`: **259**; the 6-session delta = localhost/preview, playbook
class 13). `error_occurred` is **absent from both result sets** across 14d. The two crawler bursts are
**168 of 253 (66.4%)** of the 14d count — down from 68.3% only because the window slid.

## §2b-adjacent — classifying 08-08, per `playbooks/traffic-quality-classification.md`

11 sessions, **11 unique users** (`3c97ae97`) — one session each, the one-hit shape. Referrers (`afa1970d`):
9 undefined/direct, 1 `bing.com`, 1 `wink.com` (junk). `src` = undefined on every session. Regions
(`4cb2c37a`): **undefined 8** (Mac OS X/Safari **7**, Windows/Chrome 1), Oregon/iOS 1, Virginia/Windows 1,
Iowa/Windows 1. URLs: 7 distinct well-formed `?token=` values (`YVWETH`, `AUSD&poolTypes=LP%2FDEX`,
`XAUM&chain=BSC`, `MAYHEM&chain=Solana`, `WONKY`, `ABYSS&chain=Ethereum`, `ANDREA&chain=Solana`) plus bare `/`.

| bot criterion | 08-08 | verdict |
|---|---|---|
| No acquisition | 9/11 undefined referrer, `src` undefined throughout | HIT |
| One-hit / no progression | 11 sessions = 11 uniques, ~3 events each, no chain | HIT |
| Garbage long-tail params | none — 8 well-formed URLs | miss |
| Zero engagement | **yes** — `pool_view` 0, `pool_click` 0, `plan_created` 0, `search_input` absent | HIT |
| Uniform fingerprint | 7 of 11 identical undefined-region / Mac OS X / Safari | HIT |
| `session_start` ≫ `page_view` | 11 vs 7 (1.6×) | miss |

4 of 6 → **crawler traffic** by the decision rule. Step 3's insider cut has nothing to run on (there are no
engagement events to break down by `$region`), and the shape does not match the operator's 08-07 signature
(Utrecht / iOS+Mac / engagement-carrying).

**Decomposition, per the playbook's reporting rule: 11 sessions — 11 crawler-shaped, 0 insider, 0 real.**
And the weakest form of the claim that actually decides the gate, which needs no per-session attribution at
all: **08-08 produced zero engagement events of any kind**, so no session in the window can carry information
we did not already have, whichever class each belongs to.

## North star, both legs
- **Leg (B) pool-detail CTA clicks: 0 real. 32nd straight day.** 30d by `source` (`1b1dcaa3`): `pool_click` = 2
  (`garden_cta` **1** — 08-07, operator; `undefined` 1 — the pre-123 legacy event, ages out ~08-18);
  `pool_view` = 11 (`card_click` 8, `url_direct` 3), **down from 16** yesterday because the 30d window slid,
  not because anything regressed. Gate: **≥30 real exercised events. Real = 0. Unchanged.**
- **Leg (A) agent consumption:** 14d referrers (`e75b847f`, 259 sessions): `$direct` 247, `bing.com` 3,
  `www.ecosia.org` 2, `www.perplexity.ai` **2**, `t.co` 1, `www.0xhodler.nl` 1 (the operator's own site),
  `www.tinker.com` 1, `wink.com` 1 (new junk), `www.recipebridge.com` gone from the window. The perplexity
  pair is still the same 2026-07-26 pair — **0 new AI-referred sessions**, day 5 since the strategy adoption.
  Instruments 224 (D1 edge log) / 227 (API) / 228 (MCP) remain unbuilt; leg (A) is Mixpanel-only and blind to
  non-JS agent reads (212's gap). **The false rail on leg (A)'s own surfaces was re-published again today.**

## Guardrail — 27 clean prod days
`error_occurred` absent from the 14d result set in **both** the prod-filtered (`553da6e2`) and unfiltered
(`c35d2597`) queries → **27th** straight clean prod day (control 11th). Exercise count, per LEARNINGS 082:
unchanged at **n=4** (08-07's four operator `pool_view`s) — 08-08 added **zero** analytics-mode exercises, so
today's zero rests on the same four exercises as yesterday's, all operator. Far below ≥30. It is not evidence
the app is error-free for strangers.

## §2c Orphan-PR check (`pr-orphan-detector.js`, live data)

`list_pull_requests(state=open)` → **3 open** (unchanged). Fed with `main`'s BACKLOG.md into the CLI:

Orphan PRs: **2** (#331, #322) · PARKED 1 (#386/221)

- Composition unchanged from yesterday. #331 (item 176) and #322 (item 166) are genuinely PARKED/BLOCKED in
  their PR titles but their BACKLOG rows do not lead with a legend marker, so the module's documented
  detection gap fires as designed. Detection only; disposition is a build-run or human question (§2c step 5).
- **Leg B — next id computed, not guessed:** `computeNextId(mainMax=255, openPrBranchIds=[221,176,166])` =
  **256**. No collision. (`detectIdCollisions` takes a different argument shape than the §2c prose implies —
  called with a plain id array it throws `openPrClaims is not iterable`; recorded, not worked around, and no
  collision claim is made from it this tick.)

## §2b PRODUCT AUDIT

### (1) Item 253 — still live, re-verified by running it
`grep -c "poolNotFoundTitle:" translations.js` → **0** at HEAD (`7ab2994aff`). `node test_dead_pool.js`,
run **without a pipe** (yesterday's exit-code lesson): **exit 1**, 2 of 12 assertions fail on the EN and KO
titles, summary line prints `10 dead-pool assertions passed (EN: "undefined", KO: "undefined")`. Unchanged
from yesterday. **`npm test` is RED on `main` for a second day.**

### (2) Item 254 — the false rail is not stale, it is being RE-MINTED daily by CI
`.github/workflows/sitemap-update.yml` runs on `cron: '0 2 * * *'` and its "Generate LLM files" step runs
`generate-llms.js`. Three regen commits have landed since the floor change (`748582f2d4`, `7c8105bb75`,
**`7ab2994aff` at 2026-08-09T03:41:49Z**), and `git show 7ab2994aff:llms.txt` still carries both claims:

```
:20  Live blended rate: 3.1% — median APY across the 545 pools clearing our published rails (TVL ≥ $10M, APY ≤ 1000%)
:79  TL;DR: Live highest APY opportunities (updated daily, TVL ≥ $10M).
```

**A nuance that changes 254's fix, found by reading the generator rather than the output:** both sites
**already interpolate** — `generate-llms.js:567` and `:688` call `formatTvlFloor(MIN_TVL_USD)`. The literal
`$10M` is nowhere in the template. The defect is one line up: `:50`
`const MIN_TVL_USD = 10000000; // mirrors app.js:801 (DEFAULT_MIN_TVL, $10M)` — a **hand-maintained mirror**
of a constant it no longer mirrors. This is the 212 mirror class (LEARNINGS 2026-08-03: *"when a list of
names must exist in two places and only one is read at runtime, the other is a mirror; mirrors get an
equality test against the original in the same commit"*) firing on a **number** instead of a list.
`generate-stories.js:230` is the plain re-typed-literal case, no interpolation at all.

**Sized against today's committed snapshot** (`data/pools-snapshot.json`, `generatedAt`
2026-08-09T03:40:17Z, **7,381 pools**): **739 (10.0%)** clear the advertised $10M floor; **6,642 (90.0%)**
sit below what the landing badge promises.

**And a measurement that cuts AGAINST inflating this finding.** The published *rate* is materially correct;
only its stated universe is wrong. Reconstructing the generator's median over the snapshot
(`apyBase + apyReward`, `0 < apy ≤ 1000` — my reconstruction, not the generator's own live-`apy` path, so
treat the absolute figures as approximate):

| floor | eligible pools | median APY | forever number for a $20/mo bill |
|---|---|---|---|
| $10M (published) | 533 | **3.01%** | $7,973 |
| $100K (enforced) | 5,241 | **2.99%** | $8,027 |

The forever number — the SUBSCRIPTION archetype's central number on the agent surface — moves **0.7%**.
So the honest statement is: **the floor is wrong by 100×, the pool count by ~10×, and the rate by less than
one percent.** The defect is the citation layer describing a product that no longer exists, not a wrong
number flowing into plan math.

**The playbook for this class already exists and names this exact file.** `stated-threshold-copy.md`'s
recurring-shape table opens with item **159** — `llms.txt` claiming `TVL ≥ $10k` against an enforced `$10M`.
This is the **third** instance in that table and the **second on `llms.txt`**. Its step-2 rule ("interpolate
from the constant, never a second literal") was satisfied in letter and defeated by a local mirror, and its
step-6 acceptance ("regenerate and re-grep the OUTPUT") is now insufficient on its own, because the cron
regenerates the output daily — a hand-fixed output file would be reverted within 24 hours. → playbook
updated (§4b).

### (3) `audit-app.js` fresh run — 83 surfaces, **4 findings**, and a finding that vanished is an INSTRUMENT finding
`status: "OK"`, `generatedAt` **2026-08-09T07:16:46.762Z**, `surfacesCovered` **83**, playwright local
1.61.1. Exit code read **directly from the process, not through a pipe**: **1**.

| finding | severity | 08-08 | 08-09 |
|---|---|---|---|
| `pool-prescan:apy-rail-breach` | P0 | 15 of 7,358 | 14 of 7,381 |
| `pool-prescan:mean30d-rail-breach` | P0 | 38 of 7,358 | 36 of 7,381 |
| `i18n:en-ko-parity` | P1 | 2 of 553 | 2 of 553 (item **248**, unshipped) |
| `planner-768 :: responsive` (item 235) | P2 | present | present |
| **`dead-pool :: dead-pool-empty-state`** | **P1** | **present** | **ABSENT — and the defect is not fixed** |
| `.app-footer` occlusion (item 221) | — | absent | absent — still dormant, not repaired |

**Why the dead-pool finding vanished, read off the predicate rather than guessed.** `audit-app.js:3697`:

```js
const ok = await waitForSelector(page, '.empty-state .empty-message', 10000);
if (!ok) findings.push(finding(s.name, s.vpLabel, 'dead-pool-empty-state', 'P1', …
```

It is a **presence-only selector wait**. It never reads the element's text. With `poolNotFoundTitle` deleted,
`t()` returns the key string, so `.empty-message` renders — populated with the literal `poolNotFoundTitle` —
and the check **passes**. Yesterday's fire was a `waitForSelector` **10s timeout**, i.e. a slow render, that
happened to land on the right surface on the right day.

**Correction to the 2026-08-08 snapshot and report, stated plainly:** "Three independent instruments, one
defect. That is the strongest confirmation this loop has produced for a single finding" is wrong. Two
instruments witness it (`test_dead_pool.js`, which asserts the title text; and loading the dictionary). The
third is **structurally blind** to it and its agreement was a coincidence. Per §2a, instrument blindness is a
finding, not a caveat → filed as item **256**.

The class is the fifth narrowing in this checker family (precedents 190, 198, 212, 249): a check that asserts
the honest empty state **exists** when the class it guards is that the honest empty state **says something
honest**. Weak form, and what 256 carries: **no rendered text on any audited surface may equal a key name in
the dictionary** — which catches every missing-key fallback on every surface, not this one heading.

### Coverage changes worth naming
- **`poolPrescan.scannedByLeg` = snapshot 7,381 / deep-linked-live 0** (second consecutive run at zero for
  the live leg; the log states `3607 of 3609 deep-linked ids confirmed live, 0 net-new beyond the snapshot`).
  The union is bigger than 08-07's 3,971 and narrower in one dimension. Recorded so it is not re-read as growth.
- Pool rotation candidate set **7,378**, cycle 0, 32 rendered per tick → full pass **~231 ticks**.
- Static prescan rotation: tokens cycle 0 (seen 37/2,045), chains cycle 0 (seen 18/86), plus the KO twins.
- i18n scanned **553** keys, allowlist 24. The 28.1% function-leaf blind spot (item **249**) is unchanged
  and still unshipped.
- Checks that ran: the scanner's implemented classes (number-sanity, dead-end, loading, page errors, i18n,
  dead-CTA, responsive/dark, occlusion) over the 83-surface rotation plus the prescans. Every claim in this
  section is scoped to those classes and populations only.

### Rail-breach counts: ordinary churn, not a signal
15 → 14 and 38 → 36 on a population that grew 7,358 → 7,381. Yesterday's band split (the newly admitted
$100K–$10M band carries 4.1× the mean30d breach density and 37 of 38 breaches) is not re-derived this tick;
no new claim is made about the split.

## §2a predictions
**Carried from 2026-08-05 — fuzz-class decay. Status: TRACKING, day 5 of 7.**
Outcome: the pre-223 scrape-minted fuzz classes decay to zero as crawler caches expire. Witnesses (all): ANY
prod event broken down by `$current_url` matching those strings, filtered AND control. Measured 08-07..08-09
(`a44748a3`, **unfiltered control**, 21 distinct URLs, 107 events): neither `` /?token=USDC` `` nor the empty
`/?pool=` appears. **Zero across all witnesses, five consecutive days.** Resolve at 7 (→ ~08-11); a NEW mint
string producible by the current tree reopens 223.

**Carried from 2026-08-06 — the 244 race reaching prod. Status: TRACKING, 4 exercises, no witness fired.**
Surviving witnesses: `error_occurred` matching `React is not defined` / `reading 'some'`; 224's D1 edge log
once it exists; any rendered-Playwright run reproducing it against prod. 08-08 added **zero** analytics-mode
exercises, so the count stays n=4, still below ≥30. No claim.

**Carried from 2026-08-08 — the false rail is load-bearing for leg (A). Status: TRACKING, and one witness
fired.** Outcome: an AI assistant asked about defi.garden's TVL floor will state $10M. The witness that fired
is not a consumer read — it is the **supply side**: the daily cron re-published both claims at 03:41 UTC
today, so the claim's *availability* is now demonstrated to be self-renewing rather than merely persistent.
The consumption-side witnesses (an answer-engine citation quoting a floor; 224's edge log once deployed)
remain unfired and unobservable. Resolve when every stating site derives from `DEFAULT_MIN_TVL`.

## Gate re-check (§4)
- **Correction to yesterday's snapshot and report:** they stated "0 GATED rows in the backlog". There is
  **1** — item **251** (`GATED(224 deployed by the human AND ≥1 row observed in agent_reads)`), filed by
  224's close-out on 08-04. Re-checked this tick: `edge/DEPLOY.md`'s deploy is human-owned and unperformed,
  so the gate is **unmet**; left in place silently, as the rule requires.
- **≥30-real pool-detail gate: 0 real. Unmet.**
- **≥30-real waitlist gate: 0 real.** `waitlist_opened` 7d = **16** (10 `seo_tokens_hub`, 6 `undefined`,
  `3c272c4c`), all crawler-fired; `waitlist_email_entered` / `waitlist_submitted` absent from the result set.
  `plan_saved` 7d = 2 (`src=pool`), both 08-07/operator. Unchanged.

## Experiments — no window closed, and none CAN close
No traffic gate has opened. 117.1/.2/.3 remain SHIPPED (measuring) under the 60d backstop (~09-16..18).
Human-owned GSC reads: 013 ~08-23, 211 ~09-13.

## BACKLOG table integrity — re-measured with today's predicate
248 id-rows; **13** do not split into 8 cells on unescaped pipes (ids 065, 104, 116, 120, 122, 157, 166, 189,
193, 194, 199, 236, 237). Row 236 carries a duplicated id cell (`| 236 | 236 |`) and row 237 an extra pipe.
Same count as yesterday over a larger row set; no new malformation introduced this tick. Not ticketed — it is
bookkeeping hygiene in the loop's own queue, and the file that the 2026-08-04 merge silently truncated.

## Diff vs 2026-08-08
1. **Nothing was built.** 253 and 254 are both still live on `main`; `npm test` RED for a second day.
2. **254 escalates from stale strings to a self-renewing claim:** the daily `0 2 * * *` cron re-published
   `TVL ≥ $10M` onto `llms.txt`/`llms-full.txt` at 03:41 UTC today — the third automated republication.
3. **254's mechanism is sharper than yesterday recorded:** the llms sites already interpolate; the defect is
   `generate-llms.js:50`'s hand-maintained mirror constant. The 212 mirror rule, on a number.
4. **254's size is smaller than yesterday implied, and that is stated:** floor wrong 100×, count wrong ~10×,
   **rate wrong <1%** (3.01% vs 2.99%; forever number $7,973 vs $8,027).
5. **Yesterday's "three independent instruments" claim is corrected to two** — `dead-pool-empty-state` is a
   presence-only selector wait and cannot see a rendered raw key; its 08-08 fire was a 10s timeout. → **256**.
6. Audit findings 5 → **4**, and the drop is **not** a repair: the vanished finding is the instrument, not the
   defect.
7. 08-08: **11 sessions, 11 crawler-shaped, 0 insider, 0 real** — zero engagement events of any kind.
8. North star leg (B) 30d `pool_view` 16 → **11** (window slid, not a regression); real `pool_click` still 0,
   32nd day.
9. Guardrail streak 26 → **27** clean prod days; exercise count **unchanged at n=4** (08-08 added none).
10. Orphan PRs **2** and open PRs **3** — both unchanged. Next id computed: **256**.
11. Fuzz-decay prediction: **day 5 of 7 at zero**, all witnesses, control set.
12. Leg (A): **0 new** AI-referred sessions (day 5); `bing.com` 2 → 3, one new junk referrer (`wink.com`).
13. **A GATED row exists (251)** — yesterday's "0 GATED rows remain" was wrong. Gate re-checked, unmet.
