# Signals snapshot — 2026-07-20 (heartbeat, live `Mixpanel` connector)

Project: defigarden (4042048) · org Equitee (2885044) · via `Mixpanel` connector (uppercase — live this run; lowercase `mixpanel` still needs OAuth). Prod filter = `$current_url contains www.defi.garden`. `Get-Business-Context` returns "No business context configured" (unchanged). Queries this run: prod daily line `query_id 092a9b77`, all-host totals `00648577`, 07-19 plan_created referrer `53a5104f`, 07-19 sessions×utm_source `779e6549`.

## HEADLINE: FIRST engaged prod session ever (07-19, organic SEO→pool-detail→planner) — n=1, not north-star. Build loop unfed again (117.2 #270 shipped) → 117.3 spec'd.
North star `waitlist_submitted` = **0** (structurally absent from the now-21-event lexicon — no card-funnel event has ever fired). The material delta vs 07-19: **07-19 finalized with the first genuinely engaged prod session on record** — `plan_created` 1 (first prod plan since 07-10), `search_success` 2, `share_link_opened` **1 (first-ever prod share open)**, `pool_click` **1 (first-ever)** — all from ONE organic visitor who traversed a `?token=BTC` pool-detail page into the planner (internal referrer, no utm). Guardrails clean. Build-loop delta: **117.2 SHIPPED (#270)** consumed the buildable queue → 117.3 (planner blend) spec'd.

## Lexicon — 21 events (was 19; +2)
`Get-Events` = **21** = 19 product + 2 Mixpanel auto (`$session_start`/`$session_end`). **NEW since 07-19: `pool_click`, `pool_view`** now instrumented (were listed "structurally absent" on 07-19; `pool_click` fired its first prod event 07-19). Still structurally absent: `waitlist_submitted`, `waitlist_opened`, `share_link_created`, `garden_cta`.
→ **North star `waitlist_submitted` = 0 from any host, ever.** No card-funnel event has fired (distribution/card-front-door-gated; plumbing verified sound 07-13).

## Real prod trend (`$current_url contains www.defi.garden`, daily total `$session_start`, `query_id 092a9b77`)
| date | $session_start | plan_created | search_success | error_occurred | share_link_opened | pool_click |
|------|----|----|----|----|----|----|
| 07-11 | 13 | 0 | 0 | 0 | 0 | 0 |
| 07-12 | 10 | 0 | 1 | 0 | 0 | 0 |
| 07-13 | 13 | 0 | 0 | 1 | 0 | 0 |
| 07-14 | 5 | 0 | 0 | 0 | 0 | 0 |
| 07-15 | 7 | 0 | 0 | 0 | 0 | 0 |
| 07-16 | 6 | 0 | 0 | 0 | 0 | 0 |
| 07-17 | 2 | 0 | 0 | 0 | 0 | 0 |
| 07-18 | **8** | 0 | 0 | 0 | 0 | 0 |
| 07-19 | **3** | **1** | **2** | 0 | **1** | **1** |
| 07-20 | **0 so far** | 0 | 0 | 0 | 0 | 0 |

NOTE: this run reads the Mixpanel-auto `$session_start`; prior snapshots' "session_start" column tracked the custom event — small day-count differences (07-18 read 7 then, 8 now; 07-11 10→13; 07-13 11→13) are late-arriving events + the metric-choice difference, not a trend break. `plan_created` prod was 0 since 07-10 → **1 on 07-19**. `search_success` prod was 1 total (07-12) → **+2 on 07-19**. **First prod `share_link_opened` and `pool_click` in the project's history, both 07-19.** 07-20 has no prod events yet — an empty partial day, do not act on it.

## The 07-19 engaged session — attribution (why it matters, why it's still n=1)
- `plan_created` `$referrer` breakdown (`query_id 53a5104f`): the single plan came from **`https://www.defi.garden/?token=BTC&minTvl=10000000&pool=3dd2c646-…`** — an internal analytics/pool-detail page. The visitor went **`?token=BTC` pool detail → planner → plan_created**, and also fired `share_link_opened` + `pool_click`.
- `$session_start` × `utm_source` (`query_id 779e6549`): **all sessions `utm_source=undefined`** → NOT distribution-driven, NOT a 069 spotlight click. Organic.
- Read: this is the **"convert token-search SEO landers via the pool-detail experience"** funnel (standing decision 2026-07-11) firing end-to-end organically for the first time — exactly the path 117.1/117.2 (pool-detail + analytics risk-adjusted surfaces) are built to strengthen. Encouraging, but **n=1**: below the ≥30-event floor (NORTH_STAR.md), so qualitative only — no rate/conversion claim, no experiment spun off it.

## Contamination check (`query_id 00648577`, all-host, last 10 days)
All-host totals: `$session_start` **150**, plan_created 13, share_link_opened 14, error_occurred 4, pool_click **1**. The prod filter isolates the real line: all-host share/plan counts are dev/preview/bot hosts. Cross-check — **all-host `pool_click` = 1 = the single 07-19 prod event**, confirming the prod engaged session is genuine, not filtered noise. (This run's all-host `$session_start` 150 is lower than prior snapshots' ~480 "session_start" — same auto-vs-custom-event metric-choice difference noted above, not a traffic drop.)

## Guardrails — clean
- `error_occurred` (prod): **0 on 07-14/15/16/17/18/19, 0 so far 07-20.** The 07-13 `ReferenceError: translations is not defined` (082, shipped 07-14) has **not recurred** — window to 2026-07-28 clean.
- Analytics-URL SEO lifeline: `?token=`/`?chain=`/`?pool=` render path intact — 117.2 (#270) touched only `app.js` sort comparators (verifier-confirmed byte-clean trust rails + router; the 07-19 engaged session proves `?token=BTC&…&pool=` renders and converts live).
- Planner conversion: 1 prod `plan_created` on 07-19 across ~3 sessions — a single event, uncomputable as a rate honestly → insufficient data, qualitative only.

## Sample discipline
Real-prod n: one engaged session (07-19) atop ~2–13/day trickle; `plan_created` 1/wk, everything else ≤2. **Insufficient data** for any funnel-step rate, retention, or cohort claim. Qualitative only. No ≥30-event claim available.

## Experiments — nothing to close today
Earliest open window = 005/007/008 → **2026-07-24** (4 days out). 075 → 07-27; 061/065/068 → 07-26/27; 082 → 07-28; 114/115 → 07-29; 116 → 07-30; 101 → 14d from first real share (07-19 was the first prod share OPEN, not a report/bloom share ORIGINATION — 101's window still hasn't started). No window ends 07-20. LEARNINGS.md unchanged. Compute-side: 117.1/117.2 Sharpe surfaces still activate ~07-22 as pools cross ≥8 daily history points (dormant today).

## Backlog state — build loop RE-FED via 117.3 (change vs 07-19)
- **117.2 SHIPPED** (#270, 2026-07-19; verifier PASS HIGH 8/8) — analytics "Risk-adjusted (Sharpe)" sort. Consumed the one buildable READY item → loop unfed.
- **NEW: 117.3 spec'd + backlogged** (score 6.4, HIGH, READY) — the last-in-sequence 117 surface (planner "prefer steadier yield" blend), named verbatim in 117's own row and 117.2-notes. Adds a rate-stability tie-break to the planner's pool auto-pick **within the already-rail-passing candidate set only** (anomaly demotion, $10M TVL floor, degen ⅓ haircut, `APY_SANITY_LIMIT` all untouched — the load-bearing trust rails), null-Sharpe pools unaffected, off by default until pools accrue ≥8 history pts (~07-22). Autonomously buildable NOW; grounded (117 compute value shipped); on the SEO-lander→planner path the 07-19 organic session just validated. No credentials, no NEVER-list surface. specs/117.3.md.
- **110 (read-from-DB, MEDIUM 5.5)** stays "READY (after 109)" but NOT truly buildable — needs 108's CF-D1 provisioning + 109's deploy (human-gated, NEVER-list credentials).
- **109 SHIPPED code** (#266); **DEPLOY pending human 108**. KPIs keep running on file-history until then.
- Distribution (069 spotlight packs ×3, ready + one-tap) remains **unposted** — human-owned, still the sole `waitlist_submitted` lever. Unmoved.
- **Structural note (unchanged, now 6 heartbeats running):** every heartbeat since 07-15 has fed the loop a small pool-detail/analytics trust surface — the only autonomously-buildable grounded work left. 117.3 is the LAST 117 surface; after it, the 117 line is exhausted and the loop genuinely starves unless the human unblocks distribution (069 posting), the card front door, or 108 (D1 deploy). The 07-19 organic conversion is the first evidence the trust-surface work sits on a path real users actually walk — but it needs traffic to matter, and traffic is human-owned.
- No existing-row scores changed (n=1 is not new evidence for a re-score).

## Diff vs 2026-07-19 (four pulls)
1. **07-19 finalized from "0 so far" → a REAL engaged organic session** (plan_created 1, search_success 2, share_link_opened 1, pool_click 1) — first-ever prod share open + pool_click, first prod plan since 07-10, attributed to an organic `?token=BTC` pool-detail→planner traversal (no utm). 07-18 finalized 8, 07-20 empty so far.
2. **Lexicon 19 → 21**: `pool_click` + `pool_view` now instrumented and firing (pool_click's first prod event 07-19). North star `waitlist_submitted` still 0.
3. Guardrail error 0 (082 holding, window to 07-28), SEO render path intact — unchanged.
4. **Backlog: 117.2 (#270) consumed the buildable queue → loop RE-FED by spec'ing 117.3** (planner blend, the final 117 surface). 110/109 human-gated. No existing-row score changes.
5. Three human questions still pending (SUBSCRIPTION_LADDER canonical set · 069 posting go/no-go · yield.garden mirror decision) — logged 07-14, not re-asked (Standing Decisions unchanged).
