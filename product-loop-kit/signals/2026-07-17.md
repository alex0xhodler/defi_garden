# Signals snapshot — 2026-07-17 (heartbeat, live `Mixpanel` connector)

Project: defigarden (4042048) · org Equitee (2885044) · via `Mixpanel` connector (uppercase — live this run; lowercase `mixpanel` still needs OAuth). Prod filter = `$current_url contains www.defi.garden`. `Get-Business-Context` still errors (org id not configured) — read-only insights queries used directly (`query_id 2c130f08` prod daily line, `5c94ee59` all-host totals).

## HEADLINE: near-identical no-op — one honest change: the build loop is fed (117 is buildable)
North star `waitlist_submitted` = **0** (structurally absent from the 19-event lexicon). Prod traffic flat. Guardrails clean. The only material delta vs 07-16: **117 (Sharpe-ratio compute leg) is a genuinely buildable READY item** — spec'd, no credentials, ≈free extension of `compute-kpis.js` on the daily file-history — so unlike 07-16's "tractable backlog drained" framing, the build loop has one tractable item to pick up. No scores changed (no new prod evidence), no specs manufactured (117 already spec'd).

## Lexicon — 19 events (unchanged)
`Get-Events` = **19** = 17 product events + 2 Mixpanel auto (`$session_start`/`$session_end`). Still structurally absent: `waitlist_submitted`, `waitlist_opened`, `share_link_created`, `pool_click`, `garden_cta`.
→ **North star `waitlist_submitted` = 0 from any host, ever.** No card-funnel event has ever fired (distribution-gated; plumbing verified sound 07-13).

## Real prod trend (`$current_url contains www.defi.garden`, daily total, `query_id 2c130f08`)
| date | session_start | plan_created | search_success | error_occurred | share_link_opened |
|------|---------------|--------------|----------------|----------------|-------------------|
| 07-09 | 7 | 1 | 0 | 0 | 0 |
| 07-10 | 6 | 1 | 0 | 0 | 0 |
| 07-11 | 10 | 0 | 0 | 0 | 0 |
| 07-12 | 16 | 0 | 1 | 0 | 0 |
| 07-13 | 11 | 0 | 0 | 1 | 0 |
| 07-14 | 5 | 0 | 0 | 0 | 0 |
| 07-15 | 5 | 0 | 0 | 0 | 0 |
| 07-16 | **7** | 0 | 0 | 0 | 0 |
| 07-17 | **0 so far** | 0 | 0 | 0 | 0 |

→ Flat ~5–16/day trickle, unchanged. **07-16 finalized at 7 sessions** (yesterday's pulls read "0 so far"). `plan_created` prod = **0 since 07-10**. `search_success` prod = 1 total (07-12). **No prod `share_link_opened` ever** (share row absent from the prod-filtered result → 0). 07-17 has no prod events yet — acting on an empty partial day breaks the minimum-sample rule.

## Contamination check (`query_id 5c94ee59`, all-host, same 07-08→17 window)
All-host totals: session_start **479**, plan_created 17, share_link_opened 13, error_occurred 4. Prod-filtered over the same window: session_start ~67, plan_created 2, error_occurred 1, share_link_opened 0. All-host `session_start` climbed 169 (07-16 read) → 479 — a **non-prod** delta (dev/preview/bot hosts), fully isolated by the `$current_url` filter; prod stays flat. Not a prod signal, not actionable.

## Guardrails — clean
- `error_occurred` (prod): **0 on 07-14/07-15/07-16, 0 so far 07-17.** The 07-13 `ReferenceError: translations is not defined` (082, shipped 07-14) has **not recurred** — window to 2026-07-28 clean so far.
- Analytics-URL SEO lifeline: `?token=`/`?chain=`/`?pool=` render path intact (no render-path merge disturbed it; recent #262 fixed the `/plan` clean-URL, #263 widened token search to vault-ticker pools — both landed on main, guardrails green).
- Planner conversion: uncomputable honestly at this sample (0 prod `plan_created` since 07-10) → insufficient data, qualitative only.

## Sample discipline
Real-prod n: ~67 sessions over 07-09→16 (one active week of ~5–16/day trickle), `plan_created` 0/wk since 07-10, everything downstream 0. **Insufficient data** for any funnel-step rate or retention/cohort claim. Qualitative only. No ≥30-event claim available.

## Experiments — nothing to close today
Earliest open window = 005/007/008 → **2026-07-24** (7 days out). 075 → 07-27; 061/065/068 → 07-26/27; 082 → 07-28; 116 (report-mode share) → 07-30; 101 → 14d from first real share (no prod shares yet). No window ends on 07-17. LEARNINGS.md unchanged.

## Backlog state — build loop FED (change vs 07-16)
- **117 (Sharpe-ratio compute leg, score 7.0, HIGH) is READY and buildable now** — spec `specs/117.md` written 07-15; extends `compute-kpis.js` (`apyStdev`/`stdevPop` already present; add `apyMean`+`apySharpe`, null for <8 pts or sd=0), no credentials, RATE-STABILITY Sharpe with the mandated honesty caveat. The daily file-history (retained since 087, 07-14) reaches the ≥8-point threshold ~07-22; the compute code lands now, values populate as points accumulate. → The build loop has a tractable autonomous item; the "backlog drained" narrative from 07-15/07-16 no longer holds.
- **109 SHIPPED code** (#266, 07-16 at human request) — CF Worker pool-history poller, verifier PASS HIGH 5/5. **DEPLOY pending human 108** (`wrangler login`/`d1 create`/`deploy`/`secret` — NORTH_STAR NEVER-list credentials). KPIs keep running on the file-history until then.
- **110 (read-from-DB, MEDIUM 5.5)** stays "READY (after 109)" but not actually buildable — it repoints `compute-kpis.js` at `${HISTORY_ENDPOINT}` which does not exist until 108's provisioning + 109's deploy land. Human-gated.
- Distribution (069 spotlight packs ×3, ready + one-tap) remains **unposted** — human-owned, and still the sole north-star lever. It has not moved.
- No new specs manufactured (117 already spec'd; no new prod evidence). No BACKLOG scores changed (a finalized-7 + empty partial day is not new signal).

## Diff vs 2026-07-16 (three pulls)
1. 07-16 finalized 0-so-far → **7** sessions; 07-17 empty so far. Every other prod number identical.
2. Lexicon 19, north star 0 — unchanged.
3. Guardrail error 0, 082 holding (window to 07-28) — unchanged.
4. **Backlog: build loop now fed by 117 (buildable Sharpe compute leg)** — the one real change; 109 SHIPPED code (deploy human-gated via 108), 110 still human-gated. No score changes.
5. All-host `session_start` non-prod noise climbed 169→479 (dev/preview/bot), fully filtered out of the prod line.
6. Three human questions still pending (SUBSCRIPTION_LADDER canonical set · 069 posting go/no-go · yield.garden mirror decision) — logged 07-14, not re-asked (Standing Decisions unchanged).
