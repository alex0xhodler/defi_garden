# Signals snapshot — 2026-07-15 (heartbeat, live Mixpanel pull)

Project: defigarden (4042048) · org Equitee (2885044) · via `Mixpanel` connector (uppercase — live again this run; lowercase `mixpanel` still needs OAuth). Prod filter = `$current_url contains www.defi.garden` (the 2026-07-14 correction: property is `$current_url`, NOT `page_url`). No business context configured (Get-Business-Context errors: org id required — not set up).

## HEADLINE: no-op run — nothing moved since the three 2026-07-14 pulls
Every number below re-confirms yesterday. There is **no 07-15 activity yet** (query for 07-14→07-15 returns only a 07-14 row across ALL hosts → zero events of any kind so far today). Acting on an empty partial day would break the minimum-sample rule.

## Lexicon — unchanged at 18 events
`Get-Events` = **18** (same as 07-13/07-14). Still structurally absent: `waitlist_submitted`, `waitlist_opened`, `share_link_created`, `pool_click`, `garden_cta`.
→ **North star `waitlist_submitted` = 0 from any host, ever.** (No card-funnel event has ever fired; distribution-gated, plumbing verified sound 07-13.)

## Real prod trend (`$current_url contains www.defi.garden`, daily `session_start`)
| date | session_start | plan_created | search_success | error_occurred | page_view |
|------|---------------|--------------|----------------|----------------|-----------|
| 07-09 | 7 | 1 | 0 | 0 | — |
| 07-10 | 6 | 1 | 0 | 0 | — |
| 07-11 | 10 | 0 | 0 | 0 | — |
| 07-12 | 16 | 0 | 1 | 0 | — |
| 07-13 | 11 | 0 | 0 | 1 | 2 |
| 07-14 | 5 | 0 | 0 | 0 | 1 |
| 07-15 | **0 so far** | 0 | 0 | 0 | 0 |

→ Flat ~5–16/day trickle, unchanged. `plan_created` prod = **0 since 07-10**. `search_success` prod = 1 total (07-12). No prod `share_link_opened` ever (all opens localhost dev). Distribution remains the single binding constraint, and it's human-owned.

## Contamination (still filterable, unchanged)
All-host 07-13→07-15 totals: session_start 84, plan_created 11, share_link_opened 13, error_occurred 1 — i.e. 07-14 all-host = 34 sessions / 10 plan_created / 11 share_link_opened, essentially ALL localhost/dev (per the 07-14 evening forensics). 096 (env-gate, shipped 07-14) stops NEW non-prod events going forward; historical stays filterable via `$current_url`.

## Guardrails — clean
- `error_occurred`: **0 on 07-14, 0 so far 07-15.** The 07-13 `ReferenceError: translations is not defined` (082, shipped 07-14) has **not recurred** — window to 2026-07-28 clean so far.
- Analytics-URL SEO lifeline: real sessions still land on `?chain=`/`?token=` params (07-14 read); render path intact.
- Planner conversion: uncomputable honestly at this sample (0 prod plan_created since 07-10) → insufficient data, qualitative only.

## Sample discipline
Real-prod n: ~65/wk sessions, plan_created 0/wk since 07-10, everything downstream 0. **Insufficient data** for any funnel-step rate or retention/cohort claim (single active week of background trickle). Qualitative only. No ≥30-event claim available.

## Experiments — nothing to close
Earliest open window = 005/007/008 → **2026-07-24** (still 9 days out). 061/065/068 → 07-26/27. 082 → 07-28. LEARNINGS.md unchanged. Reminder: all pre/post closes MUST use the `$current_url contains www.defi.garden` prod filter or they compare dev noise.

## Diff vs 2026-07-14 (third pull)
1. Lexicon still 18 → north star still 0.
2. Real prod trend identical (07-14 = 5); 07-15 has no data yet.
3. Guardrail error still 0 (082 holding).
4. Backlog: 093 & 095 have since SHIPPED → **094 is now the ONLY READY item** → wrote `specs/094.md` this run to keep the build loop fed.
5. No BACKLOG scores changed (no new evidence; acting on a zero-data day would violate the minimum-sample rule).

---

## Second pull — 2026-07-15 (evening, live `Mixpanel` connector)
Re-ran the full signal set. **Metrics still identical** — the connector re-confirms every number:
- Lexicon `Get-Events` = **18**, unchanged → `waitlist_submitted` structurally absent → **north star = 0**.
- Prod trend (`$current_url contains www.defi.garden`, 7d line): daily `session_start` 7/6/10/16/11/**5** (07-09→14); **still no 07-15 row → zero events today**. `plan_created` prod 0 since 07-10. `search_success` prod 1 total (07-12). `error_occurred` prod 0 on 07-14/07-15 (082 holding, window to 07-28). No prod `share_link_opened`.
- Guardrails clean; sample insufficient for any funnel-rate/retention claim (qualitative only).

### Backlog reconciliation — CONCURRENCY COLLISION on item 100
This pass independently found + **reproduced live** (via `extractParser` against the real `app.js`) the 084-notes parser bug: `"highest apy on ethereum"`→token ETH, `"best yields on solana"`→SOL, `"top apy on arbitrum"`→ARB (chain query silently narrowed to a same-prefixed token). On integrating `origin/main` I discovered a **concurrent build loop already shipped the identical fix as item 100** (`dd0a546f4`, PR #224) — same root cause, same fix (expand the fallback guard to all `chainAliases` keys), with its own specs/100.md + test_parser_chain_token.js (6/6). My independent reproduction **validates** #224's fix; my draft READY-100 was redundant and dropped. **This is the notable signal: two loops converged on the same last-candidate bug within hours — the autonomous audit backlog (094→100) is essentially drained of tractable build work.**

### Next item seeded — 101 (verified, on-theme)
Rather than manufacture marginal polish, code-audited for a genuinely north-star-relevant gap and found one: the **SUBSCRIPTION share link drops the multi-service mix** — `encodePlanToUrl` (planner.js:903) never serializes `selectedSubs`, and no `mix` param is decoded on arrival (grep 0 hits), so a multi-service garden opens as just the anchor goal on the recipient's side. Undercuts the 2026-07-11 standing decision (share link = rebuilds the sender's exact garden). Wrote **`specs/101.md`** (READY, HIGH, score 7.6). On the weekly theme (share virality / card funnel).

### Diff vs the morning pull
1. Metrics: identical (zero 07-15 activity, north star 0, guardrails clean).
2. 094 SHIPPED (2026-07-15). 100 SHIPPED by a concurrent loop (#224) — my redundant draft dropped, fix independently validated.
3. Backlog now: 1 READY (101, seeded this pass). No existing scores changed (zero-data day).
4. **Flag for the human (see report):** autonomous build work is near-exhausted; the binding constraint remains human-owned distribution.

---

## Third pull — 2026-07-15 (routine re-run, live `Mixpanel` connector)
Full signal set re-pulled once more. **Every number identical to both earlier pulls** — a genuine no-op:
- Lexicon `Get-Events` = **18**, unchanged → `waitlist_submitted` still structurally absent → **north star = 0**.
- Prod trend (`$current_url contains www.defi.garden`, daily line, live query): `session_start` 7/6/10/16/11/**5** (07-09→14); **still no 07-15 row → zero prod events today** (whole-day). `plan_created` prod 0 since 07-10 (only 07-09/10 = 1/1). No prod `share_link_opened` ever. `error_occurred` = 1 total (07-13), 0 on 07-14/07-15 → 082 fix holding, window to 07-28.
- Guardrails clean; sample insufficient for any funnel-rate/retention claim (qualitative only).

### Backlog state — DRAINED
101 has since SHIPPED (PR #225). **Zero READY items remain** — every backlog row is now SHIPPED / DONE / BLOCKED (097, yield.garden, human-owned) / PARKED. No new evidence on a zero-data day → no scores changed, and per discipline this run does **not** manufacture marginal polish to keep the loop fed. Experiments: nothing to close (earliest window 005/007/008 → 07-24, 9 days out).

### Diff vs the evening pull
1. Metrics: identical (zero 07-15 activity, north star 0, guardrails clean).
2. 101 SHIPPED (#225) → backlog now has **0 READY** items (was 1).
3. No scores changed; no new opportunities produced (zero-data day + drained tractable backlog).
4. Binding constraint unchanged and unchanged-from-this-morning: **human-owned distribution** (069 spotlight packs unposted). Not re-flagging the human a third time in one day — already in today's report.

---

## Fourth pull — 2026-07-15 (routine re-run, live `Mixpanel` connector)
Full signal set re-pulled a fourth time (live insights query, prod filter `$current_url contains www.defi.garden`). **Every metric identical to all three earlier pulls** — a genuine no-op day:
- Lexicon `Get-Events` = **18**, unchanged → `waitlist_submitted` / `waitlist_opened` / `share_link_created` / `pool_click` / `garden_cta` still structurally absent → **north star = 0 from any host, ever.**
- Prod daily line (live query, query_id 65627fe3): `session_start` 7/6/10/16/11/**5** (07-09→14), **0 on 07-15** (whole day). `plan_created` prod 1/1 (07-09/10), **0 since**. `share_link_opened` prod = **0** (event returned no prod rows — all opens remain localhost dev). `error_occurred` = 1 total (07-13 only), **0 on 07-14 and 07-15** → 082 fix holding, window to 07-28.
- Guardrails clean; sample insufficient for any funnel-rate/retention/cohort claim (single active week of ~5–16/day trickle) → qualitative only, no ≥30-event claim available.

### Backlog reconciliation — the third pull's "0 READY / drained" is now STALE (corrected)
Since the third pull, `origin/main` advanced to **ac5a64cc6 (#233 "DB leg unblocked + stack decided — CF Worker Cron + D1")** and 106 shipped (#232). #233 **reopened the data-layer epic**, so the backlog is NOT drained:
- **READY now:** 102 (one-source-of-truth: point the 3 SEO generators at `pools-snapshot.json`, score 7.0), 103 (rate-momentum on cards, 7.0, HIGH), 104 (TVL-trend, 6.5), 105 (enrich `?pool=` landings with kpis, 6.5) — KPI-surfacing quick wins, spec-less rows with full context.
- **DB-leg chain:** 108 = HUMAN KEYSTONE (wrangler login + D1 create + GH secrets — NORTH_STAR NEVER-list, loops never touch credentials) → **BLOCKED on the human**; 109 (build CF Worker poller `src/poller.js`+`wrangler.toml`, code+fixture-testable without creds) and 110 (read-from-DB) are **spec'd in `specs/108.md`** and buildable now (deploy step deferred to 108). Build loop is therefore fed — no spec written this pass.
- These are all **OFF the "distribution + card funnel" weekly theme** — data-layer infrastructure the loop has drifted to because on-theme (distribution) is human-gated. Nothing on fire → no theme override, no reprioritization.

### Diff vs the third pull
1. Metrics: identical (zero 07-15 activity, north star 0, guardrails clean).
2. **"0 READY" corrected → data-layer epic reopened by #233** (102–105 READY + 109/110 spec'd/buildable). Build loop is fed with real infra work; distribution remains the human-owned north-star lever.
3. No scores changed (zero-data day); no new opportunities/specs manufactured.
4. Binding constraint unchanged: **human-owned distribution** (069 spotlight packs still unposted). Already flagged in today's report — not re-notifying.

---

## Fifth pull — 2026-07-15 (routine re-run, live `Mixpanel` connector)
Full signal set re-pulled a fifth time (live insights `query_id 17d13fdd`, prod filter `$current_url contains www.defi.garden`).

**ONE delta vs all four earlier pulls: 07-15 is no longer empty — 3 prod sessions have landed today** (previous pulls read "0 so far"). Still far below the ≥30-event minimum-sample floor; `plan_created` 0, guardrails clean. Not actionable — noise, not signal.

- Lexicon `Get-Events` = **18**, unchanged → `waitlist_submitted` / `waitlist_opened` / `share_link_created` / `pool_click` / `garden_cta` still structurally absent → **north star `waitlist_submitted` = 0 from any host, ever.**
- Prod daily line (live, `$current_url contains www.defi.garden`): `session_start` 7/6/10/16/11/5/**3** (07-09→07-15); `plan_created` 1/1 (07-09/10), **0 since** (incl. 07-15); `share_link_opened` prod = **0** (metric returned no prod rows — all opens still localhost dev); `error_occurred` = 1 total (07-13 only), **0 on 07-14 and 07-15** → 082 fix holding, window to 07-28.
- Guardrails clean; sample insufficient for any funnel-rate / retention / cohort claim (single active week of ~3–16/day trickle) → qualitative only, no ≥30-event claim available.

### Backlog state — fed, unchanged
Since the fourth pull `origin/main` advanced to **e6f5801ef**: 103 SHIPPED (#234), 111 SHIPPED (#236), search-first landing SHIPPED (#237). Current READY (buildable now): **102** (one-source-of-truth SEO generators, 7.0), **104** (TVL-trend KPI, 6.5), **105** (enrich `?pool=` landings with kpis, 6.5). 110 (5.5) stays gated behind 109/108 (109 now BLOCKED on 108 human provisioning; 108 = human keystone, credentials, NEVER-list). Build loop is fed with real data-layer/KPI infra — all OFF the "distribution + card funnel" weekly theme, because on-theme work is human-gated. Nothing on fire → no theme override, no reprioritization, no manufactured polish.

### Diff vs the fourth pull
1. Metrics: **07-15 sessions 0→3** (partial-day trickle, sub-threshold noise); everything else identical (north star 0, plan_created 0, guardrails clean, 082 holding).
2. Backlog advanced (103/111/#237 shipped) but still fed: 102/104/105 READY. No scores changed (3-session day is not new evidence).
3. No new opportunities/specs manufactured (near-zero-data day + tractable backlog already fed).
4. Binding constraint unchanged: **human-owned distribution** (069 spotlight packs ×3 still unposted). Already flagged in today's report four times — not re-notifying on a sub-threshold delta.

---

## Sixth pull — 2026-07-15 (routine re-run, live `Mixpanel` connector)
Full signal set re-run. **Two deltas, both non-actionable; everything else re-confirmed.**

- **Lexicon = 19 (was 18).** The two new entries are Mixpanel auto-events `$session_start`/`$session_end`, NOT card-funnel events. `waitlist_submitted`/`waitlist_opened`/`share_link_created`/`pool_click`/`garden_cta` still structurally absent → **north star `waitlist_submitted` = 0, unchanged.**
- **Prod trend** (`$current_url contains www.defi.garden`, daily `session_start`): 7/6/10/16/11/5/**3** (07-09→07-15). 07-15 now shows **3 prod sessions** (all prior pulls read 0 for today) — still sub-threshold (< the ≥30-event floor), `plan_created` prod 0, `search_success` prod 0, no prod `share_link_opened` (query returns no rows). Noise, not signal.
- **Guardrails clean:** `error_occurred` prod 0 on 07-14 AND 07-15 (082 ReferenceError fix holding; window to 07-28). `?token=`/`?chain=` SEO render path intact.
- **Sample discipline:** real-prod n still ~single-week trickle; **insufficient data** for any funnel-rate/retention/cohort claim. Qualitative only.
- **Experiments:** nothing to close — earliest window (005/007/008) → 2026-07-24, still 9 days out. LEARNINGS.md unchanged.

### Backlog reconciliation (since the fifth pull)
- 103 (#234) SHIPPED (rate-momentum note), 105 SHIPPED (SEO `?pool=` kpi enrichment), 106 SHIPPED (plan-confidence), 111 SHIPPED (nav/filter polish). 102 CULLED→112 (112 SHIPPED).
- **Current READY set = 104 only** (TVL-trend note, clean/no-deps) + 110 (READY *after* 109, but 109 is BLOCKED on 108 = human Turso/DB provisioning → 110 not actually buildable). Every other item is SHIPPED or human-gated.
- 104 had **no spec** → wrote `specs/104.md` this run (mirrors 103's shipped `.rate-momentum-note` scaffold: `.tvl-trend-note` reading `pool.kpis.tvlTrend`, GROWING/SHRINKING ±10% tiers, hp≥7 gate, EN+KO, rendered-Playwright gate). Corrected the backlog risk MEDIUM→HIGH (user-facing number, trust-rail adjacent — matches the 103 precedent). Build loop is fed with one clean HIGH item.
- **No BACKLOG scores changed** (zero new evidence; acting on a 3-session partial day violates the minimum-sample rule).

### Diff vs the fifth pull
1. Lexicon 18→19 (auto-events only; north star still 0).
2. 07-15 prod sessions 0→3 (sub-threshold noise).
3. Backlog: 103/105/106/111/112 now all SHIPPED; **104 is the last clean READY item** → spec written this run to keep the loop fed.
4. Guardrails still 0; experiments still nothing to close.
