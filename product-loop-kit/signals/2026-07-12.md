# Signals snapshot — 2026-07-12 (heartbeat, live Mixpanel pull)

Project: defigarden (4042048) · org Equitee (2885044) · via Mixpanel connector (uppercase `Mixpanel` server; lowercase `mixpanel` needs OAuth). No business context configured for the project.

## Event totals — last 14 days
| event | count | note |
|-------|-------|------|
| session_start | 326 | 07-09=167 (spike), 07-10=80, 07-11=79 — steady ~80/day after launch spike |
| page_view | 330 | `/` planner=224; analytics search/chain paths≈106; `/tokens/*`=0; `/pool/*`=0 |
| search_success | 100 | see results_count breakdown below |
| search_abandonment | 8 | parse-fail rate holds ~7% (8 / 108) |
| plan_created | 6 | daily 07-09=5, 07-10=1, 07-11=0 — declining, but n tiny |
| plan_saved | 12 | flat vs last snapshot |
| pool_view | 3 | ALL `source=card_click`; `source=url_direct` (SEO `?pool=` landing) = **0** |
| error_occurred | 0 | not in Mixpanel lexicon → never fired; guardrail quiet |

North star (`share_link_opened → plan_created` closures): **0** — `share_link_created`, `share_link_opened`, `pool_click`, `waitlist_opened`, `waitlist_submitted`, `feeling_degen` are ALL absent from the Mixpanel lexicon (event never received) → structurally 0.

## search_success.results_count breakdown (14d, n=100) — 020's fix is LIVE
| results_count | events |
|---------------|--------|
| 0 | 78 |
| 1 | 4 |
| 2 | 7 |
| 3 | 1 |
| 4 | 1 |
| 5 | 3 |
| 7 | 4 |
| 34 | 2 |

- Was 50/50 hardcoded-0 in the 2026-07-11 snapshot. Now **22/100 carry a real non-zero count (1–34 pools found)** → 020 shipped and is emitting real `filteredPools.length`. The keystone gap #1 from 2026-07-11 is closed for post-deploy traffic.
- The 78 zeros are a MIX of pre-020 legacy events (hardcoded 0) and genuine no-result searches — indistinguishable at the property level. Cannot yet quote a clean "find-a-pool" rate; need ≥30 unambiguously-post-020 searches to accumulate.
- Honest partial read of 018: when instrumented, searches DO surface pools (1–34). No evidence of a broad find-nothing problem, but not enough post-020 sample to close 018.

## page_view by path (14d, total 330)
- `/` (planner): **224** — dominant surface (was 114).
- analytics-app search/chain paths: ≈106 combined (USDC 21, LEND 12, chain/All 12, COMP 8, CRV 6, chain/Ethereum 6, ARB 5, + long tail).
- `/tokens/*` (static SEO landers, 2,045 pages, instrumented by 039): **0**.
- `/pool/*` / `?pool=` (pool-detail SEO landers): **0** page_views; **0** `pool_view source=url_direct`.

## Diff vs 2026-07-11 snapshot
- session_start 247→326, page_view 165→330, search_success 50→100 — mostly window-shift (07-09's 167-session launch day now fully inside the 14d window), NOT confirmed growth. Daily run-rate is steady ~80 sessions/day.
- NEW: 020 live — `results_count` real (22/100 non-zero), `pool_view` firing (n=3, all card_click).
- UNCHANGED-ZERO despite shipped instrumentation: `/tokens/*` page_views (039) and `?pool=` `url_direct` pool_views (020). Two hypotheses, not yet separated: (a) **no organic traffic yet** — GSC indexing is a 2–6wk cycle just started, so zero SEO-lander visits is the expected null; (b) **instrumentation silently not firing on the static/landing surfaces** — 039 & 020's live-page firing was verifier-asserted via fixtures only; Playwright never ran against a real rendered page in-sandbox (node_modules/network blocked, per LOG). → new item 044 verifies (a) vs (b) cheaply with a real render.

## Behavioral note (not an artifact)
- Internal search → pool engagement: 100 searches → **3** card-click pool_views (~3%). Was 50→0 last snapshot; still near-zero. Q-d (is internal-search→card-click a goal, or is SEO→pool-detail the intended conversion path?) is treated as ANSWERED by the 2026-07-11 standing decision ("convert token-search SEO landers toward north star via pool-detail experience" → search app = acquisition, pool-detail = conversion). Not re-asking.

## Sample discipline
- ≥30-event claims allowed: search (n=108). Everything else (plans n=6/12, pool views n=3, shares/waitlist/errors = 0) is below threshold or zero → **insufficient data** for funnel/retention/cohort claims.
- Retention cohorts: not computable — still a single active week (tracking began 2026-07-09).
- No experiment measurement window has ended (005/007/008 → 2026-07-24; 013/014/018/019/020/024/025/039 → 2026-07-25). Nothing to close in LEARNINGS.md this run.
