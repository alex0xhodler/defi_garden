# Signals snapshot — 2026-08-07 (heartbeat, live `Mixpanel` connector)

Project: defigarden (4042048) · org Equitee (2885044) · prod filter `$current_url contains www.defi.garden`.
Queries this run: prod daily 14d `cd434678`; unfiltered control 14d `b16d8f7b`; referrers 14d (event-prop)
`f8767f0b`; sessions 08-06 by URL×browser `a00d1851`; 08-06 engagement events `0e0df8d1`; all URLs
08-05..08-07 filtered+control `34c218b3`; `?token=` arrivals hourly 08-06 `389acd9b`; waitlist 7d by `src`
`f2d594dd`; `pool_click`/`pool_view` 30d by `source` `69d5a276`.

## HEADLINE: a SECOND crawler burst — **76 sessions on 08-06**, bot-shaped, zero typed engagement — and the design direction moved TWICE while the backlog recorded neither. 225 was **merged to main as-is** by the human (13:00 UTC 08-06), which the BACKLOG still denies ("do NOT merge as-is"), and a live session then opened **PR #409** commissioning a *replacement* visual world (item 247). North star leg (B) = **0**, 30th straight day; leg (A) = **0 new** AI-referred sessions. `error_occurred` = 0 — 25th clean prod day — and for the first time the exercise count behind that zero is **not** zero.

## Real prod trend (daily totals, `cd434678`)
| date | session_start | error_occurred | pool_view | pool_click | page_view | plan_created |
|------|----|----|----|----|----|----|
| 07-24 | 3 | 0 | 0 | 0 | 0 | 0 |
| 07-25 | 4 | 0 | 0 | 0 | 1 | 0 |
| 07-26 | 6 | 0 | 2 | 0 | 4 | 0 |
| 07-27 | 3 | 0 | 0 | 0 | 1 | 0 |
| 07-28 | 8 | 0 | 0 | 0 | 1 | 0 |
| 07-29 | 5 | 0 | 0 | 0 | 2 | 0 |
| 07-30 | 6 | 0 | 0 | 0 | 2 | 0 |
| 07-31 | 4 | 0 | 0 | 0 | 0 | 0 |
| 08-01 | 9 | 0 | 1 | 0 | 2 | 0 |
| 08-02 | 3 | 0 | 0 | 0 | 0 | 0 |
| 08-03 | 92 (crawler burst, classified 08-04) | 0 | 0 | 0 | 23 | 0 |
| 08-04 | 8 (6 = fuzz replays) | 0 | 0 | 0 | 6 | 0 |
| 08-05 | 4 | 0 | 0 | 0 | 1 | 0 |
| **08-06** | **76 (crawler burst #2, classified below)** | 0 | 0 | 0 | 23 | 0 |
| 08-07 | 0 so far | 0 | 0 | 0 | 0 | 0 |

Prod 14d sessions: **231** (control `b16d8f7b`: **237**; the 6-session delta = localhost/preview, playbook
class 13). `error_occurred`, `pool_click` and `plan_created` are absent from BOTH result sets over 14d.
**Insufficient data** for every funnel claim at the ≥30-real-event rule — and the 14d total is now
inflated by two crawler bursts (92 + 76 = 168 of 231 sessions, **72.7%**), so the raw 14d count is a
worse demand proxy this tick than it was last tick, not a better one.

## §2b-adjacent: the 08-06 burst, decomposed and classified (`a00d1851`, `0e0df8d1`)
Followed `playbooks/traffic-quality-classification.md` — the JS-executing-crawler section, not the older
signature. **Verdict: crawler traffic. Not distribution. No opportunity.**

| URL | sessions | browsers |
|---|---|---|
| `https://www.defi.garden/` | 46 | Chrome 31, Mozilla 14, Edge 1 |
| `https://www.defi.garden/tokens` | 21 | Chrome 14, Mozilla 7 |
| `https://www.defi.garden/plan?waitlist=1&src=seo_tokens_hub` | 7 | Chrome 4, Mozilla 3 |
| `https://www.defi.garden/?token=RIMUUSDCCORE&poolTypes=Lending` | 1 | Safari |
| `https://www.defi.garden/?token=QNT&chain=Ethereum` | 1 | Safari |

Signature scored (≥3 hits = crawler; this window hits 5 of 5):
- **No acquisition** — 14d referrers are `$direct` **221 of 231**; the burst carries no `utm_*`/real referrer.
- **Zero engagement** — `search_input`, `plan_created`, `waitlist_email_entered`, `pool_view`, `pool_click`
  are all **absent** from the 08-06 result set. Only arrival-fired events exist.
- **`session_start` ≫ `page_view`** — 76 vs 23, the playbook's extra fingerprint. Bare `/` fired
  **46 session_starts and 0 page_views** (`34c218b3`) — the 08-03 shape repeating almost exactly.
- **Uniform fingerprint** — a bare-`Mozilla` UA carries 24 of the 76.
- **Follows CTAs** — 7 sessions walked the tokens-hub waitlist link to `/plan?waitlist=1&src=seo_tokens_hub`,
  auto-firing `waitlist_opened`. **Crawler-fired: does NOT count toward the gate.**

Waitlist 7d (`f2d594dd`): `waitlist_opened` **16** (10 `seo_tokens_hub` + 6 `undefined`);
`waitlist_email_entered` / `waitlist_submitted` **absent**. Traffic gate still **0 real** events.

**New this tick — the burst is a repeating pattern, not an incident.** Two bursts in four days (08-03: 92,
08-06: 76), same landing set, same CTA walk, same zero-engagement tail. Recorded as a pattern so a third
is read as cadence rather than as demand. It is NOT scored as an opportunity (playbook: honest no-op).

### The two Safari `?token=` arrivals — the only non-burst-shaped sessions, and n=2
Both are analytics-mode parameterized URLs (the sacred surface) with real token symbols and filters, on
Safari — the only browser in the window that is not part of the Chrome/Mozilla farm shape. Each produced
exactly one `page_view` and nothing else. Hourly (`389acd9b`): **06:00 UTC** and **16:00 UTC**.
Item 244's boot-race fix merged at **09:43 UTC** (`cfa36b25ac`, 11:43+02:00), so **one arrival is pre-fix
and one is post-fix**. `error_occurred` = 0 on both. **n=1 post-fix — no claim in either direction**; see
§2a.

## North star, both legs
- **Leg (A) agent consumption:** 14d referrers (`f8767f0b`, 231 total): `$direct` 221, `bing.com` 2,
  `www.ecosia.org` 2, `www.perplexity.ai` **2**, `t.co` 1, `www.0xhodler.nl` 1 (the human's own site),
  `www.tinker.com` 1, `www.recipebridge.com` 1 (junk). The perplexity pair is still the SAME 2026-07-26
  pair — **0 new AI-referred sessions** since the 08-04 strategy adoption, now day 3. `yandex.com` and
  `sogou.com` have aged OUT of the 14d window (were 1 each yesterday). Instruments 224 (D1 edge log) /
  227 (API) / 228 (MCP) remain unbuilt; leg (A) reads Mixpanel-only, blind to non-JS agent reads (212's gap).
- **Leg (B) pool-detail CTA clicks:** **0** in 14d — **30th** straight day. Real `pool_view` 14d = 0
  (30d = 12: `card_click` 10, `url_direct` 2, `69d5a276`). `pool_click` 30d = 1, still the single pre-123
  `undefined` event — ages out ~08-18.

## Guardrail — the clean streak, scoped honestly (LEARNINGS 082)
`error_occurred` = 0 across 14d in both prod-filtered and unfiltered sets → **25th** straight clean prod
day (control 9th). **The exercise count is no longer zero.** Yesterday this zero was recorded with
"~0 real analytics-mode loads"; today the analytics-mode boot path was exercised **twice** by plausibly-real
traffic (the two Safari `?token=` arrivals), **once after 244's fix deployed**. That is the first non-zero
exercise count this guardrail has ever carried for this surface. It is n=1 post-fix — far below the ≥30
rule — so it neither confirms 244's fix in prod nor refutes yesterday's prediction. It is recorded because
the exercise count, not the zero, is the number that changed.

## §2a predictions
**Carried from 2026-08-05 — fuzz-class decay. Status: TRACKING, day 3 of 7.**
Outcome: the pre-223 scrape-minted fuzz classes decay to zero as crawler caches expire. Witnesses (all):
ANY prod event broken down by `$current_url` matching those strings, filtered AND control. Measured
08-05, 08-06 and 08-07: **zero across all witnesses on all three days** (`34c218b3`, filtered + unfiltered).
Neither `` /?token=USDC` `` nor the empty `/?pool=` appears anywhere in the 08-05..08-07 URL breakdown.
Resolve at 7 consecutive zero days (→ ~08-11); a NEW mint string producible by the current tree reopens 223.

**Carried from 2026-08-06 — the 244 race reaching prod. Status: TRACKING, first exercise recorded.**
Outcome: the analytics-mode boot race can execute `PoolDetail.compiled.min.js` before `React` exists in
prod. Witnesses (all): `error_occurred` matching `React is not defined` / `reading 'some'`; a `session_start`
on a `?token=`/`?chain=` URL with NO subsequent `pool_view` in the same session; 224's D1 edge log once it
exists; any rendered-Playwright run reproducing it against prod. **Update:** the second witness finally had
something to read — 2 `?token=` sessions, and per that witness's own wording BOTH look positive (a
`session_start` with no `pool_view`). **That witness is confounded and is hereby retired**: `?token=` renders
the analytics GRID, and `pool_view` fires only on a pool-DETAIL render, so a grid arrival correctly produces
no `pool_view` whether the boot succeeded or failed. It could never have distinguished the two. `page_view`
DID fire on both (`trackPageView` is called from `app.js`, so app.js executed) — which is weak evidence
*against* a boot failure on these two, and is the witness that should have been named. Instrument blindness
recorded as a finding per §2a, not as a caveat.

## §2c Orphan-PR check (`pr-orphan-detector.js`, live data)
`list_pull_requests(state=open)` → **11 open**. Fed with `main`'s BACKLOG.md into `classifyAll`:

Orphan PRs: **8** (#409, #399, #396, #332, #331, #322, #316, #309) · PARKED 3 (#397/232, #392/224, #386/221)

Count unchanged from yesterday (8), **composition changed**: #408 (item 245) merged and left the set;
**#409 is new**. Per §2c step 5 this is detection only — disposition is a build-run or human question.
- **#409 `claude/design-world` is genuinely new work, not stale drift** (the other 7 are the known set;
  yesterday's verifier traced each individually). Opened 2026-08-06T14:32, last push 21:22, **9 commits,
  21 files, +2,803/−1,880**, `mergeable_state: clean`, and the only comment on it is Vercel's deploy bot —
  **no verifier verdict, no BACKLOG row, no terminal state**. Same shape as #399.
- **It claims id 247 without a BACKLOG row.** Its diff touches `NORTH_STAR.md` (+1 standing decision) and
  `LOG.md` (+2) but **not** `BACKLOG.md` — so 247 is claimed in prose only. `detectIdCollisions` →
  **no collision** for 247 (main has no row 247). #399's **242 collision persists, unresolved** (main
  shipped 242 to the unchecked-headline item).
- **Leg B — next id computed, not guessed:** `computeNextId(mainMax=246, openPrBranchIds=[247, 242])` =
  **248**. Any new row this tick starts at 248, not 247.

## Ship state — the design direction moved twice and the BACKLOG records neither
1. **225 (design reset) was MERGED TO MAIN as-is** — `75f7470646`, PR #393, 2026-08-06T13:00 UTC. The
   BACKLOG row still reads *"IN_REVIEW — HUMAN REVIEW FAILED 2026-08-05 (round 3 required, do NOT merge
   as-is)"*. That is a **stale status asserting the opposite of `main`**, and three rows are sequenced
   behind it. Synced this tick. (LOG records the human's verdict on merging: *"very ugly job and
   disappointed"* — merged anyway, on the record.)
2. **A live session then commissioned a REPLACEMENT world (item 247, PR #409)** — NORTH_STAR standing
   decision added on that branch, verbatim mandate *"ONLY SOTA GRADE WORK given the ICP"*; per the
   impeccable skill's doctrine "redesign REPLACES — the old look is evidence and anti-reference". So the
   just-merged Quiet system is already the anti-reference for the branch built on top of it.
3. **Two deviations from #409's own recorded commission**, both read off the branch's own bookkeeping —
   flagged for the human, NOT acted on (triage only):
   - The commission says *"Grid/search/planner/landing untouched this round; rollout + extract gated on
     the human's approval of the Vercel branch preview."* The final commit (`2b0560a`, 21:22) is titled
     *"certificate ornament system on **results, landing and search state**"* — i.e. the gated rollout leg
     ran. **No approval is recorded** in the branch's LOG or PR comments.
   - The commission names a deliverable *"world statement at `specs/247-design-world.md`"*. That file is
     **not in the branch's 21-file diff**.

## Gate re-check (§4) — one gate MET, two dependencies cleared
- **229 visuals leg `GATED(225 SHIPPED)` → GATE MET.** 225 merged to main 2026-08-06T13:00 UTC. Promoted
  to **READY** this tick. It was the only GATED row in the backlog; there are now **0 GATED rows**.
  Caveat recorded, not hidden: the gate's literal precondition (225 shipped) is met, but #409 proposes to
  replace the very design language the share-card restyle would target — so the human's 247 decision may
  moot it. Promotion is per the rule; the sequencing question is in the report.
- **238** (*"READY (after #393 — edits the same stylesheet)"*) and **246** (*"READY (after #393 merge, on
  main)"*) — both dependencies **cleared by the same merge**; both are now genuinely actionable on main.
- ≥30-real waitlist gate and ≥30-real pool-detail gate: **0 real events** this tick (the 16
  `waitlist_opened` in 7d are crawler-fired and never count) → unchanged.

## Experiments — no window closed, and none CAN close
No traffic gate has opened (real exercised pool-detail events 14d = 0, need ≥30). 117.1/.2/.3 remain
SHIPPED (measuring) under the 60d backstop (~09-16..18). Human-owned GSC reads: 013 ~08-23, 211 ~09-13.

## §2b product audit — `audit-app.js` fresh run (83 surfaces, 4 findings, 3 blocking)
`status: "OK"`, `generatedAt` **2026-08-07T07:29:15.466Z**, `surfacesCovered` **83** — both staleness gates
checked. Pools source: live fetch (15,602 pools). Exit code 1 (blocking findings present).
**Run bookkeeping, stated so the artifact is not over-read:** the audit ran TWICE this tick (07:19 full, then
07:29 full) with four isolated grid-only runs in between; the committed `audit-findings.json`/rotation
artifacts are the 07:29 run. The two full runs produced the **identical 4 findings**. The rotation state
therefore advanced twice in one tick, which slightly overstates this tick's share of the ~124-tick full pass.

### First audit on post-225 `main` — the design reset landed and the scanner is quieter, but only one of the two drops is real
Findings fell **8 → 4** (blocking 7 → 3). Decomposed:

| finding | yesterday | today | read |
|---|---|---|---|
| `grid-token` page-error + dead-end (item 244) | P0 + P1 | **absent** | **real fix** — 244 merged 09:43 UTC |
| `.app-footer` occlusion (item 221) | 4 (grid-chain 1280 P0+P1, grid-360 P1, grid-768 P1) | **absent** | **NOT a repair — see below** |
| `pool-prescan:apy-rail-breach` | 0 | **2** | churn |
| `pool-prescan:mean30d-rail-breach` | 8 | **11** | churn |
| `i18n:en-ko-parity` | 0 | **2** | **NEW — from 225's own commit** → items 248/249 |
| `planner-768 :: responsive` (item 235) | P2 | P2 | unchanged, fires every tick |

### The occlusion class went quiet and it is NOT fixed — the mechanism is grep-confirmed intact
Yesterday's snapshot recorded the inverse error (findings vanished because a surface failed to render) and
said the delta must not be misread. Today the surfaces DID render — `grid-token` produced no page-error and
no dead-end — so the absence is not masked by a render failure this time. It is still not a repair:

- **`.app-footer { position: fixed }` is UNCHANGED on `main`** — `style.css:2874-2875`, read directly.
  The mechanism that produces the entire class is live.
- **Exercise count, stated: 6 runs, 0 occlusion findings** — 2 full 83-surface runs plus 4 isolated
  `--only=grid-chain,grid-360,grid-768` runs. Yesterday the same 4 findings appeared in the full run and had
  reproduced every tick for days, so 0/6 is meaningfully stronger than 0/1 — but it remains an
  absence-of-failure claim, and item **231** measured this lens at **3/20** detection on `dead-pool`.
- **Best explanation:** 225 was the only render-path change between the two ticks; its layout/spacing moved
  those four specific elements clear of the fixed footer. The CLASS is intact and any future layout change
  can re-expose it — which is precisely what 225-successor work (236/237/238/246, and PR #409's item 247)
  is about to do to these same surfaces.
- **Consequence for item 221 (PARKED, PR #386):** #386 fixes the MECHANISM (moves the footer out of the
  fixed layer) and stays worth merging; its urgency drops from live-defect to latent. Row updated with this
  measurement rather than closed.

### NEW — `i18n:en-ko-parity` 0 → 2, and both keys came from 225's own commit
`544` keys scanned (was 540), allowlist 24, **2 suspects**: `resultsColApy` (ko `"APY"`) and `resultsColTvl`
(ko `"TVL"`), both byte-identical to EN. `git log -S"resultsColApy" -- translations.js` → **`75f7470646`**,
the 225 design-reset merge.

**Measured before scoring — 225 added 5 keys in both namespaces, 3 of 5 EN/KO byte-identical:**
`resultsColApy` "APY", `resultsColTvl` "TVL" (both caught), and ``onProtocolChain: (protocol, chain) =>
`${protocol} · ${chain}` `` (**not caught — function leaf**). `resultsColPool` was translated correctly
("Pool" → "풀").

**These two are allowlist omissions, not untranslated copy — the dictionary already answers it.** KO renders
these acronyms bare and consistently, and `I18N_UNTRANSLATED_ALLOWLIST` (`audit-app.js:961-986`) already
blesses `navFilterApy`, `navFilterTvl`, `tvl`, `planner.poolApy`, `planner.poolTvl` and — decisively —
**`tcpColApy`/`tcpColTvl`, the token/chain-page column headers for these exact two acronyms** — each with the
reason `'acronym, same in KO'`. → item **248** (2-line allowlist fix; `translations.js` byte-unchanged).

### The larger finding the miss exposed: the gate is blind to 28.1% of the dictionary
`audit-app.js:1071` skips any leaf whose EN or KO value is not a `string` (in-code comment:
*"function/array/number leaves: parity only"*). Measured against the live dictionary:

| leaf type | count | share of 544 | checked by Rule 2? |
|---|---|---|---|
| string | **391** | 71.9% | yes |
| **function** | **153** | **28.1%** | **no — key existence only** |

~80 of the 153 are `planner.*` (`bloomHeadline`, `heroTarget`, `shareTweet`, `degenHaircutNote`, …) — the
**default surface for the ICP** — plus the whole `tcp*` family behind the generated SEO estate.

**The blind region is CLEAN today, measured two ways before the item was scored** (this is the tick's main
investigation, and it stopped an inflated ticket):
- EN/KO function source byte-identical: **2 of 153 (1.3%)** — `onProtocolChain`, `poolPageTitle`; neither
  carries Latin prose in its literal text (pure interpolation + punctuation).
- The gate's **own predicate** applied to KO function bodies (strip parameter list, strip `${…}`, then
  no-Hangul AND ≥1 Latin letter): **1 suspect of 153** — `poolPageTitle`,
  `` (symbol, project) => `${symbol} (${project}) | DeFi Garden 🌱` `` — whose only Latin is the **brand name**.

So: **zero live untranslated-copy defects behind the blind spot.** Filed as coverage/latent → item **249**
(5.5), not as the P1 it superficially resembles. Third narrowing found in this gate's predicate (items
**198**, **212** are the precedents) and the first in its *type* coverage rather than its logic.

### Prescans and the rest
- **Static prescan: 4,324 pages, 0 suspects** — scoped to its 6 implemented signals
  (`broken-number-literal`, `absurd-magnitude`, `junk-slug`, `zero-yield-claim`, `link-target-integrity`,
  `pool-link-liveness`). By family: tokens 2,076 / chains 86 / ko-tokens 2,076 / ko-chains 86.
  Text surfaces: 2 scanned, 0 suspects over 5 signals.
- **Pool prescan: 3,971 union pools (735 snapshot + 3,236 deep-linked-live), 13 suspects:**
  `apy-rail-breach` **2** (was 0 — examples `18665e0b`, `fb71cd25`) and `mean30d-rail-breach` **11** (was 8).
  Both movements are **live-feed churn, not repairs and not regressions** — the same class recorded on 08-05
  (1 → 0) and 08-06 (12 → 8). 3,652 of 3,652 deep-linked ids confirmed live.
- Rotation: **32 pool-details rendered / 32 picked**, cycle 0, seen 246/3,968, wrapped=false;
  ~124 ticks for a full pass. 6 pool lenses (360px ×2, dark ×2, ko ×2), 0 skipped. `subRailPicked` 25/32.
- `[P2] planner-768 :: responsive` — `.gp-chip` matched zero elements: the known item **235** defect (the
  audit's own driver clicks the chip away before measuring). Fires every tick until 235 ships.
- `dead-pool` covered with no finding — **NOT claimed clean**: item 231 documents the occlusion lens at
  3/20 detection on that exact surface; absence is weak evidence there.
- Checks that ran: the scanner's implemented classes (number-sanity, dead-end, loading, page errors, i18n,
  dead-CTA, responsive/dark, occlusion) over the 83-surface rotation + the prescans above. Every claim in
  this section is scoped to those classes and populations only.

### BACKLOG table integrity — a numbered residual, wider than 245's notes recorded
Writing this tick's rows exposed one immediately (an unescaped `||` inside prose desynced row 249's columns;
caught and rewritten before commit). Re-measuring the whole table: **13 of 236 rows (5.5%) do not parse to 8
cells** — ids **065, 104, 116, 120, 122, 157, 166, 189, 193, 194, 199, 236, 237**. `specs/245-notes.md`
recorded this residual as "2 live rows (236, 237)"; the real figure is **13**. All degrade safely toward
ORPHAN (never a false PARKED/BLOCKED), and none is one of this tick's edited rows (221/225/229/238/246/248/249
all verified 8-cell). Not ticketed — recorded so the next parser change is sized against 13, not 2.

## Diff vs 2026-08-06
1. **Second crawler burst: 08-06 closed at 76 sessions**, not the "0 so far" yesterday's snapshot could
   see. Bot-shaped on 5 of 5 signature criteria, zero typed engagement. Two bursts in four days is now
   recorded as a **pattern**, and 72.7% of the 14d session count is burst traffic.
2. **225 merged to main as-is** — BACKLOG status was stale in the *unsafe* direction (it said "do NOT
   merge" about a commit already on main). Synced. **229's gate is met → READY**; 238 and 246 unblocked.
3. **PR #409 is a new orphan**: a live-session replacement visual world (item 247), 21 files, no verifier
   verdict, no BACKLOG row, two documented deviations from its own commission.
4. Orphan count **8 → 8**, composition changed (#408 merged out, #409 in). #399's 242 collision persists.
5. Guardrail streak 24 → **25** clean prod days, and its **exercise count moved off zero for the first
   time** (2 analytics-mode arrivals, 1 post-244-fix).
6. **A filed witness was retired as confounded**: "`session_start` on `?token=` with no `pool_view`" cannot
   see the 244 race, because `pool_view` never fires on a grid arrival regardless.
7. Fuzz-decay prediction: **day 3 of 7 at zero** across all witnesses, all three days.
8. Leg (A) referrers: `yandex.com` / `sogou.com` aged out of the 14d window; perplexity pair unchanged.
9. **Audit findings 8 → 4** (blocking 7 → 3), first run on post-225 `main`. **244's fix confirmed** —
   `grid-token`'s page-error + dead-end are gone and the surface renders. **221's 4 occlusion findings are
   also gone and that is NOT a repair** — `.app-footer { position: fixed }` unchanged at `style.css:2874`;
   0/6 runs; class dormant, mechanism live.
10. **NEW `i18n:en-ko-parity` 0 → 2**, both keys introduced by 225's own merge — and both are **allowlist
    omissions**, not untranslated copy (`tcpColApy`/`tcpColTvl` are already allowlisted for the same two
    acronyms). → **248**.
11. **The miss exposed a 28.1% blind spot**: the parity gate value-checks 391 of 544 keys and skips all 153
    function leaves, ~80 of them `planner.*`. Blind region **measured clean** (1 suspect of 153, the brand
    name) → filed as latent coverage **249**, not as a P1.
12. Pool prescan `apy-rail-breach` 0 → **2**, `mean30d-rail-breach` 8 → **11** — churn both ways, as on the
    two prior ticks. Static prescan 4,332 → **4,324** pages, still 0 suspects on 6 signals.
13. **BACKLOG table integrity re-measured: 13 of 236 rows malformed, not the 2 recorded in 245's notes.**
