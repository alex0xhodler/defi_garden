# Signals snapshot — 2026-08-04 (heartbeat, live `Mixpanel` connector)

Project: defigarden (4042048) · org Equitee (2885044) · prod filter `$current_url contains www.defi.garden`.
Queries this run: prod daily 14d `830837ff`; unfiltered control 14d `78325c0c`; spike-day by URL `bc8d0dd0`;
spike-day fingerprint `6fcb43a9`; spike-day acquisition `a1bb6ebf`; spike-day waitlist funnel `e735b851`;
`waitlist_opened` by src `abea84a5`; `pool_click` by source 30d `562fd50e`; funnel events 30d `7eef36d1`;
`page_view` by src 14d `ac34d5cd`; `page_view` by URL 7d `36d76935`; tokens-hub arrivals by day `7d7ade54`.

## HEADLINE: **08-03 logged 92 prod sessions (baseline 3–9/day) — classified CRAWLER BURST per the playbook, not distribution. But it carried three firsts: the first non-`undefined` `src` attribution ever recorded in prod (`waitlist_opened{src=seo_tokens_hub}` ×5 — the 202/204 chain is PROVEN live end-to-end), the first `waitlist_opened` events ever (9, all machine-triggered via `?waitlist=1` auto-open), and the first search-engine referral on record (bing.com, n=1).**
North star `pool_click{source ∈ garden_cta, protocol_link}` = **0** in 14d (**27th** straight day).
Guardrails clean — prod `error_occurred` **absent from the result set** for **22 straight days**, absent in
the unfiltered control too (6th consecutive day).

## Real prod trend (daily totals, `830837ff`)
| date | session_start | error_occurred | pool_view | pool_click |
|------|----|----|----|----|
| 07-21 | 6 | 0 | 0 | 0 |
| 07-22 | 4 | 0 | 0 | 0 |
| 07-23 | 7 | 0 | 0 | 0 |
| 07-24 | 3 | 0 | 0 | 0 |
| 07-25 | 4 | 0 | 0 | 0 |
| 07-26 | 6 | 0 | 2 | 0 |
| 07-27 | 3 | 0 | 0 | 0 |
| 07-28 | 8 | 0 | 0 | 0 |
| 07-29 | 5 | 0 | 0 | 0 |
| 07-30 | 6 | 0 | 0 | 0 |
| 07-31 | 4 | 0 | 0 | 0 |
| 08-01 | 9 | 0 | 1 | 0 |
| 08-02 | 3 | 0 | 0 | 0 |
| 08-03 | **92** | 0 | 0 | 0 |
| 08-04 | 0 so far | 0 | 0 | 0 |

Prod 14d sessions: **160** (unfiltered control `78325c0c`: **166** — 6-session delta = localhost/preview,
playbook class 13; both recorded, claim from the filtered one). `error_occurred` and `pool_click` absent
from BOTH result sets. **Insufficient data** for every funnel claim at the ≥30 real-event rule (see below —
the 92 do not count as real).

## The 92-session day, classified per `traffic-quality-classification.md`: CRAWLER BURST
- **URLs (`bc8d0dd0`):** `/` ×58 · `/tokens` ×22 · `/plan?waitlist=1&src=seo_tokens_hub` ×9 ·
  `/plan?lang=ko%60` ×2 · `/chains/unichain` ×1.
- **Fingerprint (`6fcb43a9`):** 13 unique users for 92 sessions; bare-`Mozilla` UA = **27 events / 1
  unique**; Chrome 63/10 (headless-farm shape); Firefox 1/1, Edge 1/1.
- **Acquisition (`a1bb6ebf`):** `$direct` 91/92; bing.com 1.
- **Engagement:** `page_view` 23 vs `session_start` 92 (bare `/` fired 58 session_starts and ZERO
  page_views); `search_success`/`pool_view`/`pool_click`/`plan_created`/`waitlist_email_entered` all 0.
- **Score:** no-acquisition ✓ (91/92), uniform fingerprint ✓ (27/1 UA), zero real engagement ✓, garbage
  param ✓ (`lang=ko%60`). ≥3 marks across ~100% of the window → **crawler traffic; not distribution.**
  New signature wrinkle, playbook UPDATED: these crawlers EXECUTE JS (session_start is a JS event) and
  FOLLOW CTAs — 9 followed the tokens-hub waitlist link, and `?waitlist=1` auto-opens the modal, so
  `waitlist_opened` fired 9× with zero human intent. **Rule: an event fired unconditionally on arrival or
  by URL param is NOT engagement**, and crawler-fired events do NOT count toward traffic gates (the ≥30
  `waitlist_opened` gate stays at 0 real events).

## The 08-02 `src` prediction (due ~08-05): RESOLVED — attribution WORKS; NOT the P1 regression
Condition finally met: static-page arrivals 08-03 = **22** on `/tokens` (`7d7ade54`; was 0 since the
202/203/204 ships). Result:
- `waitlist_opened` by src 08-03 (`abea84a5`): **`seo_tokens_hub` = 5**, undefined = 4 — the first
  non-`undefined` `src` on ANY prod event, ever. The static-CTA → tagged-URL → captured-prop chain
  (202/204 + analytics.js base-context acquisition, `analytics.js:32-56`) is live end-to-end in prod.
- `page_view` by src 14d (`ac34d5cd`) is STILL one row `undefined` = 51 — and that is the instrument's
  blindness, not a regression: `trackPageView` is called **only from app.js** (analytics mode; grep:
  planner.js/landing.js call sites = 0), so planner-bound arrivals — the exact path the waitlist CTA
  drives — never appear in a `page_view` breakdown. **Instrument correction (playbook updated): read the
  distribution check off `session_start` by `src` (acquisition is base-context on every event), never off
  `page_view` alone.**
- Residual oddity, recorded not ticketed (n=9, all crawler): 4 of the 9 `waitlist_opened` carry
  `src=undefined` despite all 9 arrivals landing on a `src=`-tagged URL — possibly storage-less bot
  sessions dropping the captured acquisition. Re-look only if it recurs on real traffic.

## URL provenance (playbook class 9) — one INVESTIGATED and traced to OUR OWN served test files
- `/plan?lang=ko%60` ×2: `%60` = a literal backtick. No generated href emits it (grep over tokens/,
  chains/, sitemaps, llms*.txt, shipped JS: 0). Source found: **the prod domain serves the entire repo** —
  `test_min_asset_boot.js:262` contains `` `/plan.html?lang=ko` `` and curl on
  `www.defi.garden/test_min_asset_boot.js` returns **200**; a crawler scraping URLs out of raw JS took the
  template literal's closing backtick with it. Also 200 live: `audit-app.js`, `generate-stories.js`,
  `product-loop-kit/NORTH_STAR.md`, `product-loop-kit/BACKLOG.md`, `CLAUDE.md`, `.claude/agents/verifier.md`.
  No secret exposed (repo is public on GitHub; credentials never committed — standing decision 2026-07-09),
  so this is hygiene, not a breach → **finding 223** (P2): crawl-budget waste on the SEO-moat domain,
  internal docs indexable beside `llms.txt`, and a live fuzz-URL mint.
- bing.com referral ×1 (`a1bb6ebf`): first search-engine referral on record — the earliest observable
  downstream of the 022 IndexNow leg. n=1, no claim.
- `/chains/unichain` ×1: real generated page, provenance clean.

## §2b product audit — `audit-app.js` (83 surfaces, 5 findings, **2 net-new tickets**)
Fresh run this tick: `status: "OK"`, `generatedAt` **2026-08-04T07:13:34.180Z**, `surfacesCovered` **83** —
both staleness gates checked. Measured at `f66ea3fde5` (origin/main tip).
- **215 is live and visible in its own numbers:** `poolPrescan.scanned` = **3,987** (737 snapshot + 3,250
  deep-linked-live; was 734 snapshot-only two ticks ago). `mean30d-rail-breach` = **14** (was 1): the
  known `201e5f6e` + 13 net-new from the widened population — exactly the class 215 predicted the old leg
  could not see. 2 of 14 promoted and rendered this tick (`63eb0ca9`, `18665e0b`): **clean** — live-only
  pools carry no `kpis`, so the record-level breach never reaches the DOM (the `kpiApplicableNote`
  states the skip honestly). Aggregate stays blocking (P0) while 12 remain un-rendered — correct per
  "undeterminable stays blocking"; rotation/promotion clears ~2/tick. No new ticket: detector working as
  specced.
- **Occlusion lens (219): the 4 known findings reproduced** — grid-token 1280×780 P0
  (`calculate-yield-btn-new` 98.8% under `.app-footer`, hit-test resolves to footer) + P1 (`pool-symbol`
  100%, +5 more); grid-360 P0 (duplicate `.theme-toggle` 100% under `.google-header-sticky`) + P1
  (`pool-symbol` 100% under footer, +3 more). These were "left as their own tickets" by 219/220 but **no
  backlog row existed** — ticketed today as **221** (grid footer occlusion, the last surface of the
  179→217→218→220 class) and **222** (duplicate theme-toggle, unpressable control).
- Static prescan clean: `scanned 4,372`, `suspectCount 0` all signals. Pool rotation: 32/32 rendered,
  subRailPicked 26/32, 3,679/3,681 deep-linked ids live. `apy-rail-breach` 0 of 3,987;
  `kpi-nonfinite` 0 of 737 applicable.

## Experiments — no window closed today
Next: 117.1/.2/.3 → 08-05; 124 → 08-06; 125 → 08-07; 129 → 08-08; 045/051/065/148 → 08-09..08-12;
180/182 → 08-13; 190/191/192 → 08-14; 196 → 08-15; 197-205 → 08-15..08-16; 206/207/210/213 → 08-16;
211 GSC leg → human ~09-13; 013 GSC leg → human ~08-23.

## Diff vs 2026-08-03
1. **92-session day — biggest single prod day on record, classified crawler burst** (JS-executing,
   CTA-following; playbook updated with the new signature). Not distribution; 069 packs still unposted.
2. **The 08-02 `src` prediction RESOLVED, not-a-regression:** first prod `src` attribution ever
   (`waitlist_opened{seo_tokens_hub}` ×5); `page_view`-by-src was the wrong instrument (planner fires no
   `page_view`) — distribution check moves to `session_start`-by-src.
3. **First-ever `waitlist_opened` in prod** (9) — instrument prod-proven firing, but all crawler-triggered;
   the ≥30 traffic gate counts 0 real events.
4. **221/222 ticketed** (219's quarantined occlusion set, un-ticketed until now) and **223 found**
   (repo served wholesale on prod; measured fuzz-URL consequence).
5. Prescan widening (215) live: rail-relative checks now reach 100% of the 3,987-pool union (was 18.5%);
   14 suspects, 2 rendered clean, 12 rotating through promotion.
6. Guardrail streak 21 → **22** clean prod days; control clean 6th day. North star 0 (**27th** day).
   `pool_click` 30d still the single pre-123 `undefined` event (ages out ~08-18).
7. Max id re-read off `origin/main` immediately before writing: **220** → today's findings are
   **221/222/223**.
