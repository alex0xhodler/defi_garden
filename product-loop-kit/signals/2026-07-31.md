# Signals snapshot — 2026-07-31 (heartbeat, live `Mixpanel` connector)

Project: defigarden (4042048) · org Equitee (2885044) · prod filter `$current_url contains www.defi.garden`.
Queries this run: prod daily 14d `470a6e97`; unfiltered control 14d `9df8d32b`; `pool_click` by `source`
30d `0921077d`; prod funnel events 30d `3601d099`; prod page_view-by-URL 7d `3bb75bb7`; prod page_view by
`src` 14d `55a355c7`.

## HEADLINE: **first tick with ZERO blocking audit findings since the scanner began originating them — and 182 closed its class completely: 735 of 736 pools now resolve a "Start Earning" CTA from our own origin, the 736th renders an honest fallback.**
Yesterday's headline was that half the north star was gated on a silently-failing third-party fetch with a
96-entry hand-maintained fallback covering 70.9% of pools. Measured this tick against the shipped code:
`data/protocol-urls.json` carries **475 entries** and covers **735/736 snapshot pools (99.9%)**. The single
uncovered project (`sdai`) is exactly the one 182's spec called out as unreachable on the happy path, and
`PoolDetail.js:150-192` now renders an honest DefiLlama link for it rather than empty space. **The class is
closed, not narrowed** — the first time on this repo a "checker follows the last bug" repair covered the
whole population instead of the instance that bit us.

North star `pool_click{source ∈ garden_cta, protocol_link}` = **0** in 14d (**23rd** straight day).
Guardrails clean — prod `error_occurred` **0 for 18 straight days**, and **0 in the unfiltered control too**.

## Real prod trend (daily totals, `470a6e97`)
| date | session_start | error_occurred | pool_view | pool_click |
|------|----|----|----|----|
| 07-17 | 2 | 0 | 0 | 0 |
| 07-18 | 7 | 0 | 1 | 0 |
| 07-19 | 6 | 0 | 3 | 1 |
| 07-20 | 17 | 0 | 0 | 0 |
| 07-21 | 6 | 0 | 0 | 0 |
| 07-22 | 4 | 0 | 0 | 0 |
| 07-23 | 7 | 0 | 0 | 0 |
| 07-24 | 3 | 0 | 0 | 0 |
| 07-25 | 4 | 0 | 0 | 0 |
| 07-26 | 6 | 0 | 2 | 0 |
| 07-27 | 3 | 0 | 0 | 0 |
| 07-28 | 8 | 0 | 0 | 0 |
| 07-29 | 5 | 0 | 0 | 0 |
| 07-30 | **6** (finalized, was "1 so far") | 0 | 0 | 0 |
| 07-31 | 0 so far | 0 | 0 | 0 |

`error_occurred` is **absent from the prod result set** across the whole 14d window — zero, not low.
**Control (`9df8d32b`):** the unfiltered run also returns `error_occurred` **absent**, second consecutive
day. So the guardrail claim holds with AND without the prod filter. Keep the filter anyway — playbook
`product-audit.md` class 13; 07-29 is the worked example of what dropping it costs.
Prod session totals 14d: **84** (unfiltered **90** — the 6-session delta is localhost/preview).
**Insufficient data** for every funnel claim (≥30 rule). All crawler-shaped except the single 07-19
engaged session.

## North-star surface (`0921077d`, 30d)
- `pool_click` 30d = **1**, `source=undefined` (the pre-123 07-19 event). **Zero CTA clicks on either entry
  path, ever.** Unchanged for a 6th day.
- Prod funnel events 30d (`3601d099`): `plan_saved` **5** · `search_success` **4** · `plan_created` **3** ·
  `share_link_opened` **1** · `share_link_created` **0** · `tend_reminder_added` **0** ·
  `waitlist_submitted` **0**. Identical to 07-30 — no new funnel events in 24h.

## Prod page_views by URL, 7d (`3bb75bb7`, total 11)
One event each: `/chains/linea`, `/chains/avalanche`, `?token=BUIDL`, `?token=BUIDL&lang=ko`,
`/tokens/buidl`, `/tokens/usdc`, `/tokens/slisbnb`, `/tokens/eth`, `?token=ICP&poolTypes=Yield%20Farming`,
`?chain=Popular&minTvl=1000000&pool=747c1d2a…`, `?token=USDC&chain=Popular&pool=4438dabc…`.
- **URL-provenance check (playbook class 9), run this tick: clean, and the 148 class stays closed.**
  Zero slugs that are pure-numeric or date-shaped. `?poolTypes=Yield+Farming&token=20` has now aged out of
  the 7d window entirely; `/tokens/20` still returns **404** on prod (curl, this tick). **No new provenance
  suspects.**
- **Two new crawler arrivals this week — `/tokens/slisbnb` and `/chains/linea`.** Both `?pool=`-bearing
  URLs from last week persist. Volume 19 → 11: fewer crawler arrivals, not a product change.

## Distribution check (`55a355c7`, the cheapest true test per LEARNINGS 2026-07-27)
Prod `page_view` by `src` over 14d: **one row, `undefined` = 47**. Zero tagged arrivals, unchanged for the
**19th day**. The 069 spotlight packs remain unposted; every distribution hypothesis stays untestable by
construction.

## Live verification of the last four ships (this tick)
| check | before | now |
|---|---|---|
| **182** — protocol-URL coverage of the snapshot | 96 static entries → **70.9%** (216 pools with no CTA) | `data/protocol-urls.json` **475 entries → 735/736 (99.9%)** |
| **182** — the one genuinely unreachable pool (`sdai`) | renders **nothing** | honest DefiLlama fallback, `PoolDetail.js:150-192`, reusing `.cta-button-protocol` verbatim |
| **184** — level-2 `?pool=` deep-link liveness | hand-run, 4 dead / 3,677 | **automated in the scanner**: `checkedIds 3707, deadIds 1, drift 1, allowance 37.07, ok: true` |
| **187** — third-party font CDN | `style.css:1` = `@import …fontshare…` | `style.css:1` = `:root {`; prod `style.min.css` carries **no** fontshare reference |
| **188** — filter URLs in `sitemap-main.xml` | 7 homepage-duplicate `?minTvl=`/`?minApy=` URLs | **0**; replaced by 6 gated `?chain=All…` rungs that reach a real grid |

All four are clean ships.

## A build loop shipped item 189 WHILE this tick was running — verified independently, and it dates the audit
`549560fc9` ("the sitemap's pool-type taxonomy was a stale fork, so 8 of 114 category URLs we submit to
Google rendered an empty grid") landed on `main` after this tick's signal read and after `audit-app.js`
had already run. Two consequences, both recorded rather than smoothed over:

1. **ID collision — this tick's findings were renumbered.** The heartbeat drafted them as 189/190 against a
   backlog whose max ID was 188; the build loop took 189 concurrently. They are filed as **190** and **191**.
   The lesson is cheap and worth keeping: **re-read `origin/main`'s max backlog ID immediately before
   writing rows, not at the start of the tick** — the two loops now run concurrently often enough that a
   tick-length gap is a real collision window.
2. **The audit result describes commit `2f848c9a0`, not the current `main` tip.** Everything in the §2b
   block below — including "static prescan clean" — was measured *before* 189 regenerated the category
   sitemaps. It is not wrong, but it is **one commit stale**, and the next tick's run is the first to cover
   189's output. Stated so nobody reads today's clean prescan as covering today's HEAD.

**189's own claim, verified here independently** (its verifier passed it; this is a second opinion from
outside that conversation): all **8** tokens 189 named as rendering an empty grid now carry a category URL
in an honest file — `WBETH` → Staking, `SUSDS` → Lending, `SAVAX` → Lending, `USDY` → RWA,
`SIERRA` → Yield-Derivatives, `BEAT`/`XMR`/`ECR` → LP-DEX. Both predicted new files exist with the
predicted counts (`sitemap-category-RWA.xml` **3**, `sitemap-category-Yield-Derivatives.xml` **4**).
Category `<loc>` total is **117** against the spec's simulated 116 — a +1 delta from ordinary pool churn
between the snapshot simulation and the live regen, which is the expected shape, not a miss.

## §2b product audit — `audit-app.js` (29 surfaces, 1 finding, **0 blocking**) — measured at `2f848c9a0`
`NODE_PATH=… node audit-app.js` → `status: "OK"`, `generatedAt` **2026-07-31T07:09:44.310Z** (this tick),
`surfacesCovered` non-empty (29) — both staleness gates from the playbook checked before claiming clean.
Playwright resolved locally (1.56.1); pools from a **live fetch of 15,873 pools**, not the committed snapshot.
- **Static prescan clean for the second consecutive day: `scanned 2179, suspectCount 0`** across all five
  signals (`junk-slug`, `zero-yield-claim`, `link-target-integrity`, `broken-number-literal`,
  `pool-link-liveness`). Text surfaces: 2 scanned, **0** across all five signals.
- `poolLinkLiveness` (184, first automated run): **3,707 ids checked, 1 dead, 1 page affected**, classified
  `drift` against an allowance of 37.07 → `ok: true`. Yesterday's hand-run measured 4/3,677. **A gate that
  now proves its own true negative** — 172's precedent, discharged.
- `poolPrescan`: 736 pools, 1 `mean30d-rail-breach` (`201e5f6e`), **auto-downgraded P1 → P2 by 171's
  reconciliation** for the third consecutive day. Working as specced.
- **Zero P0/P1 findings.** First such tick since the scanner started producing originating findings (180 on
  07-29, 182 on 07-30).

## Checked and CLEARED — no ticket (recorded so the next tick does not re-derive)
- **188's new `?chain=All&minTvl=1000000` rung skips the snapshot fast path.** `app.js:1144`
  `snapshotEligible = !urlParams.pool && urlParams.minTvl >= DEFAULT_MIN_TVL`, so a $1M floor forces
  `loadLive()`. **Not a defect — load-bearing by necessity**: `data/pools-snapshot.json` holds only ≥$10M
  pools, so the snapshot *could never* serve that page's set. Exactly 173's documented trade, 1 of the 6
  rungs affected (the `minApy` rungs omit `minTvl` entirely and stay snapshot-eligible; `minTvl=100000000`
  clears the floor). Verified, closed.
- **188 shipped correctly, and the first read of it was wrong.** A grep for `minTvl=|minApy=` on the prod
  sitemap returns 20 lines, which reads like the removal failed. It is 5 `<loc>` values plus their
  `hreflang` alternates, and none of them is one of the 7 URLs 188 removed — 188 *converted* the class into
  gated `?chain=All…` rungs rather than deleting it. **Count `<loc>` elements, never raw lines, when
  checking a sitemap claim.**
- **EN/KO key parity is perfect: 543/543 flattened keys, 0 missing in either direction**, across all three
  namespaces (top-level 185, `landing` 41, `planner` 319). A true negative, measured for the first time.

## NEW FINDING → 190 (P2/READY 5.4): the KO estate has a checker for money units and none for whether the Korean is Korean
Measured this tick with a flattened three-namespace diff of `translations.js`:
- **Key parity is clean** (543/543, above). **Value-level honesty is not.** Ten KO values contain zero
  Hangul; eight are legitimately identical by nature (brand/product names — `Claude Pro`, `Apple TV+`,
  `ChatGPT Plus`, `Uber One`, `Leviathan News`, `DefiLlama API`; the acronym `LP/DEX`). **Two are ordinary
  English prose that was never translated:** `landing.footerPoweredBy` = `"Powered by"` and
  `landing.footerMadeWith` = `". Made with AI & Degen Love."`.
- **They render on the whole static estate.** `landing.js:358-360` composes all three footer strings
  unconditionally, and `landing.js` backs **2,113 `tokens/*.html` + 88 `chains/*.html` = 2,201 pages**. So
  every KO lander closes with an English sentence. `detectLanguage()` (`landing.js:47-55`) also auto-selects
  KO from `navigator.language`, so a Korean-browser visitor gets it without ever passing `?lang=ko`.
- **KO traffic is real, not hypothetical:** `?token=BUIDL&lang=ko` is in this week's prod `page_view` log.
- **The gap is the checker, not the two strings.** `test_ko_pool_money_honesty.js` guards KO *currency
  units* (item 137's bug); `test_i18n_pages.js` and `test_translations_fallback.js` guard page wiring and
  fallback behaviour. **Nothing anywhere asserts that a KO value differs from its EN value.** This is item
  175's thesis on its Nth instance — the signal set was drawn from 137, the last i18n bug anyone was bitten
  by, so the class nobody had been bitten by (untranslated prose) went unchecked. Ships as a **repair plus a
  gate**, with an explicit brand-name allowlist so the eight legitimate cases never cry wolf.

## NEW FINDING → 191 (P2/READY 4.8): 183 fixed which pool-details get audited, not how many
183 shipped the rotation that ended the one-hardcoded-pool era (167). This tick's `poolRotation` block:
`cycle 0, seenCount 16, candidateCount 734, picked 2`.
- At `DEFAULT_POOL_SAMPLE = 2` (`audit-app.js:214`), one full pass over the north-star surface takes
  **367 days**. A given pool-detail defect has a **~0.27% chance per day** of being rendered.
- The cheap `poolPrescan` covers all 736 every tick — but only for the five *numeric* signals. The classes
  that need a render (`dead-cta`, i18n leaks, 360px clipping, console errors — the ones that found 136, 137
  and 182) still reach **2 pools a day**.
- `MAX_POOL_SAMPLE = 6` already exists (`audit-app.js:215`), so 3× throughput is a budget change, not new
  machinery: 367 days → **123**. The honest framing is that this buys a constant factor, not closure —
  worth stating in the item rather than implying a fix.
- This is item 167's own decision rule (*"what population is this target drawn from, and what fraction can
  this selection ever reach?"*) asked one level up: 167 and 183 fixed target **selection**; nobody has
  asked about target **throughput**.

## Data layer — healthy
Snapshot `generatedAt` this tick, **736 pools** (741 yesterday).
- **0** pools with total APY > 1000%; **0** non-finite KPIs; `mean30d` breach set unchanged at 1
  (`201e5f6e`, known-and-handled, auto-downgraded by 171).
- Live DefiLlama fetch succeeded in-session: **15,873 pools**, used by the scanner as an independent oracle,
  so the liveness and coverage numbers above do not rest on the committed snapshot.

## Experiments — no window closed today
- Windows still open: 124 → 08-06; 125 → 08-07; 045/148 → 08-09; 159 → ~08-10; 050 → 08-11;
  013 GSC leg → human ~08-23. 182/183/184/185/186/187/188 shipped 07-30→07-31, windows not yet due.
- 116 closed yesterday (INCONCLUSIVE at n≈0); no re-measure possible until the first 069 post.

## Diff vs 2026-07-30
1. **182's class is fully closed, verified against the shipped artifact** — 70.9% → **99.9%** protocol-URL
   coverage (475 baked entries, 735/736 pools), with an honest fallback for the 736th. Yesterday's headline
   defect is gone.
2. **First zero-blocking audit tick** since the scanner began originating findings. Static prescan clean for
   a second day; text surfaces clean; the only finding is the known, auto-downgraded `201e5f6e`.
3. **184's gate ran automated for the first time and proved a true negative** — 3,707 ids, 1 dead, inside
   181's drift budget. The fourth-hand-run-in-five-days pattern is retired.
4. **190 found** — EN/KO *key* parity measured clean for the first time (543/543), and the same measurement
   exposed that no check exists on KO *values*; 2 of 543 are untranslated English on 2,201 KO landers.
5. **191 found** — the north-star surface renders 2 of 734 pools per tick, a 367-day full pass; 183 fixed
   selection, not throughput.
6. Guardrail streak 17 → **18** clean prod days, clean in the unfiltered control for a second day.
7. Data layer: pools 741 → **736**; zero out-of-rail totals; zero non-finite KPIs; 171 auto-downgrade
   holding for a third day.
8. North star (0, **23rd** day) and the `src` breakdown (`undefined` = 47, **19th** day) unchanged.
   Prod `page_view` 7d 19 → 11; two new crawler arrivals (`/tokens/slisbnb`, `/chains/linea`).
9. **The backlog is drained of P0/P1 work.** Every row 001-**189** is SHIPPED/DONE/BLOCKED/PARKED (189 by a
   build loop concurrent with this tick), the audit is clean, and both findings this tick are P2. Flagged to
   the human as a genuine inflection — see the report.
10. **A concurrent build loop caused the first ID collision on this backlog.** Findings drafted as 189/190
    were renumbered to **190/191**; 189's independent verification is recorded above. New rule for the next
    tick: read `origin/main`'s max ID immediately before writing rows.
