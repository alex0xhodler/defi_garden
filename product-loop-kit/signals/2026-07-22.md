# Signals snapshot — 2026-07-22 (heartbeat, live `Mixpanel` connector)

Project: defigarden (4042048) · org Equitee (2885044) · via `Mixpanel` connector (uppercase — live this run; lowercase `mixpanel` still needs OAuth). Prod filter = `$current_url contains www.defi.garden`. `Get-Business-Context` = "No business context configured" (unchanged). Queries this run: prod daily line 14d `query_id 2e9d52d8`; 07-21 pages `cd4f46d0`, referrer+utm `7230c391`.

## HEADLINE: 117.x Sharpe surfaces ACTIVATED in prod today (479/745 snapshot pools now carry non-null `apySharpe`, 9 history points ≥ 8 threshold) — exactly as the 07-21 snapshot forecast. North star still 0. Build queue now FULLY drained: 110 SHIPPED (#272) — zero non-terminal READY rows remain; every lever left is human-owned.
North star `waitlist_submitted` = **0** (structurally absent — `Get-Events` = 21 events, identical set to 07-20/07-21; no card-funnel event has ever fired). Pipeline audited SOUND end-to-end on 07-21 (backlog 119) — not re-investigated per the diagnostic-row precedent. 07-21 finalized at 7 sessions, ALL bot-shaped (see below). 07-22 empty so far.

## Lexicon — 21 events (unchanged vs 07-21)
19 product + 2 Mixpanel auto. Still structurally absent: `waitlist_submitted`, `waitlist_opened`, `share_link_created`, `garden_cta`.

## Real prod trend (`$current_url contains www.defi.garden`, daily totals, `query_id 2e9d52d8`)
| date | $session_start | plan_created | search_success | error_occurred | share_link_opened | pool_click | plan_saved |
|------|----|----|----|----|----|----|----|
| 07-14 | 5 | 0 | 0 | 0 | 0 | 0 | 0 |
| 07-15 | 7 | 0 | 0 | 0 | 0 | 0 | 0 |
| 07-16 | 6 | 0 | 0 | 0 | 0 | 0 | 0 |
| 07-17 | 2 | 0 | 0 | 0 | 0 | 0 | 0 |
| 07-18 | 8 | 0 | 0 | 0 | 0 | 0 | 0 |
| 07-19 | 3 | **1** | **2** | 0 | **1** | **1** | **1** |
| 07-20 | 16 | 0 | 0 | 0 | 0 | 0 | 0 |
| 07-21 | 7 | 0 | 0 | 0 | 0 | 0 | 0 |
| 07-22 | 0 so far | 0 | 0 | 0 | 0 | 0 | 0 |

## 07-21 finalized: 7 sessions, same crawler signature as 07-20 (two pulls)
- **7 sessions = 7 unique users** (`cd4f46d0`), one each; **100% `undefined` referrer AND utm_source** (`7230c391`).
- Pages: `/` ×2, `/chains/aptos` ×1, `?token=SMON`, `?chain=Solana&token=AVICI`, `?chain=Base&token=NB`, `?chain=Berachain&token=20%29` — long-tail/garbage params (`token=20)` is a malformed-URL follow, `AVICI`/`NB`/`SMON` are not human queries). Zero engagement events.
- Read: crawler/monitoring sweep continuing at lower volume, NOT distribution. 07-19's single engaged organic session remains the only real user in 14d.

## Sharpe activation — CONFIRMED LIVE (local check on today's committed CI snapshot)
`data/pools-snapshot.json` (regenerated by today's `sitemap-update.yml` run, commit 447cb44d1): 745 pools, 745 with `kpis`, **479 with non-null `apySharpe`**, max `historyPoints` = 9 (crossed the ≥8 gate). → 117.1 (pool-detail Sharpe annotation), 117.2 (analytics Sharpe sort), 117.3 (planner steadier-yield near-tie-break) are all rendering/active in prod as of today — first day of live behavior for the whole 117 line. No isolated funnel metric (traffic-gated like everything else); flagged for the human's next visual spot-check pass (render-path activations, 003-gate advisory class).

## Guardrails — clean
- `error_occurred` (prod): **0 every day 07-14 → 07-22.** 082's window to 2026-07-28 stays clean.
- Analytics-URL SEO lifeline: intact — no code merged since 117.3/#271 except 110/#272 (CI-only `compute-kpis.js` + workflow env; verifier-proved byte-identical no-op while `HISTORY_ENDPOINT` unset) and daily sitemap commits.
- Planner conversion: no real (non-bot) prod sessions on 07-20/07-21 → insufficient data, uncomputable.

## Sample discipline
Real engaged prod sessions in 14d: **one** (07-19). 07-20's 16 + 07-21's 7 are bot-shaped and excluded. **Insufficient data** for any funnel-step, retention, or cohort claim. No ≥30-event claim available. Qualitative only.

## Experiments — nothing to close today
Earliest open window = 005/007/008 → **2026-07-24** (2 days out). Then 013/014/018/019/020 07-25; 041/048/049 07-26; 075 → 07-27; 061/065/068 → 07-26/27; 082 → 07-28; 114/115 → 07-29; 116 → 07-30. 101's 14d window still unstarted (no bloom share origination yet). LEARNINGS.md unchanged. 117.x measurement effectively STARTS today (first day Sharpe is non-null in prod).

## Backlog state — queue fully drained; every remaining lever is human-owned
- **110 SHIPPED** (#272, 2026-07-21, verifier PASS HIGH 5/5) — `compute-kpis.js` D1-read with honest file fallback; no-op until 108. The build loop found the file-fallback shape made it buildable after all. **Data-layer DB leg (109+110) is now 100% code-complete; the ONLY remaining step is human keystone 108** (provision CF D1 + deploy Worker + set `HISTORY_ENDPOINT`/`HISTORY_TOKEN` GitHub secrets).
- **READY sweep: zero non-terminal READY rows remain.** Terminal-state census: PARKED 026/057, BLOCKED 097 (yield.garden decision) / 108 (human keystone) / 118 (GSC noindex — awaiting human GSC data), everything else SHIPPED/DONE/CULLED/SUPERSEDED.
- **No new opportunities filed (honest no-op, 7th consecutive):** no real traffic → no funnel evidence; no guardrail breach; no experiment closable; manufacturing another trust surface would repeat what seven heartbeats have conceded cannot move `waitlist_submitted`. Movement requires the human: post 069 (sole distribution lever) and/or provision 108 (lights up the entire shipped data-layer DB leg).
- No score changes (07-21's bot day is not new evidence).

## Diff vs 2026-07-21
1. **Sharpe surfaces went LIVE** (479/745 pools, 9 history points) — the 117 line's first day of real behavior; measurement window effectively opens today.
2. **110 SHIPPED (#272)** → build queue fully drained (was: one gated-READY row; now: none). All remaining levers human-owned (108, 069, 097, 118).
3. **07-21 finalized at 7 bot-shaped sessions** (was "0 so far") — crawler sweep continuing, not distribution; north-star/funnel state unchanged at 0.
4. Guardrails unchanged clean (error 0 nine straight days; SEO render path untouched by #272's CI-only diff).
5. Three human questions still pending (SUBSCRIPTION_LADDER canonical set · 069 posting go/no-go · yield.garden mirror) — logged 07-14, not re-asked (Standing Decisions unchanged).
