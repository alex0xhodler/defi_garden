# Signals snapshot — 2026-07-27 (heartbeat, live `Mixpanel` connector)

Project: defigarden (4042048) · org Equitee (2885044) · prod filter `$current_url contains www.defi.garden`.
Queries this run: prod daily line 14d `query_id 791fe954`; page_view-by-URL 7d `38e95d05`; pool_view/pool_click by `source` 30d `3a0ab040`; waitlist window 07-13→07-27 `9fe39c5b`; prod page_view by `src` 07-13→07-27 `d649acd9`.

## HEADLINE: **P0 — the `APY_SANITY_LIMIT` trust rail is not applied to the `llms.txt` surface at all.** `https://www.defi.garden/llms.txt` is serving **353,114.2% APY** right now, under the header "Live highest APY opportunities", ranked APY-descending. Verified against **live prod by curl**, not just the checkout. → item **159**.
North star `pool_click{source ∈ garden_cta, protocol_link}` = **0** in 14d (unchanged). Guardrails otherwise clean — `error_occurred` 0 for **14 straight days**. Render-side rails (144/145) independently re-verified holding on live data.

## Real prod trend (daily totals, `791fe954`)
| date | session_start | plan_created | search_success | error_occurred | share_link_opened | pool_click | pool_view | plan_saved |
|------|----|----|----|----|----|----|----|----|
| 07-19 | 6 | 1 | 2 | 0 | 1 | 1 | 3 | 1 |
| 07-20 | 17 | 0 | 0 | 0 | 0 | 0 | 0 | 0 |
| 07-21 | 6 | 0 | 0 | 0 | 0 | 0 | 0 | 0 |
| 07-22 | 4 | 0 | 0 | 0 | 0 | 0 | 0 | 0 |
| 07-23 | 7 | 0 | 1 | 0 | 0 | 0 | 0 | 0 |
| 07-24 | 3 | 0 | 0 | 0 | 0 | 0 | 0 | 0 |
| 07-25 | 4 | 0 | 0 | 0 | 0 | 0 | 0 | 0 |
| 07-26 | **6** (finalized, was "0 so far") | 0 | 0 | 0 | 0 | 0 | **2** | 0 |
| 07-27 | 0 so far | 0 | 0 | 0 | 0 | 0 | 0 | 0 |

**Insufficient data** for every funnel claim (≥30 rule). Real engaged sessions in 14d: still only the 07-19 cluster, plus two pool-detail views on 07-26.

## North-star surface — first-ever `url_direct` pool landing (`3a0ab040`, 30d)
- `pool_view` 30d = **6**: `card_click` **5**, `url_direct` **1**.
- The single `url_direct` is NEW this tick and corresponds to the prod page_view on `?chain=Popular&minTvl=1000000&pool=747c1d2a-c668-4682-b9f9-296708a3dd90` (lido / STETH / Ethereum, TVL $18.2B — a healthy pool, `kpis` complete, nothing broken on it).
- `pool_click` 30d = **1**, `source=undefined` (the pre-123 07-19 event). Zero CTA clicks on either entry path. **n=1 — do not read a conversion claim into this.** Item 019's window closed 07-25 on "url_direct = 0 all window"; that is no longer literally true, but 1 landing is not a measurement.

## Prod page_views by URL, 7d (`38e95d05`, total 28)
`?token=INFINIFIUSDC` 3 · `/tokens` 2 · `/chains` 2 · `?chain=Abstract` 2 · `?chain=Popular&minTvl=1000000` 2 · then 1 each: `?token=USDC`, `?token=SMON`, `?token=AVICI✨`(×2 variants), `?chain=Solana&token=AVICI`, `?token=BUIDL`, `/tokens/buidl`, `?token=BUIDL&lang=ko`, `?chain=Popular&minTvl=1000000&pool=747c1d2a…`, `/`, `/chains/{ethereum,aptos,plasma,linea}`, `?chain=Berachain&token=20)`, `?poolTypes=Yield+Farming&token=20`, `?poolTypes=Lending&token=22OCT2026`.
- **NEW shape worth watching:** the `BUIDL` triple — `?token=BUIDL` → `/tokens/buidl` → `?token=BUIDL&lang=ko`. That is the first prod trace of one agent walking BOTH token surfaces *and* the KO locale. Still crawler-shaped (no `session_start` spike, no downstream events); noting the pattern, not claiming a human.
- The `token=20`, `token=20)`, `token=22OCT2026` rows are the **item 148 class**, unchanged — still ours, still live.

## Guardrails — clean
- `error_occurred`: **0 every day 07-14 → 07-27 (14 straight)**. Last non-zero 07-13 (1). Item 082's recurrence window (to 07-28) is clean with one day to run.
- SEO lifeline: `?token=` / `?chain=` / `?pool=` all render pool cards — `audit-app.js` drove grid-token, grid-chain, pool-detail, dead-pool, grid-loading with zero findings on those surfaces.
- Planner conversion: no planner sessions since 07-19 → uncomputable.

## §2b product audit — `audit-app.js` (16 surfaces, 5 findings, ALL the known 148 class)
Ran clean after `NODE_PATH=/opt/node22/lib/node_modules` (item 149's hardening worked — `status: "OK"`, `playwright.source: "local"`, `surfacesCovered` non-empty, exit code non-zero *because of real findings*, which is the intended 157 behaviour).
- Surfaces (16): grid-token, pool-detail, grid-chain, dead-pool, grid-loading, pool-detail-360, grid-360, pool-detail-dark, pool-detail-ko, static-page, static-page:tokens/{01,17dec2026,20261231,2027,ncloa,aiusd}.
- Prescan: **2,197 pages scanned**, 7 suspects, all `junk-slug`; `broken-number-literal` 0, `absurd-magnitude` 0, `zero-yield-claim` 0.
- Findings: 1 aggregate `static-prescan:junk-slug` (P1) + 4 rendered `junk-slug` P1s (`01`, `17dec2026`, `20261231`, `2027`). **All are item 148, still unmerged.** Item 157 predicted exactly this and said it self-clears when PR #306 merges. **No new product finding from the scanner.**

## Data layer — healthy, and the rails re-verified by hand
Snapshot `generatedAt` 2026-07-27T05:48:59Z, **735 pools**.
- **326** non-null `kpis.apySharpe` (324 yesterday), **0** with |Sharpe| > 50 (range −48.21..40.15).
- `historyPoints` median **14** (13 yesterday) — daily accrual working.
- **0** pools with total APY (`apyBase + apyReward`) > 1000% — max total **39.91%**. `APY_SANITY_LIMIT` holding on the data the product uses.
- Full KPI-tree scan for NaN/Infinity/|v|>1e11: **0 hits**. `kpis.apyMean` max **36.13%** — 144/145's rails holding.
- **Known-and-handled:** raw `apyMean30d` is still **35,447.66%** on `201e5f6e-…` (balancer-v2 WSTETH-AAVE) — the same pool 144/145 addressed. Traced all four consumers in `PoolDetail.js` (`:1376`, `:1404`, `:1452`, `:1498`); **every one gates on `mean30dSane`**, and `compute-kpis.js` correctly nulls that pool's `apyStdev`/`apyMean`/`apySharpe`. Render layer is honest. Not a finding — recorded so the next tick doesn't re-derive it.

### METHOD CORRECTION (binds future ticks — playbook updated)
`data/pools-snapshot.json` **has no `apy` field**. Keys are `apyBase`, `apyReward`, `apyMean30d`. Total APY must be computed as `apyBase + apyReward`. A rail check written as `pools.filter(p => p.apy > 1000)` returns **0 on every pool, always**, and reads as a clean rail. This tick's first data-layer scan made exactly that mistake and had to be redone. Pulling that thread is what surfaced 159.

## NEW FINDING → 159 (P0/READY 9.2): `llms.txt` publishes anomalous APYs with NO trust rail
`generate-llms.js` `pickHighYield()` (`:227-241`) filters on `tvl >= minTvlUsd && apy > 0 && isFinite(apy)` with **`minTvlUsd = 10000`** and **no `APY_SANITY_LIMIT` anywhere in the file** (grepped: zero occurrences of `SANITY`), then sorts **APY-descending**. The surface is therefore, by construction, a leaderboard of the dataset's most anomalous pools — the exact inverse of the trust rail.
- **Live prod, fetched by curl this tick** (`https://www.defi.garden/llms.txt`, verbatim):
  `TL;DR: Live highest APY opportunities (updated daily, TVL ≥ $10k).`
  `- BSC · zeebu · ZBU — 353114.2% APY, $576,877 TVL — https://www.defi.garden/?token=ZBU&chain=BSC`
  `- Base · aerodrome-slipstream · USDC-VFY — 220925.6% APY, $29,382 TVL`
  `- Avalanche · blackhole-clmm · WAVAX-USDC — 144006.1% APY, $58,744 TVL`
- **Scale:** **8** lines >1000% APY in `llms.txt`; **15** in `llms-full.txt`, spread across 6 chain sections (BSC, Base, Avalanche, Polygon, Aptos, Solana). Highest 353,114.16%.
- **Two rails breached at once:** `APY_SANITY_LIMIT` (1000%, `app.js:800` / `planner.js:19`) — not applied; and `DEFAULT_MIN_TVL` ($10M, `app.js:801`) — replaced by a $10k floor, 1000× looser. `zeebu`/`ZBU` is **not in the $10M-floored snapshot at all** — the product itself would never show this pool.
- **It regenerates daily:** `.github/workflows/sitemap-update.yml:110` runs `POOLS_FIXTURE=… node generate-llms.js`, so the surface self-refreshes with whatever the day's worst anomaly is. The committed file is a CI artifact (`chore: update sitemap and LLM files with latest yields`).
- **Why it matters more than its traffic:** `llms.txt` is the AI-discovery surface — the artifact whose entire purpose is telling LLMs and agents what DeFi Garden is and what it claims. NORTH_STAR: *"Trust rails are the moat … honest numbers beat exciting numbers."* This surface currently says the opposite, in our own voice, unprompted.

## NEW FINDING → 160 (P2/READY 5.0): the product audit is blind to every non-HTML surface
`audit-app.js` drives rendered HTML only. `llms.txt`, `llms-full.txt`, `sitemap-*.xml` and `og/*.png` are shipped, publicly-served, generator-produced surfaces that **no automated check reads**. 159 was found by hand, by tracing a schema mismatch — the same way 148 was found by hand a day after the scanner scored "0 findings". Two consecutive P0/P1-class misses in the same structural blind spot is a pattern, not luck. Cheap fix: a text-surface pass (regex the `% APY` figures in `llms*.txt` against `APY_SANITY_LIMIT`, assert the TVL floor claim matches the floor actually used).

## Experiments — SEVEN windows closed today (written to LEARNINGS.md)
Windows ending 07-27 are the items shipped 2026-07-13: **062, 063, 064, 066, 068, 075, 079**.
- Waitlist funnel over 07-13→07-27 (`9fe39c5b`): `waitlist_opened`, `waitlist_email_entered`, `waitlist_submit_attempt`, `waitlist_submitted`, `waitlist_cta_click`, `share_link_created` — **all absent from the result set (zero events, never fired)**. `page_view` 138 total / **48 prod** over the same window.
- **064 (X-spotlight attribution) has a definitive negative:** prod `page_view` broken down by `src` over the full window (`d649acd9`) returns a **single row, `undefined` = 48**. Zero `src=x_spotlight` arrivals ⇒ the human has not posted the 069 packs yet. The attribution leg is built and unexercised, not broken.
- Remaining windows: 082 error-recurrence → 07-28; 088.1 → 07-28; 094/114/115 → 07-29; 116 → 07-30; 124 → 08-06; 125 → 08-07; 045 → 08-09; 148 → 08-09; 149 → 07-29; 013 GSC leg → human ~08-23.

## Diff vs 2026-07-26
1. **P0 159 found** — `llms.txt` trust-rail breach, live on prod, 353,114% APY. Not a regression: this has almost certainly been true since the surface existed; it had simply never been read.
2. Guardrail streak 13 → **14** clean days; 07-26 finalized at 6 sessions (not 0) with 2 pool_views.
3. **First-ever `url_direct` pool_view** (n=1, lido STETH). The north-star surface got a real direct landing and produced 0 clicks — noted, not concluded.
4. `audit-app.js`: 10 → **16 surfaces** (157's prescan + rotation live), 5 findings — all 148, no new class. Scanner behaving exactly as specified.
5. Data layer: Sharpe 324 → 326, historyPoints 13 → **14**, `kpis.apyMean` max 36.13%, zero NaN/Inf. 144/145 verified holding by tracing all four `apyMean30d` consumers.
6. Seven experiment windows closed (062/063/064/066/068/075/079) — all INCONCLUSIVE at n=0, with 064 carrying a clean negative on `src`.
7. **Method correction recorded:** the snapshot has no `apy` field; `p.apy > 1000` is a vacuously-passing rail check. → playbook `derived-number-rails`.
