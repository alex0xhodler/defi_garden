# Signals snapshot — 2026-08-12 (heartbeat, live `Mixpanel` connector)

Project: defigarden (4042048) · org Equitee (2885044) · prod filter `$current_url contains www.defi.garden`.
Queries this run: prod daily 14d `03ad6a00`; `pool_click`/`pool_view` 30d by `source` prod `c11499d3`;
08-11 sessions by `$referrer`×`src` `bfae9170`; 08-11 by `$region`×`$os`×`$browser` `0b0e1fbc`;
referring domains 14d control `11e0c4f3`; URLs 08-11..08-12 control `1c7c1ee0`; waitlist+plan 7d by
`src` `fafd2385`. Live prod probes this tick: `/api/health`, `/api`, `/api/pools`, `/api/pricing`,
`/api/forever-number`, `/mcp` (JSON-RPC initialize + tools/list + tools/call), `/api/mcp`,
`/.well-known/mcp*`, `/openapi.json`, `/llms.txt`.

## HEADLINE: THE HUMAN DEPLOYED THE WORKER. The whole agent pillar is LIVE — and its front door points at a 404.

Yesterday's tick live-probed `/api/health` → Vercel 404. Today, all four pillar legs answer,
verified end-to-end, every response railed:
- **`/api/health` 200**: `poolsAvailable: 7325`, rails `{apySanityLimit: 1000, minTvl: 100000}` with
  the explanations, `generatedAt` fresh (07:16 UTC — minutes before the probe).
- **`/api/pools?limit=1` 200**: railed lido/STETH row, `railsApplied` block present, count 7,325.
- **`/api/forever-number?monthly=20` 200**: `foreverNumber: 8737.17` at `apySource: "blended"`
  2.75% — the TVL-weighted blended rate, live. (A `monthly_cost` mis-probe correctly drew a railed
  400 naming the real param — the error path carries the rails too.)
- **`/mcp` LIVE over JSON-RPC**: `initialize` → `protocolVersion 2025-06-18`, `serverInfo
  defi-garden-mcp 0.1.0`, capabilities `{tools:{}}`; `tools/list` → 4 tools; `tools/call find_pools
  {token: USDC}` → railed maple/USDC pool verbatim from api-core. GET /mcp → 405 (expected; POST-only).
- **x402 correctly DARK**: `/api/pricing` serves the schedule with `availability {enabled: false,
  mode: "test"}` and the honest "no agent is being charged right now" statement; the paid
  `/api/forever-number` serves free. No pricing leaked on.
- **llms.txt** still serves both corrected `$100K` claims (`$10M` ×0) — second day post-254/261.

**The defect, live-probed (item 265, evidence upgraded):** all three byte-identical `.well-known`
MCP discovery cards — advertised in a `Link:` header on EVERY response (`vercel.json:171`,
re-verified live on `/`) — declare `"url": "https://www.defi.garden/api/mcp"` + `"type": "sse"` +
a `prompts` capability. Live: `GET /api/mcp` → **404**, `POST /api/mcp` (initialize) → **404**;
the real server is Streamable-HTTP at `/mcp` and implements tools only. Every agent that trusts
the card fails the install. 265 was filed "must land at or before deploy"; the deploy happened
first. Score 7.8 → **8.9**, now the top actionable item. Related live-probed drift: `openapi.json`
declares `/pools` + `/planner` while the live Worker routes 5 paths (262, BLOCKED on the human);
llms.txt mentions `/api`/`MCP` **zero** times (new item **277**, the deferred 228 advertising leg —
deferral condition "would 404" has expired).

**What this changes for leg (A) instrumentation:** 224's edge log is now recording (every request
this tick made traverses the Worker), 227's API and 228's MCP are countable — but reading
`agent_reads` needs `wrangler` + Cloudflare credentials (edge/DEPLOY.md §6), which no cloud session
has. Instrument: LIVE. Readout: human-gated. Gate 251 first half MET, second half (≥1 row
OBSERVED) unmet-as-observable — left GATED with the note.

## Real prod trend (daily totals, `03ad6a00`)
| date | session_start | error_occurred | pool_view | pool_click | page_view | plan_created |
|------|----|----|----|----|----|----|
| 08-06 | 76 (crawler burst #2) | 0 | 0 | 0 | 23 | 0 |
| 08-07 | 18 (operator sweep) | 0 | 4 | 1 | 11 | 1 |
| 08-08 | 11 | 0 | 0 | 0 | 7 | 0 |
| 08-09 | 10 | 0 | 0 | 1 | 5 | 0 |
| 08-10 | 10 | 0 | 0 | 0 | 7 | 0 |
| **08-11** | **9** | **0** | **0** | **0** | **6** | **0** |
| 08-12 | 0 so far | 0 | 0 | 0 | 0 | 0 |
(Full 14d in query `03ad6a00`; 07-29..08-05 unchanged from yesterday's snapshot.) `error_occurred`
absent from the prod-filtered result set across 14d — the event returned no row at all.
(08-11 finalized at 9 sessions / 6 page_view; yesterday's mid-day read was 0 — time-of-read delta.)

## §2b(i) — classifying 08-11, per `playbooks/traffic-quality-classification.md`
9 sessions, **9 unique users** (`bfae9170`) — one each. Referrers: 9/9 undefined; `src` undefined
throughout. Fingerprint (`0b0e1fbc`): **undefined-region / Mac OS X / Safari 6** (the known crawler
cluster), Bavaria/Linux/Chrome 1, Sichuan/Windows/Chrome 1, Oregon/Mac/Chrome 1. URLs (`1c7c1ee0`):
long-tail `?token=` strings (`?token=15`, `?token=ORGMC`, `?token=GRRR&poolTypes=…`, etc.) + bare
`/` ×3. **No `/?pool=` fuzz replay — first absence in ≥7 days** (queue drained, consistent with the
rewritten playbook rule). Criteria: no-acquisition HIT (9/9), one-hit HIT (9=9), zero-engagement
HIT (pool_view/pool_click/plan_created all 0), uniform-fingerprint HIT (6/9). 4 hits → crawler.
No event carries `insider: true` (252's flag still unexercised — human hasn't visited with
`?insider=1`). **Decomposition: 9 sessions — 9 crawler-shaped, 0 insider, 0 real.**

## North star, both legs
- **Leg (B) pool-detail CTA clicks: 0 real. 35th straight day.** 30d prod by `source` (`c11499d3`):
  `pool_click` = 3 (`garden_cta` 1 operator 08-07; `undefined` 1 pre-123 legacy, ages out ~08-18;
  `yield_calculator` 1 crawler 08-09); `pool_view` = 11 (`card_click` 8, `url_direct` 3).
  Unchanged. Gate ≥30 real: **0 real.**
- **Leg (A): 0 AI-assistant-referred sessions in 14d** (`11e0c4f3`, control, 267 sessions:
  `$direct` 255, bing.com 5, ecosia 2, t.co/tinker/recipebridge/wink 1 each, and one NEW:
  **www.0xhodler.nl ×1** — the human's own domain, insider-shaped, not an AI referrer). Day 8
  since adoption. **BUT the instrument for the other three terms went LIVE today** (see headline):
  API calls + MCP invocations + crawler reads are now being recorded in D1; first read pending
  human credentials. Mixpanel-only blindness to non-JS reads (212's gap) ends at the first
  `wrangler d1` read.

## Guardrail — 30 clean prod days
`error_occurred` absent from the 14d prod-filtered result set (`03ad6a00`) → **30th** straight
clean prod day. Exercise count per LEARNINGS 082: **n=16** carried (08-11 added 6 analytics-mode
`page_view` loads but they are crawler-classified; the count of REAL exercised loads stays 16,
<30 — not evidence the app is error-free for strangers).

## §2c Orphan-PR check (`pr-orphan-detector.js`, live data)
`list_pull_requests(state=open)` → **1 open: #424** (`claude/loop-236` phase 1, opened 08-10
19:22). Detector verdict: **ORPHAN** (row 236's status leads with no terminal marker) — genuinely
awaiting a verifier verdict, now ~36h old; crosses the efficiency-reset 48h line tomorrow ~19:22
UTC. `Orphan PRs: 1 (#424)`.
**The stale pile is GONE — the human closed #419, #429, #434, #436 unmerged at 2026-08-11T20:07**
(GitHub timestamps). Yesterday's disposition question 2 is answered: #429's verified leg-B code
was discarded (consistent with the operator's own cost measurement — the fix moved a hairline
46-497px on every normal card to fix a collision 0 of 7,334 pools reach); 266's two complementary
PARKED PRs both closed — row 266 status updated (fresh build from spec, read the closed branches
first). Leg B next id: `computeNextId(mainMax=276, openPrBranchIds=[236])` = **277** — allocated
this tick (llms.txt advertising item).

## §2d vitals (window 08-06..08-12, build LOG lines + live PR data)
```
vitals | ships/wk: 23 | tokens/ship: ? | attempts: 2 | open PRs: 1 (oldest: 2) | product:process: 20:3
```
`tokens/ship` = `?`: only 6 of 23 ship lines record token counts (range 60k..~1.1M) — median not
computable; named per the vitals rule, not guessed. No breach: parked PRs 0 (≤2), process share
13% (≤25%), tokens/ship unknown (the two known ~1M runs — 260, 263 — are flagged in their own LOG
lines). No triage item filed.

## §2b(ii) PRODUCT AUDIT — 84 surfaces, 3 findings (one CLOSED), no new classes
`status: "OK"`, `generatedAt` 2026-08-12T07:22:42Z, playwright local 1.61.1, pools live fetch
(15,609 → union 7,346 railed candidates). `npm install` ran before both instruments (playbook
rule 0). Exit code 1 (findings present).
| finding | severity | 08-11 | 08-12 |
|---|---|---|---|
| `pool-prescan:apy-rail-breach` | P0 | 17 of 7,332 | 22 of 7,346 |
| `pool-prescan:mean30d-rail-breach` | P0 | 30 of 7,332 | 32 of 7,346 |
| `i18n:en-ko-parity` | P1 | 2 of 554 | **0 of 554 — CLOSED by item 248 (#440, merged 05:27 UTC today)**, allowlist 24→26 |
| `planner-768 :: responsive` (`.gp-chip` zero-match) | P2 | present | present (item **235**, READY, unshipped) |
- Rail-breach churn 17→22 / 30→32 on a population 7,332→7,346: upstream data churn, no new claim.
- NORTH_STAR `Test:` legs re-run, unpiped, this tick: `test_planner.js` / `test_protocol_parsing.js`
  / `test_qualifier_fix.js` / `test_dead_pool.js` all exit 0. Scope: those four files + the audit's
  implemented classes (number-sanity, dead-end, loading, page errors, i18n incl. raw-key, dead-CTA,
  responsive/dark, occlusion) over 84 surfaces + prescans; the 150-file `test:serial` lane not run.
- Rotation: pools cycle 0 seen 413/7,343, rendered 32/32; static tokens 49/2,066 + KO twins;
  deep-linked-live **0** (fifth consecutive zero on that leg — 3,670 of 3,671 deep-linked ids
  confirmed live, 0 net-new); `textSurfaces` scanned 2 (near-zero information, unchanged).

## §2a predictions
**Carried from 2026-08-06 — the 244 race reaching prod. Status: TRACKING, n=16 real exercises
(unchanged — 08-11 loads all crawler-classified), no witness fired.** Witnesses now include a NEW
instrument: 224's D1 log is recording as of today — the error-page fetch pattern would be visible
to it once the human reads the table. No claim; n<30.

**New prediction (weakest form, all witnesses enumerated): agent-surface consumption is now
recordable, and SOME row will exist in `agent_reads` when it is first read.** Outcome-level:
≥1 row. Witnesses, ALL enumerated: (a) the human's `wrangler d1` read (DEPLOY.md §6) — the only
one currently actionable; (b) any future in-repo read path; (c) indirectly, a D1-quota/size
signal. This tick's own probes (~15 requests across /llms.txt, /api/*, /mcp) traverse the Worker,
so the prediction is near-tautological BY DESIGN — it exists to verify the LOGGING path, not
demand: if the first read shows zero rows, the logger (fail-open by design) is silently broken,
and that is the finding. Operator/curl reads must be classified as such, never counted toward any
gate (crawler-rule extension).

## Gate re-check (§4)
- **251** `GATED(224 deployed AND ≥1 row observed in agent_reads)`: first half **MET** (live
  probes), second half unobservable in-session (wrangler credentials, human-owned). Stays GATED,
  note added to the row. Pressure: the log grows unpruned from today.
- **228 GATED window (≥30 real MCP invocations)**: the "cannot open until the human deploys"
  precondition CLEARED today; accrual can start; observed count still 0-by-absence-of-readout.
- **234 GATED (human flips X402_ENABLED)**: unmet — live-probed `enabled: false, mode: test`.
- **≥30-real pool-detail gate: 0 real, unmet** (35th day). **≥30-real waitlist gate: 0 real**
  (`waitlist_opened` 7d = 7: 5 `seo_tokens_hub` + 2 undefined, all crawler-days;
  `waitlist_email_entered`/`waitlist_submitted` absent; `plan_created` 7d = 1, `plan_saved` = 2,
  both `src=pool`, operator 08-07). Unchanged.
- 277 filed GATED on the traffic claim (≥30 real agent-classified /api-or-/mcp reads).

## Experiments — no window closed, none can close
117.1/.2/.3 SHIPPED (measuring) under the 60d backstop (~09-16..18). Human-owned GSC reads:
013 ~08-23, 211 ~09-13.

## BACKLOG table integrity — same predicate, re-measured
**24** id-rows do not split into 8 cells on unescaped pipes (was 23; +1 = 248's shipped-status
prose). Growth rate holding at ~1 per shipped item. Not ticketed: the process-cap (272 rule 3)
holds — 271 is the one READY process item, and this observation does not beat the best READY
product item by >1.0; carried as a named trend, third tick running.

## Diff vs 2026-08-11
1. **The Worker is DEPLOYED** — all four agent-pillar legs live and railed (headline). Leg (A)'s
   instrument gap is now a READOUT gap only (wrangler credentials).
2. **265's defect went from latent to live**: discovery cards advertise a 404 (`/api/mcp`, both
   verbs probed) on every response. Score 7.8→8.9, top actionable item. 277 filed (llms.txt
   advertises the agent surface 0 times). 262's wrong contract now describes a live API.
3. **The human swept the PR pile 08-11T20:07** — #419/#429/#434/#436 closed unmerged; open PRs
   4→1 (#424, awaiting verdict, ~36h). Yesterday's questions 1 and 2 both answered by action.
4. **Audit finding CLOSED**: i18n:en-ko-parity 2→0 of 554 (248 merged this morning); findings
   4→3, no new classes; rail-breach churn flat.
5. 08-11: 9 sessions — 9 crawler-shaped, 0 insider, 0 real; zero engagement; first `/?pool=`
   fuzz-replay-free day in ≥7.
6. Guardrail streak 29→**30** (real-exercise n=16 unchanged, <30).
7. Leg (B) 0 real, day 35. Leg (A) Mixpanel-read 0, day 8; new referrer www.0xhodler.nl ×1
   (human's own domain).
8. Rows synced: 274 IN_REVIEW→SHIPPED (#439); 275 spec cell fixed (specs/275.md); 266 both parked
   PRs closed-unmerged noted; 251/262 pressure notes; specs written for 265/266/277.
9. Vitals line first printed under the 272 format: 23 ships/wk, median 2 attempts, no breach.
10. BACKLOG malformed rows 23→24 — trend carried, third tick, still under the process-cap bar.
