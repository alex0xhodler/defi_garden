# Signals snapshot — 2026-07-18 (heartbeat, live `Mixpanel` connector)

Project: defigarden (4042048) · org Equitee (2885044) · via `Mixpanel` connector (uppercase — live this run; lowercase `mixpanel` still needs OAuth). Prod filter = `$current_url contains www.defi.garden`. `Get-Business-Context` returns "No business context configured" (unchanged) — read-only insights queries used directly (`query_id fd3cd2a8` prod daily line, `99c6aad2` all-host totals).

## HEADLINE: near-identical no-op — one real triage move: the build loop is unfed again, so 117.1 is spec'd
North star `waitlist_submitted` = **0** (structurally absent from the 19-event lexicon). Prod traffic flat (07-17 finalized at **2** sessions). Guardrails clean. The only material delta vs 07-17: **117's compute leg SHIPPED (#267, 07-17)** — so the one buildable READY item 07-17 celebrated is now consumed, and the build loop is unfed again (110 stays human-gated behind 108). Triage action: wrote `specs/117.1.md` (pool-detail Sharpe annotation, the next promotion candidate 117's own notes name) + added BACKLOG row 117.1 — re-feeds the build loop with a grounded, autonomously-buildable surface. No prod scores changed (no new prod evidence).

## Lexicon — 19 events (unchanged)
`Get-Events` = **19** = 17 product events + 2 Mixpanel auto (`$session_start`/`$session_end`). Still structurally absent: `waitlist_submitted`, `waitlist_opened`, `share_link_created`, `pool_click`, `garden_cta`.
→ **North star `waitlist_submitted` = 0 from any host, ever.** No card-funnel event has ever fired (distribution-gated; plumbing verified sound 07-13).

## Real prod trend (`$current_url contains www.defi.garden`, daily total, `query_id fd3cd2a8`)
| date | session_start | plan_created | search_success | error_occurred | share_link_opened |
|------|---------------|--------------|----------------|----------------|-------------------|
| 07-11 | 10 | 0 | 0 | 0 | 0 |
| 07-12 | 16 | 0 | 1 | 0 | 0 |
| 07-13 | 11 | 0 | 0 | 1 | 0 |
| 07-14 | 5 | 0 | 0 | 0 | 0 |
| 07-15 | 5 | 0 | 0 | 0 | 0 |
| 07-16 | 7 | 0 | 0 | 0 | 0 |
| 07-17 | **2** | 0 | 0 | 0 | 0 |
| 07-18 | **0 so far** | 0 | 0 | 0 | 0 |

→ Flat ~2–16/day trickle. **07-17 finalized at 2 sessions** (yesterday's pull read "0 so far") — the low end of the range but within trickle noise, not a signal at n=2. `plan_created` prod = **0 since 07-10**. `search_success` prod = 1 total (07-12). **No prod `share_link_opened` ever** (share metric absent from the prod-filtered result → 0). 07-18 has no prod events yet — acting on an empty partial day breaks the minimum-sample rule.

## Contamination check (`query_id 99c6aad2`, all-host, last 10 days)
All-host totals: session_start **481**, plan_created 17, share_link_opened 13, error_occurred 4. Prod-filtered over the same window: session_start ~66, plan_created 2 (07-09/07-10), error_occurred 1 (07-13), share_link_opened 0. The all-host share/plan/session counts are **non-prod** (dev/preview/bot hosts), fully isolated by the `$current_url` filter; prod stays flat. Not a prod signal, not actionable.

## Guardrails — clean
- `error_occurred` (prod): **0 on 07-14/15/16/17, 0 so far 07-18.** The 07-13 `ReferenceError: translations is not defined` (082, shipped 07-14) has **not recurred** — window to 2026-07-28 clean so far.
- Analytics-URL SEO lifeline: `?token=`/`?chain=`/`?pool=` render path intact (no render-path merge disturbed it; recent #262 `/plan` clean-URL fix + #263 vault-ticker token search both landed on main, guardrails green).
- Planner conversion: uncomputable honestly at this sample (0 prod `plan_created` since 07-10) → insufficient data, qualitative only.

## Sample discipline
Real-prod n: ~66 sessions over 07-09→17 (one active week of ~2–16/day trickle), `plan_created` 0/wk since 07-10, everything downstream 0. **Insufficient data** for any funnel-step rate or retention/cohort claim. Qualitative only. No ≥30-event claim available.

## Experiments — nothing to close today
Earliest open window = 005/007/008 → **2026-07-24** (6 days out). 075 → 07-27; 061/065/068 → 07-26/27; 082 → 07-28; 116 (report-mode share) → 07-30; 101 → 14d from first real share (no prod shares yet); 114/115 → 07-29. No window ends on 07-18. LEARNINGS.md unchanged.

## Backlog state — build loop RE-FED via 117.1 (change vs 07-17)
- **117 compute leg SHIPPED** (#267, 07-17; verifier PASS HIGH 4/4). `compute-kpis.js` now emits `apyMean`+`apySharpe` (rate-stability Sharpe, null for <8 pts or sd=0). `apySharpe` stays null until a pool accrues ≥8 daily history points (~07-22) — the field is live, values populate as points accrue. **Surfaces were explicitly deferred** ("one at a time, pool-detail first").
- **NEW: 117.1 spec'd + backlogged** (score 6.8, HIGH, READY) — pool-detail Sharpe annotation, the next promotion candidate 117's notes name. Extends 088.1's shipped rate-track-record render block in `PoolDetail.js` (~L1316-1360, surfaces `kpis.apyStdev`+`historyPoints`); adds an `apySharpe` line with the mandated principal-risk caveat, EN+KO, hidden when null (graceful like `rateTrackRecordNew`). Autonomously buildable NOW: value exists (compute leg shipped), renders dormant until data accrues ~07-22 then lights up — same "land now, improves as data accrues" pattern the compute leg used. No credentials, no NEVER-list surface.
- **110 (read-from-DB, MEDIUM 5.5)** stays "READY (after 109)" but NOT actually buildable — repoints `compute-kpis.js` at `${HISTORY_ENDPOINT}` which does not exist until 108's CF-D1 provisioning + 109's deploy land. Human-gated.
- **109 SHIPPED code** (#266); **DEPLOY pending human 108** (`wrangler login`/`d1 create`/`deploy`/`secret` — NEVER-list credentials). KPIs keep running on the file-history until then.
- Distribution (069 spotlight packs ×3, ready + one-tap) remains **unposted** — human-owned, and still the sole north-star lever. It has not moved.
- No BACKLOG scores changed on existing rows (a finalized-2 + empty partial day is not new signal).

## Diff vs 2026-07-17 (three pulls)
1. 07-17 finalized 0-so-far → **2** sessions; 07-18 empty so far. Every other prod number identical.
2. Lexicon 19, north star 0 — unchanged.
3. Guardrail error 0, 082 holding (window to 07-28) — unchanged.
4. **Backlog: 117 compute leg consumed the one buildable item → build loop RE-FED by spec'ing 117.1** (pool-detail Sharpe surface). 109 SHIPPED code (deploy human-gated via 108), 110 still human-gated. No existing-row score changes.
5. All-host session_start noise essentially flat (479→481) — dev/preview/bot, fully filtered out of the prod line.
6. Three human questions still pending (SUBSCRIPTION_LADDER canonical set · 069 posting go/no-go · yield.garden mirror decision) — logged 07-14, not re-asked (Standing Decisions unchanged).
