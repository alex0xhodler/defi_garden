# Signals snapshot — 2026-07-26 (heartbeat, live `Mixpanel` connector)

Project: defigarden (4042048) · org Equitee (2885044) · prod filter `$current_url contains www.defi.garden`.
Queries this run: prod daily line 14d `query_id 2d4f9359`; page_view-by-URL 7d `becb8e74`; waitlist-funnel window 07-12→07-26 `8fa2615d`.

## HEADLINE: north star still **0** (expected-zero, ~0 traffic); guardrails clean 13th straight day; data layer healthy and 144/145's rails holding; `audit-app.js` ran clean (10 surfaces, 0 findings) — but a NEW finding came from outside its check set: the token-page generator mints **nonsense SEO pages from Pendle expiry-date fragments** (148).
North star `pool_click{source ∈ garden_cta, protocol_link}` = **0** in 14d. Last `pool_click` of any kind: 07-19 (the pre-123 one carrying `source=undefined`). Denominator `pool_view` 14d = 4 (07-18 ×1, 07-19 ×3). CTR uncomputable.

## Real prod trend (daily totals, `2d4f9359`)
| date | session_start | plan_created | search_success | error_occurred | share_link_opened | pool_click | pool_view | plan_saved |
|------|----|----|----|----|----|----|----|----|
| 07-19 | 6 | 1 | 2 | 0 | 1 | 1 | 3 | 1 |
| 07-20 | 17 | 0 | 0 | 0 | 0 | 0 | 0 | 0 |
| 07-21 | 6 | 0 | 0 | 0 | 0 | 0 | 0 | 0 |
| 07-22 | 4 | 0 | 0 | 0 | 0 | 0 | 0 | 0 |
| 07-23 | 7 | 0 | 1 | 0 | 0 | 0 | 0 | 0 |
| 07-24 | 3 | 0 | 0 | 0 | 0 | 0 | 0 | 0 |
| 07-25 | **4** (finalized, was "0 so far") | 0 | 0 | 0 | 0 | 0 | 0 | 0 |
| 07-26 | 0 so far | 0 | 0 | 0 | 0 | 0 | 0 | 0 |

Real engaged sessions in 14d: still the single 07-19 cluster. **Insufficient data** for every funnel claim (≥30 rule).

## Prod page_views by URL, 7d (`becb8e74`, total 35)
`/` 4 · `?token=INFINIFIUSDC` 4 · `?token=BTC&minTvl=10000000` 2 · `/tokens` 2 · `/chains` 2 · `?app=1` 2 · `?chain=Abstract` 2 · `?chain=Popular&minTvl=1000000` 2 · then 1 each: `?token=BITCOIN`, `?token=USDC`, `?token=SMON`, `?token=AVICI✨`(×2 variants), `?chain=Solana&token=AVICI`, `?chain=Berachain&token=20)`, `?poolTypes=Lending&token=22OCT2026`, `?poolTypes=Yield+Farming&token=20`, `?token=BTC…&pool=3dd2c646…`, `/tokens/infinifiusdc`, `/chains/ethereum`, `/chains/aptos`, `/chains/plasma`, `/chains/linea`.
Composition unchanged in kind from prior ticks: INFINIFIUSDC human-test residue + AVICI✨/crawler traffic + static-surface crawl. Static SEO surface now at 7 page_views/7d (was ~9/14d yesterday) — trickle continuing, still crawl-shaped, not organic-human.
**The `token=22OCT2026` / `token=20` / `token=20)` rows are NOT crawler-invented** — traced to our own generated surface, see finding 148.

## Guardrails — clean
- `error_occurred`: 0 every day 07-14 → 07-26 (**13 straight**). Last non-zero: 07-13 (1). 082 recurrence window (to 07-28) clean.
- SEO lifeline: `?token=` / `?pool=` / `?chain=` render pool cards — `audit-app.js` drove grid-token, grid-chain, pool-detail, dead-pool, grid-loading and found 0 findings.
- Planner conversion: no planner sessions since 07-19 → uncomputable.

## §2b product audit — `audit-app.js` (item 142, first heartbeat-driven run)
Surfaces covered (10): grid-token, pool-detail, grid-chain, dead-pool, grid-loading, pool-detail-360, grid-360, pool-detail-dark, pool-detail-ko, static-page. **Findings: 0 total, 0 blocking.** Yesterday's shipped fixes (140–147) all hold.
- **Runner note (trap, logged to the playbook):** a fresh clone has no `node_modules`, so `node audit-app.js` dies with `MODULE_NOT_FOUND: playwright` and writes NO findings file. The first run this tick did exactly that. Re-run with `NODE_PATH=/opt/node22/lib/node_modules` (playwright 1.56.1 is installed globally) or `npm install` first. A crashed run is not a clean run — verify `surfacesCovered` is non-empty before recording "audit clean". → ticketed 149.

## Data layer — healthy (snapshot `generatedAt` 2026-07-26T07:00:43Z, 738 pools)
- **324** non-null `kpis.apySharpe` (316 yesterday), **0** with |Sharpe| > 50 (range −49.5..42.0), 414 flat pools honestly nulled by 122's stdev floor.
- `historyPoints` median **13** (12 yesterday) — daily accrual working.
- **0** pools with APY > 1000% (`APY_SANITY_LIMIT` holding). **`apyMean` max = 36.68%** — 144's rail gate holding (pre-144 this field hit 36,452%).
- Full KPI-tree scan for NaN/Infinity/|v|>1e11: **0 hits**. 122/144/145 all verified holding.

## NEW FINDING → 148 (P2/READY 5.5): the token-page generator mints nonsense pages from Pendle expiry fragments
`generate-token-pages.js:87` `tokenSymbols()` splits `pool.symbol` on `/[-_\/\s]/`, and `TOKEN_REGEX` (line 71, `^[A-Z0-9][A-Z0-9.\-_]{1,14}$`) accepts pure-digit and date-shaped strings. So Pendle principal tokens named `PT-<ASSET>-<EXPIRY>` contribute their **expiry date** as a "token".
- **Live snapshot today yields 8 such tokens**: `22OCT2026` (from `PT-SUSDE-22OCT2026`), `10DEC2026`, `18JUN2026`, `16SEP26`, `8OCT2026`, `17DEC2026`, `24SEP2026`, `27AUG2026`. The set churns daily as expiries roll in/out of range.
- **7 are live on the committed surface right now**, each with a sitemap entry: `/tokens/8oct2026`, `/tokens/17dec2026`, `/tokens/2027`, `/tokens/20261231`, `/tokens/00`, `/tokens/01`, `/tokens/67` (verified in `sitemap-token-pages.xml`).
- Each junk slug ships a **full page in EN and KO** (`ko/tokens/8oct2026.html` confirmed present) **plus an OG image** (`og/tokens/8oct2026.png`, `2027.png`) and gets submitted to **IndexNow**.
- Rendered content, verbatim from `tokens/8oct2026.html`: `<title>8OCT2026 DeFi Yields — Live Pools by TVL | DeFi Garden 🌱</title>`, `<h1>8OCT2026 DeFi Yields</h1>`, description *"4 live 8OCT2026 pools above the $100K TVL floor, up to 0.20% APY, across 1 chain."*
- **Prod crawl evidence**: `?poolTypes=Lending&token=22OCT2026` recorded a real prod `page_view` in the last 7d — crawlers are already walking this class.
- Scale is small and honest: ~7–16 of 2,089 token pages (<1%). This is a quality-signal item, not a traffic item — same class as 013 (sitemap quality threshold, SHIPPED), which gated on pool count/TVL but never on "is this string actually a token".

## NEW FINDING → 149 (P3/READY 4.0): `audit-app.js` is dead in a fresh clone
See the runner note above. The pre-traffic mandate's primary job runs through this script; in the cloud-routine environment it crashes on first invocation. Small hardening (resolve playwright with a global fallback, or fail loudly with a non-zero exit + an explicit "AUDIT DID NOT RUN" marker).

## Experiments — FOUR windows closed today (written to LEARNINGS.md)
Windows ending 07-26 are the items shipped 2026-07-12: **041, 048, 049, 061**.
- Waitlist funnel over the full window 07-12→07-26 (`8fa2615d`): `waitlist_opened`, `waitlist_email_entered`, `waitlist_submit_attempt`, `waitlist_submitted`, `share_link_created` — **all absent from the result set (zero events, never fired)**. `page_view` = 56 prod events over the same window.
- 041/048/049 build legs verified functional at ship; traffic legs sit at 7 static page_views/7d vs the ≥30 rule.
- Remaining windows: 062–075 cluster → 07-27; 082 error-recurrence → 07-28; 088.1 → 07-28; 094/114/115 → 07-29; 116 → 07-30; 124 → 08-06; 125 → 08-07; 045 → 08-09; 013 GSC leg → human ~08-23.

## Diff vs 2026-07-25
1. Guardrail streak 12 → **13** clean days; 07-25 finalized at 4 sessions; 07-26 at 0 so far.
2. First heartbeat-driven `audit-app.js` run: **10 surfaces, 0 findings** — 140–147 all hold. The scanner also exposed its own fresh-clone fragility (→ 149).
3. **New product finding 148** — junk token pages from Pendle expiry fragments; found by tracing three odd prod URLs back to our own generator, a provenance check the scanner does not do.
4. Data layer improved: Sharpe 316 → 324 non-null, historyPoints 12 → 13, `apyMean` max 36.68% (144 holding).
5. Four experiment windows closed (041/048/049/061) — all traffic legs INCONCLUSIVE at n=0.
