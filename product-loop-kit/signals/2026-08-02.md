# Signals snapshot — 2026-08-02 (heartbeat, live `Mixpanel` connector)

Project: defigarden (4042048) · org Equitee (2885044) · prod filter `$current_url contains www.defi.garden`.
Queries this run: prod daily 14d `9e9157cf`; unfiltered control 14d `5d150624`; `pool_click` by `source`
30d `ced0c102`; prod funnel events 30d `d2fc7b71`; prod page_view-by-URL 7d `1636e90e`; prod page_view by
`src` 14d `1b453fe8`.

## HEADLINE: **the first real SEO-lander → pool-detail journey in the loop's history — and it landed in the 88.6% of the north-star surface that nothing in this repo has ever rendered, measured, or enriched.**
Third consecutive zero-blocking audit tick, and the widest one yet: **82 surfaces** (60 yesterday),
prescan **4,304 pages** (2,186 yesterday) — item 197 landed, the `/ko/` estate is in the population, and
`static-page:ko/tokens/*` appears in `surfacesCovered` for the first time. Every gate green.
**Then the population question, one level past 197's:** 197 asked whether the *static* population was
complete. Nobody asked whether the **pool** population was. `data/pools-snapshot.json` is the
`$10M`-railed 736-pool set, and it is the sole source for (a) the audit's pool-detail rotation, (b) item
105's kpi backfill for SEO arrivals, and (c) the history series `compute-kpis.js` derives. The estate
emits **3,669 distinct `?pool=` deep links**; **420 (11.4%)** are in that snapshot. **3,249 (88.6%) are
not.**

North star `pool_click{source ∈ garden_cta, protocol_link}` = **0** in 14d (**25th** straight day).
Guardrails clean — prod `error_occurred` **0 for 20 straight days**, and **0 in the unfiltered control too**.

## Real prod trend (daily totals, `9e9157cf`)
| date | session_start | error_occurred | pool_view | pool_click |
|------|----|----|----|----|
| 07-19 | 6 | 0 | 3 | 1 |
| 07-20 | 17 | 0 | 0 | 0 |
| 07-21 | 6 | 0 | 0 | 0 |
| 07-22 | 4 | 0 | 0 | 0 |
| 07-23 | 7 | 0 | 0 | 0 |
| 07-24 | 3 | 0 | 0 | 0 |
| 07-25 | 4 | 0 | 0 | 0 |
| 07-26 | 6 | 0 | 2 | 0 |
| 07-27 | 3 | 0 | 0 | 0 |
| 07-28 | 8 | 0 | 0 | 0 |
| 07-29 | 5 | 0 | 0 | 0 |
| 07-30 | 6 | 0 | 0 | 0 |
| 07-31 | 4 | 0 | 0 | 0 |
| 08-01 | **9** (finalized; was "0 so far" at yesterday's read) | 0 | **1** | 0 |
| 08-02 | 0 so far | 0 | 0 | 0 |

`error_occurred` is **absent from the prod result set** across the whole 14d window — zero, not low.
**Control (`5d150624`):** the unfiltered run also returns `error_occurred` **absent**, fourth consecutive
day. The guardrail claim holds with AND without the prod filter. Keep the filter anyway — playbook
`product-audit.md` class 13.
Prod session totals 14d: **88** (unfiltered **94** — the 6-session delta is localhost/preview).
**Insufficient data** for every funnel claim (≥30 rule).

## North-star surface (`ced0c102`, 30d)
- `pool_click` 30d = **1**, `source=undefined` (the pre-123 07-19 event). **Zero CTA clicks on either entry
  path, ever.** Unchanged for an 8th day.
- Prod funnel events 30d (`d2fc7b71`): `plan_saved` **5** · `search_success` **4** · `plan_created` **3** ·
  `share_link_opened` **1** · `share_link_created` **0** · `tend_reminder_added` **0** ·
  `waitlist_submitted` **0**. Identical for a **third** consecutive day — 72h with no new funnel event.

## Prod page_views by URL, 7d (`1636e90e`, total 12) — **the set MOVED for the first time in 3 days**
One event each. Versus yesterday's byte-identical 11: `/chains/linea` aged out; **two new arrivals**:
`/tokens/jitosol` and **`/?pool=fdcccd6a-a491-4c30-8024-5677bd3f0993`**.
- **URL-provenance check (playbook class 9): clean.** No slug pure-numeric or date-shaped. `jitosol` is a
  real token (Jito staked SOL); the `?pool=` value is a UUID. The 148 class stays closed.
- **The two new URLs are one journey, and it is the journey the north star exists to measure.**
  `tokens/jitosol.html` emits 8 `?pool=` links and `fdcccd6a-…` is one of them (grepped: the id appears in
  exactly 2 files repo-wide, `tokens/jitosol.html` and `ko/tokens/jitosol.html`). So: **SEO lander →
  pool-detail → 0 CTA clicks.** n=1, so **no conversion claim is made** (≥30 rule) — its value is as a
  *repro target*, and it pointed straight at the finding below.
- The pool, resolved against a live DefiLlama fetch (15,822 pools) this tick: `kamino-liquidity`,
  Solana, `SOL-JITOSOL`, **TVL $1,500,009**, APY 0.757%, `apyMean30d` 0.694, `ilRisk: yes`. Live and
  healthy — **and absent from `data/pools-snapshot.json`**, because $1.5M is below the $10M rail.

## Distribution check (`1b453fe8`, the cheapest true test per LEARNINGS 2026-07-27)
Prod `page_view` by `src` over 14d: **one row, `undefined` = 47**. **21st day** — but this is the LAST tick
on which that reading means what it used to.
- **The instrument was broken and the fix shipped inside the last 24h.** 202 (merged 08-01) taught
  `captureAcquisition()` to capture `src`; 203 (merged 08-02, today) tagged the estate's `?pool=` deep
  links; 204 (merged 08-02) tagged the remaining 4,705 `?token=`/`?chain=` app links. Verified in-repo this
  tick: `analytics.js:52` now lists `'src'`, `analytics.js:37` calls `captureAcquisition()` at init (before
  any `pushState`, which matters — `app.js:941-957`'s `updateUrl()` rebuilds the URL from a fixed
  allowlist that contains neither `src` nor `pool`, so a late read would capture nothing), and
  `tokens/jitosol.html`'s pool links now carry `&src=seo_token`.
- **The 08-01 arrival is the proof the old reading was an artifact, not a fact**: the user hit
  `?pool=fdcccd6a-…` with **no `src`**, because 203 had not shipped yet when they clicked. There was no
  tag to drop.
- **FALSIFIABLE PREDICTION, recorded so the next tick can check it rather than re-derive it:** with SEO
  arrivals continuing at the current rate, `page_view` by `src` should show a non-`undefined` row
  (`seo_token` or `seo_chain`) **by ~2026-08-05**. If it still reads `undefined`-only with ≥5 static-page
  arrivals in the window, 202/203/204 did not work and that is a P1 regression ticket, not another
  "distribution is untestable" line.

## §2b product audit — `audit-app.js` (82 surfaces, 1 finding, **0 blocking**)
`node audit-app.js` → `status: "OK"`, `generatedAt` **2026-08-02T07:10:02.642Z** (this tick),
`surfacesCovered` non-empty (**82**) — both staleness gates from the playbook checked before claiming clean.
Playwright resolved from a fresh `npm ci`; measured at `c72c90cf6`, the current `origin/main` tip.
- **197 landed and is visible in the data, not just the changelog:** `prescan.scannedByFamily` =
  `{tokens: 2067, chains: 85, koTokens: 2067, koChains: 85}` = **4,304** (was 2,186), and
  `surfacesCovered` now contains 6 `/ko/` **path-prefixed** static surfaces (`ko/tokens/{nos,syrup,altt,
  flower}`, `ko/chains/{xlayer,kava}`) — checked by path prefix, not by label, exactly as
  `product-audit.md` instructs.
- **198 landed:** `i18n` block reports `scanned 543, suspectCount 0, allowlistSize 24`.
- Static prescan clean for a **fourth** consecutive day: `suspectCount 0` across all six signals.
- `poolLinkLiveness`: **3,669 ids, 0 dead, 0 pages affected**, allowance 36.69 → `ok: true`.
- `poolPrescan`: **736** pools, 1 `mean30d-rail-breach` (`201e5f6e`), **auto-downgraded P1 → P2 by 171's
  reconciliation** for the fifth consecutive day. Working as specced.
- **Zero P0/P1 findings, third consecutive tick.**

## NEW FINDING → 206 (P1/READY 8.0): the audit's pool rotation can never reach 88.6% of the deep-linked surface
`audit-app.js` builds its pool-detail rotation from `data/pools-snapshot.json` (`:610`, `:1556`); this
tick's own log line reads `pool rotation: cycle 0, seen 80/734 candidates`. **734 candidates. 3,669
deep-linked ids.** Measured on this checkout against the committed snapshot:

| population | count | share of deep links |
|---|---|---|
| generated `?pool=` deep links (EN; KO emits the identical set) | 3,669 | 100% |
| …present in `data/pools-snapshot.json` (rotation-reachable) | **420** | **11.4%** |
| …absent (rotation can NEVER pick them) | **3,249** | **88.6%** |

- TVL distribution of the deep-linked set, against a live fetch: **median $515,134**, min $100,015,
  **61.7% below $1M**, **88.6% below `DEFAULT_MIN_TVL`**. The estate floors at $100K
  (`generate-token-pages.js`, human directive 2026-07-11 — deliberate and NOT the bug); the snapshot floors
  at $10M (059, FE-payload).
- **So every "pool-detail clean" claim this loop has ever made is scoped to the richest 11.4%** — and the
  one real arrival we have on record (`fdcccd6a-…`, $1.5M) sits in the other 88.6%, on a code path the
  scanner has never rendered even once.
- This is `detector-signal-coverage.md`'s question on a **fourth axis**. 167/183 fixed *selection*;
  191/192/196 fixed *throughput and memory*; 197 fixed the *static* population. All four took the **pool**
  population as given. The rule generalises: **the audit's population must be the ARRIVAL population, not
  the data artifact that happens to be nearest to hand** — the same "validate against the population the
  generator drew from" trap `product-audit.md` class 10 already documents for `?pool=` liveness, one level up.
- Sizing honestly: this is **coverage exposure with no proven live defect** — the cheap prescan signals do
  not apply (they read on-disk HTML, and these are app routes), and `poolLinkLiveness` already proves all
  3,669 resolve. The exposure is the **render-only** classes (`dead-cta`, number sanity, console errors,
  360px clipping, KO leaks) reaching 0 of 3,249 arrival-reachable pools, forever. The defect history says
  that matters: **every** hand-found pool-detail bug (122, 144, 145, 165) was on a non-flagship pool.

## NEW FINDING → 207 (P1/READY 7.5): item 105's kpi backfill for SEO arrivals fires for 11.4% of SEO arrivals
`app.js:1253-1273` is item 105, and its own comment states its purpose verbatim: *"a direct `?pool=<id>` SEO
deep link always loads LIVE `/pools` (no kpis …) so the trust-signal notes … would hide for exactly the SEO
arrivals we most want to convert."* Its remedy is `snap.pools.find(p => p.pool === poolId)` against
`data/pools-snapshot.json` — **the same 736-pool $10M-railed set**. Measured this tick: **420 of 3,669
(11.4%) deep-linked ids are in the snapshot; 736/736 snapshot pools carry a `kpis` object, 714 have
`historyPoints ≥ 7`.** So for **3,249 arrivals (88.6%) the lookup misses by construction** and all three
trust notes stay hidden:
- `PoolDetail.js:1508` rate track-record (088.1) · `:1556` rate momentum (103) · `:1602` TVL trend (087).
- **The consequence is not a wrong number — it is a blank.** There is already an honest "no track record
  yet" tier (`translations.js` `rateTrackRecordNew`), but it is *inside* the `pool.kpis &&` gate, so a pool
  with no kpis at all renders **nothing** rather than the honest note. The 88.6% of arrivals get the
  thinnest possible version of the north-star page — no steadiness, no momentum, no TVL trend, no
  disclosure that we don't have them.
- **Root cause is upstream and is a real design decision, not an oversight to patch:** `compute-kpis.js`
  derives its history series *from* `data/pools-snapshot.json` (header `:9-11`), so no history exists for
  sub-rail pools to back-fill from. A cheap leg (render the honest absent-history note) and a structural
  leg (widen the kpi/history population) are genuinely different items — split as 207 (cheap) / 208
  (structural, needs a cost measurement first).
- **Not a trust-rail change either way.** Nothing here proposes lowering `DEFAULT_MIN_TVL` or letting a
  sub-rail pool into the grid, the planner, or any ranking — `?pool=` deep links already render these pools
  today, by design (`snapshotEligible = !urlParams.pool && …`, `app.js:1144`). This is about what the page
  *says* on a pool it already shows.

## NEW FINDING → 208 (P2/READY 5.5): widen the kpi/history population to the deep-linked set — measure the cost first
The structural half of 207. `compute-kpis.js` would need history for ~3,669 pools instead of 736 (~5×
`data/history/<date>.json`, ×30 retained points). Ticketed as **measure-then-decide**, not build-now: the
spec's first acceptance criterion is the measured byte cost of the widened history artifact and the
resulting FE payload delta, because 059 railed the snapshot to $10M for payload reasons and re-widening it
naively would undo that. A separate lean id→kpis artifact (fetched only on pool-detail, as 105 already
does) is the shape that avoids touching the main snapshot payload at all.

## Checked and CLEARED — no ticket (recorded so the next tick does not re-derive)
- **The `$10M` / `$100K` floor claims do NOT contradict each other on any surface.** Chased it hard,
  because it is `product-audit.md` class 12's exact shape. `translations.js:186` `trustFloor: "$10M minimum
  TVL"` renders at `landing.js:350` — and **`tokens/*.html` does not load `landing.js`** (its only script
  tag is `analytics.js`; grepped). The generated estate says `$100K TVL floor` on **2,154** pages and
  contains the string `$10M` **zero** times. 174's fix held.
- **`tvlTrendShrinking`'s "keeps clearing our $10M size floor" prose cannot render on a sub-rail pool.**
  It sits behind `pool.kpis && … historyPoints >= 7` (`PoolDetail.js:1602-1605`), and a sub-rail pool has
  no kpis (finding 207). So the false-claim version of this bug does not exist — the blank version does.
  Recording the *falsification* because the hypothesis is the obvious one and the next tick would re-chase it.
- **`poolLinkLiveness`'s `scope: "en"` is not a 197-shaped gap.** Extracted the `?pool=` id sets from both
  estates independently: EN **3,669**, KO **3,669**, **ids in KO but not EN: 0**. The KO estate emits the
  identical set, so the EN-scoped gate's verdict genuinely transfers. Checked, not assumed.
- **The `src`-is-`undefined` streak is explained and already fixed** — see the Distribution section. It is
  recorded as a dated prediction rather than a ticket.

## Data layer — healthy
Snapshot `generatedAt` **2026-08-02T06:40:20.730Z** (this tick), **736 pools** (737 yesterday).
- **0** pools with total APY > 1000%; **0** non-finite KPIs; `mean30d` breach set unchanged at 1
  (`201e5f6e`, known-and-handled, auto-downgraded by 171 for a 5th day).
- Live DefiLlama fetch succeeded in-session: **15,822 pools**, used as an independent oracle, so the
  liveness, TVL-distribution and coverage numbers above do not rest on the committed snapshot.

## Experiments — no window closed today
- Windows still open: 117.1/117.2/117.3 → 08-05; 124 → 08-06; 125 → 08-07; 129 → 08-08; 045/051/065/148 →
  08-09..08-12; 180/182 → 08-13; 190/191/192 → 08-14; 196 → 08-15; 197-205 → 08-15..08-16; 013 GSC leg →
  human ~08-23.
- 199–205 shipped 08-01/08-02 (overnight); windows not yet due.

## Diff vs 2026-08-01
1. **Third consecutive zero-blocking tick, on a population 2× wider again.** 197/198 landed: prescan
   **2,186 → 4,304** pages, `surfacesCovered` **60 → 82**, first-ever `/ko/` static surfaces rendered.
2. **The 7d URL set moved for the first time in three days** — and both new URLs are one journey:
   `/tokens/jitosol` → `/?pool=fdcccd6a-…`. First SEO-lander → pool-detail traversal on record. 0 CTA clicks
   (n=1, no claim made).
3. **206 found — the audit's pool population is 11.4% of the arrival population.** 734 rotation candidates
   vs 3,669 deep-linked ids; the one real arrival sits in the unreachable 88.6%.
4. **207 found — 105's SEO-arrival kpi backfill reaches 11.4% of SEO arrivals**, for the same reason, and
   the honest "no track record yet" note is trapped inside the gate it should be the fallback for.
5. **208 filed as measure-then-decide** for the structural fix, explicitly not build-now.
6. **The 21-day `src`-`undefined` reading retired as a measurement artifact**, with a dated falsifiable
   prediction (~08-05) replacing it. 202/203/204 all verified present in the shipped code this tick.
7. Guardrail streak 19 → **20** clean prod days, clean in the unfiltered control for a fourth day.
8. Data layer: pools 737 → **736**; zero out-of-rail totals; zero non-finite KPIs; 171 auto-downgrade
   holding for a fifth day.
9. North star 0 (**25th** day). Funnel events 30d **unchanged for a third consecutive day** (72h).
10. **Backlog had ZERO READY rows at pickup** (every row 001-205 terminal — the condition that has forced
    build loops to self-promote for four straight runs, per 202/203/204's own LOG lines). Max id on
    `origin/main` re-read immediately before writing: **205**, so today's findings are **206/207/208**.
