# Signals snapshot — 2026-08-08 (heartbeat, live `Mixpanel` connector)

Project: defigarden (4042048) · org Equitee (2885044) · prod filter `$current_url contains www.defi.garden`.
Queries this run: prod daily 14d `6ff51037`; `pool_click`/`pool_view` 30d by `source` `c58fd11f`; 08-07
engagement events `e2cd57dc`; 08-07 sessions by URL×browser `e461b540`; 08-07 sessions by region×OS
`35b6e8fd`; **08-07 engagement by region `b1e1f283`** (the decisive one); referrers 14d (event-prop)
`4e25c206`; unfiltered control 14d `57c81b45`; all URLs 08-06..08-08 `30e32fd4`; waitlist 7d by `src`
`c7aa9bd9`.

## HEADLINE: the north star fired for the first time in its history — and it was **us**.

`pool_click{source=garden_cta} = 1` on 2026-08-07, alongside `pool_view` 4, `plan_created` 1, `plan_saved` 2
— the first typed-engagement day ever recorded on this project, breaking a 30-day zero. Broken down by
`$region` (`b1e1f283`), **100% of it — every single engagement event — originates in Utrecht, NL, on
iOS/Mac**, on the day the repo owner merged four design PRs. It is operator self-test, not demand, and it
**does not count toward the ≥30 gate**. Second, larger finding, and it needed no traffic at all: commit
`6fceca79bb` moved `DEFAULT_MIN_TVL` **$10M → $100K (100×)** and left the product's own declarations of that
floor behind on four surface families, while **deleting a rendered translation key** — `npm test` is RED on
`main` right now. `error_occurred` = 0 (26th clean prod day), and its exercise count is genuinely non-zero
for the first time.

## Real prod trend (daily totals, `6ff51037`)
| date | session_start | error_occurred | pool_view | pool_click | page_view | plan_created |
|------|----|----|----|----|----|----|
| 07-25 | 4 | 0 | 0 | 0 | 1 | 0 |
| 07-26 | 6 | 0 | 2 | 0 | 4 | 0 |
| 07-27 | 3 | 0 | 0 | 0 | 1 | 0 |
| 07-28 | 8 | 0 | 0 | 0 | 1 | 0 |
| 07-29 | 5 | 0 | 0 | 0 | 2 | 0 |
| 07-30 | 6 | 0 | 0 | 0 | 2 | 0 |
| 07-31 | 4 | 0 | 0 | 0 | 0 | 0 |
| 08-01 | 9 | 0 | 1 | 0 | 2 | 0 |
| 08-02 | 3 | 0 | 0 | 0 | 0 | 0 |
| 08-03 | 92 (crawler burst) | 0 | 0 | 0 | 23 | 0 |
| 08-04 | 8 (6 = fuzz replays) | 0 | 0 | 0 | 6 | 0 |
| 08-05 | 4 | 0 | 0 | 0 | 1 | 0 |
| 08-06 | 76 (crawler burst #2) | 0 | 0 | 0 | 23 | 0 |
| **08-07** | **18** | **0** | **4** | **1** | **11** | **1** |
| 08-08 | 0 so far | 0 | 0 | 0 | 0 | 0 |

Prod 14d sessions: **246** (control `57c81b45`: **252**; the 6-session delta = localhost/preview, playbook
class 13). `error_occurred` absent from BOTH result sets over 14d. Two crawler bursts still account for
**168 of 246 (68.3%)** of the 14d count — down from 72.7% only because the window slid, not because the
mix improved.

## §2b-adjacent — THE INVESTIGATION: classifying 08-07, the first engaged day on record

Followed `playbooks/traffic-quality-classification.md`. **It could not answer the question**, and that
gap is this tick's compound output (§4b).

### Step 1 — the playbook's bot signature scores 08-07 as REAL, on every criterion
| criterion | 08-06 burst | 08-07 | verdict |
|---|---|---|---|
| No acquisition | `$direct`, no utm | `$direct` dominant, one `src=seo_token`, one `src=pool` | ambiguous |
| One-hit / no progression | yes | **no** — `pool_view` → `pool_click` → `/plan?...src=pool` → `plan_created` → `plan_saved` ×2 | REAL |
| Garbage long-tail params | some | **none** — 9 well-formed URLs | REAL |
| Zero engagement | yes | **no** — `plan_created` 1, `plan_saved` 2, `pool_click` 1 (all typed/multi-step per the playbook's own rule) | REAL |
| Uniform fingerprint | bare-`Mozilla` ×24 | **no** — Mobile Safari 6 / Safari 2 / Chrome 9 across 9 distinct URLs | REAL |
| `session_start` ≫ `page_view` | 76 vs 23 (3.3×) | 18 vs 11 (1.6×) | REAL |

By the playbook's decision rule this is *"a session with an acquisition source + multi-step engagement →
real user. THAT is signal."* Acting on that reading would have opened the north star's first traffic gate.

### Step 2 — the geo/device split, which the playbook does not ask for (`35b6e8fd`)
| region | sessions | OS |
|---|---|---|
| **Utrecht** | **8** | **iOS 7, Mac OS X 1** |
| California | 5 | Android 5 |
| Oregon | 2 | Mac OS X 2 |
| undefined | 3 | Windows 1, Mac OS X 2 |

California/Android ×5 and Oregon/Mac ×2 are datacenter-region shapes (the burst tail). Utrecht is not.

### Step 3 — the decisive query: engagement broken down by region (`b1e1f283`)
| event | Utrecht | everywhere else |
|---|---|---|
| `pool_click` | **1** | 0 |
| `pool_view` | **4** | 0 |
| `plan_created` | **1** | 0 |
| `plan_saved` | **2** | 0 |
| `page_view` | 9 | 2 (`undefined`) |

**Every engagement event in the window is Utrecht.** Circumstantial corroboration, stated as
circumstantial: the repo owner's commit identity is `alex@0xhodler.nl` (a `.nl` address); `www.0xhodler.nl`
appears as a referrer in the same 14d set; 08-07 is the day `#409`, `#411`, `#412` and `#413` — four design
PRs — merged to `main`; and the session set is a **reviewer's sweep**, not a searcher's task: bare `/`,
`/tokens`, `/tokens/buidl`, four different `?token=` values, a `?pool=` and the planner, with
`search_input` = **0** across all of it.

**Weakest honest claim, and it is the one filed:** 08-07's engagement originates entirely from one
non-datacenter region on operator-class devices, on the day the operator shipped four PRs. The loop has
**no instrument that can separate operator traffic from a genuine Utrecht visitor** — that inability is
the finding, not the attribution. Either way n=1, far below the ≥30 rule, so **no gate opens**.

### The class this exposes — and why it is wider than "the owner visited"
The playbook's real-user bar asks *"is there a human behind this session?"* Both exclusions it already
makes (crawlers) and the one it just missed (the operator) fail a **weaker, single predicate**: *does this
visit carry information we did not already have?* A crawler fails it for want of intent; the operator
fails it because we already knew they were looking. Adopting the weaker predicate unifies two classes the
playbook currently treats as one-and-a-gap, and it strictly contains the crawler rule. → playbook updated
(§4b) and ticketed as **252**, because without an instrument every future gate read is contaminated by
exactly the traffic the loop generates by shipping.

## North star, both legs
- **Leg (B) pool-detail CTA clicks: 1 in 14d — the first non-zero in the metric's history — and it is
  operator self-test, so the real count stays 0 (31st straight day).** 30d by `source` (`c58fd11f`):
  `pool_click` = 2 total (`garden_cta` **1**, `undefined` 1 — the pre-123 legacy event, ages out ~08-18);
  `pool_view` = 16 (`card_click` 13, `url_direct` 3), up from 12, the +4 all from 08-07/Utrecht.
  Gate: **≥30 real exercised events. Real = 0. Unchanged.**
- **Leg (A) agent consumption:** 14d referrers (`4e25c206`, 252 total): `$direct` 242, `bing.com` 2,
  `www.ecosia.org` 2, `www.perplexity.ai` **2**, `t.co` 1, `www.0xhodler.nl` 1 (the operator's own site),
  `www.tinker.com` 1, `www.recipebridge.com` 1 (junk). The perplexity pair is still the same 2026-07-26
  pair — **0 new AI-referred sessions**, now day 4 since the strategy adoption. Instruments 224 (D1 edge
  log) / 227 (API) / 228 (MCP) remain unbuilt; leg (A) is Mixpanel-only and blind to non-JS agent reads
  (212's gap). **New this tick:** the two static agent surfaces leg (A) is built on — `llms.txt` and
  `home.html`'s `navigator.modelContext` tool declaration — now publish a **false rail** (see below).

## Guardrail — 26 clean prod days, and the exercise count is real this time
`error_occurred` = 0 across 14d in both prod-filtered and unfiltered sets → **26th** straight clean prod
day (control 10th). Exercise count, stated per LEARNINGS 082: the analytics/pool-detail path was exercised
**4 times** on 08-07 (4 `pool_view`, 9 `page_view`), all post-244-fix, all operator. Yesterday this zero
carried n=1; today n=4. Still far below ≥30 — it is not evidence the app is error-free for strangers, only
that the code paths a reviewer walks did not throw.

## §2c Orphan-PR check (`pr-orphan-detector.js`, live data)

`list_pull_requests(state=open)` → **3 open** (was 11 yesterday). Fed with `main`'s BACKLOG.md into the CLI:

Orphan PRs: **2** (#331, #322) · PARKED 1 (#386/221)

- **Orphan count 8 → 2, and the drop is real cleanup, not a detector change.** Five of yesterday's eight
  left the open set overnight: **#409** (247 design world) and **#399** (239 sort fix) **merged to main**
  (`514b4ba236`, `c5b5b17a47`); #396, #316, #309 closed. #408 had already merged.
- **#331 (item 176)** and **#322 (item 166)** are the residual. Both are genuinely PARKED/BLOCKED **in
  their PR titles** but their BACKLOG rows do not lead with a legend marker, so the detector correctly
  degrades them to ORPHAN — this is the documented gap in the module's header comment, firing as designed.
  Detection only; disposition is a build-run or human question (§2c step 5).
- **Leg B — next id computed, not guessed:** `computeNextId(mainMax=251, openPrBranchIds=[221,176,166])`
  = **252**. No collision. Item **247** is now merged to `main` (`514b4ba236` + `6c33899fee` + `60278d4d47`)
  and still has **no BACKLOG row** — yesterday's "claim without a row" has become "shipped without a row";
  a row is added this tick recording it as SHIPPED rather than allocating 247 to anything new.

## §2b PRODUCT AUDIT — the code-level finding, found with zero traffic

Between yesterday's heartbeat (`b430878384`, 07:33) and this one, **six human-authored merges landed on
`main`** with no loop verifier in the path: #409, #411, #412, #413 (design world), #399 (item 239), #397
(item 232), plus the `claude/pendle-direction` and `claude/loop-161` merges and one direct commit,
`6fceca79bb`. That direct commit is where the defects are.

### `6fceca79bb` — "full 00K TVL floor alignment": the rail moved 100×, the claims did not

`DEFAULT_MIN_TVL` is now **100000** (`app.js:801`, `app.compiled.js:805`, `src/poller-core.js:19`), down
from `10000000`. This is a trust-rail relaxation, which NORTH_STAR reserves to the human — and the commit
is authored `alex@0xhodler.nl`, so it is **authorized**. Not in question. What follows is the collateral.

**Sized, so the class is not left un-numbered:** today's committed snapshot
(`data/pools-snapshot.json`, `generatedAt` 2026-08-08T03:23:24Z) holds **7,358 pools**. Only **732 (9.9%)**
clear the $10M floor the copy still advertises. **6,626 pools — 90.1% of what the app now serves by
default — sit below the floor the landing page promises.**

#### (a) P0 — a rendered translation key was DELETED from both namespaces; `npm test` is RED on `main`
`poolNotFoundTitle` count in `translations.js`, traced commit by commit:

| commit | `poolNotFoundTitle:` | `tvlTrendShrinking:` |
|---|---|---|
| `9ab58d990d`, `e8aac65f03`, `6f1d624451` (before) | **2** | 2 |
| **`6fceca79bb`** and every commit since, incl. `HEAD` | **0** | **4** |

The diff's `-` block removed `poolNotFoundTitle` from EN and KO; the `+` block replaced it with a
`tvlTrendShrinking` that already existed 50 lines above. Loaded at `HEAD`: `en.poolNotFoundTitle` and
`ko.poolNotFoundTitle` are both `undefined`.

`translations.js:1546` — `return fallback ? … : key;` — so `t('poolNotFoundTitle')` returns **the key
string itself**. It is rendered at `app.js:3558` / `app.compiled.js:3492` as the `.empty-message` heading
of the dead-pool state, i.e. on a `?pool=<unknown-id>` arrival — the **`url_direct` leg of the north-star
surface**, the one the 3,677-deep-link estate points at.

**Verified by running it, not inferred:** `node test_dead_pool.js` → **2 of 12 assertions FAIL**
(`page.waitForFunction: Timeout 8000ms exceeded` on both the EN and the KO title), and the file's own
summary line prints `10 dead-pool assertions passed (EN: "undefined", KO: "undefined")`. **Exit code 1.**
*Instrument note, per §2a:* the first read of this was through `node test_dead_pool.js | tail -25`, whose
exit status is `tail`'s, and it reported **0**. A second run without the pipe reported **1**. A "vacuous
green" finding was one pipe away from being filed against a gate that works correctly — recorded as
instrument blindness, the 08-02 `src` precedent.

#### (b) P0 — 9 live claims state a floor the app no longer enforces, across 4 surface families
Measured with a scan over the **whole dictionary** (1,106 leaves, EN+KO, functions invoked) plus a grep of
every served surface — the population is "every place the product states its own TVL floor", not the two
keys that happened to grep first:

| # | site | text | surface family |
|---|---|---|---|
| 1 | `translations.js` `landing.trustFloor` (en) | `"$10M minimum TVL"` | rendered — landing trust rail |
| 2 | `translations.js` `landing.trustFloor` (ko) | `"$1,000만 최소 TVL"` | rendered — landing trust rail |
| 3 | `translations.js` `planner.personaDegenDesc` (en) | `"…TVL ≥ $10M…"` | rendered — planner persona |
| 4 | `translations.js` `planner.personaDegenDesc` (ko) | `"…TVL ≥ $10M…"` | rendered — planner persona |
| 5 | `home.html:238` | `"…trust-rail filters (minimum $10M TVL)."` | **agent contract** — `navigator.modelContext` tool declaration |
| 6 | `llms.txt:20` | `"…the 544 pools clearing our published rails (TVL ≥ $10M, APY ≤ 1000%)…"` | **agent citation layer** |
| 7 | `llms.txt:79` | `"…(updated daily, TVL ≥ $10M)."` | **agent citation layer** |
| 8 | `llms-full.txt:22` | same as #6 | **agent citation layer** |
| 9 | `stories/kevin.html:119` | `"…credible newer pools, $10M+ TVL."` | persona SEO estate |

Sites 1 and 2 are the worst of them: `landing.js:357` renders `copy.trustFloor` inside
`<section id="trust">`, as a badge reading **"⌁ $10M minimum TVL"** — a component whose entire job is to
state the trust rail, stating it 100× wrong, in both languages.

Sites 5–8 are north-star leg (A)'s own surface. The mechanism for 6–8 is upstream and unfixed:
**`generate-llms.js:50` still hardcodes `const MIN_TVL_USD = 10000000; // mirrors app.js:801`** — it no
longer mirrors anything. Site 9 comes from `generate-stories.js:230`, likewise a hand-typed literal.

**Not a defect, checked and cleared:** `tokens/susd3.html`/`.md` grep positive for `$10M` but read
*"3 live pools clear this page's $100K TVL floor, $10M in total"* — the aggregate TVL, correct beside the
correct floor. The token/chain generators derive their floor properly; only `generate-llms.js` and
`generate-stories.js` re-type it.

**The weak form, and it is what the ticket carries:** the rail string must **derive from the constant at
every stating site**. Fixing the nine strings is the narrow hypothesis — it would have been equally true
before this commit and would break identically at the next change. The mechanism is *re-typing a rail into
prose*, and it is still live in two generators.

#### (c) P1 — `tvlTrendShrinking` is now duplicated in both namespaces, and nothing checks for it
`translations.js:62` (EN, `$10M`) is shadowed by `:112` (EN, `$100K`); `:847` (KO, `$10M`) by `:890`
(KO, `$100K`). Last-key-wins, so the **correct** copy renders and there is **no user-visible defect
today** — stated plainly rather than inflated. But the dead pair is the exact copy the fix was meant to
retire, sitting in the file looking authoritative, and **no gate in the repo can see a duplicate key**:
`audit-app.js`'s `en-ko-parity` walks the *parsed object*, where the duplicate has already collapsed. The
same blind spot is what let (a) through — a key can be deleted and a key can be doubled, and the parsed
dictionary looks healthy in both cases.

#### (d) Stale code comments (cosmetic, listed for completeness, not ticketed separately)
`src/poller-core.js:48` `// $10M floor — the ONLY drop` (the constant three lines above reads `$100K`);
`app.js:2551`; `generate-llms.js:44`; `PRODUCT.md:35` (the impeccable skill's product context, which
design agents read as fact).

## §2a predictions
**Carried from 2026-08-05 — fuzz-class decay. Status: TRACKING, day 4 of 7.**
Outcome: the pre-223 scrape-minted fuzz classes decay to zero as crawler caches expire. Witnesses (all):
ANY prod event broken down by `$current_url` matching those strings, filtered AND control. Measured
08-06..08-08 (`30e32fd4`, 17 distinct URLs, 249 events): neither `` /?token=USDC` `` nor the empty
`/?pool=` appears. **Zero across all witnesses, four consecutive days.** Resolve at 7 (→ ~08-11); a NEW
mint string producible by the current tree reopens 223.

**Carried from 2026-08-06 — the 244 race reaching prod. Status: TRACKING, 4 exercises, no witness fired.**
Outcome: the analytics-mode boot race can execute `PoolDetail.compiled.min.js` before `React` exists in
prod. Surviving witnesses: `error_occurred` matching `React is not defined` / `reading 'some'`; 224's D1
edge log once it exists; any rendered-Playwright run reproducing it against prod. (The
`session_start`-without-`pool_view` witness was retired as confounded on 08-07.) 08-07 gave it **4 real
analytics-mode exercises** and `error_occurred` stayed 0 — n=4, still below ≥30, no claim.

**Filed this tick — the false-rail claims are load-bearing for leg (A).** Outcome: an AI assistant asked
about defi.garden's TVL floor will state $10M. Witnesses (all): the text of `llms.txt`/`llms-full.txt` as
served; the `navigator.modelContext` tool description in `home.html`; the rendered landing trust badge;
any answer-engine citation of the site that quotes a floor; and 224's edge log once deployed (which UA
families fetch `llms.txt`). Resolve when every stating site derives from `DEFAULT_MIN_TVL`. Note the
prediction is stated at the **outcome** level — "some consumer will repeat the false rail" — because the
one instrument we could check today (a grep of the served files) proves only that the claim is *available*,
never that it was *consumed*.

## Gate re-check (§4)
- **0 GATED rows in the backlog** (229's gate was met and promoted 2026-08-07). Nothing to re-check.
- **≥30-real pool-detail gate: 1 event, 0 real → unmet.** This is the closest it has ever come and the
  reason the classification investigation above got the tick's main effort.
- **≥30-real waitlist gate: 0 real.** `waitlist_opened` 7d = **16** (10 `seo_tokens_hub`, 6 `undefined`,
  `c7aa9bd9`), all crawler-fired; `waitlist_email_entered` / `waitlist_submitted` absent. Unchanged.

## Experiments — no window closed, and none CAN close
No traffic gate has opened. 117.1/.2/.3 remain SHIPPED (measuring) under the 60d backstop (~09-16..18).
Human-owned GSC reads: 013 ~08-23, 211 ~09-13.

## §2b `audit-app.js` fresh run — 83 surfaces, **5 findings**, and it caught the dead-pool defect itself
`status: "OK"`, `generatedAt` **2026-08-08T07:22:44.371Z**, `surfacesCovered` **83**, playwright local
1.61.1 — both staleness gates checked. One run this tick (no rotation double-advance, unlike 08-07).

| finding | severity | 08-07 | 08-08 |
|---|---|---|---|
| `pool-prescan:apy-rail-breach` | P0 | 2 of 3,971 | **15 of 7,358** |
| `pool-prescan:mean30d-rail-breach` | P0 | 11 of 3,971 | **38 of 7,358** |
| `i18n:en-ko-parity` | P1 | 2 of 544 | 2 of 553 (item **248**, unshipped) |
| **`dead-pool` :: `dead-pool-empty-state`** | **P1** | absent | **NEW — "dead `?pool=` id did not resolve to the honest empty state within 10s"** |
| `planner-768 :: responsive` (item 235) | P2 | present | present |
| `.app-footer` occlusion (item 221) | — | absent | absent — **still dormant, not repaired** |

**The `dead-pool` finding is the `poolNotFoundTitle` deletion, found independently.** Yesterday `dead-pool`
was "covered with no finding" and that absence was explicitly *not* claimed as clean (item 231 measured the
occlusion lens at 3/20 on this surface). Today the same surface fires — and it agrees with
`node test_dead_pool.js`'s two failures and with the code read. **Three independent instruments, one
defect.** That is the strongest confirmation this loop has produced for a single finding.

**Run-bookkeeping caveat, recorded rather than assumed:** the audit's exit code was read through a
`| tail -80` pipe, which reports `tail`'s status, so **no claim is made about the audit's exit code** this
tick — the same pipe trap that nearly produced a phantom "vacuous green" finding against `test_dead_pool.js`
an hour earlier (§2b (a)). The findings array is read from `signals/audit-findings.json` directly.

### The rail-breach jump is the floor change, not a regression — measured apples-to-apples
The scanned population went **3,971 → 7,358** because the snapshot now admits everything above $100K.
Comparing across differently-composed populations would be dishonest, so the split was measured **inside
today's single population**, with a predicate that reproduces the prescan's own count exactly (1 + 37 = 38 ✓):

| band | pools | `apyMean30d` > 1000% | density |
|---|---|---|---|
| ≥ $10M (the old floor) | 732 | **1** | 0.14% |
| $100K–$10M (newly admitted) | 6,626 | **37** | **0.56% — 4.1×** |

**The newly admitted band carries 4.1× the 30-day-mean rail-breach density and contributes 37 of the 38
breaches.** This is not a defect — the anomaly machinery still demotes and flags them, and `APY_SANITY_LIMIT`
is untouched at 1000. It is the measured cost of a 100× floor relaxation, and it belongs next to that
decision. (The `apy-rail-breach` count of 15 is reported as the prescan states it; a local predicate did
**not** reproduce that figure, so no band split is claimed for it.)

### The `tvl-floor-claim` signal RAN over the false claims and returned zero — a fourth narrowing
`audit-app.js:513` registers `tvl-floor-claim` (P1); `textSurfaces` scanned **2** surfaces with **0**
suspects across all 5 of its signals. Those surfaces include `llms.txt`, which states `TVL ≥ $10M`.
Reading the predicate (`audit-app.js:700-720`) explains it: it is an **internal-consistency** check —
*"is any figure listed in the same section SMALLER than the floor this document states?"* — and it never
reads `DEFAULT_MIN_TVL`. A document can state any floor it likes, consistently, forever.

**Weak form, and the one item 254 carries:** a stated floor must equal the constant the product enforces —
checked **against `DEFAULT_MIN_TVL`, at every stating site, across every served surface**, not for
self-consistency within two text files. It strictly contains the current check. Precedents for this exact
shape in this gate family: items **198**, **212**, **249**.

### Coverage changes worth naming, since both silently shrink what a "clean" line covers
- **`poolPrescan.scannedByLeg` = snapshot 7,358 / deep-linked-live 0** (08-07: 735 / 3,236). The live
  deep-link leg contributed **nothing** this run. The union got bigger while one of its two legs went to
  zero — a "7,358 scanned" line reads as broader coverage than 08-07's "3,971" and in one dimension is
  narrower. Not ticketed; recorded so it is not re-read as growth.
- **Pool rotation candidate set 3,968 → 7,355**, cycle 0, seen 279. The full-pass horizon roughly
  **doubled** (~124 ticks → ~230) for the same per-tick sample of 32.
- Static prescan: **4,338 pages, 0 suspects** — scoped to its 6 implemented signals
  (`broken-number-literal`, `absurd-magnitude`, `junk-slug`, `zero-yield-claim`, `link-target-integrity`,
  `pool-link-liveness`); `poolLinkLiveness` checked **3,664** ids, **0** dead, 0 pages affected.
- i18n scanned **553** keys (was 544), allowlist 24. The 28.1% function-leaf blind spot (item **249**)
  is unchanged and still unshipped.
- Checks that ran: the scanner's implemented classes (number-sanity, dead-end, loading, page errors, i18n,
  dead-CTA, responsive/dark, occlusion) over the 83-surface rotation plus the prescans above. Every claim
  in this section is scoped to those classes and populations only.

## Diff vs 2026-08-07
1. **North star leg (B) fired for the first time ever — `pool_click{garden_cta}` = 1 — and region breakdown
   puts 100% of the day's engagement in Utrecht on iOS/Mac. Operator self-test. Real count stays 0.**
2. **A new traffic class the playbook could not name**: it scores REAL on all 6 bot criteria and carries
   zero demand information. Playbook updated with the weaker unifying predicate; ticketed as **252**.
3. **P0 shipped to `main` by `6fceca79bb`: `poolNotFoundTitle` deleted from EN and KO**; `t()` returns the
   key, so the dead-pool heading on `?pool=` renders the literal `poolNotFoundTitle`. `node
   test_dead_pool.js` **exits 1** (2 of 12 fail). → **253**.
4. **P0: `DEFAULT_MIN_TVL` moved $10M → $100K and 9 claims of the old floor stayed behind** — rendered
   landing trust badge (EN+KO), planner persona (EN+KO), `home.html`'s agent tool contract, `llms.txt` ×2,
   `llms-full.txt`, `stories/kevin.html`. Only **732 of 7,358** snapshot pools (9.9%) clear the advertised
   floor. `generate-llms.js:50` still hardcodes `MIN_TVL_USD = 10000000`. → **254**.
5. **P1: `tvlTrendShrinking` duplicated in both namespaces** by the same commit; last-key-wins means no
   user-visible defect today, and **no gate in the repo can see a duplicate or a deleted key** because
   every checker walks the parsed object. → **255**.
6. Audit findings 4 → **5**; `dead-pool` fires for the first time and independently confirms (3).
7. Rail-breach counts 2→15 / 11→38 are **population growth**, not regression; the newly admitted
   $100K–$10M band carries **4.1×** the mean30d breach density (0.56% vs 0.14%).
8. **`tvl-floor-claim` ran and returned 0 on files containing the false claim** — an internal-consistency
   predicate where a rail-relative one was needed. Fourth narrowing in this gate family.
9. **Orphan PRs 8 → 2** (#331, #322); open PRs 11 → 3. #409 and #399 merged; #396/#316/#309 closed.
   Item **247** is now shipped to `main` with still no BACKLOG row — row added this tick.
10. Guardrail streak 25 → **26** clean prod days; exercise count n=1 → **n=4** (all operator, all post-244).
11. Fuzz-decay prediction: **day 4 of 7 at zero**, all witnesses, filtered + control.
12. Leg (A): **0 new** AI-referred sessions (day 4); the perplexity pair is unchanged. Two of leg (A)'s own
    surfaces now publish a false rail (see 4).
13. **Instrument blindness caught twice this tick, both from the same cause** — reading an exit code
    through a pipe. Recorded per §2a as findings, not caveats.
