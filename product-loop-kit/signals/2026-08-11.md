# Signals snapshot — 2026-08-11 (heartbeat, live `Mixpanel` connector)

Project: defigarden (4042048) · org Equitee (2885044) · prod filter `$current_url contains www.defi.garden`.
Queries this run: prod daily 14d `79c7c751`; `pool_click`/`pool_view` 30d by `source` prod `1a6672c0`;
08-10 sessions by `$referrer`×`src` `176919ae`; 08-10 by `$region`×`$os`×`$browser` `2dd4bfbd`; all URLs
08-10..08-11 control `e8c97e4c`; referring domains 14d control `7657f23d`; waitlist+plan 7d by `src`
`08d31cd7`. Live prod probes this tick: `curl` on `/api/health`, `/mcp`, `/llms.txt`.

## HEADLINE: the false-rail chain is CLOSED, on `main` AND on live prod — and the build routine cleared six items in one day.

Between yesterday's tick and this one the loop merged **256 (#423), 258 (#422), 227 (#425), 246
(#426+#427), 260 leg A (#428), 261 (#430)**. The two items the last three reports led with are both
verified dead at the live surface this tick:
- `generate-llms.js:52` now **imports** `DEFAULT_MIN_TVL` from `trust-rails.js` — the hand-maintained
  mirror constant is gone (254's fix, exactly where 08-09's snapshot said it had to land).
- Today's `0 2 * * *` cron regen (`6cc72ffdf7`, 06:19 UTC) **preserved** the corrected claims: `llms.txt`
  grep at HEAD = `$100K` ×2 / `$10M` ×0, `llms-full.txt` = `$100K` ×1 / `$10M` ×0. Six straight days of
  re-minting a false rail ended the day the constant became derived.
- **Live prod serves it**: `curl https://www.defi.garden/llms.txt` returns both corrected `TVL ≥ $100K`
  claims. `CLAUDE.md:17` reads `$100K` (261's round-1/2 gap, repaired before PR #429 flagged it).

## Real prod trend (daily totals, `79c7c751`)
| date | session_start | error_occurred | pool_view | pool_click | page_view | plan_created |
|------|----|----|----|----|----|----|
| 07-28 | 8 | 0 | 0 | 0 | 1 | 0 |
| 07-29 | 5 | 0 | 0 | 0 | 2 | 0 |
| 07-30 | 6 | 0 | 0 | 0 | 2 | 0 |
| 07-31 | 4 | 0 | 0 | 0 | 0 | 0 |
| 08-01 | 9 | 0 | 1 | 0 | 2 | 0 |
| 08-02 | 3 | 0 | 0 | 0 | 0 | 0 |
| 08-03 | 92 (crawler burst) | 0 | 0 | 0 | 23 | 0 |
| 08-04 | 8 (6 = fuzz replays) | 0 | 0 | 0 | 6 | 0 |
| 08-05 | 4 | 0 | 0 | 0 | 1 | 0 |
| 08-06 | 76 (crawler burst #2) | 0 | 0 | 0 | 23 | 0 |
| 08-07 | 18 (operator sweep) | 0 | 4 | 1 | 11 | 1 |
| 08-08 | 11 | 0 | 0 | 0 | 7 | 0 |
| 08-09 | 10 | 0 | 0 | 1 | 5 | 0 |
| **08-10** | **10** | **0** | **0** | **0** | **7** | **0** |
| 08-11 | 0 so far | 0 | 0 | 0 | 0 | 0 |

`error_occurred` absent from the prod-filtered result set across 14d. The two crawler bursts are
**168 of 256 (65.6%)** of the 14d prod count. (08-10 finalized at 7 `page_view` — yesterday's tick read
1 mid-day; the delta is time-of-read, not a correction.)

## §2b(i) — classifying 08-10, per `playbooks/traffic-quality-classification.md`

10 sessions, **10 unique users** (`176919ae`/`2dd4bfbd`) — one session each. Referrers: 9
undefined/direct, 1 `bing.com`; `src` undefined on every session. Regions: **undefined 7** (Mac OS
X/Safari **5**, Linux/Chrome 1, Windows/Chrome 1), Oregon 2 (Android/Chrome, iOS/Mozilla), Iowa/Windows/
Edge 1. URLs (`e8c97e4c`, control, 08-10..08-11): `?token=TROLL&chain=Ethereum`, `?token=REX`,
`?token=REACT&chain=Ethereum`, `?token=FAI&chain=Base`, `?token=YNUSDX&poolTypes=Yield+Farming`, bare `/`
×3, **`/?pool=` ×2** (the known fuzz mint string — see §2a).

| bot criterion | 08-10 | verdict |
|---|---|---|
| No acquisition | 9/10 undefined referrer, `src` undefined throughout | HIT |
| One-hit / no progression | 10 sessions = 10 uniques | HIT |
| Garbage long-tail params | 1 of 7 distinct URLs is a fuzz string (`/?pool=` ×2 sessions) | partial |
| Zero engagement | **yes** — `pool_view` 0, `pool_click` 0, `plan_created` 0 | HIT |
| Uniform fingerprint | 5 of 10 identical undefined-region / Mac OS X / Safari | HIT |
| `session_start` ≫ `page_view` | 10 vs 7 (1.4×) | miss |

4 clear hits → **crawler traffic**. Step 3's insider cut has nothing to run on (zero engagement events),
and no event carries `insider: true` — item 252's flag is live in prod code but the human has not yet
marked a device (`?insider=1`), so the flag's value leg stays unexercised.

**Decomposition, per the playbook's reporting rule: 10 sessions — 10 crawler-shaped, 0 insider, 0 real.**
As on 08-08, the gate-deciding weakest claim needs no per-session attribution: 08-10 produced **zero
engagement events of any kind**.

## North star, both legs
- **Leg (B) pool-detail CTA clicks: 0 real. 34th straight day.** 30d prod by `source` (`1a6672c0`):
  `pool_click` = 3 (`garden_cta` 1 — 08-07 operator; `undefined` 1 — pre-123 legacy, ages out ~08-18;
  `yield_calculator` 1 — 08-09 crawler; the last two outside the north-star filter); `pool_view` = 11
  (`card_click` 8, `url_direct` 3), unchanged. Gate: **≥30 real exercised events. Real = 0.**
  Denominator caveat (item 257) now REPAIRED forward — the third entry path emits `pool_view` since
  #420; history before 08-10 remains an undercount and is not restated.
- **Leg (A) agent consumption: 0 AI-referred sessions in the 14d window** (`7657f23d`, control, 267
  sessions: undefined 191, self 66, `bing.com` 5, `t.co` 1, `www.ecosia.org` 1, `www.tinker.com` 1,
  `www.recipebridge.com` 1, `wink.com` 1 — no perplexity/chatgpt/claude/gemini). Day 7 since adoption.
  **Live-probed this tick, not inferred: the edge Worker is NOT deployed** — `curl
  https://www.defi.garden/api/health` → Vercel 404 (`iad1::` error id, no Worker intercept), `/mcp` →
  404. So 224's D1 log and 227's shipped API are both still absence-of-instrument on prod, and leg (A)
  remains Mixpanel-only, blind to non-JS reads (212's gap). 227's code merged this morning; the deploy
  is human-owned (`edge/DEPLOY.md`).

## Guardrail — 29 clean prod days
`error_occurred` absent from the 14d prod-filtered result set (`79c7c751`) → **29th** straight clean
prod day. Exercise count per LEARNINGS 082: **n=16** (08-07's 4 operator `pool_view`s + analytics-mode
`page_view` loads 08-09: 5, 08-10: 7 — each one load that gave the code a chance to throw). Below ≥30;
not evidence the app is error-free for strangers.

## §2c Orphan-PR check (`pr-orphan-detector.js`, live data)

`list_pull_requests(state=open)` → **3 open — a completely different set from the last four days.**
**#331 (item 176) and #322 (item 166) were both CLOSED by the human on 2026-08-10** (~14:41/14:50 UTC)
— the disposition question carried in four straight reports is answered: closed, not re-attempted.

Fed with today's BACKLOG.md into the CLI: Orphan PRs: **3** (#429, #424, #419) — all three fire the
module's documented no-terminal-marker gap, but their real states differ and matter:
- **#419** (`claude/loop-257-verified`) — a concurrent 257 run; its own title says "COLLIDES … do not
  merge blind". 257 SHIPPED via #420 the same day. #419 is superseded work; disposition (close) is a
  human/build call, surfaced in the report.
- **#429** (`claude/loop-260-operator`) — PARKED in-body after 3 attempts (13 verifier findings, ALL
  against claims, never the fix). Its cross-branch verification: #428 (merged) and #429 fix **opposite
  halves** of 260 — #429's instance-(ii) fix is verified code that main currently lacks (main's row 260
  recorded leg B "ABANDONED"). Backlog row 260 updated with this evidence; disposition human-gated.
- **#424** (`claude/loop-236` phase 1) — opened 08-10 19:22, no verdict marker yet; genuinely in flight,
  not stalled.
- **Leg B — next id computed, not guessed:** `computeNextId(mainMax=263, openPrBranchIds=[260,236,257])`
  = **264** — allocated this tick (containment-predicate item). Note yesterday's "mainMax=256" read was
  already stale at write time (main was at 261 with rows 262/263 landing the same morning); today's max
  re-read from the file at allocation time.

## §2b(ii) PRODUCT AUDIT — 83 surfaces, 4 findings, no new classes

`status: "OK"`, `generatedAt` **2026-08-11T07:22:16Z**, `surfacesCovered` **83**, playwright local
1.61.1, pools source live fetch (15,586). Exit code read directly: **1** (findings present). Fresh
clone had no `node_modules/`; `npm install` ran BEFORE both instruments (playbook rule 0, followed).

| finding | severity | 08-10 | 08-11 |
|---|---|---|---|
| `pool-prescan:apy-rail-breach` | P0 | 17 of 7,370 | 17 of 7,332 |
| `pool-prescan:mean30d-rail-breach` | P0 | 30 of 7,370 | 30 of 7,332 |
| `i18n:en-ko-parity` | P1 | 2 of 554 | 2 of 554 (item **248**, unshipped) |
| `planner-768 :: responsive` (`.gp-chip` zero-match) | P2 | present | present (item **235**, unshipped) |

- Rail-breach counts flat on a population 7,370 → 7,332: ordinary churn, no new claim.
- The dead-pool surface is clean AND the check is no longer presence-only: item **256** shipped (#423),
  so every audited surface now fails on rendered text equal to a dictionary key name (its non-vacuity
  was manufactured and recorded at ship). Zero raw-key findings across the 83 surfaces this tick — the
  first tick that zero is backed by a check that can actually see the defect.
- NORTH_STAR `Test:` legs re-run this tick, unpiped: `test_planner.js` / `test_protocol_parsing.js` /
  `test_qualifier_fix.js` / `test_dead_pool.js` all **exit 0**. Scope: those four files + the audit's
  implemented classes (number-sanity, dead-end, loading, page errors, i18n incl. raw-key, dead-CTA,
  responsive/dark, occlusion) over the 83-surface rotation + prescans — the 150-file `test:serial` lane
  was not run.
- Rotation: pools cycle 0 seen 379/7,329, 32 rendered; static tokens 45/2,070 + KO twins;
  `poolPrescan.scannedByLeg` snapshot 7,332 / deep-linked-live **0** (fourth consecutive zero on the
  live leg — recorded so it is not re-read as growth); `textSurfaces` scanned 2 (near-zero information).

## §2a predictions
**Carried from 2026-08-06 — the 244 race reaching prod. Status: TRACKING, 16 exercises, no witness
fired.** Witnesses unchanged (`error_occurred` matching `React is not defined` / `reading 'some'`;
224's D1 log once deployed; a rendered repro). n=16 < 30. No claim.

**Carried from 2026-08-08 — the false rail is load-bearing for leg (A). Status: RESOLVED (supply side
repaired; consumption witness never fired).** The stated resolve condition — "every stating site
derives from `DEFAULT_MIN_TVL`" — is met: 254 made `generate-llms.js` import the constant, 261 swept
the machine-readable agent surface + docs (residue named with numbers in specs/261.md), and today's
cron regen republished the TRUTH for the first time in seven runs. The consumption-side witnesses (an
answer-engine citation quoting a floor) never fired and are unobservable with leg (A) at zero — so
whether any AI cache still holds the $10M claim is unknowable until an instrument exists. Recorded per
§2a: the supply-side witness carried the whole resolution; both consumption instruments were blind.

**Fuzz replay (post-FALSIFICATION observation, no new prediction):** `/?pool=` returned again 08-10
(2 sessions + 2 page_views, `e8c97e4c`) — known persistence now **≥6 days past mint closure**. The
mint stays closed (404 re-verified 08-10; not re-probed today). Consistent with the rewritten playbook
rule: replay is queue-driven arrival, not decay.

**The denominator-lossy prediction (filed 08-10): fully closed.** Witness (c) — the rendered
Playwright leg — now exists as 257's non-vacuity gate, shipped with #420 (verifier PASS 7/7, 9-row
mutation matrix). All four witnesses accounted for: (a) code-path fired, (b) prod fired 08-09,
(c) shipped, (d) moot forward (denominator repaired).

## Gate re-check (§4)
- **GATED rows: 1** — item **251** (`GATED(224 deployed by the human AND ≥1 row observed in
  agent_reads)`). Gate re-checked with a LIVE probe this tick, not a doc read: `/api/health` → 404
  (no Worker on the domain) → **unmet**; left in place. Note the gate's pressure just rose: 227's
  merged API AND 224's log both now sit undeployed behind the same human action.
- **≥30-real pool-detail gate: 0 real. Unmet** (34th day).
- **≥30-real waitlist gate: 0 real.** `waitlist_opened` 7d = **7** (5 `seo_tokens_hub`, 2 undefined,
  `08d31cd7`), all on crawler-classified days; `waitlist_email_entered`/`waitlist_submitted` absent
  from the result set. `plan_saved` 7d = 2 (`src=pool`, 08-07 operator), `plan_created` 7d = 1. Unchanged.

## Experiments — no window closed, and none CAN close
No traffic gate has opened. 117.1/.2/.3 remain SHIPPED (measuring) under the 60d backstop (~09-16..18).
Human-owned GSC reads: 013 ~08-23, 211 ~09-13.

## BACKLOG table integrity — re-measured with the same predicate
260 id-rows; **23** do not split into 8 cells on unescaped pipes — **up from 13**, the +10 all rows
whose SHIPPED-status prose landed this weekend (145, 168, 179, 188, 202, 204, 230, 245, 258, 259, 260).
Same class as before (unescaped pipes inside status cells), now growing at roughly one per shipped
item. Still bookkeeping hygiene, still not ticketed — but named with the trend so the next tick can
ticket it if it keeps compounding.

## Diff vs 2026-08-10
1. **Six items merged in one day** — 256, 258, 227, 246, 260-legA, 261 (#422..#430). The build routine
   is demonstrably running; yesterday's report question 1 is answered by the ledger itself.
2. **The false-rail chain (253/254/255-detector/261) is closed end-to-end**: generator derives from
   `trust-rails.js`, today's cron republished `$100K`, live prod `llms.txt` serves it. Prediction
   RESOLVED (supply side).
3. **Old orphans #331/#322 CLOSED by the human 08-10** — four-day-old disposition question answered.
   New orphan set #429/#424/#419, all no-marker fires; #429 carries verified code for the half of 260
   main abandoned; #419 is superseded 257 work.
4. **227 merged → 228 (MCP server) is unblocked**; heartbeat wrote specs/228.md and un-deferred the row.
5. **Worker still undeployed, now live-probed** (`/api/health` 404): 224 + 227 both sit behind one
   human deploy; leg (A) instrument gap unchanged, day 7 at zero AI referrals.
6. 08-10: **10 sessions — 10 crawler-shaped, 0 insider, 0 real**; zero engagement events; `/?pool=`
   replay ×2 (≥6 days past closure).
7. Guardrail streak 28 → **29**; exercise count 10 → **16**.
8. Audit: 4 findings, same classes; first tick where the dead-pool zero is backed by 256's text-reading
   check. Rail-breach churn flat (17, 30).
9. New row **264** (containment predicate for the numeral guard, from #429's threshold sweep); rows
   262/263 (openapi drift — BLOCKED on `POST /planner`; in-flight-check gap) noted as filed by the
   weekend's build runs, not this tick.
10. BACKLOG malformed rows 13 → **23** — growing with each shipped-status edit; trend named.
