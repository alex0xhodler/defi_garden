# Signals snapshot — 2026-08-10 (heartbeat, live `Mixpanel` connector)

Project: defigarden (4042048) · org Equitee (2885044) · prod filter `$current_url contains www.defi.garden`.
Queries this run: prod daily 14d `38ec7ca5`; `pool_click`/`pool_view` 30d by `source` prod `12fdcdad` and
unfiltered control `e2386809`; 08-09 sessions by `$referrer`×`src` `a2d1c858`; 08-09 uniques by
`$region`×`$os`×`$browser` `5534891b`; 08-09..08-10 `pool_click` by `source`×`$region`×`$current_url`
`c21d823d`; 08-09..08-10 `pool_click` by `pool_symbol`×`pool_project`×`pool_chain` `c0c75d6f`; all URLs
08-09..08-10 control `69dea23d`; referring domains 14d `2084b8ee` / 30d `732dde66`; waitlist 7d by `src`
`efb5baac`.

## HEADLINE: a pool-detail page rendered in prod yesterday and no instrument recorded it.

The only prod `pool_click` in 33 days fired on 08-09. Followed to its pool identity it turns out to have come
from the **honest empty state's rescue alternatives**, and following it further exposes a hole in the north
star's own denominator: `handleCalculateYield` (`app.js:2778`) is the **third** entry into
`currentView === 'pool-detail'` and the **only one that never calls `trackPoolView`**. Prod on 08-09:
`pool_click` 1, `pool_view` **0**. NORTH_STAR line 37's claim — *"`pool_view` fires exactly once per
pool-detail render (verified for both current paths)"* — was scoped to two paths when there are three.

Second: item **253 shipped** (#417) and `npm test`'s dead-pool leg is green again. Third: the 2026-08-05
fuzz-decay prediction is **FALSIFIED** on day 6 after five consecutive days at zero.

## Real prod trend (daily totals, `38ec7ca5`)
| date | session_start | error_occurred | pool_view | pool_click | page_view | plan_created |
|------|----|----|----|----|----|----|
| 07-27 | 3 | 0 | 0 | 0 | 1 | 0 |
| 07-28 | 8 | 0 | 0 | 0 | 1 | 0 |
| 07-29 | 5 | 0 | 0 | 0 | 2 | 0 |
| 07-30 | 6 | 0 | 0 | 0 | 2 | 0 |
| 07-31 | 4 | 0 | 0 | 0 | 0 | 0 |
| 08-01 | 9 | 0 | 1 | 0 | 2 | 0 |
| 08-02 | 3 | 0 | 0 | 0 | 0 | 0 |
| 08-03 | 92 (crawler burst) | 0 | 0 | 0 | 23 | 0 |
| 08-04 | 8 (6 = fuzz replays) | 0 | 0 | 0 | 6 | 0 |
| 08-05 | 4 | 0 | 0 | 0 | 1 | 0 |
| 08-06 | 76 (crawler burst #2) | 0 | 0 | 0 | 23 | 0 |
| 08-07 | 18 (operator sweep) | 0 | 4 | 1 | 11 | 1 |
| 08-08 | 11 | 0 | 0 | 0 | 7 | 0 |
| **08-09** | **10** | **0** | **0** | **1** | **5** | 0 |
| 08-10 | 1 so far | 0 | 0 | 0 | 1 | 0 |

Prod 14d sessions: **258**. Control (`2084b8ee`, unfiltered, same window): **262** — the 4-session delta is
localhost/preview, playbook class 13. `error_occurred` is **absent from the prod-filtered result set** across
14d. The two crawler bursts are **168 of 258 (65.1%)**.

## §2b(i) — the 08-09 `pool_click`, followed all the way down

This is the tick's investigation, so every step is a measurement, not a reading of the code alone.

1. **It exists and it is prod.** `12fdcdad` (prod-filtered, 30d by `source`): `pool_click` = 3 —
   `garden_cta` **1** (08-07, operator), `undefined` **1** (pre-123 legacy, ages out ~08-18),
   **`yield_calculator` 1 — new since yesterday**. Prod daily puts it on **08-09**.
2. **Where it fired.** `c21d823d`: `source=yield_calculator`, `$region=Iowa`,
   `$current_url=https://www.defi.garden/?token=USDC%60` — a crawler-minted fuzz URL (see §2a).
3. **What was clicked.** `c0c75d6f`: `pool_symbol=USDT`, `pool_project=aave-v3`, `pool_chain=Ethereum`.
4. **Therefore it cannot have been a search result.** `app.js:840`
   `symbolMatchesToken(s,t) => String(s).toUpperCase().includes(String(t).toUpperCase())`;
   `"USDT".includes("USDC`")` is **false**. A top-TVL stablecoin pool on a `?token=USDC`` page can only have
   come from `getEmptyStateAlternatives()` — the honest empty state's rescue grid (`app.js:3578`), which
   renders through the **same** `renderPoolCard` and therefore the same `calculate-yield-btn-new`
   (`app.js:3048`).

**So: the only prod pool click in 33 days landed on item 012/072's rescue surface, and establishing that
required joining three event properties against a matcher function.** The event itself cannot say it:
`source` names the WIDGET (`garden_cta` / `protocol_link` / `yield_calculator`), never the SURFACE the widget
was rendered on. → filed as item **258**.

### And the P1 underneath it: a third pool-detail entry path with no `pool_view`

`grep -n "setCurrentView('pool-detail')" app.js` → **exactly three** sites:

| line | entry path | `trackPoolView`? |
|---|---|---|
| 1293 | `?pool=<id>` landing (`url_direct`) | yes — `app.js:1332` |
| 2683 | `handlePoolClick` (`card_click`) | yes — `app.js:2670` |
| **2787** | **`handleCalculateYield`** | **NO — only `trackPoolClick(pool,'yield_calculator')` at :2783** |

`trackPoolView` has exactly two emit sites repo-wide (`app.js:1332`, `app.js:2670`; grep over
`app.js`/`PoolDetail.js`/`analytics.js`). `handleCalculateYield` runs `setDetailPool(pool)` +
`setCurrentView('pool-detail')` + `window.scrollTo(0,0)` — a full pool-detail render — and emits no view.
**2 of 3 entry paths instrumented; 33% of pool-detail renders are invisible to the north star's denominator.**

Consequences, stated at the strength of the evidence:
- **Measured, prod, 08-09:** one pool-detail render, zero `pool_view`. That is the whole of the direct evidence.
- **Structural:** the north-star CTR (`pool_click{garden_cta,protocol_link}` ÷ `pool_view`) has a denominator
  that undercounts. A `garden_cta` click made from a calculator-entered pool-detail enters the NUMERATOR with
  no matching denominator event — CTR is not bounded by 1.
- NORTH_STAR line 37 is not wrong about what it verified; it is **narrower than the class it guards** — the
  RAZOR example-5 shape. `test_northstar_cta_fires.js` verified the two paths that existed in the author's
  head, and the third sailed past.

→ filed as item **257** (P1). Weakest form for the fix, and what 257 carries: **every transition into
`currentView === 'pool-detail'` emits exactly one `pool_view`**, asserted by set-equality between
`trackPoolView` emit sites and `setCurrentView('pool-detail')` sites in **both** directions (the 212 mirror
rule) — not by adding one call to one handler.

## §2b(ii) — classifying 08-09, per `playbooks/traffic-quality-classification.md`

10 sessions, **10 unique users** (`5534891b`) — one session each. Referrers (`a2d1c858`): 9 undefined/direct,
1 `bing.com`; `src` undefined on every session. Regions: undefined **5** (Mac OS X/Safari **4**,
Windows/Chrome 1), Oregon 2, Yerevan 1, Iowa 1, California 1. URLs (`69dea23d`, control, 08-09..08-10):
`?token=POKT&chain=Base`, `?token=WA7A5&chain=Ethereum`, `/chains/polkadot`, bare `/` ×3, **`?token=USDC%60`**,
`/plan`, **``?token=USDC` ``**, `/plan?lang=ko`, **`/?pool=`**.

| bot criterion | 08-09 | verdict |
|---|---|---|
| No acquisition | 9/10 undefined referrer, `src` undefined throughout | HIT |
| One-hit / no progression | 10 sessions = 10 uniques | HIT |
| Garbage long-tail params | **3 of 11 URLs are the known fuzz strings** | HIT |
| Zero engagement | one `pool_click`, no `pool_view`/`search_input`/`plan_*` | partial |
| Uniform fingerprint | 4 of 10 identical undefined-region / Mac OS X / Safari | HIT |
| `session_start` ≫ `page_view` | 10 vs 5 (2.0×; 08-06 burst was 3.3×, 08-07 operator 1.6×) | miss |

4 clear hits → **crawler traffic** by the decision rule. Step 3's insider cut: the window's single engagement
event is **Iowa / Windows / Microsoft Edge** — a datacenter region, not the operator's iOS/macOS signature and
not the 08-07 Utrecht shape; and it landed on a crawler-minted URL. Not insider.

**Decomposition, per the playbook's reporting rule: 10 sessions — 10 crawler-shaped, 0 insider, 0 real.**

Note for the record, because it is the first time this has happened: **a crawler-classified session produced a
deliberate button click**. Per the 2026-08-03 rule (bots follow CTAs) this changes no gate. It changes what the
loop can learn from a click — which is exactly what 258 is about.

## North star, both legs
- **Leg (B) pool-detail CTA clicks: 0 real. 33rd straight day.** Prod 30d by `source` (`12fdcdad`):
  `pool_click` 3 (`garden_cta` 1 operator, `undefined` 1 legacy, `yield_calculator` 1 crawler — the last two
  both outside the north-star `source` filter); `pool_view` **11** (`card_click` 8, `url_direct` 3), unchanged
  from yesterday. Gate: **≥30 real exercised events. Real = 0.**
  **Denominator caveat, new this tick:** that 11 is now known to be an undercount of pool-detail renders by an
  unquantified amount (item 257) — one confirmed missed render on 08-09, and no way to recover the history.
- **Control-vs-prod divergence worth naming:** unfiltered 30d (`e2386809`) shows `pool_view` **16**
  (`card_click` **13**) against prod's 11 (`card_click` 8) — **+5 card_click on localhost/preview since
  yesterday**, i.e. this loop's own test runs. Item **252**'s class, visible again; the prod filter is doing
  its job and a 30d unfiltered read alone would have manufactured a five-click improvement.
- **Leg (A) agent consumption: 0 AI-referred sessions in the 14d window.** `2084b8ee` (14d referring domains,
  262 sessions): `undefined` 187, `www.defi.garden` 66 (self), `bing.com` 4, `t.co` 1, `www.ecosia.org` 1,
  `www.tinker.com` 1, `www.recipebridge.com` 1, `wink.com` 1 — **no perplexity/chatgpt/claude/gemini at all.**
  The 07-26 perplexity pair has aged out; at 30d (`732dde66`) `www.perplexity.ai` still reads **1**.
  *Instrument note, not a decline claim:* this tick reads `$referring_domain`, yesterday read `$referrer` —
  different properties with different cardinality, so the 2→1 at 30d is not evidence of anything. The 14d
  **absence** is the claim, and it is property-independent. Day 6 since the strategy adoption.
  Instruments 224 (D1 edge log) / 227 (API) / 228 (MCP) remain unbuilt; leg (A) is Mixpanel-only and blind to
  non-JS agent reads (212's gap).

## Guardrail — 28 clean prod days
`error_occurred` absent from the 14d prod-filtered result set (`38ec7ca5`) → **28th** straight clean prod day.
Exercise count, per LEARNINGS 082: **n=10** (08-07's 4 operator `pool_view`s + 08-09's 5 and 08-10's 1
analytics-mode `page_view`s — `page_view` fires only from `app.js`, so each is one analytics-mode load that
gave the code a chance to throw). Up from n=4, still far below ≥30. It is not evidence the app is error-free
for strangers.

## §2c Orphan-PR check (`pr-orphan-detector.js`, live data)

`list_pull_requests(state=open)` → **3 open** (unchanged). Fed with `main`'s BACKLOG.md into the CLI:

Orphan PRs: **2** (#331, #322) · PARKED 1 (#386/221)

- Composition unchanged for a fourth day. #331 (item 176) and #322 (item 166) are PARKED/BLOCKED in their PR
  titles but their BACKLOG rows do not lead with a legend marker, so the module's documented detection gap
  fires as designed. Detection only; disposition is a build-run or human question (§2c step 5).
- **Leg B — next id computed, not guessed:** `computeNextId(mainMax=256, openPrBranchIds=[221,176,166])` =
  **257**. No collision. (`detectIdCollisions`'s argument shape still differs from §2c's prose — recorded
  again, not worked around; no collision claim is made from it.)

## §2b(iii) PRODUCT AUDIT — 83 surfaces, 4 findings

`status: "OK"`, `generatedAt` **2026-08-10T07:23:00.249Z**, `surfacesCovered` **83**, playwright 1.61.1,
pools source live fetch (15,587 pools). Exit code read directly from the process: **1**.

| finding | severity | 08-09 | 08-10 |
|---|---|---|---|
| `pool-prescan:apy-rail-breach` | P0 | 14 of 7,381 | **17 of 7,370** |
| `pool-prescan:mean30d-rail-breach` | P0 | 36 of 7,381 | **30 of 7,370** |
| `i18n:en-ko-parity` | P1 | 2 of 553 | 2 of 554 (item **248**, unshipped) |
| `planner-768 :: responsive` (item 235) | P2 | present | present |
| `dead-pool :: dead-pool-empty-state` | P1 | absent (blind) | absent — **and now genuinely repaired** |
| `.app-footer` occlusion (item 221) | — | absent | absent — still dormant, not repaired |

Rail-breach counts 14→17 and 36→30 on a population of 7,370 (from 7,381): ordinary churn, no new claim.

### Item 253 — verified repaired, by three checks that each look at a different thing
- `grep -c "poolNotFoundTitle:" translations.js` → **2** (EN `:115`, KO `:894`), restored by `5d939ceee5` (#417).
- `node test_dead_pool.js` run unpiped → **exit 0**, `12 dead-pool assertions passed (EN: "This pool is no
  longer tracked", KO: "더 이상 추적되지 않는 풀입니다")`.
- The dictionary evaluated in a VM sandbox returns both strings (not the key names).
- NORTH_STAR's `Test:` command legs re-run this tick: `test_planner.js` **exit 0** (208 assertions),
  `test_protocol_parsing.js` **exit 0**, `test_qualifier_fix.js` **exit 0**.
**Scope of the green claim:** these four files plus the audit's implemented classes — NOT the 150-file
`test:serial` lane, which was not run this tick.

### Item 255 — the duplicate is live, and the WINNER is the correct string (correction to yesterday's framing)
`tvlTrendShrinking` is defined **twice in each namespace**: EN `:62` (`$10M`) and `:112` (`$100K`); KO `:848`
(`$10M`) and `:891` (`$100K`). JS object literals take the LAST definition, so the live strings are the
**`$100K` ones — evaluated in a VM sandbox, not inferred**:
`"...keep clearing our $100K size floor..."` / `"...우리의 $100K 규모 기준을 계속 통과할 수 있습니다..."`.
So the duplicate currently renders **correct** copy in both languages; the stale `$10M` variants are shadowed
dead code. 255's defect is entirely **"nothing can see a duplicate or a deleted key"** — not live wrong text.
Rescored down accordingly. The fragility is real and unchanged: the correct string survives only by source
order, and nothing enforces that order.

### Item 254 — the false rail has now been re-published SIX times, most recently this morning
`git log 6fceca79bb..HEAD -- llms.txt` → **6** regen commits, the latest **`24f31de724` at
2026-08-10T03:54:15Z** (today's `0 2 * * *` `sitemap-update.yml` run). `llms.txt` at HEAD still carries both:
```
:20  Live blended rate: 3.1% — median APY across the 544 pools clearing our published rails (TVL ≥ $10M, APY ≤ 1000%)
:79  TL;DR: Live highest APY opportunities (updated daily, TVL ≥ $10M).
```
Mechanism unchanged from yesterday's sharpening: both sites already interpolate `formatTvlFloor(MIN_TVL_USD)`;
the defect is `generate-llms.js:50` `const MIN_TVL_USD = 10000000; // mirrors app.js:801` — a hand-maintained
mirror of a constant that now reads `100000`. The 212 mirror class, on a number.

**Re-sized against today's committed snapshot** (`data/pools-snapshot.json`, `generatedAt`
2026-08-10T03:52:40.585Z, **7,370 pools**): **737 (10.0%)** clear the advertised $10M floor; **6,633 (90.0%)**
sit below it. Reconstructing the generator's median (`apyBase + apyReward`, `0 < apy ≤ 1000` — my
reconstruction, not the generator's live-`apy` path, so treat absolutes as approximate):

| floor | eligible pools | median APY | forever number for a $20/mo bill |
|---|---|---|---|
| $10M (published) | 532 | **3.12%** | $7,682 |
| $100K (enforced) | 5,285 | **3.07%** | $7,817 |

**Floor wrong 100×, pool count wrong ~10×, rate wrong <2%.** Unchanged in character from yesterday: the
citation layer describes a product that no longer exists; it is not a wrong number flowing into plan math.

### Coverage changes worth naming
- `poolPrescan.scannedByLeg` = snapshot **7,370** / deep-linked-live **0** — third consecutive run at zero for
  the live leg (`3637 of 3639 deep-linked ids confirmed live, 0 net-new beyond the snapshot`). Recorded so it
  is not re-read as growth.
- `prescan` static: 4,300 pages scanned, **0** suspects across all six signals; `poolLinkLiveness` ran,
  3,639 ids checked, **2 dead** across 3 pages, drift 2, allowance 36.39, `ok: true`.
- `textSurfaces` scanned only **2** — the sample is tiny; its `suspectCount: 0` carries almost no information
  and should not be read as coverage.
- Pool rotation cycle 0, seen 346/7,367, 32 rendered per tick → full pass **~231 ticks**.
- i18n scanned **554** keys (up 1 — 253's restored key), allowlist 24. The 28.1% function-leaf blind spot
  (item **249**) is unchanged and still unshipped.
- Checks that ran: the scanner's implemented classes (number-sanity, dead-end, loading, page errors, i18n,
  dead-CTA, responsive/dark, occlusion) over the 83-surface rotation plus the prescans. Every "clean"/"zero"
  above is scoped to those classes and those populations only.

### Instrument-availability finding — a fresh clone's exit 1 is not a red gate
This session's checkout had **no `node_modules/`**. The first `node test_dead_pool.js` exited **1** with
`Error: Cannot find module 'playwright'`, and `audit-app.js` would have done the same. Yesterday's tick read
an exit 1 from this same file as a failing assertion — which it was, then. Today an identical exit code meant
an absent dependency. Both instruments only ran after `npm install` (exit 0). **Rule, recorded so a future
tick does not mistake one for the other: read the failure body, never the exit code alone, and state whether
`node_modules/` was present.** → playbook trap added (§4b).

## §2a predictions

**Carried from 2026-08-05 — fuzz-class decay. Status: FALSIFIED on day 6.**
Outcome predicted: the pre-223 scrape-minted fuzz classes decay to zero as crawler caches expire. Witnesses
(all): ANY prod event broken down by `$current_url` matching the mint strings, filtered AND control. It read
**zero for five consecutive days (08-05..08-08)** and was one day from resolving TRUE.
On **08-09 all three mint strings returned at once** (`69dea23d`): `?token=USDC%60` 1 session,
``?token=USDC` `` 1 session + **2 page_views**, `/?pool=` 1 session + 1 page_view.
The mint itself is still closed — re-verified live this tick: `curl https://www.defi.garden/test_list_polish.js`
→ **404**, `curl https://www.defi.garden/` → 200; `.vercelignore`'s `/test_*.js` shipped in `682cf824ae`
(2026-08-04). Provenance of the strings is ours, per the playbook's class-9 grep: `test_list_polish.js:154`
carries the literal `` `http://localhost:${PORT}/?token=USDC` `` (template literal + closing backtick) and
`test_analytics_src_attribution.js:208` carries `` `...?pool=${encodeURIComponent(POOL_ID)}` ``, whose static
prefix is `/?pool=`.
**What is now known that was not:** replay persists **≥5 days past mint closure**, and a five-day zero is not
evidence the class ended. The prediction's error was not its weakness — it named every witness correctly — it
was predicting **decay** on a queue-driven arrival process with no instrument on the queue. → playbook rule
rewritten (§4b). No re-ticket of 223: no NEW mint string appeared, and the minting files 404 on prod.

**Carried from 2026-08-06 — the 244 race reaching prod. Status: TRACKING, 10 exercises, no witness fired.**
Surviving witnesses: `error_occurred` matching `React is not defined` / `reading 'some'`; 224's D1 edge log
once it exists; any rendered-Playwright run reproducing it against prod. Exercise count rises 4 → **10**
(08-09/08-10 added six analytics-mode loads). Still below ≥30. No claim.

**Carried from 2026-08-08 — the false rail is load-bearing for leg (A). Status: TRACKING, supply-side witness
fired again.** The daily cron re-published both claims at 03:54 UTC today — the sixth republication since the
floor change, so the claim's *availability* is self-renewing. Consumption-side witnesses (an answer-engine
citation quoting a floor; 224's edge log once deployed) remain unfired and, with leg (A) at zero AI referrals
for 14d, unobservable. Resolve when every stating site derives from `DEFAULT_MIN_TVL`.

**NEW this tick — the north star's denominator is lossy.** Outcome: `pool_view` undercounts pool-detail
renders. Witnesses (all): (a) the code path — `setCurrentView('pool-detail')` sites minus `trackPoolView`
sites, non-empty **today**, already fired; (b) any prod day with `pool_click{yield_calculator} > 0` and
`pool_view = 0` — **fired 08-09**; (c) a rendered-Playwright run that clicks `.calculate-yield-btn-new` and
spies `Analytics.track`, which does not exist yet and is item 257's non-vacuity proof; (d) a CTR reading
above 1 once traffic exists. Two of four witnesses have already fired, so this is filed as **resolved TRUE at
the code+prod level** and open only on the rendered leg.

## Gate re-check (§4)
- **GATED rows: 1** — item **251** (`GATED(224 deployed by the human AND ≥1 row observed in agent_reads)`).
  `edge/DEPLOY.md`'s deploy is human-owned and unperformed → gate **unmet**; left in place silently.
- **≥30-real pool-detail gate: 0 real. Unmet.** (Yesterday's 32nd day → 33rd.)
- **≥30-real waitlist gate: 0 real.** `waitlist_opened` 7d = **16** (10 `seo_tokens_hub`, 6 `undefined`,
  `efb5baac`), all crawler-fired; `waitlist_email_entered` / `waitlist_submitted` **absent from the result
  set**. `plan_saved` 7d = 2 (`src=pool`), both 08-07/operator. Unchanged.

## Experiments — no window closed, and none CAN close
No traffic gate has opened. 117.1/.2/.3 remain SHIPPED (measuring) under the 60d backstop (~09-16..18).
Human-owned GSC reads: 013 ~08-23, 211 ~09-13.

## BACKLOG table integrity — re-measured with the same predicate
249 id-rows; **13** do not split into 8 cells on unescaped pipes (ids 065, 104, 116, 120, 122, 157, 166, 189,
193, 194, 199, 236, 237). Same 13 as yesterday over a larger row set; no new malformation introduced this
tick. Not ticketed — bookkeeping hygiene in the loop's own queue.

## Diff vs 2026-08-09
1. **253 SHIPPED** (#417 / `5d939ceee5`). `test_dead_pool.js` 12/12, dictionary evaluated, audit's dead-pool
   surface clean. The two-day `npm test` RED is closed — scoped to the four files re-run, not the full lane.
2. **The tick's headline is new and it is a P1: `handleCalculateYield` is a third pool-detail entry path that
   never fires `pool_view`** (2 of 3 sites instrumented). Prod 08-09 shows the render with no view. → **257**.
3. **The only prod `pool_click` in 33 days came from the honest empty state's rescue grid** — proved by
   `pool_symbol=USDT` on a `?token=USDC%60` page against `symbolMatchesToken`. `source` names the widget, not
   the surface, so no event can say this. → **258**.
4. **The fuzz-decay prediction is FALSIFIED at day 6** after five days at zero; the mint is still 404 on prod.
   Replay persists ≥5 days past closure. → playbook rule rewritten.
5. **254 escalates to six republications**, latest today 03:54 UTC. Re-sized on today's 7,370-pool snapshot:
   floor wrong 100×, count wrong ~10× (737 clear $10M), **rate wrong <2%** (3.12% vs 3.07%).
6. **255 corrected DOWN:** the duplicate's winner is the CORRECT `$100K` string in both namespaces (evaluated).
   Live copy is right; the missing detector is the whole defect.
7. **Leg (A) is now 0 AI referrals across the full 14d window** — the 07-26 perplexity pair aged out.
   Property caveat recorded (`$referring_domain` this tick vs `$referrer` yesterday).
8. 08-09: **10 sessions, 10 crawler-shaped, 0 insider, 0 real** — and, a first, a crawler fired a deliberate
   button click.
9. **A five-click "improvement" that wasn't:** unfiltered 30d `card_click` 8 → 13 is this loop's own test runs
   (item 252's class); prod-filtered is flat at 8.
10. Guardrail streak 27 → **28** clean prod days; exercise count 4 → **10**.
11. Audit 4 findings, 83 surfaces; the vanished dead-pool finding is now absent for the RIGHT reason, while
    the check remains presence-only (item **256** stands, unshipped).
12. Orphan PRs **2**, open PRs **3** — unchanged for a fourth day. Next id computed: **257**.
13. **Instrument availability:** no `node_modules/` in this clone; both Playwright instruments failed
    `MODULE_NOT_FOUND` (exit 1) until `npm install`. An exit code is not a verdict.
</content>
