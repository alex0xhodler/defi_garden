# Signals snapshot — 2026-08-05 (heartbeat, live `Mixpanel` connector)

Project: defigarden (4042048) · org Equitee (2885044) · prod filter `$current_url contains www.defi.garden`.
Queries this run: prod daily 14d `a55adbe7`; unfiltered control 14d `dc145006`; referrers 14d `2727728f`;
AI+social referrals by day 60d `4352ed3b`; `page_view` by URL 7d `94d85932`; waitlist by src 7d `e4ac5acd`;
`pool_click` by source 30d `61e0256d`; 08-04..05 sessions by URL×browser `c050005c`.

## HEADLINE: quiet, clean tick. North star leg (B) = **0**, 28th straight day; leg (A) = **0 new** AI-referred sessions (the 14d perplexity pair is the SAME 07-26 pair as the adoption baseline — dated via `4352ed3b`). Guardrail `error_occurred` absent from BOTH result sets — **23rd** straight clean prod day (control clean 7th). 08-04 closed at 8 prod sessions, but 6 of the 8 are RESIDUAL REPLAYS of 223's fuzz-URL mints (playbook updated); 08-05 = 0 so far. **222's fix is scanner-verified** (grid-360 duplicate `.theme-toggle` P0 no longer flagged) and **226 is live on prod** (token sitemap 130 `<loc>`s, lastmod 08-05).

## Real prod trend (daily totals, `a55adbe7`)
| date | session_start | error_occurred | pool_view | pool_click |
|------|----|----|----|----|
| 07-22 | 4 | 0 | 0 | 0 |
| 07-23 | 7 | 0 | 0 | 0 |
| 07-24 | 3 | 0 | 0 | 0 |
| 07-25 | 4 | 0 | 0 | 0 |
| 07-26 | 6 | 0 | 2 | 0 |
| 07-27 | 3 | 0 | 0 | 0 |
| 07-28 | 8 | 0 | 0 | 0 |
| 07-29 | 5 | 0 | 0 | 0 |
| 07-30 | 6 | 0 | 0 | 0 |
| 07-31 | 4 | 0 | 0 | 0 |
| 08-01 | 9 | 0 | 1 | 0 |
| 08-02 | 3 | 0 | 0 | 0 |
| 08-03 | 92 (crawler burst, classified 08-04) | 0 | 0 | 0 |
| 08-04 | 8 (6 = fuzz replays, see below) | 0 | 0 | 0 |
| 08-05 | 0 so far | 0 | 0 | 0 |

Prod 14d sessions: **162** (control `dc145006`: **168**; 6-session delta = localhost/preview, playbook
class 13). `error_occurred` and `pool_click` absent from BOTH result sets. **Insufficient data** for every
funnel claim at the ≥30 real-event rule. `pool_click` 30d (`61e0256d`): still the single pre-123
`undefined` event — ages out of the 30d window ~08-18.

## North star, both legs
- **Leg (A) agent consumption:** 14d referrers (`2727728f`, 162 total): `$direct` 147, perplexity.ai **2**,
  bing.com 2, ecosia 2, yandex 1, sogou 1 (first sogou on record, n=1), t.co 1, 0xhodler.nl 4 (the human's
  own site), recipebridge/tinker 1 each (junk). Dated via `4352ed3b`: the perplexity pair is **2026-07-26**
  — the SAME two sessions as the adoption baseline, so **0 new AI-referred sessions** since the 08-04
  strategy adoption. t.co total 60d = 3 (07-13, 07-14, 07-30) — all pre-existing; n≤1/day, no claim.
  Instruments 224 (D1 edge log) / 227 (API) / 228 (MCP) still unbuilt — leg (A) reads Mixpanel-only,
  blind to non-JS agent reads (212's recorded gap).
- **Leg (B) pool-detail CTA clicks:** 0 in 14d — 28th straight day. Real `pool_view` 14d = 3.

## The 08-04 day, decomposed (`c050005c`) — 223's mint is CLOSED at source, replays continue
8 sessions: bare `/` ×2 (Chrome) + `/?pool=` ×3 + `` /?token=USDC` `` ×3 (Chrome+Firefox mix).
- **223 post-deploy verification (curl, this tick):** all five previously-200 repo files now **404** —
  `test_min_asset_boot.js`, `audit-app.js`, `CLAUDE.md`, `product-loop-kit/NORTH_STAR.md`, `test_search.js`.
- Provenance of the two fuzz classes (class-9 grep, this tick): `` ?token=USDC` `` = template literal +
  closing backtick, present in 10 repo JS files (test_search.js et al., all now 404 on prod); `/?pool=`
  (empty id) emitted **0×** across tokens/ chains/ stories/ llms*.txt/*.html — it is the static prefix of
  `` ?pool=${…} `` scraped out of served JS. Both = **RESIDUAL REPLAY of an already-closed mint**
  (playbook updated with the decision rule). Not re-ticketed; 223 stays SHIPPED.
- Net real-ish 08-04 traffic: ≤2 sessions.
- Waitlist 7d (`e4ac5acd`): `waitlist_opened` 9 (5 `seo_tokens_hub` + 4 undefined) — the 08-03 crawler set
  aging through the 7d window; `waitlist_email_entered`/`waitlist_submitted` absent. Traffic gate still
  **0 real** events.

## §2a prediction filed (weakest form)
**Outcome:** the pre-223 scrape-minted fuzz classes (`%60`-suffixed, `` ?token=USDC` ``, empty `/?pool=`)
decay to zero as crawler caches expire. **Witnesses (all of them):** ANY prod event broken down by
`$current_url` matching those strings — `session_start`, `page_view`, or any base-context event — in the
filtered AND unfiltered sets. **Resolve:** 7 consecutive days at zero across all witnesses → resolved
decayed; a NEW mint string producible by the CURRENT tree → 223-class reopened (different finding).

## §2b product audit — `audit-app.js` fresh run (83 surfaces, 8 findings, 0 net-new tickets)
`status: "OK"`, `generatedAt` **2026-08-05T07:17:19.693Z**, `surfacesCovered` **83** — both staleness gates
checked. Pools source: live fetch, 15,669 pools.
- **Static prescan: 4,332 pages scanned, 0 suspects** — scoped to its 6 implemented signals
  (broken-number-literal, absurd-magnitude, junk-slug, zero-yield-claim, link-target-integrity,
  pool-link-liveness). 226's regenerated estate introduces no prescan suspects.
- **Pool prescan: 3,963 union pools (736 snapshot + 3,227 deep-linked-live), 13 suspects:**
  `mean30d-rail-breach` **12** (was 14 — live-feed churn, population 3,987→3,963) and `apy-rail-breach`
  **1 — first ever on the widened population** (was 0/3,987): `b81403b0` = uniswap-v3 GRVT-USDT (BSC),
  live-checked this tick: `apy` **1,043.19%** vs the 1,000% rail, `apyMean30d` 886.9%, TVL $92,781.
  Record-level; not promoted this tick; aggregate stays blocking (P0) per "undeterminable stays blocking";
  rotation clears ~2/tick. Promoted + rendered this tick: `18674a4f`, `81a51af8` — **no rendered finding
  on either** (live-only pools carry no `kpis`; same reconciliation as 08-04's two).
- **Occlusion lens: 6 findings, ALL the `.app-footer` class = item 221 (PARKED, PR #386):** grid-token
  1280 P0 (`calculate-yield-btn-new` under footer) + P1, grid-chain 1280 P0 + P1, grid-360 P1, grid-768 P1.
  **DELTA vs 08-04: the grid-360 duplicate `.theme-toggle` P0 is GONE — item 222's fix verified by the
  lens on the rendered app.** `dead-pool` surface covered with no finding — NOT claimed as clean: item 231
  documents this lens at 3/20 detection on that exact surface; absence is weak evidence there.
- Checks that ran: the scanner's implemented classes (number-sanity, dead-end, loading, page errors, i18n,
  dead-CTA, responsive/dark, occlusion) over the 83-surface rotation + the two fs prescans above. Claims
  here are scoped to those classes and populations only.

## Experiments — no window closed today (and none CAN close: no gate has opened)
117.1/.2/.3's legacy 14d calendar dates land today — under the 2026-08-04 Q1(b) regime their windows are
TRAFFIC-GATED and have never opened (real exercised pool-detail events 14d = 3, need ≥30); they stay
SHIPPED (measuring) until gate-open or the 60d backstop (~09-16..18). Same logic holds for every item on
yesterday's "next" list (124→08-06, 125→08-07, 129→08-08, 045/051/065/148→08-09..12, 180/182→08-13,
190/191/192→08-14, 196→08-15, 197-207/210/213→08-15..16) — those dates are now backstop bookkeeping, not
closures. Human-owned GSC reads: 013 ~08-23, 211 ~09-13.

## Gate re-check (§4)
229 visuals leg GATED(225 SHIPPED): 225 is IN_REVIEW behind the human screenshot gate (PR #393) → unmet,
stays GATED. The ≥30-real waitlist and pool-detail gates: 0 real events this tick → unchanged.

## In-flight truth (synced to BACKLOG on main this tick — the rows read stale READY)
224 **PARKED** at the 3-attempt budget, PR #392 open — human call to merge; 227/228 dep-blocked behind it,
so the agent-first chain's root is human-gated. 225 **IN_REVIEW**, PR #393 — human pre-merge screenshot
gate (Q2b carve-out). 221 **PARKED** verifier FAIL ×3, PR #386 — do not merge as-is. 226 **SHIPPED**
2026-08-05 (PR #394 merged; prod-verified this tick). Older human-gated PRs still open: #332 (177, rail
relaxation), #331 (176), #322 (166), #316 (161), #309 (Pendle direction).

## Diff vs 2026-08-04
1. Guardrail streak 22 → **23** clean prod days (control 7th); north star 0 (28th day); leg (A) 0 new.
2. 08-03's 92-session burst confirmed one-day: 08-04 = 8 (6 residual replays), 08-05 = 0 so far.
3. **223 verified effective on prod** (5/5 files 404) and its replay tail classified + playbook updated.
4. **222 verified fixed by the occlusion lens** (duplicate theme-toggle P0 absent from findings).
5. **226 verified live** (130-`<loc>` head sitemap, lastmod 08-05) — and its close-out's open class
   ticketed as **232** (head-page depth work, 7.8, READY, spec written).
6. `apy-rail-breach` 0 → **1** (GRVT-USDT, 1,043% vs the 1,000% rail — record-level, rotating to render).
7. Spec written for **231** (occlusion-lens reliability, 8.5 — top takeable READY row; had none).
8. Max id re-read off `origin/main` before writing: **231** → today's new item is **232**.
