# Signals snapshot — 2026-08-01 (heartbeat, live `Mixpanel` connector)

Project: defigarden (4042048) · org Equitee (2885044) · prod filter `$current_url contains www.defi.garden`.
Queries this run: prod 14d aggregate `3c0c0278`; prod daily 14d `e9f5f2ab`; unfiltered control 14d
`6f596bdb`; `pool_click` by `source` 30d `04dfbd65`; prod funnel events 30d `01e7b11b`; prod
page_view-by-URL 7d `284903b8`; prod page_view by `src` 14d `d73b192e`.

## HEADLINE: **second consecutive zero-blocking tick, and the strongest one yet — but the audit that produced it has never looked at half the estate it claims to cover.**
Every gate ran green: static prescan `2186/0`, text surfaces `2/0`, i18n `543/0`, `poolLinkLiveness`
**3,696 ids / 0 dead** (its first perfectly clean run), 60 surfaces rendered — more than double
yesterday's 29, which is item 192's widened budget arriving as promised. The one finding is the known
`201e5f6e` `mean30d` breach, auto-downgraded by 171 for a 4th day.
**Then the coverage question, asked one level up from 196's:** `listLeafPages()` is only ever called with
`'tokens'` and `'chains'`. The **2,186-page `/ko/` estate — byte-for-byte the same size as the EN estate,
2,215 `<loc>`s of it submitted to Google — has never been scanned by any signal, cheap or rendered.**
196 gave the static rotation memory; nobody asked what population it was remembering.

North star `pool_click{source ∈ garden_cta, protocol_link}` = **0** in 14d (**24th** straight day).
Guardrails clean — prod `error_occurred` **0 for 19 straight days**, and **0 in the unfiltered control too**.

## Real prod trend (daily totals, `e9f5f2ab`)
| date | session_start | error_occurred | pool_view | pool_click |
|------|----|----|----|----|
| 07-18 | 7 | 0 | 1 | 0 |
| 07-19 | 6 | 0 | 3 | 1 |
| 07-20 | 17 | 0 | 0 | 0 |
| 07-21 | 6 | 0 | 0 | 0 |
| 07-22 | 4 | 0 | 0 | 0 |
| 07-23 | 7 | 0 | 0 | 0 |
| 07-24 | 3 | 0 | 0 | 0 |
| 07-25 | 4 | 0 | 0 | 0 |
| 07-26 | 6 | 0 | 2 | 0 |
| 07-27 | 3 | 0 | 0 | 0 |
| 07-28 | 8 | 0 | 0 | 0 |
| 07-29 | 5 | 0 | 0 | 0 |
| 07-30 | 6 | 0 | 0 | 0 |
| 07-31 | **4** (finalized, was "0 so far") | 0 | 0 | 0 |
| 08-01 | 0 so far | 0 | 0 | 0 |

`error_occurred` is **absent from the prod result set** across the whole 14d window — zero, not low.
**Control (`6f596bdb`):** the unfiltered run also returns `error_occurred` **absent**, third consecutive
day. The guardrail claim holds with AND without the prod filter. Keep the filter anyway — playbook
`product-audit.md` class 13; 07-29 is the worked example of what dropping it costs.
Prod session totals 14d: **86** (unfiltered **92** — the 6-session delta is localhost/preview).
**Insufficient data** for every funnel claim (≥30 rule).

## North-star surface (`04dfbd65`, 30d)
- `pool_click` 30d = **1**, `source=undefined` (the pre-123 07-19 event). **Zero CTA clicks on either entry
  path, ever.** Unchanged for a 7th day.
- Prod funnel events 30d (`01e7b11b`): `plan_saved` **5** · `search_success` **4** · `plan_created` **3** ·
  `share_link_opened` **1** · `share_link_created` **0** · `tend_reminder_added` **0** ·
  `waitlist_submitted` **0**. Identical to 07-30 and 07-31 — no new funnel events in 48h.

## Prod page_views by URL, 7d (`284903b8`, total 11)
One event each: `/chains/linea`, `/chains/avalanche`, `?token=BUIDL`, `?token=BUIDL&lang=ko`,
`/tokens/buidl`, `/tokens/usdc`, `/tokens/slisbnb`, `/tokens/eth`, `?token=ICP&poolTypes=Yield%20Farming`,
`?chain=Popular&minTvl=1000000&pool=747c1d2a…`, `?token=USDC&chain=Popular&pool=4438dabc…`.
- **URL-provenance check (playbook class 9), run this tick: clean.** Zero slugs pure-numeric or
  date-shaped. **The set is byte-identical to yesterday's 11** — same URLs, same counts, no new arrivals
  and none aged out. **No new provenance suspects; the 148 class stays closed.**

## Distribution check (`d73b192e`, the cheapest true test per LEARNINGS 2026-07-27)
Prod `page_view` by `src` over 14d: **one row, `undefined` = 47**. Zero tagged arrivals, unchanged for the
**20th day**. The 069 spotlight packs remain unposted; every distribution hypothesis stays untestable by
construction.

## Live verification of the overnight ships (190–196, this tick, read against the shipped code)
| check | claim | verified now |
|---|---|---|
| **190** — KO value-honesty gate | new `prescanI18n` signal | `i18n` block live in the findings JSON: `scanned 543, suspectCount 0, allowlistSize 24` |
| **190** — the allowlist is the design decision | brand/acronym only, each with a reason | **all 24 entries carry a one-line reason and every one is a brand name or an acronym** — no ordinary prose allowlisted into silence. True negative, checked because `product-audit.md` 5(3) says this is where the check rots |
| **192** — widened render budget | `DEFAULT_POOL_SAMPLE` raised past 191's ceiling | `surfacesCovered` **29 → 60** this tick, wall-clock inside the cap, `status: OK` |
| **195** — non-EVM address chips | truncate base58, not just `0x` | `PoolDetail.js:104-113` now branches `0x` / Solana base58 / Tron `T…` |
| **196** — static rotation memory | persisted never-audited-first state | new `product-loop-kit/signals/audit-static-rotation.json` written this run; rotation state is read and advanced |
| **184** — level-2 `?pool=` liveness | drift-budgeted gate | **3,696 ids checked, 0 dead, 0 pages affected** — its first *perfectly* clean run (07-31: 1 dead / 3,707) |

## §2b product audit — `audit-app.js` (60 surfaces, 1 finding, **0 blocking**)
`node audit-app.js` → `status: "OK"`, `generatedAt` **2026-08-01T07:07:27.678Z** (this tick),
`surfacesCovered` non-empty (**60**) — both staleness gates from the playbook checked before claiming clean.
Playwright 1.61.1 resolved from a fresh `npm ci`; pools from a **live fetch of 15,844 pools**, not the
committed snapshot. Measured at `44f1c4013`, the current `origin/main` tip — **no build loop lapped this
tick**, unlike 07-31.
- **Static prescan clean for a third consecutive day: `scanned 2186, suspectCount 0`** across all six
  signals (`junk-slug`, `zero-yield-claim`, `link-target-integrity`, `broken-number-literal`,
  `absurd-magnitude`, `pool-link-liveness`).
- `poolLinkLiveness`: **3,696 ids, 0 dead, 0 pages affected**, allowance 36.96 → `ok: true`.
- `prescanTextSurfaces`: 2 scanned, **0** across all five signals.
- `prescanI18n` (190, first heartbeat-observed run): **543 keys, 0 suspects, allowlist 24**.
- `poolPrescan`: **737** pools, 1 `mean30d-rail-breach` (`201e5f6e`), **auto-downgraded P1 → P2 by 171's
  reconciliation** for the fourth consecutive day. Working as specced.
- **Zero P0/P1 findings, second consecutive tick.**

## NEW FINDING → 197 (P2/READY 6.0): the audit has never enumerated half the estate it audits
`audit-app.js` collects its static population with exactly two calls — `listLeafPages('tokens')` and
`listLeafPages('chains')` (`:2016-2017` for the rendered rotation, `:1398` for `prescanStaticPages()`).
There is no third call. Measured on this checkout:
- **EN population: 2,186 leaf pages. KO population: 2,186 leaf pages** (`ko/tokens/*.html` +
  `ko/chains/*.html`). Exactly half the generated HTML estate is unreachable by **every** signal — the
  cheap `fs`+regex prescan *and* the rendered rotation alike.
- **These are real, distinct, indexed pages**, not aliases: `ko/tokens/usdc.html` differs from
  `tokens/usdc.html` (18,698 B vs 19,887 B), carries `<html lang="ko">` and real Hangul body copy, and
  **2,215 `<loc>`s** of the KO estate are submitted to Google (`sitemap-token-pages-ko.xml` 2,127 +
  `sitemap-chain-pages-ko.xml` 88).
- **The `surfacesCovered` list is the proof, not an inference:** 60 surfaces, of which 7 are
  `static-page*` (`tokens/{shit,wtao,cscbusdc,kysol}`, `chains/{solana,ethereum}`, + the anchor).
  **Zero contain `/ko/`.** The three `-ko` surfaces that DO appear (`pool-detail-ko`, `planner-ko`,
  `plan-bloom-ko`) are *app* routes reached via `?lang=ko` — they say nothing about the static estate,
  and their presence is exactly why this gap reads as covered at a glance.
- **Honest sizing, measured not assumed.** I ran the real `prescanStaticPages({pages: koLeaves})` over all
  2,186 KO pages: **0 suspects**. The KO half also emits **the same 42,604 links and the same 3,696
  distinct `?pool=` ids** as the EN half, so 184's liveness verdict genuinely transfers. **So this is a
  coverage finding with no known live defect behind it today** — the exposure is the *render-only* classes
  (`dead-cta`, i18n leaks, 360px clipping, console errors) reaching 0 of 2,186 KO pages, forever.
- **The defect history is not hypothetical, though: item 190 was a KO-only defect on these exact pages**
  (two English strings closing 2,201 KO landers) and it was found by a `translations.js` dictionary diff —
  a completely different checker. The estate scan could never have found it, and still could not today.
- This is `detector-signal-coverage.md`'s question on a **third axis**: 167/183 fixed *selection*, 191/192
  and 196 fixed *throughput and memory* — both inside a population that was never questioned. Nobody asked
  **"is the population itself complete?"**

## NEW FINDING → 198 (P2/READY 5.0): 190's i18n gate implements half of its own playbook predicate
`product-audit.md` class 5(2), written 07-31, specifies the rule verbatim: *"A KO value byte-identical to
its EN value **AND containing no Hangul** … is untranslated."* The shipped gate tests only the first
conjunct. Proven by mutation probe against the real exported `prescanI18n({dict})` this tick:

| EN | KO | untranslated? | gate verdict |
|---|---|---|---|
| `Powered by` | `Powered by` | yes | **flagged** ✅ |
| `Powered by` | `Powered by ` (one trailing space) | yes | **clean** ❌ |
| `Powered by` | `Powered by the DefiLlama feed` | yes | **clean** ❌ |
| `Powered by` | `제공` | no | clean ✅ |
| `$100` | `$100` | no (a bare figure) | **flagged** — needs an allowlist entry ❌ |

- **The consequence is worst in the exact case the gate exists for.** It is named
  `I18N_IDENTICAL_ALLOWLIST` and keyed on *sameness*, so **the day someone rewords an EN string without
  touching KO, the pair stops being identical and the gate goes silent** — precisely when the KO value has
  become stale English. A drift detector that switches off on drift.
- **No live instance today, stated plainly.** Full flattened dictionary scan this tick: 543 keys,
  **24 KO values with zero Hangul, 24 byte-identical pairs, and the two sets are the same 24** — all
  allowlisted, all genuine brands/acronyms. `no-Hangul AND not-identical AND not-allowlisted = 0`. So this
  is a **gate-coverage** item, not a repair, and the fix ships with a positive control or it proves nothing.
- Sibling of `detector-signal-coverage.md`'s core pattern in an unusually pure form: the signal set was not
  drawn from the last bug, it was drawn from **half the rule the team had already written down**.

## Checked and CLEARED — no ticket (recorded so the next tick does not re-derive)
- **190's allowlist is not a relaxation.** 24 entries where 07-31 measured only 10 no-Hangul values reads
  like allowlist inflation. It is not: the shipped predicate is *identical-value*, which legitimately
  catches acronyms (`TVL`, `APY`, `LP/DEX`, `RWA`) that the no-Hangul framing also catches, plus brand rows.
  Every entry carries a reason and every reason is a brand or an acronym. **Checked because the playbook
  says this is where the check rots — clean.**
- **`listLeafPages()` also excludes `tokens/az/` and `ko/tokens/az/`** (it filters `e.isFile()`). Those are
  the A–Z index directories, not content leaves; the exclusion is correct and deliberate (`audit-app.js:171`
  documents it). Not part of 197.
- **The 7d URL set is byte-identical to yesterday's.** No new crawler arrivals, nothing aged out — so the
  provenance check had no new input, and "clean" this tick means "unchanged", which is weaker than 07-31's.
  Said explicitly rather than reported as a fresh pass.

## Data layer — healthy
Snapshot `generatedAt` this tick, **737 pools** (736 yesterday).
- **0** pools with total APY > 1000%; **0** non-finite KPIs; `mean30d` breach set unchanged at 1
  (`201e5f6e`, known-and-handled, auto-downgraded by 171 for a 4th day).
- Live DefiLlama fetch succeeded in-session: **15,844 pools**, used as an independent oracle, so the
  liveness and coverage numbers above do not rest on the committed snapshot.

## Experiments — no window closed today
- Windows still open: 117.1/117.2/117.3 → 08-05; 124 → 08-06; 125 → 08-07; 129 → 08-08; 045/051/065/148 →
  08-09..08-12; 180/182 → 08-13; 190/191/192 → 08-14; 196 → 08-15; 013 GSC leg → human ~08-23.
- 195 and 196 shipped 08-01 (overnight); windows not yet due.

## Diff vs 2026-07-31
1. **Second consecutive zero-blocking tick, on 2× the render budget.** 192's widening landed as specced:
   `surfacesCovered` **29 → 60**, still `status: OK` inside the wall-clock cap, still 0 P0/P1.
2. **184's gate went from 1 dead to 0 dead** (3,707 → 3,696 ids checked) — its first perfectly clean run.
3. **197 found — the audit's population, not its signals or its rate, is half-size.** 2,186 of 4,372
   generated pages have never been scanned by anything; the KO half is 2,215 `<loc>`s of indexed surface.
   Measured 0 suspects on the cheap signals, so it is coverage exposure, not a live bug — but 190 was a
   KO-only defect on those exact pages, found by a different checker entirely.
4. **198 found — 190's gate tests `identical`, its own playbook says `identical AND no-Hangul`.** Mutation-
   probed: an untranslated string one space away from its EN twin passes clean, and the gate goes silent
   exactly when EN is reworded without KO. 0 live instances today.
5. **190's allowlist audited and cleared** — 24/24 entries are brands or acronyms with stated reasons.
6. Guardrail streak 18 → **19** clean prod days, clean in the unfiltered control for a third day.
7. Data layer: pools 736 → **737**; zero out-of-rail totals; zero non-finite KPIs; 171 auto-downgrade
   holding for a fourth day.
8. North star (0, **24th** day) and the `src` breakdown (`undefined` = 47, **20th** day) unchanged. Prod
   `page_view` 7d **11 → 11, the identical URL set** — no new crawler arrivals for the first time.
9. Funnel events 30d **unchanged for a second consecutive day** (48h with no new funnel event of any kind).
10. **No ID collision this tick** — `origin/main`'s max backlog ID re-read immediately before writing rows
    (07-31's lesson, applied): 196, so today's findings are **197** and **198**. The overnight build loops
    shipped 190→196 and none of them landed mid-tick.
