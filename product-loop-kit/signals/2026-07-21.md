# Signals snapshot — 2026-07-21 (heartbeat, live `Mixpanel` connector)

Project: defigarden (4042048) · org Equitee (2885044) · via `Mixpanel` connector (uppercase — live this run; lowercase `mixpanel` still needs OAuth). Prod filter = `$current_url contains www.defi.garden`. `Get-Business-Context` = "No business context configured" (unchanged). Queries this run: prod daily line 14d `query_id 5a465c6f`; 07-20 spike utm `023e7df5`, referrer `78886c5b`, unique users `0421a6d8`, pages `5d111b95`.

## HEADLINE: 07-20 finalized at 16 prod sessions (biggest single day) but ALL bot/crawler-shaped — zero engagement. North star still 0. Build loop STARVED: 117.3 (#271) shipped the LAST 117 surface; no buildable READY item remains.
North star `waitlist_submitted` = **0** (structurally absent from the 21-event lexicon — no card-funnel event has ever fired). **This run's material work: audited the north-star pipeline end-to-end (see below) — it is SOUND; zeros = no usage, not breakage.** The 07-20 "spike" is crawler noise, not distribution. 07-21 empty so far.

## Lexicon — 21 events (unchanged vs 07-20)
`Get-Events` = **21** = 19 product + 2 Mixpanel auto (`$session_start`/`$session_end`). Still structurally absent: `waitlist_submitted`, `waitlist_opened`, `share_link_created`, `garden_cta`. → **North star `waitlist_submitted` = 0 from any host, ever.**

## NORTH-STAR PIPELINE AUDIT (this run — rules out a bug; bug>experiment demanded the check)
Traced the full `waitlist_submitted` path in source (like 002 did for share):
- `planner.js:1796 submitWaitlist(ev)` → on valid email + submit → `planner.js:1846 Analytics.trackWaitlistSubmitted({success:true,…})` (also fires success:false on formspree/network error, 1852/1859).
- `analytics.js:382 trackWaitlistSubmitted` → `this.track('waitlist_submitted', …)` — **event name matches the north-star metric definition exactly.**
- Waitlist UI is real and reachable: `translations.js:547-583` (CTA `ctaWaitlist`, microcopy, 3 pitch variants A/B/C, modal step-1/step-2), `.gp-waitlist-*` modal rendered `planner.js:2709+`/`3943+`, opened from the bloom checkout CTA (`test_waitlist_microcopy.js` drives it live).
- **Verdict: pipeline SOUND end-to-end. The zero is NO USAGE, not breakage.** To fire once a user must: land → complete planner → reach bloom → click checkout → open waitlist modal → enter valid email → submit. At ~2–13 organic sessions/day and 0–1 `plan_created`/wk, that depth is effectively never reached. Blocker = traffic + funnel depth (both human-owned), NOT code.

## Real prod trend (`$current_url contains www.defi.garden`, daily `$session_start` total, `query_id 5a465c6f`)
| date | $session_start | plan_created | search_success | error_occurred | share_link_opened | pool_click | plan_saved |
|------|----|----|----|----|----|----|----|
| 07-11 | 13 | 0 | 0 | 0 | 0 | 0 | 0 |
| 07-12 | 10 | 0 | 1 | 0 | 0 | 0 | 0 |
| 07-13 | 13 | 0 | 0 | 1 | 0 | 0 | 0 |
| 07-14 | 5 | 0 | 0 | 0 | 0 | 0 | 0 |
| 07-15 | 7 | 0 | 0 | 0 | 0 | 0 | 0 |
| 07-16 | 6 | 0 | 0 | 0 | 0 | 0 | 0 |
| 07-17 | 2 | 0 | 0 | 0 | 0 | 0 | 0 |
| 07-18 | 8 | 0 | 0 | 0 | 0 | 0 | 0 |
| 07-19 | 3 | **1** | **2** | 0 | **1** | **1** | **1** |
| 07-20 | **16** | 0 | 0 | 0 | 0 | 0 | 0 |
| 07-21 | 0 so far | 0 | 0 | 0 | 0 | 0 | 0 |

## The 07-20 "spike" — bot/crawler, NOT distribution (four confirming pulls)
- **16 sessions = 16 unique users** (`0421a6d8`) → one session each, no repeat visitors.
- `utm_source` breakdown (`023e7df5`): **100% `undefined`** — not a 069 spotlight click, not any campaign.
- `$referrer` breakdown (`78886c5b`): **100% `undefined`** — no referring page at all.
- Pages hit (`5d111b95`): spread thin across the SEO surface — `/` ×6, `/chains` ×2, `/tokens` ×2, `?chain=Abstract` ×3, `?token=USDC` ×1, `?token=BTCB` (Lending) ×1, `?token=22OCT2026` (Lending) ×1.
- **Zero engagement events** from any of them (0 plan_created/search/share/pool_click/plan_saved on 07-20).
- Read: 16 distinct one-page, no-referrer, no-utm, non-interacting visits fanned across sitemap URLs = **crawler/monitoring sweep signature**, not real users. Do NOT read as distribution traction; do NOT act on it. (Contrast 07-19's single genuinely-engaged organic session.)

## Guardrails — clean
- `error_occurred` (prod): **0 on 07-14 → 07-20, 0 so far 07-21.** The 07-13 `ReferenceError: translations is not defined` (082, shipped 07-14) has NOT recurred — window to 2026-07-28 clean.
- Analytics-URL SEO lifeline: `?token=/?chain=/?pool=` render path intact (117.3 #271 touched only `planner.js curatePools` comparators; trust rails byte-untouched per verifier PASS HIGH 7/7).
- Planner conversion: 0 prod `plan_created` on 07-20 across 16 (bot) sessions — uncomputable as an honest rate → insufficient data.

## Sample discipline
Real-prod engaged n over 14d: ONE session (07-19). 07-20's 16 are bot-shaped (excluded from any conversion read). **Insufficient data** for any funnel-step rate, retention, or cohort claim. Qualitative only. No ≥30-event claim available.

## Experiments — nothing to close today
Earliest open window = 005/007/008 → **2026-07-24** (3 days out). 075 → 07-27; 061/065/068 → 07-26/27; 082 → 07-28; 114/115 → 07-29; 116 → 07-30; 101's 14d window still hasn't started (07-19 was the first prod share OPEN, not a bloom share ORIGINATION). No window ends 07-21. LEARNINGS.md unchanged. Compute-side: 117.1/117.2/117.3 Sharpe surfaces activate ~07-22 as pools cross ≥8 daily history points (dormant today).

## Backlog state — build loop STARVED (the 07-20 report's forecast has landed)
- **117.3 SHIPPED** (#271, 2026-07-20; verifier PASS HIGH 7/7) — planner "prefer steadier yield" Sharpe near-tie-break. This was the **LAST-in-sequence 117 surface** → the entire 117 line is now exhausted.
- **READY sweep (full backlog): the ONLY non-terminal READY row is 110**, and it is "READY (after 109)" — NOT truly buildable (needs 108's CF-D1 provisioning + 109's deploy, both human-gated; 108 = NEVER-list credentials). 100 rows SHIPPED, 3 BLOCKED (097, 108, 118), 1 gated-READY (110).
- **No autonomously-buildable grounded work remains.** Honest call: NOT manufacturing a new trust surface this run — the six prior heartbeats have each conceded such surfaces cannot move the north star, and the 117 line that supplied them is spent. Feeding the loop busywork would be dishonest bookkeeping.
- **119 added (DONE this run):** north-star pipeline verification — durable record so no future heartbeat re-investigates "why is waitlist_submitted 0" from scratch (002/010/118 diagnostic-row precedent).
- Distribution (069 spotlight packs ×3, ready + one-tap) remains **unposted** — human-owned, still the sole `waitlist_submitted` lever. Unmoved.
- **109 SHIPPED code** (#266); **DEPLOY pending human 108**. KPIs keep running on file-history until then.
- No existing-row scores changed (the 07-20 bot spike is not new evidence; the waitlist audit confirms the existing "no usage, not breakage" read).

## Diff vs 2026-07-20 (five pulls)
1. **07-20 finalized "0 so far" → 16 sessions**, but all bot/crawler-shaped (16 unique users, 100% undefined utm + referrer, thin sitemap-URL spread, zero engagement). Not distribution.
2. **117.3 SHIPPED (#271)** → the 117 line is exhausted; the build loop is now genuinely starved (only READY item 110 is human-gated).
3. **North-star pipeline audited SOUND end-to-end** (submitWaitlist → trackWaitlistSubmitted → `track('waitlist_submitted')`; UI reachable) — rules out a north-star bug; the zero is no-usage. New backlog row 119 (DONE) records it.
4. Guardrails unchanged: error 0 (082 holding, window to 07-28), SEO render path intact.
5. Three human questions still pending (SUBSCRIPTION_LADDER canonical set · 069 posting go/no-go · yield.garden mirror decision) — logged 07-14, not re-asked (Standing Decisions unchanged).
