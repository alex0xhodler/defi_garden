# Signals snapshot — 2026-07-19 (heartbeat, live `Mixpanel` connector)

Project: defigarden (4042048) · org Equitee (2885044) · via `Mixpanel` connector (uppercase — live this run; lowercase `mixpanel` still needs OAuth). Prod filter = `$current_url contains www.defi.garden`. `Get-Business-Context` returns "No business context configured" (unchanged) — read-only insights queries run directly (`query_id 087da415` prod daily line, `23e38393` all-host totals).

## HEADLINE: near-identical no-op — one real triage move: the build loop is unfed AGAIN, so 117.2 is spec'd
North star `waitlist_submitted` = **0** (structurally absent from the 19-event lexicon). Prod traffic flat (07-18 finalized at **7** sessions — a decent trickle day). Guardrails clean. The only material delta vs 07-18: **both READY buildables landed** — 117.1 SHIPPED (#268) and 118 GSC diagnosis SHIPPED (#269) — so the build loop is unfed once more (110 stays human-gated behind 108). Triage action: wrote `specs/117.2.md` (analytics "risk-adjusted" sort — the next-in-sequence surface 117.1's notes explicitly name) + added BACKLOG row 117.2. No prod scores changed (no new prod evidence).

## Lexicon — 19 events (unchanged)
`Get-Events` = **19** = 17 product events + 2 Mixpanel auto (`$session_start`/`$session_end`). Still structurally absent: `waitlist_submitted`, `waitlist_opened`, `share_link_created`, `pool_click`, `garden_cta`.
→ **North star `waitlist_submitted` = 0 from any host, ever.** No card-funnel event has ever fired (distribution-gated; plumbing verified sound 07-13).

## Real prod trend (`$current_url contains www.defi.garden`, daily total, `query_id 087da415`)
| date | session_start | plan_created | search_success | error_occurred | share_link_opened |
|------|---------------|--------------|----------------|----------------|-------------------|
| 07-11 | 10 | 0 | 0 | 0 | 0 |
| 07-12 | 16 | 0 | 1 | 0 | 0 |
| 07-13 | 11 | 0 | 0 | 1 | 0 |
| 07-14 | 5 | 0 | 0 | 0 | 0 |
| 07-15 | 5 | 0 | 0 | 0 | 0 |
| 07-16 | 7 | 0 | 0 | 0 | 0 |
| 07-17 | 2 | 0 | 0 | 0 | 0 |
| 07-18 | **7** | 0 | 0 | 0 | 0 |
| 07-19 | **0 so far** | 0 | 0 | 0 | 0 |

→ Flat ~2–16/day trickle. **07-18 finalized at 7 sessions** (yesterday's pull read "0 so far") — mid-range, not a signal. `plan_created` prod = **0 since 07-10**. `search_success` prod = 1 total (07-12). **No prod `share_link_opened` ever** (share metric absent from the prod-filtered result → 0). 07-19 has no prod events yet — acting on an empty partial day breaks the minimum-sample rule.

## Contamination check (`query_id 23e38393`, all-host, last 10 days)
All-host totals: session_start **488**, plan_created 17, share_link_opened 13, error_occurred 4. Prod-filtered over the same window: session_start ~30, plan_created 0, error_occurred 0, share_link_opened 0. The all-host share/plan counts are **non-prod** (dev/preview/bot hosts), fully isolated by the `$current_url` filter; prod stays flat. All-host session_start crept 481→488 (dev/preview noise). Not a prod signal, not actionable.

## Guardrails — clean
- `error_occurred` (prod): **0 on 07-14/15/16/17/18, 0 so far 07-19.** The 07-13 `ReferenceError: translations is not defined` (082, shipped 07-14) has **not recurred** — window to 2026-07-28 clean so far.
- Analytics-URL SEO lifeline: `?token=`/`?chain=`/`?pool=` render path intact (117.1 touched only PoolDetail render + translations; app.js byte-unchanged, verifier-confirmed; #269 was a docs-only GSC diagnosis).
- Planner conversion: uncomputable honestly at this sample (0 prod `plan_created` since 07-10) → insufficient data, qualitative only.

## Sample discipline
Real-prod n: ~30 sessions over the last 10 days of ~2–16/day trickle, `plan_created` 0/wk since 07-10, everything downstream 0. **Insufficient data** for any funnel-step rate or retention/cohort claim. Qualitative only. No ≥30-event claim available.

## Experiments — nothing to close today
Earliest open window = 005/007/008 → **2026-07-24** (5 days out). 075 → 07-27; 061/065/068 → 07-26/27; 082 → 07-28; 114/115 → 07-29; 116 (report-mode share) → 07-30; 101 → 14d from first real share (no prod shares yet). No window ends on 07-19. LEARNINGS.md unchanged.

## Backlog state — build loop RE-FED via 117.2 (change vs 07-18)
- **117.1 SHIPPED** (#268, 2026-07-18; verifier PASS HIGH 6/6). Pool-detail Sharpe annotation live inside the 088.1 rate-track-record block; renders dormant until pools accrue ≥8 daily history pts (~07-22) then lights up.
- **118 SHIPPED** (#269, 2026-07-18) — GSC "Excluded by noindex" diagnosis, human-forwarded, docs-only.
- **NEW: 117.2 spec'd + backlogged** (score 6.6, HIGH, READY) — analytics-app "risk-adjusted (Sharpe)" sort option, the next-in-sequence surface 117.1's notes name (117.2 = analytics sort; 117.3 = planner blend, still later). Adds a `sortBy === 'sharpe'` branch to the three existing sort code paths in `app.js` (~L1822/1893/1974) + a sort toggle; ranks by `pool.kpis.apySharpe` **within the sane group only** (anomalous demotion/flag/High-risk untouched — trust rail), null-Sharpe pools sort last (tie-break TVL). Autonomously buildable NOW: value exists (117 compute leg shipped), sort is a no-op until data accrues ~07-22 then activates — same "land now, lights up as data accrues" pattern. No credentials, no NEVER-list surface.
- **110 (read-from-DB, MEDIUM 5.5)** stays "READY (after 109)" but NOT actually buildable — repoints `compute-kpis.js` at `${HISTORY_ENDPOINT}` which does not exist until 108's CF-D1 provisioning + 109's deploy land. Human-gated.
- **109 SHIPPED code** (#266); **DEPLOY pending human 108** (`wrangler login`/`d1 create`/`deploy`/`secret` — NEVER-list credentials). KPIs keep running on the file-history until then.
- Distribution (069 spotlight packs ×3, ready + one-tap) remains **unposted** — human-owned, and still the sole north-star lever. It has not moved. **Structural note: every heartbeat since 07-15 has fed the loop a small pool-detail/analytics trust surface because that's the only autonomously-buildable, grounded work available; none of it can move `waitlist_submitted` — that needs traffic, which needs distribution (human-owned) or the card-funnel front door to be driven.**
- No BACKLOG scores changed on existing rows (a finalized-7 + empty partial day is not new signal).

## Diff vs 2026-07-18 (three pulls)
1. 07-18 finalized 0-so-far → **7** sessions; 07-19 empty so far. 07-17 confirmed final at 2. Every other prod number identical.
2. Lexicon 19, north star 0 — unchanged.
3. Guardrail error 0, 082 holding (window to 07-28) — unchanged.
4. **Backlog: 117.1 + 118 both consumed the buildable queue → build loop RE-FED by spec'ing 117.2** (analytics risk-adjusted sort). 109 SHIPPED code (deploy human-gated via 108), 110 still human-gated. No existing-row score changes.
5. All-host session_start noise essentially flat (481→488) — dev/preview/bot, fully filtered out of the prod line.
6. Three human questions still pending (SUBSCRIPTION_LADDER canonical set · 069 posting go/no-go · yield.garden mirror decision) — logged 07-14, not re-asked (Standing Decisions unchanged).
