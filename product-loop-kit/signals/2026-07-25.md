# Signals snapshot — 2026-07-25 (heartbeat, live `Mixpanel` connector)

Project: defigarden (4042048) · org Equitee (2885044) · prod filter `$current_url contains www.defi.garden`. Queries this run: prod daily line 14d `query_id a76c8366`; pool_click-by-source `79576983`; last-2d sessions by URL `862c431f`; page_view-by-URL 14d `7e2b7b9b` (UNFILTERED — includes localhost test noise, read prod rows only).

## HEADLINE: north star still **0** (expected-zero, ~0 traffic); guardrails clean 12th straight day; TEN experiment windows closed today (020/021/022/039 MOVED on functional evidence, 013/014/018/019/024/025 INCONCLUSIVE zero-traffic); audit tick found 1 real product finding (140: snapshot fast path dead in prod) + first-ever prod page_views on the static SEO surface.
North star `pool_click{source ∈ garden_cta, protocol_link}` = **0** in 14d (`79576983`: the window's single pool_click, 07-19, still carries `source=undefined` — pre-123 wire). Denominator `pool_view` 14d = 4 (07-18 ×1, 07-19 ×3, all card_click; `url_direct` SEO landings still 0). CTR uncomputable.

## Real prod trend (daily totals, `a76c8366`)
| date | session_start | plan_created | search_success | error_occurred | share_link_opened | pool_click | pool_view | plan_saved |
|------|----|----|----|----|----|----|----|----|
| 07-19 | 6 | 1 | 2 | 0 | 1 | 1 | 3 | 1 |
| 07-20 | 17 | 0 | 0 | 0 | 0 | 0 | 0 | 0 |
| 07-21 | 6 | 0 | 0 | 0 | 0 | 0 | 0 | 0 |
| 07-22 | 4 | 0 | 0 | 0 | 0 | 0 | 0 | 0 |
| 07-23 | 7 | 0 | 1 | 0 | 0 | 0 | 0 | 0 |
| 07-24 | **3** (finalized, was "0 so far") | 0 | 0 | 0 | 0 | 0 | 0 | 0 |
| 07-25 | 0 so far | 0 | 0 | 0 | 0 | 0 | 0 | 0 |

07-24 finalized at 3 sessions — same mix as 07-23 (`862c431f`): INFINIFIUSDC human-test residue, AVICI✨ crawler, `/chains/plasma` static-crawl, `/` directs. Nothing engaged. Real engaged sessions in 14d: still the one 07-19 cluster. **Insufficient data** for every funnel claim (≥30 rule).

## FIRST prod page_views on the static SEO surface (`7e2b7b9b`, prod rows)
`/tokens/btc` 1 · `/tokens/infinifiusdc` 1 · `/tokens` hub 2 · `/chains` hub 2 · `/chains/ethereum` 1 · `/chains/aptos` 1 · `/chains/plasma` 1 ≈ **9 events — the first page_views this surface has EVER recorded** (039/044/045 instrumentation demonstrably firing in prod; crawl/first-trickle volume, not yet organic-human). Also observed: crawler-invented param URLs `?chain=robinhood|robinhood-chain|Popular&minTvl=1000000` (×4 each) — rendered-verified today: all resolve honestly (see audit); NOT from our pages (our links use `?chain=Robinhood%20Chain`, verified in generated HTML).

## Guardrails — clean
- `error_occurred`: 0 every day 07-14 → 07-25 (12 straight). 082 recurrence window (to 07-28) clean.
- SEO lifeline: `?token=USDC` renders 9 cards (suite green); daily sitemap CI green (06:32 run: sitemap valid, IndexNow 200).
- Planner conversion: no planner sessions since 07-19 → uncomputable.

## §2b product audit — coverage log (playbook product-audit.md)
Rotation this tick: regression sweep on ALL freshly-shipped pool-detail items + static SEO surface + crawler-hit URL classes. Rendered fixture-routed Playwright + committed snapshot (fresh-meta fixture — see playbook update).
- **Regressions green (rendered)**: 136 mobile CTA clip (`test_mobile_cta_clip`), 137 KO money honesty (`test_ko_pool_money_honesty`), 129 projection lead (`test_projection_lead`), 123/124 north-star CTAs (`test_northstar_cta_fires`), 139 archetype checkout (`test_plan_checkout_cta`), i18n pages (`test_i18n_pages`), canonical unit trio (planner/protocol/qualifier). Zero page/console errors on all driven surfaces.
- **Static SEO pages rendered** (chains/plasma, tokens/usdc, both hubs, stories/kevin @360px): CLEAN — no number-sanity hits (initial `$X.Y` flags were false-positive `$11.2K/$273.3M` abbreviations), no h-scroll, freshness stamps present, analytics fires on all 5.
- **Crawler-invented URLs rendered**: `?chain=robinhood`/`robinhood-chain` → honest empty state + **noindex verified injecting** (first rendered verification of 012 on token-empty AND chain-empty; poll ≥10s — 4s reads false-negative); `?chain=Popular` → 581 results; `?chain=Popular&minTvl=1000000` → 581 when live endpoint reachable; on live-fetch failure the page shows honest "Loading live pools…", never a false "No yields found".
- **FINDING → 140 (P1/READY 7.0)**: `SNAPSHOT_MAX_AGE_MS = 15 min` (app.js:1086) vs real bake cadence ~1.5–3h (git log meta: 8 bakes/24h; comment's "intended ~5-min cadence" never materialized — the 109 poller writes D1, not the repo file). Snapshot-first (059) is eligible ~10–15% of the day; the rest of the time every visitor — including `?pool=`/`?token=` SEO landers, the north-star surface — pays the slow live DefiLlama fetch. Rendered repro: committed 42-min-old snapshot fell through to the live path.
- **FINDING → 141 (P3/READY 3.5)**: CI's Google sitemaps-ping step is dead (deprecated 2023, returns 404 in today's log) yet the step echoes "✅ Search engines notified" — misleading green.
- **022 closure evidence (live CI log, run 30147683111)**: "📡 Submitting 2102 URL(s) to IndexNow… ✅ IndexNow responded 200".

## Data layer — healthy
Today's bake (06:32): 737 pools, **316 non-null `kpis.apySharpe`** (314 yesterday), **0 with |Sharpe| > 50** (range −45.9..43.2), 392 flat pools honestly nulled by 122's stdev floor, historyPoints 11→12 (daily accrual working), 0 pools APY >1000%. 122 verified holding second day.

## Experiments — TEN windows closed today (written to LEARNINGS.md)
- **MOVED (functional goal verified)**: 020 (funnel measurable — results_count real, pool_view + source live), 021 (2,075 real /tokens/ pages live via CI + sitemap wired), 022 (IndexNow 2,102 URLs, HTTP 200, live log), 039 (static-surface instrumentation firing in prod — today's first-ever page_views are the proof).
- **INCONCLUSIVE (zero-traffic, kept)**: 013 (GSC-measured, human window to ~08-23), 014 (traffic leg — first trickle only), 018 (search_success n=4 vs ≥30), 019 (url_direct landings 0), 024 (share_link_created 0 — same class as 005/007/008), 025 (garden_cta 0).
- Remaining windows: 041/048/049 → 07-26; 061-075 cluster → 07-26/27; 082 error-recurrence → 07-28; 088.1 → 07-28; 094/114/115 → 07-29; 116 → 07-30; 124 → 08-06; 125 → 08-07; 045 → 08-09.

## Diff vs 2026-07-24
1. Ten windows closed (first MOVED closures ever recorded — 020/021/022/039 on functional evidence).
2. First prod page_views on the static SEO surface (9 events) — instrumentation + crawl both real now.
3. New finding 140 (snapshot fast path dead in prod) — the only product-quality item the audit surfaced; yesterday's P0 class (136/137) all shipped + regression-verified green today.
4. 07-24 finalized at 3 sessions (was 0-so-far). Everything else flat-zero; guardrails clean.
