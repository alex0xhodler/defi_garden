# Signals snapshot — 2026-08-03 (heartbeat, live `Mixpanel` connector)

Project: defigarden (4042048) · org Equitee (2885044) · prod filter `$current_url contains www.defi.garden`.
Queries this run: prod daily 14d `84e660ec`; unfiltered control 14d `6a79c7fd`; `pool_click` by `source`
30d `13c301be`; prod funnel events 30d `f6dd36fa`; prod page_view-by-URL 7d `d87e4ecc`; prod page_view by
`src` 14d `7c4971dc`; `4438dabc` arrival by day `4b3dab02`; static arrivals by day 7d `c822874c`.

## HEADLINE: **206 landed and is visible in the audit's own numbers — 23 of 32 rendered pool-details this tick were sub-rail pools the scanner could never reach before, all clean. And the first grid-shaped `?pool=` arrival on record (07-29) logged a `page_view` with NO `pool_view` — it left before the live fetch ever painted.**
Fourth consecutive zero-blocking audit tick. North star `pool_click{source ∈ garden_cta, protocol_link}`
= **0** in 14d (**26th** straight day). Guardrails clean — prod `error_occurred` **absent from the result
set** (0) for **21 straight days**, and absent in the unfiltered control too (5th consecutive day).

## Real prod trend (daily totals, `84e660ec`)
| date | session_start | error_occurred | pool_view | pool_click |
|------|----|----|----|----|
| 07-20 | 17 | 0 | 0 | 0 |
| 07-21 | 6 | 0 | 0 | 0 |
| 07-22 | 4 | 0 | 0 | 0 |
| 07-23 | 7 | 0 | 0 | 0 |
| 07-24 | 3 | 0 | 0 | 0 |
| 07-25 | 4 | 0 | 0 | 0 |
| 07-26 | 6 | 0 | 2 | 0 |
| 07-27 | 3 | 0 | 0 | 0 |
| 07-28 | 8 | 0 | 0 | 0 |
| 07-29 | 5 | 0 | 0 | 0 |
| 07-30 | 6 | 0 | 0 | 0 |
| 07-31 | 4 | 0 | 0 | 0 |
| 08-01 | 9 | 0 | 1 | 0 |
| 08-02 | **3** (finalized; was "0 so far" at yesterday's read) | 0 | 0 | 0 |
| 08-03 | 0 so far | 0 | 0 | 0 |

`error_occurred` and `pool_click` are both **absent from the prod result set** across the whole window —
zero, not low. **Control (`6a79c7fd`):** `error_occurred` absent unfiltered too, fifth consecutive day.
Prod session totals 14d: **85** (unfiltered **91** — the 6-session delta is localhost/preview; playbook
`product-audit.md` class 13, both numbers recorded, claim made from the filtered one).
**Insufficient data** for every funnel claim (≥30 rule).

## North-star surface (`13c301be`, 30d)
- `pool_click` 30d = **1**, `source=undefined` (the pre-123 07-19 event; ages out of the 30d window
  ~08-18). **Zero CTA clicks on either entry path, ever.** Unchanged for a 9th day.
- Prod funnel events 30d (`f6dd36fa`): `plan_saved` **5** · `search_success` **4** · `plan_created` **3** ·
  `share_link_opened` **1** · `share_link_created` **0** · `tend_reminder_added` **0** ·
  `waitlist_submitted` **0**. Identical for a **fourth** consecutive day — 96h with no new funnel event.

## Prod page_views by URL, 7d (`d87e4ecc`, total 8) — provenance clean, one URL newly INVESTIGATED
One event each: `/tokens/usdc` · `/tokens/eth` · `/tokens/slisbnb` · `/tokens/jitosol` ·
`/chains/avalanche` · `/?token=ICP&poolTypes=Yield Farming` · `/?pool=fdcccd6a-…` (the 08-01 arrival) ·
`/?token=USDC&chain=Popular&pool=4438dabc-…`.
- **URL-provenance check (playbook class 9): clean.** No pure-numeric or date-shaped slugs. `slisbnb`
  (Lista staked BNB) and `jitosol` are real generated pages; `Popular` is a predefined chain filter
  (`app.js:1837`); `4438dabc-…` is a real pool present in BOTH `data/pools-snapshot.json` (≥$10M) and
  `tokens/usdc.html`'s links.
- **→ finding 214.** The `4438dabc` URL has sat in the 7d set since **07-29** un-investigated (yesterday's
  tick diffed only the highlighted subset). Date-attributed this tick (`4b3dab02`): page_view **07-29 = 1**;
  `pool_view` filtered to the same URL = **absent (0)** — and prod-wide `pool_view` on 07-29 = 0. So an
  arrival landed on a pool-detail URL for a **snapshot-present** pool and no pool-detail render ever fired:
  gone (or a JS-running crawler) before the blocking multi-MB live fetch resolved. n=1 — **no conversion
  claim**; the value is that it exposed the code mechanism (`app.js:1144` sends every `?pool=` arrival to
  the live fetch even when the pool is IN the committed snapshot, while the 105 kpi effect then fetches
  that same snapshot anyway, post-render). Corollary recorded: `pool_view` fires only after data resolves,
  so bounced arrivals are invisible to it — arrivals to the north-star surface = `page_view` with URL
  containing `pool=` (2 on record in 7d), not `pool_view` (1).

## Distribution check (`7c4971dc` / `c822874c`)
Prod `page_view` by `src` 14d: **one row, `undefined` = 36**. The 08-02 falsifiable prediction ("a
non-`undefined` `src` row by ~08-05, given ≥5 static-page arrivals, else P1 regression") is **OPEN and
UNJUDGEABLE**: static-page arrivals SINCE 203/204 shipped (08-02) = **0** (`c822874c`: tokens+chains rows
08-02 and 08-03 both 0; every static arrival in the 7d window predates the ships). The prediction's
condition is unmet — this is "no arrivals yet", NOT "the fix failed". Carry to the next tick; if arrivals
accrue with `src` still `undefined`-only, THEN it is the P1.

## §2b product audit — `audit-app.js` (82 surfaces, 1 finding, **0 blocking** — 4th consecutive tick)
`node audit-app.js` (after fresh `npm ci`) → `status: "OK"`, `generatedAt` **2026-08-03T07:25:56.730Z**
(this tick), `surfacesCovered` **82** — both staleness gates checked before claiming clean. Measured at
`aff5fb09d` (origin/main tip at run time; a 211 follow-up `4362bd7a1` merged during the tick — vercel.json
+ redirect test only, no audited surface touched).
- **206 landed, visible in the run's own log:** pool rotation now draws from the union population —
  `snapshot=734, deep-linked-live=3230, union=3964, reachable=3962`; **3,658 of 3,658** estate deep-linked
  ids confirmed live this tick; rendered **32/32** picks, **subRailPicked 23/32** — the first-ever tick
  where the majority of rendered pool-details are arrival-population pools. **Zero findings on all 23.**
  Full pass now ~124 ticks (the priced-in 206 trade).
- `poolPrescan`: **candidates 734** (snapshot-only — **→ finding 215**, the leg 206 did not widen),
  1 suspect (`201e5f6e` mean30d-rail-breach), rendered clean → **auto-downgraded P1 → P2 by 171's
  reconciliation** for the sixth consecutive day. Working as specced.
- Static prescan clean for a **fifth** consecutive day: `scanned 4,358` (2,094×2 tokens + 85×2 chains),
  `suspectCount 0` across all six signals. i18n: `scanned 544, suspectCount 0, allowlistSize 24`.
  Text surfaces: 2 scanned, 0 suspects. Static rotations (EN + KO) advancing, `wrapped=false`.

## NEW FINDING → 214 (P2/READY 6.5): a `?pool=` arrival never paints from the snapshot — even when its pool is in it
`app.js:1144` — `snapshotEligible = !urlParams.pool && …` — so EVERY pool arrival blocks on the multi-MB
`yields.llama.fi/pools` fetch; then the 105 kpi effect (`app.js:1253-1273`) fetches
`/data/pools-snapshot.json` ANYWAY once the detail renders. For a snapshot-present pool the instant-paint
payload is being downloaded regardless — just too late to paint from. Populations: grid-origin
share/bookmark URLs are snapshot-present ~by construction (the grid renders railed pools); estate deep
links are snapshot-present for 420/3,658 (11.5%). Evidence of harm: the 07-29 arrival above (n=1, stated
honestly: mechanism measured, harm hypothesized). NOT a trust-rail change: same data, same rails, only
which cached payload paints first; sub-rail arrivals keep today's exact path.

## NEW FINDING → 215 (P1-for-the-loop/READY 7.5): 206 widened the rotation's population but not the prescan's
This tick's run: `poolPrescan.candidates = 734` vs rotation union **3,964**. `loadLivePoolIds()` retains
the FULL live records (`audit-app.js:1546` — kept precisely "so runAudit() can build the sub-rail fixture
body… without a second fetch") yet `buildPoolSurfaces()` calls `prescanPools(pools)` with the snapshot
only (`:2917`). So the cheap rail-relative checks (`apy-rail-breach`, `mean30d-rail-breach` — the
122/144/165 class, every instance hand-found on a non-flagship pool) reach **18.5%** of the candidate
population, while the other 3,230 rely on the 32-renders/tick rotation: a ~124-tick (~4-month) full pass
for a check that is a pure record predicate needing NO render. Fifth instance of
`detector-signal-coverage.md`'s arrival-population axis, new corollary: **when you widen a population,
every leg reading the old population in the same file must widen or be explicitly deferred with a number**
— 206's spec deferred nothing; the prescan leg was simply not enumerated.

## Checked and CLEARED — no ticket (recorded so the next tick does not re-derive)
- The 8-URL 7d set against yesterday's 12: window shift only; nothing aged in that fails provenance.
- `pool_view` 07-26 = 2 with no `?pool=` URL in today's 7d window: those views predate the window's URL
  rows aging; not re-chased (7d URL set covers 07-28+).
- Experiment windows: **none closed today** (next: 117.1/.2/.3 → 08-05). 08-03's build item (211
  follow-up #374) is config-only; no audited surface changed.

## Data layer — healthy
Snapshot `generatedAt` **2026-08-03T07:12:01.100Z** (this tick), **734 pools** (736 yesterday — ordinary
churn). **0** pools with total APY > 1000%; **0** non-finite KPIs; `mean30d` breach set unchanged at 1
(`201e5f6e`, known-and-handled). Live DefiLlama reachable in-session (the audit's liveness leg resolved
3,658/3,658), so coverage numbers do not rest on the committed snapshot.

## Experiments — no window closed today
Windows still open: 117.1/117.2/117.3 → 08-05; 124 → 08-06; 125 → 08-07; 129 → 08-08; 045/051/065/148 →
08-09..08-12; 180/182 → 08-13; 190/191/192 → 08-14; 196 → 08-15; 197-205 → 08-15..08-16; 206/207 →
08-16; 210 → 08-16; 213 → 08-16; 211 GSC leg → human ~09-13; 013 GSC leg → human ~08-23.

## Diff vs 2026-08-02
1. **Fourth consecutive zero-blocking tick, and the first with a majority-sub-rail render set** — 206
   landed: rotation population 734 → **3,962 reachable**, subRailPicked **23/32**, all clean.
2. **214 found** — `?pool=` arrivals are excluded from the snapshot-first paint by construction even when
   snapshot-present, and the one grid-shaped arrival on record (07-29, newly date-attributed) bounced with
   `page_view` 1 / `pool_view` 0.
3. **215 found** — the pool PRESCAN still reads the 734-pool snapshot while the rotation reads 3,964;
   the full live records it needs are already in memory every tick.
4. **The `src` prediction is open but unjudgeable**: 0 static-page arrivals since 202/203/204 shipped.
   Not falsified, not confirmed — condition unmet.
5. Guardrail streak 20 → **21** clean prod days; control clean 5th day. North star 0 (**26th** day).
   Funnel events 30d unchanged a **fourth** day (96h). 08-02 finalized at 3 sessions.
6. Data layer: pools 736 → **734**; zero out-of-rail totals; zero non-finite KPIs; 171 auto-downgrade
   holding a 6th day.
7. Backlog had ZERO READY rows at pickup (206/207/209-213 all shipped by the 08-02→08-03 builds; 208
   PARKED). Max id re-read off `origin/main` immediately before writing: **213** → today's findings are
   **214/215**.
