# Signals snapshot — 2026-07-28 (heartbeat, live `Mixpanel` connector)

Project: defigarden (4042048) · org Equitee (2885044) · prod filter `$current_url contains www.defi.garden`.
Queries this run: prod daily line 14d `query_id 064d8388`; pool_view/pool_click by `source` 30d `be2a9cdb`;
page_view-by-URL 7d `b4f77bc0`; prod page_view by `src` 14d `d56236cb`.

## HEADLINE: **yesterday's P0 is fixed and verified on live prod — and the same surface has a second,
## independent defect underneath it: its links do not go where they claim.**
`llms.txt` now serves **0** APY figures above the 1000% rail (was 8, top 353,114.2%) and its TVL-floor claim
reads `TVL ≥ $10M`, matching the product's floor — item **159 confirmed live by curl**, not inferred from the
checkout. But the same two files publish **32 links that mis-target**: 15 pool rows point at the bare
homepage, 17 protocol/query links use a `?search=` param the router does not recognise, and **zero** `?pool=`
deep links exist anywhere — so the AI-discovery surface never points at pool-detail, the north-star surface.
→ items **166** (links), **168** (the planner is absent from the surface entirely), **169** (the new
text-surface scanner has no link signal).

North star `pool_click{source ∈ garden_cta, protocol_link}` = **0** in 14d (unchanged, 20th straight day).
Guardrails clean — `error_occurred` **0 for 15 straight days**.

## Real prod trend (daily totals, `064d8388`)
| date | session_start | plan_created | search_success | error_occurred | share_link_opened | pool_click | pool_view | plan_saved |
|------|----|----|----|----|----|----|----|----|
| 07-14 | 5 | 0 | 0 | 0 | 0 | 0 | 0 | 0 |
| 07-15 | 5 | 0 | 0 | 0 | 0 | 0 | 0 | 0 |
| 07-16 | 7 | 0 | 0 | 0 | 0 | 0 | 0 | 0 |
| 07-17 | 2 | 0 | 0 | 0 | 0 | 0 | 0 | 0 |
| 07-18 | 7 | 0 | 0 | 0 | 0 | 0 | 1 | 0 |
| 07-19 | 6 | 1 | 2 | 0 | 1 | 1 | 3 | 1 |
| 07-20 | 17 | 0 | 0 | 0 | 0 | 0 | 0 | 0 |
| 07-21 | 6 | 0 | 0 | 0 | 0 | 0 | 0 | 0 |
| 07-22 | 4 | 0 | 0 | 0 | 0 | 0 | 0 | 0 |
| 07-23 | 7 | 0 | 1 | 0 | 0 | 0 | 0 | 0 |
| 07-24 | 3 | 0 | 0 | 0 | 0 | 0 | 0 | 0 |
| 07-25 | 4 | 0 | 0 | 0 | 0 | 0 | 0 | 0 |
| 07-26 | 6 | 0 | 0 | 0 | 0 | 0 | 2 | 0 |
| 07-27 | **3** (finalized, was "0 so far") | 0 | 0 | 0 | 0 | 0 | 0 | 0 |
| 07-28 | 0 so far | 0 | 0 | 0 | 0 | 0 | 0 | 0 |

`error_occurred` is **absent from the result set** over the whole 14d window — zero events, not low.
**Insufficient data** for every funnel claim (≥30 rule). The 07-19 cluster is still the only engaged session
in the window; 82 sessions / 14d, all crawler-shaped except that one.

## North-star surface (`be2a9cdb`, 30d) — unchanged from 07-27
- `pool_view` 30d = **6**: `card_click` **5**, `url_direct` **1** (the 07-26 lido/STETH landing, still n=1).
- `pool_click` 30d = **1**, `source=undefined` (the pre-123 07-19 event). **Zero CTA clicks on either entry
  path, ever.** No new pool-detail traffic in 24h.

## Prod page_views by URL, 7d (`b4f77bc0`, total 21)
`?token=INFINIFIUSDC` 3 · `?chain=Popular&minTvl=1000000` 2 · then 1 each: `?token=SMON`, `?token=AVICI✨`,
`?token=AVICI✨&minTvl=10000000`, `?chain=Solana&token=AVICI`, `?token=BUIDL`, `?token=BUIDL&lang=ko`,
`/tokens/buidl`, `?token=ICP&poolTypes=Yield Farming`, `?chain=Berachain&token=20)`,
`?poolTypes=Yield+Farming&token=20`, `?chain=Popular&minTvl=1000000&pool=747c1d2a…`, `/`,
`/chains/{aptos,ethereum,plasma,linea}`.
- **URL-provenance check (playbook `product-audit.md` class 9), run this tick:** `token=20`, `token=20)` are
  the **item 148 class** — still ours, still live, still unmerged (PR #306, day 3).
- New this window: `?token=AVICI✨&minTvl=10000000` (the same crawler adding an explicit TVL floor) and
  `?token=ICP&poolTypes=Yield%20Farming`. Both well-formed app URLs, neither ours to generate. Not findings.

## Distribution check (`d56236cb`, the cheapest true test per LEARNINGS 2026-07-27)
Prod `page_view` by `src` over 14d: **one row, `undefined` = 47**. Zero tagged arrivals. The 069 spotlight
packs remain unposted; every distribution hypothesis stays untestable by construction.

## Guardrails — clean
- `error_occurred`: **0 every day 07-14 → 07-28 (15 straight)**. Last non-zero 07-13 (1). → closes 082.
- SEO lifeline: `?token=` / `?chain=` / `?pool=` all render pool cards — `audit-app.js` drove grid-token,
  grid-chain, pool-detail, dead-pool, grid-loading, grid-360 with zero findings on those surfaces.
- Planner conversion: no planner sessions since 07-19 → uncomputable.

## §2b product audit — `audit-app.js` (26 surfaces, 5 findings, ALL the known 148 class)
`NODE_PATH=/opt/node22/lib/node_modules node audit-app.js` → `status: "OK"`, `playwright.source: "local"`,
`generatedAt` 2026-07-28T07:09:16Z (this tick), **exit code 1** because of real findings — 157's intended
behaviour, confirmed by reading the exit status rather than assuming it.
- Surfaces **26** (was 16): text-surfaces, grid-token, pool-detail, grid-chain, dead-pool, grid-loading,
  pool-detail-360, grid-360, pool-detail-dark, pool-detail-ko, **landing, planner, planner-360, planner-ko,
  plan-bloom-{growth,target,subscription,360,ko}** (162/164 now live), static-page,
  static-page:tokens/{01,17dec2026,20261231,2027,supercbbtc,eulerrlusd}.
- Prescan: **2,198 pages**, 7 suspects, all `junk-slug`; `broken-number-literal` 0, `absurd-magnitude` 0,
  `zero-yield-claim` 0.
- Text surfaces (160, new): **2 scanned, 0 suspects** — `apy-rail-breach` 0, `broken-number-literal` 0,
  `tvl-floor-claim` 0, `empty-surface` 0. **This is a true clean on the numbers and a blind spot on the
  links** — see 166/168 below.
- Findings: 1 aggregate `static-prescan:junk-slug` (P1) + 4 rendered `junk-slug` P1s (`01`, `17dec2026`,
  `20261231`, `2027`). **All item 148, still unmerged.** No new scanner finding — third consecutive day.
- **The planner surfaces produced zero findings on their first full-rotation day** (bloom in 3 archetypes ×
  360 × KO). That is the first automated evidence the default face renders clean end-to-end.

## Data layer — healthy
Snapshot `generatedAt` 2026-07-28T05:13:22Z, **740 pools** (735 yesterday).
- **335** non-null `kpis.apySharpe` (326 yesterday), **0** with |Sharpe| > 50 (range −48.65..31.05).
- `historyPoints` median **15** (14 yesterday) — daily accrual working.
- **0** pools with total APY (`apyBase + apyReward`) > 1000%. Max total **95.55%** (was 39.91% — a real
  jump, uniswap-v3 WETH-USDC on Base at $110.8M TVL; well inside the rail, not a finding).
- Full KPI-tree scan for NaN/Infinity/|v|>1e11: **0 hits**. `kpis.apyMean` max **49.94%**.
- **Known-and-handled:** raw `apyMean30d` still out of rail on `201e5f6e-…` (balancer-v2 WSTETH-AAVE), now
  **30,282.55%**. All four `PoolDetail.js` consumers gate on `mean30dSane`; 165 additionally suppressed the
  derived-$ nodes. Not a finding — recorded so the next tick does not re-derive it.
- Method per playbook `derived-number-rails` Step 0b: total APY derived as `apyBase + apyReward` (the
  snapshot has **no** `apy` field); the check was proved non-vacuous against the 30,282% pool.

## NEW FINDING → 166 (P1/READY 8.6): the AI-discovery surface's links do not go where they claim
`llms.txt` + `llms-full.txt` are the artifacts whose entire job is telling LLMs and agents what DeFi Garden
has. **Verified on live prod by curl this tick**, three distinct defect classes, 32 links:

1. **15 pool rows → the bare homepage.** `generate-llms.js:606`:
   `const poolUrl = pool.url || meta.baseUrl;` — **no DefiLlama payload has a `url` field** (snapshot keys:
   `pool, chain, project, symbol, tvlUsd, apyBase, apyReward, apyMean30d, poolMeta, exposure, ilRisk,
   underlyingTokens, kpis`; the live `/pools` shape adds `apy`, not `url`). So the fallback is **100% of
   rows, always**, and has been for the life of the surface. Live, verbatim:
   `- uniswap-v3 · WETH-USDC — 95.55% APY, $110,825,218 TVL — https://www.defi.garden`
   Fifteen specific pool claims across 5 chain sections, every one pointing at the planner homepage.
2. **17 `?search=` links land on the landing with the query silently dropped.** 7 in `llms.txt` (6 protocol
   rows + the `"Pendle opportunities"` query example), 10 in `llms-full.txt`. `search` is **not** in
   `home.html:77` `ANALYTICS_PARAMS` (`token, chain, pool, poolTypes, protocols, minTvl, minApy, app`) nor
   `:78` `PLANNER_PARAMS`, so `__APP_MODE` resolves to `'landing'`; `landing.js` reads only `lang`
   (`landing.js:52`). The user asked for `lido` and gets an empty search box. **The correct param already
   exists: `?protocols=<slug>`.**
3. **8 top-yield rows in `llms.txt` are not pool-specific** — they link `?token=<symbol>&chain=<chain>`,
   which does resolve (`app.js:835` substring match on `pool.symbol`) but cannot address a pool. That is why
   two distinct Base uniswap-v3 WETH-USDC pools — **95.5% APY / $110,825,218 TVL** and **31.7% /
   $10,187,107** — render as two rows on one URL, reading as contradictory claims about one page.

**And the through-line: `grep -c "?pool=" llms.txt llms-full.txt` → 0, 0.** The pool id is present on every
payload record (`pool`), `/?pool=<id>` is the north-star surface per the 2026-07-23 decision, and the
AI-discovery surface has never once linked to it. An LLM that cites one of our pools cannot send its reader
to that pool.

## NEW FINDING → 168 (P1/READY 7.4): `llms.txt` positions the product as a yield screener; the planner is absent
`grep -icE "planner|garden plan|savings|goal|subscription|forever number" llms.txt` → **0**. The homepage row
reads `TL;DR: Main dashboard for discovering DeFi yields across all chains and protocols.` `plan.html`
appears **once** in `llms-full.txt` (line 2799) as a bare URL among 4,917, with no description.
Against CLAUDE.md ("Garden Planner — the DEFAULT feature, bare `/`") and the 2026-07-12 business-model
decision ("the planner's SUBSCRIPTION archetype / forever-number flip is the product's front door"), our own
AI-discovery file teaches every LLM that reads it that we are a DefiLlama-style screener — which is exactly
the positioning NORTH_STAR says we are not (the degen uses DefiLlama directly; the ICP is the cautious saver).

## NEW FINDING → 169 (P2/READY 5.4): 160's text-surface scanner has no link signal
`prescanTextSurfaces()` scanned both files this tick and returned `suspectCount: 0` while 32 mis-targeted
links sat on those exact two files. Its signal set is `apy-rail-breach`, `broken-number-literal`,
`tvl-floor-claim`, `empty-surface` — every one a number-or-emptiness check, because 160 was specced from
159, which was a number bug. **Third instance of one pattern** (148 → 159 → 166): the checker's signal set
is drawn from the last bug found, so the next bug is in whatever class nobody has been bitten by yet.

## Experiments — two windows closed today (written to LEARNINGS.md)
- **082** (planner `translations` ReferenceError hardening, shipped 07-13/14) → window to 07-28.
  `error_occurred` **0 for 15 consecutive days**, absent from the result set. **MOVED (weak-n)** — the guard
  held and nothing recurred, but only ~82 sessions and one `plan_created` exercised the planner in the
  window, so this is "no recurrence observed", not "proven robust". Kept.
- **088.1** (rate-track-record/steadiness note on pool detail, shipped 07-14) → window to 07-28. Measured on
  pool-detail engagement: `pool_view` 30d = 6, `pool_click` = 1 (pre-123, `source=undefined`), north-star
  clicks 0. **INCONCLUSIVE at n≈0** — the surface it renders on received 6 views in 30 days. Kept.
- Remaining windows: 094/114/115 + 149 → 07-29; 116 → 07-30; 124 → 08-06; 125 → 08-07; 045/148 → 08-09;
  050 → 08-11; 013 GSC leg → human ~08-23. 154–165 shipped 07-26/28, windows not yet due.
- **159's build leg is verified live** (not a window closure — window runs to ~08-10): prod `llms.txt` has
  0 figures above the rail and claims the true `$10M` floor.

## Diff vs 2026-07-27
1. **159 fixed and confirmed on prod** — 8 out-of-rail APY lines → **0**; `TVL ≥ $10k` claim → `TVL ≥ $10M`;
   top line now `uniswap-v3 · WETH-USDC — 95.5% APY` on $110.8M TVL. The P0 is closed on the live surface.
2. **166 found on the same surface, underneath 159** — the numbers are now honest and the links still are
   not. 32 mis-targeted links, 0 `?pool=` deep links, live on prod.
3. **168 found** — the planner does not exist as far as our AI-discovery surface is concerned.
4. Guardrail streak 14 → **15** clean days; 07-27 finalized at 3 sessions (not 0).
5. `audit-app.js`: 16 → **26 surfaces** (162/164's planner + bloom rotation live), still 5 findings, all 148.
   First clean automated pass over the planner and all three bloom archetypes.
6. Data layer: pools 735 → **740**, Sharpe 326 → **335**, historyPoints median 14 → **15**, max total APY
   39.91% → **95.55%** (real, in-rail). Zero NaN/Inf.
7. Two windows closed (082 MOVED-weak, 088.1 INCONCLUSIVE). North star and `src` breakdown unchanged.
8. **This tick was lapped mid-run.** It opened on `fac2e30f2` and by commit time `origin/main` had taken a
   build run's item **167** (`audit-app.js` renders one hardcoded pool out of 740, PR #323) — so the
   heartbeat's own next-id assignments collided and were renumbered 166 / 168 / 169, and every append was
   re-applied on top of `origin/main` rather than committed from the stale copy. **Consequence to read the
   audit numbers with: the 26-surface / 5-finding run above used the PRE-167 scanner.** 167 broadens
   pool-detail target selection, so tomorrow's run will cover a different (larger) pool population and its
   finding count is not comparable to today's. → LEARNINGS 2026-07-28 "a heartbeat can be lapped by a build
   run mid-tick".
9. **PR #306 is on day 3 unanswered and is now decaying**: its base sha is `33cc56ae` (07-26) while `main`
   has taken ~10 commits since, including four daily CI regens of the same 5,596-file SEO surface it
   rewrites. Every day it waits raises its conflict cost.
