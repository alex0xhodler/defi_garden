# Signals snapshot — 2026-07-13 (heartbeat, live Mixpanel pull)

Project: defigarden (4042048) · org Equitee (2885044) · via Mixpanel connector (uppercase `Mixpanel` server — live this run; lowercase `mixpanel` still needs OAuth). No business context configured for the project.

## Event totals — last 14 days
| event | count | 07-12 snapshot | note |
|-------|-------|----------------|------|
| session_start | 348 | 326 | window-shift only; run-rate is DECAYING day-over-day (see trend) |
| page_view | 342 | 330 | |
| search_success | 101 | 100 | |
| search_abandonment | 8 | 8 | flat; parse-fail ~7% (8/109) |
| plan_created | 6 | 6 | flat; daily 5→1→0→0 — declining, tracks the traffic decay; n tiny |
| plan_saved | 12 | 12 | flat |
| pool_view | 3 | 3 | flat; all `source=card_click`; `url_direct` (SEO `?pool=`) still 0 |
| performance_metric | 137 | — | perf-wave instrumentation confirmed firing |
| filter_combination | 8 | — | 042's re-enabled analytics confirmed firing |
| feeling_degen | 1 | absent | degen toggle now fired ≥1× (was absent in the lexicon on 07-12) |

## Daily run-rate trend (session_start, complete days)
07-09 = **167** (launch spike) · 07-10 = 80 · 07-11 = 80 · 07-12 = **21**.
→ The launch bump is fading fast. 07-12 is a complete day at 21 sessions — a ~74% drop from the 80/day plateau. **Traffic, not perf, is the binding constraint** (LOG 2026-07-12) — and it is now visibly decaying. Distribution (the weekly theme) is the correct focus.
- plan_created tracks it: 5→1→0→0. search_success daily 0/50/50/1 (the 50/50 is Mixpanel bucketing, not a hardcode — 020 confirmed real `results_count` last snapshot).

## North star — `waitlist_submitted` (card funnel) = 0, and the cause is TRAFFIC, not wiring
Lexicon (Get-Events, 16 events): `feeling_degen, filter_combination, page_blur, page_focus, page_view, performance_metric, plan_created, plan_saved, pool_view, proxy_test, reward_sync_diagnostic_test, search_abandonment, search_success, session_start, $session_start, $session_end`.
- `waitlist_opened`, `waitlist_submitted`, `share_link_created`, `share_link_opened`, `pool_click`, `garden_cta`, `error_occurred` are ALL still absent → never received → structurally 0.

**Code audit (read-only triage — is this the 044 "shipped but never fires" bug repeating on the new north star?): NO. The waitlist funnel is wired end-to-end in the PRODUCTION bundle.**
- `analytics.js` defines `trackWaitlistOpened`/`trackWaitlistSubmitted` (fire `waitlist_opened`/`waitlist_submitted`).
- `planner.js` calls them at 9 sites: Bloom-modal path (2373/2612/1590…) + the 062 SEO auto-open path (3513/3544…).
- `plan.html` loads **`planner.min.js`** in prod. Verified the minified bundle contains: `formspree.io/f/xzdqygjn` (2×), `DeFi Garden waitlist signup` (2×), the `urlParams.get('waitlist')` read (1×), and `trackWaitlistOpened({source:…})`. The 061 staleness precedent (min not regenerated) does NOT apply here — the min is current (built in the same 062 commit 9022c12f).
- Generators emit the entry link: `generate-token-pages.js` writes `plan.html?...&waitlist=1&src=seo_token` (chain variant too). `test_waitlist_seo_entry.js` drives the real UI and asserts `trackWaitlistOpened(source=seo_token)` fires.

→ **The card-funnel plumbing is sound.** The 0 is because nothing reaches the funnel: SEO pages carrying the 062 CTA have 0 organic traffic (GSC indexing 2–6 wk, just begun), and the Bloom path sits below `plan_created` (only 6 in 14d, declining). This is a distribution problem, not an instrumentation bug — do NOT file a "verify waitlist fires" build item (would be a wasted cycle).
- Honest caveat: verified via static bundle analysis + the passing fixture-routed Playwright test, not a live-fired event from real traffic. The only remaining unknown is "has a real human hit it" = traffic.

## Guardrails
- `error_occurred` absent from lexicon → never fired → quiet (as before).
- Planner conversion (`plan_created`/planner session): declining with traffic; n too small for a claim.
- Analytics `?token=/?chain=/?pool=` URLs: search_success 101, filter_combination 8, pool_view 3 → app rendering + firing normally.

## Sample discipline
- ≥30-event claims allowed: search only (n=109). plans (6/12), pool_view (3), waitlist/share/errors (0) → **insufficient data** for funnel/retention/cohort claims.
- Retention cohorts: not computable — still a single active week (tracking began 2026-07-09), and that week is now decaying.
- No experiment measurement window ended (005/007/008 → 07-24; 013/014/018/019/020/024/025/039 → 07-25; 041 → 07-26; 048/049/061 → 07-26; 062 → 07-27; 045/051/056 → 08-09/11). Nothing to close in LEARNINGS.md this run.

## Diff vs 2026-07-12
- No confirmed growth — totals moved by window-shift + 1 partial day. The real new fact is the **07-12 traffic drop to 21/day** (the launch cohort is churning out) and the **waitlist-wiring audit clearing the north-star plumbing** (it's traffic-gated, not broken).
