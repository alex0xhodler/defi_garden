# Signals snapshot — 2026-08-13 (local takeover, repository + human interview)

## Instrument availability
- Mixpanel: no fresh query this tick; the retired Claude routine's OAuth session is expired. Latest measured snapshot remains 2026-08-12: 9 sessions on 2026-08-11, 9/9 crawler-shaped, 0 classified real; north-star leg B remained 0 real for 35 days.
- Cloudflare `agent_reads`: deployed but no credentialed D1 read was available. This is absence of read access, not zero agent traffic.
- Product audit: no rendered audit run this strategy-reset tick. Baseline plain lane exercised 63 files: 62 PASS, 1 pre-existing test-invariant FAIL (`test_translations_number_format.js`); all observable number-format assertions in that file passed.

## Human interview decisions
- Cycle outcome: agent adoption. First slice: developer discovery + citation-ready token answers.
- Citation target: all major answer surfaces plus agent developers; canonical query is “safest yield for a token.” Public wording is `rate stability`, never `safest`.
- Canonical surface: existing EN/KO `/tokens/<slug>` HTML and Markdown twins, across every eligible token page.
- Success is layered: correct retrieval → ≥30 qualified non-loop reads → ≥3 independently observed citations OR ≥5 qualified AI referrals. Proxies never collapse into the outcome.
- x402 paid pilot paused after provider discovery showed Cloudflare Workers still require a selected receiving wallet and x402 facilitator for settlement. `X402_ENABLED` stays false.
- OpenAPI decision: remove nonexistent `POST /planner`; do not deprecate a route that the Worker does not serve.

## Queue and repository signals
- Live contract defects already have evidence: item 262 exposes 4 undocumented routes + 1 nonexistent route; item 266 duplicates both trust rails and checks `p.apy` instead of total APY; item 277 records zero `/api` or `/mcp` mentions in served `llms.txt` on 2026-08-12.
- Open PRs: 1 — #424 (`claude/loop-236`), design-gated and ORPHAN by age/state; heartbeat detects only and does not merge it.
- Item 276 has verifier PASS-WITH-AMENDS and LOG close-out but its BACKLOG row remains `IN_REVIEW`; reconcile before any duplicate build.
- Orphan PRs: 1 (#424).
- `vitals | ships/wk: 24 | tokens/ship: ? | attempts: 1 | open PRs: 1 (oldest: 2) | product:process: 20:4`

## Diff from 2026-08-12
- No new demand claim: analytics and D1 were not freshly read.
- Product direction changed by direct human interview: staged contract integrity → citation estate; x402 activation removed from this cycle.
- Payment readiness remains unknown by owner report; no production-money action is authorized.
