# BACKLOG — written by the heartbeat, culled by the human, consumed by build loops.
# Statuses: READY → IN_PROGRESS → IN_REVIEW → SHIPPED (measuring) → DONE
#           PARKED (3 strikes) · BLOCKED (question for the human) · CULLED (human said no)

| ID | Title | Score | Status | Risk | Spec | Attempts | Measure until |
|----|-------|-------|--------|------|------|----------|---------------|
| 001 | Wire Mixpanel MCP into the repo (.mcp.json) | 9.0 | DONE (shipped 2026-07-10, OAuth done) | HIGH | specs/001.md | 1 | — |
| 002 | Share-loop instrumentation truth check (sound; plan_saved double-fire fixed) | 8.7 | DONE | LOW | specs/002.md | 1 | — |
| 003 | npm test smoke gate (Playwright, both router paths) | 7.0 | DONE (auto-merged 2026-07-10) | HIGH | specs/003.md | 1 | — |
| 004 | Surface the share CTA at the bloom moment | 8.0 | DONE (superseded by 005) | HIGH | specs/004.md | 1 | — |
| 005 | One share hub at bloom — image-first, link always travels | 8.5 | DONE (window closed 2026-07-24 INCONCLUSIVE — zero share-origination traffic, kept; see LEARNINGS.md; re-measure 14d from first 069 post) | HIGH | specs/005.md | 1 | — |
| 007 | Arrival moment for share-link recipients | 8.2 | DONE (window closed 2026-07-24 INCONCLUSIVE — 1 share_link_opened ever, no recipient cohort, kept; see LEARNINGS.md; re-measure with 005) | HIGH | specs/007.md | 1 | — |
| 008 | Checkout CTA hierarchy: one action per intent level | 8.3 | DONE (window closed 2026-07-24 INCONCLUSIVE — 0 share initiations, 0 waitlist events in window, kept; see LEARNINGS.md; re-measure with 005) | HIGH | specs/008.md | 1 | — |
| 009 | Waitlist funnel instrumentation (waitlist_opened/waitlist_submitted) | 7.5 | SHIPPED | LOW | specs/009.md | 1 | — |
| 010 | GSC indexing crisis — diagnosis | 8.8 | DONE (specs/010-diagnosis.md) | LOW | specs/010.md | 2 | — |
| 011 | GSC fix: canonical truth from the router, both modes | 8.8 | SHIPPED (GSC validation 2-6 wks) | HIGH | specs/011.md | 1 | — |
| 012 | GSC fix: honest empty states — noindex + alternatives | 8.6 | SHIPPED (GSC validation 2-6 wks) | HIGH | specs/012.md | 1 | — |
| 013 | GSC fix: sitemap quality threshold in generate-sitemap.js (≥2 live pools + TVL floor per URL) | 8.0 | DONE (window closed 2026-07-25 INCONCLUSIVE in-loop — GSC-measured, human reads GSC through ~08-23; see LEARNINGS.md) | HIGH | specs/013.md | 1 | — |
| 014 | GSC fix: static token/chain landing pages, top-100 by TVL (generate-*.js pattern) — phase 1 | 7.8 | DONE (window closed 2026-07-25 — build leg MOVED via 021; traffic leg INCONCLUSIVE, first-ever static-surface page_views (~9) observed 07-25; see LEARNINGS.md) | HIGH | specs/014.md | 1 | — |
| 015 | Fix apex/referral share URL in tweet flow (redirect hygiene) | 6.0 | SHIPPED (PR #106, merged 2026-07-11) | LOW | specs/015.md | 1 | — |
| 016 | Re-brand empty-state buttons to neumorphic tokens | 7.2 | SHIPPED (PR #97, merged 2026-07-11) | LOW | specs/016.md | 1 | — |
| 017 | NL search: every advertised typing-example must parse (solana/base/kamino lenders/curve/convex) | 8.4 | superseded by 018 — fixtures passed, product failed | HIGH | specs/017.md | 1 | — |
| 018 | NL search actually works (behavior on live data, Playwright drives the real UI) | 9.0 | DONE (window closed 2026-07-25 — functional leg green in rendered suite; find-rate INCONCLUSIVE, search_success n=4 vs ≥30 rule; see LEARNINGS.md) | HIGH | specs/018.md | 3 | — |
| 019 | Pool-detail pages convert SEO landers toward the north star | 8.7 | DONE (window closed 2026-07-25 INCONCLUSIVE — url_direct landings 0 all window; superseded by the 2026-07-23 north star + 122-139 pool-detail program; see LEARNINGS.md) | HIGH | specs/019.md | 3 | — |
| 020 | Make the SEO-lander → north-star funnel measurable (real results_count on search_success + pool_view on `?pool=` landings) | 8.5 | DONE (window closed 2026-07-25 MOVED — funnel verified measurable: real results_count, pool_view + source live; see LEARNINGS.md) | LOW | specs/020.md | 1 | — |
| 021 | 014 phase 2 — networked generate real /tokens/ pages + sitemap-token-pages.xml + internal links. KEEP BOTH surfaces distinct (human 2026-07-11): `/tokens/<slug>` = static SEO content, `?token=<SYMBOL>` = interactive app — DIFFERENT content, each self-canonical, NO consolidation; makes the 014 pages earn traffic (2026 SEO: orphan pages don't index) | 8.2 | DONE (window closed 2026-07-25 MOVED — 2,075 real /tokens/ pages live via CI, sitemap wired, IndexNow submitting; traffic read human-owned via GSC; see LEARNINGS.md) | HIGH | specs/021.md | 1 | — |
| 024 | North-star: make the plan share a UNIQUE WORKING LINK that reproduces the sender's exact garden on open — one-tap "copy this garden" (human 2026-07-11: link attached to the garden, NOT an image download). Link must carry full plan state + actually rebuild the setup instantly on arrival | 7.8 | DONE (window closed 2026-07-25 INCONCLUSIVE — share_link_created 0, zero-distribution class per 005/007/008; roundtrip stays render-verified; see LEARNINGS.md) | HIGH | specs/024.md | 1 | — |
| 023 | Token-page content depth + internal linking — thin-content mitigation (2026 SEO: thin = dataset problem): short unique per-token intro + related-token/related-chain cross-links so pages aren't bare tables | 7.2 | SHIPPED (PR #114, merged 2026-07-11) | HIGH | specs/023.md | 1 | — |
| 025 | North-star: concrete prefilled projection on the garden_cta (button shows "→ ~$X in 5y", horizon prefilled, anomaly-gated) | 7.0 | DONE (window closed 2026-07-25 INCONCLUSIVE — garden_cta 0 all window; frame lives on in 129; see LEARNINGS.md) | HIGH | specs/025.md | 1 | — |
| 027 | Share-card generator: real bundled service LOGO on the first row (not 🌱) + fix label/headline mismatch (screenshot: "Spotify" label under a "ChatGPT Plus" headline). Hero viral artifact — canvas taint bug breaks all image shares, so NEEDS a browser+network run to build+verify | 8.6 | SHIPPED — verifier PASS (HIGH, 6/6), merged PR #122. Quality/trust fix on the hero share artifact; no isolated funnel metric to measure — see specs/027.md | HIGH | specs/027.md | 1 | — |
| 022 | IndexNow ping on token-page / sitemap changes (Bing/Yandex fast indexing; Google not official) — cheap freshness win for newly generated /tokens/ pages | 6.5 | DONE (window closed 2026-07-25 MOVED — live CI log run 30147683111: 2,102 URLs to IndexNow, HTTP 200; see LEARNINGS.md) | HIGH | specs/022.md | 1 | — |
| 026 | SEO ops: wire a periodic GSC Index Coverage read into the heartbeat (2026 source-of-truth) to measure whether 011–014 actually drain the not-indexed classes over the 2-6wk validation cycles | 6.5 | PARKED (human 2026-07-12, interview Q3: no connector for now — human checks GSC manually through ~2026-08-23; heartbeat must not nag before then) | LOW | — | 0 | — |
| 028 | Token-page design: reuse the app's neumorphic design system (restyle only, content unchanged) — link /style.css + tokens-only scoped CSS, neuro surface card + press physics + focus ring, light/dark for free | 7.0 | SHIPPED (PR #117, verifier PASS, merged 2026-07-11) — residual: human browser eyeball on the pulled page (light/dark) | LOW | specs/028.md | 1 | — |
| 029 | Token pages: make pool rows link to their pool-detail pages (/?pool=<id>), fallback to /?token= | 7.0 | SHIPPED (PR #120, merged 2026-07-11) — row was missing from BACKLOG, reconciled from git history + specs/029.md | LOW | specs/029.md | 1 | — |
| 030 | Token-page quality bar — only index tokens with ≥1 non-zero-yield pool (thin-content mitigation) | 7.5 | SHIPPED (PR #121, merged 2026-07-11) — row was missing from BACKLOG, reconciled from git history + specs/030.md | HIGH | specs/030.md | 1 | — |
| 031 | Token-page regeneration removes stale pages (CI clears out/*.html before writing the current set) | 7.0 | SHIPPED (PR #123, merged 2026-07-11) — row was missing from BACKLOG, reconciled from git history + specs/031.md | HIGH | specs/031.md | 1 | — |
| 032 | Token-page yield gate: require VISIBLE non-zero APY (formatApy !== "0.00%"), not just >0 | 7.5 | SHIPPED (PR #125, merged 2026-07-11) — row was missing from BACKLOG, reconciled from git history + specs/032.md | HIGH | specs/032.md | 1 | — |
| 033 | Token-page yield gate checks the DISPLAYED (top-N-by-TVL) pools, not all qualifying pools (truncation fix) | 7.5 | SHIPPED (PR #126, merged 2026-07-11) — row was missing from BACKLOG, reconciled from git history + specs/033.md | HIGH | specs/033.md | 1 | — |
| 034 | Pool-detail projection/CTA reflect the calculator's investmentAmount (was hardcoded $200/mo, disconnected from the on-page calculator) | 7.5 | SHIPPED (PR #130, verifier PASS HIGH 4/4, merged 2026-07-11) — row was missing from BACKLOG (ID collision with a concurrent 034-038 draft, renamed to 039-043 per LOG.md 2026-07-11 entry), reconciled from git history + specs/034.md | HIGH | specs/034.md | 1 | — |
| 039 | Instrument the static SEO surface (`/tokens/<slug>` x2,045, `stories/*.html` x3) — zero pages load analytics.js today, so the whole SEO-lander investment (014/021/023/028-033) is unmeasured | 9.2 | DONE (window closed 2026-07-25 MOVED — first-ever prod page_views on the static surface 07-25 prove the instrumentation fires; see LEARNINGS.md) | HIGH | specs/039.md | 1 | — |
| 040 | JSON-LD structured data: Organization/WebSite (home), BreadcrumbList (token pages + pool-detail), FAQPage (stories/kevin.html) — zero structured data anywhere in the repo today | 8.0 | SHIPPED (PR #133, merged 2026-07-12) — no isolated Mixpanel funnel metric (structured data affects search/AI-citation behavior); proxy is GSC Enhancements once 026 unblocks; PoolDetail.js's live-DOM render unverified in this sandbox (unpkg.com/yields.llama.fi network-blocked, confirmed via curl/proxy status, not a regression — pre-existing bare-/ and ?token=USDC Playwright checks fail identically) — needs human visual/render spot-check | HIGH | specs/040.md | 1 | — |
| 041 | Extend the 014/021 static-landing-page pattern to chains (`/chains/<slug>`) — ~100 sitemap-chain-*.xml entries still thin `?chain=` shells | 7.5 | SHIPPED (2026-07-12, PR #136) — verifier PASS (HIGH, 16/16, independently re-derived); generator+CI/sitemap wiring shipped in one PR (021 already proved the pattern end-to-end for tokens); CI live-verified same session: 86 real `/chains/<slug>` pages generated + `sitemap-chain-pages.xml` wired into the sitemap index on the first `sitemap-update.yml` run after merge → **DONE 2026-07-26** (14d window closed: build leg MOVED, traffic leg INCONCLUSIVE (4 chain page_views vs ≥30); written to LEARNINGS.md) | HIGH | specs/041.md | 1 | 2026-07-26 |
| 042 | Audit + selectively re-enable disabled app.js analytics calls (trackFilterChange x4, trackFiltersReset, trackFilterCombination, trackNavigation, search-input TODO at app.js:1166) | 6.5 | SHIPPED (2026-07-12, PR #135) — verifier PASS (LOW, 5/5, independently re-derived): 6/7 calls live-verified firing via Playwright with zero page errors; 7th (pool_type) confirmed pre-existing unreachable dead code, documented not fixed | LOW | specs/042.md | 1 | — |
| 043 | Semantic content clustering: group token-page related-links/intros by pool-type category (Lending/LP-DEX/Staking/Yield-Farming, already computed for the sitemap) for topical-authority internal linking | 6.0 | SUPERSEDED by 049 (its category-cross-linking leg; 2026-07-12) | HIGH | — | 0 | — |
| 044 | Verify SEO-surface instrumentation actually FIRES on a real render — 14d Mixpanel shows `/tokens/*` page_view=0 and `pool_view source=url_direct`=0 despite 039/020 shipping that instrumentation; both were verifier-PASS on fixture-strings only (Playwright never ran on a live page in-sandbox). Separate "no organic traffic yet" (expected, GSC 2-6wk) from "silently not wired" via one headless render each | 7.0 | SHIPPED (PR #145, 2026-07-12) — real bug found: `window.Analytics` guard in renderAnalyticsBootstrap always false (const in a classic script never becomes a window property), so /tokens+/chains page_view never fired regardless of traffic; fixed in generate-token-pages.js + generate-stories.js; pool_view url_direct was already sound (test_search.js/020). Real tokens/chains page regen lands on next sitemap-update.yml CI run; stories/*.html regen DONE 2026-07-12 (network unblocked, live regen picked up the fixed guard, test_stories.js 21/21) — 044 fully closed | HIGH | specs/044.md | 1 | — |
| 045 | De-orphan the SEO surface: `/tokens/` + `/chains/` hub pages linked from `home.html`, so all 2,108 spokes are ≤3 clicks from home (grep-confirmed: home.html links to the surface 0×, no hub pages exist — same root cause as the 010 GSC "not indexed" crisis, one level up) | 8.6 | SHIPPED (verifier PASS, HIGH, merged 2026-07-12) — real tokens/index.html, tokens/az/*, chains/index.html land on the next sitemap-update.yml CI run (yields.llama.fi network-blocked in-sandbox); fixture-verified generator output + home.html static links confirmed | HIGH | specs/045.md | 1 | 2026-08-09 |
| 046 | `ItemList` + `Dataset` JSON-LD on the pool tables (token + chain pages) — the pages' core ranked data has zero structured data today (040 shipped Org/WebSite/Breadcrumb/FAQ, deferred this); `ItemList` = carousel + AI-citation eligibility, and the correct type vs Product for a collection | 7.8 | SHIPPED (PR #141, merged 2026-07-12; verifier PASS HIGH 4/4 independently re-derived) — no isolable Mixpanel funnel metric (structured data affects search/AI-citation behavior, same disclosure pattern as 040); proxy is GSC Enhancements once 026 unblocks | HIGH | specs/046.md | 1 | — |
| 047 | GEO/AEO: direct-answer opener (first ~200 words answer the head query) + data-derived `FAQPage` on token/chain pages — none exist today (only kevin story has FAQPage); ~70% of DeFi queries are AI-resolved in 2026, answer-first + FAQ schema is prime citation fuel | 7.5 | SHIPPED (PR #143, merged 2026-07-12) — verifier PASS (HIGH, 5/5, independently re-derived); no isolable Mixpanel funnel metric (structured data affects search/AI-citation behavior, same disclosure pattern as 040/046); proxy is GSC Enhancements + AI-citation appearances once 026 unblocks | HIGH | specs/047.md | 1 | — |
| 048 | Freshness signals: visible "Last updated" + `dateModified`/`datePublished` schema on generated pages — grep-confirmed 0 freshness signals despite daily CI regen; 2026 AI recency bias drops citations to content that looks >3mo old | 7.0 | SHIPPED (2026-07-12, PR pending merge) — verifier PASS (HIGH, 5/5); live-verified real generator output (not fixture-only); needs human visual spot-check (render-path merge, backlog 003 gate doesn't cover static-page generators) → **DONE 2026-07-26** (14d window closed: build leg MOVED, effect leg INCONCLUSIVE (GSC/AI-citation only, no in-loop source); written to LEARNINGS.md) | HIGH | specs/048.md | 1 | 2026-07-26 |
| 049 | Cross-surface internal linking: token↔chain↔category (hub-and-spoke topical graph) — token pages link to chains 0× today; folds in / supersedes 043 (reconcile next heartbeat) | 6.5 | SHIPPED (PR #148, merged 2026-07-12) — verifier PASS (HIGH, 5/5, independently re-derived) → **DONE 2026-07-26** (14d window closed: build leg MOVED (leaf-page crawl arrival observed), traffic leg INCONCLUSIVE; written to LEARNINGS.md) | HIGH | specs/049.md | 1 | 2026-07-26 |
| 050 | `hreflang` + Korean static token/chain pages — `ko` is a first-class app language, story pages have hreflang, but all 2,108 token/chain pages are English-only with 0 hreflang | 6.0 | SHIPPED (PR #150, merged 2026-07-12) — real `ko/tokens/`+`ko/chains/` pages + `sitemap-token-pages-ko.xml`/`sitemap-chain-pages-ko.xml` land on the next sitemap-update.yml CI run (yields.llama.fi network-blocked in-sandbox, verified against offline fixtures instead); GSC Korean-query signal needs 026 to unblock | HIGH | specs/050.md | 1 | 2026-08-11 |
| 051 | Per-page dynamic OG images (unique token/chain social + SERP imagery) — every page ships the one shared `og-image.png` today | 5.5 | SHIPPED (2026-07-12, verifier PASS HIGH 4/4 — new dep + diff size, not a trust-rail/scope issue) — real og/tokens/*.png + og/chains/*.png land on the next sitemap-update.yml CI run; needs human visual spot-check once that run produces real pages | HIGH | specs/051.md | 1 | 2026-08-09 |
| 052 | Perf: precompile app.js/PoolDetail.js in CI → plain JS; home.html drops `@babel/standalone` (~2.9MB unpkg + main-thread transpile of 171K on every analytics/SEO page — the dominant mobile PageSpeed hit) | 9.0 | SHIPPED (PR #154, merged 2026-07-12) — CI compile step (compile-app.js, @babel/plugin-transform-block-scoping) commits app.compiled.js/PoolDetail.compiled.js; home.html loads compiled output, no @babel/standalone. Verifier PASS HIGH 5/5. test_smoke.js unverifiable in-sandbox (unpkg.com/yields.llama.fi network-blocked, same as 040/044/045/051) — needs human visual spot-check on next CI/live run | HIGH | specs/052.md | 1 | — |
| 053 | Perf: minify JS/CSS in CI (app 119K/planner 196K/style 123K/translations 67K/PoolDetail 52K raw); reference minified files. Sequence after 052 | 8.0 | SHIPPED (PR #156, merged 2026-07-12; verifier PASS HIGH 4/4 independently re-derived) — real *.min.js/*.min.css already committed in the PR (unlike the sitemap/token/chain generators, this needed no network fetch to run — pure JS/CSS transform); no isolable Mixpanel funnel metric (perf/PageSpeed change), proxy is Core Web Vitals/PSI once re-measured live; test_smoke.js unverifiable in-sandbox (unpkg.com/yields.llama.fi network-blocked, same precedent as 040/044/045/051/052) — needs human visual spot-check | HIGH | specs/053.md | 1 | — |
| 054 | Perf: `preconnect` to unpkg (React/Babel critical path) + yields.llama.fi — only dns-prefetch today, 0 preconnect tags | 7.0 | SHIPPED (PR #158, merged 2026-07-12) — needs human visual spot-check on next live run (render-path touch, test_smoke.js network-blocked in-sandbox); not independently Mixpanel-isolable (perf/resource-hint change, no funnel step) | LOW | specs/054.md | 1 | — |
| 055 | Perf: long-cache Cache-Control for static JS/CSS/fonts in vercel.json (only `/`+`/home` have headers today) — content-hash/versioning aware so updates still propagate | 6.5 | SHIPPED (PR #160, merged 2026-07-12; verifier PASS HIGH 7/7) — fonts get 1yr immutable, JS/CSS get 5min+1day-SWR (not immutable — unhashed filenames, multiple deploys/day); `/`,`/home` untouched at max-age=0; no isolable Mixpanel funnel metric (perf/resource-hint change, same disclosure pattern as 054), proxy is PageSpeed's cache-lifetime audit on next live measurement; needs human visual/network spot-check on next deploy (header-serving behavior, same precedent as 040/044/045/051-054) | HIGH | specs/055.md | 1 | — |
| 056 | Perf: self-host React (react/react-dom) to drop the 3rd-party unpkg critical dependency (control + cache). Pairs with 052 | 6.0 | SHIPPED (2026-07-12) — verifier PASS (HIGH, 7/7, independently re-derived incl. baseline-worktree comparison showing unpkg was unreachable pre-change, self-hosting is a verified reliability fix, not just sandbox-neutral); no isolable Mixpanel funnel metric (perf/dependency change, same disclosure pattern as 054/055), proxy is PageSpeed/Core Web Vitals on next live measurement; needs human visual spot-check on next deploy (render-path touch, same precedent as 040/044/045/051-055) | HIGH | specs/056.md | 1 | — |
| 057 | Perf: optimize og-image.png (280K) + generated OG PNGs (051) — compress/resize/modern format | 5.5 | PARKED (human 2026-07-12: "compression later"; PSI 88 mobile shows zero image-related opportunity — no urgency) | LOW | — | 0 | — |
| 060 | X-spotlight pack generator (`generate-spotlight.js`): given a pool id (or auto-picked candidates biased to SMALL collaboration-keen protocols, Curve = upper bound, through the full trust rails), emit a per-pool spotlight pack — live numbers, a WORKING example-garden share URL that rebuilds the garden on open (024's artifact), a tweet draft tagging the protocol, Canva-template field values, share-card PNG. Human edits the Canva video + posts. Direct build-out of the 2026-07-12 distribution decision | 9.0 | SHIPPED (2026-07-12/13, verifier PASS HIGH 8/8 independently re-derived) — closed a real pre-existing gap in 024's share URL (chain/token never round-tripped) as a prerequisite; `spotlights/` output not yet committed (no live-network production run made in-sandbox, same as tokens/chains/og convention — next human-triggered `npm run spotlight` run should commit real output) | HIGH | specs/060.md | 1 | — |
| 061 | Waitlist reframe to the card value prop: audit current waitlist copy/placement, reframe to the yield-funded subscription card (deposit → position → disposable card pays the sub), EN+KO together, honest early-access framing (card doesn't exist yet — no fake availability, no dark patterns); measure `waitlist_opened → waitlist_submitted` (009 events) pre/post | 8.0 | SHIPPED (PR #171, merged 2026-07-12) — verifier PASS (LOW, 9/9, independently re-derived incl. own fixture-routed Chromium render); attempt 1 caught a real bug (translations.min.js never regenerated, so the new copy shipped nowhere — plan.html loads the `.min.js`), fixed in attempt 2; also surfaced (not fixed, out of scope) that `ctaWaitlistMicro`'s only render site is pre-existing dead code (`ctaElement`, unmounted) — candidate for a future item → **DONE 2026-07-26** (14d window closed: INCONCLUSIVE — waitlist_opened/submitted/email_entered/submit_attempt ALL 0, never fired; written to LEARNINGS.md) | LOW | specs/061.md | 2 | 2026-07-26 |
| 062 | North-star: waitlist entry point on the 2,131 static SEO pages (token+chain) — today they route only to `?token=`/`?pool=`, ZERO path to the waitlist (the card funnel). One honest card-pitch CTA per page → `plan.html?waitlist=1&src=seo_*` (planner.js auto-opens waitlist + `source`); the missing SEO→north-star bridge | 8.5 | DONE (window closed 2026-07-27 INCONCLUSIVE — `waitlist_opened` never fired at all in 07-13→07-27 (`9fe39c5b`); the CTA is live on 2,131 static pages and unvisited. See LEARNINGS.md) | HIGH | specs/062.md | 1 | — |
| 063 | North-star: waitlist funnel drop-off instrumentation — 009 fires opened+submitted but the gap is invisible; add waitlist_email_entered / submit_attempt / error(reason) so we can see WHERE the card funnel leaks (measure-first, per 020) | 7.5 | DONE (window closed 2026-07-27 INCONCLUSIVE — every drop-off event (`waitlist_email_entered`, `waitlist_submit_attempt`, `waitlist_cta_click`) absent from the result set; instrumentation correct and unexercised. See LEARNINGS.md) | LOW | specs/063.md | 1 | — |
| 064 | Distribution: X-spotlight → waitlist attribution + cadence — 060 ships the spotlight pack; add utm/ref on spotlight links so `waitlist_opened(source=x_spotlight)` is attributable, + a repeatable cadence/coverage list so the human's posting is systematic (weekly theme = distribution) | 8.0 | DONE (window closed 2026-07-27 INCONCLUSIVE, with a clean NEGATIVE — prod `page_view` by `src` over the full window (`d649acd9`) returns one row, `undefined`=48: zero `src=x_spotlight` arrivals, i.e. the packs have not been posted yet. Attribution built and unexercised, not broken. See LEARNINGS.md) | LOW | specs/064.md | 1 | — |
| 065 | North-star: waitlist value-prop framings — 061 reframed the copy once; test 2-3 honest card pitches (yield-funded framing) pre/post over 14d, EN+KO, instrumented; pick the winner on submit-rate | 6.5 | SHIPPED (2026-07-13) — verifier PASS (LOW, 9/9, independently re-derived incl. own Chromium render of all variants). Gate satisfied: "sequence AFTER 064", 064 shipped 2026-07-13; built now with no traffic dependency (063 precedent). Default stays byte-identical to 061's copy (its 2026-07-26 window uncontaminated); B/C framings live behind `?pitch=b|c` (spotlight links can carry it from day one); all 5 waitlist events segment by `pitch_variant`. Winner picked on submit-rate, ≥30 opened/variant, after sequential default windows or tagged-link traffic post-07-26 | LOW | specs/065.md | 1 | 2026-08-09 |
| 066 | Custom KPI (no backend, ties 058 direction to conversion): honest per-token 'what your idle <TOKEN> could earn' headline — one number from the single /pools fetch through the trust rails, on token pages + spotlight packs; strengthens the card pitch without the data layer | 7.0 | DONE (window closed 2026-07-27 INCONCLUSIVE — headline live on the committed token surface; no waitlist event fired in window, traffic leg GSC-owned (human ~08-23). See LEARNINGS.md) | HIGH | specs/066.md | 2 | — |
| 067 | Fix $0 projection on capital-funded GROWTH plans (retirement/home) — human-reported production bug 2026-07-13, not heartbeat-derived. `/plan.html?goal=retirement&pace=rwa&capital=1000&fm=capital&years=5` rendered "≈ $0 in 5 years" / "vs $0 savings" / "deposited $0" despite correct $1,000 capital + 10.58% APY in the sidebar; `futureValue(monthly,...)` never read `capital` for GROWTH (TARGET/SUBSCRIPTION already capital-aware) | 9.5 | SHIPPED (2026-07-13, direct-to-main single commit) | HIGH | specs/067.md | 1 | — |
| 068 | North-star: planner hero-copy CRO test (SPLITSTREAM audit 2026-07-13) — the hero is the top of the `waitlist_submitted` funnel yet is feature-named. Swap 3 strings: title "Garden Planner"→outcome frame ("pay your bills with yield, forever"), tagline "Plant a goal…"→mechanism frame ("live DeFi yield covers it automatically"), splashHook→**live-derived** proof anchor (rate + honest time-to-cover). Trust-rail guardrail: V03's rate MUST be the live blended APY through the sanity/TVL/anomaly filters + `formatApy` — the audit's hardcoded "8-15% APY within months" = FAIL. EN+KO + regenerate `translations.min.js` (plan.html loads the min — 061's caught bug); measured on the 009 waitlist funnel | 8.0 | DONE (window closed 2026-07-27 INCONCLUSIVE — the 009 funnel it was measured on recorded zero events of every kind in 07-13→07-27; the hero copy is live and correct, the funnel beneath it is unvisited. See LEARNINGS.md) | HIGH | specs/068.md | 1 | — |
| 069 | Distribution: first ready-to-post spotlight packs (live run, committed ×3, distinct small protocols) + degen-haircut-honest pack numbers — packs' foreverAmt/tweet used HEADLINE APY while the linked garden projects degen at ⅓ (planner.js:1354-1357), and auto-pick selects degen pools (060-notes smoke test); fix + produce, so the human's weekly posting is one-tap (reports/2026-07-13 Q-f; standing decision 2026-07-12 assigns pack-building to loops) | 8.4 | SHIPPED (2026-07-13, verifier PASS HIGH 8/8, attempt 1) — 3 packs committed (pareto-credit/USDC/Ethereum stable · yo-protocol/USDC/Base rwa · project-x/WHYPE-USDC/Hyperliquid degen 20.10%→6.70% haircut-honest); posting is now one-tap for the human; per-pool measurement (src=x_spotlight + ref, via 064) starts at first human post — 14d window from that date | HIGH | specs/069.md | 1 | 14d from first post |
| 070 | Surface the honest "card doesn't exist yet" disclaimer at the live waitlist CTA — `ctaWaitlistMicro` (EN+KO, styled, guard-tested since 061) renders NOWHERE: its only call site is the dead `ctaElement` (flagged specs/009-notes.md + specs/061-notes.md); wire it under the live checkout CTA, delete the dead element, rendered-Playwright acceptance | 7.2 | SHIPPED (2026-07-13, verifier PASS HIGH 6/6 independently re-derived incl. baseline-worktree check of the sandbox-network claim; promoted by build loop — no READY items existed) — no isolated funnel metric of its own (honesty fix on an existing step); lands inside 061/065's waitlist-funnel windows; needs human visual spot-check (bloom checkout render path) | HIGH | specs/070.md | 1 | — |
| 071 | Pool-detail rate-volatility honesty note — human-reported (specs/067-notes.md screenshots): hero "142.84%" total APY vs "405.32%" 30d mean on ONE page, zero explanation, and Pool Information is expanded by default; render a calm neuro-token note when current vs 30d mean diverge ≥1.5×, clarifying rates move and projections use the current rate (EN+KO, compiled/minified artifacts regenerated, rendered-Playwright acceptance) | 7.4 | SHIPPED (2026-07-13, one-commit PR on the harness-pinned branch, verifier PASS HIGH 7/7 independently re-derived incl. own stash-baseline of the pre-existing test_smoke sandbox failure) — no isolated funnel metric (honesty fix on the pool-detail step, 070 disclosure pattern; lands inside 019/025's measurement stream); needs human visual spot-check (pool-detail render path) | HIGH | specs/071.md | 1 | — |
| 072 | Honest dead-`?pool=` empty state — noindex + "no longer tracked" + stable alternatives (extends 012 to pool deep links; documented candidate in specs/012-pr.md, soft-404 class from 010-diagnosis, link-rot protection for 024/060/069 share+spotlight artifacts) | 7.2 | SHIPPED (2026-07-13, one-commit PR on the harness-pinned branch; verifier attempt 1 FAIL only on missing pre-merge bookkeeping — code judged 6/6 — attempt 2 PASS HIGH) — no isolated funnel metric (honesty/SEO-quality fix, 012/070/071 disclosure pattern); proxy = GSC soft-404 class over the ongoing 2-6wk validation, human-checked per interview Q3 | HIGH | specs/072.md | 1 | — |
| 073 | Perf: minify planner-styles.css (83K) + pool-detail-styles.css (24K) — 053's explicitly documented follow-up (specs/053-notes.md); planner-styles ships raw on the funnel-top `/`+plan.html routes, pool-detail-styles raw on the analytics/pool-detail path; same clean-css pipeline, ~20-25KB expected off funnel-critical routes | 6.0 | SHIPPED (2026-07-13, one-commit PR on the harness-pinned branch, verifier PASS HIGH 7/7 independently re-derived incl. mutation-check of the rendered gate) — planner-styles 83,011→67,196 B (−19.05%), pool-detail-styles 23,812→19,244 B (−19.18%), ~20.4 KB total off the funnel-top + pool-detail routes; no isolable Mixpanel funnel metric (perf change, 053-056 disclosure precedent), proxy = PSI on next live measurement; needs human visual spot-check (render-path merge) | HIGH | specs/073.md | 1 | — |
| 074 | Fix critical fast-xml-parser vulnerability (v4→v5 major bump) — npm audit CRITICAL (6 advisories: DOCTYPE entity-encoding bypass, unbounded entity-expansion DoS, XMLBuilder overflow/injection) on a production dep of the daily SEO-pipeline CI (generate-llms.js, validate-sitemaps.js); flagged + "recommend a new backlog item" in specs/003-notes.md 2026-07-10, never filed; promoted by build loop 2026-07-13 (no READY items) | 6.8 | SHIPPED (2026-07-13, one-commit PR on the harness-pinned branch, verifier PASS HIGH 5/5 independently re-derived incl. npm-ci stash-baseline PROOF of the pre-existing test_smoke sandbox failure) — audit 1 critical → 0; zero call-site changes (v5 API-compatible); parse-stage output byte-identical (23,599 URLs); no isolable funnel metric (supply-chain/security fix, protects the SEO-pipeline guardrail) | HIGH | specs/074.md | 1 | — |
| 075 | Extend the honest idle-yield headline to chain pages — 066's explicitly documented deferred leg ("token-first this pass"); 86×2 `/chains/<slug>` pages carry the 062 waitlist CTA but no concrete pitch number; reuse `yieldHeadlineFor` (median blend, Claude-Pro anchor, null-gate) + new `tcpYieldHeadlineChain` EN/KO; promoted by build loop 2026-07-13 (no READY items) | 6.5 | DONE (window closed 2026-07-27 INCONCLUSIVE — measured inside 062 stream, which recorded zero waitlist events. See LEARNINGS.md) | HIGH | specs/075.md | 1 | — |
| 076 | Fix OG-image outRoot ignoring `--out` in token/chain generators — verifier-found footgun with demonstrated harm (075 verification briefly deleted committed og/chains/metis.png): `generateOgImages(..., process.cwd())` at generate-chain-pages.js:448 AND generate-token-pages.js:926 makes ANY scratch/verification run rewrite + stale-delete the repo's committed og/ PNGs; fix = pass `path.dirname(outDir)` (existing koOutDir sibling convention), CI path invariant | 6.6 | SHIPPED (2026-07-13, one-commit PR on the harness-pinned branch, verifier PASS HIGH 6/6 independently re-derived) — pure script+test fix, zero committed-asset churn (og:image URLs/HTML unaffected); no isolable funnel metric (loop-infrastructure/asset-integrity fix, protects the "never delete SEO surface" guardrail); new test_og_outroot.js (4 asserts) in the npm chain guards the regression | HIGH | specs/076.md | 1 | — |
| 077 | Make the smoke gate (003) pass in-sandbox: fixture-routing fallback for test_smoke.js (test_search.js pattern, prescribed by NORTH_STAR 2026-07-12) — the only Playwright test without it; halts the full `npm test` chain every loop run (069/071/073/074 notes all stash-baseline-proved it) and leaves the SEO-lifeline guardrail (`?token=` renders pool cards) unverified in-sandbox; live-snapshot-first (curl reaches llama, browser doesn't), fixture fallback, URL-based ignorable-error matching; promoted by build loop 2026-07-13 (no READY items) | 6.8 | SHIPPED (2026-07-13, one-commit PR on the harness-pinned branch, verifier PASS LOW 5/5 independently re-derived incl. own stash-baseline + mutation check) — smoke gate now 8/8 in-sandbox (live-snapshot mode: curl reaches llama, browser routed), `npm test` proceeds past test_smoke.js for the first time since 052; no isolable funnel metric (loop-infrastructure/guardrail fix, 074/076 disclosure pattern) | LOW | specs/077.md | 1 | — |
| 078 | Fix the dead sitemap health signal in the daily CI commit message — `grep -c '<url>' sitemap.xml` reads 0 on every run since the index split (sitemap.xml = `<sitemapindex>`, URLs live in children), so a REAL zero-URL wipe is indistinguishable from a healthy run and the false "0 URLs" already cost a loop run an investigation (LOG 2026-07-13); replace with index-child count + total `<url>` across root sitemap*.xml, `bash -eo pipefail`-safe, honest zero preserved | 5.8 | SHIPPED (2026-07-13, one-commit PR on the harness-pinned branch, verifier PASS HIGH 6/6 independently re-derived incl. own 4-way count cross-check 110/25,039 + own zero-match `set -eo pipefail` scratch test) — promoted by build loop (no READY items; overrides the prior run's "heartbeat candidate" call with recorded reasoning: dead guardrail-observability signal, not cosmetic); no isolable funnel metric (loop-infrastructure/observability fix, 074/076/077 disclosure pattern); live proof = next daily sitemap-update.yml run's commit message carries real non-zero counts | HIGH | specs/078.md | 1 | — |
| 079 | Waitlist CTA on the 58 hub/az spine pages (tokens/chains hubs + az sub-hubs, EN+KO) — 062's last gap: the only SEO pages with zero path to the north-star funnel, and the internal-linking spine every home→SEO navigation crosses; promoted by build loop 2026-07-14 from LOG #201's own verification (recount: 58 pages, not the 2 the tick recorded — az + KO mirrors were never checked) | 6.9 | DONE (window closed 2026-07-27 INCONCLUSIVE — hub sources land inside 062 stream, zero `waitlist_opened` of any source. See LEARNINGS.md) | HIGH | specs/079.md | 1 | — |
| 080 | Deploy 013's sitemap quality gate for real — daily CI regenerates ALL ~106 sitemap children (`generate-sitemap.js`) but `git add` stages only 5 of them, so 013's GSC crawl-budget fix (shipped 07-11) NEVER reached production: deployed children stale at ~20.7k URLs vs 448 live-gated, fresh index over stale children (lastmod lies), 12 orphan child sitemaps (combos/chains/10 chain files) deployed but unreferenced; fix = stage `sitemap*.xml` incl. deletions + generator stale-child cleanup (031 pattern) + commit real live regen now; promoted by build loop 2026-07-14 from 078's first live count (6,202 CI vs 25,051 committed) + specs/013-notes.md's documented false deployment belief | 8.2 | SHIPPED (2026-07-14, one-commit PR on the harness-pinned branch, verifier PASS HIGH 7/7 independently re-derived incl. scratch-repo pathspec demo + two mutation checks) — deployed sitemap URLs 25,051→4,774 (013's intent, finally live), 12 orphan child sitemaps removed, index/children consistent; measured via the human's manual GSC checks through ~2026-08-23 (interview Q3) + the next daily CI commit message's count converging with committed truth | HIGH | specs/080.md | 1 | 2026-08-23 |
| 081 | Honest sitemap lastmod: preserve per-URL/per-child timestamps when content is unchanged (byte-identical children, index lastmod = max of child) + workflow concurrency guard — every CI run today stamps `now` on all ~4.8k URLs/110 children, guaranteeing a commit (= a Vercel deploy, quota crisis per 2026-07-13 decision) even with ZERO content change (live proof: 8fee9e9ba, 560 lines pure lastmod churn 18 min after the prior run) and making lastmod lie daily, which Google ignores wholesale — forfeiting 013/080's crawl-budget benefit during the human's manual GSC window (to ~08-23); promoted by build loop 2026-07-14 (no READY items, new same-day CI evidence) | 7.0 | SHIPPED (2026-07-14, one-commit PR on the harness-pinned branch, verifier PASS HIGH 8/8 independently re-derived incl. own live double-run, own old-vs-new fixture diff 0-files-differ-modulo-lastmod, mutation check with sha256-proven restore) — verifier surfaced the residual: generate-llms.js stamps unconditional timestamps (3 sites), so daily CI still commits every run until llms gets the same treatment — documented follow-up candidate, NOT built this run (one-item rule); no isolable Mixpanel funnel metric (loop-infrastructure/SEO-integrity fix, 074/076/077/078/080 disclosure pattern); proxies = next CI diffs shrink to really-changed children + GSC crawl stats in the human's manual window | HIGH | specs/081.md | 1 | — |
| 082 | BUG (funnel-top guardrail): harden the planner against `ReferenceError: translations is not defined` — first-ever `error_occurred` fired 2026-07-13 on the bare `/` (planner), Chrome/desktop/en-US, stack = `planner.min.js` inside a `useMemo`; root cause `planner.js:795` `makeT()` dereferences the bare global `translations[lang]`, which throws when `translations.min.js` didn't define the global by call time (transient load failure/deploy race). `can_recover=true` but the planner render broke for that session → funnel-top failure. Defensive guard + English fallback, planner.min.js regenerated, new fallback test. NORTH_STAR: a bug at a funnel step outranks any experiment | 8.5 | SHIPPED (2026-07-14, one-commit PR on the harness-pinned branch, verifier PASS HIGH 6/6 independently re-derived incl. own mutation check proving the new test catches the regression) — safeTranslations() guard + lazy per-call re-check in makeT/rootT, key-echo degradation (inline-en-dict deviation documented + verifier-accepted: English strings live inside the payload that failed to load), planner.min.js regenerated, test_translations_fallback.js (8 asserts) in the npm chain; needs human visual spot-check (planner funnel-top render path) → **DONE 2026-07-28** (14d window closed: **MOVED (weak-n)** — `error_occurred` 0 every day 07-14→07-28, 15 consecutive clean days, absent from the result set; but only ~82 sessions and ONE `plan_created` exercised the planner in the window, so this is "no recurrence observed", not "proven robust"; written to LEARNINGS.md) | HIGH | specs/082.md | 1 | error stops recurring: 0 recurrence to 2026-07-28 |
| 083 | Honest llms.txt freshness: content-derived timestamps in generate-llms.js (skip rewrite when output is byte-identical modulo the 3 timestamp lines) — 081's verifier residual, recorded as next promotion candidate: llms.txt/llms-full.txt are the last unconditional churn, so the whole-tree porcelain gate still commits (= a Vercel deploy, 2026-07-13 quota crisis) on EVERY daily run and "Last Updated" lies daily; live proof 8fee9e9ba (6 non-lastmod lines = exactly these) | 6.8 | SHIPPED (2026-07-14, one-commit PR on the harness-pinned branch; renumbered 082→083 mid-run: a concurrent heartbeat commit on main took ID 082 for the planner ReferenceError bug — 034→039 ID-collision precedent, loop renames its own; verifier attempt 1 FAIL on a real defect — the new test hardcoded a dead-session scratchpad path, ENOENT everywhere else, proven live via 081's identically-broken sibling — attempt 2 PASS HIGH 6/6 independently re-derived incl. own mutation check + live scratch-copy idempotency run) — also repaired test_lastmod_honesty.js (081's test, same defect class, chain position 33 blocked 083's test at 34; verifier-evidenced prerequisite); no isolable funnel metric (loop-infrastructure/SEO-integrity fix, 074-081 disclosure pattern); live proof = first no-data-change daily CI run skips its commit entirely (zero Vercel deploy — 081+083's saving realized together) | HIGH | specs/083.md | 2 | — |
| 084 | Real assertions for the NORTH_STAR test gate — test_protocol_parsing.js + test_qualifier_fix.js have ZERO asserts/non-zero exits and test a stale inline parser copy (the 017 "fixtures passed, product failed" class), so 2 of the 3 commands in NORTH_STAR's verbatim human-owned Test line are theater; rewrite both to extract + assert the REAL parseNaturalLanguageQuery from app.js (vm pattern, test_translations_fallback.js precedent); flagged specs/018-notes.md:152-156 "known follow-up"; promoted by build loop 2026-07-14 (no READY items; all north-star-direct work shipped/traffic-gated/human-gated) | 6.7 | SHIPPED (2026-07-14, one-commit PR on the harness-pinned branch, verifier PASS HIGH 6/6 independently re-derived incl. own sha256-verified mutation checks on the real app.js and own reproduction of the disclosed non-biting mutation) — the human-owned Test line can now actually fail; verifier + builder both confirmed a REAL pre-existing parser bug out of scope here (token-fallback substring test: "yields on ethereum"→token ETH, "on solana"→SOL; documented specs/084-notes.md, next promotion candidate); no isolable funnel metric (guardrail-integrity fix, 074/076/077/078 disclosure pattern) | HIGH | specs/084.md | 1 | — |
| 085 | BUG (human ticket 2026-07-14): subscription checkout auto-selects Spotify alongside the picked service — mix seed uses coveredBundle(default $5k capital) instead of the user's pick (planner.js:1507-1513); seed exactly [goal], preserve share-mix restore, rendered-Playwright acceptance | 8.6 | SHIPPED (2026-07-14, verifier PASS HIGH 4/4 incl. own mutation check reproducing the exact reported bug) — share trace re-derived by verifier: share URLs never carried a mix (anchor-only), so arrival now reproduces the sender's pick exactly; mix-to-URL documented as future candidate (specs/085-notes.md); needs human visual spot-check (bloom render path); lands inside 061/065 waitlist windows | HIGH | specs/085.md | 1 | — |
| 086 | BUG (human ticket 2026-07-14): footer "Browse tokens/chains" (045) occluded by the fixed .app-footer — unclickable in analytics mode; move links INTO the Defillama disclosure footer (EN+KO), keep static raw-HTML anchors for crawlers, hide static only where duplicated visibly | 7.0 | SHIPPED (2026-07-14, verifier PASS HIGH 4/4 incl. HEAD reproduction of the real occlusion + two mutation checks + byte-identical artifact re-derivation) — links now in both .app-footer instances EN+KO, data-app-mode attribute stamp (router decision logic byte-identical), static crawler anchors preserved; needs human visual spot-check (footer render path, both modes) | HIGH | specs/086.md | 1 | — |
| 059 | Data-layer leg 1 (human sign-off 2026-07-14): static pools-snapshot.json — trust-rail-filtered 13-field CI snapshot + slices, FE snapshot-first with live fallback; drafted in docs/feasibility-data-layer.md (058); phase 1 = live payload measurement; ToS human-read still open (surface once, don't nag) | 8.0 | SHIPPED (2026-07-14, one-commit PR on the harness-pinned branch; verifier attempt 1 FAIL on a real defect — committed data/ was re-stamped by a test helper, generatedAt misrepresented generation time — attempt 2 PASS HIGH 21/21 independently re-derived incl. own byte-level slice/meta consistency checks and 12 suites run green) — measured live payload win: full /pools 10.58 MB / 2.10 MB gzip → railed 13-field snapshot 220 KB / 51.8 KB gzip (−97.6% wire); at daily CI cadence the 15-min freshness gate routes prod loads to live (honest leg-1 plumbing; cadence = human/087 decision); rollout observable via data_load_time.source in Mixpanel; 087 phase 0 now unblocked; ToS human-read still open (surface once) | HIGH | specs/059.md | 2 | — |
| 087 | Data-layer leg 2 (human directive 2026-07-14): history retention + KPI computation — phase 0 CI-retained slim snapshots + compute-kpis.js (no credentials, READY after 059); phase 1 Turso/libSQL ingestion BLOCKED on human-provisioned secrets (NEVER list: loops don't handle credentials) | 7.5 | SHIPPED (phase 0, 2026-07-14, one-commit PR on the harness-pinned branch; verifier attempt-1 FAIL solely on the missing HIGH-tier specs/087-pr.md merge-gate artifact — 12/12 spec criteria independently re-derived PASS incl. own real-data churn-trap round-trip + mutation-kill reverted clean; explainer written → gate satisfied) — new compute-kpis.js retains slim `data/history/<date>.json` points (append-only-on-change, 30-file prune) + writes null-safe per-pool `kpis` (historyPoints/firstSeen/apyMomentum/apyStdev/tvlTrend) INTO the snapshot + slices; 059 generator's freshness compare taught to ignore the kpis object (churn-trap fix — no daily Vercel deploy); enriched data/ artifacts land via the post-merge CI run (compute-kpis.js in workflow paths:), not this PR; test_compute_kpis.js 15/15 + test_pools_snapshot.js 9/9 green, full chain halts pre-existing at test_compiled_assets.js (@babel/core absent, before 087's test, diff-independent); no isolable funnel metric (data-layer infra feeding 088's ICP KPIs); phase 1 Turso/libSQL stays BLOCKED (credentials, NEVER-list). Unblocks 088. | HIGH | specs/087.md | 1 | — |
| 088 | Data-layer leg 3 (human directive 2026-07-14): ICP-facing decision KPIs in-app — rate stability/momentum, track record, TVL trend, plan-level projection confidence; calm cautious-saver language, EN+KO, KPIs rank within trust-rail-passing pools only, never override rails; start with ONE KPI on pool detail (071 pattern) | 7.5 | SHIPPED sub-item 088.1 (2026-07-14, one-commit PR on the harness-pinned branch, verifier PASS HIGH 5/5 independently re-derived incl. own compile/minify byte-identical re-derivation + a loaded-`.compiled.min.js` mutation check that bit D2/D3) — rate-track-record/steadiness note on pool detail surfacing 087's `kpis.historyPoints`+`apyStdev` in calm EN+KO cautious-saver language; renders on in-app browse→detail (snapshot carries kpis), hides on live `?pool=` SEO landings + when kpis absent, yields entirely to the 071 volatility note (mutual exclusion, same boolean); trust rails untouched (reads derived kpis only, never filters/reorders/re-flags). Today only the NEW "still building history" tier renders in prod (1 history point → apyStdev null everywhere); STEADY/TRACKED activate automatically as 087's daily CI accrues history — all 3 tiers exercised by test_kpi_track_record.js (7/7 rendered-Playwright). 088 STAYS OPEN for further surfacings (SEO deep-link kpis enrichment, momentum/TVL-trend, plan-level confidence). Needs human visual spot-check (pool-detail render path, 003 gate not pixel-level). **088.1's 14d window closed 2026-07-28: INCONCLUSIVE at n≈0** — the surface it renders on took `pool_view` 6 / 30d (`card_click` 5, `url_direct` 1) and `pool_click` 1 (pre-123, `source=undefined`), north-star clicks 0; kept, written to LEARNINGS.md | HIGH | specs/088.md | 1 | 2026-07-28 |
| 089 | Analytics-UX audit (human 2026-07-14): default browse sort → TVL, not APY — `app.js:759` `useState('apy')`; APY-first tops the grid with anomalous/reward-decayed pools, TVL-first matches the DeFi mental model (size = safety signal the cautious ICP reads first) + the app already ships an intentional non-user APY anomaly-demote (`app.js:1619-1624`). ~1-line default flip; keep the APY/TVL toggle + `userSortedApy` semantics intact; verify both router paths. COUPLES with 092 (TVL-first surfaces 0%-yield collateral at the top — ship 092 with/before this or the top rows read "0%") | 7.5 | SHIPPED (2026-07-14, one-commit on the harness-pinned branch, verifier PASS LOW 5/5 independently re-derived incl. own mutation-kill on the loaded app.compiled.min.js — revert to "apy" → 3/4 fail, restored clean) — sortBy default `'apy'`→`'tvl'` (single line); reuses 092's TVL-branch demote (0%-yield pools stay below yielding + "No supply yield" tag), APY/TVL toggle + `userSortedApy` semantics untouched, trust rails untouched; new rendered-Playwright test_default_sort.js (4/4) in the npm chain, siblings 090/092 tests green; no isolable Mixpanel funnel metric (analytics-app trust/clarity default). UNBLOCKED by 092. Needs human visual spot-check (browse render-path merge) | LOW | specs/089.md | 1 | — |
| 090 | Analytics-UX audit (human 2026-07-14): default view → list/rows, not grid/boxes — `app.js:758` `useState('grid')`; SOTA yield tables are row-first (denser scan, aligned numbers, less scroll). ~1-line default flip (`pools-list` path already exists, `app.js:2833`); keep the grid/list toggle; verify 360/768/1280 + dark. Pairs with 095 (row polish) | 7.0 | SHIPPED (2026-07-14, branch claude/loop-090 + PR, verifier PASS LOW 7/7 independently re-derived incl. own mutation check on the loaded app.compiled.min.js + a 360/768/1280+dark render spot-check) — `app.js:758` useState('grid')→('list'), the whole product change (viewMode is unpersisted component state, both views share renderPoolCard, only the container class differs); app.compiled.js/app.compiled.min.js regenerated; new rendered-Playwright test_list_default.js (3/3, real chromium, fixture-routed) asserts `?token=`/`?chain=` default to `.pools-list` + toggle round-trips. SEO lifeline intact (same cards). Row polish = 095 (soft-pair follow-up). Needs human visual spot-check (render-path default) | LOW | specs/090.md | 1 | — |
| 091 | Analytics-UX audit (human 2026-07-14): expand the category taxonomy beyond Lending/Staking/LP-DEX — add Borrowing, RWA, and Pendle/yield-derivatives (human named these) as first-class `getPoolType` categories (`app.js:84`, + the `LENDING/DEX_LP/STAKING_PROTOCOLS` lists) so the nav tabs + counts (`app.js:1537`) + NL-search parser cover them. Classification MUST stay honest (a pool's category derives from protocol/poolMeta, never fabricated); Borrowing vs Lending needs a real distinguisher (supply vs borrow side) or ship only the categories we can classify truthfully. EN+KO tab labels; extend `test_protocol_parsing`-class coverage | 7.0 | SHIPPED (2026-07-14, one-commit PR on the harness-pinned branch, verifier PASS HIGH 7/7 independently re-derived incl. own 12/12 getPoolType harness + own compile/minify byte-identity check + own rendered Playwright) — shipped RWA + Yield Derivatives as honest protocol-derived categories (live `/pools` audit: pendle 160/spectra/termmax → Yield Derivatives; ondo/centrifuge/goldfinch/openeden/midas/usual/maple/clearpool → RWA); **Borrowing DEFERRED** (documented): the yields API is supply-side only, only 0.5% of pools carry "borrow" in poolMeta, a borrow rate is a cost not an earning → no honest distinguisher. `maple` reclassified Lending→RWA (private credit, more accurate). Nav tabs data-driven via `CATEGORY_TABS`, labels now through `t()` EN+KO (fixed a latent hardcoded-English hard-rule violation); new `test_category_taxonomy.js` (5/5 parser + 4/4 rendered Playwright) in the npm chain; trust rails untouched; no isolable Mixpanel funnel metric (analytics-app trust/clarity default, 089/092 disclosure pattern). Needs human visual spot-check (browse nav render path, 003 gate not pixel-level) | HIGH | specs/091.md | 1 | — |
| 092 | Analytics-UX audit (human 2026-07-14): honest handling of 0%-yield pools in the default (non-searched) browse — screenshot shows CBBTC/WSTETH/WEETH at "0%" topping a TVL-sorted list (collateral assets with no supply yield); tidy SOTA read = DEMOTE 0%/near-0% pools below yielding ones in the default sort AND/OR tag them ("collateral — no supply yield") instead of a stark "0%", NEVER hide or fabricate a number (trust rail). Reuses the 089/`userSortedApy` demote pattern; prerequisite-pair for 089 | 6.5 | SHIPPED (2026-07-14, one-commit PR on the harness-pinned branch, verifier PASS 5/5 + HIGH tier independently re-derived — verifier ran its own Playwright demote/tag test + trust-rail no-overlap check + EN/KO placement check + token-substitution verification + compiled/minified byte-identity, all green) — DEMOTE 0%/near-0%-yield pools below yielding in the TVL sort (all 3 browse blocks) + honest "No supply yield" tag replacing the stark $0.00/day preview, 0.00% APY hero preserved (trust rail intact); reuses the userSortedApy anomaly-demote idiom; one CSS token substitution (--color-text-tertiary absent → --color-text-secondary, disclosed specs/092-notes.md); no isolable Mixpanel funnel metric (analytics-app trust/clarity fix); UNBLOCKS 089 (default sort → TVL); render-path HIGH change — needs human visual spot-check (browse cards; sandbox blocks browser pixel-QA) | HIGH | specs/092.md | 1 | — |
| 093 | Analytics-UX audit (human 2026-07-14): SOTA nav-rail IA — the top row (`app.js:2575`) mixes primary category tabs (All/Lending/Staking/LP-DEX) with secondary filter buttons (Chains/TVL/Protocols/APY) in one undifferentiated strip; restructure into a clear primary category rail + a secondary filter cluster, add consistent neuro tab/filter iconography, sticky-on-scroll, unambiguous active states (press physics per design system), EN+KO. Presentation layer of the nav; consumes 091's expanded categories | 6.5 | SHIPPED (2026-07-14, verifier PASS HIGH 10/10 independently re-derived incl. own Playwright run + stash-baseline sticky-offset check + compiled/min freshness grep) — restructured `.google-nav-tabs` into `.google-nav-primary` (category rail) → `.google-nav-divider` → `.google-nav-secondary` (4 filters), each control now carries a monochrome currentColor inline-SVG icon (new NAV_ICONS map + navIcon() helper); filter default labels routed through t() (navFilterChains/Tvl/Protocols/Apy EN+KO — closes a latent hardcoded-English hard-rule violation); class names google-nav-tab/google-filter-btn + IDs chains-btn/tvl-btn/protocols-btn/apy-btn preserved verbatim (press physics + dropdown positioning untouched). New rendered-Playwright gate test_nav_rail_ia.js 10/10; no isolable funnel metric (analytics-app IA/clarity fix); needs human visual spot-check (nav render path) | HIGH | specs/093.md | 1 | — |
| 094 | Analytics-UX audit (human 2026-07-14): pool-row iconography — rows show text-only badges ("ON MAPLE • ETHEREUM"); add protocol + chain logos to each row (and grid card) for SOTA scannability. Must degrade honestly: graceful fallback glyph on missing/broken icon, no layout shift (fixed box + `object-fit`), no new blocking third-party critical-path dep (lazy, `loading=lazy`, works offline — sandbox/analytics fetch precedent). Decide icon source in-spec (bundled set vs DefiLlama icon CDN with fallback) | 6.0 | SHIPPED (2026-07-15, one-commit PR on the harness-pinned branch, verifier PASS HIGH 9/9 independently re-derived incl. its own `node test_pool_logo.js` run 5/5, fresh-build byte-identity check of app.compiled/min + style.min + translations.min, trust-rail grep-empty, and re-run of 7/10 touched render tests) — decorative protocol logo (+ chain corner badge) on every pool row/card via a new `PoolLogo` component in the shared `renderPoolCard` path (grid+list both); DefiLlama CDN `icons.llamao.fi` lazy `<img>` → monogram fallback in a fixed 24px box (no reflow); text badge preserved; trust rails byte-identical; alt text EN+KO. Deviation: 10 other render tests got a 2-line ignore-token+abort-route for the new external host (verifier judged minimal, not scope creep). Render-path HIGH → needs human visual spot-check; no isolable Mixpanel funnel metric (analytics-app clarity/credibility default, 089/091/092/093/095 disclosure class) — WINDOW CLOSED 2026-07-29 · INCONCLUSIVE at n≈0 — measured on grid/pool engagement: prod `pool_view` 30d = 6, `pool_click` = 1 (pre-123, `source=undefined`). The surface received six views in thirty days; nothing can be attributed to logos. Kept, not reverted (`signals/2026-07-29.md`) | HIGH | specs/094.md | 1 | closed 07-29 |
| 095 | Analytics-UX audit (human 2026-07-14): list-view SOTA table polish — column alignment, `font-variant-numeric: tabular-nums` on APY/TVL, right-aligned numeric columns, calmer styling for 0/near-0 values (pairs with 092), consistent row height + hover/press physics, flawless at 360/768/1280 + dark. Pure CSS/markup polish on the `pools-list` path (`style.css` + `app.js:2833`); no data/trust-rail surface | 6.0 | SHIPPED (2026-07-14, one-commit PR, verifier PASS LOW 6/6 independently re-derived incl. own `node test_list_polish.js` 6/6 + `test_list_default.js` 3/3 regression + min-file grep confirming the rules reach the app) — four surgical `.pools-list`-scope CSS edits: uniform `min-height:84px` rows, APY + TVL columns right-aligned (`align-items:flex-end`+`text-align:right`), and a `:has(.pool-apy-tag)` calm-0 hero (secondary color + opacity .75, number preserved — trust rail intact, pairs with 092); tabular-nums already global (inherited, not duplicated). New rendered-Playwright gate test_list_polish.js (6 computed-DOM assertions, fixture-routed). Deviation: test-only async-CSS `media="all"` settle fix (Chrome `:has()` invalidation gap in headless when the sheet activates after cards render — not a prod bug) + fontshare→empty-CSS routing; 360px pill clip confirmed pre-existing (090 layout, stash-baseline proven), out of scope. Screenshots 1280/768/360/dark reviewed clean. No isolable Mixpanel funnel metric (analytics-app clarity/scannability polish, 089/091/092/093 disclosure pattern); render-path LOW → advisory human visual spot-check (003 pixel gate unshipped) | LOW | specs/095.md | 1 | — |
| 096 | Signal integrity (BUG at the measurement layer): environment-gate Mixpanel tracking — `Analytics.track()` fires unconditionally from ANY host, so localhost/Vercel-preview traffic pollutes prod metrics. Live proof (signals 2026-07-14 second pull): last-2d share_link_opened 13/13 localhost, plan_created 11/11 localhost; the "80/day plateau" was mostly `localhost:8792` (114 of 327 sessions 07-09→11); real www.defi.garden traffic is a flat ~5–16/day trickle. Hostname allowlist gate in track() (defi.garden + yield.garden prod hosts); heartbeats filter page_url retroactively for historical claims. Protects the north-star funnel + every open pre/post window (061/065/068, 005/007/008) | 9.0 | SHIPPED (2026-07-14, verifier PASS LOW 6/6 independently re-derived incl. its own mutation bite-check — forced isProductionHost→true, both tests failed, restored clean) — exact-match allowlist gate at the single choke point Analytics.track(); localhost/127.0.0.1/*.vercel.app/file: suppressed, defensive try/catch fails closed; two new tests (unit 16/16 + Playwright real-home.html 2/2 incl. mutation check) wired into npm chain; no new events/deps, trust rails + SEO untouched. NOTE: a concurrent heartbeat's git stash raced the interim commit (analytics.js briefly landed gate-less), corrected by amend before push — see 096-notes.md. Measurement = structural zero: 0 non-prod page_url events to 2026-07-28 | LOW | specs/096.md | 1 | gate live → 0 non-prod events to 2026-07-28 |
| 097 | yield.garden mirror decision — the ENTIRE 07-09 launch spike (152 sessions) landed on `www.yield.garden`, a live Vercel mirror of the same app (curl-verified; apex 307s to www); it converted to ≤1 plan_created and died next day (2/0/0/1/0 daily since). Two prod domains split link equity + duplicate the surface; DNS/Vercel + "where was the launch posted" are human-owned | 6.5 | BLOCKED (human: keep as mirror w/ redirect? 301 to www.defi.garden? what channel was the 07-09 launch?) | HIGH | — | 0 | — |
| | **— DATA-LAYER MOVEMENT: remaining tickets (legs 059/087/088 SHIPPED 2026-07-14 — static snapshot + phase-0 file history + compute-kpis + KPI #1 all LIVE; these are the genuinely-unbuilt pieces, decomposed from 058 §§2/3/6/8 per human 2026-07-15) —** | | | | | | |
| 102 | Data-layer §8 · one-source-of-truth (~~BUILDABLE NOW~~ — NOT buildable as written): point `generate-token-pages.js` / `generate-chain-pages.js` / `generate-sitemap.js` at 059's `pools-snapshot.json` instead of 3 live DefiLlama fetches. BLOCKED 2026-07-15 (build loop, spec+evidence in specs/102.md): the snapshot is $10M-railed (719 pools / 330 tokens, FE-payload win) but the generators floor at $100K (token pages, human directive 2026-07-11) / $1000 (sitemap URL enumeration) — repointing collapses 2,103 token + 86 chain pages → ~330 tokens = de-indexing ~1,770+ SEO pages = NORTH_STAR NEVER-list. 058 §8's aspiration predates 059's $10M implementation (opposite axes). RESOLVED 2026-07-15: human chose **A** (new SEO-tier shared source). Superseded by **112** (READY). | 7.0 | CULLED (→112) | HIGH | specs/102.md | 1 | — |
| 103 | Data-layer KPI surfacing (088 open · BUILDABLE NOW — 087 already computes `kpis.apyMomentum`): rate momentum on pool detail/cards — rising/falling over the retained window, honest "exciting" (falling rates said out loud, degen ⅓-haircut untouched); extends 088.1's render pattern, EN+KO, ranks WITHIN rail-passing pools only, rendered-Playwright | 7.0 | SHIPPED (2026-07-15, one-commit on the harness-pinned branch, verifier PASS HIGH 6/6 independently re-derived incl. its own `test_kpi_momentum` 8/8 render-DOM run + compiled/minified byte-identity checks + live `formatApy(2.3)==="2.3%"` deviation proof) — new `.rate-momentum-note` on pool detail surfaces 087's `kpis.apyMomentum` (last−first total APY over the retained window) as calm rising/falling copy: RISING (≥+0.5pp) / FALLING (≤−0.5pp) said out loud, gated to a ≥7-day window and a ≥0.5pp move, yields entirely to the 071 volatility note, hides on missing kpis (live SEO `?pool=` landings). Mirrors 088.1 exactly (styling/guard/test scaffold); reads only `pool.kpis.*`+apy fields, no rail constant touched, no list mutation, app.js untouched. EN+KO added together; artifacts regenerated (`app.compiled.*` byte-identical). New rendered-Playwright gate `test_kpi_momentum.js` (8/8, D1–D8) in the npm chain. Scoped to pool detail only — cards deferred to the open 088 parent (104/105). No isolable Mixpanel funnel metric (trust-signal surfacing, 088.1 disclosure lineage); renders for no pool until 087's CI accrues ≥7 history points. Needs human visual spot-check (pool-detail render path; 003 pixel gate unshipped). | HIGH | specs/103.md | 1 | activates when ≥7-pt history exists |
| 104 | Data-layer KPI surfacing (088 open · BUILDABLE NOW — 087 already computes `kpis.tvlTrend`): TVL trend (growing/shrinking) alongside the static $10M floor — a shrinking-TVL pool still passing the floor is decision-relevant for a 5-year plan; calm cautious-saver framing, EN+KO | 6.5 | SHIPPED (2026-07-15, one-commit PR on the harness-pinned branch, verifier PASS HIGH 13/13 independently re-derived incl. its OWN test_kpi_tvl_trend 8/8 + momentum-sibling 8/8 regression + test_compiled_assets 4/4 + the 7 byte-identical minify assertions + a git-stash baseline proving the 2 test_minified_assets reds are pre-existing home.html/plan.html landing-drift, 104-independent) — new `.tvl-trend-note` on pool detail surfaces 087's `kpis.tvlTrend`: SHRINKING (`tvl<=-0.25`) names the ICP risk (a pool can keep clearing the $10M floor while losing deposits), GROWING refuses the hype ("not a guarantee"); gated `!(071) && kpis obj && typeof tvlTrend==='number' && historyPoints>=7 && |tvlTrend|>=0.25`, yields entirely to the 071 volatility note (mutual exclusion), hides on live `?pool=` SEO landings (no kpis) — the 088.1/103 sibling pattern exactly. Percent via en-US `_formatNum` (never bare toLocaleString). PoolDetail.js + 2 translations keys (EN+KO) + regenerated compiled/min + new rendered-Playwright gate test_kpi_tvl_trend.js (8/8) wired into npm test. Trust rails byte-untouched (reads derived kpis only); no NEVER-list surface. No isolable Mixpanel funnel metric (trust/decision-quality surfacing on the SEO→pool-detail path, 088.1/103/105 disclosure pattern); render-path HIGH → advisory human visual spot-check (003 pixel gate unshipped). 088 STAYS OPEN (plan-level projection confidence is the remaining candidate KPI). | HIGH | specs/104.md | 1 | — |
| 105 | Data-layer KPI surfacing (088 open · main-flagged gap in 088.1 notes): enrich live `?pool=` SEO deep-link landings with `kpis` — today KPIs render only on in-app browse→detail (the snapshot carries them); direct `?pool=` landings fetch live and show no KPI, so SEO arrivals miss the trust signal. Read the snapshot's kpis for the landed pool on the live path | 6.5 | SHIPPED (2026-07-15, one-commit PR on the harness-pinned branch, verifier PASS HIGH 5/5 independently re-derived incl. its own npm run compile+minify byte-identity check, own test_kpi_seo_enrichment 5/5 + regressions 7/7·8/8, and a git-diff grep proving the effect body touches no rail constants / never mutates the `pools` array) — a direct `?pool=` SEO deep link now enriches its detail pool with the railed snapshot’s `kpis`, so the 088.1 rate-track-record + 103 rate-momentum trust notes render for SEO arrivals (they hid before because `?pool=` always loads live `/pools`, which carries no kpis). Purely additive `useEffect` in app.js: fetches `/data/pools-snapshot.json` once (ref-guarded), matches by `pool.pool`, merges ONLY a real `kpis` object onto the single `detailPool` via a functional setState; any failure/absent-pool leaves it untouched (notes hide as today). Zero PoolDetail/translations changes; app.compiled(.min).js regenerated byte-identically. New rendered-Playwright gate test_kpi_seo_enrichment.js (D1–D5) in the npm chain. Trust rails/router/`?token=` untouched, no NEVER-list surface. No isolable Mixpanel funnel metric (trust-signal surfacing on the SEO `?pool=` landing surface, 088.1/103 disclosure lineage); renders for a landed pool only once 087’s daily CI has accrued that pool’s history. Needs human visual spot-check (pool-detail render path on a live `?pool=` landing; 003 pixel gate unshipped). 088 stays open (104 TVL-trend, 106 plan-confidence). | MEDIUM | specs/105.md | 0 | — |
| 106 | Data-layer KPI surfacing (088 open · north-star surface): plan-level "projection confidence" in the planner — annotate picks with the stability of the pools behind the blended rate ("built on pools steady for 30+ days"), serving the forever-number confidence problem; EN+KO, rendered-Playwright. Reads 087's `kpis.apyStdev`/`historyPoints` | 7.0 | SHIPPED (2026-07-15, one-commit PR on the harness-pinned branch; spec 106.md written this run from documented evidence since none existed; Opus coding agent built, verifier PASS HIGH 6/6 independently re-derived incl. its OWN `node test_plan_confidence.js` 6/6 + `test_minified_assets.js` 9/9 + `test_css_minified_render.js` 2/2 + `test_planner.js` 208-assert no-regression + git-diff proof trust rails untouched) — new module-level `planConfidence(curated)` (planner.js) summarizes the track record of the ≤3 rail-passing pools behind the blended rate via 087's `kpis.historyPoints`/`apyStdev`; 3 calm tiers (STEADY all-steady+minHp≥7 / PARTIAL some-steady / BUILDING none-yet), null-safe HIDE on the live/no-kpis path (same discipline as 088.1's live-landing hide); `minHp` = min historyPoints (honest floor for any "N days" claim); reads only kpis + poolTotalApy, never filters/reorders/re-flags a pool; rendered after `heroElement` in BOTH bloom return trees (subscription + target/growth); 3 EN+natural-KO keys in the nested `planner` block; `.gp-plan-confidence` neuro-token note modeled on `.gp-tangibility`; planner.min.js/translations.min.js/planner-styles.min.css regenerated; new rendered-Playwright gate test_plan_confidence.js (6/6: STEADY/PARTIAL/BUILDING/live-absent/KO/no-errors) in the npm chain. Data reality: 087 CI at 1–2 history points → only BUILDING renders in prod TODAY; STEADY/PARTIAL activate as history accrues (088.1 precedent, fixtures exercise all 3). Trust rails byte-identical; no NEVER-list surface. Closes 088 candidate KPI #5 (088 stays open for 103 momentum / 104 TVL-trend / 105 SEO `?pool=` kpis). North-star-direct (trust signal on the plan_created/card-funnel surface); no isolable Mixpanel funnel metric (measured within planner conversion). Needs human visual spot-check (bloom render path; 003 pixel gate unshipped) | HIGH | specs/106.md | 1 | — |
| 107 | Data-layer · HUMAN KEYSTONE: DefiLlama ToS/attribution read (058 §3, still open) — NOTE the shipped 059 snapshot already re-serves a filtered subset from our CDN, so this confirmation is now both retroactive (is the live snapshot OK?) and forward (gates the DB leg 108-110). Direction signed off 2026-07-14; this is the legal-terms confirmation | 7.5 | DONE (human 2026-07-15: DefiLlama ToS read — attribution in place, data may be cached/re-served; the live 059 snapshot is cleared and the DB leg is unblocked) | LOW | — | 0 | — |
| 108 | Data-layer DB leg · HUMAN KEYSTONE (stack DECIDED 2026-07-15: **Cloudflare Worker Cron + D1**, not Turso — you already run Cloudflare, it gives a reliable intraday scheduler GH Actions lacks + writes history WITHOUT a repo commit, killing the 081/083 Vercel deploy-churn problem; the FE never reads the DB so no request-path backend / no-backend tenet intact on the read path). Human provisioning per specs/108.md: `wrangler d1 create defi-garden-history` + apply schema.sql + `wrangler deploy` + `wrangler secret put READ_TOKEN`, then add GH Actions secrets HISTORY_ENDPOINT + HISTORY_TOKEN. Unblocks 109-110. OPTIONAL upgrade over shipped phase-0 file history — buys intraday granularity + >30d windows | 6.0 | DONE (human provisioned 2026-07-23): D1 `defi-garden-history` (id 2a6a3b87-…) created + schema applied, Worker deployed with hourly cron, READ_TOKEN + GH secrets HISTORY_ENDPOINT/HISTORY_TOKEN set. Verified LIVE end-to-end: 121 backfill loaded (7,296 rows, 07-14→07-23) + the hourly poller's FIRST live batch fired 2026-07-23 15:00:57 UTC (~734 rows) → 8,030 total. CI already reaches /history (200, token OK); next daily run switches KPI source to D1 (10+ day-buckets, no gap). The whole DB leg 108→109→110 is now operational | LOW | specs/108.md | 0 | — |
| 109 | Data-layer DB leg · BUILD the CF Worker poller (`src/poller.js` + `wrangler.toml`, specs/108.md): hourly Cron Trigger fetches `yields.llama.fi/pools`, applies the SAME trust rails at write time ($10M TVL floor + 1000% sanity — anomalous never stored), `INSERT OR REPLACE` slim rows `(pool_id, ts, apy, tvl_usd)` into D1; plus an authed `GET /history?days=N` for CI to read. Attribution kept. The real "our own database" — but a poller/CI store, NOT a request-path backend | 6.0 | SHIPPED code (2026-07-16 at human request; verifier PASS HIGH 5/5 independently re-derived incl. its own `node test_poller.js` run + rail byte-equivalence check vs the snapshot + fail-closed-auth check + mutation analysis): CF Worker poller — schema.sql + wrangler.toml + src/poller-core.js + src/poller.js + test_poller.js 21/21 (in npm test) + specs/109-notes.md + 109-pr.md (HIGH explainer+quiz). Build CAUGHT + corrected a spec-108 bug (sketch DROPPED anomalous >1000% pools; now KEPT+flagged, $10M floor is the only drop — mirrors the snapshot for 110 byte-equivalence). No credentials handled. DEPLOY pending human 108 (`wrangler login/d1 create/deploy/secret`); KPIs keep running on the file-history until then | HIGH | specs/108.md | 1 | — |
| 110 | Data-layer DB leg · BUILD KPI read-from-DB: point `compute-kpis.js`'s history-IO at `${HISTORY_ENDPOINT}?days=90` (Bearer HISTORY_TOKEN) instead of `data/history/*.json`, reshaped into the same series `buildSeriesByPool()` already consumes — KPI math + snapshot bake unchanged; retention moves to a Worker `DELETE WHERE ts < …`. Daily `sitemap-update.yml` commit cadence untouched. Gets intraday granularity + >30d windows | 5.5 | SHIPPED (2026-07-21, verifier PASS HIGH 5/5 independently re-derived incl. a `reshapeDbRows` apy+0.01 mutation kill (6/7 tests red) + a pre/post byte-diff proving unset-`HISTORY_ENDPOINT` is a no-op: `pools-snapshot.json`+`history/*.json` byte-identical to HEAD) — `compute-kpis.js` gains pure `reshapeDbRows` (flat D1 `/history` rows→`[{date,pools}]`, UTC-day buckets, latest-per-pool-per-day, `[round(apy,4),tvl]` = slimPoint-equivalent) + async `fetchDbHistory` (Bearer, 15s AbortController, throws→fallback) + `enrich(...,historyOverride)` 1-line source switch; async `main()` prefers D1 when `HISTORY_ENDPOINT` set else honest file fallback. `.github/workflows/sitemap-update.yml` KPI step gains `HISTORY_ENDPOINT`/`HISTORY_TOKEN` secrets env. **Conservative: daily buckets (not intraday) to guarantee byte-equivalence + zero live KPI-value change; intraday widening is a safe follow-up** (specs/110-notes.md §1). No trust-rail/attribution/router/param-URL surface; no client fetch (fetch runs in CI Node). Deploy-inert until human keystone 108 provisions D1+Worker+secrets — then D1 read (90d window) turns on with no code change. New rendered-independent gate `test_kpi_db_source.js` 7/7 (incl. byte-equivalence DB-vs-file + fetchDbHistory happy/401) wired into npm test; `test_compute_kpis.js` 20/20 no regression. No isolable Mixpanel funnel metric (data-pipeline infra; KPIs feed the 088/117 trust surfaces). Needs human 108 provisioning to activate. | HIGH | specs/108.md | 1 | — |
| 058 | STUDY (not a build): feasibility of a DefiLlama data wrapper/cache layer — we poll llama ~5min, store (Turso/libSQL or Cloudflare D1 = the real 'sqlite'; or a static snapshot via cron), serve a pre-filtered payload from our own domain for speed + DefiLlama independence + custom KPIs on stored history. REVERSES the foundational no-backend tenet → build is human-owned; this item only produces docs/feasibility-data-layer.md + a recommendation | 7.5 | DONE (docs/feasibility-data-layer.md, PR #163) — recommends static-snapshot option (a) first; DB/KPI leg (b/c) deferred to a separate human-approved decision; follow-up build item 059 drafted inline, BLOCKED on human ToS confirmation + sign-off | LOW | specs/058.md | 1 | — |
| 098 | BUG (human ticket 2026-07-14, follow-up to 091): typing a bare protocol name that is ALSO a token symbol (e.g. "pendle") searched the TOKEN, not the PROTOCOL — `parseNaturalLanguageQuery` set `token=PENDLE`, restricting to pools whose symbol contains PENDLE, but a protocol's pools span many underlying tokens, so the result dead-ended ("No yields found for PENDLE") while the 6 live Pendle-protocol pools never showed. Human-reported via a constructed `?token=PENDLE&poolTypes=Yield+Derivatives&protocols=Pendle` URL. Fix: when the extracted token equals a detected protocol's own name, drop the token and let the protocol (+ category) filter drive results | 6.5 | SHIPPED (2026-07-14) — 4-line guard at the end of `parseNaturalLanguageQuery` (app.js): `if (token && protocols.some(p => p.toLowerCase() === token.toLowerCase())) token = ''`. Regenerated app.compiled.js/app.compiled.min.js. Extended test_category_taxonomy.js: 3 new parser-unit asserts ("pendle"/"aave" → token cleared + protocol kept; "usdc on aave" → token USDC kept, non-regression) + a new rendered-Playwright assert that TYPING "pendle" shows both pendle-PROTOCOL pools incl. `PT-USDE` (no PENDLE symbol segment → provable protocol-not-token filtering). NL-parser regression green (test_protocol_parsing 9/9, test_qualifier_fix 9/9). Trust rails untouched. No isolable funnel metric (search-correctness fix) | HIGH | specs/098.md | 1 | — |
| 099 | BUG (human ticket 2026-07-14, HIGH priority — funnel-top trust): the planner hero card shows a DIFFERENT forever-number than the headline/forever line for the SAME single subscription pick. Screenshot: only ChatGPT Plus ($20/mo) selected, blended APY 10.58% → headline + bottom line correctly say "≈$2,300 covers $20/mo" ($2,300 × 10.58% ≈ $20/mo ✓), but the right-hand hero card shows "$4,800" ONE-TIME / "generates $42/mo" (that's the forever-number for ~$42/mo, ~2× the picked $20/mo). Inconsistent money numbers on the conversion page (the `waitlist_submitted` funnel top) directly damage the trust-rail moat. Likely mechanism: the hero card uses `coveredBundle(...)`/a default-bundle capital (planner.js ~L215/L272) while the headline/forever line uses the single-goal `foreverNumber(monthlyTarget, apy)` (planner.js:137) — same 085/coveredBundle-default class. Fix: hero card, headline, and forever line must all derive from the user's ACTUAL selected coverage (here $20/mo → ~$2,300), one source of truth; EN+KO; regenerate planner.min.js (plan.html loads the min — 061/082 precedent); rendered-Playwright acceptance driving the real subscription flow (single ChatGPT Plus pick → all three numbers agree). Trust rail: every number stays live-derived through the sanity/TVL/anomaly filters + formatUsd/formatApy. | 9.3 | SHIPPED (2026-07-14, one-commit PR on the harness-pinned branch, verifier PASS HIGH 2/2 independently re-derived incl. its OWN rendered-test run + stash-baseline mutation proof 0/2→2/2 + confirmation the two waitlist test failures are pre-existing/environmental on the pre-fix baseline) — root cause: the subscription pick seeds `capital` at the conservative guidanceApy (~5.5%, planner.js:3863) but the bloom recomputes coverage at the LIVE blended apy; the `slideCapital`⇄`neededCapital` sync effect was gated behind `!mixTouched`, so the untouched hero card kept the stale seed (~$4,800/$42-mo) while the headline+forever line showed the live figure (~$2,300/$20-mo). Fix: drop the gate so `slideCapital` tracks the live-apy `neededCapital` from the initial seed on (one source of truth across hero/headline/forever line + persona/APY-change refresh); no user input lost (amount step skipped for subs; slider drag still wins). Single planner.js hunk + regenerated planner.min.js; new rendered-Playwright gate test_hero_number_consistency.js (2/2, wired into npm test). No trust-rail/NEVER-list surface touched. No isolable Mixpanel funnel metric (trust/consistency fix protecting the `waitlist_submitted` funnel top). Render-path HIGH → needs human visual spot-check. | HIGH | specs/099.md | 1 | — |
| 100 | BUG (documented 084-notes:49-66, "next promotion candidate"; promoted by build loop 2026-07-15 — no READY items, all north-star-direct work shipped/traffic-gated/human-gated): free-text search leaks a chain's short alias into the TOKEN slot — `yields on ethereum`→token ETH, `best yields on solana`→SOL, `on arbitrum`→ARB (all reproduced live this run). Root cause: the SECOND token-parse fallback in `parseNaturalLanguageQuery` (app.js ~L277-297) uses a bare `tokenCandidateText.includes(tokenLower)` substring test whose `chainNames` guard lists only LONG chain names, so 3+char short aliases (`eth`⊂ethereum, `sol`⊂solana, `arb`⊂arbitrum) slip through — the user's chain-only query gets spuriously narrowed to a token, a quiet trust erosion on the analytics search (the SEO arrival surface feeding the card funnel). Fix: expand the fallback's exclusion set to ALL `chainAliases` keys (short+long); real-parser test (084 `extractParser` pattern) + regression that whole-word tokens before `on` still resolve; recompile/minify (prod loads app.compiled.min.js). Trust rails/router/`?token=` params untouched. | 6.5 | SHIPPED (2026-07-15, one-commit PR on the harness-pinned branch, verifier PASS HIGH 6/6 independently re-derived incl. its OWN real-parser reproduction script + mutation-kill 2/6→6/6 + occurrence-count proof the alias reached app.compiled.min.js 1→2, not a false grep) — expanded the SECOND token-fallback's `chainNames` guard in `parseNaturalLanguageQuery` from 8 long chain names to all 24 `chainAliases` keys, closing the `eth`⊂ethereum / `sol`⊂solana / `arb`⊂arbitrum substring leak; single app.js hunk + regenerated app.compiled.js/min; new real-parser gate test_parser_chain_token.js (6/6) wired into npm test. Trust rails/router/`?token=` untouched, no NEVER-list surface. No isolable Mixpanel funnel metric (analytics-search correctness/trust fix, 084/098 disclosure pattern); render-path HIGH but pure query-parse logic (no visual surface) → advisory human spot-check only. | HIGH | specs/100.md | 1 | — |
| 101 | North-star (share virality / card funnel, heartbeat-derived 2026-07-15 evening): the SUBSCRIPTION share link reproduces only the anchor goal, NOT the user's full multi-service mix — `encodePlanToUrl` (planner.js:903) serializes goal/capital/monthly/years/dl/pace/chain/token but never `selectedSubs`, and no `mix` URL param is decoded on arrival (grep-confirmed 0 hits), so a Spotify+Netflix+Claude garden opens as just the single anchor on the recipient's side. Directly undercuts the 2026-07-11 standing decision ("the share link rebuilds the sender's EXACT garden — the primary artifact"); the app already models+stores the mix (planner.js:1767/1795 `mix: selectedSubs`), the only gap is the wire format. Fix: encode `mix` on subscription plans (byte-identical for single/anchor-only picks — no share-URL churn), decode+validate+apply on arrival with `mixTouched=true` (honest degradation on unknown ids), regenerate planner.min.js, rendered-Playwright round-trip test. On-theme; strengthens the `share_link_opened→plan_created→waitlist_submitted` loop. | 7.6 | SHIPPED (2026-07-15, one-commit PR on the harness-pinned branch, verifier PASS HIGH 7/7 independently re-derived incl. own test_share_mix_roundtrip 5/5 + test_hero_number_consistency 2/2 + test_subscription_mix_seed 4/4 + test_minified_assets 9/9 + test_smoke 8/8 + git-stash baseline of the sandbox-blocked failures) — subscription share links now carry the full multi-service mix. planner.js: encodePlanToUrl gains a `mix=` param written ONLY for subscription plans with a real non-anchor mix (single-pick/anchor-only/non-subscription URLs byte-identical, no churn); new isSubscriptionId validator (SUBSCRIPTION_LADDER + subscription-archetype GOALS); decodePlanFromUrl parses `mix` defensively (try/catch, drops unknown ids, mix=null→anchor fallback, never throws); Bloom seeds selectedSubs/mixTouched from a decoded mix via the INITIAL useState value (race-free vs the !mixTouched anchor-seed effect) threaded through the 060 initialChain/initialToken prop seam; 3 encode call sites threaded. 099 slideCapital⇄neededCapital consistency preserved (mixTouched=true recomputes coverage from the full mix). planner.min.js regenerated; no translations (zero new strings). New rendered-Playwright gate test_share_mix_roundtrip.js (5/5: encode+guard, decode round-trip $50/mo mix, honest degradation) in the npm chain. Trust rails byte-identical. North-star-direct (strengthens share_link_opened→plan_created→waitlist_submitted); no isolable metric until 069 spotlight posting lands share traffic (14d window from first real share). Needs human visual spot-check (subscription bloom render; 003 pixel gate unshipped). NOTE: this run first raced item 094 from a stale base (built+verifier-PASS) before the pre-push audit found 094 already merged as #223 — discarded that duplicate, reset to latest main, and built 101 (freshly promoted READY by the 07-15 evening heartbeat). | HIGH | specs/101.md | 1 | 14d from first share |
| 111 | Analytics nav + filter-dropdown UI polish (human feedback 2026-07-15 on 093's shipped nav: "the data and icons are very confusing, needs an upgrade" + "these selectors need better hover overlay controls and UI for [the open-dropdown] state"; human chose option 1 of the operator's proposal — strip category-tab icons, keep filter icons). TWO parts. (A) NAV RAIL — remove the per-tab icons from the PRIMARY category tabs (drop the navIcon() call from the `.google-nav-primary` CATEGORY_TABS map; the 6 competing glyphs read as noise next to text labels like "Lending"), KEEP the icons on the 4 filter pills in `.google-nav-secondary` (chains/tvl/protocols/apy) — retain NAV_ICONS/navIcon (still used by the filters); update test_nav_rail_ia.js assertion #4 so category tabs have NO `<svg>` while each filter pill still does. (B) OPEN FILTER-DROPDOWN STATE (`.global-filter-dropdown`, style.css ~1233-1364): (1) REMOVE the CLAUDE.md-BANNED scale-pop hover `transform: translateY(-2px) scale(1.02)` on `.filter-pill/.filter-chip:hover` (style.css ~1302) → neuro lift/press only (translateY + shadow, no scale); (2) make the SELECTED chip unmistakable — filled primary like `.google-filter-btn.has-selection` (today it's a barely-visible 1px lift, style.css ~1353); (3) add a subtle scrim/overlay behind the open panel + close-on-outside-click (audit the existing outside-close behavior first). EN+KO for any new strings (none expected). Regenerate app.compiled(.min).js + style.min.css; rendered-Playwright acceptance driving the REAL open-dropdown UI (open a filter → assert selected-chip filled state + no scale transform + scrim present + outside-click closes), flawless at 360/768/1280 + dark. RELATED (NOT this item's scope — candidate follow-up): the raw-key flash the human saw ("navFilterChains") was a transient deploy cache-skew — the new app.compiled.min.js references navFilterChains while a stale translations.min.js was still served (fixed filenames + `max-age=300, stale-while-revalidate=86400`, vercel.json ~154); it self-heals on revalidate (prod verified consistent 2026-07-15), but content-hashed asset filenames or a humanized-key fallback in t() would prevent recurrence. | 7.0 | SHIPPED (2026-07-15, one-commit PR on the harness-pinned branch, verifier PASS HIGH attempt-2 — attempt-1 FAILed on the missing 111-pr.md gate artifact + an unscoped shared-CSS-rule change; both fixed: 111-pr.md written, and the active-state transform/shadow re-scoped to `.filter-chip.active` ONLY so chain/protocol pills stay byte-identical) — (A) category tabs are now text-only (`navIcon(icon)` dropped from the `.google-nav-primary` map; NAV_ICONS/navIcon retained for the 4 filter buttons); (B) removed the CLAUDE.md-banned `scale(1.02)` hover → neuro `translateY(-1px)` only, selected TVL/APY chip now reads filled-primary (matches `.google-filter-btn.has-selection`), and a `.global-filter-scrim` (top:128px z-index 99998, light/dark tint, reduced-motion-safe) dims the results + closes on click behind a tightened outside-click handler (old `.filter-dropdown-container` selector matched nothing). New rendered-Playwright gate test_filter_dropdown_polish.js (7/7: scrim present, filled chip, no-scale hover, scrim-close, 360/768/1280 + dark) wired into npm test; test_nav_rail_ia #4 rewritten (tabs no svg / filters one svg), 10/10. Trust rails/router/`?token=` untouched; presentation-only, no NEVER-list surface. 2 pre-existing test_search protocol-false-match assertions fail in-sandbox (compiled parser byte-identical to HEAD — not this diff; 070-notes precedent). No isolable Mixpanel metric (clarity/trust polish on the analytics funnel surface; lands under planner-conversion / analytics-URL-integrity guardrails). Needs human visual spot-check (analytics nav + open-dropdown render; 003 pixel gate unshipped). | HIGH | specs/111.md | 1 | — |
| 112 | Data-layer §8 · one-source-of-truth for the 3 SEO generators (option A, HUMAN-SIGNED-OFF 2026-07-15 — supersedes CULLED 102): one CI `/pools` fetch feeds a $1000-floored CI-TRANSIENT (not committed, not served → no git-bloat, no new DefiLlama re-serve/ToS exposure, 107 untouched) consumed by `generate-token-pages.js` / `generate-chain-pages.js` (already read `--fixture`/`POOLS_FIXTURE`) + `generate-sitemap.js` (add fixture-read to its CLI `main()`, the one small code change). Collapses today's 4 live llama fetches/CI-run → 1; trust-rail filter applied once not thrice. GUARDRAIL = byte-identical output: `tokens/`+`chains/`+every `sitemap-*.xml` must match a live-fetch run over the same payload (0 dropped URLs of the ~4,881), else STOP + re-BLOCK. Live-fallback preserved (missing/empty transient → each generator live-fetches, today's exact coverage). spec has full acceptance | 7.5 | SHIPPED (2026-07-15, one-commit PR on claude/loop-112, verifier PASS HIGH 10/10 independently re-derived incl. its OWN dust-pool injection into pools-sample.json proving the $1000 floor drops exactly the sub-$1000 pools while token/chain/sitemap output stays byte-identical) — `generate-pools-snapshot.js --seo-out` writes a $1000-floored RAW-pool transient from the SAME single fetch (before the unchanged-snapshot early-return, so always fresh); token/chain/sitemap generators consume it via `--fixture`/`POOLS_FIXTURE` and fail SAFE to live via a shared `loadFixturePools` (missing/empty/malformed → null → live fetch, never a truncated run). 4 enumerated CI fetches → 1. New `test_seo_shared_source.js` (20/20, network-free) proves B1 byte-identical (incl. OG PNGs) + C1 fallback across all 3 modules. Trust rails byte-untouched ($1000 is a superset gate, not a rail change); no SEO deletion (the byte-identical proof is what keeps A off the NEVER-list that blocked 102); transient scratch-only (`$RUNNER_TEMP`) → does NOT widen 107's ToS scope. VERIFIER CAVEAT (not a 112 defect, follow-up candidate): `generate-llms.js` keeps its OWN separate `/pools` fetch (a later workflow step, not one of "the 3 SEO generators"), so the job still makes 2 fetches/run, not a literal 1 — folding llms onto the transient is a clean next item. Live proof lands on the next `sitemap-update.yml` CI run (job log: ONE snapshot fetch + `🌱 SEO transient: N pools`, generators print `📄 Loaded pools from fixture`, no committed `seo-pools.json`). No isolable Mixpanel funnel metric (CI robustness/infra). | HIGH | specs/112.md | 1 | — |
| 113 | Fold `generate-llms.js` onto 112's shared SEO transient — its own `/pools` fetch (`fetchYieldsSafe`) is a 2nd CI fetch/run (112 verifier caveat, "a clean next item"). Upside = ONE fewer daily DefiLlama fetch. BLOCKER (build loop 2026-07-15, spec+evidence specs/113.md): 112's byte-identical guarantee is STRUCTURAL for token/chain/sitemap (they floor ≥$1000 so the $1000 transient is a provable superset) but does NOT transfer to llms — `analyzeYieldData` (generate-llms.js:251-253) aggregates chain/protocol/token TVL over ALL pools `tvl>0` with NO floor, so reading the $1000 transient silently drops sub-$1000 pools from the llms "Top … by TVL" aggregates. Byte-identical ONLY empirically (live 2026-07-15: just 13 pools / $4,310 in the 0<tvl<1000 gap → no display/rank shift today) never structurally (a well-placed future dust pool changes the SEO/LLM-discovery surface, un-guardable). Un-guaranteed content change to generated SEO surface = human-judgment, not autonomous auto-merge. RESOLVED (human 2026-07-15: EXCLUDE — accept sub-$1000 aggregate drift for one fewer daily fetch) → now buildable: point generate-llms.js at the shared $1000 transient (drop its own fetchYieldsSafe), test asserts identity given identical inputs + documents the accepted excluded-dust divergence. | 4.5 | SHIPPED (2026-07-15, one-commit PR on the harness-pinned branch, verifier PASS HIGH 5/5 independently re-derived incl. own run of both test suites + byte-check of the helper semantics vs generate-sitemap.js + shared-literal-path confirmation) — generate-llms.js now prefers the shared $1000-floored transient via loadFixturePools/parseFixtureArg (verbatim from generate-sitemap.js, fail-SAFE to live fetchYieldsSafe on missing/empty/malformed fixture), main() reads POOLS_FIXTURE, workflow "Generate LLM files" step passes POOLS_FIXTURE="$RUNNER_TEMP/seo-pools.json"; daily CI drops 2 live DefiLlama /pools fetches → 1. New network-free test_llms_shared_source.js (12/12): identity-given-identical-inputs (2 end-to-end runs byte-identical modulo normalizeLlmsContent) + documented excluded-dust divergence (analyzeYieldData full vs $1000-filtered differ by EXACTLY the dust total — KNOWN, human-signed-off, not a regression) + C1 fail-safe + git-baseline guardrail. Trust rails/attribution byte-untouched, no SEO deletion (llms still generated every run). No isolable Mixpanel funnel metric (CI-efficiency + human-signed SEO-aggregate change, 074/078/081 disclosure pattern); byte-identical on the live 2026-07-15 payload, structurally may drift if future sub-$1000 pools land in a top-N bucket (accepted). Live proof = next sitemap-update.yml run makes one fewer /pools fetch. | HIGH | specs/113.md | 1 | — |
| 114 | Hook-model retention · "Welcome back" saved-garden re-entry card on the search-first landing (`landing.js`). EVIDENCE (browser-driven discovery 2026-07-15, `reports/2026-07-15-hook-model-discovery.md`): return-visit REPORT MODE is built and good (`/plan.html` with a saved `garden-plan` renders "on track / ahead of plan" + garden stage + forever-number + ladder nudge — the hook model's variable-reward leg) BUT it's siloed: the real default entry is now the search-first landing at bare `/` (item #237), and a rendered run with a seeded `garden-plan` shows NO trace of the saved garden there (`landingReturn.showsSavedGarden=false`). Reconnect investment→reward at the front door: when `localStorage['garden-plan']` is valid, render a neuro re-entry card (goal label + calm status + one-tap "Tend your garden →" link to `plan.html`, which auto-loads report mode); fail SAFE to the existing generic "Plant a garden" card for first-timers/malformed plans. EN+KO, neuro tokens only, 360/768/1280 + dark, reduced-motion. Instrument `garden_reentry_shown`/`_clicked`. Acceptance = rendered Playwright (`test_landing_return.js`, house harness): seed garden-plan via addInitScript, assert card+goal-label+plan.html link present; absent with no/malformed plan; EN+KO; no raw keys. Additive render only — does NOT touch the `__APP_MODE` router/canonical/parameterized-URL behavior. No trust-rail/NEVER-list surface → auto-mergeable after verifier PASS. | 7.2 | SHIPPED (2026-07-15, one-commit PR on the harness-pinned branch, verifier PASS HIGH 5/5 independently re-derived incl. its OWN run of test_landing_return.js 5/5 + test_minified_assets translations.min byte-identity + git-stash baseline proving the test_landing.js/test_smoke.js chain halts are PRE-EXISTING sandbox limits, not this diff) — "Welcome back" saved-garden re-entry card on the search-first landing: when localStorage[garden-plan] holds a valid plan with a recognizable goal, the existing .landing-garden-card aside swaps from the generic "Plant a garden" prompt to a return card (leaf mark + Welcome-back caption + goal label + honest "Planted <Month YYYY>" line + one-tap "Tend your garden" link to plan.html report mode); first-timers + any malformed/unknown-goal plan fail SAFE to the generic card. Additive render only — zero new CSS (reuses .landing-garden-card/.landing-card-topline/.landing-seed-icon/.landing-garden-link + LeafMark/ArrowIcon/PlantIllustration), no __APP_MODE router / canonical / parameterized-URL / trust-rail surface. GOAL_LABEL_KEYS static id->label map duplicated read-only (planner.js not loaded on the landing route; irregular id->key mapping) — fails safe for unknown ids. garden_reentry_shown/_clicked via the guarded typeof-Analytics pattern. EN+KO strings + regenerated translations.min.js. New rendered-Playwright gate test_landing_return.js (5/5) in the npm chain. No isolable Mixpanel metric yet (retention/hook re-entry; measured 14d pre/post on returning-visitor->plan.html report loads once share/bookmark return traffic materializes). Needs human visual spot-check (landing render path; 003 pixel gate unshipped). — WINDOW CLOSED 2026-07-29 · INCONCLUSIVE at n≈0 — measured on return-visits reaching a plan: prod `plan_created` 30d = **3** (all pre-window), `share_link_opened` = 1, and zero planner sessions since 07-19. No return traffic has yet existed for the card to catch. Kept (`signals/2026-07-29.md`) | HIGH | specs/114.md | 1 | closed 07-29 |
| 115 | Hook-model retention · Honest "tend your garden" monthly calendar (`.ics`) reminder at bloom + report — builds the MISSING TRIGGER leg. EVIDENCE (browser + code audit 2026-07-15, `reports/2026-07-15-hook-model-discovery.md`): of the hook model's 4 legs (`CLAUDE.md`), investment/action/variable-reward are built but the TRIGGER is entirely absent — rendered bloom `trigger=false`, rendered report `trigger_reminderAffordance=false`, codebase grep for Notification/serviceWorker/remind/calendar/every-month → nothing. The variable reward ("you're ahead of plan") is only ever seen if the user spontaneously returns; a loop with no trigger can't retain. Add a "Remind me to tend this monthly" control at bloom (near the share row) and in report mode: on tap, generate+download a client-side `.ics` VEVENT (`RRULE:FREQ=MONTHLY`, DTSTART ~30d out, URL/DESCRIPTION = the plan's `encodePlanToUrl` share link so it deep-links back into the exact garden). No dependency (Blob text), no permission prompt (calendar file, not Notifications API), no dark pattern; honest "we'll never email you" copy. EN+KO, neuro tokens, 360/768/1280 + dark. Instrument `tend_reminder_added {surface}`. Acceptance = rendered Playwright (`test_tend_reminder.js`, house harness + seeded garden-plan → report mode): assert control present, captured `.ics` contains BEGIN:VEVENT + RRULE:FREQ=MONTHLY + DTSTART ~30d + the encodePlanToUrl link; no network call; EN+KO. No trust-rail/credentials/SEO/bots surface → auto-mergeable after verifier PASS. | 7.5 | SHIPPED (measuring) — WINDOW CLOSED 2026-07-29 · INCONCLUSIVE at n≈0 — measured on its own event: prod `tend_reminder_added` 30d = **0**, against 3 `plan_created`. The trigger leg is built and instrumented and has had three opportunities to fire in thirty days. Kept (`signals/2026-07-29.md`) | HIGH | specs/115.md | 1 | closed 07-29 |
| 116 | Hook-model retention · Re-share the garden from REPORT MODE — "Share my garden" copy-link CTA in `GardenReport`. EVIDENCE (`reports/2026-07-15-hook-model-discovery.md`, candidate follow-up #1 "direct viral-loop closure"): the share hub lives only at bloom; report mode (the returning "Ahead of plan"/"On track" high-intent moment) has NO share affordance (grep/render-confirmed: `gp-report-actions`=Tend+Fresh, `gp-report-reminder`=115 .ics only) → a viral dead end. Surface the working share link (`encodePlanToUrl`, the exact call `reportTendReminder` already makes) as a one-tap copy CTA, reusing the shipped `doCopyLink` clipboard+fallback pattern + `gp-share-textlink`/`gp-tend-reminder-note` classes (ZERO new CSS). Instrument `share_link_created {method:'copy', surface:'report', goal, persona}` — new share-origination point feeding `share_link_opened → plan_created`. EN+KO, neuro tokens, 360/768/1280+dark. Additive render only — no `__APP_MODE` router / canonical / parameterized-URL / trust-rail surface, no new deps. Acceptance = rendered Playwright (`test_report_share.js`, house harness + seeded garden-plan → report mode): control present, click copies an `encodePlanToUrl` URL (no network call), copied confirmation shows, EN+KO no raw keys, dark+responsive no page errors. Promoted 2026-07-16 by build loop (no buildable READY item; 110 gated behind human-blocked 108/109). | 7.4 | SHIPPED (2026-07-16, one-commit PR on the harness-pinned branch, verifier PASS HIGH 6/6 + instrumentation gate — independently re-derived incl. its OWN `node test_report_share.js` 8/8 run, `git diff analytics.js` scope check, and revert-reasoning that only assertion 2b gates `surface`) — "Share my garden" copy-link CTA added to report mode (`GardenReport`): a sibling `gp-report-reminder` block above the 115 tend-reminder, reusing `gp-share-textlink`/`gp-tend-reminder-note` (ZERO new CSS). On tap builds `encodePlanToUrl(...)` (arg list byte-identical to `reportTendReminder`), copies via the `doCopyLink` clipboard+textarea-fallback pattern, shows transient `shareLinkCopied`, fires `share_link_created {method:'copy', surface:'report', goal, persona}`. ATTEMPT 1 verifier FAIL caught a REAL bug: `analytics.js` `trackShareLinkCreated` hardcoded `{method,goal,persona}` and silently DROPPED `surface` → measurement plan unmeasurable; fixed by adding `surface: context.surface || null` (mirrors `trackTendReminderAdded`), backward-compatible (bloom shares emit `surface:null`). Test gap closed: `test_report_share.js` now patches the bare global `Analytics.track` (item-044 lesson: `Analytics` is a top-level `const`, not a window prop) and asserts the emitted payload carries `surface:'report'` (8/8, was 7/7 passing while instrumentation was silently broken). EN+KO strings `reportShareCta`/`reportShareNote`; `planner.min.js`/`translations.min.js` regenerated (analytics.js ships unminified). North-star-direct (new share-origination surface feeding `share_link_created → share_link_opened → plan_created`); traffic-gated — no report-surface shares until the human posts 069 distribution. Needs human visual spot-check (report render path; 003 pixel gate unshipped). | HIGH | specs/116.md | 2 | 2026-07-30 |
| 117 | Data-layer KPI (human 2026-07-15): risk-adjusted yield = **Sharpe ratio per pool** over the time-series — `apySharpe = (mean(apy_t) − RISK_FREE_APY) / stdev(apy_t)`, the quantified form of 088's "steady 6% beats spiky 20%". ≈free compute extension of `compute-kpis.js` (`apyStdev`+`stdevPop` already exist; add `apyMean`+`apySharpe`, null for <8 pts or sd=0, no √T since apy is already annualized). RATE-STABILITY Sharpe only — does NOT capture principal risk (IL/depeg/exploit/TVL-flight); ships WITH that caveat, named risk-free benchmark (default ~4% T-bill), never a "safety score", ranks WITHIN rail-passing pools only, EN+KO. "Across pools" = a risk-adjusted-yield ranking (pool-detail annotation first per 088, then an optional analytics sort, then planner blend preference). Buildable NOW roughly on the daily file-history (~8 days out); sharpens with D1 intraday (108-110). specs/117.md · SURFACES tracked as 117.1 (pool-detail), 117.2 (analytics sort), 117.3 (planner blend) | 7.0 | SHIPPED compute leg (2026-07-17, PR self-merged on claude/loop-117, verifier PASS HIGH 4/4 — independently re-derived the textbook [2,4,4,4,5,5,7,9]→mean 5/stdev 2/apySharpe 0.5, both null branches (<8 pts, sd=0), and apyStdev byte-identical old-vs-new). compute-kpis.js `computeKpis` now emits `apyMean`+`apySharpe` (rate-stability Sharpe = (mean−RISK_FREE_APY 4.0)/stdev, null for <8 pts or sd=0, no √T), reusing the SAME series/sd/mean as apyStdev; 2 exported constants; 5 new tests (test_compute_kpis 20/20). Trust rails byte-untouched (only ADDS a derived field). SURFACES DEFERRED (spec "one at a time, pool-detail first"): the display-copy/EN+KO honesty-caveat/ranking criteria belong to those follow-up sub-items — next promotion candidate = 117.1 pool-detail annotation. No isolable funnel metric (data-layer infra feeding 088's "steady beats spiky"); apySharpe stays null until a pool accrues ≥8 daily history points (~8 days out). | HIGH | specs/117.md | 1 | — |
| 118 | GSC "Excluded by 'noindex' tag" — new not-indexed reason emailed by Google 2026-07-16 (human's manual GSC window). DIAGNOSIS-first (item-010 pattern): code audit done — the ONLY runtime noindex is `app.js:2341-2361` client-injecting `robots=noindex` on `emptyStateResolved`/`deadPoolResolved` (the INTENTIONAL 012/072 honest-empty-state + dead-`?pool=` fix, guarded by `pools.length>0`); home.html/plan.html/`/tokens/`/`/chains/` all ship `index,follow`. MOST LIKELY working-as-designed (the fix draining the soft-404/thin class 010 diagnosed — "Excluded by noindex" is the intended outcome for empty/dead URLs). Can't confirm without the flagged URLs (no GSC connector, 026 parked). HUMAN: paste 3-5 example URLs + count from the report → classify: empty-`?token=`/dead-`?pool=` = close as intended; static/planner/results-bearing = REGRESSION (suspect the SPA render race under the 059 snapshot-first path). specs/118.md | 6.5 | BLOCKED (human: paste GSC example URLs) — audit done, awaiting URLs to classify | HIGH | specs/118.md | 0 | — |
| 117.1 | Surface leg 1 of 117 (pool-detail Sharpe annotation) — spec'd by heartbeat 2026-07-18 because 117's compute leg SHIPPED (#267), consuming the one buildable READY item and leaving the build loop unfed (110 human-gated behind 108). Extends 088.1's shipped rate-track-record block in `PoolDetail.js` (~L1316-1360, already surfaces `kpis.apyStdev`+`historyPoints` with neuro styling + null-safe guard): add ONE line rendering `pool.kpis.apySharpe` with the named risk-free benchmark ("vs ~4% risk-free") + the mandated principal-risk caveat ("how steady the yield has been, NOT a safety score"), reusing the block's neuro classes (ZERO new CSS), EN+KO, `_formatApy`/`formatNum` (never bare toLocaleString), hidden when apySharpe is null (graceful like the existing historyPoints guard). Renders dormant now, lights up ~07-22 as pools accrue ≥8 pts — same "land now, improves as data accrues" pattern the compute leg used. Pool-detail annotation ONLY (117.2 analytics sort / 117.3 planner blend are separate, per 117's "one at a time" mandate). Trust rails/router/`?token=` byte-untouched, no NEVER-list surface → auto-mergeable after verifier PASS. Rendered-Playwright acceptance (seed a pool with kpis.apySharpe, assert annotation+benchmark+caveat present; absent when null; EN+KO; dark+responsive). specs/117.1.md | 6.8 | SHIPPED (2026-07-18, one commit on the harness-pinned branch, verifier PASS HIGH 6/6 independently re-derived incl. its OWN `node test_kpi_sharpe_annotation.js` 4/4 rendered-Playwright run + two scratch-copy mutation kills — guard removed → S2 leaks `Rate-stability score 0`; caveat stripped → S1 fails on the 4% assertion — plus a git-stash baseline proving the 2 test_minified_assets home.html/plan.html reds are pre-existing, 117.1-independent). Surfaces 117s shipped `kpis.apySharpe` as ONE calm line inside the existing 088.1 rate-track-record-note block on pool detail: `_formatNum(apySharpe)` + named ~4% risk-free benchmark + mandated principal-risk caveat (never a safety score), EN + natural KO, guarded `typeof apySharpe===number` so it null-hides <8 history pts (graceful, historyPoints>=1 discipline). ZERO new CSS (inline marginTop, reuses parent neuro classes); app.compiled.* byte-UNCHANGED (app.js untouched) — only PoolDetail.compiled(.min).js + translations.min.js regenerated (test_compiled_assets 4/4, idempotent). Trust rails/__APP_MODE router/`?token=/?chain=/?pool=` byte-untouched; not on NEVER list. Renders dormant until pools accrue >=8 daily history pts (~2026-07-22 on file-history) then lights up automatically (compute-leg pattern). Scope = pool-detail annotation ONLY (117.2 analytics sort / 117.3 planner blend remain separate per 117 one-at-a-time). No isolable Mixpanel funnel metric (trust-signal surfacing, 088.1/103/104 lineage). Needs human visual spot-check (pool-detail render path; 003 pixel gate unshipped). NOTE: this run first mis-selected 110 (5.5), built+verifier-PASSED it, then reverted on catching that 117.1 (6.8) is the higher-scored + more north-star-aligned READY item post-reset — 110 remains READY | HIGH | specs/117.1.md | 1 | 2026-08-05 (Sharpe live in prod 2026-07-22: 479/745 snapshot pools non-null apySharpe, 9 history pts — 14d window opens at activation, not merge) |
| 117.2 | Surface leg 2 of 117 (analytics "risk-adjusted (Sharpe)" sort) — spec'd by heartbeat 2026-07-19 because 117.1 (#268) + 118 (#269) both SHIPPED, consuming the buildable queue and leaving the loop unfed (110 human-gated behind 108). The next-in-sequence surface named verbatim in `specs/117.1-notes.md` (117.2 = analytics sort; 117.3 = planner blend, later). Adds a `sortBy === 'sharpe'` branch to the three existing `filtered.sort()` paths in `app.js` (token ~L1822 / chain ~L1893 / default ~L1974) + one new option in the existing sort control (reuse markup + neuro classes, ZERO new CSS). Ranks by `pool.kpis.apySharpe` DESC **within the sane group ONLY** — anomalous demotion/flag/High-risk ordering byte-identical to tvl/apy sorts (trust rail: no anomalous pool jumps the queue via Sharpe = the load-bearing verifier check); null-Sharpe pools sort last, tie-break TVL, so it's a graceful no-op until pools accrue ≥8 history pts (~07-22) then activates ("land now, lights up as data accrues", 117/117.1 pattern). EN+KO label together, no bare toLocaleString. app.js IS touched → regen compiled/min assets (idempotent). Trust rails/`__APP_MODE` router/`?token=/?chain=/?pool=` byte-untouched; not on NEVER list. Rendered-Playwright acceptance (numeric-Sharpe ordering; anomalous stay demoted; null sorts last; KO label present) added to npm chain. No isolable Mixpanel funnel metric (trust-surface polish, 103/104/117.1 lineage; 0 prod traffic to measure). Needs human visual spot-check (analytics render path; 003 pixel gate unshipped). specs/117.2.md | 6.6 | SHIPPED (2026-07-19, one-commit PR on the harness-pinned branch, verifier PASS HIGH 8/8 independently re-derived incl. its OWN mutation kill — flipping `shB-shA`→`shA-shB` + recompile drops the test 6/6→2/6, restored byte-clean — plus a live render of BOTH router paths (`/`→landing, `/?token=USDC`→analytics grid, 3 sort buttons [APY,TVL,Risk-adjusted]) and a git-stash baseline proving the 2 test_minified_assets reds pre-exist on origin/main) — new "Risk-adjusted" analytics sort ranks pools by the 117 compute leg's `kpis.apySharpe` (rate-stability Sharpe) via an identical `sortBy==='sharpe'` branch inserted FIRST in all three `filtered.sort()` comparators (tvl/apy branches byte-untouched): anomalous pools (APY>APY_SANITY_LIMIT) stay demoted below ALL sane pools (unconditional — the load-bearing trust rail, strengthened vs apy's `!userSortedApy` gate so a high-Sharpe anomaly can't jump the queue), sane group by apySharpe DESC, null-Sharpe last, tie-break TVL DESC → graceful no-op until pools accrue ≥8 history pts (~07-22) then activates (117/117.1 land-now pattern). ONE new sort button reusing `view-toggle-btn sort-toggle-btn` (ZERO new CSS), label `t('sortByRiskAdjusted')` EN "Risk-adjusted" + KO "위험 조정". app.compiled(.min).js + translations.min.js regenerated (idempotent; PoolDetail.compiled.*/planner.min.js/*.min.css byte-unchanged). Trust rails/`APY_SANITY_LIMIT`/`DEFAULT_MIN_TVL`/degen haircut/`__APP_MODE` router/`?token=/?chain=/?pool=`/Defillama attribution byte-untouched (grep 0 hits); not on NEVER list. New rendered-Playwright gate test_kpi_sharpe_sort.js (6/6: Sharpe-desc order, anomalous demoted last, null last, KO label) wired into npm test after test_default_sort.js; test_default_sort 4/4 + test_zero_yield_demote 4/4 + test_compiled_assets 4/4 green (2 test_minified_assets reds pre-existing/diff-independent, documented specs/117.1-notes.md, halt the full && chain before this test — a pre-existing structural gap affecting 117.1's own test equally). Scope = analytics sort ONLY (117.3 planner blend remains separate per 117 one-at-a-time). No isolable Mixpanel funnel metric (trust-surface polish, 103/104/117.1 lineage; 0 prod traffic). Needs human visual spot-check (analytics render path; 003 pixel gate unshipped). SELECTION NOTE (honest record): this run FIRST mis-selected 110 (5.5) — the initial READY scan missed 117.2 (6.6), which today's same-day heartbeat (d63615c01) had just promoted to READY; built + verifier-PASSED 110 (HIGH, DB-history read w/ fallback), then REVERTED it cleanly (tree restored to origin/main, 110 back to READY) on catching that 117.2 is BOTH higher-scored AND more north-star-aligned (live analytics trust-surface vs 110's deploy-inert CI plumbing blocked behind human-provisioned 108) — the exact 2026-07-18 precedent (mis-select-110→revert-for-117.1). ONE item shipped (117.2); 110 remains READY (after 109). | HIGH | specs/117.2.md | 1 | 2026-08-05 (Sharpe live in prod 2026-07-22: 479/745 snapshot pools non-null apySharpe, 9 history pts — 14d window opens at activation, not merge) |
| 117.3 | Surface leg 3 of 117 — planner "prefer steadier yield" pool-pick blend (the LAST-in-sequence 117 surface, named in 117's own row + `specs/117.2-notes.md`). Spec'd by heartbeat 2026-07-20 because 117.2 (#270) SHIPPED, consuming the buildable queue and leaving the loop unfed (110 human-gated behind 108's CF-D1 provisioning). Adds a **Sharpe near-tie-break** as the last comparator key in `planner.js` `curatePools`'s three `eligible.sort()` branches (~L606-623): among rail-passing candidates whose `poolTotalApy` are within a small documented epsilon (near-equal), prefer higher `kpis.apySharpe`; a materially-higher-APY pool NEVER leapfrogs a lower one via Sharpe; null-Sharpe pools stay neutral (no crash/NaN). Rail-filter block / `APY_SANITY_LIMIT` / `DEFAULT_MIN_TVL` / anomaly demotion / degen ⅓ haircut / `blendedApy`/`median` byte-untouched — the load-bearing trust rails; NO new user-facing copy in the smallest version (translations untouched). Graceful no-op until pools accrue ≥8 history pts (~07-22) then activates — with all-null Sharpe (current prod) `curatePools` order + `blendedApy` must be byte-identical to origin/main (the load-bearing regression). On the SEO-lander→pool-detail→planner path the 07-19 organic session (first prod plan_created since 07-10, `?token=BTC` internal referrer, no utm) just validated end-to-end. Autonomously buildable NOW; no credentials, not on NEVER list → auto-mergeable after verifier PASS. Unit acceptance (`test_planner_sharpe_pick.js`: four ordering rules + all-null byte-identical no-op; `node test_planner.js && test_protocol_parsing.js && test_qualifier_fix.js` green). No isolable Mixpanel funnel metric (selection-quality change, 088/117 lineage; 0–1 prod plans/wk → unmeasurable as a rate). specs/117.3.md | 6.4 | SHIPPED (2026-07-20, one-commit PR on the harness-pinned branch, verifier PASS HIGH 7/7 independently re-derived incl. its OWN 2 mutation kills — `return byApy` defeat drops 8→7 on numeric-beats-null; `eps=1e6` defeat drops 8→7 on material-gap-no-leapfrog — plus an independent all-null no-op stash-diff proving byte-identical `curatePools` order + `blendedApy` vs origin/main, and stash-baseline reproduction of the 2 pre-existing test_minified_assets home/plan.html reds) — adds a Sharpe near-tie-break as the LAST comparator key in all three `curatePools` `eligible.sort` branches: among rail-passing candidates whose `poolTotalApy` are within `eps=max(0.10, 0.02*max(apyA,apyB))` (0.1pp floor OR 2% of larger rate), prefer higher `kpis.apySharpe`; outside eps `sharpeTieBreak` returns 0 so APY order wins (never leapfrog on rate); null/missing-kpis Sharpe neutral (no NaN); both-null keeps prior order. With all-null Sharpe (current prod) each comparator reduces byte-identically to the prior `poolTotalApy(b)-poolTotalApy(a)` + unchanged membership keys → graceful no-op until pools accrue ≥8 history pts (~07-22). Rail-filter block / `APY_SANITY_LIMIT` / `DEFAULT_MIN_TVL` / anomaly demotion / degen ⅓ haircut / `blendedApy`/`median` byte-untouched; `translations.js` untouched (no new copy); `home.html` router / `?token=/?chain=/?pool=` untouched. planner.min.js regenerated (idempotent). New rendered-independent unit gate test_planner_sharpe_pick.js (8/8) wired into npm test after test_planner.js. No isolable Mixpanel funnel metric (selection-quality change, 088/117 lineage; 0–1 prod plans/wk). Needs human visual spot-check advisory (planner path; 003 pixel gate unshipped — though this change has no visual surface). SELECTION NOTE: this run first picked 110 (5.5, the only READY item at pickup), built + verifier-PASSED it (HIGH 5/5), then on the pre-push fetch found a concurrent heartbeat had promoted 117.3 (6.4) READY — higher-scored AND more north-star-aligned than 110's deploy-inert CI plumbing (110 human-gated behind BLOCKED 108); reverted 110 cleanly (remains READY) and shipped 117.3, per the 2026-07-18/07-19 mis-select-110 precedent. | HIGH | specs/117.3.md | 1 | 2026-08-05 (Sharpe live in prod 2026-07-22: 479/745 snapshot pools non-null apySharpe, 9 history pts — 14d window opens at activation, not merge) |
| 119 | North-star pipeline verification (002/010/118 diagnostic-row precedent) — heartbeat 2026-07-21 audited the full `waitlist_submitted` path in source after 07-20's 16-session (bot-shaped) day kept the north star at 0, to rule out a broken north-star event (a bug at the north-star step outranks any experiment). FINDING — pipeline SOUND end-to-end: `planner.js:1796 submitWaitlist` → `:1846 Analytics.trackWaitlistSubmitted` → `analytics.js:382 this.track('waitlist_submitted', …)`, event name matching the metric definition EXACTLY; waitlist UI real + reachable (`translations.js:547-583` CTA/microcopy/3 pitch variants/modal, `.gp-waitlist-*` rendered `planner.js:2709+/3943+`, opened from bloom checkout, driven live by `test_waitlist_microcopy.js`). The zero is **NO USAGE, not breakage** — to fire, a user must land→complete planner→reach bloom→open waitlist modal→enter valid email→submit; at ~2–13 organic sessions/day + 0–1 plan_created/wk that depth is effectively never reached. BLOCKER = traffic + funnel depth (both human-owned), NOT code. No code change. Durable so no future heartbeat re-derives this. See signals/2026-07-21.md. | — | DONE (verified 2026-07-21, no code change — zeros = no usage, not breakage) | LOW | — | 0 | — |
| 121 | Backfill generator for the D1 pool-history store (human 2026-07-23, during 108 provisioning): D1 starts empty, and 110 switches to the /history endpoint the moment it returns ANY rows and then ignores the file history — so turning D1 on would collapse the KPI window from the ~10 committed file-days to 1 and NULL the just-live 117 Sharpe/stability surfaces for ~8 days. `generate-history-backfill.js` emits `backfill.sql` from `data/history/*.json` (ts = midnight-UTC of each date so `reshapeDbRows` buckets it back to the same day; same table/columns/INSERT-OR-REPLACE as the poller, idempotent on PK). Round-trip test proves `reshapeDbRows(rows)` === the file entries EXACTLY → identical KPI values from D1 or files, no gap. Human runs `node generate-history-backfill.js` + `wrangler d1 execute … --file=backfill.sql --remote`. test_history_backfill.js 16/16, wired into npm test. Dry-run: 7,296 rows / 10 days. | 6.0 | SHIPPED (2026-07-23) — offline generator + test, no product/CI/trust-rail change; human-run one-time import | LOW | — | 1 | — |
| 122 | **P0 BUG (north-star surface, LIVE):** rate-stability Sharpe (117) renders **-900,719,925,474,097.9** on near-constant pools (screenshot: SUSDS). `compute-kpis.js:109` guards `sd > 0`, but a flat stable rate gives `sd` = floating-point dust (~1e-16) which passes → `(mean-RF)/sd` explodes. Fix: epsilon floor — null the Sharpe when `sd` is below a meaningful move (e.g. `sd < 0.05` pp, rate hasn't wobbled) AND belt-and-suspenders clamp any non-finite/absurd score at the render layer (117.1 pool-detail). A -900T number on the flagship pool page destroys trust → kills every CTA click. Regenerate KPIs. | 9.5 | SHIPPED (2026-07-23, verifier PASS HIGH 6/6 independently re-derived incl. own mutation-check that the float-dust test kills the old sd>0 guard) — compute-kpis.js SHARPE_MIN_STDEV=0.05 floor + SHARPE_ABS_MAX=50 cap + finite guard; PoolDetail 117.1 render gate |value|<=50 (hides stale -9e14 snapshot pre-CI-regen; magnitude check since -9e14 is finite); PoolDetail.compiled(.min) regenerated; test_compute_kpis +4 (23 assert). Trust rails untouched; snapshot self-heals next CI | HIGH | specs/122-pr.md | 1 | — |
| 123 | **P0 North-star instrumentation — DEFINE + verify (mostly done):** both CTAs already fire — `pool_click{source=garden_cta}` (PoolDetail.js:497) + `pool_click{source=protocol_link}` (:517). Confirm they fire on real renders (card_click AND url_direct pool-detail landings), ensure segmentation props attached (pool id/project/chain/apy/source), document the north-star Mixpanel query (weekly count + CTR = clicks/pool_view). Consider promoting to distinct event names if pool_click is too noisy to isolate. Measure-first: no north star without a verified metric. | 8.5 | SHIPPED (2026-07-23, verifier PASS HIGH confidence, risk tier LOW independently re-derived, 4/4 acceptance criteria, no defects) — render-verified both CTAs on both pool-detail entry paths (rendered-Playwright `test_northstar_cta_fires.js`, 7/7); found+fixed one real gap: `trackPoolClick` never emitted `source` (only `click_type`) so NORTH_STAR.md's own query definition didn't match the wire — analytics.js now emits both (additive, no rename). Emitter audit: 3 `pool_click` call sites (PoolDetail.js:497 garden_cta, :517 protocol_link, app.js:2546 yield_calculator, not north-star) — `source` fully disambiguates all three, kept `pool_click` (no rename). Mixpanel query documented in NORTH_STAR.md. No PoolDetail.js/app.js touch (analytics.js only) — no compile/minify regen needed. See specs/123-notes.md, specs/123-pr.md. | LOW | specs/123-pr.md | 1 | — |
| 124 | **P1** Elevate "**Start Earning on <protocol> ↗**" from gray fine-print to a real secondary CTA — it's now a north-star metric but renders as tiny muted text under the primary button (screenshot). Make it a visible secondary neuro button, keep the honest "Opens protocol • Wallet required" sublabel, clear two-tier hierarchy vs "Garden this pool". EN+KO. Directly grows the protocol_link click metric. | 8.0 | SHIPPED (2026-07-23, verifier PASS LOW 7/7 independently re-derived incl. rendered `test_northstar_cta_fires` 7/7 clicking `.cta-button-protocol`) — reused the orphaned `.cta-button-protocol` neuro secondary-button class (no new CSS), one className swap in PoolDetail.js, dead `.pool-action-protocol-link` removed, compiled+min assets regenerated; no copy change (EN/KO untouched); measure `pool_click{source=protocol_link}` weekly count / CTR once traffic lands; render-path merge — needs human visual spot-check | LOW | specs/124.md | 1 | 2026-08-06 |
| 125 | **P1** Repeat / sticky the primary CTA — the pool-detail page is long (hero → 3 stat cards → Long Game → calculator → Pool Info) but "Garden this pool" lives ONLY in the top-right card; a user who scrolls to read Pool Information must scroll back up to convert. Add a CTA after Pool Info and/or a sticky bottom bar (mobile). Grows both CTA clicks. | 7.5 | SHIPPED (2026-07-24, verifier PASS HIGH 7/7 independently re-derived incl. own runs of test_repeat_cta.js 5/5 + test_northstar_cta_fires.js 7/7 no-regression) — added a REPEAT "Garden this pool" + "Start Earning on <protocol>" CTA block after the Pool Information section (new final child of PoolDetail.js root div), reusing `.pool-action-card`/`.cta-button-primary`/`.cta-button-protocol` verbatim (ZERO new CSS, `git diff --stat pool-detail-styles.css` empty) so a reader at the page bottom converts without scrolling back to the hero. Additive `ctaPlacement` analytics prop (`hero`/`repeat_footer`) — `source` UNCHANGED so the north-star query counts both placements identically, secondary breakdown reveals repeat-CTA lift. New EN+KO `repeatCtaHeading`. Anomalous pools inherit `showConcreteCta=!isAnomalous` → generic CTA, no projection leak (trust rail intact). compiled/min assets regenerated. Chose repeat inline CTA over the sticky mobile bar (the "and/or") — tidier, zero fixed-position/overlap risk; sticky bar deferred. Render-path merge — needs human visual spot-check. Measure ctaPlacement breakdown of pool_click once traffic lands. | HIGH | specs/125-pr.md | 1 | 2026-08-07 |
| 126 | **P1 BUG** Money-format on the north-star page: daily earnings render "**$0.1**" (should be "$0.10") — violates the en-US `formatUsd` hard rule; a malformed number is a trust leak right next to the CTA. Audit daily/monthly/quick-estimate + calculator for the same. EN+KO unaffected (numeric). | 7.0 | SHIPPED (2026-07-23, verifier PASS HIGH 6/6 independently re-derived incl. own node formatUsd checks + all-caller large-number audit + freshness gates) — formatUsd pins min===max===maxFrac decimals ($0.0986→$0.10, $3→$3.00, projection maxFrac=0→$1,193); EN quickEstimate subhead pinned to 2dp; app.compiled(.min)+translations.min regenerated. KO unaffected (whole-won). No large-# regression (TVL uses formatCurrency). Verifier tiered HIGH (app-wide formatter) not LOW. | HIGH | specs/126-pr.md | 1 | — |
| 127 | **P1** Rate-stability copy for the ICP (pairs with 122): the second line dumps a raw analyst score ("Rate-stability score N (vs a ~4% risk-free rate)") — keep the calm first sentence ("steady so far… easier to plan a garden around"), drop/rework the raw-Sharpe line for the cautious-saver ICP. 117.1 over-exposed the number; on the conversion page it reads as jargon, not trust. EN+KO. | 7.0 | SHIPPED (2026-07-24) | LOW | specs/127.md | 1 | — |
| 128 | **P2** Dedup the earnings numbers — Daily ($0.10) + Monthly ($3) appear in the top stat cards AND the calculator's result AND its "Quick estimate for $1,000: $0.1/day" subhead (3×, screenshot). Redundancy lengthens the page and dilutes the CTA. Consolidate to one earnings surface + the calculator. | 6.0 | SHIPPED (2026-07-24, verifier PASS LOW) — kept the top stat cards as the single earnings surface + the calculator; removed the redundant numeric "Quick estimate: $X/day" calculator-header subhead (3rd occurrence), replaced with non-numeric `calcSubPrompt` (EN+KO). Rendered-Playwright acceptance `test_earnings_dedup.js` 4/4; pool-detail CTA regressions green (northstar 7/7, repeat_cta 5/5). needs human visual spot-check (render path, pre-003) | LOW | specs/128.md | 1 | — |
| 129 | **P2** Low-absolute-$ framing dampens intent on low-APY pools — "$0.10/day", "$3/mo" lead the page but read as underwhelming (SUSDS 3.6%). Per the yield-funded thesis, consider leading with the 5y projection / "keep your money AND get the thing" frame, honest numbers intact (no fabrication, trust rails hold). Product-framing — test pre/post on CTA CTR. | 6.0 | SHIPPED (2026-07-25, PR + verifier PASS HIGH 5/5 independently re-derived — reran test_projection_lead.js 7/7, regenerated compiled/min byte-identical, grep-proved the projection node moved unedited [degen/anomaly/disclaimer/CTA/event untouched], EN+KO keep-note both present, test_minified_assets red proven pre-existing via git stash -u baseline) — moved the existing "The Long Game" 5y projection card to LEAD the pool-detail body (above the underwhelming "$0.10/day / $3/mo" daily/monthly grid), reusing the node verbatim (all trust rails travel with it) + new `pool-projection-card` hook class (no new CSS) + new EN+KO `projectionKeepNote` "you keep your money" thesis line. Not APY-conditional (universal headline, deviation logged). No new event (2026-07-23 PRE-TRAFFIC MANDATE — existing pool_click CTA events measure this surface); pre/post read = CTA CTR once traffic lands. Render-path merge — needs human visual spot-check (003 pixel gate unshipped). | HIGH | specs/129-pr.md | 1 | 2026-08-08 |
| 130 | **P3** Category mislabel: SUSDS on `sky-lending` tagged "**YIELD FARMING**" (screenshot) — it's stablecoin lending, not farming (relates to 091's honest taxonomy). Accurate labels build trust on the conversion page; a cautious saver reading "yield farming" on a stablecoin lender is a mismatch. AUDIT EVIDENCE 2026-07-24 (rendered repro): ALSO mislabeled on venus-core-pool/BSC ($268M TVL lending market) — root cause is that `PoolDetail.js:73-91` carries its OWN narrower copy of the classification list (aave/compound/morpho/spark/maple/euler/radiant) diverging from `app.js:7-12` `LENDING_PROTOCOLS` (which has venus/moonwell/benqi-lending/etc.); fixing only one list won't fix the badge — de-duplicate into one shared classification or fix BOTH lists (+ add `sky-lending`). Score 5.0→5.5 (two confirmed surfaces + a structural dedupe) | 5.5 | SHIPPED (2026-07-24, verifier PASS HIGH 8/8, auto-merged) — de-duplicated into one shared `getPoolTypeShared` (owned by PoolDetail.js, app.js delegates); `sky-lending` added to LENDING_PROTOCOLS; venus/moonwell fixed on pool-detail for free (already in app.js's list, only the narrow PoolDetail copy lacked them). +playbook: dual-source-logic-divergence | HIGH | specs/130.md | 1 | — |
| 136 | **P0 BUG (north-star surface, mobile):** primary "Garden this pool" CTA + the TOTAL APY value are CLIPPED INVISIBLE on pool-detail at 360px — light AND dark, reproduced 2/2 pools (SUSDS `d8c4eff5…`, uniswap-v3 WETH-USDC `b99bcdf5…`; audit screenshots in scratchpad). Only "TOTAL AP" (cut mid-word) + the secondary protocol button survive; the entire mobile-width device class cannot see or tap the #1 conversion CTA. ROOT CAUSE (computed-geometry confirmed): `.pool-action-card` (pool-detail-styles.css ~766) has `min-width:260px` + 20px padding = ≥300px with NO mobile override, while the `@media (max-width:768px)` block (~963) narrows `.pool-hero-content` to ~240px (and gives `.apy-display-hero` `min-width:auto !important` but never `.pool-action-card`); the ~60px overflow is clipped by `.pool-hero-card`'s `overflow:hidden`. Structural (min-width-driven, likely predates 125's longer CTA copy — 125 render-verified PASS otherwise). FIX: give `.pool-action-card` the same mobile `min-width:auto` override (+ width:100%/box-sizing check), rendered-Playwright acceptance asserting the primary CTA's bounding box is fully inside the visible hero card at 360px (an element-visibility assert, NOT page scrollWidth — that check passes today and misses this class). NOT page-scroll: body scrollWidth 352 ≤ 360. | 9.2 | SHIPPED (2026-07-24, one-commit PR on the harness-pinned branch, verifier PASS HIGH — attempt 1 FAIL solely on the new test not yet wired into `npm test`, fixed; independently re-derived: min sheet byte-identical to a fresh minify of the raw, mutation check bites [strip override → 360px assert fails `child right 388 > card right 332`, 1280px still passes], no trust-rail/router/scope touches) — mobile `.pool-action-card { min-width:auto !important; width:100%; box-sizing:border-box }` added to the existing `@media(max-width:768px)` block, `pool-detail-styles.min.css` regenerated (prod loads the min, home.html:149); new rendered-Playwright `test_mobile_cta_clip.js` (4/4, element-containment asserts at 360×780 + 1280×900 regression guard) wired into the npm chain. The #1 north-star CTA (`garden_cta`) is now tappable on mobile. No isolable Mixpanel metric (0-traffic surface); proxy = the clip is physically gone. Needs human visual spot-check (mobile pool-detail render, light+dark; 003 pixel gate unshipped). | HIGH (verifier-derived; render path on the north-star surface) | specs/136.md | 1 | — |
| 137 | **P0 BUG (money honesty, KO locale):** pool-detail KO renders raw USD figures relabeled as Korean Won with NO conversion — `이 풀 가든하기 → 5년 후 약 1,189원` where the true figure is **$1,189 USD** (≈1,300× off in Won terms); same stat card shows `일일 수익 / 1,000원 기준 / $0.10` (Won label over a $-denominated value, internally inconsistent). Rendered repro any pool `?pool=…&lang=ko`. ROOT CAUSE: `formatKoreanCurrency(num)` (translations.js:1348) applies 만/억 grouping + `원` suffix directly to USD numbers from PoolDetail.js (investmentAmount/projectionAmount — the same numbers the EN strings prefix with `$`); no FX applied. Affects dailyEarningsSubLabel/monthlyEarningsSubLabel/basedOnInvestment/projectionBody/gardenThisPoolCtaConcrete (translations.js ~756-803) — every money figure adjacent to both north-star CTAs in KO. RECOMMENDED FIX (operator, human may veto): display USD honestly in KO (`$1,189` / `1,189달러` phrasing) — do NOT invent an FX conversion (live FX = new dependency + a number we can't rail); audit all formatKoreanCurrency call sites fed USD, planner included. EN+KO hard rule; regenerate translations.min.js (plan.html/home.html load min). | 9.0 | SHIPPED (2026-07-24) — KO 5 money strings now render honest `$` en-US USD (no FX); dead `formatKoreanCurrency` + export removed; translations.min.js regenerated. New rendered acceptance `test_ko_pool_money_honesty.js` 7/7 (proven RED on pre-fix baseline), north-star CTAs still fire 7/7. Honesty fix on the north-star surface — no isolated funnel metric; needs human visual spot-check (KO render path, 003 not shipped) | LOW | specs/137.md | 1 | — |
| 138 | **P1 hardening (north-star CTA resilience):** `PROTOCOL_URLS` static fallback (app.js:42-92) has NO `sky-lending`/`sky` entry, and the snapshot carries no `pool.url` — so whenever the dynamic `api.llama.fi/protocols` fetch fails/races (ad-blocker, outage, slow network), `getProtocolUrl()` (app.js:2435) returns null and BOTH placements of the "Start Earning on <protocol> ↗" secondary CTA silently render NOTHING (rendered repro: SUSDS sky-lending — the #3 pool by TVL, $4.69B, the ICP's flagship stablecoin lender — 0 `.cta-button-protocol` on the page). In prod the dynamic fetch usually masks this; the gap is graceful degradation of a north-star CTA. FIX: add sky-lending/sky (+ sweep the snapshot's top-50-TVL protocols for other missing static entries) to PROTOCOL_URLS; consider baking `url` into the 059 snapshot fields as the durable fix (ties to 134 study). | 6.8 | SHIPPED | LOW | specs/138.md | 1 | — |
| 139 | **P0 (BET A, human-approved 2026-07-23 — docs/strategy-2026-07-23-pretraffic-bets.md §3):** archetype-aware planner endings. The Bloom checkout panel's primary CTA is the card waitlist for EVERY archetype, with copy that only fits SUBSCRIPTION ("your garden's yield pays your subscription automatically through a simple card") — goal-incoherent for TARGET (sneakers, iPhone) and GROWTH (retirement, home) plans. The v2 spec's §4.4 primary CTA ("Start growing on the top pool →") was never implemented. FIX: SUBSCRIPTION unchanged; TARGET/GROWTH primary CTA becomes "Start growing on `<top pool's project>` →" linking `/?pool=<id>` of `curated[0]` (reused from the Engine-room cards), waitlist demoted to a secondary early-access link with new honest (non-subscription) modal copy. Fires `pool_click{source=plan_checkout, archetype}` — routes planner-completers onto the north-star pool-detail surface. | 8.5 | SHIPPED (2026-07-23, verifier PASS HIGH 10/10) — TARGET/GROWTH plans end on "Start growing on <top pool> →" (real `/?pool=` deep link, `pool_click{source=plan_checkout}` instrumented) instead of the goal-incoherent card waitlist; SUBSCRIPTION unchanged. | HIGH | specs/139.md | 1 | — |
| 131 | **P3** Duplicate "Browse tokens · Browse chains" — renders both under the header AND in the footer (screenshot), on the north-star page. Dedupe (086-adjacent). Minor polish. | 4.5 | SHIPPED (2026-07-25, verifier PASS LOW 5/5 independently re-derived incl. non-vacuous-test proof + `git stash` baseline check that planner case (b) fails identically pre-131; the 086 dedup rule lived only in async-loaded style.css so the static `.seo-hub-links` footer flashed alongside the React `.app-footer` copy before the `media="print"` swap fired — promoted the one rule to render-blocking inline critical CSS in home.html `<head>`, mirrors style.css, crawler raw anchors preserved) — no isolated funnel metric (product-quality polish on the north-star page per the 2026-07-23 PRE-TRAFFIC MANDATE); render-path change but 003 smoke gate DONE so no visual-spot-check flag required | LOW | specs/131.md | 1 | — |
| 132 | **BUG (SEO landing flash):** on a `?token=X` landing the empty-state top message ("No yields found for X / Try adjusting your filters") renders DURING the initial fetch — the grid-vs-empty ternary (app.js:3146) gates only on `filteredPools.length > 0`, NOT on `loading`; only the honest 012 explanation below is `emptyStateResolved`-gated. Search-engine visitors see "No yields found" flash before data loads. Fix: show a loading state (skeleton/spinner) while `loading`/before pools resolve; render the empty state only once resolved. On the north-star-adjacent SEO surface. | 8.0 | SHIPPED (2026-07-23, verifier PASS LOW 6/6 — reproduced the flash on HEAD~1 to prove the test bites) — added a loading branch `loading && !emptyStateResolved && !deadPoolResolved` → `t('loadingYields')` (EN+KO); empty state renders only once resolved; app.compiled(.min)+translations.min regenerated; new rendered test_token_loading_state.js 3/3 (real chromium, delayed fetch) in npm chain. Does NOT touch the $10M floor (that's 133). | MEDIUM | specs/132-pr.md | 1 | — |
| 133 | **Token discovery — don't dead-end obscure/small-token searches at the $10M floor (human 2026-07-23):** when a user lands on `?token=X` and the token has NO pools ≥$10M (all small/new/low-LP), the app dead-ends — the ≥$10M snapshot resolves instantly to empty and never fetches the real (sub-floor) pools (escape-hatch live-refetch only fires when minTvl DROPS, app.js:1147/1173). The token HAS pools, just below the floor. Fix (TRUST-RAIL-SENSITIVE, human-directed): when a token query resolves empty at the $10M default, fetch live and surface the below-floor pools with a clear "below our $10M safety floor — higher risk" label + one-tap opt-in — NEVER silently, NEVER as if they passed the floor. DEFAULT stays $10M (rail intact); this is honest discovery of what the user explicitly searched. Needs human sign-off on the exact behavior (auto-show below-floor vs one-tap reveal) + labeling. | 8.0 | BLOCKED (human: confirm discovery behavior + risk-label copy; touches DEFAULT_MIN_TVL NEVER-list) | HIGH | — | 0 | — |
| 134 | **STUDY — serve obscure-token discovery data fast from our own layer:** the default ≥$10M view already serves from the static snapshot (059, fast). But BOTH fast sources are $10M-floored — the snapshot (`isRailedIn`≥$10M) AND D1 (the 109 poller floors at write time) — so small pools aren't in either → discovering them forces a live DefiLlama fetch (the slow path, 133's data need). Decide: (a) add a LOWER-floored discovery dataset (112 already computes a $1000-floored CI transient; or lower the poller/snapshot floor for a discovery slice) served for token-specific queries; vs (b) keep the live fallback for below-floor discovery (simpler, slower). Payload/latency + trust-framing tradeoff; FE→D1-direct would reverse 108's no-backend-read-path. Produces docs/ recommendation; human approves any build. | 6.5 | SHIPPED (2026-07-25, verifier PASS LOW — docs-only study) — recommends **option (b) keep live DefiLlama fallback for below-floor discovery**: a served $1000-floored dataset ≈1.29MB gzip (~20× the current ~65KB hot path, wiping 059's 97% win), creates a sub-$10M second source-of-truth straining the $10M rail, and via-D1/via-112 reintroduces the no-backend/git-bloat/re-serve costs those layers avoid. Real fix (drafted follow-up 133 build): fire the EXISTING live refetch when a `?token=X` snapshot search resolves empty (today it only fires on a minTvl-slider drop, app.js:1159; empty tokens dead-end instantly at app.js:2343-2344). No floor lowered, no new dataset, no FE→D1 read. Deliverable: docs/discovery-data-layer-134.md | LOW | specs/134.md | 1 | — |
| 135 | **Minor:** the app writes `&minTvl=10000000` to the URL whenever minTvl>0 (`updateUrl`, app.js:959), so shared/bookmarked/analytics links pin the default floor explicitly — noisier URLs, and old links would pin $10M even if the default changes (133). Don't write minTvl when it equals DEFAULT_MIN_TVL (omit → the absent-param default applies). Cleaner URLs + canonical. LIVE EVIDENCE 2026-07-24 (heartbeat, `query_id 09fd9fb9`): crawlers are now re-walking indexed noisy URLs — 07-23 sessions landed on `?token=AVICI%E2%9C%A8&minTvl=10000000` and `?token=INFINIFIUSDC&minTvl=10000000`, i.e. the pinned default has leaked into externally-held URLs exactly as predicted; score bumped 4.5→5.5 (SEO-surface hygiene, no longer hypothetical) | 5.5 | SHIPPED (2026-07-24, verifier PASS 6/6, tier HIGH auto-merged — `updateUrl` omits `minTvl` when === DEFAULT_MIN_TVL; read path already defaults absent→$10M so behavior-preserving; new rendered `test_minttvl_clean_url.js` 3/3, bundles byte-identical, DEFAULT_MIN_TVL untouched) | HIGH | specs/135.md | 1 | — |
| 120 | Explicit UTM/referrer acquisition capture on analytics events (human 2026-07-22: "undefined referrer is a bug, fix per Mixpanel docs"). DIAGNOSIS: NOT a stripping bug — Referrer-Policy is standard (doesn't strip), the official Mixpanel lib auto-captures `$referrer`/`$referring_domain`/`utm_*` (confirmed from mixpanel-js source), and analytics.js already sends `referrer||'direct'`; the undefined values on 07-2x sessions are the CORRECT value for referrer-less bot/direct traffic (corroborates the crawler read). REAL FIX (forward-looking): app relied solely on lib super-props — added `captureAcquisition()` (utm_*/ref/gclid-fbclid-twclid/referring_domain, captured once at landing, session-scoped, guarded, absent→no key) spread into every event's `getBaseContext`, so the 064 spotlight `utm_*`/`ref` links produce robustly-queryable attribution on our own events (waitlist_opened etc.) the moment 069 traffic lands. Additive only, analytics.js ships unminified. test_analytics_acquisition.js 17/17. specs/120.md | 6.5 | SHIPPED (2026-07-22, verifier PASS LOW 6/6 independently re-derived incl. own test run + additive-only diff + honesty-framing confirmation) — honest note: does NOT recover current bot undefined (correct), hardens real/campaign attribution ahead of 069 | LOW | specs/120.md | 1 | — |
| 140 | **P1 (north-star surface perf/UX):** snapshot fast path dead in prod — `SNAPSHOT_MAX_AGE_MS = 15 min` (app.js:1086) vs real bake cadence ~1.5-3h (git log on data/pools-snapshot-meta.json: 8 bakes/24h; the comment's "intended ~5-min cadence" never materialized — the 109 poller writes D1, not the repo file). Snapshot-first (059) is eligible only ~10-15% of the day; the rest of the time EVERY visitor — including `?pool=`/`?token=` SEO landers, the north-star surface — pays the slow live DefiLlama fetch + "Loading live pools…" wait. Rendered repro 2026-07-25: committed 42-min-old snapshot silently fell through to the live path. Fix: raise max-age to ~2× observed bake cadence (6h), constant + comment + test; fallback/escape-hatch logic unchanged; data stays trust-railed ($10M floor baked in). | 7.0 | SHIPPED (2026-07-25, verifier PASS HIGH 5/5 independently re-derived incl. `git stash` baseline PROOF that both observed test failures — scenario (f) planner-hero flake + test_minified_assets plan.html/planner.js — fail identically at HEAD, unrelated to this diff) — constant 15min→6h in app.js + planner.js (documented deviation: identical dead gate on the default `/` surface), compiled/min artifacts regenerated (folded `216e5` verified), test_snapshot_first staleTs 1h→7h + new within-gate scenario (c2); measurement = `data_load_time` source=snapshot share over 14d; needs human visual spot-check (render-path merge, 003 smoke gate not yet shipped) | HIGH | specs/140.md | 1 | — |
| 141 | **P3 CI honesty:** sitemap-update.yml's Google sitemaps-ping step is dead (endpoint deprecated 2023, returns 404 in the 2026-07-25 06:34 log) yet the step echoes "✅ Search engines notified" — a misleading green. Remove the Google ping call (keep IndexNow — verified 200 same log); one workflow-file edit. | 3.5 | SHIPPED (2026-07-25, verifier PASS HIGH 5/5 independently re-derived incl. own YAML parse + tests green 208+9+9) — removed the ENTIRE dead "Ping Google about sitemap update" step (deviation from literal "Google ping only": the Bing ping in the same step is ALSO dead — 410 Gone, deprecated 2022, web-search-confirmed — and the unconditional `echo "✅ Search engines notified"` IS the misleading green; removing only Google would leave it intact). IndexNow step (`node indexnow-ping.js`, live Bing+Yandex path, verified 200) byte-unchanged; Google now takes sitemaps via robots.txt+GSC (both in place). Net 1 file, +6/−12. No product/runtime code, no trust rail, no SEO surface, no NEVER-list touch. No isolable funnel metric (CI observability/honesty, 078/074 lineage); live proof = next daily sitemap-update.yml log drops the "Notifying search engines"/"✅ notified"/404-410 noise, IndexNow still 200. +playbook: ci-signal-honesty | HIGH | specs/141.md | 1 | — |
| 142 | **Automate the §2b product audit** — `audit-app.js`: Playwright scanner that renders the playbook's surface rotation (pool-detail, ?token=, ?chain=, dead ?pool=, static pages, hubs @360px+dark+KO) against the committed snapshot with a fresh-meta fixture, runs checks 1-7 (number sanity incl. K/M/B-suffix-aware money regex, dead-end, loading flash, page errors, KO currency truth, CTA visibility incl. ancestor-clip, h-scroll), emits findings JSON the heartbeat tickets. Named as the candidate build in the 2026-07-23 pre-traffic mandate (NORTH_STAR line 127); today's tick hand-ran all of it — third consecutive manual pass, mechanizable end-to-end. | 6.5 | SHIPPED (2026-07-25, PR — verifier PASS LOW 7/7 independently re-derived) — new `audit-app.js` (read-only Playwright scanner, 428 lines, exports `runAudit()`) drives 10 real Chromium surfaces (grid-token/pool-detail(north-star)/grid-chain/dead-pool/grid-loading/pool-detail-360/grid-360/pool-detail-dark/pool-detail-ko/static-page) against the committed snapshot via a fresh-meta route, runs checks 1-7 with all three playbook fixture-traps guarded (K/M/B/T-suffix money regex, fresh-meta snapshot-first, poll-before-absence), emits `signals/audit-findings.json` `{generatedAt,surfacesCovered,findings[]}` and exits non-zero on any P0/P1. Positive control (`test_audit_app.js` 2/2, real render): injected −900T into `apyMean30d` → renders into the 30d-Mean-APY card → P0 number-sanity finding; clean snapshot → 0 P0/P1. ZERO product-code change (verifier-confirmed diff footprint); no new dep. `test_smoke.js`/`test_search.js` sandbox timeouts confirmed pre-existing at HEAD (files-aside repro). | LOW | specs/142.md | 1 | — |
| 143 | **"Garden this pool" arrival is mis-framed as a share (honesty + metric-integrity, north-star closure)** — the garden_cta deep-link (`plan.html?goal=&pace=&capital=&fm=capital&years=`, PoolDetail.js:253) is byte-shape-identical to a genuine share link (buildShareUrl), so the planner's `sharedPlan` path (planner.js:4062) fires for BOTH. Rendered-proven (scratchpad click-through, lido stETH): clicking "Garden this pool" lands on copy that FALSELY says "Someone sent you this garden" / "Someone shared their garden — make it yours" (nobody did — it's a self-CTA on our own pool page → trust-moat damage at the conversion moment), AND emits `share_link_opened` (planner.js:4067) polluting the viral-loop metric. Fix: `&src=pool` marker → honest pool-sourced arrival copy (EN+KO) + suppress the false share-open event; genuine-share arrival (item 007) unchanged. Rendered-Playwright acceptance. Promoted by build loop 2026-07-25 (no READY items; audit 142 clean; fresh hand-audit evidence per pre-traffic mandate). | 7.0 | SHIPPED (2026-07-25, verifier PASS HIGH — all 4 code acceptance criteria independently re-run: garden_cta_arrival 8/8, item-007 share guard intact, share_link_opened gated; risk tier HIGH independently concurred) — root-cause fix: `&src=pool` marker on the garden_cta deep-link (PoolDetail.js:253) → planner `poolSourced` branch renders honest arrival copy (EN+KO `arrivalBannerTextPool`/`sharedPlanIntroPool`) instead of the false "Someone sent/shared you this garden", and suppresses the spurious `share_link_opened` event (planner.js:4079). `delete('src')` in buildShareUrl keeps re-shares clean. Compiled/min artifacts regenerated (home.html serves them). npm test not a CI gate (no workflow runs it) + halts at pre-existing `test_minified_assets` failure (proven at HEAD); affected tests verified individually. Needs human visual spot-check (planner arrival copy render path). | HIGH | specs/143.md | 1 | — |
| 145 | **BUG (trust rails, data layer feeding the analytics sort + the planner pool pick): rate-stability KPIs derived from OUT-OF-RAIL history.** `compute-kpis.js` applies no rail to the rate *series* it consumes, so one glitch day poisons every derived KPI — and the poison comes out looking innocuous. LIVE in the committed snapshot: pool `201e5f6e-cf75-4d0e-b07f-d58da3cee23a` (balancer-v2 WSTETH-AAVE, Ethereum, TVL $12.3M, current APY 0.26%) has a single 2026-07-20 history point of **260,768.64%** (260× `APY_SANITY_LIMIT`) among eleven ~0.15–1.25% days, giving `apyMean 21731.18` / `apyStdev 72072.51` → **`apySharpe: 0.3`**, which sails through 122's `\|Sharpe\| ≤ 50` cap and 144's magnitude gates because 0.3 is a *small* number computed from big garbage (142's `audit-app.js` is blind to it too — `ABSURD_MAGNITUDE = 1e11`, clean 10-surface run this tick). Consequences: 117.2's "Risk-adjusted" analytics sort ranks this pool ABOVE every honest null-Sharpe pool, and 117.3's planner tie-break can pick it over a genuinely steadier pool. FIX (122/144 precedent — honest omission, never a clamped/cleaned value): null `apyMean`/`apyStdev`/`apySharpe` when ANY series point exceeds the rail; null `apyMomentum` only when an ENDPOINT does (the real pool's glitch is mid-series, so its −0.56 momentum is true and survives); `tvlTrend`/`historyPoints`/`firstSeen` untouched. Promoted by the build loop 2026-07-26 (no READY items existed) from `specs/144-notes.md:52`'s explicit "Ticket-worthy separately" + live snapshot/history data | 7.5 | SHIPPED (2026-07-26, PR #302 on branch `claude/loop-145`, verifier PASS HIGH 8/8 independently re-derived — verifier ran every test itself, PROVED non-vacuity by reverting `compute-kpis.js` to HEAD (4 new assertions go RED) and separately by reverting the snapshot with the fix in place (rendered test throws, so it reads live committed data, not a fixture), and independently reproduced the churn re-derivation and the pre-existing-drift claim) — `compute-kpis.js` gains a read-only `APY_SANITY_LIMIT` mirror (source of truth `app.js:800`) bounding its OWN derived math; rails themselves byte-untouched, this only ADDS a bound (not on the NEVER list, which covers *weakening*). Data regenerated for the affected pool so the fix is live on merge: churn independently verified as exactly 1 pool's `kpis` across all 3 data files, 0 non-kpis fields, only other delta = snapshot `generatedAt`. DEVIATION (the reviewable risk, full detail in `specs/145-pr.md`): a literal full `node compute-kpis.js` regen rewrites 428 files and drifts ~1,596 OTHER pools' kpis — proven pre-existing via `git stash` baseline with the UNMODIFIED script, so the code-only delta was isolated and applied on clean HEAD rather than burying a 1-pool fix in unrelated drift; `sitemap-update.yml` regenerates the full set on the next bake. New rendered-Playwright `test_kpi_rail_history.js` (real Chromium, real committed snapshot: pool sits with null-Sharpe peers in the Risk-adjusted sort; pool-detail renders the neutral "tracking for 12 days" note, no steadiness claim, no NaN/undefined/null, 0 page errors) wired into `npm test`; `test_compute_kpis.js` +75 lines. `translations.js` untouched (no new string), no new dep, no compiled/min regen needed (Node-only script). VERIFIER-FOUND DEFECT, FIXED BEFORE MERGE (not merged-with-disclosure): the first cut preserved HEAD's STALE `apyMomentum -0.56` / `tvlTrend -0.0954` for the touched pool instead of the true regen `-0.55` / `-0.0986` — decisive because `test_compute_kpis.js:202`, added by this very item, asserts `-0.55` from that same 12-point series, so the committed snapshot contradicted our own new test; corrected via a guarded exact-substring replace of that one pool's kpis in all 3 data files (no product code touched, verifier's 8/8 stands), all tests re-run green after (compute_kpis 28, kpi_rail_history 3/3, sharpe_sort 6/6, planner_sharpe_pick 8, track_record 7/7, momentum 8/8, tvl_trend 8/8). The pool's kpis object is now byte-identical to `node compute-kpis.js` output. REJECTED alternative (recorded for the human): excluding the glitch point and computing from the 11 survivors (Sharpe ≈ −12.7) — more informative, but presents a score as if the history were clean and forces a semantics change on `historyPoints`, which 3 render gates read | HIGH | specs/145.md | 1 | — |
| 144 | **P1 BUG (north-star surface, trust rails): `apyMean30d` renders unclamped** — `PoolDetail.js:1282` gates the "30d Mean APY" card on `typeof … === 'number'` only, with NO sanity bound, and the 071 volatility note (`:1368`) quotes the same value in prose. LIVE in the committed snapshot: pool `201e5f6e-cf75-4d0e-b07f-d58da3cee23a` (balancer-v2 WSTETH-AAVE, Ethereum, TVL $12.4M — clears the $10M rail, current APY 0.24%) carries `apyMean30d = 36452.38798`, so pool-detail today renders **"36,452.4%" as a trusted 30-day average** — 36× `APY_SANITY_LIMIT` — on a pool that is NOT anomaly-flagged (flagging keys off `totalApy` only, `:242`). Exactly item 122's bug class (−900T rate-stability) on the same north-star surface; root-caused and render-proven as 142's positive-control injection point (`specs/142-notes.md` §5). Fix = 122's magnitude render gate (`isFinite` alone misses it — garbage values are finite): suppress card + prose when out of rail; rendered-Playwright acceptance. Promoted by the build loop 2026-07-25 (no READY items existed) | 7.5 | SHIPPED (2026-07-25, one-commit PR on branch `claude/loop-144`, verifier PASS HIGH 7/7 independently re-derived — verifier ran every test itself, PROVED the new test non-vacuous by `git stash` baseline (3/8 with the real shipped strings `30d Mean APY36,452.39%`, the 071 prose, `0%` for NaN, `-12.5%`, `∞%`), and PROVED the 2 `test_minified_assets.js` failures pre-existing byte-for-byte at baseline) — one derived `mean30dSane` gate (typeof + isFinite + ≥0 + ≤ `APY_SANITY_LIMIT_LOCAL`) on the "30d Mean APY" card + all four copies of the 071 divergence boolean, so the prose can never quote a number the card refuses to show; honest omission, never a clamped value (122 precedent). New `test_mean30d_sanity.js` (8 rendered Chromium assertions, wired into `npm test`); compiled/min regenerated. No isolable funnel metric (trust/correctness fix) — proxy: pool-detail never renders an APY above the rail; watch north-star CTR. Needs human visual spot-check (render path, 003 smoke gate not shipped) | HIGH | specs/144.md | 1 | — |
| 146 | **BUG (product quality / honest numbers, planner share surface): the post-waitlist share path is subscription-hardcoded, number-wrong and link-less for TARGET/GROWTH plans.** `mixStats()` (planner.js ~285) resolved ANY `GOALS[]` entry with a numeric `target`, but that field means `$/mo` for a SUBSCRIPTION goal and the **item price** for a TARGET goal (`iphone` 1100, `watches` 400, `sneakers` 180, none carrying `isMonthly`) — so an iPhone was read as a $1,100/mo subscription and `foreverNumber()` turned it into ~$155k-$220k of capital the user never entered. The mix-seed effect (~1684) ran for EVERY archetype, so that bogus bundle existed from first render. It then flowed into `doWaitlistDownload`/`doWaitlistShare`, which hardcode the SUBSCRIPTION copy frame: the rendered card read `🌱 My yield covers iphone — forever · ≈$155,000 working at 8.5% · $1,100/mo covered forever` (raw untranslated id + a fabricated figure + subscription wording on a one-time purchase), the tweet read `My yield pays for iphone — forever`, and the operator's Formspree email claimed `covers iphone (~$1,100.00/mo) for ~$220,000`. Same path also passed NO plan `url` to `renderShareImage` (its own comment: "No plan URL supplied (e.g. the waitlist card download)"), violating the 2026-07-10 standing decision that every share path must carry the plan URL. Promoted by the build loop 2026-07-26 (no READY items existed) from the explicit follow-up flag item 139 left unbuilt — `specs/139.md` Territory notes L101-110, repeated in `139-notes.md:29` and `139-pr.md:80` — where 139 gated ONE symptom (a modal line) and recorded the root cause as out of scope | 7.5 | SHIPPED (2026-07-26, one-commit PR on branch `claude/loop-146`, verifier PASS HIGH 9/9 independently re-derived — the verifier ran every test itself, reproduced the `git stash` baseline failures byte-for-byte, and wrote its OWN separate neutered probe to isolate the subscription-unchanged claim from the AC5 timeout rather than trusting the builder's) — root cause fixed at `mixStats` (`g.isMonthly === true`, restoring the function's own documented contract; provably a no-op on subscriptions since all 19 subscription goals carry `isMonthly: true` and no TARGET goal does), mix seed gated to `archetype === 'subscription'`, `doShare`'s existing four-way archetype branch extracted to `buildShareCopy()` and reused for TARGET/GROWTH (2026-07-10 REUSE directive; `doShare` output byte-identical), waitlist card now carries `url: encodePlanToUrl(...)` + fires `share_link_created{method:'waitlist_card'}` (new disjoint value, existing image/copy/native series unpolluted), raw-id label fallbacks → `goalLabel(t, id)` (also stops `rent`/`phonebill` leaking), operator email archetype-branched, ONE new translation key `shareTweetGeneric` (EN+KO). ORCHESTRATOR-CAUGHT DEFECT, fixed before verification: the builder's first cut followed the spec literally and routed the SUBSCRIPTION path through `buildShareCopy()` too — but that helper reads `coveredBundle(capital)` while the waitlist functions read `selectedSubs` (what the user actually toggled), so a user who added Spotify without the capital covering it would have had it silently dropped from their own card; subscription branch restored byte-for-byte and pinned by a new toggled-mix test case (AC3b) proven identical against a stashed baseline. New rendered-Playwright `test_waitlist_share_archetype.js` (6/6, real Chromium, canvas text read via a `fillText` spy — the strings actually drawn) wired into `npm test`. No isolated funnel metric (pre-traffic quality fix per the 2026-07-23 mandate); secondary read = `share_link_created{method='waitlist_card'}` becoming non-zero, structurally impossible before. PRE-EXISTING, NOT CAUSED, NOT FIXED (both agents proved via identical stashed-baseline runs): `test_waitlist_funnel.js` 3 cases + `test_minified_assets.js` 2/9 — the latter halts the `&&`-chained `npm test` before it ever reaches this item's test, so each named test was run individually instead. Needs human visual spot-check (share-card render path; 003's smoke gate doesn't cover the canvas) | HIGH | specs/146.md | 1 | — |
| 147 | **Ship the minified bundles the site already builds, and un-blind `npm test`** — `home.html:175/196` and `plan.html:100` loaded the RAW `translations.js` (90,310 B) / `planner.js` (238,585 B) instead of the committed, CI-regenerated `translations.min.js` (69,645 B) / `planner.min.js` (100,417 B) that backlog 053 built and `053-notes.md:7`/`053-pr.md:21` record as wired — a drift back to raw (the comment at `home.html:194-195` still says "Minified in CI (backlog 053)"). Two costs from one cause: (1) ~159 KB of avoidable statically-deferred JS on `home.html`, the shell serving the `?pool=`/`?token=` SEO landers = the north-star surface (~138 KB on `plan.html`); (2) `test_minified_assets.js` is file **6** of `package.json`'s ~90-file `&&` test chain and exited 1 on unmodified `main`, so ~85 downstream tests NEVER RAN — recorded as "pre-existing, out of scope" by ELEVEN consecutive loops (117.2, 125, 128, 130, 137-140, 143, 144, 146) and never classified. Promoted by the build loop 2026-07-26 (no READY items existed) | 7.8 | SHIPPED (2026-07-26, one-commit PR on branch `claude/loop-147`, verifier PASS HIGH 9/9 independently re-derived incl. its own stash/pop non-vacuity cycle) — 3 `src` swaps + new rendered `test_min_asset_boot.js` (18 assertions, real chromium: request-level exact-pathname min-not-raw checks AND behavior-level proof the minified bundle EXECUTES — chip-click→thread-row on plan.html and `?fresh=1`, both north-star CTAs on `?pool=`, pool cards on `?token=USDC`, KO copy on `?lang=ko`), wired into `npm test` at position 7. `npm test` now clears files 1-8, stops at file 9 `test_smoke.js` on PRE-EXISTING stale bare-`/`-is-planner assertions (2026-07-15 landing pivot) — proven identical on stashed baselines by builder AND verifier, filed as the next promotion candidate. No isolable funnel metric (payload/infra fix upstream of the north star, same surface as 140); guardrails to watch: `error_occurred` rate, planner conversion, parameterized-URL integrity. Needs human visual spot-check (both router paths now boot from minified bundles) | HIGH | specs/147.md | 1 | — |
| 148 | **Stop minting nonsense token pages from Pendle expiry-date fragments** — `generate-token-pages.js:87` splits `pool.symbol` on `/[-_\/\s]/` and `TOKEN_REGEX` (line 71) accepts pure-digit and date-shaped strings, so `PT-SUSDE-22OCT2026` mints a "token" called `22OCT2026` with its own landing page. Live snapshot (2026-07-26, 738 pools) yields 8 such tokens (22OCT2026, 10DEC2026, 18JUN2026, 16SEP26, 8OCT2026, 17DEC2026, 24SEP2026, 27AUG2026); 7 junk slugs are live in `sitemap-token-pages.xml` right now (`/tokens/8oct2026`, `17dec2026`, `2027`, `20261231`, `00`, `01`, `67`), each shipping an EN page + a KO page + an OG image + an IndexNow submission. Rendered verbatim: `<h1>8OCT2026 DeFi Yields</h1>`, "4 live 8OCT2026 pools … up to 0.20% APY". Prod crawl evidence: `?poolTypes=Lending&token=22OCT2026` recorded a real page_view in 7d (`becb8e74`) — previously assumed crawler-invented, actually ours. Same class as 013 (sitemap quality threshold, DONE), which gated on pool count/TVL but never on "is this string actually a token". Fix = tighten `isValidToken()` in BOTH generators + regen; set churns daily so assert the predicate, not the slug list | 5.5 | SHIPPED (2026-07-29, human directive "merge" — the answer to the question this item had been BLOCKED on since 07-26: dropping 7 nonsense token slugs is item-013-class sitemap quality work, NOT de-indexing). **Not merged as PR #306** — that branch had merge conflicts (13 commits of drift from its 07-26 base) and merging it wholesale would have REVERTED two items that shipped the same day: 173 (`generate-token-pages.js` `appUrl` losing `&minTvl=${MIN_POOL_TVL}`, re-breaking 1,701 token-page CTAs) and 170 (`package.json` reverting to the pre-lane flat chain, dropping ~14 tests incl. `test_seo_cta_targets.js` and every `test_audit_*.js` from the gate). Instead the predicate ALONE was transplanted onto current `main`: the two `isValidToken` rejection rules (pure-numeric + date-fragment) in both mirrors, the `isValidToken` export, `test_token_slug_validity.js` verbatim (66 assertions), and a ONE-LINE `test:serial` registration. Non-vacuity re-proved on this checkout (stashed → exit 1 on origin/main's predicate; applied → 66 pass). Regen delta: 2,132 → 2,118 token pages — **7 target junk slugs dropped** (`00 01 17dec2026 20261231 2027 67 8oct2026`, zero remaining) + 7 real tickers out / 2 in from ordinary daily TVL churn. Digit-leading real tickers (`1W 4W 13W 0X0 3CRV 1INCH 40AVAX 50EIGEN`) all still have pages. Rails byte-identical. PR #306 to be CLOSED as superseded, not merged | HIGH | specs/148.md | 0 | 2026-08-12 |
| 149 | **`audit-app.js` fails silently in a fresh clone** — `node audit-app.js` dies with `MODULE_NOT_FOUND: playwright` in any checkout without `node_modules` (i.e. every cloud-routine session; observed first-hand this tick). `playwright` IS declared in devDependencies and IS installed globally at `/opt/node22/lib/node_modules` — re-running with `NODE_PATH` set produced the real result (10 surfaces, 0 findings). The danger is not the crash but the artifact: on failure NO `signals/audit-findings.json` is written, so a stale file survives and a crashed run reads as "audit clean" — silently skipping the pre-traffic mandate's PRIMARY job (NORTH_STAR 2026-07-23, heartbeat §2b). Fix = resolve playwright with a global fallback, and on total failure write `{"status":"DID_NOT_RUN","surfacesCovered":[]}` + exit non-zero. No dependency change (explicitly out of scope) | 4.0 | DONE (window closed 2026-07-29, MOVED) — SHIPPED (2026-07-26, PR #308) — verifier PASS, 7/7 criteria independently re-derived (own red→green non-vacuity cycle, own forced-failure run, own isolated-copy check that the crash path overwrites the real signals file). Risk tier **HIGH** assigned by the verifier against the builder's/spec's LOW guess — purely on size (154-line `audit-app.js` diff vs the 150-line LOW cap; infra resolver rewrite isn't cleanly copy/docs/tests/instrumentation), no NEVER-list category touched → auto-merged under autonomy (c) after the HIGH-gate explainer + quiz. Lazy `resolvePlaywright()` (local → `npm root -g` → hardcoded global fallback, version logged since the global 1.56.1 is a degraded path vs the declared `^1.61.1`); `status:"OK"`/`playwright{}` on success and a `status:"DID_NOT_RUN"` artifact written to the REAL path on any fatal error (overwriting the stale verdict is the fix); exit codes 0/1/2/3 with item-142's P0/P1 contract lifted verbatim into an exported `blockingFindings()`. Zero dependency change. `test_audit_runner.js` (9 assertions, ~0.5s, no browser) wired into `npm test`. NOT FIXED, same bug class in other files, out of this item's scope: `test_dead_pool.js` (`playwright`) and `test_token_pages.js` (`@napi-rs/canvas`) still die on missing `node_modules` — ticket-worthy separately. Known-fragile, verifier-proven, deliberately unpatched post-PASS: `resolvePlaywright.lastAttempts` goes stale after an intervening success (harmless at the single synchronous call site) — WINDOW CLOSED 2026-07-29 · **MOVED → DONE** — and the one closure this tick measurable without traffic. This heartbeat ran in a fresh cloud clone with no `node_modules`: `NODE_PATH=/opt/node22/lib/node_modules node audit-app.js` completed and wrote `status:"OK"`, a this-tick `generatedAt` (2026-07-29T07:07:15Z), 29 surfaces and a non-empty findings list. Pre-149 the same command died `MODULE_NOT_FOUND` and left the previous day's file on disk to be misread as clean (`signals/2026-07-29.md`) | HIGH | specs/149.md | 1 | DONE |
| 154 | **The product-audit scanner audits ONE hand-picked SEO page out of 2,167** — `audit-app.js:472` hardcoded `/tokens/usdc.html` (the flagship page, the one most certain to be fine) as its entire static-SEO coverage, while the checkout carries 2,079 `tokens/*.html` + 88 `chains/*.html`. That is why the 2026-07-26 heartbeat run scored "10 surfaces, 0 findings" on the same day a human found item 148's live junk pages (`<h1>8OCT2026 DeFi Yields</h1>`) by tracing prod URLs by hand — `reports/2026-07-26.md`: "the scanner cannot find this class by construction; it only drives surfaces we hand it". Fix = anchor + deterministic rotating sample (seeded by UTC date, `AUDIT_STATIC_SAMPLE`/`AUDIT_STATIC_SEED`/`AUDIT_STATIC_PAGES` overrides) plus three page-level checks the old static branch had no notion of: `junk-slug` (148 class), `zero-yield-claim` (032's gate leaking / PT fixed-yield class), `empty-table` (soft-404). Detector only — reads pages, changes no generator, deletes nothing | 6.5 | SHIPPED (2026-07-26, verifier PASS HIGH 7/7 independently re-derived incl. its own origin/main non-vacuity cycle 5→1 passed and six digit-leading real tickers checked) — promoted by the build loop: no takeable READY item existed (148 READY on main but already in flight as open PR #306, human-gated on the NEVER list). Real post-merge-gate run: 16 surfaces (was 10), 0 findings. HONEST LIMITATION recorded in specs/154-notes.md: 4 sampled token pages/day out of 2,079 ⇒ ~1.3%/day chance of hitting a specific known-junk page — structural blindness removed, sampling rate still weak; follow-up (prefer never-audited slugs / cheap grep prescan promoting suspicious pages into the rendered sample) documented, not built. No isolable Mixpanel funnel metric (loop tooling under the pre-traffic quality mandate, 142/149 disclosure pattern) | HIGH | specs/154.md | 1 | — |
| 155 | **The audit scanner's only P0 positive control is dead** — `test_audit_app.js` case 2 injects the 122 magnitude into `apyMean30d` and expects `audit-app.js`'s `number-sanity` P0 check to catch it on a real render; it has been RED since item 144 shipped `mean30dSane` (`PoolDetail.js:161-164` + the render gate at `:1290`), which suppresses the out-of-rail card entirely so the value never reaches `document.body.innerText`. The detector is fine — 4 probe renders this run: `apyMean30d` 0 findings, `apyBase`/`apyReward` 7 each, `tvlUsd` 0 (K/M/B/T suffix deliberately skipped) — only the injection point rotted. Compounding it, `test_audit_app.js` was never in `npm test`, so the red survived two loop runs recorded as "pre-existing, out of scope". Promoted by the build loop 2026-07-26 (no takeable READY item: 148 is READY on main but in flight as open PR #306, NEVER-list human-gated) from `specs/154-notes.md`'s explicit triage request + 154's LOG hypothesis ("144/145's rails may now null the injected `apyMean30d` … a positive control that cannot fail is worse than no test"). Fix = repoint case 2 at `apyBase`, convert the stale injection into a NEGATIVE control locking 144's rail, wire the file into `npm test`, and stop the test clobbering the heartbeat's committed `signals/audit-findings.json` | 6.8 | SHIPPED (2026-07-26, one-commit PR on branch `claude/loop-155`) | LOW | specs/155.md | 1 | — |
| 156 | **The `npm test` smoke gate has been blind on the bare-`/` route since the 2026-07-15 landing pivot** — `test_smoke.js:181` still waited for `#planner-root [class*="gp-"]` on bare `/` (which now mounts the search-first landing into `#landing-root`) and `test_hub_pages.js:186` still string-matched the pre-pivot two-mode router `window.__APP_MODE = needsAnalytics ? 'analytics' : 'planner';`. Both deterministic reds on `main` for 12 days. `test_smoke.js` is file **9 of 90** in `package.json`'s `&&`-chained test script, so its red hid **81 test files** from every plain `npm test`, and the default route had NO smoke coverage and NO page-error collection at any viewport. Promoted by the build loop 2026-07-27 (no READY items existed — 148 reads READY on `main` but its PR #306 is open/human-gated); item 147's own notes had already handed this off by name as the next stopper. | 7.8 | SHIPPED (2026-07-27, one-commit PR on branch `claude/loop-156`, verifier PASS 7/7 — every claim independently re-derived by execution: its own `test_hub_pages.js` 42/exit 0, its own `test_smoke.js` 11/exit 0, its own BOTH-DIRECTION non-vacuity cycle (reverted `home.html:82` → 41/exit 1 → restored → 42; broke the landing selector → 8/exit 1 → restored byte-for-byte → 11), and its own `git worktree` at `origin/main` proving `test_landing.js` red there too). Verifier assigned tier **LOW**, DISAGREEING with the spec's proposed HIGH and giving reasons: policy lists tests under LOW, 50-line diff, no new deps, and `home.html` proven byte-identical — the diff only *reads* a string describing the already-shipped router, and 5a proved a real router change still trips it red. `npm test` now clears position 9; next stopper is position 10 `test_landing.js` (pre-existing, product probed working — next promotion candidate). No isolable funnel metric (quality-gate repair) | LOW | specs/156.md | 1 | — |
| 157 | **The product-audit scanner renders 6 of 2,176 SEO pages a day — 1.3%/day odds of finding a known-bad one** — `buildStaticSurfaces()` (`audit-app.js:163-211`) spreads its sample UNIFORMLY over the sorted leaf list, so item 154's correct rendered checks (`junk-slug` P1 `:467`, `zero-yield-claim` P1 `:476`, `empty-table` P1 `:483`, `number-sanity` P0) almost never get pointed at a bad page: the 2026-07-26 heartbeat scored "10 surfaces, 0 findings" the same day a human found 148's live junk pages by hand. Promoted by the build loop 2026-07-27 from `specs/154-notes.md:243-248` (154's own builder documented the limitation, the arithmetic and this exact follow-up) — no takeable READY item existed: 148 reads READY on `main` but is in flight as open PR #306, human-gated on the NEVER list. Fix = cheap fs+regex prescan of ALL `tokens/*.html`+`chains/*.html` (~480ms for 2,176), promote the suspicious pages into the rendered sample ahead of the uniform rotation, aggregate findings per signal | 6.4 | SHIPPED (2026-07-27, one-commit PR on branch `claude/loop-157`, verifier PASS **8/8** independently re-derived by execution — it ran all three test files itself concurrently (7/7, 3/3, 5/5, all exit 0), performed its OWN non-vacuity cycle (grepped for the real `const cap = Math.min(...)` line rather than trusting the builder's line number, forced `cap = 0` → 4 passed/3 failed with criteria 3/5/6 red → restored → `diff` empty → 7/0 green), read `sampleBySeed` to confirm cap≥length returns the WHOLE suspect set (promotion is a guarantee, not a lucky hash), and confirmed no probe file leaked). Tier **HIGH** on size alone (+224/−18 in `audit-app.js` vs the 150-line LOW cap), no NEVER-list category touched → auto-merged under autonomy (c) after the HIGH-gate explainer + quiz. Measured: `scanned` 2176, 7 `junk-slug` suspects (exactly 148's live set, re-derived from disk not hardcoded — the set churns daily), 0 for `zero-yield-claim`/`broken-number-literal`/`absurd-magnitude`; the tightened magnitude regex measures 0/2,176 where the loose form false-positives on `tokens/a0t.html` ("A0T" contains "0T"). Render budget RE-AIMED not grown (promoted pages replace uniform picks). INTENDED behaviour change: a default `node audit-app.js` now exits non-zero every run with a P1 `static-prescan:junk-slug` finding while 148's 7 junk pages stay live — self-clears when PR #306 merges. New `test_audit_prescan.js` (232 lines, 7 rendered/real-checkout cases, wired into `npm test`); criteria 3/4 are REAL Chromium renders of a temp probe page deleted in `finally`. TWO documented deviations (both protect legacy tests this item may not edit, neither weakens the production path): `opts.staticOnly` defaults prescan off unless opted in; aggregate findings honour the same `opts.only` allowlist the rendered surfaces already do. NOT built, still ticket-worthy: 154-notes' Design A persisted never-audited rotation. Non-blocking verifier observation: `opts.prescanMax: 0` falls through to the default via the file's pre-existing `||`-falsy-0 pattern (same as `opts.staticSample` on `main`) — kill switch unaffected. No isolable funnel metric (loop tooling under the pre-traffic quality mandate, 142/149/154 disclosure pattern) | HIGH | specs/157.md | 1 | — |
| 158 | **`test_landing.js` — the inherited next stopper, three harness defects, zero product bugs** — file **10 of 91** in `package.json`'s `&&` test chain, deterministically red in-sandbox, so every file after it never ran on a plain `npm test`. Named by name as the next promotion candidate in `specs/156-notes.md:110-114` after 156 cleared position 9. Root-caused this run (the product is correct throughout — the landing search navigates fine and renders 9 pool cards): (1) `page.waitForURL` defaults to `waitUntil:'load'`, and `load` NEVER fires on `/?token=USDC` because ~10 `icons.llamao.fi` icons + `mp.defi.garden` + `api.fontshare.com` hang against the proxy-blocked sandbox — measured `document.readyState === 'interactive'` 12 s after a navigation that had already completed correctly; (2) the file never routed `**/data/pools-snapshot*`, so since 059's snapshot-first change its `FIXTURE_POOLS` was dead code and it silently asserted against the committed 738-pool snapshot (148 USDC results rendered vs 2 declared); (3) `IGNORABLE_ERROR_PATTERN` matched `msg.text()` only — but Chromium's "Failed to load resource" text never contains the URL — and was missing three hosts. `test_search.js:244` / `test_smoke.js:129-131` already ship the cure for all three; this file had simply never adopted it. Promoted by the build loop 2026-07-27: **no takeable READY item existed** (148 reads READY on `main` but is in flight as open PR #306, human-gated on the NEVER list) | 7.5 | SHIPPED (2026-07-27, one-commit PR on the loop branch, verifier PASS **LOW 6/6** independently re-derived — incl. its OWN mutation kill distinct from the builder's: stripping `waitUntil:'commit'` reproduces the exact original symptom, restored byte-identical at md5 `0905190f75b40a9574bd5b18170bd268`; its own scratch probe measuring 2 rendered `.pool-card`s off the fixture; its own line-by-line diff of all 5 checkpoints against `git show origin/main:test_landing.js` confirming zero assertions weakened; and its own `app.js:1119` read confirming the stale-`generatedAt` mechanism is real) — test-only diff, 27 insertions / 4 deletions, ZERO product files, no new dependency, trust rails / `__APP_MODE` router / `?token=/?chain=/?pool=` / generated SEO surface all byte-untouched. Chain now confirmed clear through position **12**: `test_smoke` 11/11, `test_landing` 5/5, `test_canonical` 24/24, `test_search` **20/20 exit 0** (verifier, 550 s budget — its apparent 2/20 then 18/20 collapses were `timeout`-kill artifacts, NOT reds). Positions 13-91 remain UNOBSERVED in-sandbox — the real-Chromium suite is slower than the 5-minute foreground timebox — and this item does not claim `npm test` is green. No isolable funnel metric (loop tooling under the 2026-07-23 pre-traffic quality mandate; 142/149/154/156/157 disclosure pattern) | LOW | specs/158.md | 1 | — |
| 159 | **P0 TRUST-RAIL BREACH, LIVE ON PROD: `llms.txt` publishes anomalous APYs with no sanity limit** — `https://www.defi.garden/llms.txt` serves `- BSC · zeebu · ZBU — 353114.2% APY, $576,877 TVL` under the header *"Live highest APY opportunities (updated daily, TVL ≥ $10k)"*, verified by curl against live prod 2026-07-27. `generate-llms.js` `pickHighYield()` (`:227-241`) filters on `tvl >= minTvlUsd && apy > 0 && isFinite(apy)` with `minTvlUsd = 10000` and **zero occurrences of `SANITY` in the whole file**, then sorts APY-descending — so the surface is by construction a leaderboard of the dataset's most anomalous pools, the exact inverse of the rail. TWO rails breached at once: `APY_SANITY_LIMIT` (1000, `app.js:800`/`planner.js:19`) not applied, and `DEFAULT_MIN_TVL` ($10M, `app.js:801`) replaced by a $10k floor, 1000× looser. Scale: **8** lines >1000% in `llms.txt`, **15** in `llms-full.txt` across 6 chain sections, max 353,114.16%. The top pool (`zeebu`/`ZBU`) is **not in the $10M-floored snapshot at all** — the product itself would never show it. Regenerates daily via `.github/workflows/sitemap-update.yml:110`, so it self-refreshes with each day's worst outlier. `llms.txt` is the AI-discovery surface: the artifact whose entire purpose is telling LLMs what DeFi Garden claims. Fix = apply the existing rail in the generator + honest TVL floor + regen; do NOT weaken any rail. NOT a growth experiment — no metric, closes on functional verification (020/048 precedent) | 9.2 | SHIPPED (2026-07-27, PR on branch `claude/loop-159`, verifier PASS **HIGH 9/9** independently re-derived — the verifier ran every test itself, did its OWN mutation kill (stripped `&& apy <= APY_SANITY_LIMIT` → 12 pass/2 fail, restored byte-identical, md5 `59ccd182b3633dcd227995d9dbd22c8f` before/after), its OWN derived-string test (mutated `MIN_TVL_USD` to 25000000, called `buildConcise()` directly, saw the TL;DR render `TVL ≥ $25M`, restored), live-fetched `yields.llama.fi/pools` to re-count eligible pools (548), cross-checked ALL 8 surviving deep links against `data/pools-snapshot.json` (8/8 resolve, spec required ≥3), and judged the NEVER-list question on the facts rather than on the operator's word) — `generate-llms.js` gains read-only MIRRORS of the product rails (`APY_SANITY_LIMIT = 1000` mirroring app.js:800, `MIN_TVL_USD = 10000000` mirroring app.js:801); `pickHighYield()`'s filter gains `&& apy <= APY_SANITY_LIMIT` and its default floor moves $10k → $10M. The rails themselves are byte-untouched — this only APPLIES an existing rail to a surface that never had one (the NEVER list forbids *weakening*, which points the other way). LIVE RESULT: `llms.txt`'s top line goes from `zeebu · ZBU — 353114.2% APY, $576,877 TVL` (a pool absent from the $10M snapshot entirely — the product would never show it) to `uniswap-v3 · WETH-USDC — 47.2% APY, $112,787,401 TVL`; lines >1000% go 8 → **0** in `llms.txt` and 15 → **0** in `llms-full.txt`, with 8 and 15 real pool lines still standing (spec floor ≥5). TVL floor decision: **$10M** per the spec's recommendation — 548 in-rail pools qualify live, far above `topN=15`, so the spec's "fall back to $1M if under ~8 lines" branch was never needed (recorded in `159-notes.md`). New `formatTvlFloor()` makes the rendered TL;DR claim DERIVE from the constant (`TVL ≥ $10M`) instead of restating it — a second hardcoded literal would be a fresh copy of the very bug being fixed. Both files regenerated ONLY via the sanctioned `npm run generate:llms`, never hand-edited. New `test_llms_rails.js` (14 assertions) wired into `npm test`: it gates the COMMITTED artifacts (parses both files off disk in both render formats, `.toFixed(1)` and `.toFixed(2)`) as well as the function, so a future bad regen fails CI. BUILDER-FOUND TEST DEFECT, fixed before verification (the reusable lesson, now in the playbook): the spec's own headline fixture — the 353,114.2%/$576,877 pool — is excluded by the **TVL floor alone**, so it stayed GREEN when the APY rail was deleted; a second fixture (50,000% APY at $500M TVL) was added so the APY ceiling is independently load-bearing. The mutation run confirms it — exactly 2 of 14 go red (huge-TVL anomaly + the `1000.01` boundary) while the headline fixture stays green. NO INSTRUMENTATION, deliberately (142/149/154 precedent): `llms.txt` is a static crawler-fetched text file with no client runtime — nothing in it can execute, so nothing in it can fire an event; inventing a metric would mean inventing one with no mechanism to ever emit. Closes on functional verification per the 020/048 precedent — no 14-day window, not an experiment, must NOT be marked REVERT-CANDIDATE for failing to move the north star. HONEST LIMITS: the regen carries ordinary daily churn alongside the rail-driven removals (split recorded: `llms.txt` 20 churn/18 rail, `llms-full.txt` 48 churn/40 rail); `test_seo_surface_audit.js` could not run (headless-Chromium TLS blocked at the proxy, NORTH_STAR 2026-07-12) but greps clean of any `llms`/`generate-llms` reference so it does not cover this surface; the full ~90-test `npm test` chain was not run end-to-end (timebox) — targeted set green instead | HIGH | specs/159.md | 1 | — |
| 160 | **The product audit is blind to every non-HTML generated surface** — `audit-app.js` drives rendered HTML only (16 surfaces this tick, incl. 157's 2,197-page prescan), while `llms.txt`, `llms-full.txt`, `sitemap-*.xml` and `og/*.png` are committed, publicly-served, generator-produced surfaces that **no automated check reads**. Two P0/P1-class defects in two consecutive days were both found BY HAND in exactly this gap: 148 (2026-07-26, junk token pages — scanner scored "0 findings" the same day) and 159 (2026-07-27, `llms.txt` rail breach — this tick's scanner run was clean apart from the known 148 class while 353,114% APY was live). Cheap fix, and `prescanStaticPages()` already proves the pattern (pure fs+regex over 2,197 files in ~480ms): a `prescanTextSurfaces()` over `llms*.txt` emitting aggregate findings — `apy-rail-breach` P0 (any `% APY` figure > `APY_SANITY_LIMIT`), `tvl-floor-claim` P1 (stated floor disagrees with the smallest TVL listed), `broken-number-literal` P0 (reuse existing predicate), `empty-surface` P1 (guards against 159's own fix over-tightening into an empty file). Sitemaps + OG images named as known-uncovered and deliberately deferred. Ships AFTER 159 | 5.0 | SHIPPED (2026-07-27, PR #317 — `prescanTextSurfaces()` over llms.txt/llms-full.txt, 4 detectors, 15/15 new tests incl. fixture positive control + both boundary directions; 159 shipped first as required) | LOW | specs/160.md | 1 | — (loop tooling — no measurement window; observable at the next heartbeat's audit-findings.json) |
| 162 | **The product audit has never rendered the planner — the product's DEFAULT face** — `audit-app.js`'s surface rotation was 16 entries, every one of them analytics app (`?token=`/`?chain=`/`?pool=`) or static SEO; `grep` for `plan.html` / `planner-root` / `landing-root` / `gp-` in the scanner returned ZERO hits, and the 2026-07-27T07:22Z `signals/audit-findings.json` confirms it live. CLAUDE.md calls the Garden Planner "the DEFAULT feature"; the north-star CTA `pool_click{garden_cta}` lands there; item 146's hand-found six-figure-capital bug lived there. Same blind-spot class as 154 (one hand-picked SEO page) / 157 (uniform sampling) / 160 (non-HTML surfaces), and the largest one left. Promoted by the build loop 2026-07-27 — no takeable READY item existed (148 is READY on main but in flight as human-gated PR #306). | 6.9 | SHIPPED (2026-07-27, one-commit PR on branch `claude/loop-162`, verifier PASS **LOW 8/8** independently re-derived — the verifier ran the full unscoped audit, both CLI-flag runs and all three test files ITSELF, and proved non-vacuity with its OWN mutation, different from the builder's: `.gp-chip` → `.gp-chip-MUTATED-NONEXISTENT` fired exactly one `dead-cta` P1, restored `md5sum`-identical, green again; builder separately mutation-killed `dead-end` via a nonexistent planner URL) — adds `kind: 'landing'` (bare `/`, `#landing-root .landing-app`) and `kind: 'planner'` (`/plan.html`, `#planner-root .gp-app`, first screen only) plus four surfaces appended AFTER `pool-detail-ko` so no existing `surfacesCovered` entry moves: `landing`, `planner`, `planner-360`, `planner-ko`. Checks reuse existing helpers/vocabulary only (`dead-end`, `number-sanity`, `page-error`, `dead-cta`, `responsive` on 360, `i18n` Hangul on KO). DEVIATION disclosed: `--only=`/`--static-only` never existed as CLI flags (the `require.main` block called `runAudit()` with no args), so ~12 lines of argv parsing were added to make A3/A4 runnable; no-flags default path unchanged. ZERO findings on all four new surfaces in both parties' runs — the run's 5 findings are the pre-existing 148-class `junk-slug` pages. HONEST GAP recorded: 146's bug class sits several steps past the first screen, so extending the driver into the conversation flow (goal → funding mode → bloom) is the follow-up that would catch it — named, not filed. Not run by either party: the full 93-file `npm test` chain (standing limitation, never observed past position 12) and `test_audit_runner.js`/`test_seo_surface_audit.js`/`test_audit_prescan.js` (grep-reasoned safe, not executed). Trust rails / `__APP_MODE` / `?token=/?chain=/?pool=` grep-proven byte-untouched; no new dep; no product file. No isolable Mixpanel funnel metric (loop tooling under the 2026-07-23 pre-traffic mandate, 142/149/154/155/160 disclosure pattern) — success is observable at the next heartbeat tick when `surfacesCovered` gains the four entries. ID NOTE: shipped as 162 because open PR #316 already holds ID 161 + branch `claude/loop-161`; the orchestrator's mid-run renumber made both subagents report a "second session" contending for the tree → new `playbooks/loop-container-contention.md`. | LOW | specs/162.md | 1 | — |
| 163 | **`npm test` — the automated half of the merge gate — was structurally unobservable**: `package.json`'s `test` was **94 `node <file>.js` steps joined by `&&`**, so (a) a fresh clone (no `node_modules` — every cloud-routine session) died at position 5 with a raw `MODULE_NOT_FOUND` stack from inside a required *generator* (measured on this checkout: non-browser subset **19 pass / 17 fail**; after `npm ci`, 3s, the same subset is **34 pass / 2 fail** — the reds were entirely environmental, item 149's finding generalised from one file to the ship gate), and (b) `&&` meant nothing after the first red ever ran — `specs/158-notes.md`: *"the chain has never been observed past position 12 in-sandbox"*, i.e. **82 of 94 files had no observed result in any recent build session, including the trust-rail tests** (`test_llms_rails.js`, `test_mean30d_sanity.js`, `test_zero_yield_demote.js`, `test_kpi_rail_history.js`) — while the standing autonomy-(c) decision merges on *"verifier PASS + **tests green**"*. FIX: `run-tests.js` — parses `test:serial` (the original chain, preserved byte-identical) as the single source of truth for the file list; preflights `node_modules` (one actionable line, exit 2, zero files run); classifies lanes by a **cycle-safe transitive scan of local require edges**, so `test_seo_surface_audit.js`/`test_audit_prescan.js`/`test_audit_app.js` land in the browser lane via `./audit-app.js` despite never naming the driver (a one-level grep — the operator's first probe — got all three wrong); per-file timeout, bounded concurrency, browser lane forced serial (`audit-app.js` binds port 8821; 162 recorded a live `EADDRINUSE`); never aborts on a red; per-file PASS/FAIL/TIMEOUT + summary; non-zero exit iff anything failed. Default timeout is lane-aware (120s plain / 600s browser) — a flat 120s would have marked `test_search.js` (~550s per 158) TIMEOUT and made a default `npm test` permanently red. Promoted by the build loop 2026-07-27: **no takeable READY item existed** (148, the sole READY row, in flight as human-gated PR #306; 010-diagnosis 011-015 all DONE; the newest report's build order 159→160 plus 162 all shipped) | 7.4 | SHIPPED (2026-07-27, one PR on `claude/loop-163`, verifier PASS **10/10 tier HIGH** independently re-derived — own from-scratch classifier reproduced the lane split with **0 disagreements across all 95 files**, A3/A5 mutation-proved with mutations the verifier invented itself, `test_run_tests.js` proved non-vacuous by mutating the code under test) — **plain lane: 34 pass / 0 fail / 0 timeout in ~7s, where the chain had never been observed past position 12**. Browser lane (61 real-Chromium files) exceeds the 5-min foreground timebox and is **not** claimed green: exactly 2 files driven end-to-end and green (`test_landing.js` 40.6s, `test_smoke.js` 134.4s under the new 600s browser default — the same file that was TIMEOUT at the old flat 120s), 59 not run — see `specs/163-pr.md` "What is NOT claimed". No funnel metric (gate-integrity fix; 020/048 precedent — closes on functional verification, no 14-day window) | HIGH | specs/163.md | 1 | — |
| 164 | **The product audit renders the planner's goal picker and stops — every number the planner computes is unwatched.** `audit-app.js:865-901` (`kind: 'planner'`, shipped by 162) waits for `#planner-root .gp-app`, checks one `.gp-chip` is visible, and closes the page; `planner.js:3902` shows `STEPS = ['goal','funding-mode','deadline','monthly','horizon','temperament','bloom']` — the scanner sees step 1 of 7, a screen of goal chips with no derived number on it, so `scanNumbers()`/`auditText()` run against nothing to scan. Item 162 flagged this gap explicitly and declined to file it (`specs/162-notes.md:170-180`: "a later item extending the planner driver into the conversation flow … is the natural way to actually catch 146's bug class"). Item 146 is the proof the class is real and shipped: `mixStats()` read TARGET goal `iphone`'s $1,100 item price as a monthly subscription and rendered `≈$155,000 working at 8.5% · $1,100/mo covered forever` — found by hand, invisible to automation. Fix = new `bloom` surface kind driving the three archetype deep links (the same URLs `test_plan_checkout_cta.js` already loads, which are also the share-recipient surface) + 360 + KO, plus one interactive goal-chip-advance check on the existing planner surface. Detectors only — no product file touched | 7.0 | SHIPPED (2026-07-27, one commit on `claude/loop-164` + PR, verifier PASS **9/9, tier LOW independently assigned in DISAGREEMENT with the spec's own HIGH guess** — the guess was conditioned on "the diff will exceed the 150-line cap", false by measurement: 84 lines (78 ins/6 del) in `audit-app.js`, 1 line of `package.json` test wiring with deps byte-identical, rest `product-loop-kit/**`; policy's "tests" bucket) — new `kind: 'bloom'` driver in `audit-app.js` loads a deep-linked plan URL (the share-recipient surface, reusing `test_plan_checkout_cta.js`'s three archetype URLs verbatim) and runs the EXISTING detectors — `dead-end` P1 on `.gp-checkout-panel`, `number-sanity` P0 via `auditText()`, `dead-cta` P1 on `.gp-checkout-cta`, `page-error` P0, `responsive` at 360, `i18n` P2 on KO — against the first screen the audit has ever rendered where the planner shows a number it COMPUTED rather than a static chip label. 5 surfaces appended after `planner-ko` (`plan-bloom-growth`/`-target`/`-subscription`/`-360`/`-ko`; `surfacesCovered` 20 → 25, verifier programmatically confirmed no pre-164 entry renamed/reordered/dropped). Plus one interactive check on the existing `planner` kind (1280/EN only): clicks the first `.gp-chip` and polls for `.gp-thread-row` — a rendered DOM signal that exists only past `step === 'goal'`, safe for every archetype because `pickGoal()` never jumps goal→bloom — catching a chip that renders but does not advance, which visibility alone cannot see. Detectors only, zero product files touched (verifier byte-compared planner.js/plan.html/home.html/app.js/PoolDetail.js/translations.js/all CSS/SEO surface). **Result: zero findings on all 5 new surfaces** — an honest null. `plan-bloom-target` drives item 146's exact `iphone` goal, and 146 was fixed 2026-07-26, so clean = first automated confirmation the fix holds, not a scanner miss. Non-vacuity proved 3× by the builder and 3× more by the verifier with its OWN different mutations, including one on the bloom `dead-cta` the builder never exercised; `test_audit_planner_flow.js` 11/11, stash-cycle 2/11→11/11. Gap left open and stated: the 5 intermediate steps (funding-mode/deadline/monthly/horizon/temperament) still render numbers no scanner has seen. No funnel metric (detector coverage; 084/098/162 disclosure pattern) | LOW | specs/164.md | 1 | — |
| 165 | **P1 BUG (trust rails, north-star surface): an out-of-rail rate compounds into a fictional dollar figure on pool-detail** — the page renders "$100 in this pool grows to ~**$49,002,948,093,727,200,000** in 5y at current rates." Item 025 wrote the principle into `PoolDetail.js:263` (`showConcreteCta = !isAnomalous`, *"anomalous rates are flagged, never hyped"*) and applied it to the CTA button only; the projection card (`:635`), daily (`:697`) / monthly (`:732`) quick-metrics and the yield calculator (`:1095`) all derive $ from the same unguarded `totalApy`. Live exposure measured 2026-07-28 (`curl yields.llama.fi/pools`, 16,050 pools): **75 pools above `APY_SANITY_LIMIT_LOCAL`, 3 with ≥$1M TVL**; worst is `6cf96588…` (zeebu/BSC/ZBU, 345,079.1%) — the same pool item 159 pulled out of `llms.txt` the day before. `?pool=` always fetches live (`app.js:1141`), so the $10M snapshot rail never covered this surface. Documented and never ticketed: `specs/155-notes.md:222-240`. Fix = display-only gate on the existing `isAnomalous`; the rate itself keeps rendering (demote+flag convention, and `test_audit_app.js` case 2's positive control depends on it) | 7.6 | SHIPPED (2026-07-28, one-commit PR on branch `claude/loop-165`, verifier PASS **9/9, tier HIGH** independently assigned — non-vacuity proved by the verifier itself: it deleted one `isAnomalous` gate, recompiled, watched `test_pool_detail_anomaly_projection.js` go red (`expected daily value "—", got: "$9,454.22"`), restored and confirmed the diff returned byte-exact. PROMOTED, not taken: no READY item was takeable (148 reads READY on `main` but is in flight as human-gated PR #306). New rendered test (9 assertions, real Chromium on the compiled bundle) asserts criterion 1 structurally — every `$`-number parsed out of `innerText`, none may be ≥$1e9 — so a *different* absurd number still fails it. Compiled+minified bundles regenerated (`home.html` loads those, not the source). No isolated funnel metric: pre-traffic quality fix per the 2026-07-23 mandate; proxy is `audit-app.js`'s `number-sanity` staying clean on anomalous `?pool=` surfaces. +playbook: derived-number-rails (Step 0c) | HIGH | specs/165.md | 1 | — |
| 167 | **The product audit renders ONE pool out of 740 — and it is the flagship.** `audit-app.js:88-91` hardcodes `PREFERRED_POOL_ID = '747c1d2a-…'` (Lido stETH) and `:1065-1068` feeds it to ALL five pool-detail surfaces (`pool-detail`, `-360`, `-dark`, `-ko`, + the dead-pool control), so daily coverage of the surface NORTH_STAR.md names as the north star is **1/740 = 0.14%**, on the single most blue-chip row in the dataset. This is verbatim item 154's finding ("audits ONE hand-picked page out of 2,167 — the one most certain to be fine"), fixed for static SEO pages by 154/157's prescan+promotion and never applied to the app's own surfaces. Proof it matters: EVERY pool-detail bug in this backlog's history was hand-found on a non-flagship pool — 122 (−900T stability score, balancer-v2), 144 + 145 (both pool `201e5f6e-cf75-4d0e-b07f-d58da3cee23a`), 165 (zeebu ZBU, $49-quintillion projection). Not one is Lido stETH, so the scanner could not have caught any of them. Live true positive the scanner has never rendered, re-derived from the committed snapshot this run: `201e5f6e-…` (balancer-v2) carries `apyMean30d` = **30,282.55%**, 30× `APY_SANITY_LIMIT`, while `apyBase+apyReward` breaches the rail on 0/740 pools — i.e. the one pool that can trip the P0 `number-sanity` check is precisely the one never rendered. Fix = 157's shipped prescan-then-promote pattern applied to the snapshot: `prescanPools()` + `buildPoolSurfaces()` (anchor unchanged, ≤2 promoted suspects, ≤2 seeded rotation), `AUDIT_POOL_*` kill switches, `poolPrescan` block in the findings JSON. Audit tooling only — no product code, no trust-rail edit. Promoted by the build loop 2026-07-28 (no READY items: 148 in flight as human-gated PR #306, 166 PARKED as open PR #322) | 7.2 | SHIPPED (2026-07-28, verifier PASS HIGH 9/9 — independently re-derived: its own full `node audit-app.js` run, its own 5-signal neuter cycle + whole-guard removal with byte-identical md5sum restores, its own true pre-167 baseline swap `git show fac2e30f2:audit-app.js` 26→29 surfaces 101s→100s, its own 6-file browser subset 6/6 + plain lane 34/34). Coverage 1/740 → anchor + ≤2 prescan-promoted suspects + ≤2 seeded rotation; `poolPrescan` block added to the findings JSON. The live suspect `201e5f6e-…` now renders and comes back CLEAN — first confirmation of item 144's `mean30dSane` gate (`PoolDetail.js:161-164`) against the real pool it was filed about rather than a synthetic injection. Verifier caveat carried forward as the natural next item: the pool driver still has no rail-relative percent check of its own (its magnitude check fires at 1e11, nowhere near 30,282), so the clean render proves the PRODUCT gate holds, not that the AUDIT could catch the class independently) | HIGH | specs/167.md | 1 | — |
| 166 | **P1 BUG (AI-discovery surface, north-star routing): the links in `llms.txt`/`llms-full.txt` do not go where they claim, and NOTHING links to pool-detail.** Verified on live prod by curl 2026-07-28, three classes, 32 links. (1) **15 pool rows point at the bare homepage** — `generate-llms.js:606` is `pool.url \|\| meta.baseUrl` and **no DefiLlama payload has a `url` field** (snapshot keys: `pool,chain,project,symbol,tvlUsd,apyBase,apyReward,apyMean30d,…`; live adds `apy`, not `url`), so the "fallback" is an unconditional branch firing on 100% of rows, always: `- uniswap-v3 · WETH-USDC — 95.55% APY, $110,825,218 TVL — https://www.defi.garden`. (2) **17 `?search=` links silently drop the query** — `search` is in neither `home.html:77` `ANALYTICS_PARAMS` nor `:78` `PLANNER_PARAMS` and `landing.js:52` reads only `lang`, so `?search=lido` lands on an empty search box; the routed param `?protocols=` already exists. (3) **8 top-yield rows are not pool-specific** (`?token=&chain=`), which is why two distinct Base uniswap-v3 WETH-USDC pools (95.5%/$110.8M and 31.7%/$10.2M) render as two rows on ONE URL. Through-line: `grep -c "?pool=" llms*.txt` → **0, 0** — the pool id is on every record and `/?pool=<id>` is the north-star surface (2026-07-23), and the AI-discovery surface has never linked to it. Fix: deep-link pool rows to `?pool=<id>`, swap `?search=`→`?protocols=`, regenerate. AI discovery is the only growth channel needing no human posting | 8.6 | IN_REVIEW→SHIPPED on merge (2026-07-28, ONE commit on branch `claude/loop-166-llms-links` + PR, merged by this run per the 2026-07-11 ship path). Verifier history, stated plainly because two of three attempts failed: attempt 1 FAIL on the missing HIGH-tier `specs/166-pr.md` merge-gate artifact; attempt 2 FAIL on this very row's earlier wording, which declared "SHIPPED / auto-merged" in the past tense while `git log origin/main..HEAD` was still empty; attempt 3 PASS **10/10, tier HIGH**. Product code was byte-identical across all three attempts (verifier confirmed mechanically against its own saved attempt-1 diff) — every FAIL was operator bookkeeping, never the build. TENSE CAVEAT, since it cannot be engineered away: the 2026-07-13 standing decision requires this row to ship in the SAME commit as the code, so any terminal status here is necessarily written before the merge it describes. The PR is the audit log — if the merge does not execute, the PR stays open and this row is wrong on its face — `generate-llms.js` gains an exported `poolUrl(pool, baseUrl)` deep-linking every pool row to `/?pool=<id>` (the north-star surface, which this file had NEVER linked to: `grep -c "?pool=" llms*.txt` → 0,0 before) with the `?token=&chain=` grid link surviving only as a genuine id-absent fallback; `pool.url || meta.baseUrl` deleted (no DefiLlama payload has a `url` field, so that "fallback" was an unconditional branch firing on 100% of rows for the life of the surface — 15 live rows pointing at the bare homepage); `?search=` → the already-routed `?protocols=` at all 3 emitters (17 links that silently dropped their query). Artifacts regenerated via the generator off a live 16,008-pool fetch, never hand-edited. Measured before/after: bare-homepage rows 15→0, `?search=` 7/10→0/0, `?pool=<uuid>` rows 0/0→8/15, unrouted URL params 17→0, class-3 URL collisions →0; line counts (76/5017), section headers, `Total URLs: 4913` and 112/112 sitemaps ALL unchanged — link targets corrected in place, nothing de-indexed (NEVER list clean, verifier independently concurred). 159's rails asserted intact (0 APY figures >1000, max 86.23%; `TVL ≥ $10M` claim; `APY_SANITY_LIMIT`/`MIN_TVL_USD` byte-unchanged). `home.html` router/`landing.js`/`app.js`/sitemaps/`og/`/`stories/`/token+chain generators/`translations.js` byte-untouched; no new dependency (package.json diff = the `test:serial` line only). New `test_llms_link_integrity.js` (24 assertions, both legs: live-payload-shaped fixtures + committed-artifact scans) wired after `test_llms_rails.js`; its router-param membership check PARSES `ANALYTICS_PARAMS`/`PLANNER_PARAMS` out of `home.html:77-78` rather than hardcoding a second copy — a copied list would be a fresh instance of the drift bug the test exists to catch. Non-vacuity proved 5×: builder reverted each of the 3 changes (3/3/6 distinct reds), verifier independently mutated 2 DIFFERENT sites (3 and 5 distinct reds) and confirmed byte-exact restoration both times. Verifier also ruled out the one way this fix could have swapped one broken link class for another: it simulated `app.js:1790-1804`'s match function over all 7 emitted slugs and confirmed `?protocols=` genuinely FILTERS (topProtocols is built from `pool.project`, the same string matched against), not merely routes. No isolable Mixpanel funnel metric TODAY and no new event by design — the destination is already instrumented (`pool_view{source=url_direct}`, item 020) and the north star `pool_click{garden_cta,protocol_link}` sits downstream; per the 07-27 takeaway no calendar window was opened on a zero-traffic funnel. Regression gate green: link_integrity 24/24, llms_rails 14/14, llms_freshness 8/8, llms_shared_source 12/12, validate-sitemaps 112/112 (full ~100-file chain NOT run — 5-min timebox, stated unrun not implied green). CI regenerates both artifacts daily, so the generator fix is what persists — both legs asserted. Needs no human visual spot-check (no render path). NOTE — ID COLLISION, human action may be wanted: open PR #322 (`claude/loop-166`, PARKED test-harness `--only` fix) carries a DIFFERENT item under this same id 166 and its own `specs/166.md`; this run therefore shipped on `claude/loop-166-llms-links` rather than clobbering that open PR's branch. #322 can no longer merge cleanly (its `specs/166.md` + BACKLOG row now conflict with main's). | HIGH | specs/166.md | 2 | traffic-gated: eval at `pool_view{url_direct}` ≥ 30 (today 1 all-time) |
| 168 | **P1 (positioning, AI-discovery surface): `llms.txt` describes DeFi Garden as a yield screener — the Garden Planner, the DEFAULT face, is absent.** `grep -icE "planner\|garden plan\|savings\|goal\|subscription\|forever number" llms.txt` → **0**. The homepage row reads *"TL;DR: Main dashboard for discovering DeFi yields across all chains and protocols."*; `plan.html` appears ONCE in `llms-full.txt` (line 2799) as a bare undescribed URL among 4,917. Against CLAUDE.md ("Garden Planner — the DEFAULT feature, bare `/`") and NORTH_STAR 2026-07-12 ("the planner's SUBSCRIPTION archetype / forever-number flip is the product's front door"), our own AI-discovery file teaches every LLM that reads it the one positioning NORTH_STAR says we are not — the degen screener, where DefiLlama already wins. Fix: a `## Garden Planner` section in both files (three archetypes named, routed `PLANNER_PARAMS` preset URLs, blended rate derived through the same rail-passing pool set so 159's rails are inherited, card mentions early-access-framed, no invented numbers). Ships after 166 | 7.4 | SHIPPED | HIGH | specs/168.md | 1 | traffic-gated: eval at planner `page_view` ≥ 30/14d (today 1/7d) |
| 169 | **P2: 160's text-surface scanner has no link signal — it scored `suspectCount: 0` on the two files carrying 32 mis-targeted links.** `prescanTextSurfaces()`'s signal set is `apy-rail-breach`, `broken-number-literal`, `tvl-floor-claim`, `empty-surface` — every one a number-or-emptiness check, because 160 was specced from 159, which was a number bug. **Third instance of one pattern (148 → 159 → 166): the checker's signals are drawn from the last bug found, so the next bug always lands in the class nobody has been bitten by yet** (the same root cause item 167 fixed for pool *targets* — this is its text-surface twin, for *claims*). Add a `link-target-integrity` signal: for every URL a generated text surface emits, assert (a) the query key is a member of `ANALYTICS_PARAMS ∪ PLANNER_PARAMS ∪ {lang}` read out of `home.html:77-78` (not a hardcoded second copy), (b) a row describing a specific pool does not link to the bare origin, (c) two rows stating different figures do not share one URL. Cheap — pure fs+regex, no render, same shape as 157's prescan. Ships after 166 so it has a true negative to prove itself against, and must be shown to go RED on the pre-166 artifacts | 5.4 | IN_REVIEW→SHIPPED on merge (2026-07-28, ONE commit on branch `claude/loop-169` + PR, merged by this run per the 2026-07-11 ship path). Verifier PASS **12/12, tier HIGH independently assigned** (525 insertions vs the 150-line LOW cap — the 154/157/163/166 precedent; the row's LOW guess is superseded), then a SECOND PASS on a post-verdict delta. Every claim re-derived by execution, not read from notes: the verifier ran its own `git show 3935e8d05:` reconstruction of the pre-166 artifacts (ground truth reproduced exactly — `llms.txt` 7 unrouted `search=` / 0 bare-origin / 1 shared URL with 2 figure sets; `llms-full.txt` 10 / 15 / 15), spot-checked the committed fixtures byte-for-byte against those bytes, confirmed on a `git stash`'d pre-169 tree that the OLD signal set scores **zero** on both files (the spec's central evidence claim), and ran four of its own neuter/restore cycles with mutation code different from the builder's. **The verifier found a real gap after passing** — rule (c) `break`ed at the first conflicting-URL group, so a file with two reported one with no count and no `+N more`; fixed in-item (collect all groups, deterministic worst-pick, true total in the detail) rather than deferred, since shipping a gate with an un-exercised blind spot reproduces this item's own thesis one level down. Ground truth on both real historical files unchanged (1 conflicting URL each) — the gap was aggregation correctness, not an undercount. True negative on today's committed `llms.txt`/`llms-full.txt`: **0** suspects. No isolable funnel metric (loop tooling under the 2026-07-23 pre-traffic mandate; 142/149/154/155/160 disclosure pattern). Browser lane (~63 files) NOT run — timebox, stated unrun rather than implied green | HIGH (verifier-assigned; row guessed LOW) | specs/169.md | 1 | — |
| 170 | **The browser lane — the rendered half of the merge gate — is structurally unrunnable, so it has not run on any shipped item for a week.** `run-tests.js:200` forced `jobs=1` on the lane (`// forced — fixed-port real-Chromium files must serialize.`) and silently discarded `--jobs`; the lane holds **64 of 100** files at ~30s each (`test_default_sort.js` measured 28.3s this run), so it is ~30min serial — worse in practice (`specs/163-notes.md:180`: `test_smoke.js` blows a 120s cap, `test_search.js` needs ~550s). Five consecutive runs (162/163/165/166/169) recorded *"Browser lane NOT run — 5-minute timebox"* in LOG.md and shipped on a hand-picked `--only` subset: each item's own surface checked, nothing checking that item N didn't break item N-30's. The stated blocker is real — port literals collide across files (7 files on 8796, 5 on 8799, 5 on 8795, 4 on 8819…), and two on one port means EADDRINUSE or a green run against the *other* file's server. Fix = don't rewrite 59 test files; make the runner conflict-aware (static port extraction → per-file conflict keys + a shared-resource key for the two files writing the committed `audit-findings.json`; unknown-port files exclusive; scheduler starts only key-disjoint files). Promoted by the build loop 2026-07-28 (no takeable READY item: 148 reads READY on `main` but has been human-gated PR #306 since 07-26, and the 07-28 report's build order 166→168→169 all shipped) | 7.0 | SHIPPED (2026-07-28, ONE commit on `claude/loop-170` + PR, merged by this run per the 2026-07-11 ship path after verifier **PASS 7/7, tier HIGH independently assigned** — the verifier proved non-vacuity itself with THREE mutants in a scratch mirror: killing `keysConflict` → RED, killing the exclusive branch → RED, and collapsing `while (activeCount < workerCount)` → `< 1` (silently serialise everything) → RED on exactly the load-bearing line *"a non-conflicting pair must overlap when jobs > 1"*; mirror restored byte-exact, repo untouched. It also independently reproduced the concurrent run at `1m26.898s` 4/4 vs a 144.75s per-file sum, walked every browser file's transitive require graph to confirm the port-scan has **no** false-negative surface today (exactly 8 files reach a listening helper — `audit-app.js`'s lowercase `port` at :1499 — and those 8 are precisely the already-exclusive set), and confirmed the `--json` shape and the 16 pre-170 assertions are unchanged). Measured: 4 browser files spanning 3 conflict groups incl. the real shared-port 8799 pair — `--jobs=1` **142.3s** → default `--jobs=3` **87.6s**, 4/4 pass both. `--lane=plain` 36/36; `test_run_tests.js` 26/26 (was 16), 5 consecutive runs, no flakiness. Scope = `run-tests.js` + `test_run_tests.js` ONLY; trust rails / `home.html` router / `?token=/?chain=/?pool=` / generated SEO surface / `package.json` deps byte-untouched (verified by `--name-only` + `-S` searches on the rail constants, not by assertion); not on the NEVER list. THREE VERIFIER-FOUND DEFECTS, all in the notes, all corrected before the commit rather than after: baseline assertion count was 17, is 16; the plain-lane path is *behaviourally*, not byte-for-byte, identical (`workerCount` hoisted, `runFile` aliased); and exclusive files are **deferred, not drained-to** — an exclusive entry at list position 2 of 8 starts last, which keeps the safety invariant but makes the inline comment `// must drain first.` inaccurate. That third one is a real semantic gap and was DISCLOSED, not patched post-verdict (editing code after PASS would invalidate the verified diff) — ticketed in `170-notes.md`. HONEST LIMIT: the full 64-file lane was **NOT run** (it exceeds the timebox — the very problem this shrinks rather than closes; Amdahl floor is `test_search.js`'s ~550s), and the first 8-file measurement set was killed at the 290s cap and is recorded as truncated with 3 files' status unknown. No isolable Mixpanel funnel metric (loop tooling under the 2026-07-23 pre-traffic quality mandate, 154/163/166/169 disclosure pattern); the honest proxy is whether the NEXT run can state a browser-lane result instead of "NOT run". needs human visual spot-check: NO (no render path touched) | HIGH | specs/170.md | 1 | — |
| 171 | **The product audit reports a blocking P0 for a defect its own rendered check disproved — and always will.** `node audit-app.js` on `main` (2026-07-29, clean `npm ci`) → `6 total, 6 blocking, exit 1`. Five are item 148's junk slugs (real, human-gated PR #306). The sixth, `pool-prescan:mean30d-rail-breach` **P0** on `201e5f6e (balancer-v2/WSTETH-AAVE)` `apyMean30d = 27421.09636`, is a false alarm: the same run promoted that pool, rendered it as `pool-detail:201e5f6e`, ran the full pool battery, and returned **zero findings** — because item 144's `mean30dSane` (`PoolDetail.js:161-164`) already gates all five `apyMean30d` render sites (`:1298/:1384/:1412/:1460/:1506`, the only ones in the product per `grep -n apyMean30d`). The aggregate finding is emitted pre-render (`audit-app.js:690`/`:926`) and never revisited, so promotion's answer is computed and discarded. Structurally unclearable: upstream data regenerates daily, 144's gate keeps correctly suppressing it — so `blocking > 0` becomes a constant and a genuinely new P0 lands invisibly. `ci-signal-honesty` class, inverted (misleading RED). Fix = reconcile aggregate prescan findings against their own promoted surfaces; downgrade to non-blocking `P2` ONLY when every suspect was promoted, covered, and clean — unverified is not clean, which is what keeps 148 at P1 | 6.2 | SHIPPED (2026-07-29, verifier PASS 10/10 tier HIGH, one commit + PR merged by the run) — promoted by the build loop: NO takeable READY item existed (148 reads READY on `main` but is in flight as human-gated PR #306, ninth consecutive run). Measured: blocking 6 → 5, exit stays 1 (148 correctly still red). No isolable funnel metric (loop quality instrumentation, pre-traffic mandate); honest proxy = the next tick's blocking count reflects only real defects | HIGH | specs/171.md | 1 | — |
| 172 | **The static-page prescan — the checker over all 2,183 generated HTML SEO pages — has never looked at a link.** `PRESCAN_SIGNALS` (`audit-app.js:175`) is `junk-slug`/`zero-yield-claim`/`broken-number-literal`/`absurd-magnitude`: every signal a number-or-slug check. Measured 2026-07-29 on this checkout: those pages emit ~41,300 same-origin links, **4,989 of them `/?pool=<id>`** — the hop SEO traffic must make to reach the north-star CTAs — and no gate has ever asserted any of them addresses a pool. Item 169 shipped `link-target-integrity` for the two generated TEXT surfaces and explicitly deferred HTML (`specs/169.md:41`, "one surface family per item"); this is that follow-on, on a surface 1,000× larger by file count. Fourth instance of the documented pattern 148 → 159 → 166 (`playbooks/product-audit.md:219-224`): a checker's signals are drawn from the last bug, so the next bug lands in the class nobody has been bitten by yet | 5.6 | SHIPPED on merge (2026-07-29, ONE commit on `claude/loop-172` + PR, merged by the run after verifier PASS **15/15 tier HIGH** — independently re-derived, incl. non-vacuity proven on REAL `tokens/usdc.html` bytes broken three ways rather than on fixtures, four verifier mutations different in kind from the builder's, and byte-identical md5 restores). PROMOTED by the build loop: no takeable READY item existed (148 reads READY on `main` but is in flight as human-gated PR #306, tenth consecutive run). Ships a GATE, not a repair — all three rules green on today's surface (0 suspects), so the true negative was proven non-vacuous rather than reported as health. Out of scope by design: `?pool=` liveness (needs network; measured 15/4,989 dead today, and the `data/pools-snapshot.json` shortcut false-positives 4,233/4,989 on the $10M-vs-$100K floor mismatch). No isolable funnel metric (loop tooling, pre-traffic mandate); observable at the next heartbeat as `prescan.bySignal['link-target-integrity']` | HIGH | specs/172.md | 1 | — |
| 173 | **P1 BUG (SEO surface, the entire static lander estate): 1,749 of our 2,200 indexed SEO pages have a primary CTA that lands on an EMPTY grid.** Measured 2026-07-29 against **live DefiLlama (16,038 pools)**, not the checkout snapshot: `chains/*.html` 48 of 88 dead (54.5%), `tokens/*.html` 1,701 of 2,112 (80.5%), **total 1,749 of 2,200 = 79.5%**. `chains/cardano.html` says verbatim *"**33 pools** across 31 tokens clear DeFi Garden's $100K TVL floor"* and its **"See live pools on Cardano →"** button goes to `?chain=Cardano`, which returns **0**. Root cause is a floor mismatch between a page and its own link: `generate-token-pages.js:60` (`MIN_POOL_TVL = 100000`) and `generate-chain-pages.js:65` build at **$100K** while `app.js:801` (`DEFAULT_MIN_TVL = 10000000`) filters the CTA's target at **$10M** — 100×. Concentrated in the long tail SEO exists to serve: pages that took prod traffic this week resolve to `/chains/linea` **1** pool, `/chains/aptos` **1**, `/chains/cardano` **0**. **Fix proven by measurement, not argued:** `&minTvl=100000` (already honoured by `app.js:927` — *"Respect explicit minTvl=0; fall back to DEFAULT_MIN_TVL only when param is absent"*) makes Cardano return exactly the **33** the page claims. NOT the NEVER list — `DEFAULT_MIN_TVL` byte-untouched, no rail weakened, nothing de-indexed; it is the unblocked slice of BLOCKED item **133** (133 = change app discovery behaviour at the floor, human-gated; 173 = stop our own pages lying about where they lead) | 9.0 | SHIPPED (2026-07-29, ONE commit + PR on the harness-pinned branch, merged after verifier **PASS 9/9 tier HIGH** on attempt 2 — attempt 1 FAILed on an undisclosed `package.json` edit, notes corrected, zero code change between attempts) — **2,200 dead CTAs → 0** (`test_seo_cta_targets.js`, red→green reproduced by the verifier in a clean `origin/main` worktree AND mutation-tested); page prose now equals what the link delivers (Cardano 33=33, Celo 15=15, 1INCH 17=17, BUIDL 9=9); rendered contrast proven in real Chromium; rails byte-untouched, nothing de-indexed (24 slugs dropped / 41 added = net +17, all 24 independently traced to genuine sub-floor churn) | HIGH | specs/173.md | 1 | traffic-gated: ≥30 `page_view` on `/tokens/*`+`/chains/*` |
| 174 | **P1 BUG (trust rails, honesty — 159's defect one surface over and 1,100× the page count): all 2,200 SEO pages publish a "DeFi Garden trust filter" that is 100× off.** Verbatim on every `chains/*.html` + `tokens/*.html`, under the FAQ heading *"Are these rates safe?"*: *"Every rate shown passes **DeFi Garden's trust filters — a $100K minimum TVL** and exclusion of anomalous (>1000% APY) pools."* The product's floor is **$10M** (`app.js:801`; CLAUDE.md verbatim *"DEFAULT_MIN_TVL = $10M everywhere"*). The claim is not scoped to the page — it asserts a property of the product's safety filter, so every reader (human or LLM) is told our floor is 1/100th of what it is. This is item **159** exactly (a generated surface publishing a TVL-floor claim contradicting the product's) — 159 fixed 2 files; nobody re-ran the check on the other 2,200 pages. **Same-fix aggravators:** (a) **1,713 pages state a "forever number" blended from sub-floor pools** — Cardano: *"Idle assets could earn ~6.98% — park $3.4K and it could run a $20/mo Claude Pro subscription, forever"*, a blend including a $715.9K pool, while NORTH_STAR requires forever numbers to come from the live blended rate through the rails (44/76 chain and 1,669/2,036 token pages carrying the claim are pages whose own CTA returns zero); (b) **916 `0.00%` APY rows across 462 pages** listed as yields (the 032/033 class). Rails byte-untouched; EN+KO together | 8.2 | SHIPPED on merge (2026-07-29, ONE commit on `claude/loop-174` + PR, merged by the run after verifier PASS) — **2,234 pages claiming "DeFi Garden's trust filters — a $100K minimum TVL" → 0**; 13 `tcp*` keys (EN+KO) now interpolate `formatUsd(MIN_POOL_TVL)` with zero re-typed literals (mutated-constant test, red→green); **902 `0.00%` rows / 457 pages → 0/0**; forever numbers blend only rail-passing, visibly-non-zero pools. Generated page SET proven identical (old vs new code on one fixture: 2,099 token slugs and 89 chain files, `diff` empty). Regenerated `tokens/`+`chains/`+KO+sitemaps+`llms*`. `app.js`/`planner.js` byte-identical. OPEN FOR THE HUMAN: should this surface exist at $100K at all, or be rebuilt at the app's $10M? (interacts with PR #332/item 177) | HIGH | specs/174.md | 1 | traffic-gated, shared with 173 |
| 175 | **P2: the link checks are blind to the biggest link defect — 4th consecutive instance of one pattern.** `prescanStaticPages()` scanned 2,200 pages this tick and returned 7 suspects, all `junk-slug`; **172's `link-target-integrity` signal, shipped this morning, scored 0 on the 2,200 pages carrying 173's 1,749 dead CTAs.** Not 172's bug: it was specced from 166, where "broken link" meant *an unrouted param*, so it checks param membership — and `?chain=Cardano` is perfectly routed, it just returns nothing. **148 → 159 → 166 → 173 is four in a row of the checker's signals coming from the last bug.** The generalisation this item encodes: a link check has three levels — (1) **routed** (172 built it), (2) **resolvable** — the value names a real pool id / project slug / preset key (done by hand this tick: 40 links, 0 misses; nothing automates it), (3) **non-empty** — the target under its own default filters returns what the page claims (nothing checks it; all 1,749 live here). Level 3 is the only one that catches two internally-consistent surfaces with a broken contract between them. **Land 173 first** — 175 on the pre-173 corpus correctly emits ~1,749 findings and holds the audit red. Decision rule deliberately tests the strategy, not just the code: if the next link-class bug is again found by hand first, that is evidence extending signals does not break the pattern | 5.6 | READY | HIGH | specs/175.md | 0 | 14d from ship |
| 176 | **The browser lane — the rendered half of the merge gate — is 8-red on `main` right now, and four of the seven guard the card funnel.** Baselined this tick per `playbooks/pre-existing-red-triage.md` in an isolated `git worktree` at `origin/main` (not assumed): `TOTAL pass=0 fail=7` for the first seven and exit 1 for `test_footer_hub_links` run singly, identical set to the working tree, so none is caused by item 148's diff. Red on `main`: `test_snapshot_first.js`, `test_analytics_fires.js`, `test_spotlight_attribution.js`, the four waitlist tests `test_waitlist_{seo_entry,funnel,pitch,microcopy}.js`, and `test_footer_hub_links.js`. The visible class is copy that changed without its tests: `test_waitlist_microcopy` expects *"Free to join • Card doesn't exist yet • We'll email you when it does"* and the page renders *"No wallet needed to explore"* — same mismatch in KO (*"무료 가입 • 카드는 아직 없어요…"* vs *"지갑 없이도 둘러볼 수 있어요"*). **Why this outranks its size:** item 170 was shipped 07-28 specifically to make this lane runnable, and the first full run after it is 7-red — so the gate that is supposed to catch render regressions has been reporting nothing useful, and the four waitlist failures sit exactly on `waitlist_submitted`, the north star's downstream input metric (NORTH_STAR line 19). Either the copy regressed (a product bug on the card funnel) or the tests rotted (a gate bug) — **decide which per test before touching either**, and do not update a test to match the page without first checking the page is right; that converts a product bug into a green build. Sequence: this ranks above 174/175, because a red gate makes every later verdict weaker | 7.6 | SHIPPED on merge (2026-07-29, ONE commit on `claude/loop-176` + PR, merged by the run after verifier PASS) — **8 red → 8 green, 4 distinct causes, 0 product bugs, 0 product-file bytes changed** (all 7 product files md5-identical to `origin/main`). Every red classified BEFORE it was touched, per `playbooks/pre-existing-red-triage.md`: **class 1** (4 files) `window.mixpanel` stub queue structurally empty on localhost since spec 096's host gate (`analytics.js:96`) — repaired by overriding `Analytics.isProductionHost` only, so every assertion keeps its end-to-end meaning (the `Analytics.track`-spy alternative was rejected for weakening them); **class 2** `test_waitlist_microcopy.js` growth cases stale since item 139's archetype-aware CTA — repointed AND the 070 "card doesn't exist yet" honesty coverage re-homed EN+KO through the secondary link into the modal, after verifying the disclosure still reaches the user; **class 3** (2 files) the 2026-07-15 landing pivot, item 156's class in the two files 156 didn't reach; **class 4** `test_analytics_fires.js` blocked hosts hanging `waitUntil:'load'` (16.9s fail → 1.8s green). Assertion counts never dropped: microcopy 6/7→8/14, snapshot_first 8/17→9/19, footer_hub_links 5/23→6/27. Two genuine product findings surfaced by a builder refusing to adjust a red expectation → filed as **179**, not silenced. Full 64-file browser lane NOT run (5-min timebox) — stated unrun, not implied green | HIGH | specs/176.md | 1 | n/a — gate-integrity item, no traffic window |
| 179 | **Two real product findings surfaced by item 176's browser-lane triage, neither fixed there (out of its scope, filed rather than silenced).** (a) **The static `.seo-hub-links` crawler footer is occluded and unclickable on bare `/`.** Since the 2026-07-15 landing pivot, landing mode renders its OWN `<footer class="app-footer">` (`landing.js:356-367`) carrying the same `/tokens` + `/chains` anchors; `style.css:2515` makes `.app-footer` `position:fixed; bottom:0; z-index:100`, and it occupies the last ~69-84px of the viewport at EVERY scroll offset, with `.seo-hub-links` (the last in-flow element) always inside that band — measured, no scroll position unhides it. `style.css:2589` hides the static block under `html[data-app-mode="analytics"]` only, so the exclusion that exists precisely for "the app renders its own footer" was never extended to landing mode. Net effect: two copies of the same links in the DOM on the default route, one of them dead. Question for the human (a product call, not a loop call): extend the hide rule to landing mode, or keep both for the crawler surface and accept the duplication? Note the crawler purpose is already satisfied — the anchors are in `home.html`'s served source (asserted by `test_footer_hub_links.js` case 1, and now by a DOM-presence case too). (b) **`/plan.html` has NO hub-link surface at all** — `grep -c seo-hub-links plan.html` = **0**; it is a separate 102-line static file not served through `home.html`, with no `data-app-mode` and no `.app-footer` (`grep -c "app-footer\|seo-hub-links" planner.js` = 0). So the planner's own production URL — one of the two faces of the product — carries none of spec 045/086's internal-linking surface. This is a genuine SEO gap, not a stale test: item 176 briefed a `/plan.html` test case on the assumption the surface existed, the builder found it red, refused to adjust the expectation, and the case was deleted rather than shipped as a permanently-red gate | 5.4 | READY | LOW | — | 0 | 14d from ship |
