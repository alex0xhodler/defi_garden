# Heartbeat report — 2026-07-10 (dry-run; updated after human session)

1. North star (viral loop closures): **0**. Baseline stands.
2. ANSWERED (human): tracking went live 2026-07-09 → all baselines date from there; prior zeros = no tracking, not no usage.
3. ANSWERED (human): plan_saved 2× was a bug → root-caused and FIXED in-session (effect re-fired on rate churn; now deduped via planSavedSignature, planner.js + 5 new tests, 190 assertions green). Post-deploy, expect plan_saved ≈ plan_created + real edits.
4. 002 DONE: share instrumentation audited SOUND end-to-end (CTA→track→encode→router→decode→track, see specs/002-notes.md). Zero shares = usage gap → new item 004.
5. Current week: 167 sessions, 5 plan_created, 0 share events, 0 errors. Guardrails quiet.
6. Top 3 READY: 001 wire Mixpanel MCP (9.0) · 004 surface share CTA at bloom (8.0) · 003 npm-test smoke gate (7.0).
7. No experiments in measurement windows.

QUESTIONS FOR THE HUMAN:
- Q-c: `plan_created` (and `plan_saved`) re-fire every time a saved plan's report page is revisited (component refs reset on remount). Should "reopened my own plan" count as plan_created? Recommendation: no — guard creation to the convo→report / shared-adoption transition. This inflates the north-star numerator's input, so it's worth deciding before the first real closures arrive.
