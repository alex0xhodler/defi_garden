# Heartbeat report — 2026-07-12

1. **North star (viral loop closures): 0.** `share_link_created`/`share_link_opened` are still absent from the Mixpanel lexicon (event never received) → structurally 0. 005/007/008/024 measuring share paths through 2026-07-24/25; no closure yet, insufficient sample.
2. **Signal (live Mixpanel, 14d):** session_start 326, page_view 330 (`/`=224, analytics≈106, `/tokens/*`=0, `/pool/*`=0), search_success 100, search_abandonment 8, plan_created 6 (daily 5→1→0), plan_saved 12, pool_view 3, error_occurred 0 (guardrail quiet). Totals rose vs 07-11 mostly from the 14d window now fully covering the 07-09 launch spike (167 sessions); run-rate is steady ~80 sessions/day, single active week — no trend/retention yet.
3. **020 CONFIRMED LIVE (keystone gap #1 closed).** `search_success.results_count` is now real: 22/100 searches carry non-zero counts (1–34 pools found) vs 50/50 hardcoded-0 last snapshot. When instrumented, searches DO surface pools — partial honest read for 018. The 78 zeros mix pre-020 legacy + genuine no-results (indistinguishable); need ≥30 clean post-020 searches to quote a find-rate. `pool_view` also fires (n=3).
4. **New keystone gap — the SEO surface is invisible, and we can't tell "untrafficked" from "unwired."** 2,045 instrumented `/tokens/*` pages (039) show 0 page_views; `?pool=` SEO landings show 0 `pool_view source=url_direct` (020). Expected null (GSC indexing is a 2–6wk cycle just begun) — BUT both were verifier-PASS on fixture-strings only; live-page firing was never observed in-sandbox. → **new item 044 (LOW, 7.0, spec written):** one headless render each to separate "no traffic yet" from "silently not firing." 017's post-mortem (fixtures passed, product failed) is the precedent.
5. **pool engagement near-zero:** 100 searches → 3 card-click pool_views (~3%); 0 `?pool=` landings. Q-d treated as ANSWERED by the 2026-07-11 standing decision (SEO landers → pool-detail = the conversion path; search app = acquisition) — not re-asking.
6. **Top 3 backlog:** 044 (7.0, READY — the only buildable READY item) · 041 chain landing pages (7.5, unspec'd) · 043 semantic clustering (6.0, unspec'd). Everything 001–042 is DONE/SHIPPED; 026 remains BLOCKED (needs GSC connector wired into the heartbeat routine — human/operator config, not repo code).
7. **Experiments:** none past window (005/007/008→07-24; 013/014/018/019/020/024/025/039→07-25). Nothing closed to LEARNINGS.md. Retention cohorts not computable (single active week).

QUESTION FOR THE HUMAN:
- Q-e: The entire SEO theme's payoff (039/014/021/023/028-033 — thousands of `/tokens/` pages) is unmeasurable right now because organic-traffic reads live in GSC, and the GSC connector isn't wired (026 is BLOCKED on your config). Should we (a) prioritize wiring the GSC connector so the SEO investment becomes readable before building more, or (b) keep shipping net-new SEO surface (041 chains, 043 clustering) on faith through the 2–6wk indexing cycle? This decides whether the next build cycles go to measurement-plumbing or more pages.
