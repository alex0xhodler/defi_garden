# Report — 2026-08-12 (heartbeat, triage only, no product code)

- **THE WORKER IS DEPLOYED — the whole agent pillar is live.** Live-probed end-to-end this tick:
  `/api/health` 200 (7,325 railed pools, rails correct), `/api/pools` railed, `/api/forever-number`
  computes from the live blended rate (2.75% → $8,737 forever number for $20/mo), `/mcp` answers
  JSON-RPC initialize/tools-list/tools-call with railed data, x402 correctly DARK (`enabled: false`,
  paid route serves free, nothing charging). llms.txt still serves the corrected $100K rails.
- **But the front door 404s: item 265 is now the top actionable item (score 7.8→8.9).** All three
  `.well-known` MCP discovery cards — advertised via `Link:` header on EVERY response — point agents
  at `sse` on `/api/mcp`, which returns 404 on both GET and POST; the real server is Streamable HTTP
  at `/mcp`. Every agent that trusts the card fails to install. Spec written (specs/265.md). Related:
  live `openapi.json` still declares the wrong path set (262, BLOCKED on you), and llms.txt mentions
  the API/MCP **zero** times — filed as **277** (specs/277.md), the 228 advertising leg whose
  "would 404" deferral expired today.
- **Leg (A) instrumentation is now a READOUT gap only**: the D1 `agent_reads` log has been recording
  since the deploy, but reading it needs `wrangler` + Cloudflare credentials (DEPLOY.md §6) — no
  cloud session has them. Gate 251: deploy half MET, ≥1-row-observed half pending your first read.
- **You swept the PR pile 2026-08-11T20:07** — #419, #429, #434, #436 closed unmerged (yesterday's
  disposition questions answered by action). Open PRs 4→1: **#424** (236 phase 1) alone, awaiting a
  verifier verdict, ~36h old — crosses the 48h line tomorrow ~19:22 UTC.
- **Audit: 84 surfaces, 3 findings, one CLOSED, no new classes** — `i18n:en-ko-parity` 2→**0** of
  554 (248 merged 05:27 today, allowlist 24→26); the two P0 prescan rail-breaches are flat churn
  (22 and 32 of 7,346); planner-768 P2 = item 235 (READY). NORTH_STAR test legs all exit 0 (scope:
  4 files + audit's implemented classes over 84 surfaces; 150-file lane not run).
- **Traffic:** 08-11 = 9 sessions, 9 crawler-shaped, 0 insider, 0 real (9/9 undefined referrer, 6/9
  the Mac-Safari cluster, zero engagement events); first `/?pool=` fuzz-free day in ≥7. Leg (B)
  0 real, **35th day**; leg (A) Mixpanel-read 0, day 8 (new referrer: your own www.0xhodler.nl ×1).
  Guardrail: `error_occurred` absent 14d → **30th** clean prod day (real-exercise n=16, <30 — not
  evidence for strangers).
- `vitals | ships/wk: 23 | tokens/ship: ? | attempts: 2 | open PRs: 1 (oldest: 2) | product:process: 20:3`
  (tokens median uncomputable — only 6/23 LOG lines carry counts; no breach: parked 0, process 13%).
- **Top 3 by score:** 236 (9.2 — phase 1 in #424, needs its verdict), **265 (8.9 — the live 404
  pointer, spec ready)**, 266 (8.4 — WebMCP hardcoded rails, spec written, both old parked PRs
  closed so it needs a fresh build). Then 275 (8.0, design-gated) and 277 (7.8, after 265).
- Ledger: row 274 synced IN_REVIEW→SHIPPED (#439); 275's spec cell fixed to specs/275.md; new row
  277 (id computed, not guessed); BACKLOG malformed rows 23→24 (named trend, third tick, under the
  process-cap bar). No experiment window closed or closeable (117.x backstops ~09-16..18).
  +playbook: agent-surface-deploy-verification (the probe checklist this tick derived).

## Questions for the human

1. **One `wrangler d1 execute` read now lights up leg (A)'s first real number** (DEPLOY.md §6, the
   `DAILY_READS_QUERY`). The log has been recording since your deploy; this tick's probes alone
   guarantee rows. That read (a) opens gate 251, (b) tells us whether the logger actually writes
   (fail-open means a silent logging bug looks identical to zero traffic until someone reads), and
   (c) starts 228's ≥30-invocation window count. Paste the output anywhere the loop can see it, or
   run it on a schedule if that's easy — the heartbeat will consume snapshots.
2. **#424 (236 phase 1: one header band + shared content width) needs a verifier verdict or a tag**
   before tomorrow evening's 48h line — a build-run verdict is fine; flagging so it doesn't rot the
   way #399 did.
3. **262 remains BLOCKED on one word from you** — live `openapi.json` now describes a LIVE API and
   declares `POST /planner`, which the Worker doesn't implement: deprecate it in place, or remove it
   from the contract? (One word decides the fix; the drift is now agent-visible in production.)

*Process note: no product code written this tick — triage, live-probe verification, spec-writing
(265/266/277) and ledger updates only; nothing met the bar for dispatching a coding agent inside a
triage-only routine. `.claude/agents/verifier.md` refreshed from the kit at run start.*
