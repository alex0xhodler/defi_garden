# Report — 2026-08-05 (heartbeat, triage only)

- **Metric state:** north star leg (B) `pool_click{garden_cta|protocol_link}` = **0**, 28th day; leg (A) = **0 new** AI-referred sessions (14d perplexity pair = the same 07-26 baseline pair). Prod 14d: 162 sessions / control 168. `error_occurred` absent from both result sets over the full 14d — **23rd clean prod day** (control 7th). Insufficient data (<30 real events) for every funnel claim.
- **08-03's burst is over and 223 WORKS:** 08-04 = 8 sessions, of which 6 are crawler REPLAYS of the pre-223 scraped fuzz URLs (`` /?token=USDC` `` ×3, `/?pool=` ×3); all five formerly-200 repo files now curl-verified **404** on prod. Classified residual replay, decay prediction filed in weakest form, playbook updated (+traffic-quality-classification). Not re-ticketed.
- **Audit (fresh, 83 surfaces, this tick's `generatedAt`):** static prescan 4,332 pages / 0 suspects on its 6 signals — 226's regenerated estate is prescan-clean. Pool prescan 3,963 union pools: 12 mean30d suspects (churn from 14) + **first-ever `apy-rail-breach`** (GRVT-USDT, live APY 1,043.19% vs the 1,000% rail, $93K TVL — record-level, rotating to render). 2 promoted pools rendered clean. Occlusion: 6 findings, all item-221's `.app-footer` class. **222's fix verified** (duplicate theme-toggle P0 gone). `dead-pool` no-finding NOT claimed clean (231: lens 3/20 there).
- **Ships verified live on prod this tick:** 226 head-curation (token sitemap = 130 `<loc>`s, lastmod 08-05, IndexNow/llms estate intact per its own tests) and 223 (above).
- **Backlog:** +**232** (7.8, READY, spec) — depth work for the 130 head pages, the class 226's close-out left open with a number ("0 of them have received depth work"); Q3b's own "width to depth" directive. Spec also written for **231** (8.5, top takeable READY — occlusion-lens 3/20 detection reliability). Statuses synced to main: 224 PARKED / 225 IN_REVIEW / 221 PARKED (rows read stale READY because the changes live in unmerged PRs).
- **Experiments:** none closed — none CAN close: no traffic gate has opened (real pool-detail events 14d = 3, need ≥30). 117.1/.2/.3's legacy calendar dates hit today; they stay SHIPPED (measuring) under the 60d backstop (~09-16..18). GATED re-check: 229 visuals stays GATED (225 unmerged).
- **Top 3 takeable next:** 231 (8.5) · 232 (7.8) · 229 targeting leg (6.5) — everything scored higher is human-gated or dep-blocked (next line).

## QUESTIONS FOR THE HUMAN (1 — three PR calls, all yours, all blocking the agent-first chain)
The program you set 08-04 is stalled at its root on three open PRs only you can move: **#392 (224 edge telemetry — PARKED at the 3-attempt budget, your call to merge; 227 API and 228 MCP are dep-blocked behind it, and north-star leg (A) stays instrument-blind until it deploys)** · **#393 (225 design reset — awaiting YOUR pre-merge screenshot review, the gate you set in Q2b)** · **#386 (221 — PARKED, verifier FAIL ×3, do not merge as-is; today's audit reproduced its class on 4 surfaces)**. Older human-gated PRs also still open: #332, #331, #322, #316, #309.

<!-- Process narration in LOG.md; full numbers in signals/2026-08-05.md. -->
