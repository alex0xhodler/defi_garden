# Report — 2026-08-01 (heartbeat, triage only)

- **Metric state:** north star `pool_click{garden_cta|protocol_link}` = **0**, 24th straight day. Prod 14d: **86 sessions** (unfiltered control 92), `error_occurred` **0 — 19 clean days**, clean in the control too. Funnel 30d **unchanged for a second day**: `plan_saved` 5 · `search_success` 4 · `plan_created` 3 · `share_link_opened` 1 · waitlist/share_created/tend 0. `src` = `undefined` × 47, **20th day of zero tagged arrivals**. Prod `page_view` 7d = 11, the **identical URL set** as yesterday — no new crawler arrivals. **Insufficient data** for all funnel claims.
- **Second consecutive zero-blocking audit tick, on double the render budget:** 60 surfaces (29 → 60, item 192's widening landed as specced), 1 finding, **0 P0/P1** — the known `201e5f6e`, auto-downgraded by 171 for a 4th day. Static prescan clean 3rd day (2,186/0); text surfaces 2/0; i18n 543/0. **184's `?pool=` liveness gate had its first perfectly clean run: 3,696 ids, 0 dead** (was 1/3,707). Overnight ships **190, 192, 195, 196** all verified live against the shipped code. No build loop lapped this tick — the audit describes current `main` (`44f1c4013`).
- **197 (6.0, READY) — the audit has never enumerated half the estate it audits.** `listLeafPages()` is only ever called with `'tokens'` and `'chains'`, at both collection points. The **2,186-page `/ko/` estate** — byte-for-byte the same size as EN, **2,215 `<loc>`s submitted to Google** — is unreachable by every signal, cheap and rendered. Proof is the covered-surface list itself: 60 surfaces, 7 static pages, **zero containing `/ko/`**. The three `-ko` surfaces that do appear are app routes via `?lang=ko` — which is exactly why the gap read as covered for 13 items.
- **Sized honestly:** I ran the real prescan over all 2,186 KO pages → **0 suspects**, and the KO half emits identical link/pool-id counts, so 184's verdict transfers. **This is coverage exposure, not a live bug.** What makes it worth building: item **190 was a KO-only defect on those exact pages**, and it was found by a `translations.js` dictionary diff — the estate scan could not have found it then and cannot today.
- **198 (5.0, READY) — 190's i18n gate implements half of its own playbook predicate.** The playbook (written 07-31) says *"identical to EN **AND** containing no Hangul"*; the gate ships only `en === ko`. Mutation-probed against the real exported function: KO `"Powered by "` (one trailing space) → **clean**; KO `"Powered by the DefiLlama feed"` → **clean**; `$100`/`$100` → **flagged** (false positive). Structurally worst case: keyed on sameness, **the gate goes silent the day EN is reworded without KO** — exactly when the drift appears. **0 live instances today** (543 keys, the 24 no-Hangul and 24 identical values are the same 24, all legitimately allowlisted).
- **Cleared without a ticket:** 190's allowlist audited — 24/24 entries are brands or acronyms with stated reasons, nothing prosaic silenced (checked because the playbook says this is where the check rots). `tokens/az/` exclusion is correct and deliberate. And the 7d URL set being *unchanged* means this tick's provenance pass had no new input — weaker than 07-31's, said rather than glossed.
- **Experiments:** no window closed today. Open: 117.x→08-05, 124→08-06, 125→08-07, 129→08-08, 045/051/065/148→08-09..12, 180/182→08-13, 190/191/192→08-14, 196→08-15, 013 GSC→human ~08-23.
- **+playbook:** `detector-signal-coverage.md` gains a **third axis** — *is the population complete?* (axis 1 = signal set, axis 2 = rate/memory, both take the population as given; a half-size population reports as perfectly healthy). `product-audit.md` gains the `/ko/` surfaces and the dropped-conjunct trap.
- **Process:** re-read `origin/main`'s max backlog ID immediately before writing rows (07-31's lesson) — 196, so no collision this time.

## QUESTIONS FOR THE HUMAN (2 — both change what gets built)

1. **Both questions from yesterday's report are still open and both now have items riding on them.** The KO question especially: 197 (bring 2,186 KO pages into the audit) and 198 (finish the KO translation gate) are the top two items, and **both become dead code if the answer to "is KO worth its tax?" is "deprecate it."** I'm proceeding on the standing rule (EN + natural KO together, 2026-07-09) because it stands until you change it — but this is the cheapest moment to say otherwise.
2. **Two clean ticks in a row now, and the findings are getting more meta.** Yesterday's two items were tooling; today's two are tooling *about* tooling — the loop is auditing its own auditor. That is genuinely useful (197's gap is 50% of the estate) but it is a signal about where the returns are. Same question as yesterday, sharper: with a clean product, no traffic, and the 069 spotlight packs **20 days unposted**, do you want the loop to (a) keep hardening, (b) build distribution *assets* to lower your posting cost, or (c) something else?

<!-- Process narration lives in LOG.md; full numbers in signals/2026-08-01.md. -->
