# Heartbeat report — 2026-07-27

- **P0, live on production right now: `llms.txt` publishes 353,114% APY.** `https://www.defi.garden/llms.txt` (fetched by curl this tick, not inferred from the checkout) leads with `- BSC · zeebu · ZBU — 353114.2% APY, $576,877 TVL` under the header *"Live highest APY opportunities (updated daily, TVL ≥ $10k)"*. `generate-llms.js` contains **zero occurrences of `SANITY`**, uses a **$10k** TVL floor against the product's **$10M**, and sorts APY-**descending** — so the file is by construction a leaderboard of the dataset's worst anomalies. **8** lines >1000% in `llms.txt`, **15** in `llms-full.txt` across 6 chains. The top pool isn't even in the $10M snapshot the app uses. Daily CI regenerates it, so it refreshes with each day's worst outlier. → **159 (READY 9.2, HIGH, spec written)**.
- **How to check it yourself:** `curl -s https://www.defi.garden/llms.txt | sed -n '64,72p'` — or `grep -oE "— [0-9.]+% APY" llms.txt | sed 's/— //;s/% APY//' | awk '$1>1000'`.
- **Why this matters beyond its traffic:** `llms.txt` is the AI-discovery surface — the one artifact whose entire job is telling LLMs and agents what DeFi Garden claims. NORTH_STAR says trust rails are the moat and honest numbers beat exciting numbers; this surface has been saying the opposite in our own voice.
- **Also ticketed: 160 (READY 5.0, LOW)** — `audit-app.js` reads rendered HTML only, so `llms*.txt`, sitemaps and OG images are read by nothing automated. 148 (07-26) and 159 (07-27) were both hand-found in that same gap on consecutive days. Fix is a cheap `prescanTextSurfaces()`; ships after 159.
- **Everything else is quiet and healthy.** North star `pool_click{garden_cta, protocol_link}` = **0** in 14d (expected — ~0 traffic). `error_occurred` **0 for 14 straight days**. Data layer: 735 pools, 326 non-null Sharpe, zero NaN/Inf, max total APY 39.91%. Items 144/145's render rails independently re-verified by tracing all four `apyMean30d` consumers in `PoolDetail.js` — all correctly gated.
- **`audit-app.js`: 16 surfaces (was 10), 5 findings — all the known 148 class**, exactly as item 157 predicted. No new scanner finding. It self-clears when PR #306 merges.
- **First-ever direct `?pool=` landing** (lido/STETH, healthy page) — `pool_view{url_direct}` = 1, up from 0 all-time. Zero CTA clicks on it. n=1; noting the milestone, not drawing a conclusion.
- **Seven experiment windows closed** (062/063/064/066/068/075/079, all shipped 07-13). **All INCONCLUSIVE at literal zero** — every waitlist event is absent from the 07-13→07-27 result set. **064 carries a clean negative worth acting on:** prod `page_view` by `src` returns one row, `undefined`=48 → **zero `src=x_spotlight` arrivals, i.e. the 069 spotlight packs have not been posted.**
- **Process recommendation (three cohorts now agree):** stop opening 14-day calendar windows on the waitlist funnel. Gate the next waitlist item on ≥30 `waitlist_opened` events instead of a date — six windows closing on one identical zero is measurement discipline spent where there's nothing to measure.
- **Build order:** 159 → 160. (148 remains the only other READY row and is in flight as human-gated PR #306.)
- +playbook: derived-number-rails (UPDATED — new Step 0 "enumerate the surfaces, a rail is a property of a surface not a codebase" with the per-generator rail-status table, and Step 0b "prove your check can fail" with the vacuous-`p.apy` trap that actually led to 159).

## Questions for the human

1. **PR #306 (item 148) — still unanswered since 07-26, and it's now blocking two runs.** 7 nonsense token slugs (`/tokens/8oct2026`, `2027`, `00`…) from Pendle expiry fragments. Is dropping them item-013-class sitemap quality work, or de-indexing SEO surface? **Merge as-is** or **close and I'll park it**. Three consecutive build runs have found no other takeable item because of it.
2. **159's TVL floor — one number, and I'll proceed without you if you're busy.** `$10k` is indefensible next to the product's `$10M`. My plan unless you say otherwise: use `$10M` for consistency, falling back to a clearly-labelled `$1M` if that leaves the surface too thin. Say the word if you'd rather it stayed permissive.
3. **The 069 spotlight packs still haven't been posted** (proven this tick: zero `src=x_spotlight` arrivals in 14 days). Three packs have been ready to post since 07-13. Nothing in the distribution half of the backlog can be measured until one goes out — this is now the single biggest blocker on the loop's usefulness.
