# Report — 2026-08-02 (heartbeat, triage only)

- **Metric state:** north star `pool_click{garden_cta|protocol_link}` = **0**, 25th straight day. Prod 14d: **88 sessions** (unfiltered control 94), `error_occurred` **0 — 20 clean days**, clean in the control too. Funnel 30d **unchanged for a third day** (72h): `plan_saved` 5 · `search_success` 4 · `plan_created` 3 · `share_link_opened` 1 · waitlist/share_created/tend 0. **Insufficient data** for all funnel claims.
- **Audit clean for a third consecutive tick, on a population that doubled again:** 82 surfaces (60 → 82), prescan **2,186 → 4,304** pages, 1 finding, **0 P0/P1** — the known `201e5f6e`, auto-downgraded by 171 for a 5th day. **197 and 198 landed and are visible in the data, not just the changelog:** `scannedByFamily` now includes `koTokens: 2067, koChains: 85`, and `surfacesCovered` carries six `/ko/` **path-prefixed** static surfaces for the first time. `poolLinkLiveness` 3,669 ids / 0 dead. Measured at `c72c90cf6`, current `origin/main`.
- **The 7d URL set moved for the first time in three days, and both new URLs are one journey:** `/tokens/jitosol` → `/?pool=fdcccd6a-…`. **The first SEO-lander → pool-detail traversal in the loop's history.** 0 CTA clicks — n=1, so **no conversion claim is made**; its value was as a repro target, and it pointed straight at the tick's findings.
- **206 (8.0, READY) — the audit's pool rotation draws from the $10M snapshot, not from the surface users arrive on.** 734 rotation candidates vs **3,669** deep-linked `?pool=` ids: **420 (11.4%) reachable, 3,249 (88.6%) not**, median deep-linked TVL **$515,134**. **So every "pool-detail clean" claim this loop has made is scoped to the richest 11.4%** — and that real arrival ($1.5M Kamino pool) sits in the 88.6%, on a code path the scanner has never rendered once. Every hand-found pool-detail bug in this backlog's history (122, 144, 145, 165) was on a non-flagship pool.
- **207 (7.5, READY) — item 105 was built for SEO arrivals and reaches 11.4% of them.** Its own comment says it exists so the trust notes *"would [not] hide for exactly the SEO arrivals we most want to convert"*; its remedy looks the pool up in the same $10M snapshot, so it cannot fire for 3,249 of 3,669. **The consequence is a blank, not a wrong number** — the honest "no track record yet" copy exists but lives *inside* the `pool.kpis &&` gate it should be the fallback for, so those arrivals get the thinnest possible north-star page. Cheap leg only; the structural half is **208** (measure-then-decide, with "stop at 207" written in as a successful outcome).
- **The floors are not the bug and no rail is touched by any of the three items.** Estate $100K / app $10M are deliberate (human directive 2026-07-11), and `?pool=` already renders sub-rail pools by design. 206 changes what the scanner looks at; 207 changes what the page *says* about a pool it already shows.
- **Cleared without a ticket** (recorded so tomorrow doesn't re-chase): the loud hypothesis — a $10M trust strip contradicting $100K page copy — is **false**; `tokens/*.html` doesn't load `landing.js` (only `analytics.js`), the estate says `$100K` on 2,154 pages and contains `$10M` zero times. `tvlTrendShrinking`'s "keeps clearing our $10M floor" prose **cannot render** on a sub-rail pool (same `kpis` gate). `poolLinkLiveness`'s `scope: "en"` is **not** a 197-shaped gap — KO emits the identical 3,669 ids, difference 0.
- **The 21-day "`src` = undefined, zero tagged arrivals" reading is retired as a measurement artifact.** 202/203/204 shipped inside the last 24h and are verified present in the shipped code; the 08-01 arrival had no `src` because 203 hadn't shipped when they clicked. Replaced with a dated falsifiable check: **a non-`undefined` `src` row by ~08-05, or it's a P1 regression** — not another "distribution is untestable" line.
- **Experiments:** no window closed today. Open: 117.x→08-05, 124→08-06, 125→08-07, 129→08-08, 045/051/065/148→08-09..12, 180/182→08-13, 190/191/192→08-14, 196→08-15, 197-205→08-15..16, 013 GSC→human ~08-23.
- **+playbook:** `detector-signal-coverage.md` gains a **fourth axis** — *is the population the ARRIVAL population, or the artifact nearest to hand?* (three separate things silently adopted a payload-optimisation artifact as "the set of pools"; 105 even documented the limit and never measured it — a documented limitation with no number next to it reads as an edge case). `product-audit.md`'s `?pool=` surface entry now states both populations.
- **Process:** backlog had **ZERO READY rows** at pickup (every row 001-205 terminal — the condition that forced build loops to self-promote four runs running). Max id re-read off `origin/main` immediately before writing: 205 → today's are 206/207/208. Triage only; no product code written this tick.

## QUESTIONS FOR THE HUMAN (2 — both change what gets built)

1. **Do you want daily rate history for ~3,669 sub-$10M pools, or is "we track the larger pools daily" a line worth keeping?** This is 208's Phase 2 gate and the only question here that money and repo size ride on. Phase 1 (measurement) runs either way and may answer it for you — if the already-wired D1 `/history` endpoint covers those pools, the cost collapses and it's cheap; if not, stopping at 207 (honest note, trust notes stay a ≥$10M feature) is a legitimate end state, not a failure. **No answer needed unless the measurement lands near the thresholds.**
2. **Yesterday's question, and now it has a data point rather than a vibe.** A real person walked SEO lander → pool-detail yesterday and didn't click either CTA. One person is not evidence of anything (n=1, ≥30 rule) — but it's the first time the funnel has been walked at all, and it happened with the **069 spotlight packs still unposted on day 21**. The loop can keep making the product better for arrivals it isn't generating, or it can lower your posting cost. Still (a) keep hardening, (b) build distribution assets, or (c) something else?

<!-- Process narration lives in LOG.md; full numbers in signals/2026-08-02.md. -->
