# Heartbeat report — 2026-07-28

- **Yesterday's P0 is fixed and confirmed on live prod.** `curl https://www.defi.garden/llms.txt` → **0** APY
  figures above the 1000% rail (was 8, top 353,114.2%), and the header now claims `TVL ≥ $10M`, matching the
  product's real floor. Item 159 verified on the live surface, not in the checkout. Top line is now
  `uniswap-v3 · WETH-USDC — 95.5% APY` on $110.8M TVL — a real pool the app itself would show.
- **Auditing the same file one layer down found a second live defect of a different class → 166 (READY 8.6,
  HIGH, spec written).** The numbers are honest now; the **links** still are not. 32 of them, on prod:
  **15 pool rows point at the bare homepage** (`generate-llms.js:606` is `pool.url || meta.baseUrl` and no
  DefiLlama payload has a `url` field — the "fallback" fires on 100% of rows, always); **17 `?search=` links
  drop the query** (`search` is in neither `ANALYTICS_PARAMS` nor `PLANNER_PARAMS`, and `landing.js` reads
  only `lang`, so `?search=lido` lands on an empty search box — while the routed `?protocols=` already
  exists); and **8 top-yield rows are not pool-specific**, which is why two different Base WETH-USDC pools
  (95.5%/$110.8M and 31.7%/$10.2M) appear as two rows on one URL. **`grep -c "?pool=" llms*.txt` → 0, 0:**
  the AI-discovery surface has never linked to pool-detail, the north-star surface.
- **Check it yourself:** `curl -s https://www.defi.garden/llms-full.txt | grep -cE "TVL — https://www\.defi\.garden$"`
  → `15`. And `curl -s https://www.defi.garden/llms.txt | sed -n '47,52p'` → the six `?search=` protocol rows.
- **Also ticketed: 168 (READY 7.4, HIGH, spec written)** — `grep -icE "planner|savings|goal|subscription|forever number" llms.txt`
  → **0**. Our own AI-discovery file describes the product as a yield screener; `plan.html` appears once in
  `llms-full.txt` as a bare undescribed URL among 4,917. That is the positioning NORTH_STAR says we are
  explicitly not, published in our own voice to every LLM that reads it.
- **And 169 (READY 5.4, LOW)** — 160's new text-surface scanner returned `suspectCount: 0` on the two files
  carrying those 32 broken links, because its four signals are all number checks (it was specced from 159, a
  number bug). Third instance of one pattern: **148 → 159 → 166, the checker's signals always come from the
  last bug, so the next bug lands in the class nobody has been bitten by yet.**
- **Everything else is quiet and healthy.** North star `pool_click{garden_cta, protocol_link}` = **0** in 14d
  (20th straight day, ~0 traffic). `error_occurred` **0 for 15 straight days**. Data layer: 740 pools, 335
  non-null Sharpe, zero NaN/Inf, max total APY 95.55% (a real jump from 39.91%, well inside the rail).
- **`audit-app.js`: 26 surfaces (was 16), 5 findings — all the known 148 class**, third consecutive day with
  no new scanner finding. Notable: the planner and all three bloom archetypes (×360 ×KO) ran their first full
  rotation and came back **clean** — first automated evidence the default face renders correctly end-to-end.
- **Two windows closed.** **082** (planner `translations` guard) → **MOVED, weak-n**: 15 clean days, but only
  ~82 sessions and ONE `plan_created` exercised the planner, so it is "no recurrence observed", not "proven
  robust". **088.1** (pool-detail track-record note) → **INCONCLUSIVE**: its surface took 6 `pool_view` in 30
  days. Both kept.
- **Process, applied not proposed:** 166 and 168 are the first items written with **traffic-gated** decision
  rules (`pool_view{url_direct}` ≥ 30 / planner `page_view` ≥ 30) instead of 14-day calendar windows — acting
  on the 07-27 takeaway rather than re-recommending it.
- **This tick was lapped:** a build run shipped its own item **167** (audit renders 1 hardcoded pool of 740,
  PR #323) while this heartbeat was thinking, so my ids were renumbered 166 / 168 / 169 and every append
  re-applied on top of the new `main`. Read the audit numbers accordingly — that run used the **pre-167**
  scanner, so tomorrow's finding count is not comparable to today's.
- **Build order: 166 → 168 → 169.** (148 remains READY-but-in-flight as human-gated PR #306.)
- +playbook: `product-audit` (UPDATED — new bug class **10, link-target integrity on generated text
  surfaces** with the three sub-checks and the `pool.url || meta.baseUrl` "unconditional branch" trap; plus a
  note that text surfaces are covered for numbers, not claims, and the "a checker's signals come from the
  last bug" rule).

## Questions for the human

1. **PR #306 (item 148) — day 3, and it is now decaying, not just waiting.** Its base is `main` at 07-26
   while `main` has taken ~10 commits since, four of them daily CI regens of the same 5,596-file SEO surface
   the PR rewrites. Every day raises its conflict cost. The question is unchanged and binary: dropping 7
   nonsense token slugs (`/tokens/8oct2026`, `2027`, `00`…) — **item-013-class sitemap quality work (merge
   as-is)** or **de-indexing SEO surface (close it and I park 148)**?
2. **The 069 spotlight packs still have not been posted** — proven again this tick: prod `page_view` by `src`
   over 14d returns one row, `undefined` = 47. Three packs ready since 07-13. This is now the single biggest
   blocker on the loop's usefulness: while it holds, every distribution hypothesis is untestable by
   construction and the product-quality work has no audience to convert.
3. **No question on 166/168** — both are unambiguous defects against standing decisions, so they build
   without you. Flagging only that 168 changes how the product *describes itself* to LLMs; if you want to
   write that positioning line yourself rather than have the loop derive it from CLAUDE.md, say so before it
   ships.
