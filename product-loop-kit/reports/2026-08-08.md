# Report — 2026-08-08 (heartbeat, triage only, no product code)

- **Metric:** leg (B) `pool_click{garden_cta}` = **1 on 08-07 — the first ever** — but `$region` puts **100% of that day's engagement (1 click, 4 pool_view, 1 plan_created, 2 plan_saved) in Utrecht on iOS/Mac**, the day you merged #409/#411/#412/#413. Operator self-test → **real count stays 0, gate stays shut** (31st day). Leg (A): 0 new AI referrals (day 4). `error_occurred` **0 over 14d, both prod-filtered and control** — 26th clean day, now with a non-zero exercise count (4 analytics-mode loads, all yours, all post-244).
- **P0 live on main — `npm test` is RED.** `6fceca79bb` deleted `poolNotFoundTitle` from **both** en and ko, so `t()` returns the key and the dead-pool heading on `/?pool=<unknown>` renders the literal string **`poolNotFoundTitle`**. Test: `node test_dead_pool.js` → **exit 1**, 2/12 fail (`EN: "undefined", KO: "undefined"`). Confirmed independently by `audit-app.js` (new P1 `dead-pool-empty-state`) and by loading the dictionary. → **item 253**, spec written, fix is restoring 2 strings from `git show 6f1d624451:translations.js`.
- **P0 — the rail moved 100×, nine of its statements didn't.** `DEFAULT_MIN_TVL` $10M → **$100K** (your call, not questioned). Still advertising $10M: the landing trust badge **"⌁ $10M minimum TVL"** (en+ko, `landing.js:357`), the planner degen persona (en+ko), `home.html:238`'s **agent tool contract**, `llms.txt` ×2 + `llms-full.txt` (**the agent citation layer**), `stories/kevin.html`. **732 of 7,358 snapshot pools (9.9%) clear the floor we promise.** `generate-llms.js:50` still hardcodes `MIN_TVL_USD = 10000000 // mirrors app.js:801`. → **item 254** (HIGH; acceptance = every site *derives* from the constant, not nine string patches). Test: `grep -n '\$10M' llms.txt home.html translations.js`.
- **The detector that should have caught it ran and scored zero.** `audit-app.js`'s `tvl-floor-claim` checks a document's *internal consistency* and never reads `DEFAULT_MIN_TVL` — fourth narrowing in this gate family (198, 212, 249). Folded into 254.
- **P1 — nothing here can see a duplicated or deleted translation key.** The same commit also doubled `tvlTrendShrinking` (dead `$10M` copy at `translations.js:62`/`:847`, live `$100K` at `:112`/`:890`). No user-visible defect today (last-key-wins) — the point is every gate walks the *parsed* object, where both defects vanish. → **item 255** (source-level check; both real defects on main are its positive controls).
- **Measurement integrity → item 252** (8.5, LOW): a persisted `insider` flag so gate reads subtract a named population instead of a forensic argument. Playbook `traffic-quality-classification.md` **widened** (not narrowed): the question is now *"does this visit carry information we did not already have?"* — which contains the crawler rule rather than sitting beside it.
- **Audit:** 83 surfaces, **5 findings** (2 P0 prescan, 2 P1, 1 P2). Rail-breach 2→15 / 11→38 is **population growth, not regression** — but measured inside today's single population, the newly admitted $100K–$10M band carries **4.1× the mean30d breach density** (0.56% vs 0.14%) and 37 of the 38 breaches. 221's occlusion class still **dormant, not repaired** (`style.css:2874` unchanged). Static prescan 4,338 pages / 0 suspects on 6 signals; 3,664 pool links checked, 0 dead.
- **Ledger:** orphan PRs **8 → 2** (#331, #322) and open PRs 11 → 3 — real cleanup. Item **247** shipped across #409/#412/#413 and had **no BACKLOG row**; row added retroactively as SHIPPED. Next id computed `computeNextId(251, [221,176,166])` = **252**, no collision. Top 3 by score: **253 (9.5)**, **254 (8.8)**, **252 (8.5)**.
- **Experiments:** none closed, none can — 0 real exercised events on every gate. Fuzz-decay prediction day 4/7 at zero, all witnesses. 0 GATED rows remain.

## Questions for the human
1. **Does the $100K floor apply to *savings plans* too, or only to listings?** 254's copy fix depends on it: today the landing badge and the planner's persona copy both state one floor. If plans should keep a higher bar than the grid, that is two numbers and two strings, not one — and I should not guess which.
2. **Dispose #331 (item 176) and #322 (item 166)?** Both are 3-attempt PARKED work with no BACKLOG terminal marker, so they classify ORPHAN every tick. Close them, or should a build run re-attempt? (#331 also still ships as patch files because of the old push failure.)
3. Restating, unanswered from 08-07 — **item 247's two recorded deviations**: the gated rollout onto results/landing/search ran with no approval recorded, and the commissioned `specs/247-design-world.md` is absent from the shipped diff. Do you want the world statement written after the fact, or is the commission closed as-shipped?

*Process note, not a question: six merges landed on `main` between yesterday's tick and this one with no verifier in the path, and one of them shipped a P0 plus a red test. The loop's gate is advisory post-merge (until 003's successor); it caught this within a day, which is the system working as designed — but a day is the lag.*
