# Heartbeat report — 2026-07-30

- **Half the north star can silently disappear, and 29.1% of pools are exposed → 182 (READY 8.4, HIGH,
  spec written).** The *"Start Earning on <protocol> ↗"* button is one of the two clicks the north star
  counts (`pool_click{source=protocol_link}`, `PoolDetail.js:517`). Both render sites are
  `protocolUrlWithRef && …`, so a missing URL renders **nothing** — no fallback, no explanation. The URL
  comes from a **second, different third-party endpoint** (`api.llama.fi/protocols`, not the pools API),
  fetched at runtime and **failing silently by design** (`app.js:1285`, verbatim), behind a **96-entry
  hand-maintained** map. Measured today: that map covers **525 of 741 pools (70.9%)**, leaving **216 pools /
  $8.9B TVL / 134 projects** with no CTA when the fetch doesn't land — and **148 of the 438 (33.8%)**
  pool-detail pages our own SEO estate deep-links. The result is cached permanently, so **the first visit
  decides** — the search-arrival cohort. One pool (`sdai`, $61M) has no URL even on the happy path.
- **Check it yourself:** block `api.llama.fi` in devtools, hard-reload
  `https://www.defi.garden/?pool=ae6b7add-6505-492e-9bee-0e55fa8c5670`, and note the missing
  "Start Earning" button. `Garden this pool →` still renders; the other half of the metric does not.
- **This was found by the scanner, not by hand — its second originating finding in two days.** And it
  survived the false-alarm check rather than dying to it: a live fetch (7,962 protocols) confirmed the pool
  resolves fine, so the finding is the *degraded path* — which is exactly what an ad-blocker, an outage, or
  an early bounce produces. That check **enlarged** the finding from one pool to 29.1% of the estate.
- **Yesterday's four ships are clean and verified live on prod.** `chains/cardano`'s CTA now reads
  `?chain=Cardano&minTvl=100000` and returns **exactly the 33 pools the page claims** (was 0) — **173**
  fixed. The FAQ now says *"Pools listed on **this page** clear a $100K minimum TVL … that is this page's
  listing bar, **not a safety guarantee**"* — **174** fixed, and honestly, not by deletion. `/tokens/20`
  returns **404**, zero junk slugs remain — **148** fixed. `llms*.txt` link signal **0 suspects** — **180**
  fixed.
- **The generated estate is clean for the first time.** `audit-app.js`: 2,179 static pages scanned,
  **`suspectCount: 0` on all five signals**; text surfaces 0 on all five. 29 surfaces, 2 findings, 1
  blocking — and the blocking one is 182 above. 171's auto-downgrade held a second day.
- **Also ticketed: 183 (READY 5.2)** — the audit's `dead-cta` said only *"missing or not visible"*;
  classifying it cost a live API fetch and three call-site reads, and left as-is this P1 recurs **every
  run**, indistinguishable from a real one. 171 already solved this for the pool prescan. Same item: the
  north-star surface is audited **3 pools deep out of 741 (0.4%)**, chosen incidentally — `ae6b7add` was
  luck. **184 (READY 4.6)** — level 2 of 175's link model is still hand-run (4th time in 5 days); measured
  **4 dead of 3,677** `?pool=` deep links (0.11%), inside 181's existing 1.0% drift budget, so it ships as
  a gate proving a true negative, not a repair.
- **Everything else quiet and healthy.** North star **0** for the 22nd straight day. Prod `error_occurred`
  **0 for 17 days** — and clean in the *unfiltered* control too this tick (the 07-15 non-prod errors aged
  out; keep the filter anyway). 86 prod sessions/14d, all crawler-shaped: **insufficient data** for every
  funnel claim. Data layer: 741 pools, zero out-of-rail totals, zero non-finite KPIs. `src` breakdown still
  one row (`undefined` = 47), 18th day.
- **One window closed: 116** (re-share from report mode) — `share_link_created{surface:'report'}` = **0**
  against 3 `plan_created` in 30 days. **INCONCLUSIVE at n≈0**, kept. That is now four share-loop items
  (005/007/008/116) closed at n≈0 for the identical reason.
- **Build order: 182 → 183 → 184.** 182 first — it is the only one touching the product, it is on the
  north-star surface, and both others are better after it lands (183's `defect` branch gains a true
  negative; a rotation over an unfixed 216-pool population would hold the gate red for the wrong reason).
  **179 was the only READY item when this tick started and it shipped mid-tick** (`a4ad4456e`, merged while
  I was writing) — so 182/183/184 are now the entire READY queue, and the build loop would otherwise have
  had nothing to take.

## Questions for the human

1. **182 adds a fourth `pool_click` source value, `defillama_fallback`, for the case where no protocol URL
   exists at all — confirm that's the right call.** The alternative is rendering nothing (today's
   behaviour). I've specced the fallback to link to the pool's DefiLlama page with its own copy and its own
   `source` value, so the north star's `source ∈ {garden_cta, protocol_link}` filter is untouched by
   construction and the number stays comparable across the change. Say the word if you'd rather show an
   honest "no link available" note instead of sending people to DefiLlama — it's one string and one href.
2. **The 069 spotlight packs are still unposted — 18th consecutive day** with `src` = `undefined` only.
   Three packs ready since 07-13. This is the same line every day for a reason: while it holds, every
   distribution hypothesis is untestable by construction, four share-loop experiments keep closing at n≈0,
   and 182's conversion effect — like every conversion effect — cannot be measured. The product-quality
   work is landing (the whole generated estate went clean today); it has no audience to convert.
