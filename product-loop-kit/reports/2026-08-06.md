# Report — 2026-08-06 (heartbeat, triage only)

- **Metric state:** north star leg (B) `pool_click{garden_cta|protocol_link}` = **0**, 29th day; leg (A) = **0 new** AI-referred sessions (the 14d perplexity pair is still the same 07-26 pair). Prod 14d: 162 sessions / control 168. `error_occurred` absent from both result sets — **24th clean prod day** — but that zero is now recorded **with its exercise count: ~0** real analytics-mode human loads, so it is not evidence the new P0 doesn't fire in prod. Insufficient data (<30 real events) for every funnel claim.
- **P0, FIRST ON RECORD — the analytics grid boots dead on `?token=`:** `React is not defined` → `Cannot read properties of undefined (reading 'some')` in `getPoolTypeShared`, and **zero pool cards render**. Mechanism *confirmed from the error signature*: `PoolDetail.compiled.min.js` line 1 destructures `React` in the same comma-separated `var` as its protocol arrays, so a throw there leaves the arrays unassigned while the hoisted function stays callable — exactly the observed pair. Cause: `home.html:178-179` load React with `defer`; `:350-364` appends the bundle via `appendChild` (**async by default**) with no barrier between them. **Rate: 1/1 contended, 0/6 isolated.** → **244** (9.0, READY, spec written).
- **The occlusion count fell 6 → 4 and that is NOT an improvement** — grid-token's two findings vanished only because the surface never rendered. Isolated runs confirm both are still there. Recorded so the delta can't be misread; the remaining 4 are all item 221's `.app-footer` class (grid-chain 1280 P0+P1, grid-360 P1, grid-768 P1).
- **Audit (fresh, 83 surfaces, 8 findings / 7 blocking):** static prescan 4,332 pages / 0 suspects on its 6 signals; text surfaces 2 / 0 on 5 signals; i18n 540 / 0. Pool prescan 3,977 union pools: `mean30d-rail-breach` **8** (was 12) and `apy-rail-breach` back to **0** (was 1) — both **churn, not repairs**. `dead-pool` no-finding NOT claimed clean (231: lens 3/20 there). `planner-768` P2 is the known item **235**.
- **#399 (item 239) is finished work with no terminal state — and it can't merge as-is.** Three checked facts: the defect is **live on `main`** (the `sharpe` comparator branch has no `hasNoSupplyYield` partition; 247 pools / $30.1B / 28 chains); there is **no verifier verdict**, so autonomy (c) correctly didn't merge — but nothing flagged it either; and its commit adds a BACKLOG row numbered **242**, an id `main` shipped to a different item today. → **245** (7.5, READY, spec). Scope correction: `sortBy` defaults to `'tvl'` (which already carries 092's fix), so this is one interaction deep via the Risk-Adjusted toggle, not first paint.
- **Merged to main overnight:** 229, 240, 241, 242, 243. **New unmerged PRs:** #396 (231, PARKED collision), #397 (232, PARKED verifier FAIL ×3), #399 (above). Statuses synced to main: 232 → PARKED, 239 → IN_REVIEW/ORPHANED.
- **Instrument note:** `$initial_referring_domain` as a **user** property returns all-`undefined`; as an **event** property it reads correctly. Reading the user form alone would have filed a phantom "referrer tracking broken" P1 — the 08-02 `src` precedent, caught this time.
- **Predictions:** fuzz-class decay at **day 2 of 7** at zero across all witnesses (both days, filtered + control). Filed new, weakest form: the 244 race reaching prod — with the finding that **every candidate witness is currently blind or unexercised**.
- **Experiments:** none closed — none can. No traffic gate has opened (real pool-detail events 14d = 3, need ≥30). GATED re-check: **229** visuals stays GATED (225 still IN_REVIEW, round 3 required) — the only GATED row.
- **Top 3 takeable next:** **244** (9.0) · **245** (7.5) · **235** (6.0) — everything scored higher is human-gated or dep-blocked (next line). `+playbook: detector-detection-rate (predicate widened — the product can be the flaky half)`.

## QUESTIONS FOR THE HUMAN (2)

**1 — #399 needs one word from you, and a live defect is waiting on it.** It's a complete, self-tested build (red-first control, 17/17 rendered assertions) that never got a verifier verdict, so it stalled silently. Either **(a)** have the next build run verify it and merge — **renumbering its residual BACKLOG row off 242 first**, which `main` shipped to a different item today — or **(b)** close it and re-file. Doing nothing keeps the Risk-Adjusted sort ranking 0%-yield pools first on `?chain=`/`?token=`.

**2 — the agent-first chain is still stalled at its root, same three PRs as yesterday.** **#392** (224 edge telemetry — PARKED at the 3-attempt budget, your call to merge; 227/228/234 are all dep-blocked behind it and north-star leg (A) stays instrument-blind until it deploys) · **#393** (225 design reset — your pre-merge screenshot review, the Q2b gate; round 3 required) · **#386** (221 — PARKED, verifier FAIL ×3, do not merge as-is; its class reproduced on 4 surfaces again today). Older human-gated PRs still open: #332, #331, #322, #316, #309.

<!-- Process narration in LOG.md; full numbers in signals/2026-08-06.md. -->
