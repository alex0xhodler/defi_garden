# Heartbeat report — 2026-07-14

- **NEW BUG at the funnel top (top billing): first-ever `error_occurred`.** `ReferenceError: translations is not defined` fired on the bare `/` (the planner), 07-13, Chrome/desktop/en-US. Root cause: `planner.js:795` `makeT()` dereferences the bare global `translations` inside a `useMemo`; throws when `translations.min.js` isn't defined at call time (transient load failure/deploy race). n=1, `can_recover=true`, but it's a funnel-top render failure → **filed as 082 (READY, 8.5, HIGH, spec written)**. Per NORTH_STAR "a bug at a funnel step outranks any experiment." This unstarves the build loop (0 READY items for the last 3 ticks).
- **Traffic decay was over-read.** session_start rebounded 07-12 **21 → 07-13 48**. The 07-13 snapshot called 21 a "~74% monotonic decay"; the next day corrects it — 07-12 was one low day, band is ~21–80/day. Distribution is still the binding constraint (0 organic funnel entrants), but not a collapse.
- **First `share_link_opened` (2×) — but both from `localhost:8794`.** Dev/test, NOT real virality. The viral-loop input metric still hasn't fired from a real user.
- **North star `waitlist_submitted` = 0**, still traffic-gated (plumbing verified sound 07-13; events still absent from lexicon).
- **Mixpanel note:** the uppercase `Mixpanel` connector is **live in-session this run** — real numbers pulled (interview Q4's "unauthenticated in cloud" no longer applies to this connector).
- **Backlog:** +082 READY. Top 3: **082** (8.5, bug, READY) · 069 (8.4, 3 spotlight packs SHIPPED, awaiting human post) · 080 (8.2, SHIPPED — sitemap membership shrank 81% by design, expect it in GSC).
- **Experiments:** none past window (earliest open 005/007/008 → 07-24). LEARNINGS.md unchanged.

**QUESTIONS FOR THE HUMAN**
1. **SUBSCRIPTION_LADDER drift** (flagged internally since 07-13, never put to you): `planner.js` ships `spotify/netflix/claude/chatgpt/amazonprime`, but CLAUDE.md + `docs/garden-planner-v3-yield-funded.md` mandate **Spotify → Netflix → Claude Pro → Gym → Phone bill**. Which is canonical? (Blocks any ladder/subscription-copy work.)
2. **Distribution is the only lever that fills the verified-sound card funnel, and it's human-owned:** the 3 ready `spotlights/` packs (069) are still unposted. Are you blocked on posting the first one, or good to go?

---

# Heartbeat report — 2026-07-14 (second run, evening)

- **The Mixpanel dataset is contaminated by dev/test traffic — the real traffic story restates.** 100% of the last 2 days' `plan_created` (11) and `share_link_opened` (13) are `localhost`; the 07-10/11 "80/day plateau" was mostly `localhost:8792` test runs (114 of 327 sessions); Vercel preview deploys add noise. Real `www.defi.garden` traffic: **7 / 6 / 10 / 16 / 11 / 5 per day since launch — a flat trickle, never a spike, never a collapse.** This morning's "rebound to 48" was an artifact (real 07-13 = 11).
- **The 07-09 "launch spike" (152 sessions) happened on `www.yield.garden`** — a live Vercel mirror of the same app — and died the next day with ≤1 plan_created. → **097 (BLOCKED, question below)**.
- **New top build item: 096 (9.0, LOW, READY, spec written)** — hostname allowlist gate in `Analytics.track()` so non-prod hosts stop firing prod events. Until it ships, every heartbeat claim filters `page_url` to prod domains (contamination is fully filterable retroactively — `page_url` is on every event).
- **North star `waitlist_submitted` = 0 ever, from any host.** Real prod plan_created: zero since 07-10. `/tokens/`+`/chains/` page_view: 0 all-time (no organic SEO arrivals yet). Distribution remains the binding constraint — the 069 spotlight packs are still the highest-leverage unposted asset.
- **Guardrails: `error_occurred` 0 today** — 082's ReferenceError has not recurred (window to 07-28). Analytics URLs render (real sessions on `?chain=robinhood*`, `?token=SMON` today).
- **Experiments: nothing to close** (earliest window 07-24). All future pre/post closes must use the prod-domain filter.
- Top 3 backlog: **096** (9.0 signal integrity, READY) · 069 (8.4 packs SHIPPED, awaiting human post) · 082 (8.5, shipped, measuring clean so far).

**QUESTIONS FOR THE HUMAN**
1. **yield.garden (097):** it's serving a full mirror of the app and took the entire 07-09 launch burst. Keep it as a tracked prod mirror, or 301 it to www.defi.garden? And where was that launch posted (the channel produced 152 visits and ~zero conversion — useful for spotlight targeting)?
2. Still open from this morning's report (not re-asking, just pending): SUBSCRIPTION_LADDER canonical set, and whether you're blocked on posting the first 069 spotlight.

---

# Heartbeat report — 2026-07-14 (third run, 21:20 UTC — re-verify)

- **No-op run: nothing moved since the 19:58 pull (~1.5h).** Live Mixpanel re-confirms the evening numbers exactly with the prod `$current_url` filter — flagging so the record shows this was verified, not skipped.
- **North star `waitlist_submitted` = 0** (still absent from the 18-event lexicon). Real prod `www.defi.garden` traffic flat at 5–16 sessions/day; `plan_created` = 0 since 07-10. Distribution remains the sole binding lever, and it's human-owned (069 packs unposted).
- **Guardrail clean:** `error_occurred` 0 on 07-14 — 082's ReferenceError fix (shipped today) is holding, window to 07-28.
- **Backlog current, no edits:** 096 SHIPPED (env-gate — stops future non-prod contamination), 097 BLOCKED (yield.garden), top READY = 093/094/095 (analytics-UX audit follow-ups). Acting on 1.5h of sub-sample data would break the minimum-sample rule, so no scores changed.
- **Experiments:** none past window (earliest 07-24). LEARNINGS.md unchanged.
- **Note for future heartbeats:** the prod URL property is `$current_url`, not `page_url` (snapshots had the wrong key name — same data).

**QUESTIONS FOR THE HUMAN** — none new. Three remain pending from earlier today (not re-asking a third time in one day): SUBSCRIPTION_LADDER canonical set, 069 first-spotlight posting, yield.garden 097 decision.
