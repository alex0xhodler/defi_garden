# Heartbeat report — 2026-07-26

- **North star:** still **0** (expected — ~0 traffic; pipeline verified). Guardrails clean: `error_occurred` 0 for **13 straight days**; 07-25 finalized at 4 sessions, 07-26 at 0 so far. Data layer healthy: 324 non-null Sharpe (316 yesterday), 0 absurd values, `apyMean` max 36.68% (144's rail holding — this field hit 36,452% pre-fix), history 12→13 points.
- **First heartbeat-driven `audit-app.js` run: 10 surfaces, 0 findings.** Everything shipped yesterday (140–147) holds. Worth knowing: the scanner exited MODULE_NOT_FOUND on first invocation (fresh clone, no `node_modules`) and writes no file when it does that — a crashed run is easy to mis-read as a clean one. → **149**.
- **New finding → 148 (READY 5.5): we are generating nonsense SEO pages.** `PT-SUSDE-22OCT2026` gets split on `-`, so the *expiry date* becomes a "token" with its own landing page. 7 are live in the sitemap now (`/tokens/8oct2026`, `17dec2026`, `2027`, `20261231`, `00`, `01`, `67`), each with an EN page + KO page + OG image + IndexNow submission. Rendered: `<h1>8OCT2026 DeFi Yields</h1>`. Today's snapshot yields 8 such tokens and the set churns daily.
  **How it surfaced:** three odd prod URLs (`?token=22OCT2026`, `?token=20`, `?token=20)`) that looked exactly like the crawler-invented `?chain=robinhood` class we cleared as harmless on 07-23 — traced back and they turned out to be ours. The scanner cannot find this class by construction; it only drives surfaces we hand it.
- **Also ticketed: 149 (READY 4.0)** — make `audit-app.js` resolve playwright from the global root and write `{"status":"DID_NOT_RUN"}` + exit non-zero instead of vanishing. No dependency change.
- **Four experiment windows closed** (all shipped 07-12): **041** chain pages — build leg MOVED, traffic INCONCLUSIVE (4 page_views vs ≥30) · **048** freshness signals — build MOVED, effect unmeasurable in-loop · **049** cross-surface linking — build MOVED (leaf pages `/chains/linea`, `/tokens/infinifiusdc` now getting crawled without hub links), traffic INCONCLUSIVE · **061** waitlist reframe — **INCONCLUSIVE**: `waitlist_opened`/`submitted`/`email_entered`/`submit_attempt` are all 0 and have *never* fired. Details in LEARNINGS.md.
- **How to check 148 yourself:** `grep -oE "https://www.defi.garden/tokens/(8oct2026|17dec2026|2027|20261231|00|01|67)" sitemap-token-pages.xml` — or just open https://www.defi.garden/tokens/8oct2026.
- **Build order recommendation:** 148 → 149. (Backlog had **zero** READY items at the start of this tick — 140–147 all shipped yesterday — so these two are the whole queue.)
- +playbook: product-audit (new check class 9 "URL provenance: is that weird URL ours?" + the crashed-scanner trap).

## Questions for the human
1. **148 needs your yes before it ships.** It deletes 7 URLs from the SEO surface, and NORTH_STAR's NEVER list says "deleting or de-indexing SEO surface" is human-gated. My read: this *completes* 013's already-shipped sitemap-quality mandate rather than being a new de-indexing decision, and 7 nonsense URLs out of 2,089 is not the lifeline that rule protects. Confirm, or I'll park it.
2. **Keep or drop `/tokens/pt`?** `PT` is a Pendle structural prefix, not an asset — but it's plausibly a real search term. Bug-class 8 (flag, don't auto-fix), so it's out of 148's scope either way.
3. Unchanged since 07-14: post 069's spotlight packs · 118 GSC example URLs · 097 yield.garden. (133 still BLOCKED on your discovery-behavior call.)
