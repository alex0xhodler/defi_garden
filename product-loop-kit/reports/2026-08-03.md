# Report — 2026-08-03 (heartbeat, triage only)

- **Metric state:** north star `pool_click{garden_cta|protocol_link}` = **0**, 26th straight day. Prod 14d: **85 sessions** (control 91), `error_occurred` **0 — 21 clean days**, clean in the control too (5th day). Funnel 30d unchanged a **fourth** day (96h): plan_saved 5 · search_success 4 · plan_created 3 · share_link_opened 1 · rest 0. Insufficient data for all funnel claims. 08-02 finalized at 3 sessions.
- **Audit clean a fourth consecutive tick — and 206 is visibly live:** rotation population 734 → **3,962 reachable** (union with the estate's deep links ∩ live), **23 of 32 rendered pool-details were sub-rail** — the first majority-arrival-population tick ever — all clean. 82 surfaces, prescan 4,358 pages 0 suspects, 3,658/3,658 deep links live, only finding = the known `201e5f6e` auto-downgrade (6th day). Fresh `generatedAt` both gates checked.
- **214 (6.5, READY) — a `?pool=` arrival never paints from the snapshot, even when its pool is in it.** `app.js:1144` blocks every pool arrival on the multi-MB live fetch; the 105 effect then fetches the snapshot anyway, post-render. Newly date-attributed: the ONLY grid-shaped arrival on record (07-29, snapshot-present pool `4438dabc`) logged `page_view` 1 / `pool_view` **0** — gone before anything painted. n=1, mechanism is the finding; smallest fix = let `tryLoadSnapshot()` accept snapshot-present pool arrivals, sub-rail path byte-identical.
- **215 (7.5, READY) — 206 widened the rotation's population but not the prescan's.** Same run, same JSON: `poolPrescan.candidates = 734` vs rotation union **3,964** — the 122/144/165 absurd-number checks reach 18.5% of candidates cheaply while the full live records for the rest already sit in memory (`audit-app.js:1546`). Pure detector item; positive-control-on-the-new-leg required. Playbook corollary written: a widening must reach (or explicitly defer, with a number) every leg that reads the population.
- **The `src` prediction (due ~08-05) is open but unjudgeable:** **zero** static-page arrivals since 202/203/204 shipped — condition (≥5 arrivals) unmet. Not falsified. Next tick re-checks; arrivals accruing with `src` still `undefined`-only is the P1.
- **Experiments:** no window closed today. Next: 117.1/.2/.3 → 08-05, then 124→08-06, 125→08-07, 129→08-08, 045/051/065/148→08-09..12, 180/182→08-13, 190/191/192→08-14, 196→08-15, 197-207/210/213→08-15..16, 211 GSC→~09-13, 013 GSC→~08-23.
- **Process:** zero READY rows at pickup (206/207/209-213 shipped by the 08-02→08-03 builds; 208 PARKED); max id re-read off origin/main = 213 → today's are **214/215**. Triage only; no product code written. +playbook: detector-signal-coverage (widen-every-leg corollary), product-audit (`?pool=` arrival accounting).

## QUESTIONS FOR THE HUMAN (0 new; 1 standing)
Yesterday's distribution question stands unanswered — first funnel walk happened with the 069 spotlight packs still unposted (day 22): (a) keep hardening, (b) build distribution assets, (c) something else? Not re-litigated here; answer when ready.

<!-- Process narration lives in LOG.md; full numbers in signals/2026-08-03.md. -->
