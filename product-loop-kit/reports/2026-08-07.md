# Report — 2026-08-07 (heartbeat, triage only)

- **Metric state:** north star leg (B) `pool_click{garden_cta|protocol_link}` = **0**, 30th day; leg (A) = **0 new** AI-referred sessions (the perplexity pair is still the same 07-26 pair). Prod 14d: **231** sessions / control 237 — but **two crawler bursts are 168 of those 231 (72.7%)**, so the raw count is a *worse* demand proxy than last tick. Insufficient data (<30 real events) for every funnel claim.
- **A SECOND crawler burst — 08-06 closed at 76 sessions**, not the "0 so far" yesterday could see. Bot-shaped on **5 of 5** playbook criteria: `$direct` 221/231, zero typed engagement (`search_input`/`plan_created`/`waitlist_email_entered`/`pool_view`/`pool_click` all absent), `session_start` 76 ≫ `page_view` 23, bare `/` firing **46 sessions and 0 page_views**, one bare-`Mozilla` UA carrying 24. It walked the tokens-hub CTA again → 7 crawler-fired `waitlist_opened` (7d total 16, **0 real**). Two bursts in four days is now recorded as **cadence, not demand**.
- **225 was MERGED TO MAIN as-is** (`75f7470646`, 2026-08-06T13:00 UTC) — and the BACKLOG still said *"do NOT merge as-is"* about a commit already on main: a stale status asserting the **opposite of main**, in the unsafe direction, with three rows sequenced behind it. Synced. **Gate re-check: 229's visuals leg gate is MET → promoted READY** (it was the only GATED row; there are now 0), and **238** + **246** had their `after #393` dependency cleared. All three are genuinely actionable on main today.
- **Audit (83 surfaces, 8 → 4 findings, 7 → 3 blocking) — first run on post-225 main.** **244's P0 fix is confirmed**: `grid-token`'s `React is not defined` + dead-end are gone and the grid renders. **221's 4 `.app-footer` occlusion findings are also gone — and that is NOT a repair**: `.app-footer { position: fixed }` is unchanged at `style.css:2874`, measured **0 findings across 6 runs** (2 full + 4 isolated). 225's layout moved those elements clear; the class is dormant, the mechanism is live, and the surfaces 236/237/238/246/247 are about to re-touch are exactly these. #386 still worth merging; urgency drops to latent.
- **NEW P1, and 225's own commit put it there:** `i18n:en-ko-parity` 0 → 2 — `resultsColApy` ko `"APY"`, `resultsColTvl` ko `"TVL"`. **These are allowlist omissions, not untranslated Korean**: `tcpColApy`/`tcpColTvl` — the token/chain-page column headers for these *exact two acronyms* — are already allowlisted `'acronym, same in KO'`, alongside 4 more precedents. → **248** (6.5, READY, spec, 2-line fix, `translations.js` byte-unchanged).
- **The miss exposed a real blind spot, measured before it was scored:** the parity gate value-checks **391 of 544 keys and skips all 153 function leaves (28.1%)** — ~80 of them `planner.*`, the ICP's default surface. Applying the gate's own predicate to that blind region gives **1 suspect of 153**, and it is the brand name "DeFi Garden". So: **zero live defects**, filed as latent coverage → **249** (5.5), not the P1 it resembles. Third narrowing found in this gate (198, 212 precedents).
- **Orphan PRs: 8** (#409, #399, #396, #332, #331, #322, #316, #309) — count flat, composition changed: #408 merged out, **#409 is new**. Also: **BACKLOG table integrity re-measured at 13 of 236 malformed rows**, not the 2 recorded in 245's notes.
- **Predictions:** fuzz-decay **day 3 of 7** at zero, all witnesses. **One filed witness retired as confounded** — "`session_start` on `?token=` with no `pool_view`" cannot see the 244 race, because `pool_view` never fires on a grid arrival either way. Guardrail: **25th clean prod day**, and its exercise count **moved off zero for the first time** (2 analytics-mode arrivals, 1 post-244-deploy; n=1, no claim).
- **Experiments:** none closed — none can. No traffic gate has opened (real pool-detail events 14d = 0, need ≥30). **Top 3 takeable now: 238 (8.8) · 246 (7.8) · 248 (6.5)** — 236/237 score higher but see Q1. `+playbook: checker-blind-region-measurement`.

## QUESTIONS FOR THE HUMAN (2)

**1 — Which design world do 236/237/238/246 get built against? They are all blocked on your 247 call, and three of them just came unblocked.** Yesterday you merged **225** (the Quiet reset) *as-is* despite rejecting it — so Quiet is the incumbent on main. Hours later a live session opened **PR #409**, commissioning item **247**: a *replacement* visual world (Besley/Public Sans, certificate/engraving language) under the mandate *"ONLY SOTA GRADE WORK"*, whose own doctrine is that redesign **replaces** — making the just-merged Quiet system its anti-reference. 236 (9.5), 237 (9.0), 238 (8.8), 246 (7.8) and 229's visuals leg all edit the same stylesheets #409 rewrites. **Building any of them now risks throwing the work away.** Either (a) merge/close #409 first and rebase the queue onto the winner, or (b) tell the loop to hold all design items until 247 is decided. I have not built any of them.

**2 — #409 needs a terminal state, and two of its own commissioned constraints were not met.** It is 9 commits / 21 files / +2,803−1,880, `mergeable_state: clean`, with **no verifier verdict and no BACKLOG row** — the same shape as #399, which has now sat 2 days. Read off the branch's own bookkeeping: (a) its commission says *"grid/search/planner/landing untouched this round; rollout gated on the human's approval of the branch preview"*, but the final commit rolls the ornament system onto **results, landing and search state** — and no approval is recorded; (b) the commissioned deliverable `specs/247-design-world.md` is **not in the diff**. Also still open and unanswered from yesterday: **#399** (item 239 — finished, unverified, and its BACKLOG row collides with id 242 on main) and the agent-first root **#392** (224 edge telemetry — 227/228/234 all dep-blocked behind it; leg (A) stays instrument-blind until it deploys).

<!-- Process narration in LOG.md; full numbers in signals/2026-08-07.md. -->
