# Heartbeat report — 2026-07-11

1. **North star (viral loop closures): 0.** Both share events (`share_link_created`/`share_link_opened`) still 0 in 14d — structurally 0. 005/007/008 measuring through 2026-07-24; insufficient sample, no decision yet.
2. **Signal now wired.** Live Mixpanel pull via connector (org Equitee 2885044 / project 4042048). 14d: session_start 247, page_view 165 (`/` planner=114, analytics search≈48, `/pool/*`=0), search_success 50, search_abandonment 4, plan_created 6, plan_saved 12, error_occurred 0 (guardrail quiet).
3. **Keystone finding — the theme's funnel is unmeasurable (3 code-verified gaps).** (a) All 50 `search_success` carry `results_count=0` (hardcoded, analytics.js:125) → "success" = parse, not results-shown; 018's 92.6% is a parse rate. (b) `trackPoolView` never fires on direct `/?pool=` SEO landings (app.js:1006-1024) → 019's garden_cta CTR has no denominator. (c) Behavioral: 50 searches → 0 pool_view → 0 pool_click, and `handlePoolClick` IS wired (app.js:2343) → internal-search users open zero pools. 100% drop at the theme's surface.
4. **New top item: 020 (LOW, score 8.5, spec written)** — real `results_count` on search_success + `pool_view` on `?pool=` landings. This is the standing "instrument the north-star funnel" priority made specific; unblocks honest reads of 018 and 019.
5. **013/014 (GSC SEO landers) re-gated behind 020** — no point optimizing SEO landings we can't measure landing on.
6. **Top 3 backlog:** 020 (8.5, READY) · 015 apex/referral share-URL hygiene (6.0, READY) · 013 sitemap quality threshold (8.0, gated on 020).
7. **Experiments:** none past window (018/019→07-25, 005/007/008→07-24). Nothing closed to LEARNINGS.md this run. Retention cohorts not computable (single active week).

QUESTION FOR THE HUMAN:
- Q-d: Is internal-search → pool-card click-through a goal, or is the pool-detail "Garden this pool" CTA meant to be reached primarily via SEO landings (search app = acquisition, pool-detail = conversion)? Data shows 50 searches → 0 card clicks. The answer decides whether 50→0 is a bug to fix (make cards clickable) or expected (drive SEO→pool-detail instead). 020 instruments both paths regardless, so build is not blocked.
