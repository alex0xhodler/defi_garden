# Report — 2026-08-04 (heartbeat, triage only)

- **Metric state:** north star `pool_click{garden_cta|protocol_link}` = **0**, 27th straight day. Prod 14d: **160 sessions** (control 166), `error_occurred` **0 — 22 clean days**, clean in the control too (6th day). Insufficient data for all funnel claims.
- **08-03 = 92 prod sessions, the biggest day on record — classified CRAWLER BURST** (playbook applied + updated): 13 uniques, bare-`Mozilla` UA 27 events/1 unique, 91/92 `$direct`, zero typed engagement; these crawlers execute JS and follow CTAs, so `waitlist_opened` fired for the first time ever (9×, machine-triggered via `?waitlist=1`). Crawler events do NOT count toward traffic gates. Not distribution; 069 packs unposted (day 23).
- **The 08-02 `src` prediction: RESOLVED, not a regression — attribution is PROVEN live.** First non-`undefined` `src` in prod history: `waitlist_opened{src=seo_tokens_hub}` ×5. `page_view`-by-src stayed `undefined`-only because it was the wrong instrument (planner never fires `page_view`); the distribution check now reads `session_start`-by-src (playbook corrected). Also: first bing.com referral on record (n=1, IndexNow's earliest downstream signal).
- **Audit (83 surfaces, fresh):** 215's widened prescan is live — rail checks now see all 3,987 union pools (was 734): **14 mean30d-rail-breach suspects** (1 known + 13 net-new), 2 promoted + rendered clean (live-only pools carry no kpis, breach never reaches the DOM), 12 rotating through; blocking-until-rendered per spec, no ticket. Static prescan 4,372 pages / 0 suspects.
- **221 (9.0, READY) — grid footer occlusion, the class's LAST surface:** grid-token 1280 P0 (`calculate-yield-btn-new` 98.8% under the fixed footer, hit-test resolves to footer) + grid-360 P1. 219's findings, reproduced 3rd tick, never ticketed until now. Fix = port 218/220's footer-leaves-fixed-layer pattern; then shrink the occlusion quarantine.
- **222 (8.5, READY) — duplicate in-flow `.theme-toggle` (`app.js:3139`) 100% under the sticky header at 360** — a control the user can never press. Preferred fix pre-registered: delete the duplicate (the header already renders a working toggle).
- **223 (6.0, READY) — www.defi.garden serves the ENTIRE repo** (~140 test files, `audit-app.js`, generators, `product-loop-kit/` incl. NORTH_STAR/BACKLOG, `CLAUDE.md`, `.claude/` — all curl-verified 200). Measured harm: the 08-03 `/plan?lang=ko%60` fuzz arrivals trace to a template literal in a SERVED test file. Not a secrets breach (repo public on GitHub); P2 hygiene → `.vercelignore` with a both-ways post-deploy curl matrix; runtime-asset allowlist is the load-bearing half.
- **Experiments:** none closed today. Next: 117.1/.2/.3 → 08-05, then 124→08-06, 125→08-07, 129→08-08, 045/051/065/148→08-09..12, 180/182→08-13, 190/191/192→08-14, 196→08-15, 197-207/210/213→08-15..16, 211 GSC→~09-13, 013 GSC→~08-23.
- **Process:** zero READY rows at pickup; max id off origin/main = 220 → today's are 221/222/223 (specs written). Triage only; no product code. +playbook: traffic-quality-classification (JS-executing crawlers; src-instrument correction).

## QUESTIONS FOR THE HUMAN (0 new; 1 standing)
The distribution question stands (day 23, 069 packs unposted): (a) keep hardening, (b) build distribution assets, (c) something else? Side note inside 223, not a gate: if the internal-docs exposure bothers you beyond the `.vercelignore` fix, making the GitHub repo private is an org-admin action only you can take.

<!-- Process narration lives in LOG.md; full numbers in signals/2026-08-04.md. -->

## ADDENDUM — strategy pivot (same day, human interview)
- The human reviewed GSC + 60d Mixpanel against 3 weeks of loop spend and reset direction: **AGENT-FIRST ACQUISITION**. Evidence: google referrals 60d = 0 (3,274 indexed, 18,555 crawled-not-indexed); bing-family ~14; perplexity.ai 2 unprompted; UI incoherence root-caused to two coexisting design systems.
- New dual north star: **agent consumption (API + MCP + AI-referrer sessions + edge-log reads) + pool-detail CTA clicks.** Classic search → informational guardrail.
- New program, in dependency order: **224** edge telemetry (CF Worker→D1, makes agent reads measurable — build first) → **225** DESIGN RESET (clean minimal, neumorphism stripped, HUMAN pre-merge screenshot gate) → **226** Google sitemap head-curation + soft-404 elimination (human-authorized de-listing, zero deletions) → **227** public railed-yield API → **228** MCP server → **229** spotlight packs v2 (targets now, visuals GATED(225)).
- Full decision text: NORTH_STAR standing decision "2026-08-04 · AGENT-FIRST STRATEGY". Specs: 224/225/226 written.
