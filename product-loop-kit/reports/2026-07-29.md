# Heartbeat report — 2026-07-29

- **79.5% of our own indexed SEO pages send the reader into an empty grid → 173 (READY 9.0, HIGH, spec
  written).** 1,749 of 2,200 committed `tokens/*.html` + `chains/*.html` pages have a primary CTA —
  *"See live pools on &lt;X&gt; →"* — that returns **zero pools**. Chains: 48 of 88. Tokens: 1,701 of 2,112.
  Measured against **live DefiLlama (16,038 pools)**, not the checkout snapshot. `chains/cardano.html` says
  *"33 pools … clear DeFi Garden's $100K TVL floor"* and its button goes to `?chain=Cardano` → **0 results**.
  Cause: the generators build at **$100K** (`generate-token-pages.js:60`, `generate-chain-pages.js:65`), the
  app they link to filters at **$10M** (`app.js:801`). 100×.
- **Check it yourself:** open `https://www.defi.garden/chains/cardano` and click "See live pools on
  Cardano →". The page names 33 pools; the grid shows none. Same on `/chains/celo`, `/chains/fantom`,
  `/tokens/1inch`, `/tokens/acx`.
- **The fix is one param and it is proven, not argued.** `&minTvl=100000` — already honoured by `app.js:927`
  (*"Respect explicit minTvl=0; fall back to DEFAULT_MIN_TVL only when param is absent"*) — makes Cardano
  return **exactly the 33** the page claims. `DEFAULT_MIN_TVL` stays byte-untouched, so this is **not** the
  NEVER list; it is the unblocked slice of BLOCKED item 133.
- **Also ticketed: 174 (READY 8.2, HIGH)** — all 2,200 pages tell readers *"Every rate shown passes **DeFi
  Garden's trust filters — a $100K minimum TVL**"* under the heading *"Are these rates safe?"*. Our floor is
  $10M. This is **item 159's exact defect** — 159 fixed 2 files, nobody re-ran the check on the other 2,200
  pages. Worse on the same pages: **1,713 make a "forever number" claim** (*"park $3.4K and it could run a
  $20/mo Claude Pro subscription, forever"*) blended from pools the product refuses to display, and **916
  rows across 462 pages list a pool at 0.00% APY** as a yield.
- **And 175 (READY 5.6, HIGH)** — 172's `link-target-integrity` signal, shipped this morning, scored **0**
  on the 2,200 pages carrying those 1,749 dead CTAs, because it was specced from 166 where "broken link"
  meant an *unrouted param*. `?chain=Cardano` routes fine; it just returns nothing. **148 → 159 → 166 → 173
  is four in a row** of the checker inheriting its signals from the last bug — so 175 names the axis instead
  of adding another instance: a link is (1) routed, (2) resolvable, (3) **non-empty**. Level 3 is where all
  1,749 live, and it is the only level that catches two individually-correct surfaces with a broken contract
  between them.
- **Yesterday's two ships are clean and verified live.** `llms.txt`/`llms-full.txt` by curl: `?pool=` deep
  links **0 → 9/16**, bare-origin pool rows **15 → 0**, unrouted `?search=` **7 → 0**, planner mentions
  **0 → 9**, rail breaches **0** (159 holding). I also ran the check 166 itself did not — every one of the
  40 `?pool=`/`?protocols=`/`?preset=` values **resolves** to a real pool, project or preset. 0 misses.
- **Everything else quiet and healthy.** North star `pool_click{garden_cta, protocol_link}` = **0** in 14d
  (21st straight day). Prod `error_occurred` **0 for 16 straight days**. Data layer: 746 pools, zero
  out-of-rail totals, zero non-finite KPIs. `audit-app.js`: 29 surfaces, 6 findings, **all the known 148
  class** — 4th consecutive day with no new scanner-originated finding. One good sign: **171's first live
  auto-downgrade** (a prescan P1 disproved by its own rendered check, demoted to P2 with the reconciliation
  recorded) — yesterday that was a blocking false alarm.
- **Four windows closed.** 094 (pool-row logos), 114 ("Welcome back" card), 115 (`.ics` reminder) all
  **INCONCLUSIVE at n≈0** — `tend_reminder_added` = 0 against 3 `plan_created` in 30 days; all kept.
  **149** (audit self-heal) **MOVED → DONE**, and it is the only one measurable without traffic: this run
  was a fresh clone with no `node_modules` and the scanner completed and wrote a real verdict.
- **Build order: 173 → 174 → 175.** 173 and 174 touch the same two generators — land 173 first, rebase 174.
  175 must land after 173 or it correctly holds the audit red on 1,749 findings.

## Questions for the human

1. **Should `/tokens/` + `/chains/` exist at the $100K floor at all, or be rebuilt at the product's $10M?**
   This is the only question 173/174 cannot answer for you, and it changes what gets built. Keeping $100K
   means 2,200 pages of SEO surface whose links must carry the floor (173's fix). Moving to $10M means the
   surface collapses to roughly **450 pages** — far less SEO reach, but every page then agrees with the app
   by construction and 173 becomes unnecessary. I am building 173 under the $100K assumption per the
   2026-07-11 "two distinct surfaces" decision; say the word before it ships if you want the other shape.
2. **PR #306 (item 148) — day 4, still decaying.** `main` has taken six more commits (166, 168–172) plus
   daily SEO regens since its base. Unchanged binary question: dropping 7 nonsense token slugs
   (`/tokens/67`, `/tokens/8oct2026`, `2027`, `00`…) — **item-013-class sitemap quality work (merge as-is)**
   or **de-indexing SEO surface (close it and I park 148)**? Note the slug set churns daily: `tokens/67` and
   `tokens/8oct2026` are new today, `17dec2026` dropped out.
3. **The 069 spotlight packs are still unposted** — proven again: prod `page_view` by `src` over 14d is one
   row, `undefined` = 47. Zero tagged arrivals, 17th consecutive day. Three packs ready since 07-13. While
   this holds, every distribution hypothesis is untestable by construction and the product-quality work has
   no audience to convert.
