// GENERATED by compile-app.js from PoolDetail.js — do not hand-edit. Run `npm run compile`.
// Standalone PoolDetail Component - Full Version
var {
  useState,
  useEffect
} = React;
function PoolDetail({
  pool,
  onBack,
  resetApp,
  calculateYields,
  futureValue,
  formatCurrency,
  formatAPY,
  formatUsd,
  formatNum,
  formatApy,
  getProtocolUrl,
  getProtocolUrlWithRef,
  isDarkMode,
  t,
  AnimatedNumber,
  toggleTheme,
  language,
  changeLanguage
}) {
  // Fallback formatters when not passed (e.g. SSR/tests)
  var _formatUsd = formatUsd || ((n, f) => '$' + Number(n || 0).toLocaleString('en-US', {
    maximumFractionDigits: f || 2
  }));
  var _formatNum = formatNum || (n => Number(n || 0).toLocaleString('en-US'));
  var _formatApy = formatApy || (pct => Number(pct || 0).toLocaleString('en-US', {
    maximumFractionDigits: 2
  }) + '%');
  var _futureValue = futureValue || ((monthly, annualRatePct, years) => {
    var P = Number(monthly) || 0;
    var months = Math.round((Number(years) || 0) * 12);
    var r = (Number(annualRatePct) || 0) / 100;
    if (r === 0) return P * months;
    var rm = r / 12;
    return P * ((Math.pow(1 + rm, months) - 1) / rm);
  });
  var [investmentAmount, setInvestmentAmount] = useState(1000);
  var [showAPYBreakdown, setShowAPYBreakdown] = useState(false);
  var [calculatorExpanded, setCalculatorExpanded] = useState(true);
  var [poolInfoExpanded, setPoolInfoExpanded] = useState(true);
  var [activeCalculatorTab, setActiveCalculatorTab] = useState('30days');
  var [isPulsing, setIsPulsing] = useState(false);
  useEffect(() => {
    if (investmentAmount > 0) {
      setIsPulsing(true);
      var timer = setTimeout(() => setIsPulsing(false), 300);
      return () => clearTimeout(timer);
    }
  }, [investmentAmount]);
  if (!pool) {
    return React.createElement('div', {
      className: 'pool-detail-empty'
    }, React.createElement('p', null, 'No pool selected'), React.createElement('button', {
      className: 'back-button',
      onClick: () => {
        // Analytics tracking for back navigation from pool detail
        if (typeof Analytics !== 'undefined') {
          Analytics.trackNavigation('pool-detail', 'search', 'back_button');
        }
        onBack();
      }
    }, t ? t('backToSearch') : '← Back to Search'));
  }
  var totalApy = (pool.apyBase || 0) + (pool.apyReward || 0);
  var yields = calculateYields(investmentAmount, totalApy);
  var protocolUrl = getProtocolUrl(pool);
  var protocolUrlWithRef = getProtocolUrlWithRef(pool);

  // Determine pool type (must be defined before getRiskAssessment)
  var getPoolType = () => {
    if (!pool.project) return 'Yield Farming';
    var projectName = pool.project.toLowerCase();
    if (pool.poolMeta && pool.poolMeta.toLowerCase().includes('lending')) {
      return 'Lending';
    }
    var lendingProtocols = ['aave', 'compound', 'morpho', 'spark', 'maple', 'euler', 'radiant'];
    var dexProtocols = ['uniswap', 'curve', 'balancer', 'pancakeswap', 'sushiswap'];
    var stakingProtocols = ['lido', 'rocket-pool', 'ether.fi', 'jito', 'marinade'];
    if (lendingProtocols.some(p => projectName.includes(p))) return 'Lending';
    if (dexProtocols.some(p => projectName.includes(p))) return 'LP/DEX';
    if (stakingProtocols.some(p => projectName.includes(p))) return 'Staking';
    return 'Yield Farming';
  };
  var poolType = getPoolType();
  var APY_SANITY_LIMIT_LOCAL = 1000; // mirror of app.js constant

  // Comprehensive Risk Assessment
  var getRiskAssessment = () => {
    var riskScore = 0;
    var factors = [];

    // Anomalous APY override — force High risk immediately
    if (totalApy > APY_SANITY_LIMIT_LOCAL) {
      factors.push('Anomalous yield');
      return {
        level: t ? t('highRisk') : 'High',
        color: 'var(--color-error)',
        description: 'Anomalous yield — extreme caution',
        factors,
        score: 100
      };
    }

    // TVL Factor (40% weight)
    if (pool.tvlUsd < 1000000) {
      riskScore += 40;
      factors.push('Low liquidity');
    } else if (pool.tvlUsd < 10000000) {
      riskScore += 20;
      factors.push('Medium liquidity');
    } else {
      factors.push('High liquidity');
    }

    // APY Factor (30% weight) - Higher APY = Higher risk
    if (totalApy > 50) {
      riskScore += 30;
      factors.push('Very high yield');
    } else if (totalApy > 20) {
      riskScore += 20;
      factors.push('High yield');
    } else if (totalApy > 10) {
      riskScore += 10;
      factors.push('Elevated yield');
    }

    // Pool Age/Maturity Factor (20% weight)
    var isNewProtocol = ['jito', 'ether.fi', 'pendle', 'eigenlayer'].some(p => pool.project?.toLowerCase().includes(p));
    if (isNewProtocol) {
      riskScore += 15;
      factors.push('Newer protocol');
    }

    // Pool Type Factor (10% weight)
    if (poolType === 'LP/DEX') {
      riskScore += 10;
      factors.push('Impermanent loss risk');
    } else if (poolType === 'Lending') {
      riskScore += 5;
      factors.push('Credit risk');
    }

    // Determine overall risk level
    var level, color, description;
    if (riskScore <= 25) {
      level = t ? t('lowRisk') : 'Low';
      color = 'var(--color-success)';
      description = 'Conservative DeFi strategy';
    } else if (riskScore <= 50) {
      level = t ? t('mediumRisk') : 'Medium';
      color = 'var(--color-warning)';
      description = 'Moderate risk profile';
    } else {
      level = t ? t('highRisk') : 'High';
      color = 'var(--color-error)';
      description = 'Advanced DeFi strategy';
    }
    return {
      level,
      color,
      description,
      factors,
      score: riskScore
    };
  };
  var riskAssessment = getRiskAssessment();

  // Persona this pool's risk tier maps to in the planner (stable/rwa/degen —
  // mirrors planner.js's PERSONAS thresholds, same 25/50 risk-score bands
  // getRiskAssessment already uses above). Drives the "Garden this pool"
  // deep link and whether the projection below applies the degen haircut.
  var gardenPersona = riskAssessment.score <= 25 ? 'stable' : riskAssessment.score <= 50 ? 'rwa' : 'degen';
  var isAnomalous = totalApy > APY_SANITY_LIMIT_LOCAL;
  var applyDegenHaircut = gardenPersona === 'degen';
  var PROJECTION_YEARS = 5;
  var projectionApy = applyDegenHaircut ? totalApy / 3 : totalApy;
  // Projection is tied to the calculator's investment amount (lump sum, default
  // $1,000) so the number below AND the CTA react to what the user selects.
  // Lump-sum compound growth: principal * (1 + r)^years.
  var projectionAmount = investmentAmount * Math.pow(1 + projectionApy / 100, PROJECTION_YEARS);
  // Prefill the planner with the SAME lump sum + horizon so it lands on exactly
  // this projection (as capital, matching the calculator). The deep link never
  // carries the pool's APY — the planner computes from its own sanity-capped rate.
  var gardenThisPoolHref = `plan.html?goal=retirement&pace=${gardenPersona}&capital=${Math.round(investmentAmount)}&fm=capital&years=${PROJECTION_YEARS}`;
  // Concrete CTA (025): show the projected outcome ON the button — but NEVER for
  // an anomalous pool (trust rail: anomalous rates are flagged, never hyped).
  var showConcreteCta = !isAnomalous;

  // BreadcrumbList JSON-LD (040): mirrors the visual breadcrumb above
  // (Search Results -> <SYMBOL> Pool) without touching its markup/behavior.
  var breadcrumbJsonLd = JSON.stringify({
    '@context': 'https://schema.org',
    '@type': 'BreadcrumbList',
    itemListElement: [{
      '@type': 'ListItem',
      position: 1,
      name: 'Search Results',
      item: `${window.location.origin}/?app=1`
    }, {
      '@type': 'ListItem',
      position: 2,
      name: `${pool.symbol} Pool`,
      item: `${window.location.origin}/?pool=${encodeURIComponent(pool.pool)}`
    }]
  }).replace(/</g, '\\u003c');
  return React.createElement('div', {
    className: 'pool-detail-container',
    style: {
      opacity: 1,
      display: 'block',
      visibility: 'visible',
      position: 'relative',
      minHeight: '100vh',
      padding: '40px 20px 20px 20px',
      maxWidth: '1200px',
      margin: '0 auto'
    }
  }, React.createElement('script', {
    type: 'application/ld+json',
    dangerouslySetInnerHTML: {
      __html: breadcrumbJsonLd
    }
  }),
  // Clean Header: Logo | Breadcrumb | Toggle
  React.createElement('div', {
    className: 'header animate-on-mount',
    style: {
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'space-between',
      padding: '28px 0 24px 0',
      marginBottom: '24px'
    }
  },
  // Left: DeFi Garden Logo
  React.createElement('div', {
    style: {
      display: 'flex',
      alignItems: 'center',
      height: '40px' // Match toggle height
    }
  }, React.createElement('h1', {
    className: 'logo',
    style: {
      fontSize: 'var(--font-size-lg)',
      fontWeight: 'var(--font-weight-black)',
      color: 'var(--color-text)',
      margin: 0,
      cursor: 'pointer',
      transition: 'color 0.2s ease',
      lineHeight: '1'
    },
    onClick: resetApp,
    onMouseEnter: e => e.target.style.color = 'var(--color-primary)',
    onMouseLeave: e => e.target.style.color = 'var(--color-text)'
  }, 'DeFi Garden')),
  // Center: Breadcrumb Navigation
  React.createElement('div', {
    className: 'pool-breadcrumb',
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: '10px',
      padding: '0 20px',
      height: '40px',
      // Match toggle height
      background: 'var(--color-background)',
      borderRadius: 'var(--neuro-radius-lg)',
      boxShadow: 'var(--neuro-shadow-pressed)',
      fontSize: 'var(--font-size-sm)',
      fontWeight: 'var(--font-weight-medium)',
      color: 'var(--color-text)'
    }
  }, React.createElement('span', {
    style: {
      color: 'var(--color-text-secondary)',
      cursor: 'pointer',
      transition: 'color 0.2s ease'
    },
    onClick: () => {
      // Analytics tracking for back navigation from pool detail
      if (typeof Analytics !== 'undefined') {
        Analytics.trackNavigation('pool-detail', 'search', 'back_link');
      }
      onBack();
    },
    onMouseEnter: e => e.target.style.color = 'var(--color-primary)',
    onMouseLeave: e => e.target.style.color = 'var(--color-text-secondary)'
  }, 'Search Results'), React.createElement('span', {
    style: {
      color: 'var(--color-primary)'
    }
  }, '→'), React.createElement('span', {
    style: {
      color: 'var(--color-text)',
      fontWeight: 'var(--font-weight-semibold)'
    }
  }, `${pool.symbol} Pool`)),
  // Right: Header controls (language + theme)
  React.createElement('div', {
    className: 'detail-header-controls'
  },
  // Language toggle
  changeLanguage && language && React.createElement('button', {
    className: 'detail-header-btn',
    onClick: () => changeLanguage(language === 'en' ? 'ko' : 'en'),
    'aria-label': `Switch to ${language === 'en' ? 'Korean' : 'English'}`
  }, language === 'en' ? 'KO' : 'EN'),
  // Theme toggle
  toggleTheme && React.createElement('button', {
    className: 'detail-header-btn',
    onClick: toggleTheme,
    'aria-label': `Switch to ${isDarkMode ? 'light' : 'dark'} mode`
  }, isDarkMode ? '🌙' : '☀️'))),
  // Hero Section - Simplified and Focused
  React.createElement('div', {
    className: 'pool-hero-card animate-on-mount',
    style: {
      background: 'var(--color-background)',
      borderRadius: 'var(--neuro-radius-lg)',
      padding: '32px',
      boxShadow: 'var(--neuro-shadow-raised)',
      marginBottom: '24px',
      position: 'relative',
      overflow: 'hidden'
    }
  },
  // Subtle gradient overlay
  React.createElement('div', {
    style: {
      position: 'absolute',
      top: 0,
      right: 0,
      width: '40%',
      height: '100%',
      background: 'linear-gradient(90deg, transparent 0%, rgba(59, 130, 246, 0.03) 100%)',
      pointerEvents: 'none'
    }
  }),
  // Main Hero Content
  React.createElement('div', {
    className: 'pool-hero-content',
    style: {
      display: 'flex',
      justifyContent: 'space-between',
      alignItems: 'flex-start',
      position: 'relative',
      zIndex: 1
    }
  },
  // Left side - Pool info
  React.createElement('div', {
    className: 'pool-info-section'
  }, React.createElement('h1', {
    className: 'pool-symbol-hero',
    style: {
      fontSize: 'var(--font-size-4xl)',
      fontWeight: '900',
      color: 'var(--color-text)',
      marginBottom: '8px',
      fontFamily: 'monospace',
      lineHeight: '1.1'
    }
  }, pool.symbol), React.createElement('div', {
    className: 'pool-meta-simplified',
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: '12px',
      marginBottom: '16px',
      fontSize: 'var(--font-size-lg)',
      color: 'var(--color-text-secondary)'
    }
  }, React.createElement('span', {
    className: 'protocol-name',
    style: {
      color: 'var(--color-text)',
      fontWeight: 'var(--font-weight-semibold)'
    }
  }, pool.project), React.createElement('span', {
    className: 'separator'
  }, '•'), React.createElement('span', {
    className: 'chain-name',
    style: {
      color: 'var(--color-primary)'
    }
  }, pool.chain)), React.createElement('div', {
    className: 'pool-type-badge-hero',
    style: {
      display: 'inline-flex',
      alignItems: 'center',
      background: 'var(--color-background)',
      color: 'var(--color-primary)',
      padding: '8px 16px',
      borderRadius: 'var(--neuro-radius-lg)',
      fontSize: 'var(--font-size-sm)',
      fontWeight: 'var(--font-weight-medium)',
      boxShadow: 'var(--neuro-shadow-pressed)',
      textTransform: 'uppercase',
      letterSpacing: '0.5px'
    }
  }, poolType),
  // Trust indicators
  React.createElement('div', {
    className: 'trust-indicators',
    style: {
      display: 'flex',
      gap: '8px',
      marginTop: '16px'
    }
  }, React.createElement('div', {
    className: 'trust-badge',
    style: {
      display: 'inline-flex',
      alignItems: 'center',
      gap: '4px',
      padding: '4px 8px',
      background: 'var(--color-background)',
      borderRadius: 'var(--neuro-radius-md)',
      fontSize: 'var(--font-size-xs)',
      color: 'var(--color-success)',
      boxShadow: 'var(--neuro-shadow-pressed)'
    }
  }, t ? t('verified') : '✓ Verified'), React.createElement('div', {
    className: 'tvl-badge',
    style: {
      display: 'inline-flex',
      alignItems: 'center',
      padding: '4px 8px',
      background: 'var(--color-background)',
      borderRadius: 'var(--neuro-radius-md)',
      fontSize: 'var(--font-size-xs)',
      color: 'var(--color-text-secondary)',
      boxShadow: 'var(--neuro-shadow-pressed)'
    }
  }, AnimatedNumber ? React.createElement(AnimatedNumber, {
    value: pool.tvlUsd,
    formatFn: v => formatCurrency(v) + ' TVL',
    duration: 1200
  }) : formatCurrency(pool.tvlUsd) + ' TVL'))),
  // Right side — unified action card
  React.createElement('div', {
    className: 'pool-action-card'
  },
  // APY section
  React.createElement('div', {
    className: 'pool-action-apy'
  }, React.createElement('div', {
    className: 'pool-action-apy-label'
  }, t ? t('totalApy') : 'Total APY'), React.createElement('div', {
    className: 'apy-value-hero'
  }, AnimatedNumber ? React.createElement(AnimatedNumber, {
    value: (pool.apyBase || 0) + (pool.apyReward || 0),
    formatFn: v => _formatApy(v),
    duration: 1500
  }) : formatAPY(pool.apyBase, pool.apyReward)), pool.apyBase > 0 && pool.apyReward > 0 && React.createElement('div', {
    className: 'pool-action-apy-breakdown'
  }, React.createElement('span', null, t ? t('baseApyBreakdown', _formatApy(pool.apyBase).replace('%', '')) : `${_formatApy(pool.apyBase)} Base`), React.createElement('span', {
    className: 'pool-action-apy-sep'
  }, ' · '), React.createElement('span', null, t ? t('rewardApyBreakdown', _formatApy(pool.apyReward).replace('%', '')) : `+${_formatApy(pool.apyReward)} Rewards`))),
  // Divider
  React.createElement('div', {
    className: 'pool-action-divider'
  }),
  // Primary CTA — garden this pool (deep-links into the planner
  // prefilled with a persona/goal/monthly matching this pool's risk tier)
  React.createElement('a', {
    className: 'cta-button-primary',
    href: gardenThisPoolHref,
    onClick: () => {
      if (typeof Analytics !== 'undefined') {
        Analytics.trackPoolClick(pool, 'garden_cta', {
          investmentAmount: Math.round(investmentAmount),
          projectionYears: PROJECTION_YEARS,
          ctaVariant: showConcreteCta ? 'concrete' : 'generic',
          ctaPlacement: 'hero'
        });
      }
    }
  }, showConcreteCta ? t ? t('gardenThisPoolCtaConcrete', projectionAmount, PROJECTION_YEARS) : `Garden this pool → ~$${Math.round(projectionAmount).toLocaleString('en-US')} in ${PROJECTION_YEARS}y` : t ? t('gardenThisPoolCta') : 'Garden this pool →'), React.createElement('p', {
    className: 'pool-action-hint'
  }, t ? t('plannerCtaHint') : 'No wallet needed'),
  // Secondary — protocol link (text only)
  protocolUrlWithRef && React.createElement('button', {
    className: 'cta-button-protocol',
    onClick: () => {
      if (typeof Analytics !== 'undefined') {
        Analytics.trackPoolClick(pool, 'protocol_link', {
          ctaPlacement: 'hero'
        });
      }
      window.open(protocolUrlWithRef, '_blank', 'noopener,noreferrer');
    }
  }, t ? t('startEarningOn', pool.project) : `Start Earning on ${pool.project}`, ' ↗'), protocolUrlWithRef && React.createElement('p', {
    className: 'pool-action-hint pool-action-hint--muted'
  }, t ? t('opensProtocol') : 'Opens protocol · Wallet required')))),
  // Quick Metrics - Force 3-column layout
  React.createElement('div', {
    className: 'quick-metrics animate-on-mount',
    style: {
      display: 'grid',
      gridTemplateColumns: '1fr 1fr 1fr',
      gap: '16px',
      marginBottom: '40px'
    }
  }, React.createElement('div', {
    className: 'metric-card-simple',
    style: {
      background: 'var(--color-background)',
      borderRadius: 'var(--neuro-radius-lg)',
      padding: '20px',
      boxShadow: 'var(--neuro-shadow-subtle)',
      textAlign: 'center',
      transition: 'all 0.3s ease'
    }
  }, React.createElement('div', {
    style: {
      fontSize: 'var(--font-size-sm)',
      color: 'var(--color-text-secondary)',
      marginBottom: '8px',
      fontWeight: 'var(--font-weight-medium)'
    }
  }, React.createElement('span', {
    style: {
      display: 'block'
    }
  }, t ? t('dailyEarnings', investmentAmount) : 'Daily earnings'), React.createElement('span', {
    className: 'metric-sublabel'
  }, t ? t('dailyEarningsSubLabel', investmentAmount) : `on $${Number(investmentAmount || 0).toLocaleString('en-US')}`)), React.createElement('div', {
    style: {
      fontSize: 'var(--font-size-xl)',
      fontWeight: 'var(--font-weight-bold)',
      color: 'var(--color-primary)'
    }
  }, AnimatedNumber ? React.createElement(AnimatedNumber, {
    value: investmentAmount * totalApy / 365 / 100,
    formatFn: v => _formatUsd(v),
    duration: 1000
  }) : _formatUsd(investmentAmount * totalApy / 365 / 100))), React.createElement('div', {
    className: 'metric-card-simple',
    style: {
      background: 'var(--color-background)',
      borderRadius: 'var(--neuro-radius-lg)',
      padding: '20px',
      boxShadow: 'var(--neuro-shadow-subtle)',
      textAlign: 'center',
      transition: 'all 0.3s ease'
    }
  }, React.createElement('div', {
    style: {
      fontSize: 'var(--font-size-sm)',
      color: 'var(--color-text-secondary)',
      marginBottom: '8px',
      fontWeight: 'var(--font-weight-medium)'
    }
  }, React.createElement('span', {
    style: {
      display: 'block'
    }
  }, t ? t('monthlyEarnings', investmentAmount) : 'Monthly earnings'), React.createElement('span', {
    className: 'metric-sublabel'
  }, t ? t('monthlyEarningsSubLabel', investmentAmount) : `on $${Number(investmentAmount || 0).toLocaleString('en-US')}`)), React.createElement('div', {
    style: {
      fontSize: 'var(--font-size-xl)',
      fontWeight: 'var(--font-weight-bold)',
      color: 'var(--color-text)'
    }
  }, AnimatedNumber ? React.createElement(AnimatedNumber, {
    value: investmentAmount * totalApy / 12 / 100,
    formatFn: v => _formatUsd(v),
    duration: 1000
  }) : _formatUsd(investmentAmount * totalApy / 12 / 100))), React.createElement('div', {
    className: 'metric-card-simple risk-card',
    style: {
      background: 'var(--color-background)',
      borderRadius: 'var(--neuro-radius-lg)',
      padding: '24px',
      boxShadow: 'var(--neuro-shadow-raised)',
      textAlign: 'center',
      transition: 'all 0.3s ease',
      border: `2px solid ${riskAssessment.color.replace('var(--color-', '').replace(')', '') === 'error' ? 'rgba(239, 68, 68, 0.2)' : riskAssessment.color.replace('var(--color-', '').replace(')', '') === 'warning' ? 'rgba(245, 158, 11, 0.2)' : 'rgba(34, 197, 94, 0.2)'}`
    }
  }, React.createElement('div', {
    style: {
      fontSize: 'var(--font-size-sm)',
      color: 'var(--color-text-secondary)',
      marginBottom: '8px',
      fontWeight: 'var(--font-weight-medium)',
      textTransform: 'uppercase',
      letterSpacing: '0.5px'
    }
  }, t ? t('riskAssessment') : 'Risk Assessment'), React.createElement('div', {
    style: {
      fontSize: 'var(--font-size-xl)',
      fontWeight: '900',
      color: riskAssessment.color,
      marginBottom: '6px'
    }
  }, riskAssessment.level), React.createElement('div', {
    style: {
      fontSize: 'var(--font-size-xs)',
      color: 'var(--color-text-secondary)',
      lineHeight: '1.3',
      marginBottom: '8px'
    }
  }, riskAssessment.description), riskAssessment.factors.length > 0 && React.createElement('div', {
    style: {
      fontSize: 'var(--font-size-xs)',
      color: 'var(--color-text-secondary)',
      opacity: 0.8
    }
  }, `Key factors: ${riskAssessment.factors.slice(0, 2).join(', ')}`))),
  // Honest mini-projection — always visible, never collapsed
  React.createElement('div', {
    className: 'metric-card-simple animate-on-mount',
    style: {
      background: 'var(--color-background)',
      borderRadius: 'var(--neuro-radius-lg)',
      padding: '24px',
      boxShadow: 'var(--neuro-shadow-raised)',
      marginBottom: '32px',
      textAlign: 'center'
    }
  }, React.createElement('div', {
    style: {
      fontSize: 'var(--font-size-sm)',
      color: 'var(--color-text-secondary)',
      marginBottom: '8px',
      fontWeight: 'var(--font-weight-medium)',
      textTransform: 'uppercase',
      letterSpacing: '0.5px'
    }
  }, t ? t('projectionHeading') : 'The Long Game'), React.createElement('div', {
    style: {
      fontSize: 'var(--font-size-lg)',
      fontWeight: 'var(--font-weight-bold)',
      color: 'var(--color-text)',
      lineHeight: '1.4'
    }
  }, t ? t('projectionBody', investmentAmount, PROJECTION_YEARS, projectionAmount) : `$${Number(investmentAmount || 0).toLocaleString('en-US')} in this pool grows to ~${_formatUsd(projectionAmount, 0)} in ${PROJECTION_YEARS}y at current rates.`), applyDegenHaircut && React.createElement('div', {
    className: 'calc-warning'
  }, t ? t('poolDegenHaircutNote', _formatApy(totalApy)) : `Projected at ⅓ haircut (${_formatApy(totalApy)} headline) — farm rates decay. Active management required.`), isAnomalous && React.createElement('div', {
    className: 'calc-warning'
  }, t ? t('calcAnomalyWarning') : '⚠ This rate is anomalous and almost certainly unsustainable.'), React.createElement('div', {
    className: 'calc-disclaimer'
  }, t ? t('calcDisclaimer') : 'Estimates based on current rates — yields change constantly. Not financial advice.')),
  // Collapsible Yield Calculator
  React.createElement('div', {
    className: `calculator-compact animate-on-mount ${calculatorExpanded ? 'expanded' : ''}`,
    style: {
      background: 'var(--color-background)',
      borderRadius: 'var(--neuro-radius-lg)',
      padding: calculatorExpanded ? '24px' : '20px',
      boxShadow: calculatorExpanded ? 'var(--neuro-shadow-raised)' : 'var(--neuro-shadow-pressed)',
      marginBottom: '32px',
      transition: 'all 0.3s ease'
    }
  },
  // Calculator Header
  React.createElement('div', {
    className: 'calculator-header',
    onClick: () => setCalculatorExpanded(!calculatorExpanded),
    style: {
      display: 'flex',
      justifyContent: 'space-between',
      alignItems: 'center',
      cursor: 'pointer',
      marginBottom: calculatorExpanded ? '24px' : '0'
    }
  }, React.createElement('div', {
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: '12px'
    }
  }, React.createElement('div', null, React.createElement('div', {
    style: {
      fontSize: 'var(--font-size-lg)',
      fontWeight: 'var(--font-weight-bold)',
      color: 'var(--color-text)'
    }
  }, t ? t('calculateYourEarnings') : 'Calculate Your Earnings'), React.createElement('div', {
    style: {
      fontSize: 'var(--font-size-sm)',
      color: 'var(--color-text-secondary)',
      marginTop: '2px'
    }
  }, t ? t('quickEstimate', investmentAmount, investmentAmount * totalApy / 365 / 100) : `Quick estimate for $${Number(investmentAmount || 0).toLocaleString('en-US')}: $${Number(investmentAmount * totalApy / 365 / 100 || 0).toLocaleString('en-US', {
    maximumFractionDigits: 2
  })}/day`))), React.createElement('div', {
    className: 'calculator-toggle',
    style: {
      width: '32px',
      height: '32px',
      borderRadius: '50%',
      background: 'var(--color-background)',
      boxShadow: 'var(--neuro-shadow-subtle)',
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'center',
      transition: 'transform 0.3s ease',
      transform: calculatorExpanded ? 'rotate(180deg)' : 'rotate(0deg)'
    }
  }, '▼')),
  // Expanded Calculator Content
  calculatorExpanded && React.createElement('div', {
    className: 'calculator-content',
    style: {
      animation: 'fadeIn 0.3s ease'
    }
  },
  // Investment Input
  React.createElement('div', {
    className: 'investment-input-group',
    style: {
      marginBottom: '24px',
      textAlign: 'center'
    }
  }, React.createElement('div', {
    className: 'input-wrapper',
    style: {
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'center',
      gap: '8px',
      marginBottom: '16px'
    }
  }, React.createElement('span', {
    style: {
      fontSize: 'var(--font-size-lg)',
      fontWeight: 'var(--font-weight-bold)',
      color: 'var(--color-text-secondary)'
    }
  }, '$'), React.createElement('input', {
    type: 'number',
    className: 'amount-input',
    value: investmentAmount,
    onChange: e => {
      var newAmount = Number(e.target.value) || 0;
      setInvestmentAmount(newAmount);
      // Analytics tracking for yield calculation
      if (typeof Analytics !== 'undefined' && newAmount > 0) {
        var calculatedYields = calculateYields(newAmount, (pool.apyBase || 0) + (pool.apyReward || 0));
        Analytics.trackYieldCalculation(newAmount, pool, {
          calculatedYields,
          trigger: 'manual_input'
        });
      }
    },
    min: '0',
    step: '100',
    style: {
      width: '180px',
      padding: '12px 16px',
      border: 'none',
      borderRadius: 'var(--neuro-radius-md)',
      background: 'var(--color-background)',
      color: 'var(--color-text)',
      fontSize: 'var(--font-size-lg)',
      fontWeight: 'var(--font-weight-medium)',
      textAlign: 'center',
      boxShadow: 'var(--neuro-shadow-pressed)',
      outline: 'none'
    }
  })),
  // Quick Amount Buttons
  React.createElement('div', {
    style: {
      display: 'flex',
      gap: '8px',
      justifyContent: 'center',
      flexWrap: 'wrap',
      marginBottom: '16px'
    }
  }, [100, 500, 1000, 2000, 5000, 10000, 100000].map(amount => React.createElement('button', {
    key: amount,
    onClick: () => {
      setInvestmentAmount(amount);
      // Analytics tracking for preset amount selection
      if (typeof Analytics !== 'undefined') {
        var calculatedYields = calculateYields(amount, (pool.apyBase || 0) + (pool.apyReward || 0));
        Analytics.trackYieldCalculation(amount, pool, {
          calculatedYields,
          trigger: 'preset_button'
        });
      }
    },
    onMouseEnter: e => {
      if (investmentAmount !== amount) {
        e.target.style.boxShadow = 'var(--neuro-shadow-flat)';
        e.target.style.transform = 'translateY(-1px)';
      }
    },
    onMouseLeave: e => {
      if (investmentAmount !== amount) {
        e.target.style.boxShadow = 'var(--neuro-shadow-subtle)';
        e.target.style.transform = 'translateY(0)';
      }
    },
    style: {
      padding: '8px 16px',
      border: 'none',
      borderRadius: 'var(--neuro-radius-md)',
      background: investmentAmount === amount ? 'var(--color-primary)' : 'var(--color-surface)',
      color: investmentAmount === amount ? 'white' : 'var(--color-text)',
      fontSize: 'var(--font-size-sm)',
      fontWeight: investmentAmount === amount ? 'var(--font-weight-semibold)' : 'var(--font-weight-medium)',
      boxShadow: 'var(--neuro-shadow-subtle)',
      transition: 'all 0.2s ease',
      cursor: 'pointer',
      whiteSpace: 'nowrap',
      minHeight: '36px',
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'center'
    }
  }, `$${amount >= 1000 ? `${amount / 1000}k` : amount}`)))),
  // Tab Navigation for Time Periods
  React.createElement('div', {
    style: {
      display: 'flex',
      gap: '4px',
      marginBottom: '24px',
      background: 'var(--color-background)',
      borderRadius: 'var(--neuro-radius-md)',
      padding: '4px',
      boxShadow: 'var(--neuro-shadow-pressed)'
    }
  }, ['1day', '7days', '30days'].map(tab => {
    var tabLabels = {
      '1day': '1 Day',
      '7days': '7 Days',
      '30days': '30 Days'
    };
    return React.createElement('button', {
      key: tab,
      onClick: () => setActiveCalculatorTab(tab),
      onMouseEnter: e => {
        if (activeCalculatorTab !== tab) {
          e.target.style.boxShadow = 'var(--neuro-shadow-flat)';
          e.target.style.transform = 'translateY(-1px)';
        }
      },
      onMouseLeave: e => {
        if (activeCalculatorTab !== tab) {
          e.target.style.boxShadow = 'var(--neuro-shadow-raised)';
          e.target.style.transform = 'translateY(0)';
        }
      },
      style: {
        flex: 1,
        padding: '8px 12px',
        border: 'none',
        borderRadius: 'var(--neuro-radius-md)',
        background: activeCalculatorTab === tab ? 'var(--color-primary)' : 'var(--color-surface)',
        color: activeCalculatorTab === tab ? 'white' : 'var(--color-text)',
        fontSize: 'var(--font-size-sm)',
        fontWeight: activeCalculatorTab === tab ? 'var(--font-weight-semibold)' : 'var(--font-weight-medium)',
        boxShadow: 'var(--neuro-shadow-subtle)',
        transition: 'all 0.2s ease',
        cursor: 'pointer',
        whiteSpace: 'nowrap',
        minHeight: '36px',
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center'
      }
    }, tabLabels[tab]);
  })),
  // Primary Yield Result (based on selected tab)
  React.createElement('div', {
    style: {
      background: 'var(--color-background)',
      borderRadius: 'var(--neuro-radius-lg)',
      padding: '24px',
      textAlign: 'center',
      boxShadow: 'var(--neuro-shadow-raised)',
      marginBottom: '16px'
    }
  }, React.createElement('div', {
    style: {
      fontSize: 'var(--font-size-base)',
      color: 'var(--color-text-secondary)',
      marginBottom: '8px',
      fontWeight: 'var(--font-weight-medium)',
      cursor: 'help',
      position: 'relative',
      display: 'inline-block'
    },
    onMouseEnter: e => {
      var tooltip = document.createElement('div');
      tooltip.textContent = 'Calculations are estimates. Actual yields may vary based on market conditions.';
      tooltip.style.cssText = `
                position: absolute;
                bottom: 100%;
                left: 50%;
                transform: translateX(-50%);
                background: var(--color-text);
                color: var(--color-background);
                padding: 8px 12px;
                border-radius: 6px;
                font-size: 12px;
                white-space: nowrap;
                z-index: 1000;
                margin-bottom: 5px;
                box-shadow: 0 2px 8px rgba(0,0,0,0.2);
              `;
      tooltip.id = 'earnings-tooltip';
      e.target.appendChild(tooltip);
    },
    onMouseLeave: e => {
      var tooltip = document.getElementById('earnings-tooltip');
      if (tooltip) tooltip.remove();
    }
  }, activeCalculatorTab === '1day' ? t ? t('estimatedDailyEarnings') : 'Estimated Daily Earnings' : activeCalculatorTab === '7days' ? t ? t('estimatedEarnings') : 'Estimated Weekly Earnings' : t ? t('estimatedMonthlyEarnings') : 'Estimated Monthly Earnings'), React.createElement('div', {
    className: isPulsing ? 'yield-pulse-active' : '',
    style: {
      fontSize: 'var(--font-size-3xl)',
      fontWeight: '900',
      background: 'linear-gradient(135deg, var(--color-primary) 0%, var(--color-forest) 100%)',
      WebkitBackgroundClip: 'text',
      WebkitTextFillColor: 'transparent',
      backgroundClip: 'text',
      lineHeight: '1.1',
      marginBottom: '8px',
      transition: 'transform 0.15s ease-out'
    }
  }, activeCalculatorTab === '1day' ? _formatUsd(investmentAmount * totalApy / 365 / 100) : activeCalculatorTab === '7days' ? _formatUsd(investmentAmount * totalApy / 52 / 100) : _formatUsd(investmentAmount * totalApy / 12 / 100)), React.createElement('div', {
    style: {
      fontSize: 'var(--font-size-sm)',
      color: 'var(--color-text-secondary)',
      fontWeight: 'var(--font-weight-medium)'
    }
  }, t ? t('basedOnInvestment', investmentAmount) : `Based on $${investmentAmount.toLocaleString('en-US')} investment`), React.createElement('div', {
    className: 'calc-disclaimer'
  }, t ? t('calcDisclaimer') : 'Estimates based on current rates — yields change constantly. Not financial advice.'), totalApy > APY_SANITY_LIMIT_LOCAL && React.createElement('div', {
    className: 'calc-warning'
  }, t ? t('calcAnomalyWarning') : '⚠ This rate is anomalous and almost certainly unsustainable.')))),
  // Collapsible Pool Information
  React.createElement('div', {
    className: `pool-info-section animate-on-mount ${poolInfoExpanded ? 'expanded' : ''}`,
    style: {
      background: 'var(--color-background)',
      borderRadius: 'var(--neuro-radius-lg)',
      padding: poolInfoExpanded ? '24px' : '20px',
      boxShadow: poolInfoExpanded ? 'var(--neuro-shadow-raised)' : 'var(--neuro-shadow-pressed)',
      marginBottom: '32px',
      transition: 'all 0.3s ease'
    }
  },
  // Pool Info Header
  React.createElement('div', {
    className: 'pool-info-header',
    onClick: () => setPoolInfoExpanded(!poolInfoExpanded),
    style: {
      display: 'flex',
      justifyContent: 'space-between',
      alignItems: 'center',
      cursor: 'pointer',
      marginBottom: poolInfoExpanded ? '20px' : '0'
    }
  }, React.createElement('div', {
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: '12px'
    }
  }, React.createElement('h3', {
    style: {
      fontSize: 'var(--font-size-lg)',
      fontWeight: 'var(--font-weight-bold)',
      color: 'var(--color-text)',
      margin: 0
    }
  }, t ? t('poolInformation') : 'Pool Information'), protocolUrl && React.createElement('a', {
    href: protocolUrl,
    target: '_blank',
    rel: 'noopener noreferrer',
    onClick: e => e.stopPropagation(),
    style: {
      display: 'inline-flex',
      alignItems: 'center',
      gap: '4px',
      padding: '4px 8px',
      background: 'var(--color-background)',
      color: 'var(--color-primary)',
      textDecoration: 'none',
      borderRadius: 'var(--neuro-radius-sm)',
      fontSize: 'var(--font-size-xs)',
      fontWeight: 'var(--font-weight-medium)',
      boxShadow: 'var(--neuro-shadow-subtle)',
      transition: 'all 0.2s ease'
    }
  }, t ? t('protocol') : 'Protocol↗')), React.createElement('div', {
    className: 'pool-info-toggle',
    style: {
      width: '28px',
      height: '28px',
      borderRadius: '50%',
      background: 'var(--color-background)',
      boxShadow: 'var(--neuro-shadow-subtle)',
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'center',
      transition: 'transform 0.3s ease',
      transform: poolInfoExpanded ? 'rotate(180deg)' : 'rotate(0deg)'
    }
  }, '▼')),
  // Expanded Pool Information Content
  poolInfoExpanded && React.createElement('div', {
    className: 'pool-info-content',
    style: {
      animation: 'fadeIn 0.3s ease'
    }
  },
  // APY Breakdown Grid
  React.createElement('div', {
    style: {
      display: 'grid',
      gridTemplateColumns: 'repeat(auto-fit, minmax(140px, 1fr))',
      gap: '12px',
      marginBottom: '20px'
    }
  },
  // APY Breakdown
  pool.apyBase > 0 && pool.apyReward > 0 && React.createElement('div', {
    style: {
      padding: '12px',
      background: 'var(--color-background)',
      borderRadius: 'var(--neuro-radius-sm)',
      boxShadow: 'var(--neuro-shadow-subtle)',
      textAlign: 'center'
    }
  }, React.createElement('div', {
    style: {
      fontSize: 'var(--font-size-xs)',
      color: 'var(--color-text-secondary)',
      marginBottom: '4px',
      textTransform: 'uppercase'
    }
  }, 'Base APY'), React.createElement('div', {
    style: {
      fontSize: 'var(--font-size-base)',
      fontWeight: 'var(--font-weight-bold)',
      color: 'var(--color-text)'
    }
  }, _formatApy(pool.apyBase))), pool.apyBase > 0 && pool.apyReward > 0 && React.createElement('div', {
    style: {
      padding: '12px',
      background: 'var(--color-background)',
      borderRadius: 'var(--neuro-radius-sm)',
      boxShadow: 'var(--neuro-shadow-subtle)',
      textAlign: 'center'
    }
  }, React.createElement('div', {
    style: {
      fontSize: 'var(--font-size-xs)',
      color: 'var(--color-text-secondary)',
      marginBottom: '4px',
      textTransform: 'uppercase'
    }
  }, 'Reward APY'), React.createElement('div', {
    style: {
      fontSize: 'var(--font-size-base)',
      fontWeight: 'var(--font-weight-bold)',
      color: 'var(--color-primary)'
    }
  }, _formatApy(pool.apyReward))),
  // Pool Age (if available)
  React.createElement('div', {
    style: {
      padding: '12px',
      background: 'var(--color-background)',
      borderRadius: 'var(--neuro-radius-sm)',
      boxShadow: 'var(--neuro-shadow-subtle)',
      textAlign: 'center'
    }
  }, React.createElement('div', {
    style: {
      fontSize: 'var(--font-size-xs)',
      color: 'var(--color-text-secondary)',
      marginBottom: '4px',
      textTransform: 'uppercase'
    }
  }, t ? t('poolType') : 'Pool Type'), React.createElement('div', {
    style: {
      fontSize: 'var(--font-size-sm)',
      fontWeight: 'var(--font-weight-medium)',
      color: 'var(--color-text)'
    }
  }, poolType)),
  // 30d Mean APY (if available) — substantiates whether today's rate is stable or a spike
  typeof pool.apyMean30d === 'number' && React.createElement('div', {
    style: {
      padding: '12px',
      background: 'var(--color-background)',
      borderRadius: 'var(--neuro-radius-sm)',
      boxShadow: 'var(--neuro-shadow-subtle)',
      textAlign: 'center'
    }
  }, React.createElement('div', {
    style: {
      fontSize: 'var(--font-size-xs)',
      color: 'var(--color-text-secondary)',
      marginBottom: '4px',
      textTransform: 'uppercase'
    }
  }, t ? t('apyMean30d') : '30d Mean APY'), React.createElement('div', {
    style: {
      fontSize: 'var(--font-size-sm)',
      fontWeight: 'var(--font-weight-medium)',
      color: 'var(--color-text)'
    }
  }, _formatApy(pool.apyMean30d))),
  // Exposure (if available)
  pool.exposure && React.createElement('div', {
    style: {
      padding: '12px',
      background: 'var(--color-background)',
      borderRadius: 'var(--neuro-radius-sm)',
      boxShadow: 'var(--neuro-shadow-subtle)',
      textAlign: 'center'
    }
  }, React.createElement('div', {
    style: {
      fontSize: 'var(--font-size-xs)',
      color: 'var(--color-text-secondary)',
      marginBottom: '4px',
      textTransform: 'uppercase'
    }
  }, t ? t('exposure') : 'Exposure'), React.createElement('div', {
    style: {
      fontSize: 'var(--font-size-sm)',
      fontWeight: 'var(--font-weight-medium)',
      color: 'var(--color-text)',
      textTransform: 'capitalize'
    }
  }, pool.exposure)),
  // IL Risk (if available) — flagged in warning color when present, never hidden
  pool.ilRisk && React.createElement('div', {
    style: {
      padding: '12px',
      background: 'var(--color-background)',
      borderRadius: 'var(--neuro-radius-sm)',
      boxShadow: 'var(--neuro-shadow-subtle)',
      textAlign: 'center'
    }
  }, React.createElement('div', {
    style: {
      fontSize: 'var(--font-size-xs)',
      color: 'var(--color-text-secondary)',
      marginBottom: '4px',
      textTransform: 'uppercase'
    }
  }, t ? t('ilRisk') : 'IL Risk'), React.createElement('div', {
    style: {
      fontSize: 'var(--font-size-sm)',
      fontWeight: 'var(--font-weight-medium)',
      color: pool.ilRisk === 'yes' ? 'var(--color-warning)' : 'var(--color-text)',
      textTransform: 'capitalize'
    }
  }, pool.ilRisk === 'yes' ? t ? t('yes') : 'Yes' : t ? t('no') : 'No'))),
  // Rate-volatility honesty note (071) — full-width, calm. Fires only when
  // the current total APY and the 30-day mean both exist, are > 0, and
  // diverge by >=1.5x (max/min). Conservative: never on missing/zero data.
  typeof pool.apyMean30d === 'number' && (pool.apyBase || 0) + (pool.apyReward || 0) > 0 && pool.apyMean30d > 0 && Math.max((pool.apyBase || 0) + (pool.apyReward || 0), pool.apyMean30d) / Math.min((pool.apyBase || 0) + (pool.apyReward || 0), pool.apyMean30d) >= 1.5 && React.createElement('div', {
    className: 'rate-volatility-note',
    style: {
      background: 'var(--color-background)',
      borderRadius: 'var(--neuro-radius-sm)',
      boxShadow: 'var(--neuro-shadow-subtle)',
      color: 'var(--color-text-secondary)',
      fontSize: 'var(--font-size-sm)',
      lineHeight: '1.5',
      padding: '12px 16px',
      marginBottom: '20px'
    }
  }, t ? t('rateVolatilityNote', _formatApy((pool.apyBase || 0) + (pool.apyReward || 0)), _formatApy(pool.apyMean30d)) : `This pool's rate moves a lot: ${_formatApy((pool.apyBase || 0) + (pool.apyReward || 0))} right now vs a ${_formatApy(pool.apyMean30d)} 30-day average. Reward emissions change daily — projections on this page use the current rate and will move with it.`),
  // Rate-track-record note (088.1) — full-width, calm. Reuses 071's exact
  // neuro styling. Surfaces 087's kpis.historyPoints + kpis.apyStdev as
  // calm cautious-saver language. Yields entirely to the 071 volatility
  // note (same divergence boolean) so the two are mutually exclusive, and
  // renders nothing when kpis are missing (live SEO deep-link landings).
  !(typeof pool.apyMean30d === 'number' && (pool.apyBase || 0) + (pool.apyReward || 0) > 0 && pool.apyMean30d > 0 && Math.max((pool.apyBase || 0) + (pool.apyReward || 0), pool.apyMean30d) / Math.min((pool.apyBase || 0) + (pool.apyReward || 0), pool.apyMean30d) >= 1.5) && pool.kpis && typeof pool.kpis === 'object' && Number(pool.kpis.historyPoints) >= 1 && React.createElement('div', {
    className: 'rate-track-record-note',
    style: {
      background: 'var(--color-background)',
      borderRadius: 'var(--neuro-radius-sm)',
      boxShadow: 'var(--neuro-shadow-subtle)',
      color: 'var(--color-text-secondary)',
      fontSize: 'var(--font-size-sm)',
      lineHeight: '1.5',
      padding: '12px 16px',
      marginBottom: '20px'
    }
  }, function () {
    var _cur = (pool.apyBase || 0) + (pool.apyReward || 0);
    var _k = pool.kpis;
    var hp = Number(_k.historyPoints);
    var stdev = typeof _k.apyStdev === 'number' ? _k.apyStdev : null;
    if (hp < 7) {
      return t ? t('rateTrackRecordNew') : "We're still building this pool's rate history — not a long enough track record yet to judge how steady it is. A longer history makes a rate easier to trust.";
    }
    if (stdev !== null && _cur > 0 && stdev / _cur <= 0.2) {
      return t ? t('rateTrackRecordSteady', hp) : `Steady so far: across the ${hp} days we've tracked it, this pool's rate has stayed close to level. Steadier rates are easier to plan a garden around.`;
    }
    return t ? t('rateTrackRecordTracked', hp) : `We've been tracking this pool's rate for ${hp} days. Watching how a rate holds up over time is one honest way to judge it.`;
  }(),
  // Rate-stability Sharpe annotation (117.1) — one calm extra line under
  // the stdev sentence, reusing the parent note's neuro surface/color/
  // font (inline top-gap only, zero new CSS). Present only when 087's
  // kpis.apySharpe is a finite, sane-magnitude number (null for <8 history
  // points / flat rate). 122: gate on finite AND |value| <= 50 so a stale
  // pre-fix snapshot value (e.g. -900,719,925,474,097.9 from float-dust
  // stdev) is never rendered even before CI regenerates the snapshot.
  typeof pool.kpis.apySharpe === 'number' && Number.isFinite(pool.kpis.apySharpe) && Math.abs(pool.kpis.apySharpe) <= 50 && React.createElement('div', {
    style: {
      marginTop: '6px'
    }
  }, t ? t('rateSharpeNote', _formatNum(pool.kpis.apySharpe)) : `Rate-stability score ${_formatNum(pool.kpis.apySharpe)} (vs a ~4% risk-free rate): how steady this pool's yield has been relative to how much it pays — not a measure of principal safety.`)),
  // Rate-momentum honesty note (103) — full-width, calm. Reuses 071's
  // exact neuro styling. Surfaces 087's kpis.apyMomentum (last − first
  // total APY over the tracked window) as calm cautious-saver language:
  // rising AND falling rates said out loud (degen-honesty precedent).
  // Yields entirely to the 071 volatility note (same divergence boolean)
  // so the two are mutually exclusive, renders nothing when kpis/momentum
  // are missing (live SEO deep-link landings), needs a ≥7-day window, and
  // stays silent below a meaningful move (|momentum| < 0.5, 088.1 covers).
  !(typeof pool.apyMean30d === 'number' && (pool.apyBase || 0) + (pool.apyReward || 0) > 0 && pool.apyMean30d > 0 && Math.max((pool.apyBase || 0) + (pool.apyReward || 0), pool.apyMean30d) / Math.min((pool.apyBase || 0) + (pool.apyReward || 0), pool.apyMean30d) >= 1.5) && pool.kpis && typeof pool.kpis === 'object' && typeof pool.kpis.apyMomentum === 'number' && Number(pool.kpis.historyPoints) >= 7 && Math.abs(pool.kpis.apyMomentum) >= 0.5 && React.createElement('div', {
    className: 'rate-momentum-note',
    style: {
      background: 'var(--color-background)',
      borderRadius: 'var(--neuro-radius-sm)',
      boxShadow: 'var(--neuro-shadow-subtle)',
      color: 'var(--color-text-secondary)',
      fontSize: 'var(--font-size-sm)',
      lineHeight: '1.5',
      padding: '12px 16px',
      marginBottom: '20px'
    }
  }, function () {
    var mom = pool.kpis.apyMomentum;
    var hp = Number(pool.kpis.historyPoints);
    if (mom >= 0.5) {
      return t ? t('rateMomentumRising', _formatApy(Math.abs(mom)), hp) : `This pool's rate has climbed about ${_formatApy(Math.abs(mom))} over the ${hp} days we've tracked it. Rates that rose can slip back just as easily — this page projects on today's rate, not the climb.`;
    }
    return t ? t('rateMomentumFalling', _formatApy(Math.abs(mom)), hp) : `This pool's rate has eased down about ${_formatApy(Math.abs(mom))} over the ${hp} days we've tracked it. Falling rates are normal once reward emissions taper — worth knowing before you plan a garden around today's number.`;
  }()),
  // TVL-trend honesty note (104) — full-width, calm. Reuses 071's exact
  // neuro styling. Surfaces 087's kpis.tvlTrend (signed fraction of the
  // deposit-base change over the tracked window) as calm cautious-saver
  // language: a shrinking pool that still clears the $10M floor is the
  // ICP-relevant risk; a growing pool is one honest sign, never a
  // guarantee. Yields entirely to the 071 volatility note (same
  // divergence boolean) so a volatile pool shows exactly one note,
  // renders nothing when kpis/tvlTrend are missing (live SEO deep-link
  // landings), needs a ≥7-point window, and stays silent below a
  // meaningful move (|tvlTrend| < 0.25).
  !(typeof pool.apyMean30d === 'number' && (pool.apyBase || 0) + (pool.apyReward || 0) > 0 && pool.apyMean30d > 0 && Math.max((pool.apyBase || 0) + (pool.apyReward || 0), pool.apyMean30d) / Math.min((pool.apyBase || 0) + (pool.apyReward || 0), pool.apyMean30d) >= 1.5) && pool.kpis && typeof pool.kpis === 'object' && typeof pool.kpis.tvlTrend === 'number' && Number(pool.kpis.historyPoints) >= 7 && Math.abs(pool.kpis.tvlTrend) >= 0.25 && React.createElement('div', {
    className: 'tvl-trend-note',
    style: {
      background: 'var(--color-background)',
      borderRadius: 'var(--neuro-radius-sm)',
      boxShadow: 'var(--neuro-shadow-subtle)',
      color: 'var(--color-text-secondary)',
      fontSize: 'var(--font-size-sm)',
      lineHeight: '1.5',
      padding: '12px 16px',
      marginBottom: '20px'
    }
  }, function () {
    var tvl = pool.kpis.tvlTrend;
    var hp = Number(pool.kpis.historyPoints);
    var pctStr = _formatNum(Math.round(Math.abs(tvl) * 100)) + '%';
    if (tvl <= -0.25) {
      return t ? t('tvlTrendShrinking', pctStr, hp) : "This pool's deposits have shrunk about " + pctStr + " over the " + hp + " days we've tracked it. A pool can keep clearing our $10M size floor while quietly losing deposits — worth watching for a garden you plan to hold for years.";
    }
    return t ? t('tvlTrendGrowing', pctStr, hp) : "This pool's deposits have grown about " + pctStr + " over the " + hp + " days we've tracked it. More deposits isn't a guarantee, but a pool that's holding or gaining size is one honest sign of staying power.";
  }()),
  // Tokens Section (if available)
  pool.underlyingTokens && pool.underlyingTokens.length > 0 && React.createElement('div', {
    style: {
      marginTop: '16px',
      paddingTop: '16px',
      borderTop: '1px solid rgba(59, 130, 246, 0.1)'
    }
  }, React.createElement('div', {
    style: {
      fontSize: 'var(--font-size-sm)',
      color: 'var(--color-text-secondary)',
      marginBottom: '8px',
      fontWeight: 'var(--font-weight-medium)'
    }
  }, t ? t('underlyingAssets') : 'Underlying Assets'), React.createElement('div', {
    style: {
      display: 'flex',
      flexWrap: 'wrap',
      gap: '6px'
    }
  }, pool.underlyingTokens.map((token, idx) => {
    var isAddress = typeof token === 'string' && token.startsWith('0x') && token.length >= 40;
    if (isAddress) {
      // Derive display symbol from pool.symbol split on '-' or '/'
      var symbolParts = pool.symbol ? pool.symbol.split(/[-\/]/).map(s => s.trim()) : [];
      var addressCount = pool.underlyingTokens.filter(t => typeof t === 'string' && t.startsWith('0x') && t.length >= 40).length;
      var displayLabel = symbolParts.length === addressCount && symbolParts[idx] ? symbolParts[idx] + ' ↗' : `${token.slice(0, 6)}...${token.slice(-4)} ↗`;
      return React.createElement('a', {
        key: idx,
        href: `https://blockscan.com/address/${token}`,
        target: '_blank',
        rel: 'noopener noreferrer',
        title: token,
        style: {
          display: 'inline-flex',
          alignItems: 'center',
          padding: '6px 10px',
          background: 'var(--color-background)',
          color: 'var(--color-primary)',
          borderRadius: 'var(--neuro-radius-sm)',
          fontSize: 'var(--font-size-xs)',
          fontWeight: 'var(--font-weight-medium)',
          boxShadow: 'var(--neuro-shadow-subtle)',
          textDecoration: 'none',
          transition: 'all 0.2s ease',
          fontFamily: 'monospace'
        }
      }, displayLabel);
    }
    return React.createElement('span', {
      key: idx,
      style: {
        padding: '6px 10px',
        background: 'var(--color-background)',
        color: 'var(--color-text)',
        borderRadius: 'var(--neuro-radius-sm)',
        fontSize: 'var(--font-size-xs)',
        fontWeight: 'var(--font-weight-medium)',
        boxShadow: 'var(--neuro-shadow-subtle)'
      }
    }, token);
  }))))),
  // Repeat CTA block (125) — mirrors the hero action card's two north-star
  // CTAs so a reader who has scrolled to the bottom (past Pool Information)
  // can convert without scrolling back up. Reuses .pool-action-card + the
  // hero button/hint classes verbatim (zero new CSS); only additive
  // instrumentation via ctaPlacement:'repeat_footer'. Inherits showConcreteCta
  // (!isAnomalous) so no anomalous projection ever leaks into this CTA.
  React.createElement('div', {
    className: 'pool-action-card',
    style: {
      maxWidth: '420px',
      margin: '32px auto 0'
    }
  }, React.createElement('p', {
    style: {
      fontSize: 'var(--font-size-base)',
      fontWeight: 'var(--font-weight-semibold)',
      color: 'var(--color-text)',
      textAlign: 'center',
      margin: 0
    }
  }, t ? t('repeatCtaHeading') : 'Ready to start this garden?'),
  // Primary CTA — garden this pool (repeat)
  React.createElement('a', {
    className: 'cta-button-primary',
    href: gardenThisPoolHref,
    onClick: () => {
      if (typeof Analytics !== 'undefined') {
        Analytics.trackPoolClick(pool, 'garden_cta', {
          investmentAmount: Math.round(investmentAmount),
          projectionYears: PROJECTION_YEARS,
          ctaVariant: showConcreteCta ? 'concrete' : 'generic',
          ctaPlacement: 'repeat_footer'
        });
      }
    }
  }, showConcreteCta ? t ? t('gardenThisPoolCtaConcrete', projectionAmount, PROJECTION_YEARS) : `Garden this pool → ~$${Math.round(projectionAmount).toLocaleString('en-US')} in ${PROJECTION_YEARS}y` : t ? t('gardenThisPoolCta') : 'Garden this pool →'), React.createElement('p', {
    className: 'pool-action-hint'
  }, t ? t('plannerCtaHint') : 'No wallet needed'),
  // Secondary — protocol link (repeat)
  protocolUrlWithRef && React.createElement('button', {
    className: 'cta-button-protocol',
    onClick: () => {
      if (typeof Analytics !== 'undefined') {
        Analytics.trackPoolClick(pool, 'protocol_link', {
          ctaPlacement: 'repeat_footer'
        });
      }
      window.open(protocolUrlWithRef, '_blank', 'noopener,noreferrer');
    }
  }, t ? t('startEarningOn', pool.project) : `Start Earning on ${pool.project}`, ' ↗'), protocolUrlWithRef && React.createElement('p', {
    className: 'pool-action-hint pool-action-hint--muted'
  }, t ? t('opensProtocol') : 'Opens protocol · Wallet required')));
}

// Simple fade-in animation for calculator
var fadeInStyles = `
@keyframes fadeIn {
  from { opacity: 0; transform: translateY(10px); }
  to { opacity: 1; transform: translateY(0); }
}
`;

// Inject animation styles
if (typeof document !== 'undefined') {
  var styleSheet = document.createElement('style');
  styleSheet.textContent = fadeInStyles;
  document.head.appendChild(styleSheet);
}

// Export for use in main app
if (typeof module !== 'undefined' && module.exports) {
  module.exports = PoolDetail;
}
