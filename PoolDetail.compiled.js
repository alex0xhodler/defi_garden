// GENERATED by compile-app.js from PoolDetail.js — do not hand-edit. Run `npm run compile`.
// Standalone PoolDetail Component - Full Version
var {
  useState,
  useEffect
} = typeof React !== 'undefined' ? React : {};

// Yield-Funded Virtual Card Catalogs & Pure Mathematical Helpers (PRD Design 3)
function calculateMonthlyYield(deposit, netApy) {
  var C = Number(deposit) || 0;
  var apy = Number(netApy) || 0;
  if (C <= 0 || apy <= 0) return 0;
  var r = apy / 100;
  return C * r / 12;
}
function calculateRequiredCapital(monthlyCost, netApy) {
  var B = Number(monthlyCost) || 0;
  var apy = Number(netApy) || 0;
  if (B <= 0) return 0;
  if (apy <= 0) return Infinity;
  var r = apy / 100;
  return Math.ceil(B * 12 / r);
}
function isRungCovered(monthlyYield, monthlyCost) {
  var Y = Number(monthlyYield) || 0;
  var B = Number(monthlyCost) || 0;
  return Y >= B - 0.00001;
}
var STABLE_SYMBOLS = ['USDC', 'USDT', 'DAI', 'USDS', 'FRAX', 'FDUSD', 'PYUSD', 'TUSD', 'BUSD', 'LUSD', 'GUSD', 'CRVUSD', 'USDD', 'USDE', 'SUSD', 'SUSDS', 'EURS', 'EURC'];
function isStableSymbol(symbol) {
  if (!symbol) return false;
  var parts = String(symbol).toUpperCase().split(/[-_\/\s+]/).map(s => s.trim()).filter(Boolean);
  if (parts.length === 0) return false;
  return parts.every(p => STABLE_SYMBOLS.includes(p));
}
var YIELD_CARD_CATALOG_KR = [{
  id: 'baemin_club',
  name: '배민클럽 (배달의민족)',
  monthlyCostKrw: 3990,
  monthlyCostUsd: 2.95,
  domain: 'baemin.com',
  emoji: '🛵',
  category: ['food_delivery', 'lifestyle']
}, {
  id: 'naver_plus',
  name: '네이버플러스 멤버십',
  monthlyCostKrw: 4900,
  monthlyCostUsd: 3.60,
  domain: 'naver.com',
  emoji: '🟢',
  category: ['shopping', 'content']
}, {
  id: 'opencode_go',
  name: 'OpenCode Go',
  monthlyCostKrw: 6800,
  monthlyCostUsd: 5.00,
  domain: 'opencode.ai',
  emoji: '⚡',
  category: ['developer_tool', 'ai_service']
}, {
  id: 'coupang_wow',
  name: '쿠팡 와우 멤버십',
  monthlyCostKrw: 7890,
  monthlyCostUsd: 5.80,
  domain: 'coupang.com',
  emoji: '🚀',
  category: ['shopping', 'delivery']
}, {
  id: 'melon',
  name: '멜론 스트리밍',
  monthlyCostKrw: 10900,
  monthlyCostUsd: 8.10,
  domain: 'melon.com',
  emoji: '🎵',
  category: ['music', 'streaming']
}, {
  id: 'tving',
  name: '티빙 (TVING)',
  monthlyCostKrw: 13500,
  monthlyCostUsd: 10.00,
  domain: 'tving.com',
  emoji: '📺',
  category: ['entertainment', 'streaming']
}, {
  id: 'youtube_kr',
  name: '유튜브 프리미엄',
  monthlyCostKrw: 14900,
  monthlyCostUsd: 11.00,
  domain: 'youtube.com',
  emoji: '▶️',
  category: ['video', 'subscription']
}, {
  id: 'claude_pro',
  name: 'Claude Pro',
  monthlyCostKrw: 29000,
  monthlyCostUsd: 20.00,
  domain: 'claude.ai',
  emoji: '🤖',
  category: ['ai_service', 'software_subscription']
}, {
  id: 'codex_pro',
  name: 'Codex Pro / ChatGPT Plus',
  monthlyCostKrw: 29000,
  monthlyCostUsd: 20.00,
  domain: 'openai.com',
  emoji: '💬',
  category: ['ai_service', 'software_subscription']
}, {
  id: 'cursor_pro',
  name: 'Cursor Pro',
  monthlyCostKrw: 29000,
  monthlyCostUsd: 20.00,
  domain: 'cursor.com',
  emoji: '✨',
  category: ['developer_tool', 'ai_service']
}];
var YIELD_CARD_CATALOG_USD = [{
  id: 'prime_video',
  name: 'Amazon Prime Video',
  monthlyCostUsd: 4.99,
  domain: 'amazon.com',
  emoji: '📦',
  category: ['entertainment', 'streaming']
}, {
  id: 'telegram_prem',
  name: 'Telegram Premium',
  monthlyCostUsd: 4.99,
  domain: 'telegram.org',
  emoji: '✈️',
  category: ['messaging', 'social']
}, {
  id: 'opencode_go',
  name: 'OpenCode Go',
  monthlyCostUsd: 5.00,
  domain: 'opencode.ai',
  emoji: '⚡',
  category: ['developer_tool', 'ai_service']
}, {
  id: 'spotify',
  name: 'Spotify Premium',
  monthlyCostUsd: 11.00,
  domain: 'spotify.com',
  emoji: '🎵',
  category: ['music', 'streaming']
}, {
  id: 'xbox',
  name: 'Xbox Game Pass',
  monthlyCostUsd: 17.00,
  domain: 'xbox.com',
  emoji: '🎮',
  category: ['gaming', 'subscription']
}, {
  id: 'codex_pro',
  name: 'Codex Pro',
  monthlyCostUsd: 20.00,
  domain: 'openai.com',
  emoji: '💬',
  category: ['ai_service', 'software_subscription']
}, {
  id: 'claude_pro',
  name: 'Claude Pro',
  monthlyCostUsd: 20.00,
  domain: 'claude.ai',
  emoji: '🤖',
  category: ['ai_service', 'software_subscription']
}, {
  id: 'cursor_pro',
  name: 'Cursor Pro',
  monthlyCostUsd: 20.00,
  domain: 'cursor.com',
  emoji: '✨',
  category: ['developer_tool', 'ai_service']
}, {
  id: 'openai_api',
  name: 'OpenAI API Tier 2',
  monthlyCostUsd: 50.00,
  domain: 'platform.openai.com',
  emoji: '⚡',
  category: ['developer_tool', 'ai_api']
}];
function getYieldCardCatalog(langOrLocale) {
  var isKo = false;
  if (typeof langOrLocale === 'string') {
    isKo = langOrLocale.toLowerCase().startsWith('ko');
  } else if (typeof window !== 'undefined') {
    var tz = typeof Intl !== 'undefined' && Intl.DateTimeFormat ? Intl.DateTimeFormat().resolvedOptions().timeZone : '';
    var browserLang = (navigator.language || '').toLowerCase();
    var storedLang = typeof localStorage !== 'undefined' && localStorage.getItem('defi-garden-lang') || '';
    isKo = storedLang === 'ko' || browserLang.startsWith('ko') || tz === 'Asia/Seoul';
  }
  return isKo ? YIELD_CARD_CATALOG_KR : YIELD_CARD_CATALOG_USD;
}
function serializeWaitlistPayload(opts) {
  var pool = opts.pool || {};
  var sub = opts.subscription || {};
  var netApy = ((pool.apyBase || 0) + (pool.apyReward || 0)) / 100;
  var randomHex = Math.random().toString(16).substring(2, 10);
  return {
    waitlist_id: `yc_${randomHex}`,
    timestamp: Date.now(),
    user_email: opts.email,
    target_pool: {
      pool_id: pool.pool || null,
      chain: pool.chain || 'DeFi',
      token: pool.symbol || '',
      net_apy: Number(netApy.toFixed(4))
    },
    subscription: {
      id: sub.id || 'custom',
      name: sub.name || 'Virtual Card Subscription',
      monthly_limit_usd: Number((sub.monthlyCostUsd || 20).toFixed(2)),
      merchant_category_lock: sub.category || ['software_subscription', 'ai_service']
    },
    simulated_deposit_usd: Number((Number(opts.depositAmount) || 4000).toFixed(2))
  };
}
function renderEmvChip() {
  return React.createElement('svg', {
    className: 'visa-gold-chip visa-gold-chip-svg',
    viewBox: '0 0 46 34',
    width: 42,
    height: 31,
    'aria-hidden': 'true',
    role: 'img'
  }, React.createElement('defs', null, React.createElement('linearGradient', {
    id: 'emv-metallic-grad',
    x1: '0%',
    y1: '0%',
    x2: '100%',
    y2: '100%'
  }, React.createElement('stop', {
    offset: '0%',
    stopColor: '#ded5c5'
  }), React.createElement('stop', {
    offset: '35%',
    stopColor: '#bfae95'
  }), React.createElement('stop', {
    offset: '70%',
    stopColor: '#d6cbba'
  }), React.createElement('stop', {
    offset: '100%',
    stopColor: '#9e8c72'
  })), React.createElement('linearGradient', {
    id: 'emv-bevel-grad',
    x1: '0%',
    y1: '0%',
    x2: '0%',
    y2: '100%'
  }, React.createElement('stop', {
    offset: '0%',
    stopColor: 'rgba(255,255,255,0.7)'
  }), React.createElement('stop', {
    offset: '100%',
    stopColor: 'rgba(0,0,0,0.3)'
  }))), React.createElement('rect', {
    x: 0.5,
    y: 0.5,
    width: 45,
    height: 33,
    rx: 4.5,
    fill: 'url(#emv-metallic-grad)',
    stroke: 'rgba(30,25,18,0.4)',
    strokeWidth: 0.8
  }), React.createElement('rect', {
    x: 1.2,
    y: 1.2,
    width: 43.6,
    height: 31.6,
    rx: 4,
    fill: 'none',
    stroke: 'url(#emv-bevel-grad)',
    strokeWidth: 0.6
  }), React.createElement('path', {
    d: 'M 13 1 L 13 33 M 33 1 L 33 33 M 1 17 L 13 17 M 33 17 L 45 17 M 13 11.5 C 18 11.5, 28 11.5, 33 11.5 M 13 22.5 C 18 22.5, 28 22.5, 33 22.5 M 19 11.5 L 19 22.5 M 27 11.5 L 27 22.5',
    fill: 'none',
    stroke: 'rgba(50, 40, 25, 0.75)',
    strokeWidth: 0.75,
    strokeLinecap: 'round'
  }));
}
function renderNfcIcon() {
  return React.createElement('svg', {
    className: 'visa-nfc-icon',
    viewBox: '0 0 24 24',
    width: 18,
    height: 18,
    fill: 'none',
    stroke: 'rgba(255,255,255,0.85)',
    strokeWidth: 1.8,
    strokeLinecap: 'round',
    'aria-hidden': 'true'
  }, React.createElement('path', {
    d: 'M7 16a5.5 5.5 0 0 1 0-8'
  }), React.createElement('path', {
    d: 'M11 18.5a9 9 0 0 1 0-13'
  }), React.createElement('path', {
    d: 'M15 21a12.5 12.5 0 0 1 0-18'
  }), React.createElement('path', {
    d: 'M3 13.5a2 2 0 0 1 0-3'
  }));
}
function renderShieldCheckIcon() {
  return React.createElement('svg', {
    className: 'pillar-svg-icon',
    viewBox: '0 0 20 20',
    width: 16,
    height: 16,
    fill: 'none',
    stroke: 'currentColor',
    strokeWidth: 1.6,
    strokeLinecap: 'round',
    strokeLinejoin: 'round',
    'aria-hidden': 'true'
  }, React.createElement('path', {
    d: 'M10 18s6-3 6-7.5V4.5L10 2 4 4.5v6c0 4.5 6 7.5 6 7.5z'
  }), React.createElement('path', {
    d: 'm7.5 10 2 2 3.5-3.5'
  }));
}
function renderLockKeyIcon() {
  return React.createElement('svg', {
    className: 'pillar-svg-icon',
    viewBox: '0 0 20 20',
    width: 16,
    height: 16,
    fill: 'none',
    stroke: 'currentColor',
    strokeWidth: 1.6,
    strokeLinecap: 'round',
    strokeLinejoin: 'round',
    'aria-hidden': 'true'
  }, React.createElement('rect', {
    x: 4,
    y: 9,
    width: 12,
    height: 9,
    rx: 2
  }), React.createElement('path', {
    d: 'M7 9V6a3 3 0 0 1 6 0v3'
  }), React.createElement('circle', {
    cx: 10,
    cy: 13.5,
    r: 1,
    fill: 'currentColor'
  }));
}
function renderPhoneWalletIcon() {
  return React.createElement('svg', {
    className: 'pillar-svg-icon',
    viewBox: '0 0 20 20',
    width: 16,
    height: 16,
    fill: 'none',
    stroke: 'currentColor',
    strokeWidth: 1.6,
    strokeLinecap: 'round',
    strokeLinejoin: 'round',
    'aria-hidden': 'true'
  }, React.createElement('rect', {
    x: 3,
    y: 4,
    width: 14,
    height: 12,
    rx: 2
  }), React.createElement('path', {
    d: 'M3 8h14'
  }), React.createElement('circle', {
    cx: 14,
    cy: 12,
    r: 1,
    fill: 'currentColor'
  }));
}
function renderLockIcon() {
  return React.createElement('svg', {
    className: 'card-lock-icon',
    viewBox: '0 0 16 16',
    width: 11,
    height: 11,
    fill: 'currentColor',
    'aria-hidden': 'true'
  }, React.createElement('path', {
    d: 'M8 1a3.5 3.5 0 0 0-3.5 3.5V6H4a2 2 0 0 0-2 2v6a2 2 0 0 0 2 2h8a2 2 0 0 0 2-2V8a2 2 0 0 0-2-2h-.5V4.5A3.5 3.5 0 0 0 8 1zm2 5H6V4.5a2 2 0 1 1 4 0V6z'
  }));
}
function renderMailIcon() {
  return React.createElement('svg', {
    className: 'input-mail-icon',
    viewBox: '0 0 20 20',
    width: 16,
    height: 16,
    fill: 'none',
    stroke: 'currentColor',
    strokeWidth: 1.5,
    strokeLinecap: 'round',
    strokeLinejoin: 'round',
    'aria-hidden': 'true'
  }, React.createElement('rect', {
    x: 2,
    y: 4,
    width: 16,
    height: 12,
    rx: 1
  }), React.createElement('path', {
    d: 'M2 6l8 5 8-5'
  }));
}
function renderVisaSvg() {
  return React.createElement('svg', {
    className: 'visa-logo-svg',
    viewBox: '0 0 780 250',
    width: 58,
    height: 19,
    fill: '#ffffff',
    'aria-label': 'VISA',
    role: 'img'
  }, React.createElement('path', {
    d: 'M292.5 6.6L193.3 243.4H128L78 57.6C75 45.8 72.4 41.5 62.9 36.3C47.4 27.9 22.2 20.3 0 15.3L3.8 6.6H107.5C121.3 6.6 133.7 15.8 136.8 31.8L163 171.1L228.3 6.6H292.5ZM548.8 167.3C549.4 104.3 461.9 100.8 462.8 72.8C463.2 64.3 471.3 55.2 489.6 52.8C498.7 51.6 523.8 50.6 552.1 63.8L563.3 11.7C548 6.2 528.2 0.8 502.9 0.8C442.2 0.8 399.1 33.1 398.6 79.1C397.7 113.3 428.3 132.3 451.6 143.7C475.6 155.3 483.6 162.8 483.4 173.3C483.1 189.4 463.8 196.4 446 196.7C415 197.2 396.9 188.4 382.4 181.7L370.8 235.8C385.7 242.7 413.2 248.6 441.7 248.9C506 248.9 548.2 217.2 548.8 167.3ZM712.3 243.4H768.8L719.6 6.6H668.1C656.3 6.6 646.6 13.4 642.3 23.8L548.8 243.4H614.3L627.3 207.3H707.4L712.3 243.4ZM645.4 157.6L678.8 65.6L698.1 157.6H645.4ZM387.9 6.6L336.2 243.4H274.6L326.3 6.6H387.9Z'
  }));
}
function YieldCardWidget({
  pool,
  totalApy,
  t,
  formatCurrency,
  formatUsd,
  formatNum,
  formatApy,
  riskAssessment
}) {
  var [depositAmount, setDepositAmount] = useState(4000);
  var [selectedGoalId, setSelectedGoalId] = useState('codex_pro');
  var [email, setEmail] = useState('');
  var [validationError, setValidationError] = useState('');
  var [isSubmitted, setIsSubmitted] = useState(false);
  var [reservedSpot, setReservedSpot] = useState(2481);
  var [linkCopied, setLinkCopied] = useState(false);
  var [tokenPrice, setTokenPrice] = useState(null);
  var isStable = isStableSymbol(pool && pool.symbol);
  useEffect(() => {
    if (isStable) {
      setTokenPrice(1.0);
      return;
    }
    if (!pool || !pool.underlyingTokens || pool.underlyingTokens.length === 0) {
      return;
    }
    var isMounted = true;
    var token = pool.underlyingTokens[0];
    var chainSlug = (pool.chain || 'ethereum').toLowerCase().replace(/\s+/g, '-');
    var coinKey = token.includes(':') ? token : `${chainSlug === 'avalanche' ? 'avax' : chainSlug === 'binance' ? 'bsc' : chainSlug}:${token}`;
    if (typeof fetch !== 'undefined') {
      fetch(`https://coins.llama.fi/prices/current/${coinKey}`).then(res => res.json()).then(data => {
        if (!isMounted) return;
        if (data && data.coins) {
          var matched = data.coins[coinKey] || Object.values(data.coins)[0];
          if (matched && typeof matched.price === 'number' && matched.price > 0) {
            setTokenPrice(matched.price);
          }
        }
      }).catch(() => {});
    }
    return () => {
      isMounted = false;
    };
  }, [pool && pool.pool, pool && pool.symbol, isStable]);
  var _t = t || (k => k);
  var _formatUsd = formatUsd || ((n, f) => '$' + Number(n || 0).toLocaleString('en-US', {
    maximumFractionDigits: f || 2
  }));
  var _formatNum = formatNum || (n => Number(n || 0).toLocaleString('en-US'));
  var isKorean = typeof window !== 'undefined' && (localStorage.getItem('defi-garden-lang') === 'ko' || navigator.language && navigator.language.toLowerCase().startsWith('ko') || typeof Intl !== 'undefined' && Intl.DateTimeFormat && Intl.DateTimeFormat().resolvedOptions().timeZone === 'Asia/Seoul');
  var catalog = getYieldCardCatalog(isKorean ? 'ko' : 'en');
  var selectedSub = catalog.find(i => i.id === selectedGoalId) || catalog[0];
  var monthlyYield = calculateMonthlyYield(depositAmount, totalApy);
  var formatReqCap = (costUsd, costKrw) => {
    var cost = isKorean && costKrw ? costKrw / 1380 : costUsd;
    var req = calculateRequiredCapital(cost, totalApy);
    if (!Number.isFinite(req) || req <= 0) return '—';
    if (req >= 1000) {
      return `$${(req / 1000).toFixed(1)}k`;
    }
    return `$${_formatNum(req)}`;
  };
  var handleSliderChange = e => {
    var val = Number(e.target.value) || 1000;
    setDepositAmount(val);
    if (typeof Analytics !== 'undefined' && Analytics.trackYieldCardSliderChange) {
      Analytics.trackYieldCardSliderChange({
        pool,
        depositAmount: val,
        monthlyYield: calculateMonthlyYield(val, totalApy)
      });
    }
  };
  var handleCardClick = (item, isCovered, reqCap) => {
    if (isCovered) {
      setSelectedGoalId(item.id);
      if (typeof Analytics !== 'undefined' && Analytics.trackYieldCardSubscriptionSelected) {
        Analytics.trackYieldCardSubscriptionSelected({
          pool,
          goalId: item.id,
          monthlyCost: item.monthlyCostUsd,
          isCovered: true
        });
      }
    } else {
      var targetDeposit = Math.min(25000, Math.max(1000, reqCap));
      setDepositAmount(targetDeposit);
      setSelectedGoalId(item.id);
      if (typeof Analytics !== 'undefined') {
        if (Analytics.trackYieldCardSliderChange) {
          Analytics.trackYieldCardSliderChange({
            pool,
            depositAmount: targetDeposit,
            monthlyYield: calculateMonthlyYield(targetDeposit, totalApy)
          });
        }
        if (Analytics.trackYieldCardSubscriptionSelected) {
          Analytics.trackYieldCardSubscriptionSelected({
            pool,
            goalId: item.id,
            monthlyCost: item.monthlyCostUsd,
            isCovered: false
          });
        }
      }
    }
  };
  var handleSubmit = e => {
    e.preventDefault();
    var trimmedEmail = (email || '').trim();
    var emailRegex = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
    if (!emailRegex.test(trimmedEmail)) {
      setValidationError(_t('yieldCard.invalidEmail') || 'Please enter a valid email address.');
      return;
    }
    setValidationError('');
    var payload = serializeWaitlistPayload({
      email: trimmedEmail,
      pool,
      subscription: selectedSub,
      depositAmount
    });
    try {
      if (typeof localStorage !== 'undefined') {
        var existing = [];
        try {
          var raw = localStorage.getItem('defi_garden_card_waitlist');
          if (raw) existing = JSON.parse(raw);
          if (!Array.isArray(existing)) existing = [existing];
        } catch (_) {}
        existing.push(payload);
        localStorage.setItem('defi_garden_card_waitlist', JSON.stringify(existing));
        setReservedSpot(2480 + existing.length);
      }
    } catch (_) {}
    if (typeof Analytics !== 'undefined' && Analytics.trackYieldCardReserved) {
      Analytics.trackYieldCardReserved({
        pool,
        goalId: selectedSub.id,
        depositAmount,
        email_provided: true
      });
    }
    setIsSubmitted(true);
  };
  var handleCopyLink = () => {
    if (typeof navigator !== 'undefined' && navigator.clipboard) {
      navigator.clipboard.writeText(window.location.href);
      setLinkCopied(true);
      setTimeout(() => setLinkCopied(false), 2500);
    }
  };
  return React.createElement('div', {
    id: 'yield-card-widget',
    className: 'yield-card-terminal animate-on-mount'
  },
  // Context alert banner
  React.createElement('div', {
    className: 'yield-card-context-banner'
  }, React.createElement('div', {
    className: 'yield-card-badge-row'
  }, React.createElement('span', {
    className: 'yield-card-badge'
  }, _t('yieldCard.badge') || 'EARLY ACCESS'), React.createElement('span', {
    className: 'yield-card-context-meta'
  }, `${pool.symbol} (${pool.chain || 'DeFi'}) • Net APY: ${Number(totalApy || 0).toFixed(2)}% • Risk: ${riskAssessment ? riskAssessment.level : 'Low'} • TVL: ${formatCurrency(pool.tvlUsd)}`)), React.createElement('h2', {
    className: 'yield-card-title'
  }, _t('yieldCard.title') || 'Yield-Funded Virtual Card'), React.createElement('p', {
    className: 'yield-card-subtitle'
  }, _t('yieldCard.subtitle') || 'Fund software and lifestyle subscriptions with idle yield — keep your principal 100% intact.')),
  // Deposit simulator slider section
  React.createElement('div', {
    className: 'yield-card-slider-section'
  }, React.createElement('div', {
    className: 'yield-card-slider-header'
  }, React.createElement('div', {
    className: 'yield-card-deposit-readout'
  }, React.createElement('span', {
    className: 'readout-label'
  }, _t('yieldCard.simulatedDeposit') || 'Simulated Deposit'), React.createElement('span', {
    className: 'readout-value'
  }, function () {
    var sym = pool.symbol || 'USDC';
    if (isStable) {
      return `$${_formatNum(depositAmount)} ${sym}`;
    }
    if (typeof tokenPrice === 'number' && tokenPrice > 0) {
      var tokenQty = depositAmount / tokenPrice;
      var formattedQty = tokenQty >= 1000 ? _formatNum(Math.round(tokenQty)) : tokenQty >= 1 ? tokenQty.toFixed(2) : tokenQty.toFixed(4);
      return `$${_formatNum(depositAmount)} USD (≈ ${formattedQty} ${sym})`;
    }
    return `$${_formatNum(depositAmount)} USD (${sym})`;
  }()), !isStable && typeof tokenPrice === 'number' && tokenPrice > 0 && React.createElement('span', {
    className: 'yield-card-price-note'
  }, `@ ${_formatUsd(tokenPrice, tokenPrice < 1 ? 4 : 2)} / ${pool.symbol || 'token'}`)), React.createElement('div', {
    className: 'yield-card-yield-readout'
  }, React.createElement('span', {
    className: 'readout-label'
  }, _t('yieldCard.monthlyYield') || 'Monthly Yield Generated'), React.createElement('span', {
    className: 'readout-value yield-card-monthly-yield'
  }, `${_formatUsd(monthlyYield)} ${_t('yieldCard.perMonth') || '/ month'}`))), React.createElement('div', {
    className: 'yield-card-slider-wrapper'
  }, React.createElement('input', {
    type: 'range',
    className: 'yield-card-slider',
    min: '300',
    max: '25000',
    step: '50',
    value: depositAmount,
    onChange: handleSliderChange,
    'aria-label': _t('yieldCard.simulatedDeposit') || 'Simulated Deposit'
  }), React.createElement('div', {
    className: 'yield-card-presets'
  }, [300, 1000, 2000, 4000, 10000, 25000].map(amt => React.createElement('button', {
    key: amt,
    type: 'button',
    className: `yield-preset-btn${depositAmount === amt ? ' is-active' : ''}`,
    onClick: () => {
      setDepositAmount(amt);
      if (typeof Analytics !== 'undefined' && Analytics.trackYieldCardSliderChange) {
        Analytics.trackYieldCardSliderChange({
          pool,
          depositAmount: amt,
          monthlyYield: calculateMonthlyYield(amt, totalApy)
        });
      }
    }
  }, `$${amt >= 1000 ? `${amt / 1000}k` : amt}`))))),
  // Dynamic subscription unlock grid
  React.createElement('div', {
    className: 'yield-card-grid-section'
  }, React.createElement('div', {
    className: 'yield-card-grid'
  }, catalog.map(item => {
    var isCovered = isRungCovered(monthlyYield, item.monthlyCostUsd);
    var isSelected = selectedGoalId === item.id;
    var reqCap = calculateRequiredCapital(item.monthlyCostUsd, totalApy);
    return React.createElement('div', {
      key: item.id,
      className: `yield-card-item ${isCovered ? 'is-covered' : 'is-locked'}${isSelected ? ' is-selected' : ''}`,
      'data-goal-id': item.id,
      onClick: () => handleCardClick(item, isCovered, reqCap)
    }, React.createElement('div', {
      className: 'yield-card-item-top'
    }, React.createElement('img', {
      className: 'yield-card-brand-icon',
      src: `https://www.google.com/s2/favicons?domain=${item.domain}&sz=64`,
      alt: '',
      'aria-hidden': 'true',
      loading: 'lazy',
      onError: ev => {
        var t2 = ev.target;
        if (!t2 || !t2.parentNode) return;
        var s = document.createElement('span');
        s.className = 'yield-card-brand-icon-fallback';
        s.textContent = item.emoji || '⚡';
        t2.parentNode.replaceChild(s, t2);
      }
    }), React.createElement('span', {
      className: 'yield-card-item-name'
    }, item.name)), React.createElement('div', {
      className: 'yield-card-item-bottom'
    }, React.createElement('span', {
      className: 'yield-card-item-price'
    }, isKorean && item.monthlyCostKrw ? `₩${_formatNum(item.monthlyCostKrw)}/mo` : `$${item.monthlyCostUsd.toFixed(2)}/mo`), React.createElement('span', {
      className: `yield-card-status-pill ${isCovered ? 'pill-covered' : 'pill-locked'}`
    }, isCovered ? _t('yieldCard.statusCovered') || '✓ COVERED' : _t('yieldCard.requiresCapital', formatReqCap(item.monthlyCostUsd, item.monthlyCostKrw)) || `Requires ${formatReqCap(item.monthlyCostUsd, item.monthlyCostKrw)}`)));
  }))),
  // Card Preview & Reservation Row
  React.createElement('div', {
    className: 'yield-card-bottom-row'
  },
  // Left: Virtual Visa Card Mockup
  React.createElement('div', {
    className: 'virtual-visa-card-wrapper'
  }, React.createElement('div', {
    className: 'virtual-visa-card'
  },
  // Guilloche lathework pattern background
  React.createElement('div', {
    className: 'visa-card-guilloche',
    'aria-hidden': 'true'
  }), React.createElement('div', {
    className: 'visa-card-specular',
    'aria-hidden': 'true'
  }),
  // Card top row: EMV Chip + NFC Wave (left) & Visa Logo (right)
  React.createElement('div', {
    className: 'visa-card-top-row'
  }, React.createElement('div', {
    className: 'visa-card-chip-group'
  }, renderEmvChip(), renderNfcIcon()), React.createElement('div', {
    className: 'visa-card-brand-group'
  }, renderVisaSvg(), React.createElement('span', {
    className: 'visa-card-type-badge'
  }, 'DEBIT'))),
  // Card center: Masked PAN & Dedicated Spend label
  React.createElement('div', {
    className: 'visa-card-center'
  }, React.createElement('div', {
    className: 'visa-card-pan'
  }, '4242  ••••  ••••  8842'), React.createElement('div', {
    className: 'visa-card-label-sub'
  }, isKorean ? 'DEFI GARDEN • 가상 발급 전용' : 'DEFI GARDEN • VIRTUAL ISSUING'), React.createElement('div', {
    className: 'visa-card-funded-label'
  }, isKorean ? `${selectedSub.name} ${_t('yieldCard.cardDedicatedSuffix') !== 'yieldCard.cardDedicatedSuffix' && _t('yieldCard.cardDedicatedSuffix') || '결제 전용'}` : `${selectedSub.name.toUpperCase()} ${_t('yieldCard.cardFundedSuffix') !== 'yieldCard.cardFundedSuffix' && _t('yieldCard.cardFundedSuffix') || 'FUNDED'}`)),
  // Card bottom row: Expiration, Network info & Spend cap badge
  React.createElement('div', {
    className: 'visa-card-bottom-row'
  }, React.createElement('div', {
    className: 'visa-card-meta-left'
  }, React.createElement('span', {
    className: 'visa-card-expiry'
  }, 'VALID 08/31'), React.createElement('span', {
    className: 'visa-card-network-info'
  }, isKorean ? `${pool.symbol || 'USDC'} • ${Number(totalApy || 0).toFixed(1)}% 이자 직결` : `${pool.symbol || 'USDC'} • ${Number(totalApy || 0).toFixed(1)}% ${_t('yieldCard.liveApyFunded') || 'YIELD FUNDED'}`)), React.createElement('div', {
    className: 'visa-card-cap-badge'
  }, renderLockIcon(), React.createElement('span', null, isKorean && selectedSub.monthlyCostKrw ? _t('yieldCard.cardCapKrw', _formatNum(selectedSub.monthlyCostKrw)) || `월 한도: ₩${_formatNum(selectedSub.monthlyCostKrw)}` : _t('yieldCard.cardCap', selectedSub.monthlyCostUsd.toFixed(2)) || `CAP: $${selectedSub.monthlyCostUsd.toFixed(2)}/MO`))))),
  // Right: Reservation Lead Capture / Receipt
  React.createElement('div', {
    className: 'yield-card-reservation-wrapper'
  }, !isSubmitted ? React.createElement('div', {
    className: 'yield-card-reservation'
  },
  // Live queue indicator
  React.createElement('div', {
    className: 'reservation-queue-status'
  }, React.createElement('span', {
    className: 'queue-pulse-dot'
  }), React.createElement('span', {
    className: 'queue-text'
  }, isKorean ? `⚡ ${reservedSpot.toLocaleString('en-US')}명이 얼리 액세스 대기 중` : `⚡ ${reservedSpot.toLocaleString('en-US')} in launch queue`)), React.createElement('h3', {
    className: 'reservation-title'
  }, _t('yieldCard.reserveTitle') || 'Reserve Virtual Card For This Pool'), React.createElement('p', {
    className: 'reservation-subtitle'
  }, _t('yieldCard.reserveSubtitle') || 'Free to join • Card spends yield, never principal • No wallet required'),
  // 3 Trust Pillars with clean vector iconography
  React.createElement('div', {
    className: 'reservation-pillars'
  }, React.createElement('div', {
    className: 'pillar-item'
  }, React.createElement('div', {
    className: 'pillar-icon'
  }, renderShieldCheckIcon()), React.createElement('div', {
    className: 'pillar-text'
  }, React.createElement('span', {
    className: 'pillar-headline'
  }, isKorean ? '원금 100% 보존' : '100% Principal Protected'), React.createElement('span', {
    className: 'pillar-subline'
  }, isKorean ? '수익금만 자동 결제' : 'Spends yield only'))), React.createElement('div', {
    className: 'pillar-item'
  }, React.createElement('div', {
    className: 'pillar-icon'
  }, renderLockKeyIcon()), React.createElement('div', {
    className: 'pillar-text'
  }, React.createElement('span', {
    className: 'pillar-headline'
  }, isKorean ? '구독처 전용 잠금' : 'Merchant-Locked Routing'), React.createElement('span', {
    className: 'pillar-subline'
  }, isKorean ? '지정 서비스만 승인' : 'Dedicated to selected app'))), React.createElement('div', {
    className: 'pillar-item'
  }, React.createElement('div', {
    className: 'pillar-icon'
  }, renderPhoneWalletIcon()), React.createElement('div', {
    className: 'pillar-text'
  }, React.createElement('span', {
    className: 'pillar-headline'
  }, isKorean ? 'Apple/Google Pay 지원' : 'Apple & Google Pay Ready'), React.createElement('span', {
    className: 'pillar-subline'
  }, isKorean ? '출시 즉시 간편 등록' : 'Direct digital wallet')))), React.createElement('form', {
    className: 'reservation-form',
    noValidate: true,
    onSubmit: handleSubmit
  }, React.createElement('div', {
    className: 'reservation-input-group'
  }, React.createElement('div', {
    className: 'input-with-icon'
  }, renderMailIcon(), React.createElement('input', {
    type: 'email',
    className: 'email-input',
    placeholder: _t('yieldCard.emailPlaceholder') || 'Enter developer / user email...',
    value: email,
    onChange: e => setEmail(e.target.value),
    required: true
  })), React.createElement('button', {
    type: 'submit',
    className: 'reserve-submit-btn'
  }, _t('yieldCard.submitBtn') || 'Issue My Card at Launch →')), validationError && React.createElement('div', {
    className: 'validation-error'
  }, validationError)), React.createElement('p', {
    className: 'reservation-micro-hint'
  }, isKorean ? '지갑 연결이나 KYC 없이 100% 무료 등록 • 출시 즉시 이메일 안내' : 'No wallet connection or KYC required to reserve • 100% free forever')) : React.createElement('div', {
    className: 'yield-card-receipt animate-on-mount'
  }, React.createElement('div', {
    className: 'receipt-icon-badge'
  }, '🌱'), React.createElement('div', {
    className: 'receipt-spot-badge'
  }, _t('yieldCard.spotNumber', reservedSpot) || `Waitlist Spot #${reservedSpot}`), React.createElement('h3', {
    className: 'receipt-title'
  }, _t('yieldCard.receiptTitle') || 'Waitlist Spot Reserved 🌱'), React.createElement('div', {
    className: 'receipt-card-preview-chip'
  }, `${pool.symbol || 'USDC'} Yield Card • ${selectedSub.name} • ${isKorean && selectedSub.monthlyCostKrw ? `₩${_formatNum(selectedSub.monthlyCostKrw)}/mo` : `$${selectedSub.monthlyCostUsd.toFixed(2)}/mo`}`), React.createElement('p', {
    className: 'receipt-note'
  }, _t('yieldCard.receiptNote') || 'We will email you the moment merchant-locked virtual card issuing launches for this pool.'), React.createElement('button', {
    type: 'button',
    className: 'receipt-share-btn',
    onClick: handleCopyLink
  }, linkCopied ? _t('yieldCard.linkCopied') || 'Copied!' : _t('yieldCard.shareLink') || 'Copy share link')))));
}

// Pool type categorization — SINGLE SOURCE OF TRUTH (spec 130).
// These list constants + getPoolTypeShared live here (PoolDetail.js loads
// BEFORE app.js per home.html's script order, so these top-level declarations
// are globals by the time app.js runs). app.js's getPoolType delegates to
// getPoolTypeShared — do not fork a second copy of this classifier.
var LENDING_PROTOCOLS = ['aave', 'aave-v2', 'aave-v3', 'compound', 'compound-v2', 'compound-v3', 'morpho', 'morpho-blue', 'spark', 'sparklend', 'maple', 'euler', 'radiant', 'iron-bank', 'cream', 'benqi-lending', 'venus', 'tectonic', 'moonwell', 'strike', 'granary', 'pac-finance', 'dforce', 'annex', 'sky-lending'];
var DEX_LP_PROTOCOLS = ['uniswap', 'uniswap-v2', 'uniswap-v3', 'curve', 'curve-dex', 'balancer', 'balancer-v2', 'pancakeswap', 'pancakeswap-v2', 'pancakeswap-v3', 'sushiswap', 'quickswap', 'traderjoe', 'spookyswap', 'spiritswap', 'honeyswap', 'dfyn', 'viperswap', 'pangolin', 'lydia', 'defiswap', 'varen', 'levinswap', 'aerodrome', 'aerodrome-slipstream', 'velodrome', 'solidly', 'bancor', 'kyberswap', 'dodoex', '1inch', 'osmosis', 'raydium', 'orca'];
var STAKING_PROTOCOLS = ['lido', 'rocket-pool', 'rocketpool', 'ether.fi', 'ether.fi-stake', 'stakewise', 'jito', 'jito-liquid-staking', 'marinade', 'binance-staked-eth', 'coinbase-wrapped-staked-eth', 'frax', 'frax-ether', 'benqi', 'benqi-staked-avax', 'staked-frax-ether', 'ankr', 'pstake', 'stader', 'chorus-one', 'figment'];
var YIELD_DERIVATIVES_PROTOCOLS = ['pendle', 'spectra', 'spectra-v2', 'spectra-metavaults', 'termmax', 'napier', 'sense', 'notional', 'element'];
var RWA_PROTOCOLS = ['ondo', 'centrifuge', 'goldfinch', 'openeden', 'matrixdock', 'midas-rwa', 'midas', 'usual', 'credix', 'clearpool', 'maple', 'superstate', 'franklin', 'backed', 'hashnote', 'mountain-protocol'];

// Shared pool-type classifier (spec 130) — the complete, single-source logic
// (RWA -> Yield Derivatives -> poolMeta-lending -> LENDING -> DEX -> STAKING ->
// default Yield Farming), used by BOTH the analytics grid (app.js delegates)
// and this pool-detail hero badge.
function getPoolTypeShared(pool) {
  if (!pool.project) return 'Yield Farming';
  var projectName = pool.project.toLowerCase().replace(/\s+/g, '-');

  // Protocol-native RWA / yield-derivative classification wins over the generic
  // lending/dex/staking lists (honest, protocol-derived — spec 091).
  if (RWA_PROTOCOLS.some(protocol => projectName.includes(protocol))) {
    return 'RWA';
  }
  if (YIELD_DERIVATIVES_PROTOCOLS.some(protocol => projectName.includes(protocol))) {
    return 'Yield Derivatives';
  }

  // Check for lending pool indicators
  if (pool.poolMeta && pool.poolMeta.toLowerCase().includes('lending')) {
    return 'Lending';
  }

  // Check against protocol categories
  if (LENDING_PROTOCOLS.some(protocol => projectName.includes(protocol))) {
    return 'Lending';
  }
  if (DEX_LP_PROTOCOLS.some(protocol => projectName.includes(protocol))) {
    return 'LP/DEX';
  }
  if (STAKING_PROTOCOLS.some(protocol => projectName.includes(protocol))) {
    return 'Staking';
  }

  // Default to yield farming for unmatched pools
  return 'Yield Farming';
}

// Classify an underlying-token string for the "Underlying Assets" chip row
// (item 195, root-caused by 193's scanner work — specs/193-notes.md residual
// (b)). blockscan.com resolves EVM addresses only; pointing a Solana mint,
// Tron address or any other non-EVM id at it is a link to a 404, which is
// worse than rendering no link at all. This classifier is the single place
// that decides chip-vs-plain-span AND which explorer (if any) a chip may
// link to, so that guarantee only has to be reasoned about once. Rules are
// evaluated in order; the first match wins. Pure — no DOM, no React.
function classifyUnderlyingToken(token, chain) {
  if (typeof token !== 'string') return {
    chip: false
  };
  var trimmed = token.trim();

  // 1. Chain-prefixed EVM, e.g. "ethereum:0xdac1...1ec7" (Base/Plasma pools).
  var prefixedMatch = trimmed.match(/^[a-z0-9-]+:(0x[0-9a-fA-F]{40,})$/);
  if (prefixedMatch) {
    var address = prefixedMatch[1];
    return {
      chip: true,
      address,
      href: `https://blockscan.com/address/${address}`
    };
  }

  // 2. Bare EVM address (existing behavior, unchanged).
  if (trimmed.startsWith('0x') && trimmed.length >= 40) {
    return {
      chip: true,
      address: trimmed,
      href: `https://blockscan.com/address/${trimmed}`
    };
  }

  // 3. Solana base58 mint.
  if (chain === 'Solana' && /^[1-9A-HJ-NP-Za-km-z]{32,44}$/.test(trimmed)) {
    return {
      chip: true,
      address: trimmed,
      href: `https://solscan.io/token/${trimmed}`
    };
  }

  // 4. Tron base58 token address ("T" + 33 chars).
  if (chain === 'Tron' && /^T[1-9A-HJ-NP-Za-km-z]{33}$/.test(trimmed)) {
    return {
      chip: true,
      address: trimmed,
      href: `https://tronscan.org/#/token20/${trimmed}`
    };
  }

  // 5. Generic opaque id (Stellar Soroban contract ids, Stacks principals,
  // and any other chain we don't have an explorer map for yet). Long enough
  // to be an address, not a human-readable slug — chip it for the truncation
  // treatment, but never link it: we don't know where it resolves.
  if (trimmed.length >= 32 && !/\s/.test(trimmed)) {
    return {
      chip: true,
      address: trimmed,
      href: null
    };
  }

  // 6. Anything else (e.g. "coingecko:openeden-tbill") — a short readable
  // slug, not an address. Left as plain, untouched, full text.
  return {
    chip: false
  };
}

// Drawn disclosure chevron (247 world — craft floor: icons are drawn, never
// unicode glyphs). Stroke inherits currentColor; rotation is CSS-owned via
// the panel's .expanded class.
function renderChevronIcon() {
  return React.createElement('svg', {
    width: 12,
    height: 12,
    viewBox: '0 0 12 12',
    'aria-hidden': 'true',
    focusable: 'false'
  }, React.createElement('path', {
    d: 'M2.5 4.25 L6 7.75 L9.5 4.25',
    fill: 'none',
    stroke: 'currentColor',
    strokeWidth: 1.5,
    strokeLinecap: 'round',
    strokeLinejoin: 'round'
  }));
}
function PoolDetail({
  pool,
  onBack,
  calculateYields,
  futureValue,
  formatCurrency,
  formatAPY,
  formatUsd,
  formatNum,
  formatApy,
  getProtocolUrl,
  getProtocolUrlWithRef,
  t,
  AnimatedNumber
}) {
  // Fallback formatters when not passed (e.g. SSR/tests)
  var _formatUsd = formatUsd || ((n, f) => '$' + Number(n || 0).toLocaleString('en-US', {
    maximumFractionDigits: f || 2
  }));
  var _formatNum = formatNum || (n => Number(n || 0).toLocaleString('en-US'));
  var _formatApy = formatApy || (pct => Number(pct || 0).toLocaleString('en-US', {
    maximumFractionDigits: 2
  }) + '%');
  var _futureValue = futureValue || ((monthly, annualRatePct, years) => {
    var P = Number(monthly) || 0;
    var months = Math.round((Number(years) || 0) * 12);
    var r = (Number(annualRatePct) || 0) / 100;
    if (r === 0) return P * months;
    var rm = r / 12;
    return P * ((Math.pow(1 + rm, months) - 1) / rm);
  });
  var [investmentAmount, setInvestmentAmount] = useState(1000);
  var [showAPYBreakdown, setShowAPYBreakdown] = useState(false);
  var [calculatorExpanded, setCalculatorExpanded] = useState(false);
  var [poolInfoExpanded, setPoolInfoExpanded] = useState(true);
  var [activeCalculatorTab, setActiveCalculatorTab] = useState('30days');
  var [isPulsing, setIsPulsing] = useState(false);
  // Spec 207 settle gate: whether the async 105 kpi-snapshot backfill (app.js)
  // has had time to land before we decide the pool truly has no rate history.
  var [historyLookupSettled, setHistoryLookupSettled] = useState(false);
  useEffect(() => {
    if (investmentAmount > 0) {
      setIsPulsing(true);
      var timer = setTimeout(() => setIsPulsing(false), 300);
      return () => clearTimeout(timer);
    }
  }, [investmentAmount]);

  // Spec 207 settle gate. app.js's 105 effect fetches /data/pools-snapshot.json
  // and merges kpis onto the detail pool asynchronously for the ~11.4% of
  // deep-linked pools present in that snapshot. Without this gate, the new
  // "no rate history" note (below) would flash for those pools before 105's
  // backfill lands and the real 088.1 track-record note replaces it — a
  // visible regression on the exact path 207 requires stay behaviour-
  // unchanged. So: reset on pool change, and only declare "settled" (safe to
  // render the no-history note) 1s after a kpis-less pool mounts, giving 105
  // a window to backfill first. Pools that already have kpis need no wait.
  useEffect(() => {
    setHistoryLookupSettled(false);
    if (!(pool && pool.kpis)) {
      var timer = setTimeout(() => setHistoryLookupSettled(true), 1000);
      return () => clearTimeout(timer);
    }
  }, [pool && pool.pool, pool && !!pool.kpis]);
  if (!pool) {
    return React.createElement('div', {
      className: 'pool-detail-empty'
    }, React.createElement('p', null, 'No pool selected'), React.createElement('button', {
      className: 'back-button',
      onClick: () => {
        // Analytics tracking for back navigation from pool detail
        if (typeof Analytics !== 'undefined') {
          Analytics.trackNavigation('pool-detail', 'search', 'back_button');
        }
        onBack();
      }
    }, t ? t('backToSearch') : '← Back to Search'));
  }
  var totalApy = (pool.apyBase || 0) + (pool.apyReward || 0);
  var yields = calculateYields(investmentAmount, totalApy);
  var protocolUrl = getProtocolUrl(pool);
  var protocolUrlWithRef = getProtocolUrlWithRef(pool);

  // spec 182 leg D — renders EITHER the existing protocol CTA (byte-identical
  // behaviour/copy) when protocolUrlWithRef resolves, OR an honest DefiLlama
  // fallback when every getProtocolUrl() tier returns null (sdai today, and
  // any future true-null protocol). Reuse over duplication (2026-07-10
  // directive): one helper used at both the hero and repeat-footer CTA
  // sites, which differ only by `placement` ('hero' | 'repeat_footer') — that
  // becomes both the ctaPlacement analytics prop and this function's arg.
  // Returns an array of the two child elements (button + hint) so a call
  // site can splice them in with `...renderProtocolCtaBlock('hero')`.
  function renderProtocolCtaBlock(placement) {
    if (protocolUrlWithRef) {
      return [React.createElement('button', {
        className: 'cta-button-protocol',
        onClick: () => {
          if (typeof Analytics !== 'undefined') {
            Analytics.trackPoolClick(pool, 'protocol_link', {
              ctaPlacement: placement
            });
          }
          window.open(protocolUrlWithRef, '_blank', 'noopener,noreferrer');
        }
      }, t ? t('startEarningOn', pool.project) : `Start Earning on ${pool.project}`, ' ↗'), React.createElement('p', {
        className: 'pool-action-hint pool-action-hint--muted'
      }, t ? t('opensProtocol') : 'Opens protocol · Wallet required')];
    }

    // True-null case (spec 182 leg B): an honest labelled link to the pool's
    // DefiLlama page instead of empty space. Must NOT impersonate the
    // protocol CTA (different copy) and must NOT inflate the north star:
    // fires 'defillama_fallback', never 'protocol_link' — reusing that value
    // here would silently redefine the north-star metric. Reuses
    // `.cta-button-protocol` / `.pool-action-hint--muted` verbatim — zero new
    // CSS (Territory note T5 — both classes already exist for exactly this).
    var defillamaUrl = 'https://defillama.com/yields/pool/' + encodeURIComponent(pool.pool);
    return [React.createElement('button', {
      className: 'cta-button-protocol',
      onClick: () => {
        if (typeof Analytics !== 'undefined') {
          Analytics.trackPoolClick(pool, 'defillama_fallback', {
            ctaPlacement: placement
          });
        }
        window.open(defillamaUrl, '_blank', 'noopener,noreferrer');
      }
    }, t ? t('viewOnDefillama') : 'View this pool on DefiLlama', ' ↗'), React.createElement('p', {
      className: 'pool-action-hint pool-action-hint--muted'
    }, t ? t('opensDefillamaFallback') : 'No protocol link available · Opens DefiLlama, our data source')];
  }

  // Determine pool type (must be defined before getRiskAssessment) — single
  // shared classifier (spec 130); same categories as the analytics grid.
  var poolType = getPoolTypeShared(pool);
  var APY_SANITY_LIMIT_LOCAL = 1000; // mirror of app.js constant

  // 144: apyMean30d is presented as fact; anything outside the trust rail is not
  // presentable. Magnitude gate (not isFinite alone) — 122's precedent: garbage
  // values are finite.
  var mean30dSane = typeof pool.apyMean30d === 'number' && Number.isFinite(pool.apyMean30d) && pool.apyMean30d >= 0 && pool.apyMean30d <= APY_SANITY_LIMIT_LOCAL;

  // Comprehensive Risk Assessment
  var getRiskAssessment = () => {
    var riskScore = 0;
    var factors = [];

    // Anomalous APY override — force High risk immediately
    if (totalApy > APY_SANITY_LIMIT_LOCAL) {
      factors.push('Anomalous yield');
      return {
        level: t ? t('highRisk') : 'High',
        color: 'var(--color-error)',
        description: 'Anomalous yield — extreme caution',
        factors,
        score: 100
      };
    }

    // TVL Factor (40% weight)
    if (pool.tvlUsd < 1000000) {
      riskScore += 40;
      factors.push('Low liquidity');
    } else if (pool.tvlUsd < 10000000) {
      riskScore += 20;
      factors.push('Medium liquidity');
    } else {
      factors.push('High liquidity');
    }

    // APY Factor (30% weight) - Higher APY = Higher risk
    if (totalApy > 50) {
      riskScore += 30;
      factors.push('Very high yield');
    } else if (totalApy > 20) {
      riskScore += 20;
      factors.push('High yield');
    } else if (totalApy > 10) {
      riskScore += 10;
      factors.push('Elevated yield');
    }

    // Pool Age/Maturity Factor (20% weight)
    var isNewProtocol = ['jito', 'ether.fi', 'pendle', 'eigenlayer'].some(p => pool.project?.toLowerCase().includes(p));
    if (isNewProtocol) {
      riskScore += 15;
      factors.push('Newer protocol');
    }

    // Pool Type Factor (10% weight)
    if (poolType === 'LP/DEX') {
      riskScore += 10;
      factors.push('Impermanent loss risk');
    } else if (poolType === 'Lending') {
      riskScore += 5;
      factors.push('Credit risk');
    }

    // Determine overall risk level
    var level, color, description;
    if (riskScore <= 25) {
      level = t ? t('lowRisk') : 'Low';
      color = 'var(--color-success)';
      description = 'Conservative DeFi strategy';
    } else if (riskScore <= 50) {
      level = t ? t('mediumRisk') : 'Medium';
      color = 'var(--color-warning)';
      description = 'Moderate risk profile';
    } else {
      level = t ? t('highRisk') : 'High';
      color = 'var(--color-error)';
      description = 'Advanced DeFi strategy';
    }
    return {
      level,
      color,
      description,
      factors,
      score: riskScore
    };
  };
  var riskAssessment = getRiskAssessment();

  // Persona this pool's risk tier maps to in the planner (stable/rwa/degen —
  // mirrors planner.js's PERSONAS thresholds, same 25/50 risk-score bands
  // getRiskAssessment already uses above). Drives the "Garden this pool"
  // deep link and whether the projection below applies the degen haircut.
  var gardenPersona = riskAssessment.score <= 25 ? 'stable' : riskAssessment.score <= 50 ? 'rwa' : 'degen';
  var isAnomalous = totalApy > APY_SANITY_LIMIT_LOCAL;
  var applyDegenHaircut = gardenPersona === 'degen';
  var PROJECTION_YEARS = 5;
  var projectionApy = applyDegenHaircut ? totalApy / 3 : totalApy;
  // Projection is tied to the calculator's investment amount (lump sum, default
  // $1,000) so the number below AND the CTA react to what the user selects.
  // Lump-sum compound growth: principal * (1 + r)^years.
  var projectionAmount = investmentAmount * Math.pow(1 + projectionApy / 100, PROJECTION_YEARS);
  // Prefill the planner with the SAME lump sum + horizon so it lands on exactly
  // this projection (as capital, matching the calculator). The deep link never
  // carries the pool's APY — the planner computes from its own sanity-capped rate.
  var gardenThisPoolHref = `plan.html?goal=retirement&pace=${gardenPersona}&capital=${Math.round(investmentAmount)}&fm=capital&years=${PROJECTION_YEARS}&src=pool`;
  // Concrete CTA (025): show the projected outcome ON the button — but NEVER for
  // an anomalous pool (trust rail: anomalous rates are flagged, never hyped).
  var showConcreteCta = !isAnomalous;

  // BreadcrumbList JSON-LD (040): mirrors the visual breadcrumb above
  // (Search Results -> <SYMBOL> Pool) without touching its markup/behavior.
  var origin = typeof window !== 'undefined' && window.location && window.location.origin || 'https://www.defi.garden';
  var poolUrl = `${origin}/?pool=${encodeURIComponent(pool.pool || '')}`;
  var breadcrumbJsonLd = JSON.stringify({
    '@context': 'https://schema.org',
    '@type': 'BreadcrumbList',
    itemListElement: [{
      '@type': 'ListItem',
      position: 1,
      name: 'Search Results',
      item: `${origin}/?app=1`
    }, {
      '@type': 'ListItem',
      position: 2,
      name: `${pool.symbol} Pool`,
      item: poolUrl
    }]
  }).replace(/</g, '\\u003c');

  // FinancialProduct JSON-LD (AEO/GEO & GSC metadata) — defensive, trust-railed
  var financialProductJsonLd = JSON.stringify({
    '@context': 'https://schema.org',
    '@type': 'FinancialProduct',
    name: `${pool.project || 'DeFi'} ${pool.symbol || ''} Pool on ${pool.chain || 'DeFi'}`,
    description: `Real-time yield pool on ${pool.project || 'DeFi'} (${pool.chain || 'DeFi'}) with ${_formatApy(totalApy)} APY and ${formatCurrency(pool.tvlUsd)} TVL.`,
    ...(showConcreteCta ? {
      annualPercentageRate: Number(Number(totalApy || 0).toFixed(2)),
      interestRate: Number(Number(totalApy || 0).toFixed(2))
    } : {}),
    provider: {
      '@type': 'Organization',
      name: pool.project || 'DeFi Protocol'
    },
    currency: 'USD',
    category: poolType,
    url: poolUrl
  }).replace(/</g, '\\u003c');

  // Dataset JSON-LD (Rich Data metadata)
  var poolDatasetJsonLd = JSON.stringify({
    '@context': 'https://schema.org',
    '@type': 'Dataset',
    name: `${pool.symbol || ''} ${pool.project || ''} Yield Data (${pool.chain || ''})`,
    description: `Live DefiLlama yield data for ${pool.symbol || ''} on ${pool.project || ''} (${pool.chain || ''}) with ${formatCurrency(pool.tvlUsd)} TVL.`,
    url: poolUrl,
    creator: {
      '@type': 'Organization',
      name: 'DeFi Garden',
      url: origin
    },
    publisher: {
      '@type': 'Organization',
      name: 'DeFi Garden',
      url: origin
    }
  }).replace(/</g, '\\u003c');

  // 247 world: layout is owned by pool-detail-styles.css (the certificate
  // document column) — no inline layout styles on the container.
  return React.createElement('div', {
    className: 'pool-detail-container'
  }, React.createElement('script', {
    type: 'application/ld+json',
    dangerouslySetInnerHTML: {
      __html: breadcrumbJsonLd
    }
  }), React.createElement('script', {
    type: 'application/ld+json',
    dangerouslySetInnerHTML: {
      __html: financialProductJsonLd
    }
  }), React.createElement('script', {
    type: 'application/ld+json',
    dangerouslySetInnerHTML: {
      __html: poolDatasetJsonLd
    }
  }),
  // Header — app.js renders the SAME full-width `.app-header-sticky` band
  // used by the grid immediately above this component (logo + controls +
  // search bar, pre-filled with the current context — spec 247 search-as-
  // navigation). Do not reintroduce a pool-view-local header or back link
  // here: the query IS the navigation state now — submit a new one to
  // search, clear it to return to these results — which retires the old
  // "← Back to Search" breadcrumb link entirely (`onBack` is still wired
  // for the defensive `!pool` empty state above, unreachable in normal use).

  // Hero — 225 round 3c recomposition: ONE composed panel. Identity (left)
  // and the headline metric with its honest qualifier (right) sit in one
  // deliberate grid relationship; a single action band closes the panel.
  // The old detached right-hand .pool-action-card, the decorative gradient
  // overlay, and the gradient-text APY are gone (craft-floor bans).
  React.createElement('div', {
    // 247 world: `is-anomalous` carries the trust rail into the engraving —
    // the sheet's security work prints in caution ink when the rate is past
    // the sanity limit. Purely presentational; the rail itself (the carmine
    // rate, the warning slip, forced High risk) is unchanged.
    className: `pool-hero-card animate-on-mount${isAnomalous ? ' is-anomalous' : ''}`
  }, React.createElement('div', {
    className: 'pool-hero-content'
  },
  // Identity column (was className 'pool-info-section' — renamed: that
  // class belongs to the Pool Information panel below and the collision
  // made both unstyleable independently).
  React.createElement('div', {
    className: 'pool-hero-identity'
  }, React.createElement('h1', {
    className: 'pool-symbol-hero'
  }, pool.symbol),
  // One quiet metadata line: protocol · chain. No accent at rest
  // (One Voice rule), no wrap-prone dot spans.
  React.createElement('div', {
    className: 'pool-meta-simplified'
  }, React.createElement('span', {
    className: 'protocol-name'
  }, pool.project), React.createElement('span', {
    className: 'separator'
  }, ' · '), React.createElement('span', {
    className: 'chain-name'
  }, pool.chain)),
  // Metadata chips: type + trust row as ONE group, one chip language.
  React.createElement('div', {
    className: 'trust-indicators'
  }, React.createElement('div', {
    className: 'pool-type-badge-hero hero-chip'
  }, poolType), React.createElement('div', {
    className: 'trust-badge hero-chip',
    style: {
      color: 'var(--color-success)'
    }
  }, t ? t('verified') : '✓ Verified'), React.createElement('div', {
    className: 'tvl-badge hero-chip'
  }, AnimatedNumber ? React.createElement(AnimatedNumber, {
    value: pool.tvlUsd,
    formatFn: v => formatCurrency(v) + ' TVL',
    duration: 1200
  }) : formatCurrency(pool.tvlUsd) + ' TVL'),
  // Risk chip — riskAssessment.description stays reachable via the
  // title attribute; text colored by riskAssessment.color.
  React.createElement('div', {
    className: 'trust-badge hero-chip',
    title: riskAssessment.description,
    style: {
      color: riskAssessment.color
    }
  }, `${t ? t('riskAssessment') : 'Risk Assessment'}: ${riskAssessment.level}`))),
  // Headline metric — the number and its honest qualifier are one unit.
  React.createElement('div', {
    className: 'pool-hero-metric'
  }, React.createElement('div', {
    className: `pool-action-apy${totalApy === 0 ? ' is-zero' : ''}`
  }, React.createElement('div', {
    className: 'pool-action-apy-label'
  }, t ? t('totalApy') : 'Total APY'), React.createElement('div', {
    className: 'apy-value-hero'
  }, AnimatedNumber ? React.createElement(AnimatedNumber, {
    value: (pool.apyBase || 0) + (pool.apyReward || 0),
    formatFn: v => _formatApy(v),
    duration: 1500
  }) : formatAPY(pool.apyBase, pool.apyReward)), pool.apyBase > 0 && pool.apyReward > 0 && React.createElement('div', {
    className: 'pool-action-apy-breakdown'
  }, React.createElement('span', null, t ? t('baseApyBreakdown', _formatApy(pool.apyBase).replace('%', '')) : `${_formatApy(pool.apyBase)} Base`), React.createElement('span', {
    className: 'pool-action-apy-sep'
  }, ' · '), React.createElement('span', null, t ? t('rewardApyBreakdown', _formatApy(pool.apyReward).replace('%', '')) : `+${_formatApy(pool.apyReward)} Rewards`))),
  // Rate-quality note tier (210 A3) — the number's honest qualifier,
  // rendered as plain quiet text directly under the APY it qualifies
  // (225 round 3c: the disconnected gray wells are gone; classes,
  // conditions and copy unchanged). Mutually-exclusive tier chain —
  // exactly one of the three renders. LEFT IN Pool Information
  // (unmoved): .rate-momentum-note (103) and .tvl-trend-note (104).

  // Rate-volatility honesty note (071).
  mean30dSane && (pool.apyBase || 0) + (pool.apyReward || 0) > 0 && pool.apyMean30d > 0 && Math.max((pool.apyBase || 0) + (pool.apyReward || 0), pool.apyMean30d) / Math.min((pool.apyBase || 0) + (pool.apyReward || 0), pool.apyMean30d) >= 1.5 && React.createElement('div', {
    className: 'rate-volatility-note pool-hero-qualifier'
  }, t ? t('rateVolatilityNote', _formatApy((pool.apyBase || 0) + (pool.apyReward || 0)), _formatApy(pool.apyMean30d)) : `This pool's rate moves a lot: ${_formatApy((pool.apyBase || 0) + (pool.apyReward || 0))} right now vs a ${_formatApy(pool.apyMean30d)} 30-day average. Reward emissions change daily — projections on this page use the current rate and will move with it.`),
  // Rate-track-record note (088.1).
  !(mean30dSane && (pool.apyBase || 0) + (pool.apyReward || 0) > 0 && pool.apyMean30d > 0 && Math.max((pool.apyBase || 0) + (pool.apyReward || 0), pool.apyMean30d) / Math.min((pool.apyBase || 0) + (pool.apyReward || 0), pool.apyMean30d) >= 1.5) && pool.kpis && typeof pool.kpis === 'object' && Number(pool.kpis.historyPoints) >= 1 && React.createElement('div', {
    className: 'rate-track-record-note pool-hero-qualifier'
  }, function () {
    var _cur = (pool.apyBase || 0) + (pool.apyReward || 0);
    var _k = pool.kpis;
    var hp = Number(_k.historyPoints);
    var stdev = typeof _k.apyStdev === 'number' ? _k.apyStdev : null;
    if (hp < 7) {
      return t ? t('rateTrackRecordNew') : "We're still building this pool's rate history — not a long enough track record yet to judge how steady it is. A longer history makes a rate easier to trust.";
    }
    if (stdev !== null && _cur > 0 && stdev / _cur <= 0.2) {
      return t ? t('rateTrackRecordSteady', hp) : `Steady so far: across the ${hp} days we've tracked it, this pool's rate has stayed close to level. Steadier rates are easier to plan a garden around.`;
    }
    return t ? t('rateTrackRecordTracked', hp) : `We've been tracking this pool's rate for ${hp} days. Watching how a rate holds up over time is one honest way to judge it.`;
  }()),
  // Rate-history-unavailable note (207).
  !(mean30dSane && (pool.apyBase || 0) + (pool.apyReward || 0) > 0 && pool.apyMean30d > 0 && Math.max((pool.apyBase || 0) + (pool.apyReward || 0), pool.apyMean30d) / Math.min((pool.apyBase || 0) + (pool.apyReward || 0), pool.apyMean30d) >= 1.5) && !(pool.kpis && typeof pool.kpis === 'object') && historyLookupSettled && React.createElement('div', {
    className: 'rate-history-unavailable-note pool-hero-qualifier'
  }, t ? t('rateHistoryUnavailable') : "We don't have a rate history for this pool — we track rates day by day only for the largest pools, so there's nothing here to judge how steady this one has been. The rate above is live from DefiLlama."))),
  // Action band — closes the hero panel. ONE primary action; the protocol
  // link reads as the clear secondary (quiet-link treatment via CSS,
  // markup/events/payloads untouched).
  React.createElement('div', {
    className: 'pool-hero-actions'
  }, React.createElement('div', {
    className: 'pool-hero-action-primary'
  },
  // Primary CTA — garden this pool (deep-links into the planner
  // prefilled with a persona/goal/monthly matching this pool's risk
  // tier). Hero label stays the plain generic string (210); the ~$X
  // in 5y concrete projection lives at the earnings-block echo where
  // the user has parameterised the input.
  React.createElement('a', {
    className: 'cta-button-primary',
    href: '#yield-card-widget',
    onClick: e => {
      e.preventDefault();
      var el = document.getElementById('yield-card-widget') || document.querySelector('.yield-card-terminal');
      if (el) {
        el.scrollIntoView({
          behavior: 'smooth',
          block: 'start'
        });
      }
      if (typeof Analytics !== 'undefined') {
        Analytics.trackPoolClick(pool, 'spend_yield_cta', {
          investmentAmount: Math.round(investmentAmount),
          projectionYears: PROJECTION_YEARS,
          ctaVariant: 'card_widget_scroll',
          ctaPlacement: 'hero',
          ctaPosition: 'hero'
        });
      }
    }
  }, t ? t('spendYieldCta') : 'Spend yield →'), React.createElement('p', {
    className: 'pool-action-hint'
  }, t ? t('spendYieldHint') : 'Fund subscriptions • Keep principal intact')),
  // Secondary — protocol link, or an honest DefiLlama fallback when
  // no protocol URL resolves at all (spec 182 leg B/D).
  React.createElement('div', {
    className: 'pool-hero-action-secondary'
  }, ...renderProtocolCtaBlock('hero')))),
  // Engraved rule between hero and Yield Card Terminal
  React.createElement('div', {
    className: 'cert-divider',
    'aria-hidden': 'true'
  }, React.createElement('span', {
    className: 'orn-band cert-divider-strand'
  }), React.createElement('span', {
    className: 'cert-divider-node'
  }), React.createElement('span', {
    className: 'orn-band cert-divider-strand'
  })),
  // Yield-Funded Virtual Card Terminal (PRD Design 3)
  React.createElement(YieldCardWidget, {
    pool: pool,
    totalApy: totalApy,
    t: t,
    formatCurrency: formatCurrency,
    formatUsd: _formatUsd,
    formatNum: _formatNum,
    formatApy: _formatApy,
    riskAssessment: riskAssessment
  }),
  // Engraved rule between the document's clauses (247 world). Decorative
  // only: aria-hidden, no text, and the ornament primitives are
  // pointer-events: none, so this can never sit between a user and a
  // control.
  React.createElement('div', {
    className: 'cert-divider',
    'aria-hidden': 'true'
  }, React.createElement('span', {
    className: 'orn-band cert-divider-strand'
  }), React.createElement('span', {
    className: 'cert-divider-node'
  }), React.createElement('span', {
    className: 'orn-band cert-divider-strand'
  })),
  // Collapsible Yield Calculator — now the single "your garden" earnings
  // block (210 B). The standalone pool-projection-card ("THE LONG GAME")
  // and the entire quick-metrics grid (daily card, monthly card, risk card)
  // that used to render here as top-level sections are gone; their content
  // (risk -> hero trust-indicators chip, projection copy -> inside this
  // block below the input, daily/monthly -> the 1D/7D/30D toggle result)
  // now lives input-first inside calculator-compact's expanded content.
  React.createElement('div', {
    className: `calculator-compact animate-on-mount ${calculatorExpanded ? 'expanded' : ''}`
  },
  // Calculator Header — 247 world: geometry/spacing owned by CSS; the
  // disclosure chevron is a drawn SVG (craft floor: no glyph icons).
  React.createElement('div', {
    className: 'calculator-header',
    onClick: () => setCalculatorExpanded(!calculatorExpanded)
  }, React.createElement('div', null, React.createElement('div', {
    className: 'pool-section-title'
  }, t ? t('calculateYourEarnings') : 'Calculate Your Earnings'), React.createElement('div', {
    className: 'pool-section-sub'
  }, t ? t('calcSubPrompt') : 'See your daily, weekly & monthly returns')), React.createElement('div', {
    className: 'calculator-toggle'
  }, renderChevronIcon())),
  // Collapsed action row — quick scroll to the Yield-Funded Virtual Card terminal
  !calculatorExpanded && React.createElement('div', {
    className: 'calc-collapsed-action-row'
  }, React.createElement('button', {
    type: 'button',
    className: 'calc-scroll-up-btn',
    onClick: e => {
      e.stopPropagation();
      var el = document.getElementById('yield-card-widget') || document.querySelector('.yield-card-terminal');
      if (el) el.scrollIntoView({
        behavior: 'smooth'
      });
    }
  }, '↑ Explore Yield-Funded Virtual Card')),
  // Expanded Calculator Content
  calculatorExpanded && React.createElement('div', {
    className: 'calculator-content',
    style: {
      animation: 'fadeIn 0.3s ease'
    }
  },
  // Principal line (247 world) — the amount written on the document's
  // rule; denominations as stamped presets. All geometry/hover states
  // owned by CSS classes; events and analytics payloads unchanged.
  React.createElement('div', {
    className: 'investment-input-group'
  }, React.createElement('div', {
    className: 'input-wrapper'
  }, React.createElement('span', {
    className: 'currency-symbol'
  }, '$'), React.createElement('input', {
    type: 'number',
    className: 'amount-input',
    value: investmentAmount,
    'aria-label': t ? t('calculateYourEarnings') : 'Investment amount',
    onChange: e => {
      var newAmount = Number(e.target.value) || 0;
      setInvestmentAmount(newAmount);
      // Analytics tracking for yield calculation
      if (typeof Analytics !== 'undefined' && newAmount > 0) {
        var calculatedYields = calculateYields(newAmount, (pool.apyBase || 0) + (pool.apyReward || 0));
        Analytics.trackYieldCalculation(newAmount, pool, {
          calculatedYields,
          trigger: 'manual_input'
        });
      }
    },
    min: '0',
    step: '100'
  })),
  // Quick Amount Buttons
  React.createElement('div', {
    className: 'calc-denominations'
  }, [100, 500, 1000, 2000, 5000, 10000, 100000].map(amount => React.createElement('button', {
    key: amount,
    className: `quick-amount-btn${investmentAmount === amount ? ' active' : ''}`,
    onClick: () => {
      setInvestmentAmount(amount);
      // Analytics tracking for preset amount selection
      if (typeof Analytics !== 'undefined') {
        var calculatedYields = calculateYields(amount, (pool.apyBase || 0) + (pool.apyReward || 0));
        Analytics.trackYieldCalculation(amount, pool, {
          calculatedYields,
          trigger: 'preset_button'
        });
      }
    }
  }, `$${amount >= 1000 ? `${amount / 1000}k` : amount}`)))),
  // Long Game headline (210 B2b) — the former standalone
  // pool-projection-card, INPUT-FIRST inside the earnings block: it
  // recomputes live from investmentAmount above, so the user sets
  // their number before reading this result. 225 round 3c: the gray
  // box is gone — the projection reads as the section's own prose
  // (classes/conditions/copy unchanged). Its own .calc-disclaimer is
  // dropped here — the block keeps exactly ONE disclaimer, near the
  // calculator readout below (210 B2d).
  React.createElement('div', {
    className: 'metric-card-simple pool-projection-card'
  }, React.createElement('div', {
    className: 'pool-projection-label'
  }, t ? t('projectionHeading') : 'The Long Game'), React.createElement('div', {
    className: 'pool-projection-body'
  }, isAnomalous
  // 165: an out-of-rail totalApy compounds into a fictional dollar figure
  // (e.g. $49 quintillion) — never derive $ from it. Honest replacement
  // line, no numbers to rail. Hero APY keeps rendering the raw rate
  // elsewhere (demote + flag convention) — this gate is display-only,
  // on this node.
  ? t ? t('projectionBodyOutOfRange') : 'This rate is too far outside normal ranges to project a dollar amount from — the number would be fiction, not a forecast.'
  // 241: projectionAmount pre-rounded to a whole dollar before t() —
  // the translations.js entry no longer applies its own
  // maximumFractionDigits:0 (the accessor chokepoint pre-formats
  // numeric args via the shared formatCount, which has no rounding
  // option), so the caller must round first. Matches the
  // Math.round() already used in the no-t fallback string below.
  : t ? t('projectionBody', investmentAmount, PROJECTION_YEARS, Math.round(projectionAmount)) : `$${Number(investmentAmount || 0).toLocaleString('en-US')} in this pool grows to ~${_formatUsd(projectionAmount, 0)} in ${PROJECTION_YEARS}y at current rates.`),
  // Yield-funded thesis line (129): the deposit stays the user's — you keep
  // your money AND it keeps working. Honest framing, no numbers to rail.
  React.createElement('div', {
    className: 'pool-projection-note'
  }, t ? t('projectionKeepNote') : 'Your deposit stays yours — you keep your money, and it keeps working.')
  // The degen-haircut warning and the isAnomalous warning that used to
  // render here moved OUT of this card (still verbatim class/copy) —
  // see the trust-rail fix below the collapsible content. They must
  // survive .calculatorExpanded being false, so they cannot live
  // inside this node (which is itself inside the calculatorExpanded
  // && guard).
  ),
  // Coupon tabs (247 world) — 1 Day / 7 Days / 30 Days with perforated
  // separators; selection state owned by CSS classes. EN labels are the
  // test-pinned strings; behavior unchanged.
  React.createElement('div', {
    className: 'calc-tabs'
  }, ['1day', '7days', '30days'].map(tab => {
    var tabLabels = {
      '1day': '1 Day',
      '7days': '7 Days',
      '30days': '30 Days'
    };
    return React.createElement('button', {
      key: tab,
      className: `calc-tab${activeCalculatorTab === tab ? ' active' : ''}`,
      onClick: () => setActiveCalculatorTab(tab)
    }, tabLabels[tab]);
  })),
  // Primary Yield Result (based on selected tab) — 225 round 3c: a
  // plain composed readout, not another gray well; the amount is the
  // section's one strong number (text color, tabular — gradient text
  // is banned).
  React.createElement('div', {
    className: `calc-readout${totalApy === 0 ? ' is-zero' : ''}`
  }, React.createElement('div', {
    className: 'calc-readout-label',
    onMouseEnter: e => {
      var tooltip = document.createElement('div');
      tooltip.textContent = 'Calculations are estimates. Actual yields may vary based on market conditions.';
      tooltip.style.cssText = `
                position: absolute;
                bottom: 100%;
                left: 50%;
                transform: translateX(-50%);
                background: var(--cert-ink);
                color: var(--cert-paper);
                padding: 8px 12px;
                border-radius: 4px;
                font-size: 12px;
                font-family: var(--cert-sans);
                text-transform: none;
                letter-spacing: normal;
                white-space: nowrap;
                z-index: 1000;
                margin-bottom: 5px;
                box-shadow: var(--ui-shadow-overlay);
              `;
      tooltip.id = 'earnings-tooltip';
      e.target.appendChild(tooltip);
    },
    onMouseLeave: e => {
      var tooltip = document.getElementById('earnings-tooltip');
      if (tooltip) tooltip.remove();
    }
  }, activeCalculatorTab === '1day' ? t ? t('estimatedDailyEarnings') : 'Estimated Daily Earnings' : activeCalculatorTab === '7days' ? t ? t('estimatedEarnings') : 'Estimated Weekly Earnings' : t ? t('estimatedMonthlyEarnings') : 'Estimated Monthly Earnings'), React.createElement('div', {
    className: `calc-readout-amount${isPulsing ? ' yield-pulse-active' : ''}`
  }, isAnomalous ? '—' : activeCalculatorTab === '1day' ? _formatUsd(investmentAmount * totalApy / 365 / 100) : activeCalculatorTab === '7days' ? _formatUsd(investmentAmount * totalApy / 52 / 100) : _formatUsd(investmentAmount * totalApy / 12 / 100)), React.createElement('div', {
    className: 'calc-readout-basis'
  }, t ? t('basedOnInvestment', investmentAmount) : `Based on $${investmentAmount.toLocaleString('en-US')} investment`)
  // The ONE .calc-disclaimer for the whole earnings block moved OUT of
  // this node — see the trust-rail fix below the collapsible content
  // (it must survive calculatorExpanded===false). The isAnomalous
  // .calc-warning that used to duplicate here (210 B2e) stays removed
  // — the single anomaly warning below covers this block.
  )),
  // Trust-rail fix (post-210 verifier round): the degen-haircut warning,
  // the anomalous-pool warning, and the single .calc-disclaimer MUST
  // render regardless of whether the user has collapsed the calculator
  // — collapsing must never silently drop a trust-rail disclosure or
  // leave the moved repeat CTA's concrete "~$X in 5y" projection label
  // undisclaimed. So these three are siblings of the calculatorExpanded
  // && block above (not nested inside it), placed AFTER the collapsible
  // content and BEFORE the repeat CTA. Same classes/copy as before
  // (verbatim) — only their position in the tree moved from "inside the
  // guard" to "always". In the default expanded state this renders in
  // the exact same visual order as before (right after the toggle+
  // readout, right before the repeat CTA).
  applyDegenHaircut && React.createElement('div', {
    className: 'calc-warning'
  }, t ? t('poolDegenHaircutNote', _formatApy(totalApy)) : `Projected at ⅓ haircut (${_formatApy(totalApy)} headline) — farm rates decay. Active management required.`), isAnomalous && React.createElement('div', {
    className: 'calc-warning'
  }, t ? t('calcAnomalyWarning') : '⚠ This rate is anomalous and almost certainly unsustainable.'), React.createElement('div', {
    className: 'calc-disclaimer'
  }, t ? t('calcDisclaimer') : 'Estimates based on current rates — yields change constantly. Not financial advice.'),
  // Repeat CTA (210 B3), reduced to a slim contextual TEXT-LEVEL echo
  // (spec 237 — exactly ONE primary-weight composition per page; this is
  // the earnings block's own closing action, not a second hero). Prior
  // rounds (225 round 3c) kept the echo on the `.cta-button-primary`
  // class, which computes to the SAME 54px filled button as the hero —
  // audit C3 caught the contract as still unmet on the render. Now: no
  // `.cta-button-primary`, no `.cta-button-protocol` — a single
  // `.cta-echo-link` (transparent background, underlined text, no fixed
  // height) carries the concrete showConcreteCta projection label to the
  // SAME planner destination as the hero, with `pool_click{source=
  // garden_cta}` unchanged and `ctaPlacement: 'earnings_block'` kept
  // verbatim for report continuity; `ctaPosition: 'calculator'` is new
  // (237's added segmentation — see analytics.js). "Start Earning on
  // <protocol>" is dropped here entirely — it renders once, at the hero.
  React.createElement('div', {
    className: 'pool-action-card pool-cta-echo'
  }, React.createElement('a', {
    className: 'cta-echo-link',
    href: gardenThisPoolHref,
    onClick: () => {
      if (typeof Analytics !== 'undefined') {
        Analytics.trackPoolClick(pool, 'garden_cta', {
          investmentAmount: Math.round(investmentAmount),
          projectionYears: PROJECTION_YEARS,
          ctaVariant: showConcreteCta ? 'concrete' : 'generic',
          ctaPlacement: 'earnings_block',
          ctaPosition: 'calculator'
        });
      }
    }
  }, showConcreteCta
  // 241: pre-rounded before t() — see projectionBody's call above for why.
  ? t ? t('gardenThisPoolCtaConcrete', Math.round(projectionAmount), PROJECTION_YEARS) : `Garden this pool → ~$${Math.round(projectionAmount).toLocaleString('en-US')} in ${PROJECTION_YEARS}y` : t ? t('gardenThisPoolCta') : 'Garden this pool →'))),
  // Engraved rule between the document's clauses (247 world). Decorative
  // only: aria-hidden, no text, and the ornament primitives are
  // pointer-events: none, so this can never sit between a user and a
  // control.
  React.createElement('div', {
    className: 'cert-divider',
    'aria-hidden': 'true'
  }, React.createElement('span', {
    className: 'orn-band cert-divider-strand'
  }), React.createElement('span', {
    className: 'cert-divider-node'
  }), React.createElement('span', {
    className: 'orn-band cert-divider-strand'
  })),
  // Collapsible Pool Information — 225 round 3c: reference-weight ledger,
  // not a grid of equal gray boxes.
  React.createElement('div', {
    className: `pool-info-section animate-on-mount ${poolInfoExpanded ? 'expanded' : ''}`
  },
  // Pool Info Header — 247 world: geometry owned by CSS; drawn chevron.
  React.createElement('div', {
    className: 'pool-info-header',
    onClick: () => setPoolInfoExpanded(!poolInfoExpanded)
  }, React.createElement('div', {
    className: 'pool-info-title-row'
  }, React.createElement('h3', {
    className: 'pool-section-title',
    style: {
      margin: 0
    }
  }, t ? t('poolInformation') : 'Pool Information'), protocolUrl && React.createElement('a', {
    className: 'pool-info-protocol-link',
    href: protocolUrl,
    target: '_blank',
    rel: 'noopener noreferrer',
    onClick: e => e.stopPropagation()
  }, t ? t('protocol') : 'Protocol↗')), React.createElement('div', {
    className: 'pool-info-toggle'
  }, renderChevronIcon())),
  // Expanded Pool Information Content
  poolInfoExpanded && React.createElement('div', {
    className: 'pool-info-content',
    style: {
      animation: 'fadeIn 0.3s ease'
    }
  },
  // Rate-momentum honesty note (103) — full-width, calm. Reuses 071's
  // exact neuro styling. Surfaces 087's kpis.apyMomentum (last − first
  // total APY over the tracked window) as calm cautious-saver language:
  // rising AND falling rates said out loud (degen-honesty precedent).
  // Yields entirely to the 071 volatility note (same divergence boolean)
  // so the two are mutually exclusive, renders nothing when kpis/momentum
  // are missing (live SEO deep-link landings), needs a ≥7-day window, and
  // stays silent below a meaningful move (|momentum| < 0.5, 088.1 covers).
  // Promoted (210 C3) above the tile grid — prose insight outranks
  // reference data.
  !(mean30dSane && (pool.apyBase || 0) + (pool.apyReward || 0) > 0 && pool.apyMean30d > 0 && Math.max((pool.apyBase || 0) + (pool.apyReward || 0), pool.apyMean30d) / Math.min((pool.apyBase || 0) + (pool.apyReward || 0), pool.apyMean30d) >= 1.5) && pool.kpis && typeof pool.kpis === 'object' && typeof pool.kpis.apyMomentum === 'number' && Number(pool.kpis.historyPoints) >= 7 && Math.abs(pool.kpis.apyMomentum) >= 0.5 && React.createElement('div', {
    className: 'rate-momentum-note pool-info-note'
  }, function () {
    var mom = pool.kpis.apyMomentum;
    var hp = Number(pool.kpis.historyPoints);
    if (mom >= 0.5) {
      return t ? t('rateMomentumRising', _formatApy(Math.abs(mom)), hp) : `This pool's rate has climbed about ${_formatApy(Math.abs(mom))} over the ${hp} days we've tracked it. Rates that rose can slip back just as easily — this page projects on today's rate, not the climb.`;
    }
    return t ? t('rateMomentumFalling', _formatApy(Math.abs(mom)), hp) : `This pool's rate has eased down about ${_formatApy(Math.abs(mom))} over the ${hp} days we've tracked it. Falling rates are normal once reward emissions taper — worth knowing before you plan a garden around today's number.`;
  }()),
  // TVL-trend honesty note (104) — full-width, calm. Reuses 071's exact
  // neuro styling. Surfaces 087's kpis.tvlTrend (signed fraction of the
  // deposit-base change over the tracked window) as calm cautious-saver
  // language: a shrinking pool that still clears the $10M floor is the
  // ICP-relevant risk; a growing pool is one honest sign, never a
  // guarantee. Yields entirely to the 071 volatility note (same
  // divergence boolean) so a volatile pool shows exactly one note,
  // renders nothing when kpis/tvlTrend are missing (live SEO deep-link
  // landings), needs a ≥7-point window, and stays silent below a
  // meaningful move (|tvlTrend| < 0.25). Promoted (210 C3) above the
  // tile grid — prose insight outranks reference data.
  !(mean30dSane && (pool.apyBase || 0) + (pool.apyReward || 0) > 0 && pool.apyMean30d > 0 && Math.max((pool.apyBase || 0) + (pool.apyReward || 0), pool.apyMean30d) / Math.min((pool.apyBase || 0) + (pool.apyReward || 0), pool.apyMean30d) >= 1.5) && pool.kpis && typeof pool.kpis === 'object' && typeof pool.kpis.tvlTrend === 'number' && Number(pool.kpis.historyPoints) >= 7 && Math.abs(pool.kpis.tvlTrend) >= 0.25 && React.createElement('div', {
    className: 'tvl-trend-note pool-info-note'
  }, function () {
    var tvl = pool.kpis.tvlTrend;
    var hp = Number(pool.kpis.historyPoints);
    var pctStr = _formatNum(Math.round(Math.abs(tvl) * 100)) + '%';
    if (tvl <= -0.25) {
      return t ? t('tvlTrendShrinking', pctStr, hp) : "This pool's deposits have shrunk about " + pctStr + " over the " + hp + " days we've tracked it. A pool can keep clearing our $10M size floor while quietly losing deposits — worth watching for a garden you plan to hold for years.";
    }
    return t ? t('tvlTrendGrowing', pctStr, hp) : "This pool's deposits have grown about " + pctStr + " over the " + hp + " days we've tracked it. More deposits isn't a guarantee, but a pool that's holding or gaining size is one honest sign of staying power.";
  }()),
  // The register (247 world) — ledger rows with dot leaders. Label and
  // value are DIVs (childless label divs are the rendered contract
  // test_mean30d_sanity.js reads; #393's span markup broke that pin —
  // this restores the original intent). Same facts, same conditions,
  // same t() keys and formatters.
  React.createElement('div', {
    className: 'pool-facts'
  },
  // TVL row (210 C2) — same t('tvl') key and formatCurrency helper
  // the hero's tvl-badge uses.
  React.createElement('div', {
    className: 'pool-fact-row'
  }, React.createElement('div', {
    className: 'pool-fact-label'
  }, t ? t('tvl') : 'TVL'), React.createElement('div', {
    className: 'pool-fact-value'
  }, formatCurrency(pool.tvlUsd))),
  // 30d Mean APY (if available) — substantiates whether today's rate is stable or a spike
  mean30dSane && React.createElement('div', {
    className: 'pool-fact-row'
  }, React.createElement('div', {
    className: 'pool-fact-label'
  }, t ? t('apyMean30d') : '30d Mean APY'), React.createElement('div', {
    className: 'pool-fact-value'
  }, _formatApy(pool.apyMean30d))),
  // Exposure (if available)
  pool.exposure && React.createElement('div', {
    className: 'pool-fact-row'
  }, React.createElement('div', {
    className: 'pool-fact-label'
  }, t ? t('exposure') : 'Exposure'), React.createElement('div', {
    className: 'pool-fact-value',
    style: {
      textTransform: 'capitalize'
    }
  }, pool.exposure)),
  // IL Risk (if available) — flagged in warning color when present, never hidden
  pool.ilRisk && React.createElement('div', {
    className: 'pool-fact-row'
  }, React.createElement('div', {
    className: 'pool-fact-label'
  }, t ? t('ilRisk') : 'IL Risk'), React.createElement('div', {
    className: 'pool-fact-value',
    style: {
      color: pool.ilRisk === 'yes' ? 'var(--color-warning)' : undefined
    }
  }, pool.ilRisk === 'yes' ? t ? t('yes') : 'Yes' : t ? t('no') : 'No'))),
  // Tokens Section (if available) — 247 world: the register's schedule.
  // Chip styling owned by .pool-token-chip (the 246-flagged mono-caps
  // remnant is retired with the inline styles); classifier behavior,
  // link targets and truncation rules unchanged.
  pool.underlyingTokens && pool.underlyingTokens.length > 0 && React.createElement('div', {
    className: 'pool-tokens-section'
  }, React.createElement('div', {
    className: 'pool-tokens-label'
  }, t ? t('underlyingAssets') : 'Underlying Assets'), React.createElement('div', {
    className: 'pool-tokens-list'
  }, pool.underlyingTokens.map((token, idx) => {
    var classified = classifyUnderlyingToken(token, pool.chain);
    if (classified.chip) {
      var address = classified.address;

      // Derive display symbol from pool.symbol split on '-' or '/'.
      // addressCount now counts every chip-classified token (not
      // just EVM), so this stays a no-op for EVM-only pools (the
      // count is identical to the old EVM-only filter there).
      var symbolParts = pool.symbol ? pool.symbol.split(/[-\/]/).map(s => s.trim()) : [];
      var addressCount = pool.underlyingTokens.filter(t => classifyUnderlyingToken(t, pool.chain).chip).length;
      var truncated = `${address.slice(0, 6)}...${address.slice(-4)}`;
      var displayText = symbolParts.length === addressCount && symbolParts[idx] ? symbolParts[idx] : truncated;
      if (classified.href) {
        return React.createElement('a', {
          key: idx,
          className: 'pool-token-chip',
          href: classified.href,
          target: '_blank',
          rel: 'noopener noreferrer',
          title: token
        }, displayText + ' ↗');
      }

      // Rule 5 (opaque non-EVM id, e.g. Stellar/Stacks): chip for
      // the truncation treatment, but never a link — we don't know
      // which explorer (if any) resolves it, and blockscan.com is
      // EVM-only, so a guessed link is a guaranteed 404.
      return React.createElement('span', {
        key: idx,
        className: 'pool-token-chip',
        title: token
      }, displayText);
    }

    // Rule 6: not address-shaped (e.g. "coingecko:openeden-tbill")
    // — a short readable slug, not an address. Plain chip, full text.
    return React.createElement('span', {
      key: idx,
      className: 'pool-token-chip'
    }, token);
  }))))));
}

// Simple fade-in animation for calculator
var fadeInStyles = `
@keyframes fadeIn {
  from { opacity: 0; transform: translateY(10px); }
  to { opacity: 1; transform: translateY(0); }
}
`;

// Inject animation styles
if (typeof document !== 'undefined') {
  var styleSheet = document.createElement('style');
  styleSheet.textContent = fadeInStyles;
  document.head.appendChild(styleSheet);
}

// Attach pure calculation helpers and widget for external testing and reuse
PoolDetail.calculateMonthlyYield = calculateMonthlyYield;
PoolDetail.calculateRequiredCapital = calculateRequiredCapital;
PoolDetail.isRungCovered = isRungCovered;
PoolDetail.getYieldCardCatalog = getYieldCardCatalog;
PoolDetail.serializeWaitlistPayload = serializeWaitlistPayload;
PoolDetail.YieldCardWidget = YieldCardWidget;

// Export for use in main app
if (typeof module !== 'undefined' && module.exports) {
  module.exports = PoolDetail;
}
